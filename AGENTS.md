# EMERGENCY RAISA/ARIADNE RECOVERY PRECEDENCE
Gate G1E is active only for read-only configuration and installed controller assessment
Missing, malformed, stale, or contradictory programme state is a hard stop.

# EMR4 / Raisa — Recovery and Development Instructions

Date: 2026-09-12
Timestamp: 2026-09-12T11:23:24.602402+10:00

## 1. Project and durable objective

Restore a trustworthy development foundation, then consolidate the earlier Ariadne-Graph-Compass-Clockwork harness work and resume Raisa architecture and feature development. Recovery work must advance that objective through working, integrated changes.

Raisa is a projection-native medical office: one tenant-scoped command/event truth, rendered through Word, the Diary and other typed surfaces. AI proposes; deterministic policy and authorised people decide. FastAPI/PostgreSQL owns clinical and diary truth. A stale projection cannot authorise a command. Preserve explicit identity, confirmation, provenance, auditability, concurrency controls and an accessible non-AI path with the same semantics.

The controlling programme is `docs/programme/raisa-ariadne-recovery-programme.md`; the wider product blueprint is `implementation_plan.md`. Earlier architecture and experimental branches are prior art to assess at the appropriate gate, not automatic merge candidates.

## 2. Mandatory Rehydration

Read this file, the relevant structured state/gate, the active plan and the latest applicable checkpoint. Resolve the current work from its exact commit, candidate and evidence. Read only the ordinary paths needed for the next action.

These five existing source identities remain the continuity vocabulary:

| Source identity | Current source |
|---|---|
| `live_handover_current_baton` | Section 3 and its latest verified checkpoint |
| `current_authority_allocation` | Sections 4 and 6, current owner instructions and applicable session constraints |
| `active_plan_and_acceptance` | Structured programme state, gates, active admission contract and execution plan |
| `protected_evidence_boundaries` | Section 5 and any stricter active boundary |
| `git_refs_and_worktree` | Fresh observations of the exact relevant HEAD, tree, index and named refs |

Reuse evidence when its source, runtime and relevant inputs remain unchanged. Repeat a check when its result could have become stale or an enforced gate requires it. Do not create duplicate receipts merely because context was compacted or a side question was answered. Honour actual machine-enforced admission requirements; this file cannot substitute for their evidence.

Structured `orchestration/programme/current-state.json`, `orchestration/programme/gates.yaml` and the active admission policy outrank narrative descriptions of phase or eligibility. Missing, malformed or contradictory authoritative state fails closed. Historical fields and consumed latches must be interpreted with their accepted transition and publication receipts, never silently rewritten to fit a test.

## 3. Current Baton

| Item | Current position |
|---|---|
| Mode | Recovery; one active implementation batch |
| Local machine and repository | `DESKTOP-RLTV4BS`, `C:/Users/there/EMR4` |
| Recovery publication destination | `refs/heads/codex/raisa-ariadne-recovery-g0` at `https://github.com/yurifrusin/emr4` |
| Verified preflight repair source | `07e65e4e49cc3e7f954f33030ed6d64fd050adaf`, published with exact parent/tree and remote readback. Obtain a fresh exact HEAD observation before acting. |
| Last verified remote recovery source | `3e0204b7999ff53fd4d5785d2ea831b3a58da4ca` before this current-control repair; obtain fresh readback. |
| Local ref arrangement | Detached HEAD; the five named recovery/protected local refs were absent at migration and preflight publication. Preserve that arrangement unless an admitted operation explicitly changes it. |
| Protected remote refs | `master` and `handoff/current`: `2e34bdad732fdab32fbf778280b3d3c70d66d602` |
| Active programme gate | G1E configuration/core assessment; the bounded G1D provenance component is accepted. The installed controller is eligible for read-only assessment; operational multi-task control and G2 remain unaccepted. |
| Published repairs | Cumulative-history admission correction `d512399aa52fdc1d43b68e3e5117d3142037671d`; journal `723e42fc70841b0e8ceed7fd5a2f63e0eecd573e`; historical state-test repair `f127ec9e14b5ac1966e50cfba1d1285527a2e759`. |
| Active execution plan | `docs/programme/recovery-execution-plan-20260911.md` |
| Local evidence | `C:/Users/there/EMR4-migration/20260911-v1`; current continuation checkpoint: `current-control-repair-20260912-v1/coordinator-checkpoint.json` beneath that root |
| Next dependency | Verify the installed configuration/core component using orchestration/programme/g1e-configuration-core-scope.json; its read-only assessment explicitly supersedes the preserved G1D operation. |

The original remote-machine checkout, worktrees and historical evidence remain preserved. Do not replay a consumed source-machine publication operator. Do not assume an old branch label, timestamp or narrative baton is current authority.

The historical full-suite result is 430 passed, 33 failed, 1 skipped. It is not a current defect count or a current full-suite result. The preflight repair passed nine selected cases using actual controller decisions with authored policy/Git observations. That does not establish whole-loader, transition-builder or full-suite acceptance. The failed push and its authentication diagnosis are preserved. After owner sign-in, a separately reviewed push-only continuation published the existing commit; no commit operation was repeated.

## 4. Authority Allocation

The recovery coordinator owns task selection, coupled implementation, evidence interpretation and continued progress. Use an independent reviewer for substantive controller/admission changes and the required publication review. A worker cannot independently accept its own implementation or acquire protected integration authority.

Keep the coordinator at Yuri's selected maximum reasoning effort for this recovery and its architecture decisions. Subagents inherit the coordinator's model and effort unless an explicit task allocation and current session rules permit a different setting. Match any permitted cheaper allocation to genuinely mechanical, bounded work; escalate when uncertainty changes correctness, scope or acceptance. Do not claim to change a UI setting through prose.

Use another agent only for a concrete separable contribution or required independence when it saves a meaningful cycle. Keep shared-state and tightly coupled work serial. Do not create mandatory parallelism paperwork or dispatch workers merely to fill lanes. External model/provider lanes remain closed; archived Sol, DeepSeek and Gemini allocations do not reopen them.

## 5. Protected Evidence and Closed Gates

### Protected holdouts v1-v10

Protected holdouts v1, v2, and v3 remain sealed. Protected holdouts v1-v10 share the same no-access boundary. Do not open, enumerate, list, search, import, run, regenerate, evaluate, hash-check, infer labels from, or tune against any protected fixture, support module, authoring surface, manifest, seal, receipt or per-case report. Historical metadata-enumeration incidents authorise no reuse.

Only the previously permitted committed aggregate report/closeout for v2 and aggregate report/closeout/acceptance for v3-v10 may inform planning. V10 is consumed and sealed. Certification ended the standing fresh-version cycle; no V11 is authorised. No synthetic v3 or frozen-v2 refinement is authorised. Use ordinary development or newly authored synthetic evidence within the active scope.

### Recovery effects and data

- Protected-ref movement, merge/integration, deployment, release and GitHub Pages are closed without their specific applicable authority.
- Real patient/clinical data, live external model/identity/clinical providers and product runtime effects are closed during this recovery work. Do not start application, database or browser runtimes merely to satisfy a historical test fixture.
- T3.1-T3.4 remain intact and blocked by default. Historical T3/provider exceptions and occupied ledgers are consumed; no T3R7 retry or silent transport/model fallback.
- Keep raw historical diary material local and ignored under `local_data/historical-diary-trove/`. No broad reading, external transmission, runtime retrieval, training or commitment is authorised by recovery.
- Never expose credentials or secrets in commands, logs or packets. No credential/IAM/cloud-policy mutation authority is inferred from development authority.
- Preserve unrelated user files, branding material, worktrees, branches, stashes and evidence. No destructive cleanup or history rewrite to obtain a clean workspace.

Use literal ordinary-file allowlists and the accepted opaque-index/selected-path mechanisms while protected observation is quarantined. No repository-wide inventory, broad Git status/diff, whole-policy loader, repository conftest or full pytest invocation that would cross that boundary. A transfer-only exception does not authorise subsequent inspection.

Product history remains bounded by its accepted plans: AI is advisory, the backend owns identity/confirmation/writes/audit, real Microsoft/clinical connections remain closed, and prior synthetic or provider runs grant no continuing runtime authority. `Raisa` and `Clinician One` remain candidate names; branding is not public rename or release authority.

## 6. User Decision Boundaries

Yuri reaffirmed standing authority on 2026-09-11: continue after publication until an actually material, rather than procedural, decision requires him. This covers ordinary in-scope planning, implementation, tests, review, recovery, documentation, task-branch publication and the next dependency-satisfied recovery task.

Derive the exact scope and effect boundary from the accepted programme and current instructions. Missing descendant paperwork, a routine failure, a commit, a receipt or a completed checkpoint is not a reason to ask for repeat permission. A new plan cannot erase an explicit closure or change acceptance merely to obtain a pass.

Refer only a genuinely non-inferable choice between materially different user-owned outcomes, a human-only action, an unresolved acceptance/authority conflict, exhausted bounded recovery, a proposed material risk acceptance, protected-evidence access, protected integration, new provider/real-data access or work outside the authorised sequence. Explain the concrete decision and consequences after completing the useful work already authorised.

Side questions and status requests do not cancel active work. Answer briefly and continue. At an accepted checkpoint, record the result and immediately start the next justified task unless a material decision, explicit pause or actual technical blocker prevents it.

## 7. Work, Verification and Publication

Define one reviewable batch by its shared cause: exact base and owned files, intended invariant, forbidden effects, meaningful verification, publication authority and recovery route. Reuse accepted mechanisms. A separate proof is useful only if it resolves a remaining uncertainty that integration cannot yet test safely.

Use historical failures to identify relevant present controls. A precise past cause may remain unknown once focused current evidence establishes the control we need. Preserve the old result and uncertainty; do not label it a current defect or require incident reconstruction solely because it once failed. For a control needing repair, retain its intended safety check, establish a valid baseline, require the targeted rejection reason, and record residual coverage. Fix fixtures when they encode the wrong historical phase; change production behaviour only when evidence demonstrates a defect. Never weaken assertions, suppress a required failure or alter historical authority records to make tests green.

Focused tests prove their exercised boundary. Synthetic observations, substituted state loading, real Git mechanics and full runtime integration must be labelled accurately. Do not add supporting proof case counts to the number of original failures repaired. Required red checks and repair budgets still apply.

Publication requires the exact reviewed candidate, explicit permitted destination, current lease, complete scope verification and post-effect readback. Retain separate commit, push and acceptance facts. If a response is lost, inspect the existing attempt and remote result; never retry blindly. Preserve failed attempts and uncertainty honestly. Do not normalise local refs or move protected refs as a handoff convenience.

Use one concise durable checkpoint at a meaningful result, linking existing evidence and the next action. Keep the existing failure matrix as the disposition record. Do not build another ledger, launcher or reporting layer without a concrete unmet requirement. Mechanical gates that remain in force are not waived by this economy rule.

## 8. Product and Environment Orientation

Recovery Python: `C:/Users/there/EMR4-tools/Python314/python.exe`.
Focused test environment: `C:/Users/there/EMR4-tools/recovery-venv/Scripts/python.exe`.
Git: `C:/Program Files/Git/cmd/git.exe`.
Bind executable identities to the operation when its accepted contract requires it; do not reuse an old machine's runtime claim.

When product gates reopen, Word Online remains the Office target and the native browser Diary the scheduling surface. Use API Spine contracts under `docs/api-spine/` for command/read-model, proposal/confirmation, audit, security and idempotency seams. Application/PostgreSQL tests sharing one schema run serially. Clinical authority and tenant/concurrency guarantees stay server-owned.

## 9. Historical and Topic Index

The complete predecessor to this reconciliation is preserved byte-for-byte at `docs/handover-archive/AGENTS-2026-09-11-pre-reconciliation.md`, SHA-256 `7f3f2b7eea22d347bbf4212a3ea0ba8c5a1d93194f6622fd6a2e6aa4fc9c0729`. Its former current baton, provider allocations, source-machine paths and broad handoff commands are historical evidence.

The older immutable snapshot and manifest remain unchanged:

- `docs/handover-archive/AGENTS-2026-07-15-pre-compaction.md`
- `docs/handover-archive/AGENTS-2026-07-15-pre-compaction.manifest.json`
- SHA-256 `ad86887db6b640bdeac40111aa9f83c9e422f4ccab5f2eb61334a49449126b4c`; Git blob `ace44b93507737141a5e44004c24a087755561af`; source commit `6801fb214d41c41c14f94b90642f6a7d9ee0a6d6`.

Topic navigation:

- `docs/handover-ledgers/README.md`
- `docs/handover-ledgers/current-baton-acceptance-index.md`
- `docs/handover-ledgers/bernie-language-evaluation.md`
- `docs/handover-ledgers/orchestration-and-agent-runtime.md`
- `docs/handover-ledgers/historical-diary-and-interpretation.md`
- `docs/handover-ledgers/product-platform-api-and-security.md`

Consult history only for a concrete dependency and within its evidence boundary. Archived instructions cannot override current owner instructions or structured programme authority.

## 10. Maintaining This File

Update durable direction, authority, environment and next dependencies here; put chronology and test detail in the existing plan, matrix and checkpoint. Preserve superseded instructions and immutable evidence. A documentation refresh does not activate a gate, accept a candidate, move protected refs or authorise an external notification.
