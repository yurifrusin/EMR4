{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "operation_id", "execution_attempt_id", "candidate_source", "runner_sha256", "effective_tool_guard_sha256", "preset_sha256", "fixed_identity_sha256", "target_path_sha256", "result", "last_admitted_stage", "error_class", "safe_guard_coordinate", "safe_guard_detail", "preset_mount_terminal", "preset_mounted", "model_selection_installed", "veto_exact", "veto_rejected", "agent_create_invocation_count", "private_agent_preparation_count", "private_session_preparation_count", "live_agent_count", "live_session_count", "session_created_event_count", "agent_created_event_count", "agent_session_start_event_count", "turn_count", "request_count", "broker_process_count", "broker_request_count", "occupied_worker_count", "model_request_count", "provider_request_count", "database_invocation_count", "docker_invocation_count", "target_created", "target_used", "raw_error_retained"],
  "properties": {
    "schema_version": {"const": "ariadne.native_harness_preset_composition_safe_terminal_sidecar.v1"},
    "operation_id": {"const": "deepseek-native-harness-provider-free-preset-mount-sanitized-terminal-native-rehearsal"},
    "execution_attempt_id": {"const": "preset-mount-sanitized-terminal-native-attempt-001"},
    "candidate_source": {"$ref": "#/$defs/oid"},
    "runner_sha256": {"$ref": "#/$defs/digest"},
    "effective_tool_guard_sha256": {"$ref": "#/$defs/digest"},
    "preset_sha256": {"$ref": "#/$defs/digest"},
    "fixed_identity_sha256": {"$ref": "#/$defs/digest"},
    "target_path_sha256": {"$ref": "#/$defs/digest"},
    "result": {"enum": ["closed_subcoordinate_failure", "preset_mount_failure_attributed", "preset_composition_failure_attributed", "prepublication_veto_diagnosed"]},
    "last_admitted_stage": {"type": "string"},
    "error_class": {"type": ["string", "null"]},
    "safe_guard_coordinate": {"type": ["string", "null"]},
    "safe_guard_detail": {"type": ["string", "null"]},
    "preset_mount_terminal": {"type": ["object", "null"]},
    "preset_mounted": {"type": "boolean"},
    "model_selection_installed": {"type": "boolean"},
    "veto_exact": {"type": "boolean"},
    "veto_rejected": {"type": "boolean"},
    "agent_create_invocation_count": {"type": "integer", "minimum": 0},
    "private_agent_preparation_count": {"type": "integer", "minimum": 0},
    "private_session_preparation_count": {"type": "integer", "minimum": 0},
    "live_agent_count": {"type": "integer", "minimum": 0},
    "live_session_count": {"type": "integer", "minimum": 0},
    "session_created_event_count": {"type": "integer", "minimum": 0},
    "agent_created_event_count": {"type": "integer", "minimum": 0},
    "agent_session_start_event_count": {"type": "integer", "minimum": 0},
    "turn_count": {"type": "integer", "minimum": 0},
    "request_count": {"type": "integer", "minimum": 0},
    "broker_process_count": {"type": "integer", "minimum": 0},
    "broker_request_count": {"type": "integer", "minimum": 0},
    "occupied_worker_count": {"type": "integer", "minimum": 0},
    "model_request_count": {"type": "integer", "minimum": 0},
    "provider_request_count": {"type": "integer", "minimum": 0},
    "database_invocation_count": {"type": "integer", "minimum": 0},
    "docker_invocation_count": {"type": "integer", "minimum": 0},
    "target_created": {"type": "boolean"},
    "target_used": {"type": "boolean"},
    "raw_error_retained": {"const": false}
  },
  "$defs": {
    "oid": {"type": "string", "pattern": "^[0-9a-f]{40}$"},
    "digest": {"type": "string", "pattern": "^[0-9a-f]{64}$"}
  }
}
