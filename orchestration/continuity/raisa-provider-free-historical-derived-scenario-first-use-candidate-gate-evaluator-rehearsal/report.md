# Raisa provider-free historical-derived scenario first-use candidate gate evaluator rehearsal — report

Date: 2026-08-24

Timestamp: 2026-08-24T10:07:35.8928123+10:00 (Australia/Brisbane)

Result: `accepted_evaluator_first_use_still_closed`

Reviewed source: `abcd4206a363b0c565c070e0f2cb9c54d627b3b3`

## Conclusion

The first-use gate is now executable rather than documentary. Candidate input
is a strict Pydantic form with no prose field: event kinds and policy values are
closed literals, numbers are bounded, unknown keys are forbidden, the accepted
source is one exact full Git object ID, and the candidate digest and utility are
recomputed rather than trusted.

One authored-synthetic minimised candidate produced an exact, non-transitive
binding. Five hostile candidates blocked, two incomplete or broader candidates
required revision, and five invalid forms were rejected before evaluation.
Whole-day or near-lossless replay is unrepresentably broad for initial use and
always blocks.

## Authority meaning

The synthetic positive case does not open historical first use. The existing
contract excludes wholly authored-synthetic tests, the evaluator exposes no
writer, and every result keeps archive, provider, product, runtime, ordinary-
practice, publication and protected-ref authority false. No historical file,
ignored attempt output, provider or model was accessed.

The mechanism is ready to guard one actual minimised candidate. A later
materialiser must hold that candidate in memory, obtain an exact digest-bound
decision and possess separate write authority before a reusable fixture can
exist.

## Verification and workflow reading

Seven focused controls and all 222 existing historical-Diary controls pass: 229
unique controls across 25 files. Ruff, compileall, typed-schema, source-boundary
and Git-diff checks also pass.

The first preplanning receipt used descriptive event `pre_plan` instead of the
profile's exact `pre_sprint_planning` token. It rejected before planning and was
corrected from the executable event list. The first focused test also asserted
`operates on` where the frozen threat text says `accepts`; six behavioral tests
already passed, the literal was aligned and the next focused run passed all
seven. AER-1150 and AER-1151 preserve those two contained costs in register
revision 660. Neither caused archive, provider, implementation or acceptance
reruns.

## Next work

The live clockwork's existing historical mode correctly forbids fixtures and
caps measurement at an ignored local candidate. It therefore cannot yet
represent the exact gate-wrapped first use without weakening or bypassing its
closed vocabulary.

The next tranche adds one provider-free closed materialisation subgate to the
clockwork and proves it on authored-synthetic boundary forms only. It opens no
archive access or fixture write itself. Once that exact mode passes, the
following tranche may perform one local-only, no-retry attempt: hold a minimised
check-in-context scenario in memory, obtain exact digest-bound admission, and
write one local-test fixture or nothing.
