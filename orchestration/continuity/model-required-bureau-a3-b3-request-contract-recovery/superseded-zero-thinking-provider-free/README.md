# Superseded zero-thinking provider-free rehearsal

This directory preserves the completed zero-provider, zero-cost rehearsal that
used `thinkingBudget: 0`. It passed deterministic isolation and proofreading
but was superseded before any occupied call after Yuri clarified the durable
architecture preference for positive bounded model reasoning.

These artefacts are diagnostic history only. They do not authorise or describe
the current occupied request. The active recovery request uses a 1,024-token
thinking budget and a 2,048-token total output cap.
