{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "ariadne.deepseek_native_harness_effective_tool_native_boot_evidence.v1",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version", "operation_id", "planning_source", "attempt_id", "result",
    "failure_classification", "package", "source_contract", "launch", "composition",
    "lifecycle", "terminal", "provider_boundary", "cleanup"
  ],
  "properties": {
    "schema_version": {"const": "ariadne.deepseek_native_harness_effective_tool_native_boot_evidence.v1"},
    "operation_id": {"const": "deepseek-native-harness-provider-free-effective-tool-composition-native-boot-proof"},
    "planning_source": {"type": "string", "pattern": "^[0-9a-f]{40}$"},
    "attempt_id": {"const": "native-composition-attempt-001"},
    "result": {"enum": ["pass", "fail"]},
    "failure_classification": {"type": ["string", "null"]},
    "package": {"type": "object"},
    "source_contract": {"type": "object"},
    "launch": {"type": "object"},
    "composition": {"type": "object"},
    "lifecycle": {"type": "object"},
    "terminal": {"type": ["object", "null"]},
    "provider_boundary": {"type": "object"},
    "cleanup": {"type": "object"}
  }
}
