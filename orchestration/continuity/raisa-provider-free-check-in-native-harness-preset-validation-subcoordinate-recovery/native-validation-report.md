# Native Harness preset-validation subcoordinate report

- Result: `failed_closed`
- Native terminal coordinate: `PRESET_ROW_FOUND`
- Markers reached: `PRESET_ROW_DISCOVERY_ENTERED`
- Native processes / automatic retries: `1 / 0`
- Agents / turns: `0 / 0`
- Broker / model / provider / network requests: `0 / 0 / 0 / 0`
- Docker / database invocations: `0 / 0`
- Raw logs retained: `false`
- Process and disposable-root absence: `true / true`

The deterministic rc.7 source proof and direct package-only characterization
found one exact healthy `emr4-bounded-worker` row and bound its 158 bytes and
SHA-256 digest. The sole checkpoint-bound provider-disabled native Harness
process booted the no-agent runner and entered row discovery, but it did not
reach the exact-row-found marker. The sanitized boundary cannot distinguish a
failed `agentPresets.list()` call from a zero, duplicate or nonmatching native
row set, so no narrower causal claim is made.

This terminal proves a remaining difference between direct package discovery
and the native Harness service path. It does not prove native preset validation,
preset mount, agent creation, a DeepSeek request, coding performance, attempt
006, database behavior or product readiness. The process is consumed and may
not be retried.
