{
  "$id": "https://emr4.invalid/schemas/raisa-intent-shaping-single-use-ledger.v1.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "attempt_id": {
      "pattern": "^raisa-intent-shaping-",
      "type": "string"
    },
    "fallback_permitted": {
      "const": false
    },
    "lane": {
      "const": "rayleen_context_fabric_intent_shaping"
    },
    "ledger_id": {
      "pattern": "^ledger-raisa-intent-shaping-",
      "type": "string"
    },
    "maximum_provider_calls": {
      "enum": [
        0,
        1
      ]
    },
    "policy_id": {
      "const": "raisa-context-fabric-model-required-intent-shaping-sydney-v1"
    },
    "provider_calls_consumed": {
      "enum": [
        0,
        1
      ]
    },
    "reserved_cost_usd": {
      "enum": [
        0,
        0.25
      ]
    },
    "schema_version": {
      "const": "emr4.raisa_intent_shaping.single_use_ledger.v1"
    },
    "status": {
      "enum": [
        "open",
        "consumed"
      ]
    }
  },
  "required": [
    "schema_version",
    "ledger_id",
    "attempt_id",
    "lane",
    "policy_id",
    "status",
    "maximum_provider_calls",
    "provider_calls_consumed",
    "reserved_cost_usd",
    "fallback_permitted"
  ],
  "title": "Raisa intent-shaping single-use ledger",
  "type": "object"
}
