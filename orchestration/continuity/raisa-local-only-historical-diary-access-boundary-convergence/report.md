# Raisa local-only historical Diary access boundary convergence — report

Date: 2026-08-24

Timestamp: 2026-08-24T02:33:58+10:00 (Australia/Brisbane)

Result: `raisa_local_only_historical_diary_access_boundary_convergence_pass`

Reviewed source: `7fef03bb8dc0acf38772850793e1aac35e878502`

## Conclusion

The clockwork can now represent the useful privacy gate without opening a
general historical-data exception. It admits only the already-published legacy
full denial, a separated typed full denial, or the complete exact local Diary
probe. The probe is inseparable from the accepted contract digest and all ten
scope members.

Partial scopes, altered digests, missing product-data denial, contradictory
deny/allow modes, invented historical labels and unknown `allow_` labels all
fail before a generation can be published. Existing denial-only latches remain
valid.

## Verification

- Five new boundary-focused test functions exercise the three valid modes and
  hostile variants.
- All 172 tests in the complete clockwork closeout suite pass through the
  provider-free wrapper.
- Ruff, compileall and Git diff checks pass.
- The committed non-executable subgate contract hashes exactly to
  `e312d58d7743b9b4d79d8a947b765732eea142f47586e0bd1f4e738047802615`.
- No historical Diary file was opened, listed, searched, sampled, hashed or
  parsed, and no real path was bound.

One first evidence draft contained a manually mis-expanded planning commit.
Machine `git rev-parse` readback rejected it before staging or publication.
The first incident-bearing rehearsal then lacked its required revision reading
and stopped before verification. Register revision 654 records both contained
corrections.

## Next operation

Proceed to
`raisa-local-only-bounded-historical-diary-snapshot-measured-privacy-probe`.
After fresh rehydration it may freeze the exact local plan and bind one ignored
leaf root, one dense day and an at-most-80-file manifest before any content
read. The strongest result remains an ignored locally restricted research
candidate; fixture, provider, model, product and publication authority remain
closed.
