# Superseded provider-free selector rehearsal

This zero-call rehearsal passed its isolation and deterministic proofreader
path, but the inner release used the future occupied evidence label while the
run itself was provider-free. It released nothing to a product surface and
made no provider call. The artifacts are preserved here rather than reused.

The repaired descendant uses the mode-neutral inner release value
`proofreader_admitted_display_projection`; the outer tranche alone owns the
provider-free or occupied evidence label.
