# Raisa local-only historical Diary leading explicit time-token bounded measurement rehearsal — report

Date: 2026-08-24

Timestamp: 2026-08-24T09:01:37.5638009+10:00 (Australia/Brisbane)

Result: `locally_restricted_candidate`

Reviewed and empirical source: `a9e00638b960306766260d6df674c3489be58b86`

## Conclusion

The strict leading-time hypothesis is supported. The sole fresh run opened and
parsed all 80 bound snapshots without retry. It found 5,281 valid leading time
tokens across 34 distinct minutes, mapped 5,281 of 12,557 eligible structural
segments, recovered a 10-minute modal interval, preserved 199 stable records
and observed 502 adjacent changes. Every frozen utility threshold passed.

This is a useful private research candidate, not a reusable dataset. It proves
that the historical Diary can contribute realistic temporal structure to later
scenario work. It does not prove anonymity, justify a whole-day replay or
permit historical-derived content to enter a fixture, corpus, benchmark,
memory, product or provider.

## Containment and interpretation

Source-value leakage and provider/model calls were zero. The private manifest,
projection, Word control and count-only progress sidecars are absent. Only four
generic readings remain under the ignored local attempt root, and none contains
the raw-root literal, a document extension or a true source-emission flag. The
user's pre-existing Word process remains present with its original start time.

The final progress reading counted 17,764 gross extracted segments, while the
terminal utility denominator is 12,557. This is expected: progress is counted
before empty, date and time-only segments are filtered, whereas the terminal
denominator contains occupancy-eligible segments only. It is not a change in
the source slice or a second run.

The internal record and trajectory uniqueness diagnostics remain structural
attack proxies, not a mathematical probability of reconstructing a person's
identity. Their nontrivial rates support strict minimisation and rule out a
whole-day or near-lossless first artifact.

## Workflow reading

The complete pre-access regression gate found one stale assertion in an older
attempt-specific test. That test still treated the immediately previous ignored
root as the active root. The correction retired the mutable assertion from the
old test and kept active-root ownership in the new v3 test. The gate was rerun
once; no historical file had been opened and the historical content run was
not repeated. AER-1148 records this repository-origin lapse in register
revision 659. The first closeout rehearsal then rejected the descriptive stage
`verification`; the exact closed value is `deterministic_verification`. The
corrected draft then exposed the adjacent coined category
`closed_vocabulary_lapse`, whose registered coordinate is `operator_error`.
The third dry reading rejected custom archive-denial labels in the successor
boundary. Because the successor opens no archive access, they were replaced by
ordinary private-archive and source-derived-artifact denials without importing
the historical-access subgate vocabulary. Nothing was published. AER-1149
records the compound orchestrator lapse, its three dry rehearsals and the
subsequent direct-validator reading that restored the required exact
`no_historical_data_access` mode token. The corrected direct validation passed;
its diagnostic wrapper then assumed a nonexistent top-level result key and was
rerun once to report the admitted shape without changing any evidence. The
first admitted driver tick and its direct diagnostic tick then both exposed a
missing prerequisite: the incident rows referenced the human revision-659
reading before that file existed. The bounded reading is now part of the
artifact set; neither failed tick published or staged anything.

All 222 controls across 24 historical-Diary test files pass, together with
Ruff, compileall, PowerShell parsing, JSON validation and source-boundary
checks.

## First use and next work

The existing first-use contract is now useful because a genuine candidate
exists. It remains closed. The next tranche will implement a typed,
provider-free, candidate-specific evaluator that operates on an in-memory
proposed derivative before any reusable artifact can be written. Authority can
be admitted only for one exact digest, artifact class, development purpose and
non-transitive local-test ceiling. Unsafe or incomplete proposals must fail
closed.

That evaluator tranche will use authored-synthetic candidates only. It will not
reopen the historical archive or materialise the first historical-derived
scenario.
