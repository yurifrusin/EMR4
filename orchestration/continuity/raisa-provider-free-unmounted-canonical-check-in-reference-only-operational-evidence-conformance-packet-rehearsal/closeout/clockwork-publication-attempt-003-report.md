# Governance clockwork publication attempt 003

The generation at lease 209 and its postpublication governance suites passed,
but the source efficacy reading counted only the two earlier publication
attempts. The clockwork restored the previous generation byte-exactly at lease
210 so the final source could state the convergent total. The attempt is not
accepted.
