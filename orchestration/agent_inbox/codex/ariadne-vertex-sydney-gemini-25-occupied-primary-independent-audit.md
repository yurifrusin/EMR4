# Independent Audit — Sydney Vertex Gemini 2.5 Flash Occupied Primary

Date: 2026-07-24
Mode: repository-read-only; no cloud, provider, credential, network, Docker,
external-worker, Git or protected-evidence operation
Result:
`ariadne_vertex_sydney_gemini_25_occupied_primary_independent_audit_pass`

The independent review accepts the recorded primary result
`ariadne_vertex_sydney_gemini_25_occupied_rehearsal_revision_required` and
accepts the no-retry disposition
`closed_deterministic_request_contract_defect_not_identified`.

The exact binding validates as Google Vertex AI, `gemini-2.5-flash`,
`bernie-emr4-dev`, the authorised non-human service account, keyless
impersonated ADC with the cloud-platform scope, exact prediction permission,
`australia-southeast1`, and the Sydney regional `generateContent` path. API-key
authentication, automatic fallback and provider tools were absent.

The seven-event occupied audit chain independently recomputes to
`sha256:d95f9b9afa213e3ac22814f3f67f468b2f8a201a576ad22ca76b4605a813ee5a`.
The request and response-schema hashes match the local deterministic
constructors. Exactly one occupied call was reserved and attempted. Its ledger
is consumed with one call consumed and fallback prohibited.

The provider result is bounded to HTTP 400, provider code 400,
`INVALID_ARGUMENT`, an empty allowlisted field-path set, a constant redaction
marker and the hash of discarded error bytes. No free-form provider message,
raw response, raw prompt, token, key material, sensitive data or reasoning is
retained.

The primary stopped before proofreader or release. It made no retry or
fallback. Because the bounded record identifies no deterministic
request-contract defect, the conditional retry criterion is unsatisfied; the
unused absolute call ceiling does not itself grant retry authority.

All eight provider-free Tranche 4 ledgers are consumed with zero calls. Their
eight audit chains independently validate: one interrupted setup, six failed
setup diagnostics and the accepted real-isolation dry run. The accepted dry-run
and occupied-primary evidence both validate against the rehearsal schema and
their embedded evidence hashes.

The recorded post-run checks show no task containers, networks, images, broker
processes, temporary tokens or temporary contexts, and no daemon-wide prune.
This audit validates that hashed cleanup evidence without reinspecting live
Docker state.

Continuity graph revision 41, Compass source revision 41, Compass map revision
28 and the exact rendered Compass hashes match the pre-attempt receipt. This
audit does not accept, integrate, move the baton or alter protected refs.

No result claims provider request-contract acceptance, model inference,
model-generated output, proofreader handling of model output, typed release,
actual token usage, model version, billing, Australian physical processing or
sovereign processing. No audit defect was found. No retry, fallback or
continuation is authorised without a fresh Yuri decision.
