# Reception One Receptionist-first v6.3 Provider-free Sol Acceptance

Status: accepted for the separately gated occupied cohort
Recorded: 2026-07-30

## Decision

The provider-free v6.3 form-toolkit repair passes its acceptance contract and
may advance to a revision-bound, read-only cloud preflight and then the
authorised Sydney Vertex cohort if that preflight passes.

This acceptance opens no product provider wiring, real or historical data,
database access, appointment write, confirmation, deployment or release.

## Evidence

- all twenty-four original v6 authored-synthetic requests are included,
  including every v6.0 pass;
- all twenty-four deterministic reference forms remain admitted by the
  unchanged output schema and proofreader;
- the system prompt now distinguishes mention evidence from resolved typed
  entities and gives the receptionist an exact selected-appointment move
  toolkit;
- the response ceiling is 3072 tokens while temperature remains zero,
  thinking budget remains 1024 and thought content remains excluded;
- no `no-show` to `dna` alias was added: the unstamped status case remains a
  clarification reference outcome;
- the model and proofreader still receive the same canonically hash-bound,
  minimal desk-context packet;
- the two-turn real-isolation rehearsal rejected a wrong first form and
  admitted the corrected form; both cells were non-root, read-only,
  network-none, mount-free and credential-free; and
- cleanup found no owned container, image or temporary-context residue.

## Verification

- 368 broad focused, typed-language-lineage, Vertex-boundary, Continuity,
  Compass and relevant API Spine tests passed;
- Ruff, Python compilation, JSON parsing and `git diff --check` passed;
- both repository readiness reports passed with the product provider boundary
  still disabled and no database, memory/RAG or historical Diary access; and
- no provider call or credential read occurred during provider-free
  acceptance.

## Occupied boundary

The occupied cohort is frozen to `gemini-2.5-flash`, project
`bernie-emr4-dev`, the exact Bernie impersonated service account, keyless ADC,
`australia-southeast1` and
`australia-southeast1-aiplatform.googleapis.com`. It may consume twenty-four
primary calls and no more than one terminal second call per case, never more
than forty-eight calls or USD 1. The prompt, request schema, proofreader and
generation settings may not change after the cohort starts.

Continuity and Compass must advance together, render and validate before the
first occupied call.

## Claim limit

This proves a bounded provider-free repair over a reused authored-synthetic
development cohort. It does not prove model generalisation, production fitness,
Australian physical or sovereign processing, or safety for product-derived,
patient, health, clinical, protected or historical data.
