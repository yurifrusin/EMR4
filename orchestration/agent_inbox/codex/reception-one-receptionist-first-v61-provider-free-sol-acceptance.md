# Sol Acceptance — Reception One Receptionist-first v6.1 Provider-free Gate

Disposition: accepted for the bounded occupied gate
Recorded: 2026-07-30

The separately versioned v6.1 repair passes its provider-free acceptance:

- all fifteen closed v6 misses have exact deterministic reference forms that
  the unchanged typed compiler/proofreader admits;
- all fourteen reconstructable semantically wrong v6 forms now fail with
  `recognized_intent_goal_mismatch`;
- the one pre-schema v6 failure remains a bounded historical classification;
- a novel unrecognized action is not closed by the one-way assertion;
- a grounded noun fragment without an action cannot become a create request;
- the correction ticket identifies constraints and never selects a
  replacement;
- real isolation passes across two non-root, read-only, network-none,
  credential-free cells with no container or image residue; and
- the model remains a receptionist whose JSON packet carries separate natural
  speech and typed form channels. Natural speech is not parsed into the form.

The occupied gate remains restricted to the exact fifteen authored-synthetic
cases, Gemini 2.5 Flash, the Bernie project and impersonated service account,
the Sydney Vertex locational hostname, keyless ADC, at most thirty calls and
USD 1. No API key, fallback, product/database data, write, confirmation,
delivery, deployment or release is authorised.
