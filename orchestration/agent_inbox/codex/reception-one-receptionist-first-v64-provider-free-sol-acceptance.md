# Reception One Receptionist-first v6.4 Provider-free Sol Acceptance

Status: accepted for the separately revision-bound occupied cohort
Recorded: 2026-07-30

## Decision

The provider-free v6.4 exact-constraint repair passes its frozen acceptance
contract and may advance to the exact read-only Bernie/Sydney cloud preflight,
then the authorised full twenty-four-case cohort if that preflight passes.

This acceptance opens no product-provider wiring, product or database context,
real or historical data, appointment confirmation or write, deployment or
release.

## Evidence

- all twenty-four original v6 authored-synthetic requests are included,
  including every v6.0 pass;
- all twenty-four deterministic reference forms remain admitted;
- every independently recognized `semantic_ref` and `context_ref` is now
  compared with the model-authored normalized source graph;
- a deliberately broadened corrected-time form is rejected for omitting both
  exact 3:00 PM bounds, receives a closed correction ticket, and its exact
  replacement releases only `synthetic-slot-july28-1500`;
- unknown compositions are not forced to copy a deterministic operator
  sequence when no recognized actionable manifest exists;
- the natural receptionist response, decision note and typed form remain
  separately bounded and must describe the same goal;
- the output schema, 3072-token response ceiling, zero temperature,
  1024-token thinking budget and hidden-thought exclusion are unchanged;
- the no-show-to-`dna` alias remains closed; and
- the two-turn real-isolation rehearsal passed in non-root, read-only,
  network-none, mount-free, credential-free cells with complete cleanup.

## Verification

- 374 focused, lineage, Vertex-boundary, Continuity, Compass and relevant API
  Spine tests passed;
- the fixed repository-only Ariadne verifier passed;
- Ruff, Python compilation, JSON parsing, JavaScript syntax and
  `git diff --check` passed;
- both repository readiness reports passed with the product-provider boundary
  disabled and no database, memory/RAG or historical Diary access; and
- no provider call or credential read occurred during provider-free
  acceptance.

The API Spine classification remains a typed minimal, source-labelled,
freshness-bound and non-authoritative context/proposal lane. No GraphQL,
REST-command or product mutation authority is added.

## Occupied boundary

The occupied cohort remains frozen to `gemini-2.5-flash`, project
`bernie-emr4-dev`, the exact Bernie impersonated service account, existing
keyless ADC, `australia-southeast1` and
`australia-southeast1-aiplatform.googleapis.com`. It may consume twenty-four
primary calls and no more than one terminal second call per case, never more
than forty-eight calls or USD 1. Prompt, schema, proofreader, context and
generation settings may not change after the cohort starts.

Continuity and the rendered Compass must advance and validate together before
the first occupied call.

## Claim limit

This proves a bounded provider-free repair over a reused authored-synthetic
development cohort. It does not prove independent generalisation, production
fitness, Australian physical or sovereign processing, or safety for real,
product-derived, patient, health, clinical, protected or historical data.
