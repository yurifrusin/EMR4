# Reception One v5 Broad Language Cohort Provider-free Sol Acceptance

Disposition: `accept_provider_free_only`

The twenty-four authored-synthetic cases, their exact typed oracles and the
forty-eight-call maximum are frozen without changing the v5 prompt, response
schema, compiler, proofreader, executor or provider binding.

All twenty-four provider-free oracles are proofreader-admitted. Nineteen
produce only the expected in-memory proposal or squeeze-in assessment and five
produce only non-command clarification. The deliberate `no-show` language gap
is correctly predeclared as clarification under the current deterministic
extractor and cannot be rescored after provider observation.

Provider contact and credential reads are zero. Distinct case-bound one-use
identifiers reject cross-case pairing. Focused broad and inherited multi-case
tests pass.

Accept only the provider-free preparation. The occupied cohort remains blocked
until real isolation, full repository/API Spine checks, revision and rendered
Compass binding, fresh exact Bernie ADC/control preflight and independent
pre-call residue checks pass.
