# Corrected Gemini 3.7 Flash/high egress-recovery veto — DeepSeek native Harness required-service injection recovery

Date: 2026-08-20

Timestamp: 2026-08-20T09:48:32.4383560+10:00 (Australia/Brisbane)

Perform one fresh read-only egress-recovery veto of exact candidate
`056c59d14de4efc302898e84ec7a69cbf729dfce` against frozen plan source
`3e9c004e09b09c228937276f520d39e4b2c3d36b`.

## Exact candidate, authority and preserved failure

- Worktree: `C:\Users\sarashera\EMR4-worktrees\required-service-injection-corrected-gemini-056c59d1`
- Branch: `codex/review-required-service-injection-corrected-056c59d1`
- HEAD: `056c59d14de4efc302898e84ec7a69cbf729dfce`
- Required model/effort: `gemini-3.7-flash-high` / `high`
- Original semantic rejection:
  `orchestration/agent_inbox/antigravity/deepseek-native-harness-required-service-injection-recovery-gemini37-review-receipt.json`
- Sol correction disposition:
  `orchestration/agent_inbox/codex/deepseek-native-harness-required-service-injection-recovery-gemini37-attempt-001-disposition.json`
- Non-admitted corrected egress failure:
  `orchestration/agent_inbox/antigravity/deepseek-native-harness-required-service-injection-recovery-corrected-gemini37-review-receipt.json`
- Egress-failure disposition:
  `orchestration/agent_inbox/codex/deepseek-native-harness-required-service-injection-recovery-corrected-gemini37-egress-attempt-001-disposition.json`

The immediately preceding fresh project executed all ten commands with exit
code zero and produced a substantive `pass` review, but its terminal envelope
mistranscribed C10 as `git diff status --short --branch`. The exact manifest
requires, and the reviewer actually executed, `git status --short --branch`.
The broker correctly admitted no verdict. Do not reuse that conversation or
claim it passed. This is one fresh project and one complete review.

Read `AGENTS.md` completely and name all five rehydration sources. Validate the
in-progress latch for the current operation. Restore the explicit dispositions:
DeepSeek declined; Gemini dispatched for this one fresh egress-recovery veto;
native subagents declined under current developer policy and the small serial
source-semantic boundary.

Read the plan, threat delta, current contract/schemas/evidence/report,
implementation/test, original rejected review and Sol disposition, the
non-admitted corrected egress failure and its disposition, accepted failed
native-boot evidence, preterminal-observability recovery and effective-tool
guard. Do not inspect `docs/branding/`, protected evidence or unrelated paths.

## Mandatory challenges

1. Confirm the exact corrected candidate/worktree and a clean tracked tree.
2. Reperform the original semantic challenges: eight exact cached package
   identities and members; base `tools`; absent headless `agent-presets`;
   official inert Web roster row; AgentPresets/ToolRuntime service names and
   dependencies; profile shipped-root rule; Cordis/loader injection and
   inactive-until-complete behavior; exact future patch/runner declarations;
   hostile cases; immutable attempts; strict zero-execution and claim ceiling.
3. Confirm Finding 1 is corrected without expanding the provider-free
   environment: when `LOCALAPPDATA` exists it remains authoritative; when the
   wrapper removes it, the locator selects the first existing non-symlink
   `AppData/Local/npm-cache` under the repository ownership ancestry; when no
   such candidate exists it fails closed. Confirm positive stripped-environment
   and negative no-candidate tests.
4. Confirm the regenerated deterministic evidence binds the corrected
   controller and focused-test digests and still equals its current projection.
5. Confirm Finding 2 is contained without modifying the accepted predecessor
   test: the isolated-worktree manifest omits only
   `test_deepseek_native_harness_provider_free_preterminal_observable_composition_recovery_boot_evidence.py`,
   whose physical `docs/branding/` assertion is non-transferable to Git-created
   worktrees. The current candidate must still directly hash-check both
   predecessor attempts and validate both retained failed results. The primary
   worktree preservation check remains outside the verifier worktree.
6. Execute exactly the ten-command manifest in the injected structured command
   list. C03 must pass 86 tests. Do not run `--publish`, Node, npm install,
   Harness, broker, Docker, PostgreSQL, DeepSeek or another provider. Every
   command must return zero and leave the review worktree unchanged.
7. Confirm no product/API/config/client/feature flag/allowlist/grammar/
   waiting-area, ordinary-practice, data, production, deployment, Pages or
   protected-ref authority changed; `emr4-bounded-worker` remains
   unmaterialised and no future native process is authorised.
8. In the terminal structured envelope, copy every command `id` and `argv`
   exactly and in order from the injected manifest. In particular, C10 is
   exactly `["git", "status", "--short", "--branch"]`; do not infer,
   normalize, amend or reconstruct any command argv.

Do not edit, commit, push, install, access protected data, deploy, publish or
move protected refs. Return one schema-constrained `pass` only if both original
findings are corrected/contained, the candidate is exact and fail-closed, all
ten exact command results are reproduced without transcription change, and no
P0-P2 finding remains. Otherwise return `revision_required` with exact
evidence. Emit one terminal decision.
