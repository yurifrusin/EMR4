# Gemini 3.7 Flash/high veto — emr4-bounded-worker preset materialisation recovery

Date: 2026-08-20

Timestamp: 2026-08-20T10:30:02.1366272+10:00 (Australia/Brisbane)

Perform one fresh read-only veto of exact candidate
`962e91d9af90fbe977177d251f9f3e3134aaad19` against frozen planning source
`817add1e9f36ff143e6e46e854cdefd81fd45969`.

## Exact candidate and authority

- Worktree: `C:\Users\sarashera\EMR4-worktrees\bounded-worker-preset-gemini-962e91d9`
- Branch: `codex/review-bounded-worker-preset-962e91d9`
- HEAD: `962e91d9af90fbe977177d251f9f3e3134aaad19`
- Required model/effort: `gemini-3.7-flash-high` / `high`
- Provider boundary: review-provider use only; the candidate itself starts no
  Node/native Harness process, agent, broker, DeepSeek model or provider.

Read `AGENTS.md` completely and name all five rehydration sources. Validate the
in-progress latch for
`deepseek-native-harness-provider-free-emr4-bounded-worker-preset-materialisation-recovery`.
Restore the explicit dispositions: DeepSeek declined; Gemini dispatched for
this one fresh exact-candidate veto; native subagents declined under current
developer policy and the small serial source-semantic boundary.

Read the plan, threat delta, contract/schemas/evidence/report, exact materialised
preset, implementation/test, accepted required-service recovery and accepted
effective-tool guard. Do not inspect `docs/branding/`, protected evidence or
unrelated paths.

## Mandatory challenges

1. Confirm the exact candidate, branch and clean isolated worktree. Confirm the
   planning and accepted predecessor fields are full 40-character Git object
   ids and the predecessor commits are ancestors of the candidate.
2. Reperform the exact local-cache identity/member checks for the six bound
   rc.7 packages. No registry, npm command, package install, Node, Harness,
   broker, DeepSeek or external network action is permitted.
3. Confirm rc.7 discovery semantics require the exact future relative path
   `.agent-presets/emr4-bounded-worker/agent.cordis.yml`, the exact preset-id
   grammar, a top-level row list, broken-composition rejection, first-root-wins
   behavior and resolve-before-standing-parent mount ordering.
4. Confirm the materialised payload is byte-exact UTF-8/LF, has exactly the two
   ordered official rows, contains no alias/tag/dynamic expression/surplus key,
   and requires `sampleOverCapGlobResults: false`.
5. Challenge the two-stage security claim. Exact tool source must show the raw
   inherited surface is unconditional `edit`, `glob`, `grep`, `read`, `write`
   plus conditional `read_image`; the preset alone is not the authority
   boundary. The already accepted post-mount guard must reduce minimal and
   attachment-present cases to exactly sorted `edit`, `glob`, `read`, while the
   outer broker allowlist remains independently mandatory.
6. Confirm all 21 hostile payload/path variants fail closed, selected-name
   drift/missing inherited/scope-local cases reject, and no real Harness home or
   external path is written.
7. Confirm both consumed native attempts remain byte-exact failed evidence with
   no retry/resume/reclassification, and every Node/Harness/agent/session/turn/
   broker/model/provider/network/Docker/database count is zero.
8. Execute exactly the ten-command manifest. C03 must pass 101 tests. Do not run
   `--write`, `--publish`, Node, npm, Harness, Docker, PostgreSQL, DeepSeek or
   another provider. Every command must return zero and leave the worktree
   unchanged.
9. Confirm the claim ceiling: this proves a repository-retained installation-
   ready payload and deterministic composition with the accepted guard. It does
   not prove native discovery, mount, combined service activation, scope,
   effective runtime schema, another native boot, occupied worker completion or
   model/provider reliability.
10. Confirm no product/API/config/client/feature flag/allowlist/grammar/
    waiting-area, ordinary-practice, data, production, deployment, Pages or
    protected-ref authority changed.

Do not edit, commit, push, install, access protected data, deploy, publish or
move protected refs. Return one schema-constrained `pass` only if the candidate
is exact and fail-closed and no P0-P2 finding remains. Otherwise return
`revision_required` with exact evidence. Emit one terminal decision.
