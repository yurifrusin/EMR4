# Sol acceptance — complete package-unloaded runner evaluation

Date: 2026-08-22

Decision: **accepted through exact process-free reconciliation**

Accepted result:
`complete_package_unloaded_runner_evaluation_reconciled_pass`

Sol accepts one and only one complete package-unloaded runner process. The
immutable envelope proves exit zero, empty stderr, exact stdout and sidecar byte
counts/hashes, complete cleanup, one Node process and zero installed-package,
native-Harness or worker/model/provider activity. The exact runner sidecar
proves the private authored-synthetic factory path reached successful preset
and tool composition, local model-selection installation, exact publication
veto and empty rollback state.

The original `complete_runner_result_rejected` terminal remains valid as a
controller decision but not as evidence of runner failure. AER-0939 and the
process-free reconciliation prove that generic sorted evidence serialization
was incorrectly used for a declared-order wire comparison. Both exact frozen
preimages match the immutable envelope; no retry or second process occurred.

This acceptance does not extend to the installed package, native Harness,
DeepSeek worker, provider transport, worker quality, product/data behavior,
ordinary-practice enablement, production, deployment, release, Pages or
protected integration.

The authorised successor is one bounded occupied native-Harness DeepSeek
worker on useful Raisa development work, after fresh five-source rehydration
and an exact worker contract. No further speculative provider-free checkpoint
is accepted.
