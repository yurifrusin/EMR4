# Sol acceptance — provider-free native Harness HMR boot proof

Date: 2026-08-20

Decision: `accept`

I accept the pinned rc.7 provider-free stock-headless-to-custom-runner HMR
proof. The package identity and source mechanism are exact; the initial layer
cannot reach the stock model runner; readiness comes from the in-process HMR
registry; only the post-readiness atomic patch contains the custom runner; the
four events are exact and ordered; that runner owns exit 0; and all network,
model, broker, provider and session counts are zero.

I separately retain the prelaunch source-predicate rejection. It created no
native Harness process and cleaned its disposable root, so the narrowly
corrected native attempt remains exactly `native-attempt-001`, not a concealed
retry. The passing controller also reaped its process and removed its exact
root. The temporary source-inspection directory is absent from its original
location and recoverable from the Windows Recycle Bin.

Gemini 3.7 Flash/high independently passed all ten read-only verifier commands
at exact candidate `5c3325e9213afc5690453812e2078c61135c8a38`, including 39
provider-free tests, and reported no P0-P2 finding. The exact review worktree
and branch are absent.

This accepts startup traceability only. No model/provider call, occupied
worker, attempt-004 execution, product/API/config/client/data change,
ordinary-practice activation, production, deployment, release, Pages or
protected-ref movement is accepted. The dependency-satisfied successor starts
by freezing the separately named relay-free recovery attempt-004 plan and
cannot execute before its own fail-closed admission.
