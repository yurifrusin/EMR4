# Sol acceptance — check-in environment-manifest and secret-posture architecture

Date: 2026-08-19

Timestamp: 2026-08-19T15:38:31+10:00 (Australia/Brisbane)

Decision: **accept**

GPT Sol accepts exact reviewed candidate `a1f309a6d52d01f9866432f7e9abb8095788d023` as the bounded provider-free, repository-static environment-manifest and operational secret-posture architecture.

Acceptance is limited to these claims:

1. the canonical population is empty and default denial remains exact;
2. the logical runtime role is `appointment_check_in_ordinary_runtime_v1`, with non-owner, `NOBYPASSRLS`, no product-relation ownership, exact-environment and cross-tenant-denial expectations only;
3. exactly three ordered opaque reference-only secret/key slots are defined, and raw secret values, endpoints, connection strings and secret-material digests are forbidden;
4. future rotation evidence must bind the exact environment, slot, key, version, generation, immutable evidence digest, full 40-character Git object, freshness and independent verifier;
5. break glass is deny-only and cannot add authority;
6. `evidence_gate_satisfied` is not admission and cannot execute a route, command, secret lookup, database action, role action or configuration change;
7. all sixteen source bindings, 337 hostile mutations with zero escapes, 17 focused tests and 103 material surrounding tests passed; and
8. Gemini 3.7 Flash/high passed its independent exact-candidate veto with a clean unchanged worktree.

The broad local run's four failures are recorded as stale historical/current-state fixtures, not hidden as product defects or repaired by scope creep. No ordinary practice, secret, database, provider, product data, production runtime, deployment, release, Pages or protected integration is accepted.

The next operation may prove the role and tenant-isolation expectations only in a uniquely named disposable local PostgreSQL instance with authored-synthetic inputs and exact cleanup. It may not use a live secret, existing database, product data or persistent role.

The live clockwork publication passed exactly once at generation `gen-a7b3f1fc6696f535ef4f7d5e0d5b99c89f19bdc87546e5b4cf242b3b7483e88d`, lease sequence 3 and Continuity 333 / Compass 315, with zero drift, zero caller-derived fields, zero bespoke updater runs and zero republications. Three mutable-current assertions were made generation-aware after publication; the complete material suite then passed without canonical repair.

The non-PHI continuing Pushover notification succeeded with request `0b6402ec-152d-454c-8024-a80b1dfcb887`.
