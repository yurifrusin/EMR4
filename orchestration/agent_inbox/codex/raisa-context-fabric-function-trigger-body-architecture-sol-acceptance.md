# Sol acceptance — durability function-and-trigger-body architecture

Date: 2026-08-08

Decision: `pass`

Result: `raisa_provider_free_unmounted_durability_function_trigger_body_architecture_pass`

Accepted source HEAD: `a93d07405ad35d7d6c0603065625c17ec14ab23e`

Contract hash:
`sha256:f71287f266a3252d2a0736e511287600939a40bc70397710600c12581e24d4f3`

Sol accepts the fourth exact-veto replacement after reconciling the complete
339/339 deterministic packet and one fresh schema-constrained independent
`pass`. The accepted key-rotation program contains exactly one eleven-`EQ`
`F_ANCHOR` assertion immediately after the current-anchor lock and before the
prior-key lock, digest use or any effect. It compares the four locked checkpoint
fields and seven locked generation control digests to the anchor field by
field. Exact identical-key replay remains inert before the new-effect branch.

The fresh Gemini 3.6 Flash/high reviewer ran 44/44 focused tests, matched the
canonical builder digest, passed Ruff and diff checks, challenged hostile
substitutions and found no P0-P3 issue. Its worktree remained clean at exact
HEAD before and after. The earlier no-decision Antigravity exit remains
preserved as transport evidence and is corrected, not erased, by this distinct
fresh-project result in AER revision 80.

The closeout packet also exposed and corrected two pre-existing repository
baseline defects: the Continuity Schema omitted an optional artifact-pointer
property already used by accepted nodes, and the general Compass test retained
a superseded current-position literal. They are preserved as AER-0082/0083 in
revision 81 and do not alter the accepted source HEAD or contract.

This acceptance is architecture-only. It creates no SQL/DDL, migration,
function, trigger, grant, role or database object; opens no database/source/
feed/watcher/listener, product/patient data, provider product path, command,
runtime, deployment, production, release, Pages or protected-ref authority.
The next safe descendant is the provider-free unmounted inert DDL rehearsal.
