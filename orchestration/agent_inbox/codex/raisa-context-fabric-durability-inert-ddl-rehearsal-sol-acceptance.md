# Sol acceptance — durability inert DDL rehearsal

Date: 2026-08-07

Decision: `pass`

Result: `raisa_provider_free_unmounted_durability_inert_ddl_rehearsal_pass`

Accepted source HEAD: `46e16622471a192353cb82a33acf301dc2cfb7aa`

Canonical inert SQL artifact:
`sha256:92d234f8f57d5492e0c4215f9c0d3c54efe9e9ec9cbdaa9b4321a5120b983012`

Sol accepts the recovered renderer after reconciling the complete 62/62
deterministic packet and one fresh schema-constrained independent `pass`. The
accepted standard-library-only compiler consumes the exact immutable structure
and body contracts, accounts for all 22 parent programs, applies only eight
position-sealed representability operations and emits one 23-program effective
artifact to the fixed `.sql.inert` path.

The recovered SQL text is byte-stable, contains 412 statements and closes the
PostgreSQL-16 static catalogue: nine entry points, fourteen trigger functions,
fourteen triggers, eighteen fabric relations, forty-four policies, exact owner
transfers and no application owner mutation or runtime schema-creation grant.
Four immediate guards reselect keyed physical-row `xmin` before effect; three
deferred fences depend narrowly on their mandatory same-table guard. The
appointment guard is inert for zero producer bindings, exact for one and
fail-closed for duplicates. No trigger-row `OLD.xmin`/`NEW.xmin` remains.

The fresh Gemini 3.6 Flash/high reviewer independently recomputed the program,
trigger, reselection, dependency, seal, ownership and privilege populations,
ran all 62 focused tests plus renderer and Ruff checks, found no P0-P3 issue and
left the exact review worktree clean and unchanged.

The two rejected DeepSeek candidates and the orchestrator's two preflight HEAD
binding mistakes remain preserved. AER-0087 through AER-0091 in register
revision 84 close them only through explicit rejection, Sol recovery, exact-
HEAD independent veto and the new literal-HEAD preflight rule.

This acceptance is inert evidence only. It executes no SQL, creates no
migration or database object, contacts no database/source/feed/watcher/listener,
reads no product or patient data, changes no application/API/Diary behavior and
opens no provider, command/write, persistence, runtime, deployment, production,
release, Pages or protected-ref authority. The next safe action is to plan the
separately bounded disposable local PostgreSQL-16 parse-and-catalogue rehearsal.
