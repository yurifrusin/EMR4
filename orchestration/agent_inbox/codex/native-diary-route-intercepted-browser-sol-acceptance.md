# Sol acceptance: native-Diary route-intercepted application-session browser

Date: 2026-08-03

Accepted result:
`provider_free_native_diary_application_session_route_intercepted_browser_pass`

## Decision

Accept. Real headless Chromium loaded the ordinary standalone Diary and the two
accepted ES modules through a loopback static server. The enabled fixed reader
rendered exactly Avery and Morgan, used no legacy practitioner transport before
transition, invalidated a held stale result after bootstrap disable, recovered
the exact feature-off GraphQL directory, and failed generically with no partial
grid or fallback when the reader rejected.

The first independent native review rejected the candidate because its fixture
allowlist constrained paths but not methods. Root repaired it to one exact
method per admitted API path, made wrong methods and unknown paths fail closed,
recorded method/path tuples in every case and added negative regression. The
committed evidence then reproduced exactly.

This proves only `route_intercepted_browser` over authored-synthetic values. It
does not prove a live backend/PostgreSQL path, real application-session or
cookie injection, real identity/data, default-on behavior, accessibility,
usability, cross-browser/cross-tab behavior, command/write authority,
deployment, production or release.

## Verification

- Root integrated gate: 133 serial tests passed.
- Fresh Gemini 3.6 Flash/high veto: pass, including exact evidence reproduction
  and 101 verifier tests.
- Ruff, three Node syntax checks, JSON/YAML, closed-boundary, process and diff
  gates passed.
- Candidate/review HEAD remained `f551a91d861baa65d04fae8f50dfee0a52440035`.
- No screenshot, `app.main`, backend, database, provider or branding artifact
  entered the result.

Reasoning level: High under the frozen bounded browser-evidence contract.
