# Sol Review — Ariadne Sydney Vertex Gemini 2.5 Flash Cache-Disabled Continuation

Date: 2026-07-24
Decision:
`ariadne_vertex_sydney_gemini_25_occupied_rehearsal_revision_required`

I accept the Tranche 3 control preflight, provider-free Tranche 4 isolation
evidence, one-call accounting, fail-closed provider result, bounded external
audit and cleanup evidence. I do not accept a generated-draft or proofreader
claim.

The credential-free work cell, exact-path relay and purpose-built broker
executed the accepted provider-free exchange under the inspected non-root,
read-only, resource-bounded and capability-dropped policy. The dry-run typed
fixture passed the deterministic proofreader with only the authorised
mechanical repairs. This is local fixture evidence, not model evidence.

The occupied ledger was then consumed exactly once. The broker used only the
verified keyless impersonated Bernie ADC and the exact Sydney Vertex hostname.
The provider returned HTTP 400 `INVALID_ARGUMENT` before presenting a
candidate. The bounded error contains no allowlisted field path and therefore
does not identify a deterministic request-contract defect. The conditional
retry predicate is not met. No retry or fallback is accepted or performed.

The seven-event occupied audit validates directly and records one call, one
bounded failure, no proofreader release and complete task-scoped cleanup. The
audit itself passes even though the rehearsal result is revision-required.
The independent repository-only audit also recomputed the request and schema
hashes, occupied chain, all eight provider-free ledgers and chains, no-retry
disposition and recorded cleanup. It found no defect and grants no integration
or baton authority.

The API Spine boundary remains closed: no product command, database,
appointment write, patient information, clinical authority, production path,
deployment or human delivery was opened.

The evidence supports only the configured and observed
`australia-southeast1` locational request path and Google's published
contractual location statement. It does not establish Australian physical or
sovereign processing, provider acceptance, inference, model quality, token
usage or actual billing.

This descendant is closed. Any diagnostic, request revision or further
occupied call requires a fresh Yuri decision. Continuity graph revision 42,
Compass map revision 29 and the exact rendered Compass bind this terminal
state without revising historical nodes.
