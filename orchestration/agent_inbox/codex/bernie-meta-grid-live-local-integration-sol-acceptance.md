# Sol Acceptance — Bernie Meta-grid Live-local Integration

Date: 2026-07-20

Reviewer: GPT Sol

Decision: `meta_grid_live_local_integration_pass`

## Acceptance

I accept the bounded provider-free live-local synthetic integration/evaluation
tranche against
`docs/bernie-meta-grid-live-local-integration-evaluation-plan.md`.

The real authenticated native Diary/meta-grid path passed through the existing
FastAPI/PostgreSQL read and proposal-only boundaries. Plain-language focus and
refinement, patient timeline, availability, touch/keyboard selection,
proposal-not-committed review, aligned comparison, privacy, interruption
recovery and ordinary fallback all behave within the accepted projection
grammar.

The two bounded product corrections are accepted:

- a loopback-only standalone browser Office bootstrap that preserves normal
  hosted Office.js loading off loopback; and
- proposal-interruption reconciliation that discards transient patient,
  selection and proposal state before freshly reading the underlying
  availability scope.

Neither correction changes an API, command, write, database, event or provider
surface.

## Evidence accepted

- mandatory five-source rehydration, pre-plan, preacceptance, precommit and
  corrected predispatch receipts passed;
- the first invalid predispatch receipt is preserved rather than hidden;
- final browser evidence is accurately labelled
  `live_local_browser_backend_postgres` with no route/API interception;
- five required viewport states and native keyboard interactions passed with
  zero horizontal overflow, zero enabled control below 44 pixels and a clean
  console/page-error record;
- the replacement desktop evidence measures full 1440-pixel DOM/host geometry,
  painted extent 1.0 and rightmost-20-percent painted ratio 1.0;
- only exact loopback bootstrap/read/proposal traffic occurred, with no
  confirmation or session/event route;
- PostgreSQL counts and canonical hashes remained identical: six seeded
  appointments and zero audit, idempotency, booking-session or session-event
  rows;
- the exact marker-verified disposable database was removed after final
  readback;
- 20 focused tests after the CLI non-disclosure guard, the 180-test combined
  population and a complete clean
  139/139 Diary smoke rerun passed, with the preceding isolated timing failure
  and exact-node recovery recorded; and
- a fresh Gemini 3.5 Flash (High) veto returned `pass`, no findings, boundaries
  intact and evidence calibration accurate.

## Scope calibration

This is local authored-synthetic browser/backend/PostgreSQL integration
evidence. It is not provider, PII, committed-event, representative-usability,
Stage 3B, production, deployment, release or appointment-write evidence.

PR 52's initial Advanced Security high finding on generic database-report CLI
serialization was treated as a blocking gate. The accepted correction emits
only a fixed non-sensitive CLI status envelope and leaves imported evidence
readback unchanged.

No protected holdout or historical Diary material was accessed. No API,
GraphQL, OpenAPI/Pydantic, database model/migration, confirmation, provider,
event-runtime, deployment or release artifact changed.

API steward scope verdict: **client-only compatibility**; no API Spine contract
or command authority changed.

## Disposition

Accept and integrate through the normal protected, check-gated pull-request
workflow. After protected refs align, return the baton to Yuri for a focused
review of the live-local client. Any visual reorganization, external design
model, Stage 3B, voice, event runtime, confirmation/write, provider, PII,
production, deployment or release work remains a fresh decision.
