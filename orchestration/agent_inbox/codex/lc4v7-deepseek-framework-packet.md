# LC4V7 DeepSeek Flash Empty-Framework Packet

Date: 2026-07-16

## Assignment

Implement one content-blind LC4V7 certification framework candidate. This is a
bounded implementation/test lane, not planning, corpus authorship, acceptance,
or integration authority.

- Model: DeepSeek V4 Flash/high through Claude Code `--bare`
- Worktree: `C:\Users\sarashera\EMR4-worktrees\claude`
- Branch: `claude/current`
- Exact source head: `4038ba2fdfb44fc904419b14a4ea141dd8c32dea`
- Conductor/integrator: GPT Sol

Read completely:

- `AGENTS.md`
- `orchestration/agent_inbox/codex/lc4v7-sol-contract.md`
- `orchestration/agent_inbox/codex/lc4v7-one-shot-acceptance-rule.md`

You may then read only the ordinary sources expressly allowed by the contract:
the LC4V6D1 development runner and tests, `semantic_extraction.py`,
`lc4v4d3_policy_resolution.py`, generic Ariadne rules, and ordinary
non-protected tests needed to understand those interfaces.

## Protected and forbidden surfaces

Holdouts v1-v6 are sealed. Do not open, enumerate, list, search, import, run,
regenerate, hash-check, infer labels from, or tune against any protected
fixture, support/authoring module, manifest, seal, receipt, test, filename, or
per-case evidence. Do not use broad file discovery under Bernie holdout paths.

Do not create any V7 receptionist utterance, Gold value, synthetic diary row,
family identifier, real scenario ID, or coverage cell. Unit tests may use only
clearly non-product placeholder labels and synthetic callbacks. Do not edit the
contract, thresholds, parser, policy runtime, AGENTS.md, historical evidence,
routes, APIs, UI, database, provider, deployment, or release code.

## Owned files

You may create or edit only:

- `app/services/bernie/lc4v7_content_blind_framework.py`
- `app/services/bernie/lc4v7_acceptance_rule.py`
- `scripts/run_bernie_lc4v7_certification.py`
- `tests/test_bernie_lc4v7_content_blind_framework.py`
- `tests/test_bernie_lc4v7_acceptance_rule.py`
- `orchestration/agent_inbox/claude/lc4v7-deepseek-candidate.md`

## Required implementation

Implement the exact population/schema validation, canonical hashing, source
binding, one-use seal, two-repeat serial evaluation, non-intercepted extraction
and explicit Option A policy boundary, 13-dimensional layer-specific scoring,
aggregate-only report construction, fail-closed validity gates, and frozen
acceptance rule in the Sol contract.

The scorer must compare extraction clarification only with extraction Gold,
policy clarification only with policy Gold, then score composition. It must
never require those two clarification states to equal. Gold must not flow into
either runtime call. Seal consumption must be attempted before real evaluation
and remain consumed after validation, runtime, scoring, or acceptance failure.

The CLI may require explicit corpus/manifest/seal/report paths and a source
commit. It must have no default path that discovers a corpus. It must refuse
protected prior-version inputs, overwrite, a consumed seal, hash drift, schema
drift, and case-level report output.

Tests must cover the fail-closed cases listed in the contract, including a
non-vacuous example where extraction clarification is false and policy
clarification is true while composition passes. They must not instantiate the
real 288-scenario population or contain product utterances.

## Verification and closeout

Run focused tests serially, then the narrow ordinary preservation tests needed
for the interfaces you used. Run `git diff --check`. Review your changed-file
scope, commit all owned changes to `claude/current`, and do not push protected
refs.

Write the candidate note with:

- exact starting and final heads;
- exact changed files;
- test commands and results;
- explicit proof that no V7 content exists;
- known limitations or assumptions; and
- exactly one terminal line: `DECISION: candidate_ready` or
  `DECISION: blocked`.

Do not self-accept the framework or alter the baton.
