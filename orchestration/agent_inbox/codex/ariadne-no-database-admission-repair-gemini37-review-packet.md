# Gemini 3.7 Flash/high veto — provider-free no-database admission repair

Date: 2026-08-20

This is one fresh independent read-only veto of exact candidate
`91ee5ee06b7f1fb698b0240208295b226c4b87ff` against base
`440fc7bbd071fbb97a97c986e8c80fe69b83f747`.

## Exact candidate and authority

- Worktree: `C:\Users\sarashera\EMR4-worktrees\no-db-admission-gemini-91ee5ee0`
- Branch: `codex/review-no-db-admission-91ee5ee0`
- HEAD: `91ee5ee06b7f1fb698b0240208295b226c4b87ff`
- Review base: `440fc7bbd071fbb97a97c986e8c80fe69b83f747`
- Required model/effort: `gemini-3.7-flash-high` / `high`

Read `AGENTS.md` completely and explicitly name all five sources:
`live_handover_current_baton`, `current_authority_allocation`,
`active_plan_and_acceptance`, `protected_evidence_boundaries`, and
`git_refs_and_worktree`. Validate the in-progress operation latch. Restore the
explicit parallelism assessment: DeepSeek declined while its controlling gear
is under repair; Gemini owns this exact-candidate veto; native subagents are
declined by current policy and serial coupling.

Read the plan, threat delta, contract/schema, WorkOrder v2 schema, deterministic
evidence/report, all changed source and test files, the preplanning receipt,
this packet, the exact command manifest, worktree preflight and pre-verifier
orchestrator receipt. Do not inspect `docs/branding/` or unrelated paths.

## Substantive challenges

1. Confirm the candidate/worktree identities and clean tracked tree.
2. Audit the AST classifier for false admission: conftest imports (including
   dynamic forms), shared fixtures, local/autouse/transitive fixtures, class and
   module marks, literal/indirect parametrization, fixture cycles, unknown
   fixtures, path escapes, symlinks, duplicates and unsupported declarations
   must fail closed without importing a selected module or collecting pytest.
3. Audit false denial risk against the frozen pytest-core fixture set and the
   existing 12-file clockwork selection that has already passed 527 tests.
4. Confirm validation preflight derives the exact same selection, stores its
   aggregate digest, and passes the engine-derived selection digest to the
   child runner; drift rejects before subprocess creation.
5. Confirm ordinary pytest remains forbidden in command manifests and the only
   executable pytest path is the provider-free runner with `--noconftest`,
   plugin autoload disabled and database/provider environment removed.
6. Confirm new WorkOrders require v2 command/admission bindings by default;
   v1 creation and Node consumption require explicit test-only compatibility.
   Verify the v2 shadow bundle contains both exact artifact bodies and that the
   work-order-issued base digest, bundle digest and mutual artifact bindings
   reject tamper/replay.
7. Confirm the Node broker loads and validates both bound artifacts before
   `broker-ready` or simulated upstream I/O, and rejects missing, mutated or
   incomplete coverage.
8. Review the version changes and the frozen-source efficacy correction: it
   must preserve the accepted historical metric rather than hiding current
   implementation cost.
9. Reproduce all eleven exact commands. C03 must pass 48 focused tests. C04
   must reproduce 128/128 hostile mutation rejection and zero selected-module,
   pytest-collection, ordinary-pytest, Docker, PostgreSQL, provider and occupied
   DeepSeek invocations. Every command must return zero and leave the exact
   worktree clean.

No product/API/configuration/client source is in scope. Do not edit, commit,
push, install, invoke Docker or PostgreSQL, invoke DeepSeek or another provider,
access product/patient/appointment/clinical/protected data, deploy, publish or
move protected refs.

Return one schema-constrained `pass` only if every command and challenge passes
with no P0-P2 candidate or review-control finding. Otherwise return
`revision_required` with exact allowlisted-path evidence. Emit exactly one
terminal decision.
