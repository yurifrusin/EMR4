# Sol acceptance — Reception One selected-action-console orientation

Date: 2026-08-14

Timestamp: 2026-08-14T14:29:56+10:00 (Australia/Brisbane)

Decision: accept

Accepted source: `2d602cfd822235977676bfe9ee8d8dc0a14714fe`

Result: `raisa_reception_one_selected_action_console_consolidation_orientation_pass`

## Acceptance reasoning

The exact candidate satisfies the read-only orientation boundary. It selects a
deterministic four-button palette with no editor open initially and exactly
zero or one existing field-specific editor visible. Idle switching discards an
unsubmitted outgoing draft; busy, stale, interrupted and confirmation states
forbid switching. The active editor remains mounted through confirmation and
fresh reconciliation so existing Escape and focus-return behavior survives.

The architecture consolidates presentation only. Status retains its current
status proposal/confirm family. Time, duration and practitioner retain their
current bridges and the update proposal/confirm family. The design expressly
rejects a generic command object, multi-field payload, new route and
command-capable language intent.

Two native read-only packages independently converged on the selected
boundary. DeepSeek was explicitly and correctly declined because no mechanical
package existed. Gemini returned one exact `pass`, ran 77/77 tests and left
candidate HEAD and worktree clean and unchanged.

## Authority finding

No product source, patient/product data, backend, API Spine, database, provider,
network, command, deployment, release, Pages or protected ref changed.
`docs/branding/` and all unrelated untracked files remain outside the
candidate.

The next safe descendant is the bounded presentation-only progressive-
disclosure composition. Standing authority applies and no user-attention fork
is present.
