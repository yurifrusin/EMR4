# Context Fabric disposable PostgreSQL behavior/transaction plan correction veto

Perform one fresh read-only independent review of exact clean commit
`07e8750548ed69aba5a19f693a72397121a340e5` in
`C:/Users/sarashera/EMR4-worktrees/r74` on branch
`codex/review-context-fabric-durability-behavior-plan-07e87505`, using the
configured `gemini-3.6-flash-high` model at explicit `high` effort.

Rehydrate all five named `AGENTS.md` sources. Verify local/origin `master` and
`handoff/current` remain protected
`2e34bdad732fdab32fbf778280b3d3c70d66d602`. Inspect only the planning diff
`bc0120f574ff4a9fb34a5d463537542f906c5916..HEAD`, the six parent files named
by the candidate contract, AER-0111/AER-0112 and the preserved r73 receipt. Do
not inspect protected evidence or unrelated paths.

The replacement veto must establish all of these without changing the
worktree:

1. The planning candidate remains internal async/context-durability evidence
   only. It adds no API, Diary, application, Alembic, provider, product/patient
   data, command, deployment, Pages, runtime or protected-ref authority.
2. All six text parent bindings use canonical UTF-8/LF bytes, permit only
   mechanical CRLF-to-LF checkout normalization, reject lone carriage returns
   and reject every other byte drift. Independently recompute each canonical
   hash. The body contract must equal
   `sha256:634dbc5c1a5294c1ac2de6a913671cd968a9838aa763d4c2a4d229bbcd9c0271`;
   inert SQL remains
   `sha256:a33baca6f622835b62fc84c378f05a49c2936cf28925db6fb5fe4a4fb4d50a36`.
   No parent file changed.
3. The whole-document contract/schema still freezes exactly twenty ordered
   scenarios with exact category counts `6/4/3/4/3`; scenario meaning,
   principals, SQLSTATEs, effect/readback sets and closed boundaries are
   unchanged from candidate `1f21a3c`.
4. The positive thread and isolation remain coherent: registration -> producer
   position one -> PRIMARY admission -> coordinator apply -> exact replay,
   with isolated position-two conflict and three outer-rollback partitions.
5. Bootstrap stays outside behavior evidence; scenario principals use fresh
   connections and pre-transaction session authorization. Fixture grants touch
   only the four synthetic `public.*` relations and grant no Fabric DML,
   trigger execute, inheritance, membership, `BYPASSRLS`, schema create or
   owner authority.
6. Runtime containment remains no-pull/no-network/no-port/no-mount/tmpfs/
   argv-only/one-container/one-database/exact-ID cleanup. Fixtures and evidence
   remain opaque, authored-synthetic and value-minimized.
7. Concurrency, key rotation, retention execution, unknown commit, applied
   migration, application/API/Diary wiring, operational persistence,
   watcher/listener/source access, product/patient data, providers,
   deployment, production, release and protected refs remain closed.
8. AER revision 92 must validly preserve the r73 canonical-hash defect as
   AER-0111 and its inaccurate packet count as AER-0112. Preserve the valid P2
   finding but do not repeat the r73 count narrative as fact.
9. Print and reconcile these exact per-file collection and pass counts:
   - new behavior/transaction plan: 27;
   - parse/catalogue parent plan: 9;
   - migration/transaction parent plan: 12;
   - function/trigger body parent plan: 7;
   - API Spine artifacts: 36;
   - idempotency continuity index: 4 admitted plus exactly 1 named baseline
     deselection;
   - audit correlation continuity index: 7;
   - update-confirm idempotency route contract: 22.
   The exact admitted planning packet is `27+9+12+7+36+4+7+22=124`, and all
   124 must pass. Also collect and pass exactly 79 AER tests. A terminal pass is
   forbidden unless every file and arithmetic total is explicitly reported.
10. Validate both planning JSON documents and the AER register/schema, run Ruff
    on both changed test files, run
    `git diff --check bc0120f574ff4a9fb34a5d463537542f906c5916..HEAD`,
    confirm no diff under `app`, `alembic`, `docs/api-spine`, `docs/diary` or
    `docs/branding`, and leave exact HEAD plus worktree clean.

The named baseline failure is
`tests/test_api_spine_idempotency_continuity_index.py::test_idempotency_continuity_index_covers_openapi_command_paths`.
It identifies the three already-tracked omitted paths recorded in the plan,
exists unchanged at baseline `bc0120f...`, is the sole deselection, and is not
claimed as repaired by this tranche.

Use the primary repository venv executables by absolute path while keeping cwd
at r74. Do not run `uv`, install dependencies, edit files, fetch, push, start
Docker/PostgreSQL, access protected evidence, read patient/product data, call a
provider/product runtime, deploy or rebuild Pages. Report every P0-P3 finding.
`pass` requires zero P0-P2 findings and no unresolved meaning-changing P3. Do
not repair. Emit exactly one schema-constrained terminal decision.
