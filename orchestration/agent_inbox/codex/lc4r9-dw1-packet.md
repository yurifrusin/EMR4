# LC4R9 DW1 — Generator-Backed Contract Repair

You are the bounded implementation/test worker for LC4R9. Work only in the
disposable worktree supplied as your current directory. GPT Sol is Conductor,
architecture/acceptance/recovery owner, and protected integrator. You have no
planning, acceptance, integration, push, provider, deployment, or write
authority.

Read completely:

- `AGENTS.md` current baton, authority, and protected-evidence sections;
- `orchestration/agent_inbox/codex/lc4r9-generator-contract-repair-contract.md`;
- `orchestration/agent_inbox/codex/lc4r8-sol-acceptance.md`;
- `docs/bernie-lc4r8-replay-contract-audit.json`;
- `app/services/bernie/scale_corpus.py`; and
- the explicitly named development-only evaluator/loader modules needed for
  the contract.

Do not read the incident file: its details are Sol-only containment evidence.
Do not open, enumerate, list, search, import, run, regenerate, hash-check, or
inspect any protected holdout fixture, support module, seal, receipt, or
report. Do not search the broad `tests/` tree or use broad repository listings.

Implement only the nine files owned by the sprint contract. Use the accepted
LC4R8 redacted selection as the frozen authorization boundary. Add a fail-closed
source-generator allowlist for exactly the 11 selected surface scenarios and
override only their expected audit vocabulary from `create_requested` to
`created`. Do not change the global audit derivation, interpreter, replay,
comparator, scenario schema, or any unselected record. Never edit generated
fixture JSON by hand: regenerate through the authoritative generator.

The helper and tests must independently prove the exact changed scenario IDs,
exact changed fixture files and fields, complete composed passes for the 11,
full temporary generator round-trip, unchanged semantic/safety/variance
baselines, and the remaining 53/40 contract blockers. They must fail closed and
must not derive acceptance expectations from the observed output.

Use `C:\Users\sarashera\emr4\.venv\Scripts\python.exe`. Run focused tests,
the helper `--check`, Python compilation, a byte-for-byte temporary full
regeneration check, and `git diff --check`. Commit the nine-file result on the
worker branch, leave the worktree clean, write complete provenance and command
results to `orchestration/agent_inbox/codex/lc4r9-dw1-completion.md`, and do not
push.

Return `DECISION: pass` only when the exact repair and all frozen boundaries
hold. Otherwise return `revision_required`.
