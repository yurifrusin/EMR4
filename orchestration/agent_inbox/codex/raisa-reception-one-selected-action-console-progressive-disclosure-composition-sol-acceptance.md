# Sol acceptance — Reception One selected-action-console progressive disclosure

Date: 2026-08-14

Timestamp: 2026-08-14T18:40:01+10:00 (Australia/Brisbane)

Decision: accept

Accepted source: `1d9e58fd2624f87b8b3def538297054999e7bef3`

Result: `raisa_reception_one_selected_action_console_progressive_disclosure_composition_pass`

## Acceptance reasoning

The exact candidate implements the frozen presentation-only contract. It shows
one current-truth summary and four native action choices with no editor open
initially and at most one existing field editor mounted. Palette activity is
route-inert. Idle collapse or switching discards unsubmitted drafts; busy,
confirmation, stale, interrupted and reconciliation states fail closed.
Appointment reselection cannot erase an in-flight action latch, and fresh
external projection replacement cannot resurrect an idle draft.

Status, time, duration and practitioner retain their exact field-specific
renderers, executors, bridges, proposal/confirm requests and fresh readback.
No generic command object, automatic sequential execution, compound-update
claim or second write path was introduced. Baseline and candidate Git blobs for
`docs/diary/diary.js`, the appointment router and the appointment OpenAPI
command contract are identical.

Deterministic admission includes 23 new browser checks, 49 existing field
checks, one paired-projection truth case, a 167-test exact broader packet, the
196-test canonical fast profile with 209 maintained-source compilation, and
five typed evidence checks. Gemini independently returned one schema-constrained
`pass` over the unchanged clean candidate with the same 167 exact tests.

## Worker and evidence finding

The two native read-only analyses had positive leverage. DeepSeek's bounded
test artifact required Sol recovery after the one permitted correction and had
negative net leverage, but no failed worker candidate was admitted. Gemini
provided the required independent veto. Browser command evidence is correctly
labelled `route_intercepted_browser`; the in-app local shell inspection does not
broaden it to live backend or database evidence.

## Authority finding

No backend, API Spine, GraphQL, database, event, watcher, provider, credential,
IAM, patient/product/clinical data, deployment, production, release, Pages or
protected ref changed. `docs/branding/` and all unrelated untracked files remain
outside the candidate.

The next safe descendant is the read-only multi-change request atomicity
orientation. Standing authority applies and no user-attention fork is present.
