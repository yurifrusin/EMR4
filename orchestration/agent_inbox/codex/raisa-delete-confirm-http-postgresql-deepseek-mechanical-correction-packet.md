# DeepSeek bounded correction — delete-confirm HTTP/PostgreSQL harness

Date: 2026-08-17

Timestamp: 2026-08-17T08:04:50.3297826+10:00 (Australia/Brisbane)

Model: DeepSeek V4 Flash/high through Claude Code `--bare`

Exact worktree: `C:\Users\sarashera\EMR4-worktrees\delete-confirm-http-pg-worker-265497a0`

Branch: `codex/worker-delete-confirm-http-pg-265497a0`

Exact correction parent: `d67ad6b4df64099de85928c4c599fb16bfb2d808`

This is the one allowed bounded same-lane mechanical correction. GPT Sol
retains semantics, live execution, admission, integration and acceptance.

## Hard process correction

Do not run `pip`, `uv`, Poetry, Conda, a package manager, installer, browser,
Docker, PostgreSQL, external network or another provider. Do not create or
modify a virtual environment. Dependencies are already present. For tests,
execute only the existing interpreter
`C:\Users\sarashera\emr4\.venv\Scripts\python.exe` without writing outside the
worker worktree. If it cannot run, stop with `revision_required`.

Before editing, verify exact clean HEAD `d67ad6b4df64099de85928c4c599fb16bfb2d808`.
Edit only the same three owned files. Do not access or change any other path
except the exact read-only sources already named in the original packet.

## Exact defects to correct together

1. DHI-S04 currently requires replayed public HTTP bytes to equal the private
   stored receipt. Reverse this contradiction: public first/replay bytes must
   match each other, private first/replay bytes must match each other, public
   and private bytes must remain distinct, and exactly one audit, receipt and
   appointment effect must exist. Invoke the existing private/public proof.
2. DHI-S05 asks the route for a new proposal after the first target is already
   Cancelled, so `_proposal` must reject before an idempotency conflict can be
   tested. Seed one second authored-synthetic appointment under the same
   practice, actor and practitioner; obtain valid evidence for it; reuse the
   first command's idempotency key; require exact 409 conflict and zero effect
   on the second target. Missing and blank key probes must be proved
   non-mutating before the first valid commit.
3. DHI-S08 treats an absent required binding as a 200 blocked response, but
   Pydantic correctly returns 422 before the handler. Require exact 422 for
   absence, the accepted blocked outcome for malformed/tampered bindings, and
   zero command-session construction and zero effects for every structural
   probe.
4. Strengthen zero-effect proof: `_assert_unchanged` must require Booked,
   version 1, zero audit, zero idempotency rows and zero completed v1 receipt.
   Use it for authentication/non-mutating, cross-practice, warning mismatch,
   default-denial, revocation and forced-trigger rollback partitions.
5. DHI-S02 must seed a waiting-area value, acknowledge the returned
   `waiting_area_cleared` warning through the actual proposal payload, and
   require it cleared alongside adjacent version, one correlated audit and one
   complete receipt. DHI-S03 must likewise prove one adjacent effect, audit and
   complete receipt through the alias.
6. Validate both pass and failure evidence against the closed evidence schema
   before returning from `run_rehearsal`; failure paths must not bypass schema
   validation.
7. Add focused regressions that would fail on each original contradiction,
   including a source/AST guard against comparing public replay bytes directly
   with `_stored_bytes`, a sibling-target idempotency-conflict guard, exact
   absent-binding 422 handling, complete zero-effect fields, waiting-area
   clearing and unconditional final evidence validation.

Do not change plan, contract, schemas, product source, command meaning,
lifecycle authority, evidence shape or claim boundary. Do not run the occupied
harness.

Run the two owned tests, Ruff, compilation and `git diff --check` with no
package installation. Stage only the three exact files and make one new commit.
Report exact commit/parent, paths, checks, commands, absence of package/network
activity and one terminal `DECISION: pass` or `DECISION: revision_required`.
