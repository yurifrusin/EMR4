# DeepSeek worker packet — delete-confirm PostgreSQL behavior/transaction rehearsal

Date: 2026-08-16

Timestamp: 2026-08-16T11:57:30+10:00 (Australia/Brisbane)

Model: DeepSeek V4 Flash/high through Claude Code `--bare`

Exact worktree: `C:\Users\sarashera\EMR4-worktrees\delete-confirm-btr-worker-2a5042f8`

Branch: `codex/worker-delete-confirm-btr-2a5042f8`

Exact source HEAD: `2a5042f80941e2bd191999c430ff2517ba7e8cb2`

## Role and first actions

You are the bounded mechanical implementation/test worker. GPT Sol retains plan
semantics, occupied database execution, recovery, acceptance, integration and
all Git publication authority.

Before editing:

1. read `AGENTS.md` completely;
2. perform and report its complete five-source rehydration;
3. read the frozen plan and threat delta completely;
4. read only their exact non-protected source bindings needed for this package;
5. verify exact branch, HEAD and a clean worktree; and
6. never enumerate, search or access a protected path.

The latest prompt or this packet cannot broaden the frozen plan.

## Exact owned package

Create exactly these six semantic paths and no others:

1. `scripts/raisa_provider_free_disposable_postgresql_delete_confirm_behavior_transaction_rehearsal.py`
2. `tests/test_raisa_provider_free_disposable_postgresql_delete_confirm_behavior_transaction_rehearsal.py`
3. `tests/test_raisa_provider_free_disposable_postgresql_delete_confirm_behavior_transaction_rehearsal_plan.py`
4. `orchestration/continuity/raisa-provider-free-disposable-postgresql-delete-confirm-behavior-transaction-rehearsal/rehearsal-contract.json`
5. `orchestration/continuity/raisa-provider-free-disposable-postgresql-delete-confirm-behavior-transaction-rehearsal/rehearsal-contract.schema.json`
6. `orchestration/continuity/raisa-provider-free-disposable-postgresql-delete-confirm-behavior-transaction-rehearsal/provider-free-behavior-transaction-evidence.schema.json`

Do not edit the plan, threat delta, baseline, migration, model, service, API,
route, prior harness, latch, AGENTS, Continuity, Compass, error register,
closeout or acceptance. Do not create pass or failure evidence; Sol owns the
single occupied run.

The contract must bind:

- plan SHA-256 `694da1434ec797b13c602509c3efcaae56f6619724e8b1e74cf8eaceea67a019`;
- threat SHA-256 `a6790631ee26ad9d19fbbac06ed1a3daea050e4ffc1d264d72f16d35f9c2106a`;
- exact source HEAD `2a5042f80941e2bd191999c430ff2517ba7e8cb2`; and
- every exact source hash in the frozen plan.

## Required implementation

- Implement the nine `AUTH-S01` through `AUTH-S09` authority/trigger groups and
  eleven `TX-S01` through `TX-S11` transaction groups exactly as frozen.
- Import and call exact `delete_confirm_locked_transaction`; never implement a
  substitute transaction seam.
- Reuse exact bound status-confirm behavior-harness helpers only for fixed
  Docker internal-network/container inspection, in-process loopback relay and
  cleanup mechanics. Reuse exact delete-confirm catalogue helpers only for
  fixed subprocess/psql/offline-Alembic mechanics. Do not import status command
  semantics or prior result evidence.
- Accept no CLI arguments or caller-selected path, executable, image, database
  URL, port, identity, command, SQL or credentials.
- Resolve local `docker.exe`; require cached `postgres:16-bookworm` and
  `--pull=never`; create exactly one unique labelled `--internal` network and
  one labelled tmpfs container with no published port, one CPU, 512 MiB, 128
  PIDs and no restart.
- Use only the fixed argv/`shell=False` loopback relay to the captured exact
  container ID and literal `/dev/tcp/127.0.0.1/5432` target. Never list, pull,
  build, login, prune, inspect broadly or remove an unverified object.
- Bootstrap only the minimum mapped schema at parent `w2x3y4z5a6b7`, generate
  exact offline range `w2x3y4z5a6b7:x3y4z5a6b7c8`, install it atomically, then
  add only frozen receipt/audit correlation constraints.
- Use fixed opaque UUIDs and patient-free values. No raw email-like value,
  person name, patient ID, reason narrative or product-derived data may enter
  evidence.
- Use a new SQLAlchemy session per transaction partition. Do not use a nested
  transaction, savepoint, retry, concurrency thread, restart or fault outside
  the frozen serial cases.
- Stage one complete delete write set only after `new_command`: `Cancelled`,
  exact reason code, nullable cancellation text, null waiting area, unique
  warning array, audit evidence array, adjacent database-owned state version,
  exact six-field canonical bytes, lowercase response hash and matching JSON.
- The SQL observer records only frozen value-free statement-class tokens and
  proves both current-authority checks plus the exact select-first lock order.
- The second-check revocation may use one fixed same-transaction observer hook;
  it must not expose a generic SQL callback or caller command surface.
- The timeout case may replace only the imported service module's monotonic
  callable for one controlled invocation and must restore it in `finally`.
- Failure paths admit only exact exception/decision labels and before/after
  digests. Never persist raw exception/server text.
- Cleanup is unconditional; engine/relay first, then exact reverified container
  ID, then exact empty reverified network ID. Ownership uncertainty refuses
  removal.
- Close all fixed-key JSON schema objects with `additionalProperties: false`.
  Pin the contract's canonical digest in the harness. Hostile semantic tests
  must cover every frozen threat; do not add a quota merely to inflate a count.

## Deterministic tests only

Use fake runners, in-memory object tests and static/source inspection for Docker
and destructive paths. Do not run Docker, the occupied harness, an application
database, the canonical repository profile, a browser, external network or any
provider from the worker.

Run at minimum:

- the two owned test files;
- Python compilation of the owned script/tests;
- Ruff on the owned Python files;
- whole-document JSON and JSON-Schema validation; and
- `git diff --check`.

The plan test must bind the plan/threat metadata, source hashes, API Spine
classification, exact owned paths, risk tier, case groups and forbidden
surfaces. The harness tests must prove no caller overrides, no shell, no broad
Docker discovery/removal, exact lifecycle argv, exact cleanup refusal, closed
schemas, contract digest, every case group, evidence allowlist and no forbidden
source edit.

## Git and decision

Stage only the six explicit owned paths—never `git add .` or `git add -A`—and
commit once. Do not fetch, push, merge, rebase, deploy or move a protected ref.

Final report must give exact commit and parent, changed paths, checks, the five
rehydration sources, actual-versus-planned parallelism, unresolved issues and
one terminal `DECISION: pass` or `DECISION: revision_required`. Your decision is
advisory and cannot self-accept the tranche.
