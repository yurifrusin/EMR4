# Disposable PostgreSQL durability parse/catalogue plan challenge

Date: 2026-08-07

Candidate HEAD: `d202b310318496fb7a414d97916bbac54e6ec349`

Review worktree:
`C:/Users/sarashera/EMR4-worktrees/disposable-pg-plan-review-d202b310`

Review branch: `codex/review-disposable-pg-plan-d202b310`

Model allocation: one fresh Antigravity project, exact
`gemini-3.6-flash-high`, explicit high reasoning.

## Authority and containment

This is a read-only pre-execution architecture veto. Do not edit, create,
stage, commit, fetch, push, install, write caches, access the primary worktree,
contact Docker or any database, inspect/list Docker state, execute/apply SQL,
create a migration, touch credentials, call any other provider product path,
or access patient/product/protected/historical-PHI data. Sol retains plan
recovery, implementation, disposable runtime ownership, acceptance and
integration.

## Exact allowlist

Read completely from the review worktree only:

- `AGENTS.md` sections 2-7;
- `docs/raisa-provider-free-disposable-postgresql-durability-parse-catalogue-rehearsal-plan.md`;
- `docs/raisa-provider-free-disposable-postgresql-durability-parse-catalogue-rehearsal-design.md`;
- `docs/security/raisa-provider-free-disposable-postgresql-durability-parse-catalogue-rehearsal-threat-model-delta.md`;
- `tests/test_raisa_provider_free_disposable_postgresql_durability_parse_catalogue_rehearsal_plan.py`;
- the parent inert-DDL plan, design, threat delta, closeout and Sol acceptance;
- the parent `render-manifest.json` and `lowering-contract.json`; and
- the exact migration/transaction contract's four `existing_model_contract`
  relation entries only when needed to challenge synthetic prerequisites.

Do not enumerate or read other paths. Do not access any protected evidence.

## Required independent challenge

Decide whether the plan is sufficiently closed and safe to authorise one local
disposable PostgreSQL-16 parse-and-catalogue rehearsal.

1. Reconcile the accepted parent identity: source HEAD, planning baseline,
   canonical UTF-8/LF hash/byte count, statement count, PostgreSQL major and
   Windows CRLF-only normalization. Identify any path that could admit altered
   SQL.
2. Challenge Docker containment: exact already-local image, `--pull=never`,
   `--network=none`, no port/mount/volume/socket, tmpfs PGDATA, resource/time
   bounds, no registry/package fallback, no global enumeration and exact image
   ID verification.
3. Challenge exact ownership and cleanup. Try name collision, create/start
   failure, changed labels, changed image, stale/reused ID, daemon loss,
   timeout, output overflow and partial initialization. Confirm no unrelated
   object can be deleted and a pass cannot survive incomplete cleanup.
4. Challenge the four empty synthetic prerequisite shapes. Determine whether
   the accepted external columns/types and minimum keys can be created without
   product values or overclaiming real application compatibility, and whether
   owners/row counts are protected.
5. Challenge atomic admission and rollback: prerequisite transaction versus
   canonical artifact transaction, `ON_ERROR_STOP=1`, psql
   `--single-transaction`, fixed invalid suffix in a fresh synthetic database,
   and proof that no fabric schema or accepted role survives the failed
   transaction. Identify any PostgreSQL DDL that invalidates this claim.
6. Challenge catalogue completeness for all types, relations, constraints,
   indexes, forced RLS, policies, roles, functions, `proconfig`, triggers,
   owners and ACLs. Identify any dangerous reliance on unstable deparse text or
   counts without exact identities.
7. Verify the plan never invokes entry/trigger behavior, policy enforcement,
   application commands or rows, and does not misstate PostgreSQL function
   creation as complete PL/pgSQL body validation.
8. Try at least eight concrete hostile mutations spanning image fallback,
   network/port, host mount, trust auth, caller-selected command, catalogue
   omission, broad cleanup and claim widening. State whether the planned static
   tests can reject each.
9. Reconfirm no Alembic/application/API/Diary/source/provider-product/data,
   operational credential, durable persistence, deployment, Pages or
   protected-ref authority.

Report every P0-P3 finding with exact path/section and the smallest sufficient
correction. Passing tests do not override a conceptual defect. Do not repair.

## Allowlisted commands

```powershell
$env:PYTHONDONTWRITEBYTECODE='1'
$env:RUFF_NO_CACHE='true'
git rev-parse HEAD
git status --short
C:/Users/sarashera/emr4/.venv/Scripts/python.exe -m pytest -q -p no:cacheprovider tests/test_raisa_provider_free_disposable_postgresql_durability_parse_catalogue_rehearsal_plan.py
C:/Users/sarashera/emr4/.venv/Scripts/python.exe -m ruff check tests/test_raisa_provider_free_disposable_postgresql_durability_parse_catalogue_rehearsal_plan.py
git diff --check HEAD^ HEAD
git status --short
git rev-parse HEAD
```

Read-only `Get-Content`, exact-path `rg`, `git show`, `git diff` and short
in-memory expressions over the exact allowlisted JSON files are also allowed.
Do not create files or caches.

## Terminal response

Return one schema-constrained object only, with `decision` exactly `pass` or
`revision_required` and all evidence/findings in `review`. Include parent
binding, Docker/cleanup, prerequisite, atomicity/rollback, catalogue, claim
boundary, hostile mutations, tests and clean exact-HEAD postflight. Do not
self-repair.
