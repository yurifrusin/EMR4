# Sol acceptance — unclassified pre-HMR structured diagnostic seam recovery

Date: 2026-08-21

Timestamp: 2026-08-21T08:24:15.9013739+10:00 (Australia/Brisbane)

Reviewed source: `af22b3e7b2ce3ece15dd6d8530688d19380c1403`.

Clockwork binding source: `4b09bc3665bed77ed7d8cd6f50dc793d1e35d2de`.

Verdict: `accepted`.

I accept the candidate as the narrowest useful provider-disabled seam. The
error object is observed before stderr serialization; every retained value is
closed, the cause chain is bounded, unsafe reads collapse, writes are
exclusive, and the original rejection is rethrown unchanged. The separate v2
terminal and future controller envelope preserve the accepted v1 component,
controller and all three consumed attempts byte-for-byte.

The deterministic recovery passed exact inspection of six pinned rc.7 source
files and 11 hostile fixtures. The focused 52-test suite, Ruff and Python
compilation pass. Recovery activity includes zero Node, Harness, broker,
worker, model or provider process and zero raw attempt-stream read.

I also accept six contained workflow observations: the crossed accepted-source
boundary, stale historical equality selection, rejected clockwork vocabulary
shape, rejected manual Git-object repetition and rejected freehand incident
stages, plus one rolled-back next-latch boundary omission. The first five
closeout-control rejections occurred before canonical mutation; the published
rejection restored byte-exactly, and the corrected receipt retains machine-
owned Git evidence.

I reject any claim that attempt 003 is now classified, the JavaScript wrapper
has run, DeepSeek was reached, the native Harness is operationally ready or
another occupied attempt is authorised. The accepted next dependency is an
authored provider-free Node fixture rehearsal of the wrapper only.
