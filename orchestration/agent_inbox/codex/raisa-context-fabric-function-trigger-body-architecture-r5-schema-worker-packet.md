# R5 structural-schema implementation worker packet

## Assignment

Implement only structural JSON-Schema closure for R5C's two exact set
expressions and focused resealed schema tests.

- Worktree: `C:/Users/sarashera/emr4`
- Branch: `codex/ariadne-bernie-davida-parallel-seam`
- Source HEAD: `22e4ce818442fa9ea1aa8d5bd169c3b33166334f`
- Role: bounded implementation worker; no acceptance or integration authority

## Owned files

1. `scripts/raisa_provider_free_unmounted_durability_function_trigger_body_architecture_schema.py`
2. `tests/test_raisa_provider_free_unmounted_durability_function_trigger_body_architecture_second_veto_schema.py`
3. `orchestration/agent_inbox/codex/raisa-context-fabric-function-trigger-body-architecture-r5-schema-worker.md`

Do not modify any other file. Do not stage, commit or push.

## Exact structural contract

Add closed schema branches for exactly:

- `SET_CONTAINS_KEY` with required and only `op`, `set`, `source_relation`,
  ordered non-empty `key_pairs` of required/only `source_column` and
  `set_column`, and Boolean `type`; and
- `SET_COVERS_KEYS` with required and only `op`, `required`, `evidence`, ordered
  non-empty `key_pairs` of required/only `required_column` and
  `evidence_column`, and Boolean `type`.

Each set operand must use the existing closed local-reference shape. No raw SQL,
generic function/operator, free relation/column object, extra property or empty
pair list is representable. Preserve positional critical scalar closure without
whole-body `const` snapshots.

Focused tests must build schema from an in-memory canonical candidate, locate
the emitted R5 expressions and prove schema rejection after resealing for:
unknown/missing/extra property, empty pairs, malformed local set reference,
swapped pair field names and non-Boolean result type. Do not write the generated
contract/schema; Sol owns regeneration after lane integration.

## Forbidden surfaces

No builder/body program/validator, generated artifacts, existing tests,
plan/design/threat/AER, app/API/Diary/migration files, SQL/DDL, database,
source/provider/network/browser, patient/product data, runtime, credential,
command/write, deployment, production, release, Pages, protected refs,
branding or unrelated untracked files.

## Verification and result

Do not run repository pytest while parallel lanes are active. Run Ruff only on
owned Python paths. Inspect the owned diff and write the durable worker result
last with exact source HEAD, paths changed, schema branches/tests added, Ruff
result, integration dependencies, forbidden-surface confirmation and exact
terminal `RESULT: candidate_ready` or `RESULT: revision_required`.
