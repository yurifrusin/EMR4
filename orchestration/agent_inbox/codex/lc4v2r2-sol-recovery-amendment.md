# LC4V2R2 Sol Recovery Amendment

## Recovery decision

DeepSeek V4 Flash/high ran through Claude Code `--bare` from exact source
`5b6bef84`. The outer launcher timed out while the child remained active. The
worker later wrote a complete-looking candidate but entered repeated identical
pytest-summary extraction loops and never produced a launcher receipt or
candidate commit. Sol closed the non-productive transport loop and preserved
the untrusted candidate verbatim as task-branch commit `6c091d59` and protected
history commit `8ea0f6c4`.

The worker's `DECISION: pass` is rejected as acceptance evidence because:

1. its candidate-commit field remained the source commit;
2. it called `sha256:aa2d...` an unchanged ordinary corpus hash even though the
   authoritative unchanged corpus hash is `sha256:af8f...`;
3. it described the 28-case two-repeat check as 2,304 samples rather than 56
   fixture evaluations; and
4. several exact-fixture regexes were too broad outside the frozen examples.

No Flash correction loop was opened. GPT Sol adopted the source as an untrusted
candidate under the recovery lease.

## Sol-owned amendments

- narrowed clash-check bypass matching to the local `skip ... clash check`
  phrase;
- required premature completion claims to place completion before or without
  confirmation, preserving truthful post-confirmation wording;
- scoped `choose whichever` refusal to explicit non-unique identity language;
- required a single-given-name patient candidate to be followed by `with` or
  `for`, avoiding temporal-word false positives;
- added adversarial tests for truthful post-confirmation wording, appointment-
  time choice, unrelated `skip` clauses, temporal words, and genuine single-
  name ambiguity;
- corrected only Sol-owned documentation, leaving the worker artifact intact;
  and
- reran the exact report, LC4V2R1, LC4R10, focused, semantic, and reconciliation
  checks serially.

## Protected-evidence process incident

During Sol recovery orientation, one broad repository search unintentionally
returned protected v1 fixture path names. No protected file content, label,
manifest, seal, receipt, hash-check, evaluation, or tuning occurred. Subsequent
commands were restricted to exact authorized development paths. This metadata-
only incident does not authorize holdout reuse and both holdouts remain sealed.

## Boundaries

No provider correction call, T3.5 activation, route/API, database, UI,
deployment, historical diary, runtime, confirmation, release, or write-
authority surface was opened. Gemini independent review remains required on the
exact recovered head before Sol acceptance.
