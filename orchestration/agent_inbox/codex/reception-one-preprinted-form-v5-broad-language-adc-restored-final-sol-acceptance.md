# Sol Acceptance — Reception One v5 Broad Language ADC-restored Result

Disposition: `accept`

Accepted only as `capability_threshold_fail_closed`.

## Review

The 24-case frozen authored-synthetic cohort completed through the exact
Gemini 2.5 Flash Sydney Vertex and Bernie keyless impersonated-ADC boundary.
All 32 occupied calls returned HTTP 200, all 40 opened ledgers are consumed,
the USD 1 and 48-call ceilings held, and owned residue is absent.

The transport repair is bounded and evidence-backed. A 9,095-byte typed
correction packet exceeded the relay's 8,192-byte limit even though the broker
already admitted 32,768 bytes. The correction aligns those local limits,
retains one-use semantics and adds only pre-connect refusal retry. Eight failed
relay ledgers consumed zero provider calls and were not reused.

The capability result must not be described as a pass. Fourteen of 24 cases
matched their exact oracle. Five mismatches failed closed with no release, but
five semantically wrong typed values passed the current deterministic
proofreader. The present typed language and proofreader therefore demonstrate
substantial bounded usefulness but are not semantically sufficient for
production.

The nominated next direction—minimal prompt-context teaching plus stronger
semantic proofreader assertions—is justified by the repeated
`source_type_mismatch` and `operator_arity_invalid` clusters. It must be a new
revision-bound tranche with an untouched holdout; no learning should be
performed against the frozen evaluation cases and then reported as independent
evidence.

## Boundary confirmation

No API key, global endpoint, provider or regional fallback, product/database
access, real or historical data, confirmation, write, human delivery,
production, deployment or release occurred. No credential, token, raw prompt,
raw provider response or chain of thought is retained. The result proves the
configured and observed Sydney locational path, not Australian physical or
sovereign processing.
