# LC4V8D1 Sol Contract

Date: 2026-07-16

Decision before baseline: `fresh_development_projection_diagnostic_frozen`

Yuri authorized bounded ordinary-development LC4V8D1. GPT Sol owns planning,
fresh probe authorship, cross-field Gold adjudication, classification meaning,
baseline acceptance, recovery, remediation authority, protected integration,
and push. DeepSeek V4 Flash/high through Claude Code `--bare` may implement one
bounded runner/test candidate after the authored evidence is independently
reviewed. Gemini 3.5 Flash through fresh Antigravity projects must review the
authored contract before baseline and the final exact head before acceptance.
DeepSeek Pro is forbidden.

## Evidence and incident boundary

This tranche uses only the committed public V8 aggregate categories and 24 new
inspectable Sol-authored ordinary-development probes. Holdouts v1-v8 remain
sealed. V8 cannot be opened, enumerated, listed, searched, imported, executed,
regenerated, hash-checked, inferred from, repaired, rerun, or used as case-level
authority.

The pre-authoring metadata-only search incident is preserved in
`lc4v8d1-preauthoring-protected-search-incident.md`. It exposed no utterance,
Gold value, case identity, diary state, or per-case result. No D1 artifact or
external packet existed. All D1 content is authored independently from public
aggregates and named ordinary interfaces; all `lc4v8*` protected implementation,
fixture, authoring, manifest, seal, marker, test, and per-case surfaces are
forbidden from this point.

## Frozen questions and classification precedence

The public V8 result has policy resolution `0/576`, while policy clarification,
clarification composition, interpretation projection, replay, safety, and ten
other dimensions are `576/576`. Temporal relation and normalization are each
`528/576`. D1 asks:

1. Does ordinary `PolicyResolution` behavior satisfy independently authored
   semantic invariants across all six actions and clarification/refusal/no-op
   boundaries?
2. Can one explicit lossless JSON projection represent every typed policy field
   without tuple/list, omitted/null, identity, diary-relation, delta-count, or
   simulated-write ambiguity?
3. Do fresh independent surface-time and relation-composition probes preserve
   canonical values and exact source fragments?

Each case is classified exactly once, in this precedence:

1. `authoring_invalid`: schema or cross-field Gold contradiction;
2. `normalization_gap`: expected source fragment/canonical form/span absent;
3. `parser_gap`: intended action or temporal relation/bounds mismatch;
4. `policy_behavior_gap`: semantic policy invariants fail;
5. `policy_projection_gap`: semantic invariants pass but the exact canonical
   policy projection differs from Gold;
6. `pass`: every layer matches.

This separation is decisive: an exact projection mismatch must not be called a
product-policy failure when the independently scored semantic behavior passes.

## Frozen fresh matrix

`tests/fixtures/bernie_lc4v8d1_development/probes.json` contains exactly 24
cases, six in each family:

- `canonical_policy_actions`: one clean case for each implemented action;
- `policy_boundaries`: omitted/ambiguous/unknown identity, unsafe refusal,
  explicit action negation, and diary conflict;
- `time_surface_forms`: independently chosen numeric and spoken exact times;
- `time_relation_composition`: lower/upper bounds, interval, complementary
  cross-turn bounds, and correction replacement.

All cases execute twice through ordinary non-intercepted `extract_semantics`
and explicit Option A `resolve_policy`. Expected values are never passed to
product code. The inspectable report must preserve observations, mismatch
fields, semantic-invariant results, exact classification, two-repeat variance,
fixture hash, frozen non-pass selection hash, and complete report hash.

## Canonical policy projection

The runner must explicitly project the typed result to exactly these JSON-safe
fields: clarification flag/choices; resolved patient, practitioner, and
practitioner ID; selected tools; authority; diary relation/conflicting fields;
downstream outcome; appointment and audit delta counts; simulated-write flag;
and entity-semantics-unchanged flag. Tuples become ordered arrays; nullable
values remain explicit `null`; no field is omitted. The projection must contain
no expected value and no probe-ID branch.

Semantic policy invariants are scored separately as one of
`propose_mutation`, `proceed_read`, `clarify`, `refuse`, or `no_action`, plus
mutation allowance and safety. Cross-field validation must reject contradictory
Gold before product execution.

## Baseline and remediation gate

No runtime remediation is authorized until:

- Gemini passes the authored contract, fixture, cross-field invariants, and
  exact frozen bytes before product baseline;
- the recovered runner passes fail-closed structural and no-oracle tests;
- the two-repeat baseline is committed with zero authoring-invalid cases;
- every valid non-pass case is adjudicated into the precedence above; and
- the exact non-pass selection is frozen by hash.

Only selected ordinary product behavior gaps may authorize rule-based,
scenario-ID-free repair. A projection-only gap authorizes evaluator/projection
repair but not parser or policy changes. If all fresh product semantics pass,
no product runtime change is authorized. Temporal/normalization repair requires
independent valid fresh misses.

## Bounded worker surface

DeepSeek Flash may create/edit only:

- `app/services/bernie/lc4v8d1_development_evidence.py`;
- `tests/test_bernie_lc4v8d1_development.py`; and
- `orchestration/agent_inbox/claude/lc4v8d1-deepseek-candidate.md`.

The worker may read only this contract, the fresh D1 fixture/authorship test,
the incident record, `semantic_extraction.py`, `language_normalization.py`,
`lc4v4d3_policy_resolution.py`, `lc4v7d1_development_evidence.py`, and named
ordinary tests in its packet. Every V8 protected path and every product parser/
policy edit is forbidden. A conceptual evidence-integrity defect moves directly
to Sol recovery without a Flash correction loop.

## Exit and closed surfaces

D1 exits only with zero authoring invalidity and variance, explicit aggregate
and selection hashes, risk-proportional serial preservation, and Gemini
`DECISION: pass` on the exact accepted head. It is ordinary development
evidence, not V8 recertification and not authority for V9.

T3.1-T3.4 remain intact and blocked. T3.5/providers, historical data,
runtime/product wiring, routes, APIs, UI, database, deployment, release, and
all live/write authority remain deferred.
