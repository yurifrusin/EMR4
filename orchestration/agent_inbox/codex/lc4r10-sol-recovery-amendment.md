# LC4R10 Sol recovery amendment

## Recovery authority

The Flash candidate failed conceptually, so the zero-conceptual-correction-loop
rule applied. GPT Sol invoked the documented recovery lease, preserved the
worker failure, and directly owned every amendment below.

## Sol amendments

- corrected the Sol contract's pre-policy `20/33` arithmetic to the accepted
  post-policy `22/31` split;
- replaced the worker's 93-ID literal with independently hash-validated 53/40
  source selections and a disjoint 93-record union;
- restricted resolved-dialogue semantic overrides to the exact 53 records;
- derived create/move/resize/cancel temporal, normalization, entity, duration,
  clarification, outcome, tool, appointment-delta, and audit-delta contracts
  from the four authored dialogue templates and diary policy;
- represented fail-closed and withdrawn behavior as a required explicit null
  outcome with zero appointment/audit deltas;
- allowed T1-backed create outcomes for `same_day_distinct` and terminal prior
  appointments while retaining stale, concurrent, roster, break, no-slot, and
  elapsed-window fail-closed behavior;
- preserved `action_negated` when the scaled evaluator rebuilds observations
  for repeat samples, closing the revealed simulated-write safety regression;
- regenerated only the 53 source-selected group files plus the manifest, rather
  than changing all 96 groups;
- replaced the worker report/check with independent recomputation and
  byte-for-byte generator verification.

## Recovered evidence

- frozen selections: 53 `9496e23c6f339603`, 40 `defe4c59877753e9`, combined
  93 `d8d138cb267b4304`;
- all 93/93 pass every composed semantic, clarification, outcome, tool, delta,
  authority, and safety dimension;
- full semantic counts: `880/814/672/154/330/835`;
- safety: 1,152/1,152;
- variance: zero over 2,304 samples;
- focused Sol tests: 20/20 passing;
- generator/report check: passing and byte-for-byte reproducible.

The recovery changes no parser/extraction behavior, protected evidence, T3
provider boundary, route/API, database, UI, deployment, or write authority.
