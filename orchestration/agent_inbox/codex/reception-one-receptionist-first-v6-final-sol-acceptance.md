# Sol Acceptance — Reception One Receptionist-first v6 Final Result

Status: accepted as a fail-closed development result
Recorded: 2026-07-30

The terminal v6 evidence is accepted as
`reception_one_receptionist_first_v6_paired_development_fail_closed`.

The full twenty-four-case sequence consumed twenty-seven calls, stayed within
the exact provider/model/project/identity/region and USD 1 / 48-call boundaries,
used no API key or fallback, released no write, consumed every ledger and left
no task-scoped residue.

The capability threshold does not pass. Nine cases matched their exact oracle.
One pre-schema provider response released nothing. Fourteen semantically wrong
but structurally valid forms were admitted, principally because the model
re-litigated the grounded selected-appointment binding and the proofreader
checked internal agreement rather than explicit intent compatibility.

The natural receptionist channel is useful audit evidence, but its agreement
with the typed form is not independent semantic proof. The accepted next
candidate is a separately versioned targeted development repair over the
fifteen misses. The v6 prompt, ledgers, audits and results must remain immutable,
and no targeted repair may be called a holdout.
