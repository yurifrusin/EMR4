# Model-required Bureau A4 Sol acceptance

Date: 2026-08-05

Decision: `accept`

Accepted result: `model_required_bureau_a4_product_read_ui_pass`

Source HEAD: `fb3cf995e03d8500c88fca7484fa04aeb0b698d9`

## Basis

Sol accepts the complete A4 candidate against
`docs/emr4-model-required-bureau-a4-product-read-ui-plan.md` and its threat-model
delta.

- A4.1 passes its dedicated Receptionist-only authorization, GraphQL, database-
  role, minimal projection, live-local HTTPS browser/FastAPI/PostgreSQL and UI
  lifecycle gates. Database truth is unchanged and owned cleanup is complete.
- Yuri's direct browser evidence confirms the visible 30-second warning, expiry
  clearing and restored Escape behavior.
- A4.2 used the exact Sydney Vertex/Bernie/keyless boundary. The first candidate
  failed grounding and released nothing. The single planned materially distinct
  correction passed a fresh exact source veto and the unchanged deterministic
  proofreader.
- The cumulative provider ledger is consumed at exactly two calls/USD 0.50. No
  call followed admission; raw model content and credentials were not retained.
- The final model-selected view passed the loopback HTTPS Diary harness with one
  card, exact provenance, refresh, Escape, reopen, empty console/page/HTTP/
  request-failure arrays and complete server/TLS cleanup.
- Expiry was never extended or ignored. Provider-free freshness revalidation
  proved the unchanged frame retained the exact appointment, practitioner and
  waiting area; it changed no selection and made zero provider calls.
- The independent final veto passed 116 tests, both occupied audit chains, 27
  artifact hashes and 33 source hashes with no findings. Receipt:
  `orchestration/agent_inbox/codex/model-required-bureau-a4-final-independent-review-receipt.json`,
  SHA-256
  `21465d4868925963735c1be7bac9b7a489322f8edd8be0f8617882452afd631d`.

## Boundaries retained

This accepts authored-synthetic development product-read/UI evidence only. It
does not accept real patient/clinical/product-derived data, patient-facing use,
commands or writes, provider tools, production, deployment, release, Pages,
protected evidence or protected-ref movement. It does not claim Australian
physical or sovereign processing.

`docs/branding/` and the existing Consultant, Gate-minus-one and A3/B3 pre-push
receipt/state files remain user-owned, untracked and excluded.

## Next baton

After explicit-path task-branch publication, rehydrate and freeze the paired
A5.1 Rayleen check-in/status and B4.1 Davida default-location command-runtime
descendant. Standing programme authority permits that dependency-satisfied
continuation without another ceremonial permission pause. The new descendant
must separately freeze its command/write, confirmation, idempotency, atomic
audit/outbox, replay and deterministic readback boundaries before acting.
