# Inert durability DDL PostgreSQL-representability recovery plan challenge

Date: 2026-08-07

Candidate HEAD: `d7757e19153a1e635520da059bd67c7f9dac2c3f`

Review worktree: `C:/Users/sarashera/EMR4-worktrees/r39`

Review branch:
`codex/review-durability-inert-ddl-postgresql-recovery-d7757e19`

Model allocation: one fresh Antigravity project, exact
`gemini-3.6-flash-high`, explicit high reasoning.

## Authority and containment

This is a read-only pre-implementation architecture veto. Do not edit, create,
stage, commit, fetch, push, install, write caches, access the main worktree,
open a database/source/outbox/feed/watcher/listener/product runtime, execute or
apply SQL/DDL, create a migration, touch credentials, call any other provider
product path, or access patient/product/protected/historical-PHI data. Sol
retains recovery meaning, implementation, acceptance and integration.

The first DeepSeek implementation and its sole correction are rejected
evidence. Do not treat either candidate or worker receipt as authority.

## Exact allowlist

Read completely from `r39`:

- `AGENTS.md` sections 2-7 only;
- the inert DDL plan, design, original plan recovery, new PostgreSQL-
  representability recovery and threat-model delta;
- both focused inert-DDL plan tests;
- the exact accepted structural and body contract JSON files;
- the accepted body trigger-program builder only to locate the six frozen
  trigger-row `xmin` references; and
- the accepted parent closeout and Sol acceptance files.

Do not enumerate or read other paths.

## Required independent challenge

Decide whether the recovery is machine-closed, PostgreSQL-16-representable and
no wider than the accepted inert renderer gate.

1. Reproduce that the immutable body parent has 22 programs, 13 trigger
   functions/declarations and exactly six `TRIGGER_COLUMN` expressions naming
   `xmin`. Identify their body/node paths and classify them as immediate or
   deferred.
2. Challenge the sole effective-body delta: one `BEFORE UPDATE` appointment
   guard, 9/14/14/23 effective populations, exact pre-effect `xmin`
   reselection in four immediate guards, and narrow paired-guard dependencies
   for the three deferred expressions. Report any way an old/current row could
   evade the intended second-update, event-delete or outbox-delete fence.
3. Challenge appointment applicability. Zero active producer bindings must be
   inert for ordinary writers, one must activate exact proof and more than one
   must fail `F_CARDINALITY`/`CF004` without choosing a stream. Determine
   whether this can be rendered without a race, hidden helper or new runtime
   privilege.
4. Verify exact keyed reselects are possible before each update/delete and do
   not rely on `OLD.xmin`, `NEW.xmin`, `ctid`, caller input, dynamic SQL or an
   application DML privilege. Challenge the listed application and outbox keys.
5. Verify PostgreSQL-16 form/order for null-safe array count, support-helper
   dependency, `CREATE CONSTRAINT TRIGGER` deferrability, schema authorization,
   fabric type/relation ownership transfers and signature-specific function
   ownership. Identify any final-owner or installation-executor authority gap.
6. Verify the manifest/static recognizer can position-close every recovery
   operation, effective population, owner, dependency, trigger kind and paired
   guard while preserving the immutable parent and fixed `.sql.inert` output.
7. Try at least six concrete counterexamples: empty set, policy before helper,
   ordinary trigger used for a deferred fence, direct `OLD.xmin`, absent
   appointment guard, and migration/runtime ownership conflation. Add any
   stronger counterexample you find.
8. Reconfirm no database parse/execution, source/runtime/data opening, API or
   Diary change, migration, deployment, Pages or protected-ref authority.

Report every P0-P3 finding with exact path/section and the smallest sufficient
correction. Passing tests do not override a conceptual defect. Do not repair.

## Allowlisted commands

```powershell
$env:PYTHONDONTWRITEBYTECODE='1'
$env:RUFF_NO_CACHE='true'
git rev-parse HEAD
git status --short
C:/Users/sarashera/emr4/.venv/Scripts/python.exe -m pytest -q -p no:cacheprovider tests/test_raisa_provider_free_unmounted_durability_inert_ddl_rehearsal_plan.py tests/test_raisa_provider_free_unmounted_durability_inert_ddl_postgresql_representability_recovery.py
C:/Users/sarashera/emr4/.venv/Scripts/python.exe -m ruff check tests/test_raisa_provider_free_unmounted_durability_inert_ddl_rehearsal_plan.py tests/test_raisa_provider_free_unmounted_durability_inert_ddl_postgresql_representability_recovery.py
git diff --check HEAD^ HEAD
git status --short
git rev-parse HEAD
```

Read-only `Get-Content`, exact-path `rg`, `git show`, `git diff` and short
in-memory Python expressions over the two allowlisted contract JSON files are
also allowed. Do not create files or caches.

## Terminal response

Return one schema-constrained object only, with `decision` exactly `pass` or
`revision_required` and all evidence/findings in `review`. Include exact
population/path reconciliation, paired-guard/applicability judgment, ownership
judgment, hostile counterexamples, tests and clean exact-HEAD postflight. Do
not self-repair.
