# Sol review — Reception One Bureau live isolated-planner evaluation

Verdict: accept the failed-closed closeout; do not accept the occupied product
objective as passed.

## Independent findings

1. The browser submitted one `isolated_vertex` route request without
   interception.
2. The broker made exactly one call using Gemini 2.5 Flash, project
   `bernie-emr4-dev`, the exact Bernie service account, keyless impersonated
   ADC, `australia-southeast1` and the regional Vertex hostname.
3. Vertex returned HTTP 200. The ledger and both durable hash chains are valid.
4. The retained context review records `selected_appointment_absent`.
5. The model produced a clarification form; the proofreader rejected its goal
   against the recognized resize intent. No field was released.
6. The new one-call guard terminated at
   `terminal_call_ceiling_no_release`; there was no retry or fallback.
7. Database before/after hashes match. Post-run and independent residue checks
   show no owned runtime resource.

## Assessment

The proofreader behaved correctly because a resize cannot safely target an
unselected appointment. The natural receptionist response was also reasonable
for the context it received. The acceptance harness—not the provider request
contract—failed to perform the explicit UI selection already demonstrated by
the provider-free extended-proposal harness.

The occupied objective therefore remains unmet, while the safety, isolation,
audit and call-ceiling objectives pass.

## Required next gate

Repair and exercise the appointment-selection path provider-free. A future
provider retry requires fresh authority because the sole call in the current
authority has been consumed.
