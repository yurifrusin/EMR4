# Disposable PostgreSQL exact-catalogue correction veto

Date: 2026-08-07

Candidate HEAD: `c5f0960a240b7f162b1b34e1b09fb166d12fd42e`

Review worktree: `C:/Users/sarashera/EMR4-worktrees/r42`

Review branch: `codex/review-disposable-pg-catalogue-c5f0960a`

Model allocation: one genuinely fresh Antigravity project, exact
`gemini-3.6-flash-high`, explicit high reasoning.

## Authority and containment

This is a read-only catalogue-delta veto after Sol rejected the prior
reviewer's acceptance. Do not edit, create, stage, commit, fetch, push, install,
write caches, access the primary worktree, contact Docker or any database,
inspect/list Docker state, execute/apply SQL, create a migration, touch
credentials, call any other provider product path, or access
patient/product/protected/historical-PHI data. Sol retains implementation,
runtime ownership, recovery, acceptance and integration.

## Exact allowlist

Read completely from `r42` only:

- `AGENTS.md` sections 2-7;
- `docs/raisa-provider-free-disposable-postgresql-durability-parse-catalogue-rehearsal-plan.md`;
- `docs/raisa-provider-free-disposable-postgresql-durability-parse-catalogue-rehearsal-design.md`;
- `docs/raisa-provider-free-disposable-postgresql-durability-parse-catalogue-rehearsal-plan-recovery.md`;
- `docs/security/raisa-provider-free-disposable-postgresql-durability-parse-catalogue-rehearsal-threat-model-delta.md`;
- `tests/test_raisa_provider_free_disposable_postgresql_durability_parse_catalogue_rehearsal_plan.py`;
- `orchestration/continuity/raisa-provider-free-unmounted-durability-inert-ddl-rehearsal/render-manifest.json`;
- `orchestration/continuity/raisa-provider-free-unmounted-durability-inert-ddl-rehearsal/lowering-contract.json`;
- `orchestration/agent_inbox/antigravity/raisa-context-fabric-durability-disposable-postgresql-plan-recovery-review-receipt.json`;
- `orchestration/agent_inbox/codex/raisa-context-fabric-durability-disposable-postgresql-plan-recovery-review-sol-rejection-receipt.json`; and
- `docs/ariadne-agent-error-correction-register-revision-88.md`.

Do not enumerate or read any other path. Do not access protected evidence.

## Mandatory exact-catalogue challenge

The previous `pass` has no acceptance authority. Reproduce rather than inherit
its claims.

1. Mechanically group every exact `render-manifest.json` `ordered_nodes` row by
   its `kind`. Reproduce and report `DOMAIN=4`, `ENUM=19`, `COMPOSITE=9`,
   `TYPE_OWNER=32`, and verify the plan/design now state 4/19/9/32 exactly.
2. Extract and compare every exact identifier in the four domain, nineteen
   enum and nine composite populations. Confirm none is omitted, duplicated or
   assigned to the wrong kind. The aggregate 32 is not a substitute for the
   required subdivision and identities.
3. Reconcile the whole ordered-node kind population, including at least ROLE,
   SCHEMA, TABLE, CONSTRAINT, UNIQUE_INDEX, SUPPORT_FUNCTION, RLS_ENABLE,
   RLS_FORCE, RLS_POLICY, RELATION_OWNER, ENTRY_POINT, TRIGGER_FUNCTION,
   TRIGGER_DECLARATION, REVOKE and GRANT. Report any plan claim that disagrees
   with the manifest even if outside the corrected type split.
4. Confirm the correction changes only the exact catalogue population and
   acceptance state. Recheck rollback-before-success, database-local fabric
   absence plus cluster-wide role absence, and exact `psql --file=-`,
   `ON_ERROR_STOP=1`, `--single-transaction` mechanics for accidental
   regression.
5. Challenge at least ten hostile mutations, including 4/17/11, aggregate-only
   32, omitted enum, duplicated composite, misclassified domain, success before
   rollback, database-local-only role proof, plain stdin, omitted `--file=-`,
   and any widening into Docker/database execution or behavior claims.
6. Confirm Docker/database contact is still forbidden until this veto is
   admitted and that application migration/runtime/source/data/provider,
   deployment, Pages and protected refs remain closed.

Report every P0-P3 finding with exact path/section and the smallest sufficient
correction. Passing tests do not override a conceptual or identity-population
defect. Do not repair.

## Allowlisted commands

```powershell
$env:PYTHONDONTWRITEBYTECODE='1'
$env:RUFF_NO_CACHE='true'
git rev-parse HEAD
git status --short
C:/Users/sarashera/emr4/.venv/Scripts/python.exe -m pytest -q -p no:cacheprovider tests/test_raisa_provider_free_disposable_postgresql_durability_parse_catalogue_rehearsal_plan.py
C:/Users/sarashera/emr4/.venv/Scripts/python.exe -m ruff check tests/test_raisa_provider_free_disposable_postgresql_durability_parse_catalogue_rehearsal_plan.py
git diff --check HEAD^ HEAD
git status --short
git rev-parse HEAD
```

Read-only `Get-Content`, exact-path `rg`, `git show`, `git diff` and short
in-memory expressions over the exact allowlisted JSON are also allowed. Do not
create files or caches.

## Terminal response

Return one schema-constrained object only, with `decision` exactly `pass` or
`revision_required` and all evidence/findings in `review`. Include the exact
4/19/9/32 counts, exact identifier reconciliation, whole-kind population,
hostile mutations, unchanged rollback/psql/protected boundaries, tests and
clean exact-HEAD postflight. Do not self-repair.
