# Sol acceptance — admission-replay parse characterization rebind

Date: 2026-08-08

Decision: accepted for exactly one disposable parse/catalogue characterization.

Exact candidate `f5c8fb0f01dc5836647b90acdf96c8ed6c21fc05` binds the
independently accepted admission-replay recovery source, exact 424-statement
artifact, 1,436,664 LF bytes and SHA-256
`dc475f71005a2b5a37de829e7f5e21be425dc970091e5b5567099cf2449142d7`.
Its canonical contract SHA-256 is
`942ed41554fee46b3c4d11e663afd5e62c3c5e00250fc21fabb3e06009c98726`.

Fresh Gemini 3.6 Flash/high review receipt SHA-256
`9f15818e73e788369cc7c3b7c16242cdf12b5cffc566c26a09f1c580f24d0330`
returns exactly `pass` with identical clean pre/post HEAD. It verified that
`characterization_only` has an empty digest map, cannot self-accept the new
catalogue, preserves the reviewed SQL and authority boundaries, and retains
the fixed pull-never, networkless, portless, mountless, tmpfs-only and exact-
ID cleanup controls.

AER-0236 records that the first local preflight used an abbreviated commit ID;
the strict helper rejected it before any provider/database action. The fresh
full-HEAD preflight and postflight pass. This register-only correction does not
alter the reviewed harness candidate.

This acceptance permits exactly one no-argument run in one newly owned local
disposable PostgreSQL 16 container using only repository-authored synthetic
prerequisites and the exact inert artifact. The result must be preserved under
a new immutable name, the historical mutable parse alias restored byte-exact,
and exact container absence verified. It grants no rerun, exact-digest
acceptance, behavior attempt, operational database, watcher/feed,
application/API/Diary wiring, product/patient/clinical data, provider,
deployment, release, Pages or protected-ref authority.
