# Inert durability DDL PostgreSQL-representability implementation veto

Date: 2026-08-08

Candidate HEAD: `46e16622471a192353cb82a33acf301dc2cfb7aa`

Review worktree: `C:/Users/sarashera/EMR4-worktrees/r40`

Review branch:
`codex/review-durability-inert-ddl-postgresql-recovery-46e16622`

Model allocation: one fresh Antigravity project, exact
`gemini-3.6-flash-high`, explicit high reasoning.

## Authority and containment

This is a read-only exact-commit implementation veto. Do not edit, create,
stage, commit, fetch, push, install, write caches, access the main worktree,
open a database/source/outbox/feed/watcher/listener/product runtime, execute or
apply SQL/DDL, create a migration, touch credentials, call another provider
product path, or access patient/product/protected/historical-PHI data. Sol
retains recovery meaning, implementation, acceptance, integration and Git.

The two DeepSeek candidates are rejected historical evidence. The recovered
Sol candidate at the exact HEAD above is the only review subject. Passing tests
do not override a source, PostgreSQL-semantics, authority or evidence defect.

## Exact allowlist

Read completely from `r40`:

- `AGENTS.md` sections 2-7 only;
- `docs/raisa-provider-free-unmounted-durability-inert-ddl-rehearsal-plan.md`;
- `docs/raisa-provider-free-unmounted-durability-inert-ddl-rehearsal-design.md`;
- `docs/raisa-provider-free-unmounted-durability-inert-ddl-rehearsal-plan-recovery.md`;
- `docs/raisa-provider-free-unmounted-durability-inert-ddl-rehearsal-postgresql-representability-recovery.md`;
- `docs/security/raisa-provider-free-unmounted-durability-inert-ddl-rehearsal-threat-model-delta.md`;
- `scripts/raisa_provider_free_unmounted_durability_inert_ddl_rehearsal.py`;
- the four exact files under
  `orchestration/continuity/raisa-provider-free-unmounted-durability-inert-ddl-rehearsal/`;
- `tests/test_raisa_provider_free_unmounted_durability_inert_ddl_rehearsal.py`;
- `tests/test_raisa_provider_free_unmounted_durability_inert_ddl_rehearsal_plan.py`;
- `tests/test_raisa_provider_free_unmounted_durability_inert_ddl_postgresql_representability_recovery.py`;
- the two exact accepted structural/body contract JSON files; and
- the accepted pure body builder only where needed to challenge effective-body
  construction.

Do not enumerate or read other paths.

## Required independent veto

1. Recompute the immutable and effective populations: 22 immutable programs,
   23 effective programs, 9 entry points, 14 trigger functions and 14 trigger
   declarations. Confirm the sole addition is the immediate appointment guard.
2. Recompute all six immutable trigger-row `xmin` sites. Confirm the effective
   body contains no `OLD.xmin`/`NEW.xmin`, performs four exact keyed physical-row
   reselections before effect, and removes or narrows only the three deferred
   expressions through the three recorded same-table paired-guard dependencies.
3. Challenge the appointment guard and fence using zero, one and duplicate
   active producer bindings. Ordinary writers must be inert; one exact binding
   must run proof; duplicates must raise `F_CARDINALITY`/`CF004` without stream
   choice. Challenge identity-change, second-update and deferred-final-state
   paths for an evasion.
4. Recompute all eight recovery operation seals from the immutable and
   effective fragments. Verify affected IDs, source/effective expression seals,
   four reselect-node seals and exact order are position-closed by the lowering
   contract and whole-contract schema. Report any self-consistent regeneration
   path that could hide a changed transform.
5. Inspect the actual SQL bytes for PostgreSQL-16 representability: null-safe
   array count, support helper before every policy, seven ordinary guards,
   seven `CREATE CONSTRAINT TRIGGER` deferred fences with correct clause order,
   fixed search paths, exact function owners and no invalid trigger grammar.
6. Reconcile final ownership: schema authorization, 32 fabric type/domain
   transfers, 18 fabric relation transfers, 24 function owner statements,
   no application owner change, no runtime schema `CREATE`, and no trigger
   function runtime execute grant.
7. Challenge the static recognizer and tests with at least these hostile cases:
   bare `array_length`, helper/policy reversal, wrong trigger kind, direct
   `OLD.xmin`, missing appointment guard, missing/wrong owner, application owner
   mutation, unknown lock mode and recovery seal drift. Identify any mutation
   admitted without a specific deterministic failure.
8. Reconfirm fixed-path determinism and evidence calibration. This may prove
   only a byte-stable inert PostgreSQL-shaped artifact; it must not claim server
   parse, catalogue acceptance, trigger behavior, RLS, concurrency or migration
   safety.
9. Reconfirm no database parse/execution, source/runtime/data opening, API or
   Diary change, migration, deployment, Pages or protected-ref authority.

Report every P0-P3 finding with exact path/line and the smallest sufficient
correction. Do not repair.

## Allowlisted commands

```powershell
$env:PYTHONDONTWRITEBYTECODE='1'
$env:RUFF_NO_CACHE='true'
git rev-parse HEAD
git status --short
C:/Users/sarashera/emr4/.venv/Scripts/python.exe scripts/raisa_provider_free_unmounted_durability_inert_ddl_rehearsal.py check
C:/Users/sarashera/emr4/.venv/Scripts/python.exe -m pytest -q -p no:cacheprovider tests/test_raisa_provider_free_unmounted_durability_inert_ddl_rehearsal.py tests/test_raisa_provider_free_unmounted_durability_inert_ddl_rehearsal_plan.py tests/test_raisa_provider_free_unmounted_durability_inert_ddl_postgresql_representability_recovery.py
C:/Users/sarashera/emr4/.venv/Scripts/python.exe -m ruff check --no-cache scripts/raisa_provider_free_unmounted_durability_inert_ddl_rehearsal.py tests/test_raisa_provider_free_unmounted_durability_inert_ddl_rehearsal.py tests/test_raisa_provider_free_unmounted_durability_inert_ddl_rehearsal_plan.py tests/test_raisa_provider_free_unmounted_durability_inert_ddl_postgresql_representability_recovery.py
git diff --check d7757e19153a1e635520da059bd67c7f9dac2c3f HEAD
git status --short
git rev-parse HEAD
```

Read-only exact-path `Get-Content`, `rg`, `git show`, `git diff` and short
in-memory standard-library Python expressions over allowlisted JSON are also
allowed. Do not create files or caches.

## Terminal response

Return one schema-constrained object only, with `decision` exactly `pass` or
`revision_required` and all evidence/findings in `review`. Include exact
population/seal reconciliation, PostgreSQL/authority judgment, hostile
counterexamples, test results and clean exact-HEAD postflight. Do not
self-repair.
