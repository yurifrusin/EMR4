# Independent audit — Reception One shared typed occupied 002

## Disposition

`fail_closed_proofreader_rejection`

The consumed ledger records exactly one provider call. The request used
`gemini-2.5-flash`, project `bernie-emr4-dev`, the exact Bernie impersonated
service account, keyless ADC and
`australia-southeast1-aiplatform.googleapis.com`. It received HTTP 200 in
1764 ms with 1815 prompt, 127 candidate and 1942 total tokens.

The agent's separately bounded note passed its independent gate:
“Review proposal for moving an appointment; no booking was changed.” It was
retained for audit only, was not parsed into the plan and was not delivered to
the product.

The schema-admitted PlanProgram then failed the deterministic proofreader with
`output_index_invalid` at the closed steps path. No operator was admitted, no
proposal or other value was released, no product or database access occurred
and no appointment write or confirmation was possible.

The former audit contract retained the PlanProgram hash but discarded its
closed integer fields with the raw response. Consequently the exact invalid
source coordinate cannot be reconstructed honestly. This does not weaken the
proofreader result. It does limit the precision of the diagnostic.

Cleanup and the durable audit hash chain pass. The cell, relay, internal
network, task images, temporary token, temporary context and broker process are
absent. No fallback occurred and no API-key authentication was used.

## Bounded repair finding

The deterministic defect class is sufficiently identified for one frozen-lane
repair: the request explained the prior-output arithmetic but did not give
concrete zero-based examples or a typed proofreader-feedback packet. Model
input v3 adds those examples and carries only the allowlisted violation code,
closed path code and prior program hash. It does not carry the rejected draft,
change the utterance or alter any provider, model, project, identity, region,
credential, data, isolation, audit, proofreader, output, cost or authority
boundary.

Future audit events retain the schema-admitted PlanProgram's integer-only form
without its operator note. This is the bounded form-fed typewriter record Yuri
requested. The natural-language note remains separately gated. Neither is a
command channel.

One actual provider call remains under the absolute two-call ceiling. It is
eligible only after focused regression, all provider-free gates, fresh
read-only controls, a new Continuity/Compass binding and a fresh single-use
ledger.

## Claim limit

This evidence proves one configured and observed Sydney locational request
path and a fail-closed proofreader boundary. It does not prove reliable model
interpretation, Australian physical or sovereign processing, product-data
safety, production fitness, deployment or release readiness.
