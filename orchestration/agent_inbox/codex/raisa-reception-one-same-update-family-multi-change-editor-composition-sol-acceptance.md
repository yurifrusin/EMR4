# Sol acceptance — Reception One same-update-family multi-change editor composition

Date: 2026-08-15

Timestamp: 2026-08-15T04:08:00+10:00 (Australia/Brisbane)

Decision: accept

Accepted reviewed source: `daed421954d65c159871585559f45caa32d95aee`

Result: `raisa_reception_one_same_update_family_multi_change_editor_composition_pass`

## Acceptance reasoning

All frozen interaction, authority, accessibility and evidence criteria are
satisfied. Time, duration and practitioner are three views of one local inert
draft. Same-family switching is request-free and preserves valid intent;
collapse, status crossing, reselection and interruption discard the whole
draft. Status never enters the update body.

One review sends all three effective values through one bounded client bridge
and one existing `handleMoveResize` call. The bridge freshly admits the target
practitioner and validates the combined interval. Every admissible proposal,
including a safe/no-warning proposal, stops before mutation at the existing
dialog. Only visible explicit confirmation can reach the existing update-
confirm route, and all terminal states reconcile fresh authoritative truth.

No second write surface, optimistic truth, sequential field writes or cross-
family transaction was introduced.

## Evidence reconciliation

Sol reproduced 70 focused route-intercepted UI tests, 173 widened Reception
One/API tests, 391 API Spine/latch/register tests and the canonical 196-test fast
profile with all static gates. After AER-0319 was added, the full 242-test
correction-register suite passed again. The final nine-module closeout packet
passed 356 tests.

Fresh Gemini 3.6 Flash/high independently returned `pass` after reproducing the
exact 173-test packet, Ruff, both JavaScript syntax checks and Git whitespace at
unchanged clean source `daed421954d65c159871585559f45caa32d95aee`.

AER-0312 through AER-0319 preserve and correct worker egress, module discovery,
schema vocabulary, tool routing, verifier-worktree and closeout-evidence
mapping/register-fixture/lifecycle-fixture defects. None is product evidence
and none altered the accepted candidate after the verifier pass.

## Authority finding

The change is limited to the native Diary client, cache references, bounded
route-intercepted tests and orchestration evidence. Existing authenticated,
practice-scoped update proposal/confirm authority, signed freshness,
idempotency, audit and backend transaction semantics remain unchanged.

No backend/API/schema/database/event expansion, real patient/product/clinical
data, model or provider product call, external patient/channel authority,
credentials/IAM/network, deployment, production, release, Pages or protected
ref moved. `docs/branding/` and every unrelated untracked file remain excluded.

The next safe descendant is a provider-free read-only post-editor Compass
orientation. It can select the next dependency-satisfied tranche but cannot
open a new command/event family, external channel or model authority. Standing
authority applies and no user-attention fork is presently identified.
