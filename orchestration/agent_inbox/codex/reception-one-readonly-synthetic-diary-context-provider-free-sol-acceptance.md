# Reception One read-only synthetic Diary-context provider-free acceptance

Status: `accepted_provider_free_readiness`

The existing trusted product-context adapter created an exact
`reception.one.bureau.plan-input.v1` frame from one owned disposable
authored-synthetic PostgreSQL fixture. Practice-scoped truth counts and the
readback hash were unchanged; the database was removed after the read. Raw
database UUIDs and the opaque-handle map did not enter the frozen v6.8 turn
packet.

The model-visible desk context contains only the authored-synthetic request,
current Diary source label, selected appointment, grounded mentions and
freshness metadata. It contains no full Diary or unselected appointment
payload. The frozen deterministic oracle admitted the resize plan and released
only a typed 45-minute proposal requiring human confirmation. Real isolation
and 51 focused product-context/API Spine tests pass.

The first provider-blocked attempts exposed three ordinary local assumptions:
the fixture appointment is 09:00, the relevant handle map has three entries,
and the frozen proofreader uses its authored-synthetic evidence clock. Each
attempt made zero provider calls and removed its exact disposable database.
The repair aligned the fixture and frame to those frozen facts without
changing the prompt, schema, proofreader, provider or authority.

This is not an occupied result. One primary Sydney Vertex call and one
terminal proofreader correction are authorised only after fresh cloud
preflight, Continuity/Compass binding and zero-residue evidence.
