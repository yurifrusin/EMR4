# DeepSeek worker packet — delete-confirm PostgreSQL parse/catalogue rehearsal

Date: 2026-08-16

Timestamp: 2026-08-16T10:31:22+10:00 (Australia/Brisbane)

Model: DeepSeek V4 Flash/high through Claude Code `--bare`

Exact worktree source: `38f2fbb054736cbd63627daea6951d676907461c`

## Role and first actions

You are the bounded mechanical implementation/test worker. GPT Sol retains
architecture, occupied execution, recovery, acceptance, integration and Git
publication authority.

Before editing:

1. read `AGENTS.md` completely;
2. perform its complete five-source rehydration in your final report;
3. read the frozen plan and threat delta completely;
4. read exact migration `x3y4z5a6b7c8`, its accepted scaffold contract, and
   only the non-protected predecessor files named by the plan; and
5. verify your branch, exact HEAD and clean worktree.

Never enumerate, search or access a protected path. Do not use a conversation
summary as authority.

## Frozen implementation package

Create exactly these six semantic paths and no others:

1. `scripts/raisa_provider_free_disposable_postgresql_delete_confirm_scaffold_parse_catalogue_rehearsal.py`
2. `tests/test_raisa_provider_free_disposable_postgresql_delete_confirm_scaffold_parse_catalogue_rehearsal.py`
3. `tests/test_raisa_provider_free_disposable_postgresql_delete_confirm_scaffold_parse_catalogue_rehearsal_plan.py`
4. `orchestration/continuity/raisa-provider-free-disposable-postgresql-delete-confirm-scaffold-parse-catalogue-rehearsal/rehearsal-contract.json`
5. `orchestration/continuity/raisa-provider-free-disposable-postgresql-delete-confirm-scaffold-parse-catalogue-rehearsal/rehearsal-contract.schema.json`
6. `orchestration/continuity/raisa-provider-free-disposable-postgresql-delete-confirm-scaffold-parse-catalogue-rehearsal/provider-free-disposable-postgresql-evidence.schema.json`

Do not edit the plan, threat delta, migration, application, API, latch, AGENTS,
continuity graph, Compass, closeout or acceptance files. Do not create occupied
pass/failure evidence; Sol owns the one stateful run.

## Required mechanics

- Adapt the accepted fixed-path status-confirm containment pattern, but implement
  only the frozen empty-instance representation boundary.
- Accept no caller-selected arguments, paths, executable, image, database URL,
  port, container name or credential.
- Resolve local `docker.exe`; inspect exact cached `postgres:16-bookworm`; use
  `--pull=never`, `--network=none`, no port, one tmpfs data directory, one CPU,
  512 MiB, 128 PIDs and no restart.
- Use a unique closed-prefix container name, exact ownership and cleanup-nonce
  labels, capture the exact 64-hex container ID, inspect only exact targets and
  remove only that ID after complete ownership re-verification.
- Use `docker exec -i` and in-container Unix-socket `psql`, `shell=False`, bounded
  output and timeouts. Never list/prune/pull/build/login or touch Docker Desktop.
- Generate offline SQL only through `sys.executable -m alembic upgrade
  w2x3y4z5a6b7:x3y4z5a6b7c8 --sql` with the fixed synthetic dialect URL. Bind
  the exact range, source hashes, wrapper shape and required statements; reject
  database creation and destructive table statements.
- Install only the four empty prerequisite relations named in the plan, then
  stream the exact migration body with `ON_ERROR_STOP=1` and
  `--single-transaction`.
- Run read-only catalogue and zero-row queries only. Do not insert a user,
  capability grant, receipt, audit or appointment. Do not run update/delete,
  downgrade, lock, concurrency, restart or other behavior probes.
- Match every exact catalogue assertion in the frozen plan: columns, constraints,
  index, exact three functions, exact three non-system triggers, no extras in the
  owned families, head and four zero-row counts.
- Evidence contains only allowlisted object names, counts, digests and lifecycle
  states. Failure detail is hashed, not copied.
- Cleanup is unconditional in `finally`; ownership uncertainty refuses removal.
- Close both JSON Schemas with `additionalProperties: false` recursively where
  objects have fixed keys. The contract canonical digest is pinned in the
  harness. At least eighty hostile contract leaf mutations must fail closed.
- Tests must use fake runners/static inspection for all lifecycle/destructive
  paths. They must prove no shell, broad Docker discovery, caller override,
  behavior SQL, product row or forbidden source edit.

The contract must bind the exact plan and threat hashes:

- plan: `71486b39e69bdab10e23b22bafa9d48a348c816b0270ad4e11c38700dcd06b62`
- threat delta: `653658d228eecdd48389d7158cfa5b211f062a35ab9af0ba85a9db4a91a68edd`

and every exact source hash listed in the plan. Its source HEAD is the exact
worktree source above.

## API Spine rules

Classify this only beneath private REST/OpenAPI
`confirmAppointmentDeleteProposal`. Do not edit OpenAPI. GraphQL stays
read-only. Events stay non-authoritative acceleration hints. The rehearsal
creates evidence, not a product command receipt or authorization decision.

## Checks and commit

You may run pure/static focused tests without opening repository PostgreSQL and
must run Python compilation, Ruff on owned Python files, JSON/JSON-Schema checks
and `git diff --check`. Do not run Docker, the occupied harness, the repository
canonical profile or any provider other than your already-open worker transport.

Stage only the six explicit paths—never `git add .` or `git add -A`—and commit
them once. Do not push, fetch, merge, rebase, deploy or move a protected ref.

Final report: exact commit and parent, changed paths, checks, five-source
rehydration statement, actual-versus-planned parallelism, unresolved issues and
`DECISION: pass` or `DECISION: revision_required`. A self-pass is advisory only.
