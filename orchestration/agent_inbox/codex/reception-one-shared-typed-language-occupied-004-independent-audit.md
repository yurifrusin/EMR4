# Independent audit — Reception One shared typed occupied 004

## Disposition

`fail_closed_call_ceiling_exhausted`

The final ledger consumed the second and last actual provider call. It used
`gemini-2.5-flash`, project `bernie-emr4-dev`, the exact Bernie impersonated
service account, keyless ADC and
`australia-southeast1-aiplatform.googleapis.com`. It received HTTP 200 in
1624 ms with 2110 prompt, 124 candidate and 2234 total tokens.

The separately bounded natural-language channel worked as intended. The note
“Proposal for review: no booking was changed.” passed independent screening,
was retained for audit only, was not parsed into the program and was not
product-delivered.

The repaired audit also retained the schema-admitted integer-only form. Its
first four steps used valid bindings and prior-output coordinates. In the final
`prepare_move_proposal` step, however, source code `1040` decodes to step index
2, output index 8. That earlier `resolve_date_expression` operator exposes only
output index 0. The needed candidate-slot set would have to come from the later
`search_available_slots` output, but changing the model's chosen dependency is
a semantic plan repair, not an authorised mechanical normalization.

The deterministic proofreader therefore returned
`output_index_invalid`, admitted no operator and released nothing. No
appointment, proposal, command, product field or human-facing value crossed
the gate.

The audit hash chain has ten events and validates to terminal hash
`sha256:ef69bff4829b8df465f5a804798aa739e8e0956b876eff03cf84abae1b9830a9`.
Cleanup and independent residue checks pass. No fallback, API key, product or
database access, confirmation, appointment write or external state change
occurred.

## Call accounting

Lifecycles 001 and 003 closed at zero provider calls. Lifecycles 002 and 004
each consumed one actual call. The cumulative actual-call count is two, so no
further call is eligible under the frozen ceiling.

## Claim limit

This proves the configured and observed Sydney locational request path, two
bounded HTTP 200 responses, independently gated agent speech, integer-form
audit retention and fail-closed deterministic proofreading. It does not prove
a proofreader-admitted live planner, reliable model interpretation, Australian
physical or sovereign processing, product-data safety, production fitness,
deployment or release readiness.
