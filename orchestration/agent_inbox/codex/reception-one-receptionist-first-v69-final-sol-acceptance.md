# Reception One v6.9 untouched holdout — Sol review and acceptance

Disposition: `accepted`

The sealed holdout result is internally consistent and within authority. All
twelve expected safe outcomes passed under the byte-frozen v6.8 contract.
Thirteen calls were consumed against the absolute ceiling of twenty-four, with
one policy-eligible terminal proofreader correction. The local shell timeout
did not create a retry ambiguity: the first eight cases had complete immutable
evidence and consumed ledgers, and the resume path loaded them without provider
contact before running only the four remaining cases.

The exact provider, model, project, keyless impersonated service account,
Sydney location and regional hostname bindings are present in each admitted
case. Hash-bound typed inputs were shared by model and proofreader. All ledgers
are consumed, audit chains verify, cleanup is complete, and the evidence
retains no raw prompts, raw provider responses, credentials, token values,
API-key information or hidden thought content.

The accepted claim is deliberately narrow: this supports limited
authored-synthetic generalisation and the proposal-only architecture. It does
not establish production suitability, real-data authority or sovereign
locality. The pre-authorised next descendant is a minimal read-only
authored-synthetic Diary-context bridge; it must preserve backend ownership of
truth, source labels, freshness, opaque references, human confirmation and the
closed mutation boundary.
