# LC4V4D5 Sol Contract — Development-Wide Option A Adoption Audit

Date: 2026-07-16

Authority: GPT Sol owns taxonomy, evidence meaning, acceptance, recovery, and
protected integration. One DeepSeek V4 Flash/high lane through Claude Code
`--bare` may implement the frozen audit mechanics. A fresh Gemini 3.5 Flash
Antigravity project is the independent veto reviewer. DeepSeek Pro is not
authorized.

## Objective

Audit explicit Option A over all 60 ordinary LC4V4D1 development probes before
any broader development default or remediation. This tranche is diagnostic
only: it freezes and reports the complete legacy/Option-A difference surface,
preserves the accepted D4 20-case overlay, and separates expected versioned
changes from newly demonstrated adoption blockers.

Source baseline: `fbd29a5cf2e8ac0497271137eaf298b2804b4275`.

Frozen hashes:

- all-60 population:
  `sha256:ed65fe7821b0239066c532320bff05cc31a0699674987de8587efd74e05bbd44`;
- accepted D4 20-case selection/current policy population:
  `sha256:d3c6618cb586f5dc43824ec7a6e9e33957fee587a45c19f89e5a4bb425006e2a`;
- newly surfaced five-case difference selection:
  `sha256:b06da04e89b195b6de271b7ca4b8c22453426917b1d8c76389e4d41bf727aec7`;
- D4 report:
  `sha256:dd1ecc077a59bf05e777eda1f3a5450c0a1b97a4c8a3fd21dc0363d473abd653`;
- legacy 60-probe baseline:
  `sha256:665851ffe055efb40f2ba1e43291d6b945c4764b4f441837781d4fc964d6ff27`.

## Frozen baseline taxonomy

Sol's pre-plan execution of the accepted composed runner found:

- 60 total cases;
- 35 exact legacy/Option-A equivalences;
- 20 accepted D4 versioned changes;
- 5 additional differences;
- zero Option A repeat variance; and
- zero forbidden outcome/tool observations.

The five additional differences are frozen as:

1. `lc4v4d1_diary_exact_duplicate_02` — `expected_versioned_relation`.
   Replay and operational interpretation remain legacy-equivalent; the only
   difference is the newly explicit `diary_relation=exact_duplicate`.
2. `lc4v4d1_safety_move_safe_03` — `adoption_blocker_missing_mutation_deltas`.
   Outcome/tools remain correct but Option A drops the supported moved
   appointment/audit deltas and simulated-write marker.
3. `lc4v4d1_safety_cancel_safe_07` —
   `adoption_blocker_missing_mutation_deltas`, with an additional benign
   `exact_duplicate` relation.
4. `lc4v4d1_safety_status_safe_09` —
   `adoption_blocker_missing_mutation_deltas`, with an additional benign
   `exact_duplicate` relation.
5. `lc4v4d1_safety_resize_safe_05` —
   `adoption_blocker_target_field_conflict_and_missing_mutation_deltas`.
   The requested 45-minute duration is the mutation target, not conflicting
   diary identity evidence; Option A wrongly clarifies and drops the supported
   resize deltas.

The four adoption blockers are independently supported by the ordinary safe
surface, synthetic initial diary state, accepted action semantics, and the
already-supported legacy replay. They do not authorize parser changes. D5 may
recommend a separate bounded policy/replay remediation only after its audit is
accepted.

## Required evidence

Add a provider-free fail-closed D5 audit that:

1. dynamically authors and validates exactly 60 ordinary probes and both
   population hashes;
2. binds the accepted D4 report and legacy baseline hashes;
3. runs legacy and Option A twice for every case and retains complete typed
   observations/fingerprints;
4. dynamically verifies the current 20 D4 policy population and exact D4
   behavior rather than trusting a static tuple;
5. classifies every case into exactly one of:
   `legacy_equivalent`, `accepted_d4_versioned_change`,
   `expected_versioned_relation`, or one of the two frozen adoption-blocker
   classes;
6. proves exact counts `35/20/1/3/1` in that order;
7. proves the exact five-case selection and exact four-case blocker selection;
8. proves all three authoring-invalid D1 cases remain quarantined and
   legacy-equivalent;
9. proves zero variance and zero forbidden observations across all 120 Option A
   observations;
10. records no remediation, parser change, fixture rewrite, default-version
    switch, or product/write claim; and
11. makes every gate control a diagnostic decision and complete report hash.

Decision is `option_a_adoption_audit_valid_with_4_blockers` only when every
gate passes. Any missing, duplicate, extra, misclassified, partial, or
non-deterministic case returns `revision_required`.

## Verification and owned paths

Own only:

- new `app/services/bernie/lc4v4d5_adoption_audit.py`;
- new `tests/test_bernie_lc4v4d5_adoption_audit.py`;
- new `docs/bernie-lc4v4d5-option-a-adoption-audit.json` and `.md`;
- new `orchestration/agent_inbox/claude/lc4v4d5-deepseek-candidate.md`.

Do not edit the composed runner, D3 resolver, utterance parser, generic scorer,
D1-D4 fixtures/reports/tests/acceptances, `AGENTS.md`, protected evidence,
providers, routes, databases, UI, historical diary, deployment, release, or
write surfaces. Search and read only exact files named in the worker packet.

Acceptance requires exact population/hashes/counts, complete two-repeat
evidence, fail-closed tamper tests, serial D4/D5 and adjacent preservation
tests, `git diff --check`, and a fresh Gemini `DECISION: pass` on the exact
recovered report head.

## Closed gates

Holdouts v1-v4 remain sealed and unavailable. T3.1-T3.4 remain blocked;
T3.5/live providers, product/runtime wiring, API/UI/database/write authority,
historical diary, deployment, release, and certification remain deferred.
