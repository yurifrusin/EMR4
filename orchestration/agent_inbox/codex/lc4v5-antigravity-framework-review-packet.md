# LC4V5 Content-Blind Framework Independent Review Packet

Date: 2026-07-16

Reviewer: Gemini 3.5 Flash through a fresh Antigravity project.

This is a pre-content veto. No real LC4V5 scenario content, authoring script,
fixture, manifest, seal, production receipt, or report exists. Do not create any
of those surfaces.

## Exact review surface

- `orchestration/agent_inbox/codex/lc4v5-sol-contract.md`
- `orchestration/agent_inbox/codex/lc4v5-one-shot-acceptance-rule.md`
- `orchestration/agent_inbox/codex/lc4v5-sol-recovery-amendment.md`
- `orchestration/agent_inbox/claude/lc4v5-deepseek-framework-candidate.md`
- `app/services/bernie/lc4v5_holdout_framework.py`
- `tests/test_bernie_lc4v5_holdout_framework.py`
- ordinary dependency contracts only:
  `app/services/bernie/scenario_spec.py` and
  `app/services/bernie/composed_evaluator.py`

Do not discover, list, search, inspect, import, run, regenerate, evaluate, or
hash-check any v1-v4 holdout fixture, support/authoring module, test, manifest,
seal, receipt, filename, or per-case evidence. Do not use broad file discovery
under `tests` or holdout-named paths.

## Review questions

Return `DECISION: pass` only if the exact checked-out head proves all of these:

1. strict unknown-field rejection and canonical lossless hashing;
2. exact 24/12/288/72/216/576 population, 288 distinct coverage cells, all six
   actions, Gold/adjudicated synthetic declaration, and two typed repeats;
3. source/framework/evaluator/corpus/manifest/seal/report hash binding without a
   circular or mutable trust gap;
4. an exclusive persistent one-shot marker that denies rerun after success,
   exception, malformed evidence, or partial transition;
5. aggregate-only persisted output with no case IDs, utterances, exception text,
   or failure-selection artifact;
6. exactly the twelve frozen semantic dimensions and four failure layers, with
   absent/extra dimensions failing closed;
7. exact frozen thresholds: complete and every semantic dimension at least
   548/576, safety 576/576 and zero safety-layer failures, other layers at most
   28, every predefined slice at least 90%, worst slice at least 0.90, zero
   variance;
8. evidence invalidity takes precedence over product failure and never creates
   a certification claim; and
9. no runtime/default/provider/T3.5/API/UI/database/deployment/write authority is
   opened.

Run only the focused synthetic test plus the exact ordinary dependency tests if
needed. Treat the DeepSeek receipt as provenance, not proof. Inspect the Sol
recovery directly.

## Output

Write exactly one review artifact:

`orchestration/agent_inbox/antigravity/lc4v5-antigravity-framework-review.md`

Include exact reviewed Git head, commands/results, findings, and one final line:

- `DECISION: pass`, or
- `DECISION: revision_required`

Do not modify application code, tests, contracts, thresholds, or any other file.
