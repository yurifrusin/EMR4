# Sol acceptance — DeepSeek native Harness provider-free required-service injection recovery

Date: 2026-08-20

Timestamp: 2026-08-20T09:54:56.7001117+10:00 (Australia/Brisbane)

Decision: `accepted`

GPT Sol accepts exact reviewed candidate
`056c59d14de4efc302898e84ec7a69cbf729dfce` for the bounded provider-free
required-service injection recovery.

Acceptance basis:

- eight exact rc.7/Cordis/loader cached package identities and required members
  are bound by registry integrity and local member digests;
- the evidence reproduces the missing `agent-presets` row, inherited `tools`
  row, underdeclared loader/runner injection and Cordis inactive-until-complete
  rule;
- the exact future declaration is ordered `hmr`, `agentPresets`, `tools`, with
  the official `agent-presets` row configured to `default: standard`;
- all hostile patch and runner variants fail closed;
- both consumed native attempts remain byte-exact failed evidence;
- the corrected candidate passes 15 focused tests, while the independent
  manifest passes 86 exact provider-free tests plus Ruff, compilation,
  deterministic projection and Git cleanliness checks;
- the final Gemini 3.7 Flash/high receipt at SHA-256
  `2015153bbda12f644e05df9ad0c1a1c43168c05fbe1d294fd4f469026848358b`
  records one schema-admissible `pass`, ten exact zero-exit command results,
  unchanged candidate HEAD and a clean isolated worktree.

The initial `revision_required` review and the first corrected but
non-admitted egress remain immutable. Their two semantic defects and one C10
output-contract mismatch are qualifying incidents for clockwork-owned register
intake. They are not erased or reclassified by this acceptance.

Claim ceiling: this proves a deterministic future service-injection repair,
not a successful native DeepSeek worker. `emr4-bounded-worker` remains
unmaterialised, and no native Harness, Node, agent, broker, model, provider,
product, data, runtime, deployment, release, Pages or protected-ref authority
is granted.
