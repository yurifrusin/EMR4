# Sol acceptance: Raisa AES-C5 product-runtime admission

Date: 2026-08-11

Decision: `accepted`

Result: `raisa_agent_execution_surface_containment_gate_aes_c5_product_runtime_admission_pass`

Reviewed and occupied-evidence source HEAD:
`4e5d96ada19c51432fa4db46c76e23c952147c52`

## Basis

I accept AES-C5 as exactly one ordinary authenticated, practice-scoped product
route read over a fresh disposable PostgreSQL schema containing only the
frozen authored-synthetic practitioner population, followed by exactly one
broker-admitted Sydney Vertex `gemini-2.5-flash` request over the minimized
fresh ContextFrameSet.

The source and provider operations used separate immutable AES-C0 generations,
each with one grant, one destination, one broker-owned single-use lease, full
AES-C1 validation, current-authority recheck, pre-I/O budget commitment,
terminal exhaustion and revocation. No lease or budget transferred between
them. The provider generation was created only after the route response passed
auth/practice/field/freshness validation and minimization.

The one provider attempt returned HTTP 200/`STOP` in 2,713 ms with 451 prompt,
105 candidate, 391 reasoning and 947 total tokens. The deterministic
proofreader released only the exact digest-bound four-field booking-context
match with `command_authority: false`. No retry, repair, fallback, redirect or
provider tool occurred.

Both ledgers are consumed; all aliases, leases and tokens are revoked; product
and database counts are unchanged; the disposable schema is absent; and no
process, listener, reusable capability or temporary task root remains. The
evidence retains no sensitive values, names, UUIDs, credential, database URL,
raw prompt, provider text, raw response or reasoning.

## Verification and issue disposition

The recovered exact source passed one local route/PostgreSQL fake-provider
proof and a fresh Gemini 3.6 Flash/high veto with 71 focused tests, Ruff,
compilation and unchanged clean Git state. The subsequent official-fact and
sanitized read-only cloud/ADC gates passed before the sole occupied attempt.
Final closeout then passed 75 AES-C5 focused/continuity tests, the wider serial
route/API/baton/Compass/register packet, the 190-test canonical fast profile,
184 CI-static tests, Ruff, target-3.11 maintained-source compilation, Diary
JavaScript, historical-Diary leakage lint and Git whitespace. The CI wrapper's
literal Python 3.11 runtime assertion was unavailable on this Python 3.14-only
workstation; its remaining exact lint/static commands passed independently.

The initial worker's incompatible one-generation/two-destination composition
and related packet-shape weaknesses were never admitted. Sol recovered them
under the recorded recovery lease, and the fresh independent veto passed the
repaired source. AER-0262 through AER-0268 preserve the tranche's qualifying
orchestration corrections. The first gcloud CLI-store wrapper's routine stderr
warning was discarded as a wrapper failure; the corrected zero-inference
probe passed without printing or persisting a token or changing cloud state.

## Acceptance boundary

This acceptance proves only the exact authored-synthetic application-route,
minimization, provider, deterministic release, accounting and cleanup path. It
does not prove real-person or patient/clinical data safety, production identity
or RLS, reusable runtime, general model safety, semantic prompt-injection
detection, command safety, Australian physical/sovereign processing,
deployment, production or release.

No continuing product/database read, provider call, filesystem capability,
provider tool, generic network, credential/IAM change, command/write, Pages or
protected-ref movement is opened. AES-C5 completes the finite AES-C0 through
AES-C5 sequence. No AES-C6 is planned or authorized; any further descendant is
a new Yuri-owned programme choice.
