# Gemini 3.7 Flash/high veto — DeepSeek native Harness required-service injection recovery

Date: 2026-08-20

Timestamp: 2026-08-20T09:30:13.4920641+10:00 (Australia/Brisbane)

Perform one fresh read-only veto of exact candidate
`99ecaa6ac20a3b6394f4dd097550c7233a4a0436` against frozen plan source
`3e9c004e09b09c228937276f520d39e4b2c3d36b`.

## Exact candidate and authority

- Worktree: `C:\Users\sarashera\EMR4-worktrees\required-service-injection-gemini-99ecaa6a`
- Branch: `codex/review-required-service-injection-99ecaa6a`
- HEAD: `99ecaa6ac20a3b6394f4dd097550c7233a4a0436`
- Required model/effort: `gemini-3.7-flash-high` / `high`

Read `AGENTS.md` completely and name all five rehydration sources. Validate the
in-progress latch for
`deepseek-native-harness-provider-free-required-service-injection-recovery`.
Restore the explicit dispositions: DeepSeek declined because Harness is the
provider-free subject and no native/model execution is authorised; Gemini
dispatched for this exact-candidate independent veto; native subagents declined
under current developer policy and the small serial source-semantic boundary.

Read the active plan, threat delta, contract/schemas, evidence/report,
implementation/test, accepted failed native-boot closeout/evidence, accepted
preterminal-observability recovery and effective-tool guard. Do not inspect
`docs/branding/`, protected evidence or unrelated paths.

## Mandatory challenges

1. Confirm candidate/worktree identity and a clean tracked tree.
2. Confirm all eight exact cached package identities, tar/member digests and
   package identities are checked in memory without install, extraction,
   retained package source, Node, Harness or network access.
3. Challenge the exact diagnosis: base supplies the `tools` provider row;
   base + headless supplies no `agent-presets` provider; the accepted runner
   and loader row declared only `hmr`; and therefore the aggregate
   `SERVICES_UNAVAILABLE` necessarily included absent `agentPresets` while
   underdeclared dependencies also failed to gate `tools` activation.
4. Confirm the Web rc.7 bundle is used only as the official inert roster-row
   reference and specifies exactly `@deepseek-ai/dsh-agent-presets` with
   `default: standard`.
5. Confirm source proves `AgentPresets` provides `agentPresets`, requires
   `loader`, and requires `default`; `ToolRuntime` provides `tools` and requires
   `systemPrompt`; `composeProfile()` adds the shipped root only when an
   `agent-presets` row exists.
6. Confirm Cordis normalises module `inject`, its loader merges row `inject`
   into the fiber, and a fiber remains inactive while any declared provider is
   absent. Reject a claim based only on `ctx.get()` observation.
7. Confirm the generated patch adds exactly one official `agent-presets` row
   and declares ordered `hmr`, `agentPresets`, `tools`; the generated future
   runner changes only the matching export declaration and preserves every
   accepted activation/terminal coordinate and lifecycle byte.
8. Confirm hostile missing, reordered, surplus, renamed or duplicate services
   and rows fail closed; both consumed attempts remain exact and unchanged.
9. Confirm `emr4-bounded-worker` is not a shipped rc.7 preset and remains
   explicitly unmaterialised. Do not accept preset mount, scope creation,
   effective `edit`/`glob`/`read`, native boot, occupied worker, model quality
   or provider reliability.
10. Confirm Node/native-Harness, occupied-worker, agent/session/turn, broker,
    model/provider, network, Docker and database counts are all zero and that
    no product/API/config/client/feature flag/allowlist/grammar/waiting-area,
    ordinary-practice, data, production, deployment, Pages or protected-ref
    authority changed.
11. Execute exactly the ten-command manifest. C03 must pass 89 tests. Do not
    run `--publish`, Node, npm install, Harness, broker, Docker, PostgreSQL,
    DeepSeek or another provider. Every command must return zero and leave the
    worktree unchanged.

Do not edit, commit, push, install, access protected data, deploy, publish or
move protected refs. Return one schema-constrained `pass` only if the candidate
is exact, attributable, fail-closed and has no P0-P2 finding. Otherwise return
`revision_required` with exact evidence. Emit one terminal decision.
