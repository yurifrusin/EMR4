# Independent veto packet: unmounted Rayleen waiting-room Context Fabric source adapter

Date: 2026-08-06

You are the independent Gemini 3.6 Flash/high veto reviewer. Review only the
exact committed candidate below. Do not edit any file and do not implement a
repair.

## Exact candidate

- Worktree: `C:\Users\sarashera\EMR4-worktrees\context-fabric-rayleen-source-adapter-review`.
- Branch: `codex/review-context-fabric-rayleen-source-adapter`.
- Required HEAD: `3edbe828fa1f261e59b8478db79d80e4c291cbbc`.
- Frozen planning source: `1f008abf806c27c7e37251384f846a4a513dbad5`.
- The worktree must remain tracked-clean and unchanged.

## Review objective

Determine whether the provider-free unmounted source adapter safely transforms
one already-authorised serialized authored-synthetic Rayleen A4
`emr4.waiting_room_context_frame.v1` into the exact accepted Context Fabric
`current_waiting_room_projection` source envelope, without invoking a source,
opening authority or modifying the accepted Current-weave parent.

Review the complete diff
`1f008abf806c27c7e37251384f846a4a513dbad5..3edbe828fa1f261e59b8478db79d80e4c291cbbc`
and especially:

- the frozen plan, design and threat delta;
- both new adapter scripts and the focused test;
- the three files under the new continuity namespace;
- the exact accepted A4 waiting-room frame schema;
- the unchanged accepted Current-weave module, tests and evidence;
- the Sol recovery lease and DeepSeek timeout/late-partial failure receipt; and
- AER-0038, register revision 32 and its deterministic tests.

Provider-free test execution is permitted using
`C:\Users\sarashera\emr4\.venv\Scripts\python.exe` with the review worktree as
cwd. At minimum collect and run these exact files serially:

- `tests/test_raisa_provider_free_unmounted_rayleen_waiting_room_context_fabric_source_adapter.py`;
- `tests/test_raisa_provider_free_practice_context_fabric_current_operational_weave.py`;
- `tests/test_raisa_provider_free_practice_context_fabric_bureau_memory_contract.py`;
- `tests/test_model_required_bureau_a4_product_read.py`; and
- `tests/test_ariadne_agent_error_register.py`.

Adversarially verify:

1. the adapter accepts only a recursively closed serialized A4 frame and exact
   sealed parent binding/grant plus a recursively closed sealed alias manifest;
2. exact Receptionist/Rayleen/current-operational/source-triple, practice,
   location, session, time and all-false authority are admitted before source
   payload use;
3. fact/signal coordinates, two-minute maximum TTL, uniqueness, labels,
   elapsed minutes, threshold bands, missing-arrival/overdue exceptions and
   longest-wait ranks are recomputed and exact rather than trusted;
4. the alias manifest is complete and one-to-one, digest-bound to the exact
   source/binding/grant/session, and cannot select or leak unrelated or raw
   identifiers;
5. released content omits patient display tokens, raw UUIDs, waiting-area ids,
   source ids and scheduled/arrival timestamps while retaining only opaque
   bounded references and closed operational values;
6. expiry is the minimum of source, binding, grant and manifest, cardinality
   and canonical bytes are bounded, and no source lifetime is extended;
7. missing arrival never becomes guessed elapsed time; its exceptional frame
   composes only after deterministic scope omits unavailable derived fields;
8. the adapter-built positive envelope replaces only the hand-authored waiting
   source and the unchanged parent assembler/same-packet proofreader releases;
9. the accepted Current-weave source/evidence is byte-preserved and its
   historical equality test still passes;
10. acceptance evidence is closed, reproducible, hash-bound, 13/13, parent
    `RELEASE`, and every provider/network/database/product/watcher/command/
    deployment/protected count is zero;
11. the DeepSeek lease is correctly classified as a transport incident with
    quarantined late uncommitted partial state and no source adoption; and
12. no `app/**`, Diary UI, GraphQL/REST route, provider, database, watcher,
    event transport, command, deployment, production, release, Pages or
    protected-ref surface was added.

Look especially for seal verification gaps, KeyError/partial-release paths,
incorrect Python bool/int equivalence, incomplete raw-value leakage scans,
alias collisions, stale authority admission, output-size checks after release,
schema/evidence drift, or accidental reliance on the quarantined worker files.

Do not access ADC or cloud configuration; do not call Vertex; do not inspect
patient/clinical/product data, protected holdouts, historical Diary material or
`docs/branding/`; do not deploy, release, rebuild Pages or move any Git ref.

Return one concise structured review with every material finding identified by
file and line. If any material uncertainty remains, choose
`revision_required`. Do not edit the worktree and do not emit multiple or
conflicting terminal decisions.
