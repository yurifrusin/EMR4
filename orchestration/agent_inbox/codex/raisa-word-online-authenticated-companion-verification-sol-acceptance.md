# Sol acceptance — Raisa Word Online authenticated companion verification

## Disposition

Accepted as a terminal, evidence-bounded integration pass.

The task-specific `ReadDocument` manifest executed in one signed-in personal
Word Online session. After one fail-closed hosted-capability mismatch and a
focused exact-service repair, the post-repair exchange admitted one
authored-synthetic typed request, retained three appointment details in the
native Diary and released only the exact generic result count to Word.

The repair is acceptably narrow: only `smoke` and
`reception_one_companion_demo` become available on the exact origin-bound
public zero-authority policy. Provider, backend, credential, microphone,
command, document-write and production authority remain false. No other
service, IAM policy or organisation policy changed.

Observed taskpane and Diary resource timing shows zero API, credential and
provider requests. No document-body read/write, database action, appointment
command or confirmation occurred. Both task-created blank documents are in the
recoverable OneDrive Recycle Bin, the pre-existing named synthetic document is
untouched, and task-local residue is zero.

This acceptance does not establish EMR4 application authentication,
clinician-role authorization, organisational Office deployment, safety for
real or product-derived data, backend or provider readiness, processing
geography, production fitness or release readiness.

Continuity revision 180, Compass revision 161 and the rendered Compass report
validate exactly. The focused Word/host/Continuity suite passes 52 tests and
the relevant Word/API Spine regression suite passes 92 tests. JSON parsing,
Python compilation, Ruff, JavaScript syntax, Office manifest validation,
Cloud Run read-only posture and independent local residue checks pass.
