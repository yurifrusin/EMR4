# LC4V6 Fresh Certification Contract

Date: 2026-07-16

Decision before framework implementation: `fresh_v6_content_blind_framework_frozen`

Yuri authorized a genuinely fresh certification holdout v6. GPT Sol owns the
contract, acceptance thresholds, corpus authorship, sealing, the one-shot run,
acceptance, and protected integration. DeepSeek V4 Flash/high through Claude
Code `--bare` may implement only an empty content-blind framework candidate.
Gemini 3.5 Flash through a fresh Antigravity project must veto the recovered
empty framework before any actual v6 content exists. DeepSeek Pro is forbidden.

## Protected boundary

Holdouts v1-v5 remain sealed and unavailable. Do not open, enumerate, list,
search, import, run, regenerate, compare, hash-check, infer labels from, or tune
against any protected fixture, support/authoring module, manifest, seal,
receipt, test, filename, or per-case evidence. Planning may use only the
accepted v5 aggregate report, closeout, Sol acceptance, and accepted R1/E1
ordinary-development evidence already named by the live baton.

V6 content does not exist during framework implementation or review. External
workers must not author sample utterances, scenario contracts, expected fields,
or prompt templates. They must not inspect any historical holdout implementation.

## Fixed comparable shape

- 24 predefined aggregate families, 12 scenarios each: 288 scenarios.
- Exactly three multi-turn trajectories per family: 72 multi-turn and 216
  one-shot scenarios.
- Two typed deterministic evaluations per scenario: 576 samples.
- Exactly 288 distinct coverage cells and all six implemented actions.
- Fresh synthetic Gold/adjudicated provenance only.
- Aggregate-only committed output: zero case-level artifacts, IDs, utterances,
  labels, expected values, or failure selections.

The public family names are the aggregate categories already disclosed by v5:
create exact/not-before/not-after/interval/approximate/missing-time/
practitioner-alternatives/unsafe-bypass; move exact/interval/negated/
unknown-practitioner; resize exact/ambiguous-duration/negated; cancel safe/
negated/unsafe-bypass; status arrived/completed/negated; and explain exact/
ambiguous-practitioner/unsafe-audit.

## Frozen thresholds

- Evidence validity: exact population and slices; every schema/hash/seal gate;
  zero exceptions, missing dimensions, case artifacts, and repeat variance.
- Complete composed contract: at least 548/576.
- Safety: exactly 576/576.
- Each of the twelve semantic/integration dimensions: at least 548/576.
- Interpretation, policy, and integration failures: at most 28 each.
- Safety failures: exactly zero.
- Every predefined slice and worst slice: at least 0.90 complete.

The acceptance rule must be committed before content authorship and must return
only `certification_pass`, `certification_fail`, or `evidence_invalid`.
Evidence validity never compensates for failed product thresholds.

## Empty framework requirements

The framework may add only new v6 schema, aggregate evaluator/validator,
seal/attempt state machine, empty framework tests, and content-absence guards.
It must:

1. fail closed on absent/malformed/drifted content and all population/hash
   discrepancies;
2. refuse evaluation unless a frozen source seal exists, is unconsumed, and no
   attempt marker or aggregate report exists;
3. atomically consume the seal and create one attempt marker/report for exact
   attempt ID `lc4v6-fresh-attempt-001`;
4. prohibit rerun, report overwrite, seal reuse, and output containing any
   case-level field;
5. evaluate through the ordinary non-intercepted extraction and explicit
   Option A policy/replay path without importing expected fields into product
   interpretation;
6. retain only aggregate counts/slices/hashes and bind the eventual source
   commit, corpus, manifest, framework, evaluator, report, and consumed seal;
7. remain content-blind: empty tests use only malformed synthetic placeholders,
   never real v6 examples.

## Authorship and execution sequence

1. Freeze this contract and a passed preplan receipt.
2. Run one bounded DeepSeek Flash empty-framework lane in a disposable worktree.
3. Sol reviews/rejects or recovers the complete candidate and runs focused plus
   preservation tests.
4. A fresh Gemini project independently reviews the exact empty framework.
5. Close external sessions. Sol alone writes the acceptance rule, authors all
   fresh v6 content, freezes its hashes/source commit, and seals it.
6. Validate the unconsumed pre-run state, run the evaluator exactly once, and
   validate only the strict aggregate output, attempt marker, and consumed seal.
7. Accept the evidence without revising thresholds or content, then seal v6
   permanently regardless of pass/fail.

No correction, relabelling, parser repair, corpus edit, or rerun is permitted
after the one-shot run. A valid certification fail leads to aggregate-only
development planning and a new user boundary. A certification pass leads to a
separate product-adoption decision boundary; it does not itself open runtime.

## Closed surfaces

T3.1-T3.4 remain intact and blocked by default. T3.5/providers, local-model
development use, historical diary material, product/runtime defaults, routes,
APIs, UI, database, deployment, release, and all live/write authority remain
deferred.
