# Independent veto packet: C5 interpreter-binding repair descendant

Date: 2026-08-06

You are the independent Gemini 3.6 Flash/high veto reviewer. Review only the
exact committed candidate below. Do not edit any file and do not implement a
repair.

## Exact candidate

- Worktree: `C:\\Users\\sarashera\\EMR4-worktrees\\c5i-review`
- Branch: `codex/review-model-required-bureau-c5-interpreter-binding`
- Required HEAD: `dff672049ab5ce47058d7340525e63589fefc5c1`
- Rejected ancestor: `88b330870bd559b5276ae8191a41d152c48e9d7b`
- Candidate branch is non-protected and must remain clean and unchanged.
- Protected local/origin `master` and `handoff/current` must remain:
  `2e34bdad732fdab32fbf778280b3d3c70d66d602`.

## Review objective

Determine whether the new descendant closes your exact isolated-worktree
Python-path finding without weakening executable identity, source binding or
any accepted C5 boundary. Recheck the Windows endpoint-ownership and fresh
Ariadne-authority repairs because the candidate must pass as a whole.

Inspect at least:

- `scripts/model_required_bureau_c5_rehearsal.py`
- `scripts/model_required_bureau_c5_live.py`
- `scripts/model_required_bureau_c5_acceptance.py`
- `scripts/model_required_bureau_c5_contract.py`
- `tests/test_model_required_bureau_c5_live.py`
- `tests/test_model_required_bureau_c5_rehearsal.py`
- `tests/test_model_required_bureau_c5_contract.py`
- `orchestration/continuity/model-required-bureau-c5-disposable-live-development-recovery/provider-free-acceptance-evidence.json`
- `orchestration/agent_inbox/codex/model-required-bureau-c5-worktree-interpreter-binding-repair.md`
- the C5 plan, live pre-execution boundary, threat deltas, schemas and
  documentation-only OpenAPI artifact.

Adversarially verify:

1. `resolve_python_executable()` uses only the absolute process-owned
   `sys.executable`, requires an existing regular file, never searches `PATH`,
   reads `VIRTUAL_ENV`, scans sibling checkouts, accepts a caller path or uses
   an environment fallback.
2. The fixed launch vector and real `ProcessAdapter.preflight()` require the
   exact active controller interpreter and re-hash it immediately before each
   spawn; alternative, missing, relative or digest-drifted executables fail
   closed.
3. The target module remains pinned within the exact source worktree and hash
   bound. `-I`, `shell=False`, the minimal credential-free environment and
   exact 11-element argument vector remain mandatory.
4. Provider-free evidence uses `controller://active-python` only after runtime
   validation and persists no absolute checkout path. Its 24 artifact hashes
   and canonical set digest reproduce exactly.
5. Execute the full provider-free serial-orchestration test from this isolated
   worktree using the primary interpreter; the earlier missing local `.venv`
   failure must be gone.
6. Exact owned-process absence, Windows `SO_EXCLUSIVEADDRUSE` before bind, no
   `SO_REUSEADDR`, exact-port reacquisition, retained reservation through
   generation 2, bounded address-in-use-only retry, attempt accounting,
   readback, rollback and cleanup remain sound.
7. The custom pre-execution gate still requires and revalidates a fresh
   Ariadne runtime-state digest, exact event/action, five named sources,
   branch/HEAD/protected refs and no more than a 30-minute authority interval
   before any capability.
8. Sydney Vertex identity/routing/model/thinking, authored-synthetic envelope,
   no-retention, no-fallback, one primary plus at most one proofreader-ticketed
   correction, USD 0.50 ceiling, deterministic proofreader/authority and
   one-use evidence remain closed.
9. The API remains documentation-only and unmounted with `servers: []`,
   `paths: {}`, `security: []`; no product/database/runtime/patient-data,
   deployment, release, Pages or protected-ref surface opens.

Run this provider-free command from the exact review worktree:

`C:\\Users\\sarashera\\emr4\\.venv\\Scripts\\python.exe -B -m pytest tests/test_model_required_bureau_c5_live.py tests/test_model_required_bureau_c5_contract.py tests/test_model_required_bureau_c5_rehearsal.py tests/test_model_required_bureau_c5_plan.py tests/test_model_required_bureau_c5_implementation_readiness_continuity.py tests/test_ariadne_agent_error_register.py -q`

Also run:

`C:\\Users\\sarashera\\emr4\\.venv\\Scripts\\python.exe -B scripts/model_required_bureau_c5_acceptance.py --check`

Do not access ADC or cloud configuration; do not call Vertex; do not start a
real C5 target; do not open a socket/port or create a task directory; do not
inspect protected holdouts, historical diary material, patient/clinical/product
data or `docs/branding/`; do not deploy, release, rebuild Pages or move any Git
ref.

## Decision contract

Report concise evidence and every material finding with file and line. If any
material uncertainty remains, require revision. End with exactly one terminal
decision line, using only `DECISION: pass` or
`DECISION: revision_required`. Do not emit a second decision line or any later
follow-up.
