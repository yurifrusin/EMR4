# LC4R3 Sol Recovery Amendment

Date: 2026-07-14

Recovery owner and author: GPT Sol.

DeepSeek V4 Flash's initial candidate (`44148635`) and revision (`43783c66`)
are preserved as untrusted worker provenance. Their `DECISION: pass` artifacts
are worker self-assessments, not acceptance. Sol found that the revision made
several equal-size substitutions in the frozen evidence selection, compared
only aggregate repeat totals, omitted the report-level protected-metadata
incident disclosure, and omitted semantic baseline-floor assertions.

Under the documented recovery lease, Sol adopted the bounded extraction code
and made evidence-only amendments:

- restored the actual thirteen aligned cancel groups rather than a contiguous
  thirteen-group proxy;
- restored the actual forty-five explicit-status, thirteen check-in, and
  thirteen bare-arrival memberships;
- split target and deferred selection hashes so 154 repair cases are not mixed
  with 26 protected adjacent cases;
- used the LC4R2 candidate-quality audit's per-scenario fingerprints over two
  repeats (2,304 samples) rather than aggregate-count comparison;
- added every frozen semantic baseline floor to report assertions;
- added the report-level metadata-enumeration incident disclosure; and
- added focused regression tests that fail on equal-size case substitution.

No extraction rule, scenario fixture, replay policy, action grammar, provider,
route/API, database, UI, T3 gate, deployment, or write authority was added by
the recovery amendment. The frozen LC4R2 report remains byte-for-byte
preserved. Gemini independent review remains required before Sol acceptance.
