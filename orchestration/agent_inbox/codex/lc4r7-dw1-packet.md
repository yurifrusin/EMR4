# LC4R7 DW1 — Silver reconciliation queue and exit gate

You are the DeepSeek V4 Flash/high implementation/test worker for LC4R7. Work
only in the disposable worktree supplied as your current directory. GPT Sol is
the Conductor, architecture/acceptance/recovery owner, and protected integrator.
You may not change scope, accept your own work, integrate, push, deploy, or open
any provider/live/write authority.

Read completely:

- `AGENTS.md` current baton and authority sections;
- `orchestration/agent_inbox/codex/lc4r7-silver-reconciliation-contract.md`;
- `orchestration/agent_inbox/codex/lc4r6-sol-acceptance.md`;
- `docs/bernie-lc4r6-temporal-evidence-audit.md`;
- `scripts/bernie_lc4r4_report.py` normalization evidence helpers;
- `scripts/bernie_lc4r6_temporal_evidence_report.py`; and
- the public APIs of `development_gap_audit`, composed evaluator/replay,
  semantic extraction, interpretation harness, and native Diary action grammar
  needed to implement the contract without changing them.

Do not open, enumerate, list, search, import, run, regenerate, hash-check, or
inspect any protected holdout fixture, support module, seal, receipt, or report.
Do not use broad repository file listings.

Implement only the six owned files in the contract. Use the public development
audit for the 572 aligned-failure selection. Generate the 1,436-record redacted
queue exactly as contracted. Never feed expected values or source-span field
names into interpretation; use them only after scoring to classify audit
evidence. Do not include utterances, names, expected/observed values, span text,
diary state, tool payloads, or deltas in the committed queue.

Treat the 53 expected-clarification/observed-no-clarification records as
`requires_adjudication`, not parser failures. Treat the 51 semantic-pass replay
records as `non_language_contract_mismatch`. Preserve all 39 native `check_in`
surfaces as planned-not-implemented, including the 13 older-extractor
near-matches. Emit zero `surface_supported_parser_gap` records and keep the exit
gate blocked.

Add real original/shuffled/reversed order-invariance tests and fail-closed tests
that actually call the checker. Use `sys.executable`, not a machine-specific
Python path. Keep tests serial. Run focused tests, `--check`, and `git diff
--check`; review the exact six-file diff; update the completion artifact with
non-circular provenance; commit the candidate; leave the worktree clean. Do not
push.

Write the durable JSON receipt to the launcher-supplied output path with
`DECISION: pass` only if all requirements and tests succeed. Otherwise return
`revision_required` with exact blockers.
