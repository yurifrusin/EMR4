# C5 disposable live-development recovery — DeepSeek implementation packet

Source HEAD: `953073e18ab48420b58d80ed78d41e8033534cb8`

Worktree:
`C:\Users\sarashera\EMR4-worktrees\model-required-bureau-c5-implementation`

Branch: `codex/worker-model-required-bureau-c5-implementation`

Model/transport: exact DeepSeek V4 Flash/high through Claude Code `--bare`.
No model fallback is authorised.

## Mandatory source pass

Before editing, read `AGENTS.md` completely and state the exact five
rehydration sources. Read these files completely:

- `docs/emr4-model-required-bureau-c5-disposable-live-development-recovery-plan.md`
- `docs/security/emr4-model-required-bureau-c5-disposable-live-development-recovery-threat-model-delta.md`
- `orchestration/agent_inbox/codex/model-required-bureau-c5-plan-sol-acceptance.md`
- `orchestration/agent_inbox/antigravity/model-required-bureau-c5-plan-review-receipt.json`
- `docs/emr4-model-required-bureau-c4-allowlisted-actuator-simulator-plan.md`
- `docs/emr4-model-required-bureau-c4-allowlisted-actuator-simulator-closeout.md`
- `scripts/model_required_bureau_c4_simulator.py`
- `docs/emr4-model-required-bureau-c3-d3-provider-free-architecture.md`
- `docs/emr4-model-required-bureau-gate-zero-shared-contract.md`
- the C3-C5 and current-next-action sections of
  `docs/emr4-rayleen-davida-controlled-recovery-development-plan.md`
- `orchestration/api_spine_adr.md`
- `orchestration/api_spine_programme.md`

Verify the exact source, branch and clean worktree before editing. The Gemini
receipt is a passed plan veto, not implementation authority and not a source
that may widen the frozen plan.

## Exact implementation task

Implement only the frozen provider-free C5 foundation. Write the closed typed
contracts, source-bound policy/catalog/examples, deterministic parser and
proofreader, authority/evidence/idempotency state machine, fixed target module,
fixed process adapter, controller logic, provider-free fakes, focused tests and
reproducible authored-synthetic acceptance evidence needed before any live
execution can be considered.

Do **not** start a process, bind or connect a socket, allocate a port, create or
remove a temporary directory, invoke a provider, inspect ADC, or run the live
rehearsal. All worker tests and acceptance generation must use fakes and exact
operation counters. Actual capability-bearing classes may be implemented but
must remain uninvoked and structurally source-inspected in this worker tranche.

The only target/runbooks are exact:

- environment `c5_disposable_authored_synthetic`;
- kind `task_owned_loopback_http_service`;
- id `synthetic:c5-recovery-target`;
- host `127.0.0.1`;
- forward `start-c5-disposable-service.v1`;
- rollback `stop-c5-disposable-service.v1`;
- initial generation `1`, faulted state process absent plus connection refused;
- recovered generation `2`, exact artifact digest and `healthy` state.

The fixed target module may serve only one health path and one closed JSON
shape. It must validate exact constant host, OS-assigned server-held port,
opaque nonce and generation arguments. It imports no `app`, product settings,
database, provider or cloud module and accepts no caller payload.

The real adapter source may use only an argument-array process API with
`shell=False` semantics. It must resolve and hash the exact repository virtual-
environment Python executable and frozen target module, include `-I`, pass host,
port, nonce and generation as distinct arguments, supply a minimal explicit
credential-free environment, and retain the exact process handle. It must not
accept caller executable/path/PID/environment/command values, discover or
enumerate processes, invoke a shell, use dynamic import/reflection, inherit
stdin, or terminate anything except its owned handle.

The controller must enforce:

1. exact source/policy/catalog/schema bindings;
2. fresh baseline process and HTTP agreement before the sole fault;
3. exact controller-owned termination and post-fault process-absent plus
   connection-refused agreement before model eligibility;
4. provider ledger/reservation state without making a provider call;
5. closed candidate parsing and proofreader admission against the exact frame
   digest, stopped-process evidence, one allowed runbook, rollback and risk;
6. at most one closed correction ticket; no executable content or success
   claim;
7. exact plan-hash/target/runbook/provider/cost/expiry/one-rehearsal human
   approval materialisation from the fixed Yuri standing-authority basis;
8. opaque cryptographic, expiring, one-use execution evidence;
9. one shared store/critical section for authority, execution evidence,
   idempotency, attempt sequence and launch state across runtime objects;
10. exact same-key replay, changed-body conflict, different-key evidence reuse,
    stale authority/target rejection and cross-runtime single-winner behavior;
11. success only from distinct fresh process plus HTTP readback of exact
    generation `2`, target nonce, artifact digest and `healthy` state;
12. exact stop rollback with fresh process-absence/connection-refused proof and
    distinct verified versus inconclusive terminal outcomes; and
13. idempotent cleanup of only validated owned resources with proof of no
    process, listener, task directory, open ledger or reusable capability.

Model/provider content remains a candidate. Do not implement a live provider
client in the capability controller. A provider-request builder may be pure and
must encode the exact `gemini-2.5-flash`, project, identity, regional endpoint,
1,024 thinking, 2,048 output, one-candidate, temperature-zero, two-call/USD
0.50/no-fallback envelope without importing a provider SDK or transmitting.

## Closed schemas and evidence

Implement closed Draft 2020-12 schemas and canonical examples under
`orchestration/continuity/model-required-bureau-c5-disposable-live-development-recovery/`
for the nine objects named in the plan. All schemas require
`additionalProperties: false`, strict scalar bounds and exact enum values.
Reject duplicate JSON keys, bool-as-int, non-canonical encodings, unknown
properties, executable-shaped keys/values and attacker prose before authority
or capability lookup.

Acceptance evidence must be provider-free and prove only source/contract/state-
machine behavior using fakes. Its exact label is
`provider_free_authored_synthetic_c5_implementation_readiness`, not the later
occupied/live evidence label. Operation accounting must separately show zero
process starts/stops, socket binds/connects, port allocations, directory
creates/removals, provider calls, database, product, cloud/IAM, deployment,
external event and protected operations during worker acceptance.

## Owned paths

- `scripts/model_required_bureau_c5_contract.py` (new)
- `scripts/model_required_bureau_c5_target.py` (new; never executed here)
- `scripts/model_required_bureau_c5_rehearsal.py` (new; capability adapters
  implemented but never invoked here)
- `scripts/model_required_bureau_c5_acceptance.py` (new; provider-free fakes
  only)
- `tests/test_model_required_bureau_c5_contract.py` (new)
- `tests/test_model_required_bureau_c5_rehearsal.py` (new; fakes/source checks
  only, no process/socket/filesystem/provider operation)
- `orchestration/continuity/model-required-bureau-c5-disposable-live-development-recovery/**`
- `docs/api-spine/openapi/technical-control-live-development-recovery-commands.yaml`
  (optional, new, declarative and explicitly `not_mounted` only)

Do not edit any other path. In particular do not edit `AGENTS.md`, the frozen
plan/threat/review/acceptance artifacts, C4 source, any existing test, `app/**`,
migrations, GraphQL, provider configuration, deployment/workflows, global
Continuity/Compass maps, `docs/branding/**`, protected/historical evidence or
another worktree.

## Mandatory acceptance coverage

Focused tests and provider-free acceptance must prove at least:

- all schemas/examples/policy/catalog validate, close and reproduce LF-byte
  hashes and canonical digests;
- exact frame minimisation excludes port/PID/nonce/path/environment/credential/
  log/product values from provider-visible data;
- provider request metadata is exact without any SDK/network call;
- candidate parsing/proofreading grounds every claim and permits only the exact
  stopped-process diagnosis, runbook/rollback/risk/target, with no executable or
  success content;
- standing approval is exact-plan-bound, non-transferable, expiring and one-use;
- cryptographic references/nonces are non-caller-selectable in production code;
- shared-store replay, idempotency, target/authority mutation and cross-runtime
  concurrency produce at most one fake launch attempt;
- fixed argument-vector construction rejects executable/path/host/port/module/
  environment override and contains no shell or string command;
- PID/handle and listener nonce/generation/artifact checks prevent substitution;
- launch/audit/readback faults never release success and exact fake stop rollback
  distinguishes verified from inconclusive rollback;
- cleanup validation rejects workspace/broad/caller paths and fake cleanup
  proves exact owned-resource absence without deleting anything;
- source/import tests prove no `app`/database/cloud/provider SDK, generic runner,
  process discovery, shell or mounted route; and
- worker acceptance counters are zero for every live/external capability and
  the Context Fabric is not implemented.

## Mechanics and forbidden claims

Run provider-free acceptance generation/check, focused tests using the serial
pytest lease, relevant inherited C4/C3/Gate-zero/API Spine tests, Ruff,
`py_compile`, Bandit, JSON/YAML parsing, source/import checks and
`git diff --check`. No test may start the target or import a module that starts
it as a side effect. Do not fetch, merge, rebase, switch, push, deploy or move
protected refs. Stage only owned paths by explicit pathname, verify no
`docs/branding/` path is cached and commit once to the worker branch.

This is implementation-readiness code only. It grants no C5 target/provider
execution, patient/clinical/product/protected data, real service/database,
Context Fabric implementation, deployment, production, release, Pages or
protected-ref authority. It does not establish live recovery.

Return the five-source statement, exact commit/files/checks/blockers and finish
with exactly one `DECISION: pass` or `DECISION: revision_required`.
