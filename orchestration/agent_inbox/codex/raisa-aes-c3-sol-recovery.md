# Sol recovery lease: Raisa AES-C3 hostile containment rehearsal

Date: 2026-08-11

Recovery owner: GPT Sol / `openai-primary-orchestrator`

Worker candidate: `480a0301a1102108fa0779efb98809d55adf0ffa`

Primary adoption commit: `102fdd34`

Status: `recovered_candidate_pending_fresh_independent_veto`

## Provenance

The DeepSeek blue closeout remains the worker's non-transferable statement.
Sol rejected its self-pass in
`orchestration/agent_inbox/codex/raisa-aes-c3-blue-candidate-sol-rejection.md`
and recorded AER-0257. The seven worker paths were then adopted only as an
untrusted candidate under `docs/ariadne-orchestrator-recovery-lease.md`.
Nothing in this record rewrites or transfers the worker's attestation.

## Sol amendments

1. The public evaluator validates the complete closed canonical attempt before
   indexing fields or selecting a mutation. Non-object, missing-field, open or
   cross-bound attempts return one minimized `reject` result without raising.
2. Every outer scenario is bound to its exact family, carrier, declared
   inherited base, mutation, expectation and replay-presence rule. The exact
   declared C2 scenario is copied from the committed C2 packet; an exact C1
   base is copied into a closed C2 wrapper. The outer C3 ID no longer selects a
   different implicit success base.
3. Lease, alias and token fixtures now carry exact generation, manifest ID and
   digest, Bureau, work-cell and authority-binding identities. The broker-owned
   current wrapper identity is compared before inherited evaluation. A fixture
   with no stale/cross-authority mismatch cannot claim a replay stop.
4. Replay fixture IDs, digests and bindings are checked for absence from the
   work-cell view before evaluation. The outer non-exposure claim is emitted
   only after that check.
5. Contained, invalid-result, egress, replay, revocation/kill and supply-chain
   outcomes now require a schema-valid exact C2 result with the required inner
   status, reason, admission decision/reasons, actual call count, release flag
   and digest posture. A contradictory result becomes a closed rejection; its
   observed call count is retained and its digests are not released.
6. The boundary-probe sequence checks both denials, exact count transitions,
   the terminal latch and the third stop. The repeated-failure sequence counts
   a failure only after an exact one-call/no-release malformed-result stop and
   checks the third zero-call budget stop.
7. Hostile attempt mutation coverage increased from 20 to 33 so the declared
   base, expected release count, manifest/Bureau/work-cell wrapper identities,
   replay presence and every replay binding are independently challenged.
8. New regressions reproduce every rejection finding: missing replay fixture,
   cosmetic base substitution, malformed roots and fields, contradictory
   inner release/call evidence, permissive C1 results and released cumulative
   C2 results.

## Deterministic evidence so far

- standalone generated report: `passed`, 61 scenarios, exact 21 contained / 15
  rejected / 25 stopped, 28 pure calls, 21 digest-only releases, 33/33 hostile
  attempt mutations rejected and 18/18 contract mutations rejected;
- focused recovered AES-C3 tests: 25/25 passed;
- serial AES-C3/C2/C1/C0/API packet: 106/106 passed;
- focused agent-error register suite after AER-0257/AER-0258: 207/207 passed;
- maintained static CI packet: 180/180 passed without repository `conftest.py`;
- canonical fast profile: 186/186 passed, with 202 maintained source files,
  33 exact verification files, Diary JavaScript syntax and Git whitespace;
- Ruff check and format: passed; and
- zero runtime, provider, adapter, credential, network, filesystem capability,
  database/source, executable tool, command or patient/product operation.

The first attempted focused packet named a nonexistent API test file and was
admitted as no evidence. AER-0258 preserves that low-severity Sol dispatch
error; the corrected complete 106-test packet used the exact frozen worker
packet path and passed.

## Pending gates

Exact diff/scope, recovery commit and fresh Gemini 3.6 Flash/high exact-HEAD
veto remain pending. AER-0257 remains contained, not corrected, until those
gates and the independent veto pass. This recovery grants no acceptance,
runtime, provider, data, tool, command, deployment, release, Pages or
protected-ref authority.
