# LC4V9 Second Post-Veto Ordinary-Interface Amendment

Date: 2026-07-16
Status: `fresh_third_veto_required_before_content`

Gemini 3.5 Flash/medium returned `DECISION: pass` on exact amended
content-blind head `b5aaa89cfc8ed4bf697e4b68e41cfaa301c59e38`. Its review remains valid
for that head, but it does not authorize authorship after this further
material framework amendment.

Before creating any V9 content, Sol compared the Gold cross-field rules with
the accepted ordinary policy interface in
`app/services/bernie/lc4v4d3_policy_resolution.py`. That ordinary interface
legitimately returns a clarification with an empty `clarification_choices`
array when a required practitioner is omitted or cannot be resolved safely.
The V9 framework incorrectly required every clarification to contain at least
one choice, which would have forced future Gold to misrepresent that product
state or made structurally valid evidence fail before evaluation.

This was an authoring-contract defect, not a parser defect and not evidence
about any protected holdout case. No V9 protected content surface exists, and
no protected v1-v8 path was opened, listed, searched, imported, run, or
inferred.

## Sol amendment

- Clarification still requires `requires_clarification: true`, the exact
  non-mutating `request_clarification` tool, `authority: clarify`, the
  `clarification_required` downstream outcome, and zero mutation evidence.
- `clarification_choices` remains a required array of strings, but may be
  empty when the product cannot safely project alternatives.
- A focused regression proves that omitted-practitioner clarification with an
  empty choice list is accepted without weakening any hidden-mutation guard.

The combined content-blind framework and ordinary LC4V8D1 development suite
passes. No V9 corpus, evaluator, authoring module, thresholds, manifest, seal,
marker, report, or protected content exists.

The first and second Gemini passes are preserved as historical review evidence
for their exact heads only. A third fresh Antigravity project must return
`DECISION: pass` on the exact committed amended head before Sol-only
authorship begins.
