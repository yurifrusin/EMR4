# Reception One Extended Proposal Runtime — Occupied Attempt 001 Independent Audit

## Disposition

`revision_required`, with the sole pre-authorised deterministic request-contract repair eligible.

Attempt `reception-one-extended-runtime-occupied-001` consumed exactly one call and ledger `reception-one-extended-runtime-ledger-001`. The configured `australia-southeast1` Vertex endpoint returned HTTP 200 after 921 ms, but the one-use broker rejected the response at `$.goal` before candidate construction or deterministic proofreading. No draft or value was released. The ledger and seven-event hash chain close correctly, and container, relay, network, image, process, temporary-context and temporary-token cleanup all pass.

## What the evidence establishes

- The call used `gemini-2.5-flash`, project `bernie-emr4-dev`, the exact Bernie impersonated service account and keyless ADC through `australia-southeast1-aiplatform.googleapis.com`.
- No API-key authentication, fallback, tool, grounding, retrieval, cache reference, product/database access, confirmation or write occurred.
- The response reached the broker, and the stricter local wire contract rejected it before the proofreader.
- The bounded retained defect location is only `$.goal`; the discarded value is not known and must not be reconstructed as fact.

## Repair assessment

The provider-facing low-state schema permits any string at `goal`, while the local contract permits only the seven closed goal tokens. The request did list those tokens but did not literally map receptionist wording such as move/reschedule/rebook to the exact `move` token. Adding that closed mapping is a request-instruction repair. It neither normalizes nor reinterprets the rejected value, nor changes provider, model, project, identity, region, credentials, data class, isolation, proofreader, output contract, cost or residency claims.

A focused regression now fixes that mapping in the constructed request. The model-lane, typed-protocol, extended-runtime, API Spine, continuity, Compass, repository-only, real-isolation, live-local browser/backend/database and exact ADC/control gates all pass again. The provider-free rerun again recorded four request families, zero provider calls and zero writes.

## Retry conclusion

One distinct repaired ledger is eligible under Yuri's frozen two-call ceiling. That retry must stop the sequence whether it passes or fails. This audit does not predict provider acceptance and does not strengthen the claim beyond the configured and observed Sydney locational request path.
