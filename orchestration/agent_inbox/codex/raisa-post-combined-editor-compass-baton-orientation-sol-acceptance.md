# Sol acceptance - post-combined-editor Compass and baton orientation

Date: 2026-08-15

Timestamp: 2026-08-15T06:42:00+10:00 (Australia/Brisbane)

Decision: accept and pause at the recorded programme fork

Accepted reviewed source: `2ca3a111d2ee9277571ea3c905f22ce78c8e9745`

Result: `raisa_post_combined_editor_compass_baton_orientation_pass`

## Acceptance reasoning

The candidate faithfully maps the current command reach: status is one complete
family, while practitioner, local time and duration are three views over one
complete update family. Cancellation, check-in and patient-channel execution
would each introduce materially distinct meaning or authority, so standing
continuation authority cannot select among them.

Cancellation is the best-supported recommendation because it is a natural
selected-appointment action and the API Spine already contains a distinct
delete proposal/confirm family. A read-only readiness review must come first:
the ordinary Diary can fall back from delete proposal to `Cancelled` status,
where `cancellation_reason` is omitted, and Reception One has no delete bridge.
The candidate correctly treats this as a seam to classify, not a defect or
vulnerability already proved.

Delegated client authority remains separate from patient identity and command
truth. It is narrow, expiring and revocable for future acts. Revocation before
commit blocks the delegated action; an already committed appointment remains
truth until a separately authorised cancellation or rescheduling command
changes it.

## Verification finding

Eight focused assertions, the 115-test exact independent packet and the
196-test canonical fast profile passed. Ruff, maintained-source compilation,
Diary JavaScript syntax and Git whitespace checks passed. Gemini 3.6 Flash/high
cleared all ten challenges at unchanged clean source.

AER-0320 records a pre-verifier state rejected for invalid disposition and
workspace-receipt shape before any external call. The corrected state passed;
the single Gemini call followed. The candidate and protected evidence were
unchanged.

## Authority finding

The orientation is repository-static and provider-free. It opens no command,
event, product, database, source, watcher, channel, identity, delegation,
provider, data, deployment, production, release, Pages or protected-ref
authority.

The next executable stage is deliberately empty until Yuri chooses a programme
direction. The recommended choice is appointment cancellation, beginning only
with the read-only command-path readiness review.
