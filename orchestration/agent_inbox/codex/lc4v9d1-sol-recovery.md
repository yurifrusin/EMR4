# LC4V9D1 Sol Recovery Lease

Date: 2026-07-16
Worker candidate: `02155888`
Decision: `candidate_rejected_conceptual_contract_failure`

DeepSeek Flash produced useful four-file mechanical scaffolding and passed its
own 69 tests, but Sol rejects its baseline and `candidate_ready` decision.

## Conceptual defects

1. Every `explain_schedule` probe omits a patient, despite the frozen contract
   requiring a fresh full patient identity in every one of the 30 probes. This
   removes the exact identity surface D1 is intended to diagnose.
2. Several actions replace the six frozen language structures with bespoke
   negation/unsafe labels. The contract permits matched safety variants inside
   the fixed 30; it does not permit them to replace the fixed structure
   taxonomy.
3. Candidate tests merely require six distinct arbitrary strings per action,
   so they self-pass the taxonomy deviation instead of enforcing the contract.

These are category/Gold/acceptance defects, not missing mechanical assertions.
Under the standing Flash complexity rule there is no correction loop. Sol
adopts the source only as untrusted scaffolding under the recovery lease.

## Sol amendments required before baseline

- replace all six explain rows with patient-bearing fresh synthetic language
  and independently adjudicated Gold;
- normalize every action to the exact six frozen structure labels while
  retaining safe-negated and unsafe content only inside the appropriate
  polite/two-turn structures;
- strengthen schema/tests to enforce the exact per-action structure set and
  a non-null full patient Gold identity for all 30 rows;
- rerun fail-closed validation, focused tests, and the diagnostic; and
- accept classifications only after direct Sol utterance/Gold audit.

No protected V9 content was accessed. Parser and policy product code remain
unchanged during recovery.
