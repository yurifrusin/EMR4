# Sol acceptance: durability generation-lock RLS recovery

Date: 2026-08-10

Candidate `e115f6f4cb31df1131c5c67d24f3a475a2ca6127` is accepted for the
repository-only parse-parent rebind sequence.

Sol independently reconciled immutable failure 035 and its deterministic
diagnosis, the exact `pol_cf_06_update` predicate change, empty coordinator
direct relation grants, unchanged typed-body population, structural digest
`3ce31780`, body digest `32edb340`, 421-statement inert SQL digest `aa26f926`,
and clean exact-HEAD review postflight. Gemini 3.6 Flash/high returned one
schema-constrained `pass` after 201/201 tests and ten-file Ruff verification.

The reviewer prose contains one non-authoritative hyperlink label using
`unmounted` where the committed behavior recovery document uses
`disposable-postgresql`; its verified failure, diagnosis, contract and test
claims resolve to the correct committed paths and hashes. The prose path is not
copied as authoritative evidence.

This acceptance authorizes only deterministic parse contract rebind,
characterization planning and fresh review. It does not yet authorize a
PostgreSQL run and grants no applied migration, operational database,
provider/product/patient/clinical data, runtime wiring, command, deployment,
release, Pages or protected-ref authority.
