# Sol Acceptance — Reception One Bureau Model Text Lane

## Disposition

Accept the provider-blocked tranche, the fail-closed occupied result and the
post-failure provider-blocked diagnostic as accurately bounded.

The implementation preserves the API Spine: the model emits only an untrusted
typed plan candidate, the host attaches scope, the deterministic proofreader
retains admission, and the only executor remains proposal-only and in memory.
The occupied attempt used exactly one regional Vertex call, released nothing,
left no owned residue and did not retry.

The compact wire repair is structurally narrower and is regression-tested, but
has not been provider-validated. Any further occupied attempt requires a fresh
exact user grant and a distinct one-use ledger.
