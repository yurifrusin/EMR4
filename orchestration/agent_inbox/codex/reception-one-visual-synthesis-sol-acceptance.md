# Sol acceptance — Reception One visual and interaction synthesis

## Disposition

`accepted_with_vertex_candidate_rejected`

Accept the deterministic Reception One visual and interaction synthesis as
`reception_one_visual_interaction_synthesis_pass_with_vertex_candidate_rejected`.

The exact-raster responsive evidence, inherited Diary/API contracts, focused
tests, repository-only verifier, static checks, schema checks and final residue
checks pass. The UI remains deterministic and unoccupied.

The occupied Sydney Vertex sub-attempt remains `revision_required`: its
candidate failed `priority_order_invalid`, released no field, was not used and
did not qualify for the request-contract-only retry. This acceptance does not
relabel that sub-attempt or authorize another provider call.

The detailed review is
`orchestration/agent_inbox/codex/reception-one-visual-synthesis-sol-review.md`.
