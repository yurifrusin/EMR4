# LC4V4D4 Sol Recovery Amendment

Date: 2026-07-16

Worker candidate: `822889f73b82a3cb659c193543e0901b22ef1599` on
`claude/lc4v4d4-composed-integration`.

Adopted candidate integration: `73398585`.

Authority: GPT Sol recovery lease. DeepSeek V4 Flash/high through Claude Code
`--bare` supplied useful additive scaffolding, but its `candidate_complete`
evidence is rejected. The defects concern acceptance meaning and complete
evidence binding, so the standing Flash complexity rule forbids another
same-lane correction loop.

## Preserved worker value

The candidate added the intended two-value version vocabulary, a typed composed
result, an explicit legacy-default runner seam, a D4 evidence module, focused
tests, and report scaffolding. It left the utterance parser, generic scorer, D3
resolver, historical evidence, protected surfaces, and product runtime
unchanged. The 60-probe legacy hash and direct-runner equivalence were correct.

## Rejected completion evidence

The worker's self-green report did not satisfy the frozen contract:

- Option A's `InterpretationObservation` retained extraction-time
  clarification, choices, selected tools, and authority instead of carrying
  policy-final operational fields into the ordinary typed boundary.
- `replay_fields_exact` compared two executions of the new runner with each
  other. It never compared replay with the frozen accepted D3 policy output.
- `exact_20_case_population` merely checked that the authored probe dictionary
  contained the 20 static IDs; it did not rerun the authoritative development
  diagnostic and recover the current policy-gap population.
- the selection-hash gate hashed the static expected tuple rather than the
  current diagnostic population;
- forbidden outcomes and tools were hard-coded empty in Option A replay;
- the incompatible-D1 gate checked only that a constant set had length six,
  without proving exact members or observable legacy/Option-A differences;
- the semantic-preservation gate incorrectly required policy-operational fields
  to remain equal to legacy, contradicting the contract's separation between
  pure utterance semantics and versioned policy decisions;
- the overlay checks were partial and did not bind complete typed observations,
  policy authority, all replay fields, or resolved identities to accepted D3;
  and
- `git show --check 822889f7` found trailing whitespace in the generated
  Markdown despite the receipt claiming `git diff --check` passed.

The worker did accurately report one failure in the historical LC3 committed-
report equality node. Sol reproduced the same failure on unchanged baseline
`fbcd1c63`; it is an immutable historical report mismatch, not a D4 regression.

## Bounded Sol recovery

Sol retained the additive seam and independently:

1. made the Option A interpretation carry policy-final clarification, choices,
   tools, and authority while preserving intended action, action semantics,
   temporal relation, normalized values, entity semantics, completion claim,
   and negation from pure extraction;
2. routed Option A outcome/tool safety through the ordinary forbidden-
   observation helper without changing the legacy call path;
3. recomputed the current 20-case policy population through `run_diagnostic`
   and derived the selection hash from that actual population;
4. validated the exact 20-case population embedded in the frozen accepted D3
   report;
5. compared every D4 pure semantic, operational interpretation, replay, diary
   relation, conflicting-field, and resolved-identity field with the complete
   accepted D3 case output;
6. limited semantic-preservation evidence to the pure utterance fields;
7. recorded complete legacy and Option A typed observations plus deterministic
   fingerprints for all 20 cases;
8. proved the exact six incompatible D1 members and their concrete versioned
   differences, including separate diary relation/conflicting fields;
9. replaced nominal tamper coverage with fail-closed missing-D3-case and legacy-
   hash tamper tests; and
10. changed the accepted decision vocabulary to
    `versioned_composed_integration_valid` only when every gate passes.

The recovered layer remains development-only. Holdouts v1-v4, T3.1-T3.4,
T3.5/providers, product routes/runtime, database/UI, historical diary,
deployment, release, and write authority remain closed.
