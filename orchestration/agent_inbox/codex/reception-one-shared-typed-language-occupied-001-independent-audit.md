# Independent Audit — Shared Typed Language Occupied 001

## Disposition

`zero_call_local_transport_failure_closed_and_repair_eligible`

The distinct ledger is consumed with `provider_calls_consumed: 0`. The durable
hash chain contains only `broker_ready`; no cell request reached the broker, no
provider request was constructed, no prompt was transmitted and no inference,
PlanProgram, operator note, proofreader disposition or release exists.

The retained evidence is sufficient to diagnose one deterministic local
transport defect. The generated cell request was written as 11,273-byte
pretty-printed JSON, exceeding the relay's 8,192-byte cap. The identical
canonical JSON is 6,251 bytes. Compact serialization therefore repairs the
local representation without changing the request value, model, provider,
project, identity, region, endpoint, credential, data, isolation, proofreader,
output, cost or residency boundary.

The focused regression proves every supported authored-synthetic shared request
fits the relay cap and that the build context contains exactly the canonical
bytes. The old ledger cannot be reopened. A fresh distinct ledger is eligible
only after the complete provider-free and read-only control gates pass again.

Independent residue checks found no owned container, relay, network, image,
broker process or temporary context.
