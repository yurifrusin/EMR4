# Bounded inert durability DDL renderer implementation packet

Date: 2026-08-07

Source branch: `codex/ariadne-bernie-davida-parallel-seam`

Source HEAD: `4a1cf9ce811a60aab6eac28cb17a72fa8a7aec09`

Worker worktree: `C:/Users/sarashera/EMR4-worktrees/r38`

Worker branch: `codex/worker-durability-inert-ddl-4a1cf9ce`

Allocation: exact `deepseek-v4-flash`, high reasoning, through Claude Code
`--bare`.

Authority: bounded mechanical implementation and focused-test execution only.
No architecture reinterpretation, self-acceptance, Git stage/commit/push,
network/provider product/database/source/runtime contact, SQL execution,
migration, product read/write, patient/protected data, dependency installation,
deployment, release, Pages or protected-ref authority.

## Read allowlist

Read completely from `r38`:

- `AGENTS.md` sections 2-7 only;
- the accepted inert-DDL plan, design, recovery, threat delta and Sol plan
  acceptance;
- both immutable parent contract JSON/Schema files, closeouts and Sol
  acceptances;
- the accepted function/trigger-body builder, schema and validator modules;
- the accepted migration/transaction builder, schema and validator modules;
- the existing focused inert-DDL plan test; and
- narrowly relevant standard-library-only pure-renderer/test patterns found by
  exact filename search under `scripts/` and `tests/`.

Do not enumerate or read `docs/branding/`, unrelated receipts, application
routes, database configuration, credentials, product data, historical Diary or
holdout paths.

## Owned files

Create or edit exactly these six paths and no others:

1. `scripts/raisa_provider_free_unmounted_durability_inert_ddl_rehearsal.py`
2. `orchestration/continuity/raisa-provider-free-unmounted-durability-inert-ddl-rehearsal/lowering-contract.json`
3. `orchestration/continuity/raisa-provider-free-unmounted-durability-inert-ddl-rehearsal/lowering-contract.schema.json`
4. `orchestration/continuity/raisa-provider-free-unmounted-durability-inert-ddl-rehearsal/durability-schema.sql.inert`
5. `orchestration/continuity/raisa-provider-free-unmounted-durability-inert-ddl-rehearsal/render-manifest.json`
6. `tests/test_raisa_provider_free_unmounted_durability_inert_ddl_rehearsal.py`

Parent artifacts are immutable. No cache, temporary or receipt file may remain
in the worktree.

## Required implementation

Implement the accepted plan literally:

- verify both fixed parent paths and canonical `contract_sha256` bindings before
  output;
- mechanically apply exact REC01-REC26 to a deep copy, prove parent bytes
  unchanged and reconcile the complete effective catalogue;
- create one closed Draft-2020-12 lowering contract/Schema fixing PostgreSQL 16,
  source bindings, output paths, six phases, all position-closed statement
  families, digest profiles/type tuples, 22 declared/21 observed instruction
  opcodes with only `DERIVE_BINDING` absent, and 34/34 expressions;
- lower every node of all 22 programs exactly once, with no prose-derived
  semantics, generic SQL/function call or compatibility fallback;
- map every implicit `EXACTLY_ONE` failure to registered value-free
  `F_CARDINALITY`/`CF004`;
- for all 21 `INSERT_OR_RELOAD_COMPARE` nodes derive exactly one enforcing
  primary/unique name from the effective catalogue, catch only SQLSTATE 23505,
  inspect `CONSTRAINT_NAME`, rethrow non-matches unchanged, reload using exact
  key plus `winner_predicate`, and raise `F_CARDINALITY`/`CF004` for
  missing/mismatch; never use `ON CONFLICT DO NOTHING`, no-op update or
  `WHEN OTHERS`;
- inline exact canonical SHA-256 framing using qualified PostgreSQL-16 core
  primitives and the accepted null/type/byte-length/UTC-microsecond rules;
- validate and erase only the accepted constant-false retry wrapper; add no
  retry/catch;
- emit exactly six phases, exact security-definer headers/search paths,
  functions/triggers/revokes/grants and reference-only `public.*` behavior to
  the fixed `.sql.inert` path;
- build a closed typed manifest with parent/effective/artifact hashes, byte and
  statement counts, exact ordered nodes, per-phase spans, catalogue/privilege
  assertions and digest vectors; and
- implement a separate standard-library-only closed-subset recognizer that
  tokenizes emitted bytes independently of the emitter and enforces the
  calibrated non-server-parse claim.

The module must expose pure functions usable by tests plus one fixed-path
regeneration/check CLI. It must import no subprocess, socket, HTTP, provider,
SQLAlchemy/psycopg, database, Alembic or environment-selected output surface.
Canonical repository artifacts must be UTF-8/LF with one terminal newline and
byte-identical across isolated renders. No current time, random value, absolute
path, environment value or Git metadata may enter them.

## Focused acceptance tests

The owned test module must exercise all sixteen acceptance clauses in the plan.
At minimum prove:

- exact parent immutability/hashes, recovery population and effective catalogue;
- whole lowering-contract JSON Schema validation and hostile unknown fields;
- two isolated byte-identical renders and exact canonical regeneration;
- all six phases and every required population/count/order;
- full 22-program node/expression accounting and failure ceilings;
- all 21 unique mappings plus wrong-constraint, zero/multiple-winner and broad
  handler hostile mutations;
- digest delimiter/null/type/order/time-zone hostile vectors;
- independent recognizer rejection of extra/missing/swapped statements,
  malformed quotes/dollar bodies, unqualified identifiers, search-path
  widening, security invoker, missing revoke, added grant, application DML,
  top-level DML/transaction control, psql meta-command, extension/file/network
  operation, helper/overload and output-path escape;
- no dependency/process/socket/database/provider/environment/Alembic reachability;
  and
- `DERIVE_BINDING` occurrence, wrong SQLSTATE, wrong OLD/NEW, omitted
  trigger/policy/constraint and arbitrary retry elimination all fail closed.

Tests must use authored-synthetic mutations only and must never execute SQL or
contact a database/network/provider. Do not weaken existing validation.

## Permitted commands

Run only:

```powershell
$env:PYTHONDONTWRITEBYTECODE='1'
$env:RUFF_NO_CACHE='true'
C:/Users/sarashera/emr4/.venv/Scripts/python.exe -m pytest -q -p no:cacheprovider tests/test_raisa_provider_free_unmounted_durability_inert_ddl_rehearsal_plan.py tests/test_raisa_provider_free_unmounted_durability_inert_ddl_rehearsal.py
C:/Users/sarashera/emr4/.venv/Scripts/python.exe -m ruff check scripts/raisa_provider_free_unmounted_durability_inert_ddl_rehearsal.py tests/test_raisa_provider_free_unmounted_durability_inert_ddl_rehearsal.py
git diff --check
git status --short
```

The implementation module's fixed-path `--check` or `--regenerate` CLI may be
run only to produce/check the owned canonical inert files; it must not contact
PostgreSQL or any external surface.

Return a concise account of changed files, exact test/Ruff/diff results, known
limitations and exactly one terminal line: `RESULT: candidate_ready` or
`RESULT: revision_required`. Do not commit.
