# Gemini 3.7 Flash/high veto — provider-free native Harness HMR boot proof

Date: 2026-08-20

Perform one fresh read-only veto of exact candidate
`5c3325e9213afc5690453812e2078c61135c8a38` against accepted base
`f5a00279f428698b5ce789d022404335a54a7d95`.

## Exact candidate and authority

- Worktree: `C:\Users\sarashera\EMR4-worktrees\native-hmr-boot-gemini-5c3325e9`
- Branch: `codex/review-native-hmr-boot-5c3325e9`
- HEAD: `5c3325e9213afc5690453812e2078c61135c8a38`
- Required model/effort: `gemini-3.7-flash-high` / `high`

Read `AGENTS.md` completely and name all five rehydration sources. Validate the
in-progress latch. Restore the explicit dispositions: DeepSeek declined because
its native Harness is the provider-free subject under test, Gemini dispatched
for this independent exact-candidate veto, and native subagents declined under
current policy and the serial process/evidence ownership constraint.

Read the active plan, threat delta, contract/schema, retained passing evidence,
evidence schema/report, prelaunch rejection, implementation, test, historical
rc.7 occupied-negative evidence and its accepted closeout. Do not inspect
`docs/branding/` or unrelated paths.

## Mandatory challenges

1. Confirm exact candidate/worktree identity and a clean tracked tree.
2. Confirm the installed-source predicates bind exact rc.7 package/bin,
   documented stock headless launch, disabled stock HMR row, required Node
   `--expose-internals`, HMR `registerConfig`, watch-only fallback and both
   `watchUserPatches` calls without patching package source.
3. Establish causality: the initial layer must disable the stock model runner,
   code runtime and telemetry and must not contain the custom runner; only an
   atomic mutation after in-process HMR readiness may add the HMR-injected
   custom runner; exact event order and exit ownership must be unavoidable.
4. Challenge containment: credential scrubbing, offline/no-scripts package
   materialisation, Node DNS/socket/TLS/HTTP/fetch/WebSocket denial, no agent or
   provider boundary, bounded deadline, process reap and exact-root cleanup.
   Distinguish the guard's stated residual limit from a general VM claim.
5. Confirm the first retained failure is prelaunch construction evidence with
   native boot count zero and complete cleanup, not a hidden failed native
   attempt or an erased terminal. Confirm the corrected predicate is narrowly
   tested and the passing evidence names `native-attempt-001`.
6. Confirm no raw logs/environment/path payloads are retained and all claims in
   the report are supported by the exact JSON evidence.
7. Confirm the result authorises no model/provider call, occupied worker,
   attempt-004, development edit, product/runtime/data change, deployment,
   Pages or protected-ref movement.
8. Execute exactly the ten-command manifest. C03 must pass 39 tests. Do not run
   `--execute`, npm install, the native Harness boot, Docker, PostgreSQL,
   DeepSeek or another provider. Every command must return zero and leave the
   review worktree unchanged.

Do not edit, commit, push, install, access protected data, deploy, publish or
move protected refs. Return one schema-constrained `pass` only if the proof is
attributable, contained, exact and has no P0-P2 finding. Otherwise return
`revision_required` with exact evidence. Emit one terminal decision.
