# LC4V6 Sol Empty-Framework Acceptance

Date: 2026-07-16

Decision: `content_blind_framework_accepted_content_authorship_authorized`

DeepSeek V4 Flash/high produced one bounded empty candidate through Claude Code
`--bare`. Sol rejected its conceptual self-pass and recovered it under the
lease without a correction loop. The exact worker source remains at `70d462d1`;
Sol recovery source is `f53bb976`.

The recovered framework measures repeat variance, validates exact manifest and
typed-observation populations, recursively rejects case-level aggregate keys,
binds exact hashes, validates twelve dimensions/actions/slices, and uses a
durable exclusive lock plus retained consumed-seal/report/marker hashes. A
structurally safe evidence-invalid run consumes permanently; unsafe or leaking
output cannot consume.

Sol's focused suite passed 36/36 and combined framework/R1/E1/orchestrator tests
passed 79/79. Gemini 3.5 Flash/medium through a fresh Antigravity project
independently reproduced 55/55 and returned `DECISION: pass` on exact recovered
head `f53bb976`. The Antigravity worker read only new V6 orchestration receipts
beyond the named product files; it did not inspect any protected holdout.

The separate acceptance rule is now frozen before content authorship: complete
and every dimension at least 548/576, safety exactly 576/576, interpretation/
policy/integration failures at most 28, safety failures zero, every slice and
worst slice at least 0.90, and zero evidence defects/variance. Evidence-invalid
is distinct from product certification failure.

V6 content did not exist during external implementation or review. External
sessions are closed. Sol alone may now author, freeze, and seal the genuinely
fresh V6 corpus. Holdouts v1-v5 and every T3/provider/product/write boundary
remain closed.
