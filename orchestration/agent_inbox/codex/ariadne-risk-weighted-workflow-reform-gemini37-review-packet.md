# Gemini 3.7 Flash/high independent veto — Ariadne risk-weighted workflow reform

Date: 2026-08-16

Timestamp: 2026-08-16T09:45:11+10:00 (Australia/Brisbane)

Decision owner: GPT Sol. Reviewer role: one fresh final independent veto only.

## Exact candidate

- Worktree: `C:\Users\sarashera\EMR4-worktrees\ariadne-risk-weighted-review-51866ce0`
- Branch: `codex/review-ariadne-risk-weighted-51866ce0`
- Candidate HEAD: `51866ce084c33fce600b792c66b180927658ed9e`
- Candidate tree: `1dc69f23c8e93fe33de2289e563b2363fcd03539`
- Frozen plan source: `721f4cf1dd7bce27429ed97fdeb6028ba85ed954`
- Rejected worker candidate: `85a0877face2f16af80a07217918bf86b370febd`
- Required model: `gemini-3.7-flash-high`
- Required effort: `high`
- Evidence label: `provider_free_authored_synthetic_repository_only_workflow_reform`

The exact clean candidate passed 121 focused tests, Ruff, JSON/schema checks,
Git whitespace and the canonical fast profile: maintained-source compilation,
196 tests, Diary syntax and whitespace. Worker candidate defects are immutable
in AER-0345; this exact descendant is Sol's one bounded recovery.

## Review question

Return `pass` only if this exact source faithfully implements the frozen
risk-weighted reform while retaining every hard control. Return
`revision_required` for any material classification, baseline/freeze/reuse,
rerun, independent-review, authority-path, verifier-root, evidence, incident,
tail-deferral, scope or fail-closed defect.

Inspect only these exact candidate artifacts:

- `AGENTS.md`
- `docs/ariadne-risk-weighted-workflow-reform-plan.md`
- `docs/security/ariadne-risk-weighted-workflow-reform-threat-model-delta.md`
- `orchestration/harness_settings/risk_weighted_workflow.yaml`
- `orchestration_harness/risk_weighted_workflow.py`
- `scripts/ariadne_risk_weighted_workflow.py`
- `scripts/ariadne_serial_pytest.py`
- `scripts/ariadne_verifier_worktree_preflight.py`
- `orchestration/continuity/ariadne-risk-weighted-workflow-reform/tranche-profile.schema.json`
- `orchestration/continuity/ariadne-risk-weighted-workflow-reform/tranche-result.schema.json`
- `orchestration/continuity/ariadne-risk-weighted-workflow-reform/tranche-profile.example.json`
- `orchestration/continuity/ariadne-risk-weighted-workflow-reform/tranche-result.example.json`
- `orchestration/continuity/ariadne-risk-weighted-workflow-reform/provider-free-authored-synthetic-evidence.json`
- `tests/test_ariadne_risk_weighted_workflow.py`
- `tests/test_ariadne_serial_pytest.py`
- `tests/test_ariadne_verifier_worktree_preflight.py`
- `scripts/ariadne_antigravity.py`
- `scripts/ariadne_evidence_gate.py`
- `orchestration/harness_settings/verifier_execution_policy.yaml`

Use only the exact command manifest. Do not enumerate the repository or
protected paths, inspect outside this allowlist, modify source, commit, push,
deploy, open a database, use product/patient/clinical data, open credentials or
IAM, call another provider, or invoke a tool beyond that manifest.

## Required invariants

1. Highest applicable tier wins. A caller cannot lower it; contradictory
   `docs_only`/`unmounted` claims and a Tier-0 review trigger fail closed or
   raise the tier deterministically.
2. Tier 1-3 require a pre-edit canonical baseline. The exact result binds the
   baseline and full semantic freeze; a canonical-pass reuse fingerprint is
   derived from stable semantic inputs and rejects drift without including
   volatile closeout evidence.
3. Mixed post-freeze changes take the exact rerun union. Semantic edits
   invalidate prior verifier evidence; metadata-only tails receive only their
   targeted gates.
4. Tier 2-3 require exactly one independent final veto. A DeepSeek mechanical
   implementation lane cannot count as that veto, stacked reviewers fail, and
   actual parallelism plus capability/scope/handoff fields bind the plan.
5. Every named RWW threat has passing gate coverage; mutation count remains
   advisory. Minimal incidents precede retries, distinct causal signatures are
   preserved, and no safety/authority/integrity/privacy/atomicity/protected-
   evidence/irreversible item can be deferred.
6. Any reason, including declared-decision mismatch, returns
   `revision_required`; no pass may carry reasons.
7. The renderer is deterministic, timestamped and useful, but cannot decide
   acceptance or write AGENTS, Git, Continuity, Compass or the live latch—even
   through another worktree path.
8. Verifier preflight requires an exact clean `codex/review-` branch and exact
   40-hex candidate. It rejects missing/outside candidate paths, silently
   narrowed repository-path JSON, configurable protected prefixes and any
   external serial-runner root not exactly equal to the candidate worktree.
9. The serial pytest launcher resolves relative tests against the explicit
   candidate repository root while retaining the shared-schema lock.
10. No Raisa product, database/migration runtime, provider, product/patient/
    clinical data, credential/IAM, deployment, release, Pages or protected ref
    is opened. Reviewer evidence cannot accept or integrate its own result.

## Decision contract

Return exactly one schema-constrained terminal decision through the launcher.
If `revision_required`, identify precise findings with exact paths and
evidence. Do not wrap the decision in prose or emit a second terminal decision.
