# Corrected independent source veto: A3/B3 request-contract recovery

Use only this fresh read-only worktree:

- `C:\Users\sarashera\EMR4-worktrees\model-required-bureau-a3-b3-recovery-review-2`
- branch `codex/review-model-required-bureau-a3-b3-recovery-2`
- exact HEAD `a70d06fd047733bac9a72921d0fd2f81e1b946db`

Read `AGENTS.md` completely and rehydrate the five named sources. Do not edit,
stage, commit, push, deploy, inspect credentials/data, call A3/B3 Vertex or move
refs. Verify the same bounded recovery claims in the failed packet, plus the
AER-0020 preservation and narrow asynchronous-finalization control at revision
13. The earlier duplicate-marker envelope has no admitted decision.

Review for findings in:

- exact request diff: only `thinkingBudget: 1024` and
  `maxOutputTokens: 2048`;
- unchanged prompt/schema/proofreader/provider/project/identity/region/data/
  fallback/cost/authority boundaries;
- recovery adapter isolation, exact broker launch, distinct ids/ledgers/runtime
  names, Rayleen-before-Davida admission gating and no post-admission call;
- safe response-shape metadata with no provider text/thought/header/token leak;
- schema, audit-chain, evidence-hash, LF, zero-call dry-run and cleanup proof;
- positive bounded reasoning as an architecture-strengthening preference that
  cannot expand deterministic or material authority;
- unmounted Access AI harness/API Spine posture with zero product/database/
  GraphQL mutation/command/write/actuator surface; and
- truthful uncertainty: the old provider failure's exact cause is not proven.

Run the recovery acceptance, the same focused five-module pytest suite, Ruff,
diff check and final Git status/HEAD checks from the failed packet. Also run
`tests/test_ariadne_agent_error_register.py` and confirm revision 13 reports 20
incidents, no open incident and the exact four-occurrence duplicate-decision
pattern.

Important terminal protocol:

1. If any command becomes asynchronous, wait for it and consume every pending
   completion notification before drafting the final report.
2. Do not use the prefix `DECISION:` in progress, intermediate or tool-wait
   responses.
3. Send exactly one final report only when no background task or notification
   remains.
4. If there are no blocking findings, its final line must be `DECISION: pass`;
   otherwise that final line uses `revision_required` after the same prefix.
5. After the terminal line, stop. Do not send a follow-up response even if a
   delayed notification appears.

Report findings by severity, commands/results, observed facts versus inference,
claims not established, and the exact clean worktree/HEAD postcondition.
