# Sol acceptance: Bureau C5 provider-free implementation readiness

Date: 2026-08-05

Decision: `model_required_bureau_c5_provider_free_implementation_readiness_pass`

Accepted source HEAD: `d82de54ba59071d231adbf45a3aae1bbc0642ff4`

## Acceptance basis

- The frozen C5 plan and threat delta permit provider-free implementation and
  independent source verification before any live target or provider action.
- Sol's named recovery lease closes all fourteen independently reproduced
  worker defects with direct regressions, without widening the target,
  authority, provider, data, cost, runtime or claim envelope.
- The first exact-head Gemini veto is preserved and its two evidence findings
  are corrected through checkout-pinned runtime validation, portable durable
  path identity and distinct plan/candidate source binding.
- The focused and 269-test widened suites, reproducible acceptance evidence,
  Ruff, compilation, Bandit, JSON/YAML and whitespace checks pass.
- A genuinely fresh Gemini 3.6 Flash/high project passed corrected exact clean
  HEAD `d82de54ba59071d231adbf45a3aae1bbc0642ff4` with one terminal decision and
  changed no file.

## Decision

Accept the source only as
`provider_free_authored_synthetic_c5_implementation_readiness`. The complete
bindings, monotone shared state, inherited-socket ownership, fixed process
adapter, fresh readback, rollback and cleanup controls are authoritative for
preparing the frozen rehearsal. Model text, reviewer text, plan text and the
unmounted OpenAPI artifact carry no execution authority.

AER-0027 remains immutable rejected-worker evidence and is corrected only by
the completed Sol recovery lease and exact-head independent pass recorded in
revision 23.

No live C5 target, socket, port, task directory, candidate-runtime provider
call, ADC inspection, product route, patient/product data, database, ordinary
service, Context Fabric implementation, deployment, production, release,
Pages, protected evidence or protected ref is accepted.

The next action is to create the distinct exact-head pre-execution receipt and
then run the frozen serial C5 rehearsal. Passing source review does not itself
declare recovery success.
