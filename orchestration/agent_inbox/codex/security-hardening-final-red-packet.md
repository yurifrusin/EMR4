# Final red packet — Fresh exact-candidate veto

Decision format: `DECISION: pass` or `DECISION: revision_required`.

Worktree: `C:\Users\sarashera\EMR4-worktrees\security-hardening-final-red`.
Branch: `gemini/security-hardening-final-red`.
Frozen final candidate: `4efe9ff3363c3f563a03a1f5bd0978998ca55d07`.

Use a fresh Antigravity project. Audit the exact final candidate and its diff
from `604b3452787d45ad99d9f08e70101bfd87516671`, but do not read either worker's
earlier review artifact, the blue packet/artifact, Sol acceptance, or protected
holdout/provider/historical-data surfaces.

Attempt to make the plan or acceptance gate pass with:

- worker-controlled or unjustified non-material classification;
- aliased/overlapping packet or artifact paths, including Windows case/path
  variants and paths outside the repository;
- stale, unhashed, decision-unbound, or candidate-unbound review evidence;
- a recovered lane with no exact-final independent pass;
- malformed or case-varied unresolved critical/high findings;
- a falsified purple cadence or modified cadence ledger;
- non-local/file/data/blob smoke/dev activation;
- a malicious ngrok-lookalike backend hostname;
- an unapproved confirmation path, insecure random fallback, or identifier
  selector injection.

Also verify local `file:`/localhost smoke, approved ngrok backend selection,
all five canonical confirmation paths, and appointment re-selection remain
supported. Reproduce the executable plan gate, the focused 44-test command,
`node --check docs\diary\diary.js`, and `git diff cc6925f9 --check` serially.

Write only
`orchestration/agent_inbox/antigravity/security-hardening-final-red-review.md`
and commit it. Do not implement fixes, integrate, push, alter settings, or read
the prohibited artifacts.
