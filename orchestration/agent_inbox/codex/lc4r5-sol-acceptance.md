DECISION: pass

# LC4R5 Sol Acceptance — Explanation Clarification/Action Semantics

Date: 2026-07-14

Conductor, sprint planner, architecture/acceptance owner, recovery owner, and
protected integrator: GPT Sol.

## Authority and provenance

Sol planned LC4R5 directly after a mandatory post-compaction rehydration
receipt. Live development profiling corrected the handover's 96-record
shorthand into 84 practitioner-resolved repair cases and 12 genuinely
ambiguous preservation cases before worker dispatch.

DeepSeek V4 Flash/high through Claude Code `--bare` implemented the bounded
candidate at worker commit
`dc5c69fd142bd2318101b49beae374ee6dade7a1`. DeepSeek Pro and Deep Code were
not used. The hardened launcher pinned the child cwd, `PWD`, `INIT_CWD`, and an
explicit authorized-root packet boundary to the disposable worktree.
Protected master was clean before, throughout, and after the lane.

The worker completion note omitted the literal candidate hash required by its
packet. The durable launcher receipt and Git branch established that hash;
Sol preserved the deficient artifact unchanged and recorded the reconciliation
in `lc4r5-sol-recovery-amendment.md`. One PowerShell `Set-Content` attempt
inside the disposable worktree was denied; the worker completed through
permitted tools and left no temporary helpers or uncommitted changes.

Sol adopted the source as an untrusted runtime candidate, reviewed it, reran
focused checks, and integrated it as protected commit
`426eade96c11eef3b50ce94755927b19eda78b68`. Gemini 3.5 Flash/medium then
independently reviewed exact recovered head
`9a87f4227deee9fc1da9398fb4fcb0a6591b5df0` through a fresh Antigravity
worktree and returned `DECISION: pass` in commit `c4502c19`.

## Accepted result

The oracle-free interpretation path now treats a resolved practitioner
(`exact` or `corrected`) as sufficient read-only context after
`explain_schedule` has already been recognized. It no longer demands a patient
identity merely to explain that practitioner's schedule.

The bounded target and preservation set reproduce exactly:

- practitioner-resolved repair: 84/84, selection hash
  `b69abbcbc6febe29`;
- ambiguous-practitioner preservation: 12/12 still clarifying, selection hash
  `34c95db64c716f56`.

Existing resolved-patient explanation behavior remains supported. Ambiguous
`some doctor` and context-free explanation requests continue to clarify.
Generic calendar, schedule, and availability language is not promoted into an
action, and `_EXPLAIN_PATTERNS` is unchanged.

Full-development semantic results are:

- intended action: 880/1,152;
- action semantics: 814/1,152, up from 730 (+84);
- temporal relation: 628/1,152;
- normalized values: 101/1,152;
- entity semantics: 300/1,152;
- clarification: 782/1,152, up from 698 (+84); and
- safety: 1,152/1,152 with zero per-scenario variance over 2,304 samples.

## Verification

The Flash worker reported 146 semantic-extraction tests and two LC4R4 report
tests passing. Sol independently reproduced those 148 tests, the LC4R5 report
check, and diff hygiene before adoption. Gemini independently reproduced the
same focused checks and both selection hashes on the exact recovered head.

Sol's broader serial acceptance selected 967 tests across the semantic and
LC4R4 report surfaces, action grammar and interpretation harness, Ariadne
preflight, composed evaluator, development audit, T1 scenario replay/evidence,
T2 booking matrices, LC3 mutations, LC4 scale, scenario/evidence contracts,
and T3.1-T3.4 shadow surfaces. The result was 965 passes, one expected xfail,
and one expected skip.

Three historical exact-report equality nodes were deliberately deselected:

- the LC3 composed evaluation committed-report comparison;
- the LC4R2 development-gap committed-report check; and
- the LC4 scaled exact-report regeneration comparison.

Each live evaluator was otherwise exercised. Those reports freeze earlier
semantic baselines and are preserved as historical evidence rather than
regenerated after an accepted repair. LC4R4 used the same evidence-preservation
policy.

## Boundaries and continuation

No generated development fixture, generator, scenario schema, scorer/audit
policy, action grammar/route contract, protected holdout evidence, provider,
T3.5 adapter, API/route, database, UI, deployment, historical diary, memory,
RAG/GraphRAG, confirmation, or write authority changed. Protected holdout v1
remains sealed; T3.1-T3.4 remain preserved and blocked by default.

Post-LC4R5 profiling leaves 572 aligned-failure records. Within them are 477
normalized-value, 449 entity-semantics, 159 temporal-relation, 157
clarification, 117 action-semantics, and 26 intended-action field failures.
All 26 intended-action misses are `check_in` wording mapped by Silver to
`status_change`; `check_in` remains deliberately planned-not-implemented and
must not be promoted by language repair. The remaining action-semantics misses
are clarification-preserving ambiguous or incomplete surfaces, including the
12 explanation cases intentionally preserved here.

The next bounded step should be an LC4R6 temporal source-evidence audit over
the 159 aligned temporal mismatches. It should separate explicit relation/span
gaps from malformed or contradictory Silver phrases before authorizing any
parser change, while keeping 477 normalization failures quarantined as
contract-quality evidence.
