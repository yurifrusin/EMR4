# Reception One Proofreader Dialogue v4 — Sol Review

## Decision

`accepted_fail_closed_call_ceiling_exhausted`

The provider-free v4 implementation satisfies its typed-language, correction
ticket, proofreader-authority, isolation, audit and API Spine contracts. The
model remains an untrusted form filler; no route, database, event, appointment
write, confirmation or product release path was added.

The occupied sequence is terminal and not a success. The first call failed at
the provider request schema. The focused provider-only repair was eligible,
fully regated and used the sole remaining call. That call returned HTTP 200
but failed local schema admission at `$.version_code`; no candidate reached
the proofreader and no value was released.

The leading diagnosis—a provider-generated integer other than the required
local constant `3`—is explicitly labelled an inference because the rejected
value and raw response were correctly discarded. The evidence does not support
a stronger claim.

Both ledgers are consumed, all four audit chains and three evidence hashes
recompute, all task residue is absent, and no retry or fallback remains. The
correct closeout is therefore fail-closed acceptance of the controls, not
acceptance of live model form filling or multi-turn correction.

Any protocol-version redesign or further occupied call requires a fresh
descendant and authority. Real, product-derived, patient, health, clinical,
historical and protected data; product/database access; writes; Word; voice;
production; deployment and release remain closed.
