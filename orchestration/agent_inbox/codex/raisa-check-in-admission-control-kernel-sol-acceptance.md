# Sol acceptance — unmounted default-off ordinary-practice check-in admission-control kernel

Date: 2026-08-19

Timestamp: 2026-08-19T04:37:32.9752761+10:00 (Australia/Brisbane)

Decision: accept

Exact reviewed source: `4204ec6348abb0f92b1a30314699d4a469fa860a`

I accept the candidate as the narrowest provider-free executable descendant of
the admission-control architecture.

Acceptance is limited to these claims:

- the module is pure, standard-library and unmounted;
- the canonical reading has zero active ordinary records;
- ordinary activation authority is false and every ordinary request denies;
- synthetic-only admission remains exact while overlap denies;
- exactly five disable-biased record transitions may pass and none produces
  `active`;
- the kill switch is one-way and dominant;
- all command-envelope requirements fail closed, including full lowercase
  40-character Git identity;
- unknown commit releases no success and permits no blind retry;
- all 63 scenarios and 341 hostile mutations pass with zero escapes; and
- the corrected Gemini 3.7 Flash/high veto passes 9/9 commands at unchanged
  clean exact HEAD.

The rejected first veto is workflow evidence, not a candidate defect. Its
correction adds a useful clockwork invariant: command side effects must be
  typed before dispatch, and every external-harness completion must produce an
  admitted or digest-only rejected receipt. Register revision 521 accepts all
  corrections through AER-0602.

This grants no ordinary-practice enablement, product/API/database mount,
feature or allowlist change, generic-status `Arrived`, grammar/client/waiting-
area change, product data, provider, runtime, deployment, release, Pages, live
clockwork adoption or protected-ref movement.

The authorized successor is the provider-free shadow clockwork/DeepSeek broker
gear architecture. It must remain non-actuating and prove that derived ticks
reduce manual workflow burden before any live migration can be considered.
