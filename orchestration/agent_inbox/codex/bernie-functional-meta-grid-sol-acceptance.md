# Sol Acceptance — Bernie Functional Meta-Grid Client

Date: 2026-07-20

Reviewer: GPT Sol

Decision: `functional_meta_grid_client_pass`

## Acceptance

I accept the bounded functional meta-grid client tranche against
`docs/bernie-functional-meta-grid-client-plan.md`.

The implementation is a non-authoritative native Diary projection client over
existing read and proposal-only boundaries. It provides the accepted ordinary,
focused practitioner, patient timeline, availability, comparison, selection,
proposal-review and clarification families with deterministic plain-language
roots/refinements, reversible state, explicit scope/freshness/omission labels,
privacy masking and ordinary-Diary fallback.

Every projection retains `appointment_write_authority: false`. Smoke proposal
review cannot hand off or manufacture a receipt. The client contains no
confirmation call, arbitrary network primitive, browser persistence, event
runtime, voice or provider surface. No API, GraphQL, OpenAPI/Pydantic,
database, production or release artifact changed.

## Evidence accepted

- mandatory five-source rehydration passed before planning;
- preacceptance Ariadne receipt passed with all five rehydration sources;
- the final protected-safe serial population passed 102 tests;
- the complete existing Diary smoke review passed 139 tests after its legacy
  pilot eligibility gate was decoupled from meta-grid smoke visibility;
- Node syntax, focused Ruff, blocked-primitive scan, closed API/database diff
  scan and `git diff --check` passed;
- desktop 1440×900, tablet 1024×768 and 768×1024, and phone 390×844 and
  844×390 browser states passed with zero horizontal overflow, zero enabled
  target below 44 pixels and a clean final browser console;
- Enter, Space, Escape and native computed tab-sequence evidence passed;
- privacy, interruption/reconciliation and ordinary fallback passed; and
- browser evidence and screenshots are hash-bound in
  `orchestration/prototypes/bernie-functional-meta-grid/browser-acceptance-evidence.json`.

## Scope calibration

Browser results are exactly
`authored_synthetic_client_fixture_browser`. They are not live-backend,
PostgreSQL, provider, PII, production, representative-usability, deployment,
release, committed-event or write evidence.

Stage 3B, voice, event runtime, high-fidelity styling, external design models,
new APIs/actions and appointment confirmation remain fresh decisions.

## Disposition

Accept and integrate through the normal protected, check-gated pull-request
workflow. Return the baton to Yuri after ref alignment. The next recommended
decision is a separately authorised provider-free live-local synthetic
integration/evaluation tranche over existing read and proposal-only surfaces;
it grants no continuing authority by this acceptance.
