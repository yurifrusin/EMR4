# Sol acceptance — native Harness future-attempt identity and target rebinding

Date: 2026-08-21

Timestamp: 2026-08-21T23:08:25.7482343+10:00 (Australia/Brisbane)

Decision: `accepted`

I accept the provider-free candidate at exact machine-resolved source
`5f30c69d8cd0ad6f4c01f33b465fe7b541ef1f47`.

Acceptance is supported by an exact reversible one-literal runner
transformation, a helper regenerated from one typed fresh identity, separate
runner/helper/controller/target hashes, a canonical exclusive six-path bundle,
and a terminal that copies rather than narrates those bindings. The consumed
attempt target and predecessor helper identity are absent, while occupied
launch, target use, executable activity and raw retention remain false.

The frozen identity candidate is the full planning source
`deaf0b3ccc23adb2f2f17b275a6a7faa4d2ae2ac`; it is distinct from this reviewed
implementation source by design. Both values were copied from machine Git
readings, never abbreviated.

Completed evidence contains 53 focused, 187 accepted-chain and 548 governance
tests, plus generator, Ruff, compilation, schema and diff checks. DeepSeek,
Gemini and native subagents were declined with negative leverage for this
provider-free serial boundary. GPT Sol completed implementation, hostile-
fixture verification, review and acceptance.

The closeout preserves five contained orchestrator corrections: a rejected
caller-supplied Git object in machine-snapshot prose; restoration of a direct
candidate latch update so the clockwork remains the sole canonical latch
writer; a rejected guessed live-tick schema; a rejected noncanonical intent
filename; and a missing prospective incident-revision document rejected before
commands. Each now has an exact deterministic control. None changes the
accepted candidate or opens execution.

This decision proves deterministic identity and inert-target rebinding only.
It does not authorise or prove native Harness boot, a DeepSeek worker, model or
provider activity, target-file creation, product or clinical data, production,
deployment, release, Pages or protected-ref movement.
