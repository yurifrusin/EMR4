# Inert durability DDL rehearsal recovered-plan replacement challenge

Date: 2026-08-07

Candidate HEAD: `e00d5d01d534a9e005cd6deea5c82bb41ec73120`

Review worktree: `C:/Users/sarashera/EMR4-worktrees/r37`

Review branch:
`codex/review-durability-inert-ddl-plan-recovery-e00d5d01`

Model allocation: one fresh Antigravity project, exact
`gemini-3.6-flash-high`, explicit high reasoning.

## Authority and containment

This is a replacement read-only pre-implementation architecture veto. Do not
edit, create, stage, commit, fetch, push, install, write caches, access the main
worktree, open a database/source/outbox/feed/watcher/listener/product runtime,
execute or apply SQL/DDL, create a migration, touch credentials, call a provider
product path, or access patient/product/protected/historical-PHI data. Do not
read the first review receipt or its conclusion. Sol retains plan meaning,
recovery and acceptance.

## Exact allowlist

Read completely from `r37`:

- `AGENTS.md` sections 2-7 only;
- the inert DDL plan, design, recovery and threat-model delta;
- the focused inert-DDL plan test;
- the exact accepted structural and body contract JSON files;
- the two parent closeouts and Sol acceptance files; and
- Ariadne error-register revision 82 only for the stated rejected-evidence
  boundary, not as a substitute for inspecting the contracts.

Do not enumerate or read other paths.

## Required replacement challenge

Independently determine whether the recovered plan is exact and feasible for a
mechanical implementation worker without invented security-critical semantics.

1. Traverse the exact immutable body contract and report both populations
   separately: 22 declared/21 observed instruction opcodes, naming
   `DERIVE_BINDING` as the sole absent declaration; and 34 declared/34 observed
   expression opcodes. A different count requires `revision_required` with the
   machine path used to derive it.
2. Verify that lowering only 21 observed instruction forms is total for the
   hash-bound 22 programs and that encountering the unobserved form fails rather
   than silently adding compatibility semantics.
3. Inspect every `EXACTLY_ONE` form and decide whether the recovered universal
   implicit mapping to registered value-free `F_CARDINALITY`/`CF004` is both
   complete and compatible with every body-specific reachable-failure ceiling.
   Identify any program for which that mapping would add an unaccepted effect.
4. Inspect all 21 `INSERT_OR_RELOAD_COMPARE` nodes. Mechanically verify that
   each ordered `conflict_key_columns` population maps to exactly one effective
   primary/unique constraint or uniquely named enforcing index after REC01-REC26,
   and that PostgreSQL `CONSTRAINT_NAME` can fence the intended SQLSTATE 23505.
   Verify winner reload by key plus `winner_predicate`, `F_CARDINALITY`/`CF004`
   on missing/mismatch, and unchanged rethrow of every other violation. Report
   any node that cannot be rendered from exact operands/catalogue facts.
5. Verify the exact descendant activation delta: parent omission evidence stays
   immutable and only fixed-path `.sql.inert` rendering opens after both hashes
   and all 22 programs reconcile. It must not imply migration or execution.
6. Recheck effective catalogue derivation, six phases, reference-only
   `public.*`, digest framing/core PostgreSQL-16 primitives, retry-marker
   elimination, static-recognizer claim calibration, hostile mutations and
   separation of the later disposable PostgreSQL gate.
7. Try at least four concrete counterexamples, including wrong unique
   constraint, zero/multiple winner, encountered `DERIVE_BINDING`, and a parent
   omission flag treated as execution authority.

Report every P0-P3 finding with exact path/section and minimal correction.
Passing tests does not override a conceptual gap. Do not repeat or infer any
prior review decision.

## Allowlisted commands

```powershell
$env:PYTHONDONTWRITEBYTECODE='1'
$env:RUFF_NO_CACHE='true'
git rev-parse HEAD
git status --short
C:/Users/sarashera/emr4/.venv/Scripts/python.exe -m pytest -q -p no:cacheprovider tests/test_raisa_provider_free_unmounted_durability_inert_ddl_rehearsal_plan.py
C:/Users/sarashera/emr4/.venv/Scripts/python.exe -m ruff check tests/test_raisa_provider_free_unmounted_durability_inert_ddl_rehearsal_plan.py
git diff --check HEAD^ HEAD
git status --short
git rev-parse HEAD
```

Read-only `Get-Content`, exact-path `rg`, `git show`, `git diff` and short
in-memory Python expressions over the two allowlisted contract JSON files are
also allowed. Do not create files or caches.

## Terminal response

Return one schema-constrained object only, with `decision` exactly `pass` or
`revision_required` and all evidence/findings in `review`. Include the exact
declared/observed counts, unique-node reconciliation result, failure-registry
mapping, hostile counterexamples, tests and clean exact-HEAD postflight. Do not
self-repair.
