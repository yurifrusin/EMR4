# Sol acceptance — default-off ordinary-practice canonical check-in admission-control architecture

Date: 2026-08-19

Timestamp: 2026-08-19T03:24:22.2795586+10:00 (Australia/Brisbane)

Decision: accept

Exact reviewed source: `752b521c59f5b44bf46de0cf776a33ac74b8134d`

I accept the architecture as the narrowest API-Spine-compatible, provider-free
and default-off successor to the 6/3/3 readiness review.

Acceptance is limited to these claims:

- authored-synthetic and future ordinary-practice admission are distinct,
  non-substitutable lanes;
- ordinary admission defaults absent and denied;
- the four-state, six-transition record has no resume edge;
- withdrawal is disable-only and cannot activate;
- the in-generation kill switch is globally dominant and one-way;
- all five future command operations are unmounted and unauthorized now;
- Git authority requires a resolved lowercase 40-character commit;
- all three operational-evidence gates remain mandatory;
- five non-PHI metric families and six non-actuating alerts are frozen;
- the deterministic validator rejects all 390 hostile mutations;
- Gemini 3.7 Flash/high passes the exact candidate with no P0-P2 finding.

AER-0590 is accepted as corrected pre-provider workflow evidence. The repeated
launcher lapse strengthens the case for projecting command grammar from the
typed clockwork rather than relying on orchestrator memory.

AER-0591 likewise records and corrects a read-only multi-ref `rev-parse`
invocation. The clockwork must generate one typed `VerifyRef` tick per object.

This acceptance grants no ordinary-practice enablement, feature-flag or
allowlist change, product code/configuration or mounted route, generic-status
`Arrived` change, grammar/client change, waiting-area movement, product data,
provider, runtime, deployment, release, Pages, live clockwork adoption or
protected-ref movement.

The authorized successor is the provider-free unmounted default-off kernel
rehearsal with zero active records and exact default denial.
