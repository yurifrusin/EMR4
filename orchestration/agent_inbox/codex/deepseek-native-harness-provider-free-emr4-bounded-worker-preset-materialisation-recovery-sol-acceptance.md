# Sol acceptance — DeepSeek native Harness provider-free emr4-bounded-worker preset materialisation recovery

Date: 2026-08-20

Timestamp: 2026-08-20T10:52:45.8367551+10:00 (Australia/Brisbane)

Decision: `accepted`

GPT Sol accepts exact reviewed candidate
`962e91d9af90fbe977177d251f9f3e3134aaad19` for this bounded provider-free
preset materialisation recovery.

Acceptance basis:

- six exact rc.7 cached packages and their required members match registry and
  local digests without registry, npm, Node, network or disk extraction;
- the exact 158-byte payload at
  `.agent-presets/emr4-bounded-worker/agent.cordis.yml` has only the two ordered
  official plugin rows and the required false search-sampling configuration;
- the raw inherited surface is explicitly recorded as broader than authority;
- the accepted post-mount guard maps both attachment postures to exactly sorted
  `edit`, `glob`, `read`, with the broker allowlist still mandatory;
- 21 hostile variants fail closed and both consumed native attempts remain
  immutable;
- the candidate passes 24 focused tests, while the independent manifest passes
  101 exact provider-free tests plus Ruff, compilation, deterministic check and
  Git cleanliness checks; and
- the first Gemini 3.7 Flash/high receipt at SHA-256
  `c21fa4751e42ec42b4c392f5f67088063855b85ef52333917d554ea5ecae54a8`
  records an exact `pass`, ten zero-exit commands, unchanged candidate HEAD and
  a clean isolated worktree.

The bounded prepublication mechanical corrections recorded in the efficacy
reading do not alter candidate semantics or require a provider retry. The
postpublication compatibility chain did repeat the previously registered
discarded-session-handle error. Its unchanged observed rerun passed at exit
zero; the first lease-53 publication is preserved, rolled back byte-exactly at
lease 54 and admitted as a qualifying clockwork-owned incident before final
acceptance.

The corrected full-envelope run then exposed one stale register compatibility
assertion after correct derivation of `AER-0666`. Lease 55 is likewise
preserved and rolled back byte-exactly at lease 56; the exact prospective
recurrence fixture is corrected and this repository defect enters the same
clockwork-owned revision before acceptance.

Claim ceiling: this proves an exact installation-ready preset payload and its
deterministic post-mount projection. It does not prove live discovery, mount,
combined service activation, native scope or schema observation, an occupied
DeepSeek worker, or model/provider reliability. No product, data, runtime,
deployment, release, Pages or protected-ref authority is granted.
