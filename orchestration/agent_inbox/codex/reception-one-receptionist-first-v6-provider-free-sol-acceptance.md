# Sol Acceptance — Reception One Receptionist-first v6 Provider-free Gate

Status: accepted
Recorded: 2026-07-30

## Review

The receptionist-first v6 descendant is accepted through its provider-free
gate.

The evidence establishes:

- one exact model response envelope containing a bounded natural receptionist
  response, bounded decision note, evidence indices and a separate typed form;
- broker injection of only `version_code: 3`;
- no derivation of typed fields from natural prose;
- independent deterministic checks over prose/form agreement and the inherited
  typed compiler, proofreader, freshness, supersession and no-write release;
- one closed correction ticket and an absolute one-correction-per-case limit;
- a fixed 1024-token thinking budget with `includeThoughts: false` and retention
  only of provider-supplied token counts;
- twenty-four reproducible authored-synthetic typed oracles with zero
  credential reads or provider calls; and
- a two-turn real-isolation pass using non-root, read-only, network-none,
  resource-bounded, credential-free cells with complete cleanup.

## Finding

The dual-channel boundary is coherent: JSON is the API transport and
pre-printed packet, not the agent's occupational identity. The natural response
remains useful for human audit, while the typed form alone feeds deterministic
compilation and no-write proposal execution. The stronger prose gate cannot
compensate for a wrong typed meaning, so exact oracle comparison remains
decisive.

The reused v5 cases are development comparison inputs, not an untouched
holdout. Any occupied result must retain that limitation.

## Occupied admission

No provider call is admitted by this acceptance alone. Occupied execution
still requires:

1. all focused, inherited API Spine and repository-only regressions;
2. Continuity/Compass revision binding and an exact rendered Compass pass;
3. a fresh read-only verification of the existing Bernie impersonated ADC and
   cloud-control posture;
4. zero task-scoped container, image, network, credential-file and process
   residue; and
5. a fresh pre-attempt receipt.

The exact provider, model, project, service account, keyless ADC, Sydney
endpoint, authored-synthetic data, 48-call, USD 1, no-fallback, no-tool,
no-product, no-write and no-delivery boundaries remain unchanged.

## Disposition

`reception_one_receptionist_first_v6_provider_free_accepted`
