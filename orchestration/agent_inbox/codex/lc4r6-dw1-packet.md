# LC4R6 DW1 — Temporal Source-Evidence Audit

You are the bounded report/test worker for LC4R6. Work only in the disposable
worktree and branch supplied as your current directory. GPT Sol is Conductor,
acceptance/recovery owner, and protected integrator. You have no planning,
acceptance, integration, or push authority.

Read `AGENTS.md` and
`orchestration/agent_inbox/codex/lc4r6-temporal-source-evidence-contract.md`
completely, then implement that contract exactly.

Constraints:

- Use DeepSeek V4 Flash/high only through this Claude Code bare session.
- Do not open, enumerate, list, search, import, run, regenerate, hash-check, or
  inspect protected holdout fixtures, support modules, seals, receipts, or
  reports. Do not use broad repository file listings.
- Use only the ordinary development loader, current oracle-free extractor,
  composed scorer, and development audit.
- Do not modify runtime interpretation, core audit/scorer/replay, fixtures,
  generators, schemas, AGENTS.md, earlier reports, action grammar, providers,
  routes/API, database, UI, deployment, T3 gates, or historical material.
- Do not treat source-span field names or expected values as interpreter truth.
- Preserve exact `tomorrow at 3pm`, lossless normalization, `check_in` as
  planned-not-implemented, T3.1-T3.4, and all live/write deferrals.

Required work:

1. Add the one audit-only report script, one focused test module, JSON report,
   concise audit note, and completion artifact listed in the Sol contract.
2. Reproduce selection 159/hash `f56b4a20aad6161c`; insufficient evidence
   84/hash `c341652065504d17`; surface conflicts 75/hash
   `fd04b9c86a54fea4`; parser gaps 0/hash `e3b0c44298fc1c14`.
3. Reproduce all frozen subtype counts and unchanged LC4R5 semantic, safety,
   and variance results.
4. Add deterministic order-invariance, fail-closed taxonomy/report drift,
   aggregate-only output, oracle-independence, and protected-boundary tests.
5. Run focused/proportional checks and `git diff --check`.
6. Commit only the five owned files. The completion artifact must record exact
   implementation commit provenance in a non-circular way: identify the source
   commit after committing implementation files, then use a final evidence
   commit for the completion artifact if necessary. Do not push.
7. Finish with exactly one final line: `DECISION: pass` or
   `DECISION: revision_required`.

Use `C:\Users\sarashera\emr4\.venv\Scripts\python.exe` for Python and pytest.
If any frozen result differs, return `revision_required`; do not silently
change the taxonomy.
