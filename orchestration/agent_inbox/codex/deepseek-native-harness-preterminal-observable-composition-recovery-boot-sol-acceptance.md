# Sol acceptance — preterminal-observable composition recovery boot

Date: 2026-08-20

Decision: accept the bounded fail-closed result; reject native effective-tool
composition.

I accept exact deterministic candidate
`e4dddfcb48cd236e147bca2560e54f42d84017df`, the one consumed
`preterminal-observable-composition-recovery-boot-attempt-001`, its exact
`SERVICES_UNAVAILABLE` terminal, `10025 ms` finally-path duration, zero
prohibited-call reading and complete process/filesystem cleanup.

I do not accept effective `edit`, `glob`, `read` composition: the runner entered
but found its required `agentPresets` or `tools` service unavailable before
scope creation and guard entry. The terminal identifies the exact bounded
branch but not which service or binding mechanism is causal.

The attempt is immutable. No retry, resume or reclassification is authorised.
Gemini review was correctly withheld because its gate required a passing native
terminal. The next admissible work is a provider-free deterministic
required-service injection recovery; any later native process requires a new
separately frozen operation and attempt identity.

This accepts no occupied worker, model/provider call, product/data/runtime,
ordinary-practice, deployment, Pages or protected-ref authority.
