# Sol acceptance — historical-derived scenario first-use candidate gate evaluator

Date: 2026-08-24

Timestamp: 2026-08-24T10:07:35.8928123+10:00 (Australia/Brisbane)

Exact reviewed source: `abcd4206a363b0c565c070e0f2cb9c54d627b3b3`

Decision: `accept_evaluator_first_use_still_closed`

I accept the provider-free typed evaluator. Its input surface has no
unconstrained prose field; unknown keys and values fail schema validation; the
source is one exact full Git object ID; and both candidate digest and utility
are recomputed. The output vocabulary is closed and an admitted result binds
one exact digest, purpose, class and local-test ceiling non-transitively.

The authored-synthetic matrix is complete: one positive, five blocked, two
revision-required and five schema-rejected cases behave exactly as frozen. All
229 unique controls across the focused and historical-Diary families pass with
static and source-boundary checks.

I accept no actual first use. No historical candidate or reusable artifact was
created, the evaluator opens no writer, and no archive, ignored attempt,
provider, product, runtime or ordinary-practice surface was accessed.

DeepSeek was declined with negative leverage because the native Harness remains
paused, Claude is not a silent fallback and this small authority seam is not a
separable worker package. Gemini was not applicable with neutral leverage
because deterministic provider-free acceptance supplied the complete reading.
Native subagents were declined with negative leverage because schema, digest,
decision and tests were one tightly coupled unit. GPT Sol owned the tranche.

AER-1150 and AER-1151 record the contained preplanning-event and literal-test
reruns. Neither changed gate meaning or caused private/provider work.

The dependency-satisfied successor is one provider-free clockwork subgate. The
current historical mode explicitly forbids fixtures, so the clockwork must gain
one exact gate-wrapped minimised-materialisation mode before private access. It
will use authored-synthetic boundary forms only. After it passes, one local
materialisation attempt may proceed and must write nothing unless this exact
evaluator admits the in-memory candidate and written bytes match the digest.
