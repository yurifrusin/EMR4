# Sol Review — Gemini 2.5 Flash Sydney ADC-Restored Continuation

Date: 2026-07-24
Disposition: accepted fail-closed result
Result: `ariadne_vertex_sydney_gemini_25_cache_control_blocked`

Accept the terminal result. Yuri's external authentication action resolved the
earlier ADC-refresh gate for a distinct continuation. The repeated read-only
preflight proved the exact impersonated credential type, project, service
account, scope and non-interactive refresh, plus the recorded billing/API,
prediction-only role/binding/permission, Data Access audit,
request-response-logging, key-inventory, Sydney endpoint and publisher-model
catalogue controls.

The check did not verify explicit `disableCache: true`. That is a frozen stop
condition, and Codex has no authority to determine or change the external
setting beyond the completed read-only check. Tranches 4–6 never opened.

Occupied provider calls, retries, durable rehearsal/occupied ledgers,
containers, networks, images, broker processes, temporary credential files
and model-call cost are zero. No prompt, model payload, provider inference,
draft or proofreader release exists.

The continuation-specific external audit passes its eight-event chain and
artifact bindings. Independent repository-only review passes 36 tests after
binding the restored audit, correcting cache-state wording and distinguishing
key inventory metadata from key material. It reviewed exact Continuity graph
revision 37 and Compass map revision 24; the final closeout increment must bind
these review artifacts in the next synchronized Continuity/Compass revision.

Continuation requires an externally authorised operator to determine the
current Vertex provider-managed in-memory cache state for `bernie-emr4-dev`
and establish explicit `disableCache: true` if needed without changing any
other frozen boundary. A fresh five-source rehydration and complete repeated
Tranche 3 preflight are then required.

No evidence proves provider acceptance, inference, real isolation, typed
release, Australian physical processing or sovereign processing.
