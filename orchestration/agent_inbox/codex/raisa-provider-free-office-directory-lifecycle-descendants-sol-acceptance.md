# Sol acceptance: provider-free Office directory lifecycle descendants

Date: 2026-08-03

Decision: `accepted`

Terminal result: `provider_free_office_directory_lifecycle_descendants_pass`

## Results accepted

1. `provider_free_office_reload_terminal_reconciliation_pass`: repeated
   delivery revokes surviving authority once and returns an explicit inert page;
   the stale original DOM cannot complete a read.
2. `provider_free_office_session_loss_reconciliation_pass`: revoked and expired
   sessions converge on one fixed, sanitized close/reopen terminal state.
3. `provider_free_office_cross_surface_replay_isolation_pass`: cookie/surface,
   CSRF and result-nonce swaps and replay are denied with zero product reads.
4. `provider_free_office_lifecycle_observability_pass`: a closed ten-reason
   counter set contains no identifiers, opaque values or routing authority.
5. `provider_free_default_off_office_consumer_adapter_pass`: the reusable
   lifecycle decision service remains route-free, unmounted and unable to own
   cookies, database access, identity, product reads or commands.

## Evidence judgment

Three fresh disposable PostgreSQL scenarios passed with complete database,
four-role and pool cleanup per scenario. Their evidence is labelled
`provider_free_in_process_backend_postgres`; it is not mislabelled as browser or
real Office evidence. The accepted parent still supplies the supervised real
installed Word and Word Online path. Durable evidence contains no exercised
session, CSRF or nonce value and records zero provider, identity, patient,
clinical, document, product-read, product-write, deployment or production side
effect for the lifecycle matrix.

## API Spine judgment

The existing fixed GraphQL read and REST logout command are unchanged. The
adapter cannot choose an operation, field, practice, role, resource or command.
No route is added to `app.main`; no OpenAPI/GraphQL command contract, migration,
provider adapter or async transport is introduced.

## Limits and next candidate

This accepts lifecycle hardening only. It establishes no live Microsoft or real
identity mapping, patient/clinical/document access, broader product read,
command/write, distributed abuse resistance, monitoring/SIEM, organisational
Office deployment, protected integration, production or release. The next safe
candidate is an architecture/composition review for a default-off native Diary
consumer of the same active-practitioner read; implementation remains a fresh
authority gate.

Reasoning level: High. Each descendant stayed within the frozen provider-free,
authored-synthetic, active-practitioner and no-command boundary; no failed gate
was overridden and no material product fork appeared.
