# Sol acceptance: native Harness root-service-forwarding process-free correction

Date: 2026-08-22

Timestamp: 2026-08-22T09:01:43.2459891+10:00 (Australia/Brisbane)

Decision: **accepted at its prospective source-correction meaning**

I accept exact implementation source
`2c25ce7d65199e82c4d4fe93bbd1d0efc80474fe` and its immutable v2 evidence. All
23 ordered predicates, the focused 11-test file, the exact broader 169-test
collection and the governance gates pass.

The accepted correction makes the runner's root preset service explicit,
removes every private service dereference from the guard and places the service
check, mount read, mount validation and bound invocation inside the typed
bridge sanitizer. The contract contains no caller-authored Git object ID; plan
and candidate commits are machine-resolved. The accepted candidate binding is
idempotent: recomputation after the evidence commit remains byte-equal and
retains the controller-owning implementation source.

This acceptance proves no JavaScript evaluation, installed module graph,
native-Harness boot, DeepSeek turn, model/provider access, worker quality or
retry. The dependency-satisfied successor is one separately frozen isolated
Node-fixture rehearsal of the derived bridge behavior.
