# Sol review — Reception One visual and interaction synthesis

## Disposition

`pass_with_vertex_candidate_rejected`

The product tranche is acceptable as a bounded deterministic UI synthesis.
The occupied Vertex sub-attempt remains correctly recorded as
`revision_required`; it is not relabelled as a successful model-design result.

## Reviewed boundaries

- The Reception One UI node remains unoccupied and deterministic.
- The one live model ran only in the disposable authored-synthetic
  design-synthesis cell.
- The exact Sydney Vertex project, service account, keyless ADC, model and
  regional hostname are preserved in the external audit.
- The proofreader released no field after `priority_order_invalid`.
- The request-contract-only second call was not spent on a semantic output
  failure.
- The rejected candidate was not retained or used.
- No API, GraphQL, OpenAPI, Pydantic, database, appointment-write, new event
  family, production, deployment or release surface changed.
- The local UI source contains no provider transport or credential path.

## Product review

The implemented hierarchy is materially clearer than the prior card list:

1. current scoped intent;
2. exact current Diary state;
3. candidate time projection;
4. selected-but-not-reserved boundary; and
5. proposal preparation or ordinary-Diary escape.

The implementation correctly diverges from the generated concept where the
concept invented three practitioner lanes and busy blocks. The product renders
only facts supplied by the existing scoped read/proposal boundary.

The responsive evidence proves exact 1440×900, 1024×768, 768×1024 and 390×844
rasters, zero horizontal overflow, complete touch targets, distinct candidate
positions, native keyboard movement/selection and Escape fallback. Screenshot
hashes verify.

The exact-raster runner observed the expected default-off local committed-event
feed 404 once per fresh context. It treats only that precise read-only response
as known; unexpected HTTP, console, page, external-host and command-route
findings remain empty.

## Acceptance condition

Accept the visual-synthesis node only with the candid compound result:

`reception_one_visual_interaction_synthesis_pass_with_vertex_candidate_rejected`

Do not claim a released Vertex design, physical or sovereign Australian
processing, representative usability, production readiness, public brand
adoption or any model presence in the UI node.
