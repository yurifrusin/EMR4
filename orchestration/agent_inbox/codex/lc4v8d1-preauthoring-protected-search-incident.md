# LC4V8D1 Pre-Authoring Protected Search Incident

Date: 2026-07-16

Decision: `metadata_only_incident_contained_no_d1_authoring_contamination`

During Sol's ordinary-code discovery before the V8D1 contract or probes were
authored, a repository search excluded the protected V8 fixture and tests but
failed to exclude the protected V8 evaluator filename. The result contained
only the evaluator path and interface-level matches identifying its ordinary
`resolve_policy` dependency and `policy_resolution` output dimension. Both
facts were already public in the frozen V8 contract and aggregate acceptance.

The search returned no receptionist utterance, Gold value, case ID, coverage
cell, diary state, per-case result, selection, or recoverable oracle content.
The evaluator file was not opened, imported, executed, hashed, edited, or sent
to an external worker. No D1 fixture, contract, runner, baseline, or worker
packet existed at the time, so no authored development evidence can contain
case-level V8 information.

Containment is immediate:

- all protected `lc4v8*` implementation, authoring, fixture, manifest, seal,
  marker, test, and per-case surfaces remain forbidden;
- subsequent discovery uses exact named ordinary-development files only;
- D1 authorship uses solely the committed public V8 aggregate acceptance and
  independently chosen ordinary receptionist prompts; and
- external worker/reviewer packets must forbid every V8 protected path and
  must record this incident without receiving any protected source.

This incident does not authorize V8 reuse, inspection, rerun, rescoring, or
repair. It is preserved as a scope breach, but its metadata-only content adds
no non-public semantic label and does not block clean ordinary-development D1
authorship under Yuri's existing authorization.
