# Independent veto packet: Context Fabric model-required intent shaping

Date: 2026-08-06

You are the independent Gemini 3.6 Flash/high veto reviewer. Review only the
exact committed candidate below. Do not edit any file and do not implement a
repair.

## Exact candidate

- Worktree: `C:\Users\sarashera\EMR4-worktrees\context-fabric-model-intent-shaping-review`
- Branch: `codex/review-context-fabric-model-intent-shaping`
- Required HEAD: `44f341481b55f99a18a47838da0f2b7e43a2f73e`
- Frozen planning source: `5ddc052fae16298436d8873312e48464d52a9567`
- The worktree must remain tracked-clean and unchanged.

## Review objective

Determine whether the new authored-synthetic model-required Context Fabric
intent-shaping rehearsal is safe to admit to its distinct source-bound
pre-execution receipt and then the frozen bounded occupied rehearsal. Review
the complete diff `5ddc052fae16298436d8873312e48464d52a9567..44f341481b55f99a18a47838da0f2b7e43a2f73e`
and the exact 13-file source-hash set:

- the eight files under
  `orchestration/continuity/raisa-authored-synthetic-model-required-practice-context-fabric-intent-shaping-rehearsal/`
  named `authored-synthetic-intent-shaping-request.json`, the six schemas, and
  `occupied-rehearsal-evidence.schema.json`;
- `scripts/raisa_authored_synthetic_model_required_practice_context_fabric_intent_shaping_contracts.py`;
- `scripts/raisa_authored_synthetic_model_required_practice_context_fabric_intent_shaping_broker.py`;
- `scripts/raisa_authored_synthetic_model_required_practice_context_fabric_intent_shaping_live.py`;
- `scripts/raisa_authored_synthetic_model_required_practice_context_fabric_intent_shaping_acceptance.py`; and
- `tests/test_raisa_authored_synthetic_model_required_practice_context_fabric_intent_shaping_rehearsal.py`.

Also inspect the frozen plan/design/threat delta and the committed provider-free
acceptance, dry-run, ledger, audit and attempt evidence. Provider-free test
execution is permitted using
`C:\Users\sarashera\emr4\.venv\Scripts\python.exe` with this review worktree
as cwd.

Adversarially verify:

1. the model sees only the authored-synthetic staff utterance, closed intent,
   temporal-coordinate and cue vocabulary, never source frames, catalog,
   authority binding, patient/product data or parent release material;
2. provider transport is fixed to Vertex `gemini-2.5-flash`, project
   `bernie-emr4-dev`, exact Bernie impersonated service account,
   `australia-southeast1`, thinking budget 1024, output cap 2048, one
   candidate, temperature zero, no tools/retrieval/cache/fallback and a
   provider-supported response-schema subset;
3. the stricter local Draft 2020-12 schema and deterministic proofreader still
   enforce exact cue uniqueness/order, grounding, all-false authority and the
   unchanged parent same-packet proofreader;
4. a live request is materialised fresh with exactly 600 seconds of life and
   independently rejected by the broker when future, expired, naive-clock or
   wrong-duration, while the committed provider-free fixture remains stable;
5. schema-invalid bodies can reach only the one closed correction path, every
   contact consumes a single-use ledger, at most two calls/USD0.50 are possible,
   positive reported thinking is terminally required before proof/release, and
   no call follows admission;
6. release, nested candidate envelope and parent packet seals are revalidated;
   zeroisation cannot mutate a released value;
7. final tranche evidence contains only a closed digest-bound attempt summary,
   with no raw model/provider/credential material, while separately retained
   authored-synthetic attempt evidence is exact-path and hash bound;
8. all nested schema object/array positions are bounded, result/call/review/
   path/hash relationships fail closed, and zero-call evidence cannot masquerade
   as occupied evidence;
9. the isolated Docker cell remains no-mount/read-only/internal-network,
   cleanup-complete and provider-free in dry-run; and
10. no application, GraphQL, REST/OpenAPI, product runtime, database, watcher,
    command, clinical/administrative authority, deployment, release, Pages or
    protected-ref surface was opened.

Look especially for a path that could admit stale or unreviewed source, retain
raw/provider/credential content, undercount calls or cost, let model text become
authority, accept tampered nested release material, or fake the occupied pass.

Do not access ADC or cloud configuration; do not call Vertex; do not inspect
patient/clinical/product data, protected holdouts, historical Diary material or
`docs/branding/`; do not deploy, release, rebuild Pages or move any Git ref.

## Decision contract

Report concise evidence and every material finding with file and line. If any
material uncertainty remains, require revision. End with exactly one terminal
decision line, using only `DECISION: pass` or
`DECISION: revision_required`. Do not emit a second decision line or any later
follow-up.
