# Sol acceptance — Raisa public access and Word Online verification

## Disposition

Accepted as an evidence-bounded partial integration result.

The exact Cloud Run service-only public-access mechanism, public static route
matrix, security policy, hosted companion surface and Office manifest
validation pass. Zero `allUsers` IAM bindings remain, and no other service, IAM
policy or organisation policy changed.

The Word Online gate is not accepted as executed. The browser reached the
developer `Upload Add-in` dialog but transmitted no manifest because the local
Chrome extension file-URL access prerequisite is unavailable. No add-in,
synthetic request, document content, provider, backend, database, confirmation
or command path ran.

The next action is narrowly blocked on Yuri enabling **Allow access to file
URLs** for the ChatGPT Chrome Extension. The exact service revision and manifest
must be reused. After a terminal Word result, both task-created blank documents
must be removed and the node must receive a final closeout descendant or
superseding acceptance.

This acceptance does not establish Word Online interoperability, authenticated
Office identity, clinician-role authorization, safety for real or product data,
processing geography, production fitness or release readiness.
