# Sol Final Acceptance — Reception One Pre-printed Form v5

Disposition: `accept_terminal_first_turn_pass`

The v5 descendant achieved its bounded objective without using its correction or
request-repair allowance. Exactly one `gemini-2.5-flash` call used the existing
Bernie impersonated ADC and the
`australia-southeast1-aiplatform.googleapis.com` endpoint. It returned HTTP 200
in 2,574 ms with 2,229 prompt, 535 candidate and 2,764 total tokens.

The model-authored body contained only `operator_note`, `goal_code` and `steps`.
The broker injected only `version_code: 3`, recorded distinct body,
pre-printed-manifest and assembled-program hashes, and performed no judgement
repair. The unchanged PlanProgram-v3 validation, compiler and deterministic
proofreader admitted six typed operators with no violation, safe repair or wire
repair. The atomic release is an authored-synthetic move proposal requiring
human confirmation; it performed no write.

The primary ledger is consumed. No second ledger exists, no call followed
admission, no correction ticket or fallback was used, and all ten turn-audit
events and four parent-audit events recompute. The provider-free two-cell
isolation, occupied cleanup and independent residue checks pass. The caller's
initial wait timed out while the underlying lifecycle finished, but the
lifecycle itself completed and wrote its evidence; nothing was reconstructed or
replayed.

This is a clean positive untaught baseline. It does not justify fine-tuning, and
it does not yet prove that prompt-context teaching is unnecessary: one known
authored-synthetic move is too small a sample. The next defensible training
decision should follow a frozen multi-case authored-synthetic evaluation across
the existing goal and operator catalogue, comparing exact first-turn form,
compiler and proofreader outcomes before introducing examples.

This acceptance proves the configured and observed Sydney locational request
path, not Australian physical or sovereign processing. It grants no product,
database, patient, health, clinical, appointment-write, Word, voice,
production, deployment or release authority.
