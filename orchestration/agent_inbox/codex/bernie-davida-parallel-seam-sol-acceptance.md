# Sol acceptance: Bernie/Davida parallel seam

Date: 2026-08-03

Decision: `accepted`

Terminal result: `bernie_davida_parallel_seam_pass`

## Judgment

The candidate meets its frozen architecture-only acceptance. It separates the
Bernie and Davida cognitive/runtime identities while sharing only deterministic
kernel mechanics under separately pinned policies. The backend remains sole
database and command authority. The design preserves the API Spine split among
GraphQL reads, REST/OpenAPI commands, committed events and declarative
manifests.

The Diary audit correctly narrowed its lane to an application-session
composition alongside the already existing bearer GraphQL/REST-fallback path
and excluded both agent proofreaders and Office terminal lifecycle semantics.
The Davida audit correctly preserved backend ownership of Practice
Administration truth, the advisory-only practice-knowledge substrate and a
future human-confirmed command plane rather than reusing write-authorized Diary
envelopes or side-effecting resource reads.

The existing Ariadne harness is sufficient. Exact worktree/path packets,
serial PostgreSQL and Gemini queues, explicit staging and root-only shared
reconciliation provide the required protected single-track integration. The
fresh Gemini review found no issue and reproduced the exact deterministic
population on an unchanged candidate.

## Limits

This accepts a shared seam and lane authority only. It creates no provider or
model product runtime, real data/identity, database connection for an agent,
read or mutation route, apply command, container deployment, cloud/IAM change,
production/release result or protected integration. The user-owned branding
directory remains untouched and excluded.

Reasoning level: Extra High for material architecture/security/authority
acceptance; High for bounded deterministic and Git mechanics.
