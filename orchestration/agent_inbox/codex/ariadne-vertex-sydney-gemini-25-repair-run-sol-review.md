# Sol Review — Ariadne Sydney Vertex Gemini 2.5 Flash iterative repaired run

Date: 2026-07-24
Decision: `ariadne_vertex_sydney_gemini_25_occupied_rehearsal_pass`

I accept the final result. A small authored-synthetic typed request was
accepted by `gemini-2.5-flash` through the exact
`australia-southeast1-aiplatform.googleapis.com` Vertex locational endpoint
using the existing keyless Bernie impersonated ADC. The deterministic
proofreader released exactly `summary`, `total_tiles`, `risk_level` and
`evidence_ids`; it found no defect and performed no repair.

The successful attempt was not a blind replay. The prior string-valued enum
still returned HTTP 400. Official structured-output guidance permits only
string enums, so the next constructor removed the enum from the INTEGER field
and used exact numeric minimum/maximum constraints while leaving the stricter
release proofreader unchanged. The complete request parsed the installed
official Vertex protobuf and focused regressions passed.

One provider-free run then exposed a local relay-listener readiness race before
broker contact. Its zero-call ledger was consumed and cleanup completed. A
connection-refused-only pre-connect retry repaired that race without allowing a
second request after connection. The next distinct provider-free lifecycle
passed its typed exchange, proofreader, audit chain and residue gates.

The successful occupied ledger reserved and consumed exactly one call. The
eight-event audit chain records HTTP 200, 1108 ms, 176 prompt tokens, 50
candidate tokens and 226 total tokens. No API-key path, service-account key,
global endpoint, regional fallback, provider tool, grounding, retrieval,
explicit cache, product data, database access or command authority was used.
No provider call followed success.

All opened repair-run ledgers are consumed, including the provider-free setup
failure. Task containers, networks, images, broker processes, temporary tokens
and contexts are absent. Pre-existing local development servers were preserved
as unrelated user state.

The API Spine remains closed. This result opens no product command, database,
appointment write, patient or clinical information, production path,
deployment, release or human delivery.

The evidence supports the configured and observed Sydney locational request
path and Google's published contractual regional support. It does not establish
Australian physical or sovereign processing. A container constrains local
capability and credential exposure; it does not determine remote provider
processing geography.

This descendant closes on success at Continuity graph revision 49 and Compass
map revision 36. No additional provider call or fallback is warranted.
