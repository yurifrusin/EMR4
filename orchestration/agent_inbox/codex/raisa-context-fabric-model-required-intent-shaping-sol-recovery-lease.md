# Raisa Context Fabric model-required intent shaping — Sol recovery lease

Date: 2026-08-06

Status: open; worker source preserved as untrusted recovery input

Frozen planning source:
`5ddc052fae16298436d8873312e48464d52a9567`

Initial DeepSeek worker candidate:
`d0d7584ff02df0d311b3f71d556cb95333292a82`

Single bounded same-lane repair candidate:
`ce317df5e28bc0fad17c315199afda0b0ecdc012`

The worker receipts remain the worker's own non-transferable closeout. GPT Sol
does not accept or rewrite those claims. Under
`docs/ariadne-orchestrator-recovery-lease.md`, Sol may adopt only the source as
an untrusted candidate, record every amendment, rerun deterministic gates and
obtain a fresh exact-HEAD Gemini 3.6 Flash/high veto before any occupied call.

## Preserved review findings

The first candidate's release shared a mutable envelope later cleared by the
broker; schema-invalid provider bodies bypassed the eligible correction path;
positive-thinking failure occurred after release and could strand accounting;
and source review omitted exact acceptance/HEAD/clean-worktree binding. The
single bounded worker repair corrected those four findings and added the
canonical cue-order prompt.

Sol's complete review of the repaired source then found three remaining
provider/evidence-readiness gaps:

1. the Vertex `responseSchema` contains `uniqueItems`, which is absent from
   Google's currently documented supported structured-output field subset;
   local JSON Schema and the deterministic proofreader can retain exact
   uniqueness without transmitting the unsupported provider field;
2. the committed deterministic request's fixed `issued_at`/`expires_at` pair is
   already stale for an occupied call and live broker validation does not bind
   the request to current wall-clock freshness; and
3. `occupied-rehearsal-evidence.schema.json` is top-level closed but leaves
   `lane_results` structurally unconstrained, so it cannot prove that final
   evidence contains only bounded summaries and no raw provider, credential or
   arbitrary nested content.

These are ordinary unintegrated candidate defects found during expected Sol
review, not accepted-result failures and not an agent-error-register incident.
No provider call, credential inspection, Docker run, product/database/API/
command action, deployment, protected-evidence access or protected-ref movement
occurred.

## Sol-owned amendments

Sol may amend only the existing new intent-shaping implementation/test/schema
paths and this recovery evidence to:

- remove provider-unsupported response-schema keywords while retaining the
  stricter local provider-body schema and exact proofreader;
- materialize a fresh bounded live `IntentShapingRequest`, enforce live broker
  wall-clock issuance/expiry and duration checks, and keep the committed
  deterministic fixture unchanged for provider-free evidence;
- replace arbitrary final `lane_results` retention with a fixed, digest-bound,
  closed attempt-summary shape and strengthen exact path/hash/result
  relationships in the occupied evidence schema; and
- add direct regressions plus recursive schema-closure checks.

No provider/model/project/identity/region/budget/cost/data/authority, parent
retrieval meaning, API surface, product runtime, command or clinical boundary
may change. Any broader issue stops before provider execution.

Closeout requires: explicit Sol diff attribution; provider-free evidence;
focused and inherited serial tests; Ruff/compile/schema/diff gates; an actual
zero-call isolated dry-run with complete cleanup; exact clean source hashes;
and one fresh Gemini 3.6 Flash/high read-only veto on the final non-protected
candidate. Only then may the already-authorised read-only preflight and bounded
occupied call sequence become eligible.

## Sol recovery result before independent review

Sol adopted the two worker commits unchanged and amended exactly the six
intent-shaping source/test/schema paths named by the frozen source-hash set.
The provider response schema no longer transmits `uniqueItems`; the local
Draft 2020-12 body schema and deterministic proofreader still enforce cue
uniqueness and exact order. Live requests now receive a fresh UTC issuance
window of exactly 600 seconds, and the live broker independently rejects a
future, expired, naive-clock or wrong-duration request. Final tranche evidence
now retains only a closed attempt summary bound to the separately written
attempt evidence, release and parent packet by SHA-256 digest. Recursive schema
closure tests reject unbounded nested object or array positions.

Provider-free acceptance passed. The isolated dry-run then admitted the exact
authored-synthetic comparison through the unchanged parent retrieval and
proofreader with a consumed zero-call ledger, zero reserved cost and complete
container/network/image/process/temp cleanup. Focused plan and rehearsal tests
passed 45/45; the wider inherited run passed 117 tests and exposed only one
pre-existing stale Compass wording assertion in an older Bureau Memory
continuity test, outside this descendant. Ruff, compilation and diff checks
passed. No provider call, credential inspection, patient/product data, product
runtime/API/database/command action, deployment, Pages or protected-ref
movement occurred.

Status: recovered provider-free candidate; pending exact-HEAD Gemini 3.6
Flash/high read-only source veto before any occupied action.
