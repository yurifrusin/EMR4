# Sol acceptance: Raisa AES-C4 bounded occupied authored-synthetic provider proof

Date: 2026-08-11

Decision: `accepted`

Result: `raisa_agent_execution_surface_containment_gate_aes_c4_bounded_occupied_provider_proof_pass`

Reviewed source HEAD: `e569da0a9081117b799e9437d8b7025230e2162b`

## Basis

I accept AES-C4 as exactly one broker-admitted, newly authored-synthetic Sydney
Vertex `gemini-2.5-flash` request under the frozen Bernie project, keyless
impersonated ADC, one-call/no-retry and USD 0.25 envelope. The provider returned
HTTP 200/`STOP`; deterministic proofreading released only the exact nonce-bound
four-field object with `command_authority: false`.

The evidence records one call, 1,726 ms latency, 363 prompt, 40 candidate, 139
thought and 542 total tokens, no provider tools or alternate destination, and
zero product, database/source, filesystem, command, deployment or protected
operations. The ledger is consumed with zero retries, all aliases/leases/tokens
revoked, further calls disabled and no reusable process, listener, capability
or temporary root remaining.

Before that call, current lifecycle, region and pricing facts were rechecked;
the read-only cloud/ADC and separate gcloud CLI-store gates passed. Fresh
Gemini 3.6 Flash/high independently passed 368 serial tests, Ruff, compilation
and exact diff checks against unchanged clean source
`e569da0a9081117b799e9437d8b7025230e2162b`.

## Incident disposition

AER-0261 preserves the first zero-call factual-rebind pair with its
non-existent source string. That pair supplied no evidence. The corrected pair
is bound to the exact existing commit, passed deterministic verification and
received the fresh veto before any cloud or provider gate. No second provider
call or occupied repair occurred.

## Acceptance boundary and next fork

This acceptance proves only the exact authored-synthetic broker, typed-release,
accounting and cleanup path. It proves a configured/observed Sydney request
path, not Australian physical or sovereign processing, and grants no reusable
runtime, product/patient data, database/source, filesystem, provider tool,
command, deployment, production or release authority.

AES-C5 remains separately closed. Its first product-runtime read needs a
non-inferable choice of one source and one purpose plus explicit privacy,
identity, retention and product-data boundaries. Yuri's attention is required
before AES-C5 planning can select or read real product-derived context.
