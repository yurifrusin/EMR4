# Ariadne Sydney Vertex Tranche 1 — Sol Review

Date: 2026-07-24
Reviewer: GPT Sol
Disposition: `accepted_fail_closed`

## Decision

Accept Tranche 1 as
`ariadne_vertex_sydney_provider_admission_blocked`.

The current official model card and per-model residency matrix do not publish
`gemini-3.5-flash` for `australia-southeast1`. The published Sydney service
hostname does not cure that missing model-location contract. Yuri explicitly
prohibited a model, provider or region substitution, so the seven-tranche
sequence ends before launcher, ADC, Cloud, container or provider activity.

## Independent findings resolved

- Continuity and Compass are incremented together from the initially
  authorised active state to the blocked closeout state.
- The provider policy now records Google's default project-isolated,
  not-at-rest, 24-hour in-memory caching separately from storage, abuse
  monitoring and request-response logging.
- Any future route must verify that caching is disabled for the exact project,
  or stop for Yuri without changing Cloud state.
- Claims distinguish the independent current-document check from the local
  validator's narrower proof of schema, semantic and canonical-hash
  consistency.

## Accounting and exclusions

Zero occupied calls, retries and fallbacks occurred. No occupied single-use
ledger was opened. No credential, ADC file, token, API-key information,
project entitlement, Cloud control plane, container, prompt or provider
runtime was inspected or used. No external state was changed.

This acceptance does not prove entitlement, ADC usability, audit posture,
provider acceptance, a typed model output, proofreader release, Australian
physical or sovereign processing, zero data retention or the exact project's
cache setting.

Fresh Yuri intervention is required before any continuation. The current
authority cannot be reused for a substitute or a silent documentary retry.
