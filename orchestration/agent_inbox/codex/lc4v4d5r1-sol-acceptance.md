# LC4V4D5R1 Sol Acceptance

Date: 2026-07-16

Decision: `exact_four_remediation_accepted`

GPT Sol retained planning, taxonomy, recovery, acceptance, and integration
authority. DeepSeek V4 Flash/high through Claude Code `--bare` supplied one
bounded candidate. Its conceptual fail-open defects were preserved and
recovered by Sol without a correction loop. Gemini 3.5 Flash/medium through a
fresh Antigravity project independently returned `DECISION: pass`. DeepSeek
Pro was not used.

## Provenance

- Contract commit: `96ddd3698a1d69832de8141b7cddb20bda06e2a4`.
- Dispatch head: `574fda9a620734b0d22bff891d7afabfab8448ab`.
- Preserved Flash candidate: `c67ec1f773180d6ee80373eb892dc7e1edcd9abd`,
  adopted as untrusted at `ef0238de`.
- Sol recovery: `75678fceaaf4270597d06f90a071ecceaf43f52d` and
  deterministic evidence source `33ce3d89f5519e40276037a03b4e7742804080e5`.
- Frozen report source: `33ce3d89f5519e40276037a03b4e7742804080e5`;
  report commit `c09211de` and rendering normalization `82063c50`.
- Gemini review commit: `174a0858182cd4567eec00250e708adf22c64c99`,
  integrated at `3d99d56f`.

## Accepted result

All 60 ordinary development probes ran twice under legacy and twice under
explicit Option A, retaining 240 complete typed observations. Exact taxonomy:

- 37 `legacy_equivalent`;
- 20 `accepted_d4_versioned_change`;
- 3 `expected_versioned_relation`, differing only by `diary_relation`;
- zero blockers, unexpected differences, or Option A failures.

The four D5 blockers are closed. Safe move, resize, cancel, and status-change
now emit the already-supported deterministic simulated appointment/audit delta
shape. Resize duration is treated as the mutation target rather than diary
identity evidence. The three benign relations are exactly the original create
duplicate plus safe cancel and safe status.

The report hash is
`sha256:0cb444d1aeba82a80f5a16170b30b8ea203842dec4af81b768a688e5aae9bcdf`.
The three-relation selection hash remains `sha256:98df6544...`, and the
observed empty blocker selection is
`sha256:4f53cda18c2baa0c0354bb5f9a3ecbe5ed12ab4d8e11ba873c2f11161202b945`.
All 28 gates pass. Legacy-60, immutable D4/D5 report hashes, normalized exact
D4 behavior, fixture/population hashes, zero variance, and zero forbidden
observations are preserved.

## Recovery and verification

Flash reached the headline taxonomy but emitted deltas in uncertain diary
states, permitted a null practitioner ID, retained fingerprints instead of
typed observations, accepted arbitrary D4 differences, and hard-coded its
empty blocker evidence. Sol recovered those conceptual defects under the
lease and added direct negative guards for seven uncertain diary states and an
unknown mutation practitioner.

The serial D1-D4 plus D5R1 preservation chain passed 177/177 tests. Gemini then
reproduced the focused D3/D4/D5R1 chain at 120/120, all hashes and gates, and
the fail-closed negative behavior on exact recovered head `82063c50`.
`git diff --check` passed.

D5R1 creates replay-only evidence, not runtime write authority. Holdouts v1-v4
remain sealed; T3.1-T3.4 remain blocked; T3.5/providers, product/runtime
defaults, routes, APIs, UI, database, deployment, release, and all live/write
authority remain deferred.

The next ordinary step is a Sol-owned development exit reassessment binding
D4, D5, and D5R1 without loading any holdout. A clean exit will reach the
documented user decision boundary for a fresh certification holdout versus an
explicit reviewed reuse policy.
