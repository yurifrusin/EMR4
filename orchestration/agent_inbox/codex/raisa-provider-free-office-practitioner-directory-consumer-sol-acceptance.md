# Sol acceptance: provider-free Office practitioner-directory consumer

Date: 2026-08-03

Decision: `accepted`

Result: `provider_free_office_practitioner_directory_consumer_pass`

## Acceptance judgment

The implementation satisfies the frozen Office-consumer contract. A separate
default-off application composes the existing application-auth logout command
and two surface-bound instances of the accepted read-only GraphQL factory. The
client cannot choose its operation, practice, policy, fields or scope; every
response is closed-shape validated before text-only rendering.

Installed Word and Word Online independently passed the same visible sequence:
host admission, fixed active-directory read, exactly two authored-synthetic
rows, required authorization audit, logout and direct post-logout rejection.
The final accepted run used one fresh disposable database and four distinct
least-privilege roles and closed with complete owned cleanup.

## API Spine review

Boundary classification is one scoped GraphQL read plus the existing explicit
REST session-logout command. There is no GraphQL mutation, provider adapter,
external service, command tunnel or product write. Read authorization remains
practice/role/resource checked and audited; logout remains typed and auditable.
Manifest and UI assets declare the fixed surface while runtime code enforces it.

The route-intercepted browser preview is labelled exactly and is not used as a
substitute for the real Office results. The final evidence label identifies the
real local Office/backend/PostgreSQL product-read path without claiming a live
provider or production service.

## Repairs reviewed

The strict proxy-header correction preserves application ownership of exact
one-hop interpretation. The icon repair converts a framework-coercion defect
into an exact string allowlist. The Word Online CSP repair adds only the
observed outer personal OneDrive ancestor. Each failed attempt is separately
preserved, each owned database/role set was cleaned, and only the final fresh
two-surface run is accepted.

## Limits

This accepts only a provider-free authored-synthetic supervised Office read. It
establishes no real identity, live Microsoft federation, patient/clinical or
document access, broader product resource, product command/write, general
mount, production key/session custody, distributed abuse resistance,
organisational deployment, protected integration, production, release, Pages
or Dependabot authority.

Reasoning level: High. The API Spine boundary remained read-only, the existing
logout command retained command-plane ownership, exact host and least-privilege
controls were proven, and no failed gate was overridden.
