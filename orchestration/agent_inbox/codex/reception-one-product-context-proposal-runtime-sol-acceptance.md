# Sol Acceptance Review — Reception One Product-Context Proposal Runtime

## Disposition

Accept the technical tranche, with one closed sequencing deviation, as:

`reception_one_product_context_proposal_runtime_pass_with_closed_sequencing_deviation`

The implementation adds a separate, default-off, development-only Reception
One proposal path. Authenticated trusted code performs the minimum
practice-scoped patient, practitioner and availability reads, replaces backend
identifiers with request-scoped opaque handles, and retains ownership of
freshness, exact slot search, confirmation and all writes. The untrusted
planner receives only the closed typed frame and may compose only admitted
operators. The deterministic proofreader remains the sole release gate.

Repository tests prove the off, authentication, development-environment and
synthetic-practice gates; opaque request-scoped handles; typed clarification;
default duration binding; existing API Spine slot-search reuse; proposal-only
output; and absence of writes or provider access from the product service.
Browser validation proves that the compact Reception One sheet can request,
display and select the typed result while retaining the selected-not-reserved
boundary and the ordinary Diary behind it.

The first browser evidence did not satisfy the plan's exact Tranche E
requirement: it combined a real API/PostgreSQL test with a separate browser
fixture rather than proving one uninterrupted browser-to-FastAPI-to-PostgreSQL
request. The occupied call was therefore made out of the frozen order. This is
a control deviation and the historical sequence is not rewritten. The defect
was repaired without another provider call: a distinct IPv6-loopback harness
made exactly one non-intercepted browser request through the real FastAPI route
and disposable PostgreSQL database, returned eight typed candidate slots,
preserved every appointment and forbidden-write hash, made zero provider calls,
then removed its database, processes and temporary root.

The provider-free isolation rehearsal passed. The sole occupied call then used
the exact `gemini-2.5-flash`, Bernie keyless impersonated-ADC,
`australia-southeast1` lane. It returned HTTP 200 and a five-operator candidate
which the unchanged deterministic proofreader admitted without repair or
violation. Release was atomic, required staff confirmation, and recorded
`write_performed=false`. The ledger is consumed, no later call occurred, the
hash chain validates, the USD 1 guard was not approached, and independent
cleanup and residue checks pass.

The sequencing deviation did not transmit product context to Vertex: the
occupied cell used only its separately authored-synthetic static frame. It
nevertheless prevents an unqualified claim that every frozen gate preceded the
call.

This acceptance does not open real, product-derived, patient, health, clinical
or historical data; appointment confirmation or mutation; voice; representative
staff sessions; production; deployment; release; or the legacy Bernie
Interpretation Harness. It proves the configured and observed Sydney
locational request path, not Australian physical or sovereign processing.
