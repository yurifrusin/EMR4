# Sol acceptance — native Harness post-HMR sidecar integration

Date: 2026-08-21

Decision: `accepted`

I accept the provider-free candidate at exact machine-resolved source
`3a16f5a00e50259caf993decc67719173348dc0a`.

The evidence is sufficient because the accepted runner is hash-validated
before its closed transformation, the complete future source validates every
owned stage and interval boundary, and the controller cannot select a
pre-request coordinate from the sidecar alone. The separately canonical
broker record must carry the same operation, attempt and full Git identity and
all five counters must be zero. Every other admitted case is unresolved,
generic terminal or rejected exactly as the frozen contract requires.

DeepSeek, Gemini and native-subagent lanes were declined with negative
leverage. This provider-free tranche forbids the execution surface being
instrumented, the exhaustive typed proof left no separate semantic veto, and
developer policy keeps the coupled trust boundary serial. GPT Sol performed
the implementation, source review, hostile-fixture verification and
acceptance.

This decision proves deterministic integration semantics only. It does not
authorise or prove native Harness loading, a broker process, a DeepSeek request,
useful worker performance, product behavior, live data, production,
deployment, release, Pages or protected-ref movement.
