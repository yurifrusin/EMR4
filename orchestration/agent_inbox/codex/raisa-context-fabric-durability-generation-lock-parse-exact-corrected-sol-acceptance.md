# Sol acceptance — corrected generation-lock parse exact-binding veto

Date: 2026-08-10

Decision: `accepted_for_one_exact_reproduction`

Candidate `3b35f173d7a09fd4a8dfb65f0716c49b4de6e7f9` received one valid fresh
Gemini 3.6 Flash/high pass in clean bounded r158. The verifier ran 140/140
focused tests, Ruff check and format, exact diff validation and tracked
immutable evidence reconciliation. Its HEAD remained exact and the worktree
remained clean.

The earlier r157 terminal pass is rejected and grants no authority. AER-0197
preserves its substitution of a hash-equal tracked historical path for an
absent primary-only mutable alias. The corrected packet prohibited that claim;
the r158 receipt explicitly confirms that no primary-only untracked mutable
evidence path was inspected, inferred or substituted.

The admitted tracked surface proves:

- immutable characterization SHA-256
  `78c157c72243036d395c3bcff30f778fa8b1032bb98eec9a32b37110efbcf536`;
- exact characterized attempt `7ab702e5fa8cd5c75a7a8e6c` and verified
  cleanup/absence for container `aa3d7ccc...`;
- exact unchanged object populations;
- fourteen of fifteen acceptance digests unchanged against the immediately
  preceding admission-row-shape reproduction, with only `policies` changed;
- exact-bound contract canonical SHA-256
  `dbedcaf7628a68859412d898e86292b2366209941d18f58363c45174b6fc60ba`;
  and
- unchanged non-aliasing pass, characterization and failure evidence routing.

Sol separately reverified in the primary worktree that mutable accepted parse
evidence remains SHA-256 `97d1385c...`, historical failure remains
`3bf66870...`, and mutable behavior evidence remains `09907bf6...`. These
primary-only facts are not delegated to the verifier.

Exactly one later fixed no-argument exact reproduction may now run in one
newly owned, pull-never, networkless, no-port, no-mount, tmpfs-only local
PostgreSQL 16 container. It must pass all exact digests, route the pass to the
mutable parse target, be copied to a new immutable exact-reproduction path,
restore the protected mutable parse alias after preservation, prove exact-ID
cleanup/absence, and leave the historical failure and mutable behavior
evidence byte-exact.

No behavior run, applied migration, operational database or credential,
watcher/listener/feed, patient/product/clinical data, provider,
application/API/Diary command or write, deployment, production, release,
Pages or protected-ref movement is authorised.
