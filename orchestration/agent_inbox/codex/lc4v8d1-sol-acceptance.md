# LC4V8D1 Sol Acceptance

Date: 2026-07-16

Decision: `development_exit_pass_empty_selection_user_v9_decision_required`

## Authority and protected boundary

Yuri authorized bounded ordinary-development LC4V8D1. GPT Sol retained
planning, fresh authorship, taxonomy, adjudication, recovery, acceptance,
integration, and push authority. D1 used only the public V8 aggregate categories
and 24 fresh inspectable Sol-authored probes. V8 remained consumed and sealed;
D1 did not inspect, rerun, rescore, relabel, repair, or recertify it.

Before D1 authorship, one search returned only the protected V8 evaluator path
and interface-level names already public in the frozen contract. It exposed no
utterance, Gold value, case ID, diary state, or per-case result. The incident
was recorded and contained before any fixture, contract, baseline, or external
dispatch existed. All later paths were exact-file-bound.

## Fresh authorship and independent veto

Sol froze 24 cases: six canonical policy actions, six policy boundaries, six
numeric/spoken time surfaces, and six temporal relation/composition cases.
The exact raw fixture hash is
`sha256:ebcfe4bbbd9c89dff00f1ff30643f2b9dc21f5cfba5febf62fd22e041f76269c`;
canonical hash is
`sha256:a15a9ad47cd576679ac393c758216a3257ad1f67aa4b4455ef8c6b574c5f376e`.
Authorship checks separate independently scored policy semantics from an exact
14-field JSON-safe typed-policy projection and reject cross-field contradictions.

Gemini 3.5 Flash/medium reviewed the immutable authored evidence before product
execution, reproduced 11/11 tests and the raw hash, and returned
`DECISION: pass` at task head `23914f64`.

## Flash disposition and Sol recovery

DeepSeek V4 Flash/high ran once through Claude Code `--bare`. The parent
launcher timed out at 600 seconds; its child completed shortly afterward with
279 passes, a 24/24 self-pass, and a completed receipt. It recorded 92,177
uncached input, 2,233,216 cache-read, and 38,313 output tokens, with a
non-authoritative USD 2.547088 adapter estimate.

Sol rejected the self-pass. The worker did not commit its files, misreported
source head `7cc32932` instead of actual dispatch head `f58f6300`, omitted
required Gold cross-field gates and raw-byte execution binding, derived mutation
semantics without requiring tools/deltas/write evidence, allowed hidden mutation
to pass safety, and produced incomplete invalid-report hashes. These are
conceptual provenance and acceptance defects, so no Flash correction loop was
opened.

Sol preserved exact uncommitted hashes, adopted the source explicitly under the
recovery lease at `39a95240`, and recovered it at source head `8823bf0d`. The
recovered runner binds raw bytes before product execution, validates semantic/
projection consistency, derives mutation allowance from actual tool/delta/write
state, rejects hidden mutation in every non-mutation outcome, hashes invalid
reports deterministically, and proves invalid inputs execute no product code.

## Frozen baseline and interpretation

Two independent recovered invocations produced identical complete report hash
`sha256:e7507a4333316012449168f4e11ab93e0b8b60b29c1495b1864eb932bd5fa0bd`.
All 24 cases pass normalization, extraction, semantic policy behavior, exact
policy projection, composition, and safety over 48 observations with zero
variance. All four families pass 6/6. Every non-pass classification is zero;
the empty selection hash is
`sha256:4f53cda18c2baa0c0354bb5f9a3ecbe5ed12ab4d8e11ba873c2f11161202b945`.

No parser, normalizer, policy, or projection runtime repair is authorized. The
fresh evidence demonstrates that the ordinary typed resolver and an explicit
canonical projection agree across the sampled action, boundary, and temporal
surface. It therefore supports the narrow inference that V8's policy-resolution
`0/576` is V8-specific Gold/evaluator contract mismatch evidence rather than a
general resolver failure. The independent temporal sample also reproduces no
broad temporal/normalization defect. The exact V8 case-level cause remains
unknown and sealed.

## Independent final review and preservation

Gemini independently returned `DECISION: pass` against exact recovered/baseline
head `b72cf748` at task head `040ad784`. It reproduced 292 selected D1/focused
nodes, 13 handover/preflight nodes, every hash, the empty selection, all recovery
gates, and the no-remediation conclusion.

The broader final serial preservation gate passed 581/581 selected nodes.
Exactly ten nodes were deselected and no historical report regenerated:

- seven accepted pre-existing V2R1 nodes: four superseded entity-case
  assertions, one negated-location assertion, and two frozen audit/check nodes;
- two immutable LC4V4D3 population/report equality nodes; and
- the documented runtime-isolation baseline for the intentionally blocked
  `app/config.py` gate path.

## Acceptance and next authority

LC4V8D1 is accepted as `development_exit_pass` with an empty repair selection.
This is ordinary development evidence, not product certification.

Pause for Yuri. Recommended next option: authorize genuinely fresh LC4V9,
beginning content-blind and standardizing the D1-proven canonical policy
projection plus semantic/projection cross-field validation before any content
exists. V9 must receive fresh Gemini pre-content veto, Sol-only corpus
authorship, immutable binding/seal, and one one-shot evaluation. It may not
reuse or inspect V8. A new V9 is a separate user decision.

T3.1-T3.4 remain intact and blocked. T3.5/providers, historical data,
runtime/product wiring, APIs, UI, database, deployment, release, and all
live/write authority remain deferred.
