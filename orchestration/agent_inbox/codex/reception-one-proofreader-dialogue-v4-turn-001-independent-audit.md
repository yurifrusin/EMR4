# Reception One Proofreader Dialogue v4 Turn 001 — Independent Audit

## Disposition

`accepted_fail_closed_request_contract_defect`

Turn 001 used the exact authorised provider, model, project, impersonated
service account, keyless ADC class, Sydney region and hostname. Its ledger is
consumed at one actual call. The durable seven-event turn chain and four-event
parent chain validate.

Vertex returned HTTP 400 `INVALID_ARGUMENT` before a candidate. The bounded
message class identifies excessive structured-schema serving states. No
PlanProgram reached the proofreader, no correction ticket or release exists,
and all task-scoped runtime residue is absent.

The first result is immutable. The provider-only state-bound reduction is a
deterministic request-contract repair within the original authority. It may
use the sole remaining actual call only after the full gate rerun and a fresh
revision-bound receipt. That call exhausts the absolute ceiling; no semantic
correction or third call can follow.

The evidence proves a configured and observed Sydney locational request path
and fail-closed lifecycle. It does not prove model interpretation, Australian
physical or sovereign processing, general reliability, production suitability
or safety for real or product-derived data.
