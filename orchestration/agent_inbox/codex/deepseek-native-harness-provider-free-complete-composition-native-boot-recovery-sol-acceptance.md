# Sol acceptance — DeepSeek native Harness provider-free complete-composition native-boot recovery

Date: 2026-08-20

Timestamp: 2026-08-20T11:55:38.3228395+10:00 (Australia/Brisbane)

Decision: `accepted_with_contained_p3_procedure_incidents`

GPT Sol accepts exact reviewed candidate
`6ef058b87a2c927efd9d9d2027b59d6ad279fec5`.

Acceptance basis:

- the sole non-retryable rc.7 native Harness process reached exact readiness,
  three-service dependency-gated runner activation and the exact successful
  terminal;
- the accepted 158-byte preset and post-mount guard produced only `edit`,
  `glob`, `read`;
- process count was one, retry count zero, exit code zero and duration 9316 ms;
- every provider-boundary count was zero, and exact process/root cleanup passed;
- immutable evidence SHA-256 is
  `9ba784b0726addb5644ac3786def410aed56e5bf9da3e23ec21d8e10f6ba1ea0`;
- one Gemini 3.7 Flash/high veto passed all ten commands and 86 safe
  provider-free tests at unchanged clean candidate HEAD; and
- no P0-P2 semantic or security finding remains.

Sol accepts Gemini's conclusion that the ancillary mock-Node unit-test breach
is a contained P3 procedure incident, not a failure of the one-Harness-process
claim. Sol corrects one factual lapse in the receipt: the three Node-spawning
modules were
`test_deepseek_native_harness_provider_free_effective_tool_composition_native_boot_proof.py`,
`test_deepseek_native_harness_provider_free_hmr_boot_proof.py` and
`test_deepseek_native_harness_provider_free_preterminal_observable_composition_recovery_boot.py`.
The receipt instead named three non-spawning modules. Direct source inspection
and the exact executed manifests support this correction; a second provider
call is neither needed nor authorised.

Both lapses enter clockwork incident intake. The prospective controls are:
execution-sensitive test manifests must statically classify and exclude
Node/subprocess launchers unless the plan explicitly admits them; verifier
packets must bind exact incident filenames and returned receipts must preserve
those names without substitution.

One first clockwork publication also required correction. Lease 58 omitted an
exact legacy next-latch compatibility token even though its narrower default-
off controls were semantically aligned. The dedicated postpublication suite
returned the exact failure; lease 58 is preserved and rolled back byte-exactly
at lease 59. Corrected publication retains the full exact compatibility subset
and admits this closeout lapse as AER-0670 rather than hiding the rerun.

Post-closeout five-source rehydration exposed a fourth control failure: lease
60 selected the already accepted route-adapter operation as the next tranche.
That contradicts the accepted Continuity graph and is not a valid worker
target. Sol preserves lease 60, the stale latch and the rejected first receipt,
accepts byte-exact rollback lease 61, and replaces the historical hard-coded
successor assertion with a pre-publication graph-identity interlock. The first
receipt's invalid leverage enum and the historical guard defect are separately
retained. The accepted Harness terminal and Gemini veto remain unchanged and
are not rerun.

The first corrected check at full source
`64d319d7771c3fe60952e70ec17b70248079673c` then rejected one incident-stage
value before publication. Sol treats the late rejection as a contained
repository control defect, corrects all adjacent incident vocabulary in one
pass and moves the complete register-bound date, identifier, enum and const
admission into `validate_tick_intent`. No canonical projection changed.

The exact unrepeated successor is
`raisa-provider-free-check-in-relay-free-recovery-attempt-005`. It has planning
authority only until a fresh exact plan, WorkOrder, deterministic admission and
one-run checkpoint exist; attempt 004 remains immutable and no execution is
silently inherited from this acceptance.

Claim ceiling: this is a real provider-free pre-provider Harness composition
success, not an occupied worker or proof of DeepSeek reasoning, coding quality,
broker behavior, production isolation or product suitability. No product,
data, deployment, release, Pages or protected-ref authority is granted.
