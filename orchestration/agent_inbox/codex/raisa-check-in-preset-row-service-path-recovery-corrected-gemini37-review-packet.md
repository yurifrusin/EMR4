# Corrected Gemini 3.7 Flash/high veto — native preset-row service path

Date: 2026-08-20

Timestamp: 2026-08-20T18:04:04.8737830+10:00 (Australia/Brisbane)

Review only. GPT Sol remains implementation, checkpoint, recovery and
acceptance owner. The first review transport is immutable rejected evidence:
its C07 command regenerated a tracked evidence file under a different
credential-environment count, so the read-only postcondition failed and no
terminal decision was admitted. The exact candidate is unchanged. This one
bounded corrected review replaces C06/C07 with non-writing function calls.

## Exact candidate

- Worktree: `C:\Users\sarashera\EMR4-worktrees\preset-row-service-gemini-da525145-v2`
- Branch: `codex/review-preset-row-service-da525145-v2`
- Candidate HEAD: `da52514548ef1c1253e379f983165545482592a6`
- Diff base: `e37e7b5d408cc687c67d2bc1c1e1a4012a793672`
- Model/effort: `gemini-3.7-flash-high` / `high`

Read `AGENTS.md` completely, restore all five named sources, validate the latch
and protected boundaries. DeepSeek is declined, Gemini owns only this veto,
native subagents are declined, and Sol retains all acceptance.

## Review question

Return `pass` only if the exact rc.7 source proves that native
`composeProfile()` replaces configured preset roots with the shipped root and
that `AgentPresets` appends `$DSH_HOME/.agent-presets` only when
`includeUserRoot` is true; the predecessor profile disabled that user root;
the five-scenario package-only fixture accurately reproduces absence,
corrected one-row success, missing-root absence, duplicate shadowing and
configured-root displacement; and the minimal native candidate only re-enables
the derived user root and changes the exact EMR4 trust expectation to `user`.

A pass makes a separate one-process checkpoint eligible. Do not run a native
Harness stage and do not contact DeepSeek.

## Exact allowlist

Read only `AGENTS.md`, the exact candidate diff and these paths. Do not
enumerate the repository or inspect branding/unrelated files.

- `docs/raisa-provider-free-check-in-native-harness-preset-row-service-path-recovery-plan.md`
- `docs/security/raisa-provider-free-check-in-native-harness-preset-row-service-path-recovery-threat-model-delta.md`
- `orchestration/continuity/ariadne-active-operation-latch/current.json`
- `orchestration/continuity/raisa-provider-free-check-in-native-harness-preset-row-service-path-recovery/contract.json`
- `orchestration/continuity/raisa-provider-free-check-in-native-harness-preset-row-service-path-recovery/contract.schema.json`
- `orchestration/continuity/raisa-provider-free-check-in-native-harness-preset-row-service-path-recovery/static-evidence.schema.json`
- `orchestration/continuity/raisa-provider-free-check-in-native-harness-preset-row-service-path-recovery/fixture-evidence.schema.json`
- `orchestration/continuity/raisa-provider-free-check-in-native-harness-preset-row-service-path-recovery/source-and-effective-root-evidence.json`
- `orchestration/continuity/raisa-provider-free-check-in-native-harness-preset-row-service-path-recovery/service-input-fixture-evidence.json`
- `orchestration/continuity/raisa-provider-free-check-in-native-harness-preset-row-service-path-recovery/service-input-fixture-report.md`
- `scripts/raisa_provider_free_check_in_native_harness_preset_row_service_path_recovery.py`
- `tests/test_raisa_provider_free_check_in_native_harness_preset_row_service_path_recovery.py`
- `tests/test_raisa_provider_free_check_in_native_harness_preset_row_service_path_recovery_plan.py`

## Required challenges

1. Verify exact branch, full HEAD and clean pre/post worktree.
2. Bind all six retained rc.7 source/manifest digests and the lockfile without
   installing, updating or accessing the registry.
3. Confirm the native forced overlay is appended after profile/home/CLI
   overlays, preserves prior config fields but replaces `roots` with the
   shipped system root.
4. Confirm `AgentPresets.resolvedRoots` appends the DSH-home user root only
   when `includeUserRoot` is true and `list()` scans that resolved set.
5. Confirm the predecessor profile set `includeUserRoot: false`, leaving only
   the shipped roster and excluding `emr4-bounded-worker`.
6. Confirm the shipped roster is exact and the passing direct scan was
   explicitly given the user root.
7. Confirm the corrected fixture yields shipped system then derived user roots,
   exactly one healthy EMR4 row with user trust, 158 bytes and exact digest.
8. Confirm missing-root absence and configured-root displacement are not
   success, and an earlier duplicate is rejected as shadowed.
9. Confirm package analysis creates no Harness service graph, agent, mount,
   session or turn and records zero network/provider/Docker/database activity.
10. Confirm the prospective native runner injects only `agentPresets`, checks
    exactly two roots, calls `list()` once, requires user trust, has no
    agent/mount/session/turn/model path, and writes one sanitized terminal.
11. Confirm schemas reject broadened fields, native-process admission and
    mislabelled corrected trust.
12. Confirm C06 and C07 do not call `main()` or write repository artifacts;
    then run exactly C01-C12, all zero, leaving the worktree clean.

## Forbidden actions and decision

Do not edit, format, commit, push, install, run any native Harness process,
call DeepSeek, invoke Docker/database, access product/protected data, inspect
branding, deploy or accept output. Return one schema-constrained `pass` only if
no material P0-P2 issue or command failure remains; otherwise return
`revision_required` with exact allowlisted evidence. Do not emit a second
decision.
