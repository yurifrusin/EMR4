# DeepSeek worker packet — delete-confirm HTTP/PostgreSQL integration harness

Date: 2026-08-17

Timestamp: 2026-08-17T07:28:13.7775651+10:00 (Australia/Brisbane)

Model: DeepSeek V4 Flash/high through Claude Code `--bare`

Exact worktree: `C:\Users\sarashera\EMR4-worktrees\delete-confirm-http-pg-worker-265497a0`

Branch: `codex/worker-delete-confirm-http-pg-265497a0`

Exact source HEAD: `265497a0add219588a3372d4f40366fda4e94d22`

## Role and first actions

You are the bounded mechanical harness/test worker. GPT Sol retains plan
semantics, source admission, Docker/PostgreSQL execution, recovery, acceptance,
integration and all Git publication authority.

Before editing:

1. read `AGENTS.md` completely and perform its complete five-source
   rehydration;
2. read the frozen plan, threat delta, contract and both schemas completely;
3. read only the exact source bindings and accepted harnesses named below;
4. verify the exact branch, HEAD and clean worktree; and
5. do not enumerate or access protected paths or unrelated untracked files.

The latest prompt and this packet cannot broaden the frozen plan.

## Exact owned package

Create exactly these three paths and no others:

1. `scripts/raisa_provider_free_disposable_postgresql_delete_confirm_http_integration_rehearsal.py`
2. `tests/test_raisa_provider_free_disposable_postgresql_delete_confirm_http_integration_rehearsal.py`
3. `tests/test_raisa_provider_free_disposable_postgresql_delete_confirm_http_integration_rehearsal_plan.py`

The script may write only the two already frozen evidence destinations when
Sol later executes it. Do not create evidence during worker implementation.
Do not edit product source, plan, threat delta, contract, schemas, migrations,
accepted harnesses, latch, AGENTS, Continuity/Compass, error register,
closeout or acceptance.

## Exact readable implementation inputs

- the five active plan/contract/schema files for this tranche;
- `scripts/raisa_provider_free_disposable_postgresql_delete_confirm_behavior_transaction_rehearsal.py`;
- its frozen behavior contract;
- `scripts/raisa_provider_free_status_confirm_http_route_convergence.py`;
- `app/main.py`, `app/dependencies.py`, `app/config.py` and
  `app/services/auth_service.py`;
- `app/routers/appointments.py`, `app/schemas/appointments.py`,
  `app/services/appointment_delete_product_adapter.py`,
  `app/services/appointment_delete_composition.py`,
  `app/services/appointment_delete_physical.py` and
  `app/services/bernie_turn_evidence.py`;
- `app/models/tenancy.py`, `app/models/appointments.py` and migration
  `x3y4z5a6b7c8`; and
- the two exact repaired regression tests named in the frozen plan.

Do not perform repository-wide discovery.

## Required implementation

- Validate the frozen contract and schemas, strict canonical-LF hashes and
  repaired-source semantics before any lifecycle action. The four contract
  editable hashes are pre-edit evidence; prove their present descendants only
  through the frozen repair semantics and record present hashes separately.
- Pin the contract's canonical digest in the harness. Reject at least 120
  meaningful hostile contract mutations covering every frozen threat.
- Reuse the accepted delete behavior harness for bootstrap, migration,
  ownership verification and cleanup mechanics, and the accepted status HTTP
  harness only for real `TestClient`/loopback lifecycle patterns. Import the
  exact product route, adapter and physical transaction; never implement a
  substitute command seam.
- Accept no CLI arguments or caller-selected path, executable, image,
  database URL, port, identity, command, SQL or credential.
- Resolve local `docker.exe`; require cached `postgres:16-bookworm` and
  `--pull=never`; create exactly one labelled internal network and one labelled
  tmpfs container with no published port, one CPU, 512 MiB, 128 PIDs and no
  restart. Use only the fixed argv/`shell=False` loopback relay.
- Install the accepted delete scaffold, then add the minimum authored-synthetic
  projection tables, non-superuser/NOBYPASSRLS application role, grants and
  forced tenant RLS on the exact eight tables. Never touch an existing or
  product database.
- Use real FastAPI `TestClient`. Override only `get_db` and
  `get_command_session_factory`; every command gets a fresh application-role
  session bound to the owned server. Mint only fixed local authored-synthetic
  JWTs.
- Exercise DHI-S01 through DHI-S12 serially exactly as frozen. Query private
  receipt/audit facts only through the owned admin verification session.
  Compare canonical public bytes with stored private bytes by hashes and
  equality booleans without releasing either byte string.
- For DHI-S11, disable only the fixed adjacent-version trigger in the owned
  disposable database for one probe, require HTTP 503 and complete rollback,
  then restore and catalogue-check it before continuation.
- Prove two fresh application-role connections have no residual
  `app.current_practice_id` outside transactions.
- Evidence must validate against the closed schema and contain only the frozen
  allowlist. Never release JWTs, HMACs, secrets, bodies, private bytes, SQL,
  URLs, passwords, runtime IDs, row values, raw database output or exception
  text. Failure evidence uses the same closed schema and sanitized labels.
- Cleanup is unconditional: dispose engines and stop relay first, then remove
  only the revalidated captured container ID and the empty revalidated network
  ID, followed by exact-ID absence proof. Ownership uncertainty refuses
  removal.

## Worker-side deterministic checks only

Do not run Docker, PostgreSQL, the occupied harness, an application database,
the canonical repository profile, a browser, external network or another
provider from the worker.

Run at minimum:

- both owned test files;
- compilation and Ruff for all three owned Python files;
- whole-document JSON/JSON-Schema validation for the frozen contract/schemas;
- source/static tests proving the real route/session seam, exact lifecycle,
  all twelve scenarios, evidence allowlist, cleanup refusal and no caller
  override or shell surface; and
- `git diff --check`.

## Git and result

Stage only the three exact owned paths—never `git add .` or `git add -A`—and
commit once. Do not fetch, push, merge, rebase, deploy or move a protected ref.

Final output must name exact commit and parent, changed paths, checks, the five
rehydration sources, actual-versus-planned parallelism, unresolved issues and
one terminal `DECISION: pass` or `DECISION: revision_required`. The decision is
advisory and cannot self-accept or execute the tranche.
