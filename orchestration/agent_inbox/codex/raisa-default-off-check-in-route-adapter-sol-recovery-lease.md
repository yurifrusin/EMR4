# Sol recovery lease — default-off canonical check-in route-adapter convergence

Date: 2026-08-18

Status: active bounded recovery lease

## Trigger

The occupied DeepSeek V4 Flash/high worker exited 1 before producing a terminal
worker result, diagnostic stderr, owned file or commit. Exact readback proved
its isolated branch remained clean at frozen plan source
`4daa2d772ffcf64e55f69917d2fb21802e959673`.

The transport non-result is preserved at
`orchestration/agent_inbox/deepseek/raisa-default-off-check-in-route-adapter-first-transport-failure.json`.
It is not worker implementation, review or acceptance evidence.

## Lease

GPT Sol may implement only the package frozen in
`docs/raisa-provider-free-default-off-canonical-check-in-route-adapter-convergence-rehearsal-plan.md`.
No worker source exists to adopt. Sol therefore authors the bounded route
delegation, the compatibility-preserving adapter ordering amendment and the
named focused tests under its own identity.

The lease does not expand the tranche. The A5.1 gate remains default off and
authored-synthetic allowlisted; the exact request/response contract and default
denial remain unchanged. Generic status, action grammar, first-party clients,
waiting-area movement, product data, providers, production, deployment,
release, Pages and protected refs remain closed.

## Admission

Deterministic verification and exact diff/ownership review are required. Since
the package changes a runtime route and transaction composition, a fresh
Gemini 3.7 Flash/high independent veto remains mandatory before acceptance.
The failed worker branch is prohibited from late source adoption.
