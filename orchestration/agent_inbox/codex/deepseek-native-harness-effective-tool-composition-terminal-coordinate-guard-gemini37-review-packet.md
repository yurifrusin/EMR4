# Gemini 3.7 Flash/high veto — provider-free effective-tool composition and terminal-coordinate guard

Date: 2026-08-20

Timestamp: 2026-08-20T04:55:32.6602238+10:00 (Australia/Brisbane)

Perform one fresh read-only veto of exact candidate
`51fe669f7925a9468e036bb242d6f7bcf2613dc9` against accepted base
`80d9e69c3dee0ddeb4b7fc620cbd0673d7ad0fa4`.

## Exact candidate and authority

- Worktree: `C:\Users\sarashera\EMR4-worktrees\effective-tool-guard-gemini-51fe669f`
- Branch: `codex/review-effective-tool-guard-51fe669f`
- HEAD: `51fe669f7925a9468e036bb242d6f7bcf2613dc9`
- Required model/effort: `gemini-3.7-flash-high` / `high`

Read `AGENTS.md` completely and name all five rehydration sources. Validate the
in-progress latch for
`deepseek-native-harness-provider-free-effective-tool-composition-and-terminal-coordinate-guard`.
Restore the explicit dispositions: DeepSeek declined because the Harness is
the provider-free subject and provider dispatch is closed; Gemini dispatched
for this exact-candidate independent veto; native subagents declined under
current developer policy and the small serial composition boundary.

Read the active plan, threat delta, contract/schema, evidence schema, retained
evidence/report, implementation/test, accepted rc.7 HMR boot proof, current
profile family and consumed worker diagnosis. Do not inspect `docs/branding/`
or unrelated paths.

## Mandatory challenges

1. Confirm exact candidate/worktree identity and a clean tracked tree.
2. Confirm all four exact rc.7 npm cache identities, frozen source-member
   digests and seven semantic predicates are checked without package install,
   source patching, retained package source or network access.
3. Challenge the generated helper's causal order: exact sorted input;
   `await agentCtx.agentPresets.mount`; scope resolution; one pre-restriction
   `tools.view`; rejection of `knownNames - restrictableNames`; proof that all
   selected names are inherited; exactly one `tools.restrict`; and one final
   schema projection requiring exactly `edit`, `glob`, `read`.
4. Confirm no scope-local name can reach rc.7's inherited restriction surface,
   no missing inherited name can pass, and surplus/missing/duplicate/malformed
   final schemas fail closed.
5. Confirm the terminal sanitizer admits only the nine closed failure
   coordinates plus the success coordinate, downgrades unknown exceptions to
   `EFFECTIVE_TOOL_COMPOSITION_UNCLASSIFIED`, and retains at most sorted safe
   tool names. Raw exception text, stack, prompt, payload, path, credential and
   environment values must remain absent. Generic `CUSTOM_RUNNER_FAILURE` may
   appear only as the contract's forbidden historical coordinate, never as a
   generated-helper terminal.
6. Confirm the outer broker remains independently mandatory and still enforces
   the same exact three-name allowlist. The guard must not be presented as an
   authorization or sandbox boundary.
7. Confirm the evidence/report support only deterministic guard construction:
   Node/native-Harness boots, occupied workers, sessions, broker/model/provider
   requests, network, Docker and database counts are all zero. Do not accept a
   native-boot, agent, model-quality, provider-reliability or production claim.
8. Confirm historical attempt 004 and native worker attempt 001 remain
   immutable and no attempt 005, product/API/config/client/flag/grammar/waiting
   area, ordinary-practice, data, production, deployment, Pages or protected
   ref authority was added.
9. Execute exactly the ten-command manifest. C03 must pass 73 tests. Do not run
   `--publish`, Node, npm install, the native Harness, broker, Docker,
   PostgreSQL, DeepSeek or another provider. Every command must return zero and
   leave the review worktree unchanged.

Do not edit, commit, push, install, access protected data, deploy, publish or
move protected refs. Return one schema-constrained `pass` only if the guard is
exact, attributable, fail-closed and has no P0-P2 finding. Otherwise return
`revision_required` with exact evidence. Emit one terminal decision.
