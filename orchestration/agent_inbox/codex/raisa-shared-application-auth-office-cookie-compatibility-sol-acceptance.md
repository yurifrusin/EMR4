# GPT Sol acceptance — shared application-auth Office cookie compatibility

Decision: **accepted** as
`raisa_shared_application_auth_office_cookie_compatibility_pass`.

The closed real-host evidence proves the complete existing session-cookie
lifecycle once in installed Word and once in Word Online. Each host used its
own authored-synthetic principal, one-use bootstrap and result channel; both
logged out and then received the expected authentication denial. No session
remains.

The installed-Word preflight failure was correctly bounded before taskpane
delivery. Enabling direct developer debugging repaired only host admission;
the matching stop command removed the registration, and no trusted catalogue,
tenant or Office policy setting changed. The reversed order of the two
successful runs has no security effect because the surface state was
independent and the failed preflight consumed nothing.

The static route allowlist, no-fallback scans, deterministic negatives,
expanded shared-auth/API/security suite, canonical lint/Bandit gates,
dependency audits, JavaScript syntax and three Office manifest validations all
pass. The parent runtime-foundation evidence's two stale source digests were
reconciled exactly without behavior change.

Publication readback exposed new development-only high Dependabot alert 17
after the parent governance snapshot. Static triage found no supported EMR4
sink because the affected optional project-service/default-project matcher is
not configured. The alert is registered as `SF-0020` with owner and SLA but
remains native-open pending an explicit disposition decision; no alert or
dependency mutation occurred.

The first PR 70 CodeQL wrapper then reported high alert 544 in the test's
URL-like CSP substring assertion and warning 543 for an ineffective local
variable wipe. Exact CSP directive parsing/equality and deletion of the dead
assignment are narrow complete repairs. Fresh JavaScript/TypeScript and Python
analyses plus the CodeQL wrapper pass, and both native instances report fixed
without dismissal or alert-state mutation.

The task-owned harness, tunnel, listeners and Office developer registration
are absent. The still-visible blank Word process has no registration or live
taskpane and remains for ordinary user closure; no document was read, written
or deleted.

This acceptance grants no real identity, Microsoft federation, product or
patient/clinical data, database-backed Office-host result, distributed abuse
resistance, organisational Office deployment, cloud/IAM mutation, production,
release or protected integration authority.
