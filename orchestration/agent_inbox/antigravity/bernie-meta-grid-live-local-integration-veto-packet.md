# Fresh Gemini Veto Packet — Meta-grid Live-local Integration

## Role and decision authority

You are a fresh-context independent veto reviewer. You do not implement,
accept your own work, move the baton, commit, push or alter any file. Sol keeps
all planning, correction, acceptance and protected-integration authority.

Return exactly one decision: `pass`, `revision_required`, or `blocked`.

## Workspace and source

- worktree: `C:\Users\sarashera\emr4`
- branch: `codex/bernie-meta-grid-live-local-integration`
- source HEAD: `1fdf4035341edda62765b8e11f9b0175ec1a3919`
- owned files: none; this is read-only review
- durable output: the launcher-owned file
  `orchestration/agent_inbox/antigravity/bernie-meta-grid-live-local-integration-veto.md`

## Authority being reviewed

Yuri authorised the bounded provider-free live-local synthetic
integration/evaluation tranche recommended by the accepted functional
meta-grid client closeout. It may use existing read and proposal-only routes,
one disposable local synthetic PostgreSQL database and real-browser evidence.
API/write/event-runtime/provider/PII/Stage 3B/deployment/release authority is
closed.

## Exact review inputs

Read only these files plus the exact task-branch diff for these paths:

1. `AGENTS.md` — current baton, authority and protected boundaries;
2. `docs/bernie-meta-grid-live-local-integration-evaluation-plan.md`;
3. `docs/bernie-functional-meta-grid-client-closeout.md`;
4. `orchestration/agent_inbox/codex/bernie-functional-meta-grid-sol-acceptance.md`;
5. `docs/diary/diary.html`;
6. `docs/diary/office-bootstrap.js`;
7. `docs/diary/meta-grid.js`, limited to the `refreshCurrent` reconciliation
   change and its immediately surrounding function;
8. `scripts/bernie_meta_grid_live_local_harness.py`;
9. `scripts/bernie_meta_grid_live_local_acceptance.py`;
10. `tests/test_bernie_meta_grid_live_local_integration.py`;
11. `tests/test_bernie_functional_meta_grid.py`, limited to the cache-version
    change;
12. `orchestration/prototypes/bernie-meta-grid-live-local-integration/browser-acceptance-evidence.json`;
13. the six PNG files named and hash-bound by that JSON; and
14. `orchestration/agent_inbox/codex/bernie-meta-grid-live-local-integration-preacceptance-receipt.json`.

Do not enumerate, open, search, infer or inspect any protected holdout v1-v10,
historical Diary material, provider raw response, secret, token, `.env`, local
patient file or unrelated repository path. Do not run repository-wide
discovery. Do not make network calls. Do not run the live harness or drop its
database. You may run only read-only syntax or the exact focused deterministic
test nodes below if needed.

## Evidence already produced

- five-source rehydration, pre-plan and preacceptance receipts: passed;
- live real-browser label: `live_local_browser_backend_postgres`;
- route interception: false;
- desktop 1440×900, tablet 1024×768 and 768×1024, smartphone 390×844 and
  844×390: passed;
- desktop DOM/body/host width: 1440; painted extent: 1.0; rightmost 20% painted
  ratio: 1.0;
- console/page errors, horizontal overflow and enabled targets below 44px: zero;
- API traffic: loopback only, exact existing bootstrap/read/proposal routes,
  zero failed responses, no confirmation or session/event route;
- provider: disabled; cloud credentials: absent;
- before/after PostgreSQL: identical six authored-synthetic appointment rows;
  zero appointment-audit, command-idempotency, Bernie booking-session and
  Bernie session-event rows;
- combined focused/API/Stage 3A/handover/Ariadne population: 180 passed;
- complete Diary smoke: one isolated 3-second timing failure, exact-node rerun
  passed, then complete clean rerun 139/139 passed;
- Node, Ruff and `git diff --check`: passed.

## Required veto questions

1. Does the implementation stay within the frozen existing read/proposal-only
   authority, with no API, database, confirmation, event-runtime or provider
   expansion?
2. Does `standalone_diary=true` remain strictly loopback-only, preserve normal
   Office.js loading, and avoid mislabelling intercepted evidence as live?
3. Does the Playwright runner use the ordinary visible client/backend path,
   avoid `page.route`, confirmation handoff and internal command calls, and
   label its one headless DOM blur fallback accurately?
4. Are the before/after database hashes and zero-row write/event invariants
   sufficient and actually enforced?
5. Does interrupted proposal recovery discard patient/selection/proposal state
   and perform a fresh availability read rather than retaining stale command
   material?
6. Do desktop, tablet, smartphone and keyboard claims match the artifact,
   including the corrected 1440-pixel painted-width evidence?
7. Are there any security, privacy, auth-token, synthetic-data, cleanup,
   evidence-integrity or backward-compatibility defects that require revision?
8. Are the claimed non-results calibrated: no provider, PII, committed event,
   Stage 3B, representative usability, production, deployment or release?

## Exact optional verification

If needed, run only:

```powershell
.\.venv\Scripts\python.exe -m pytest -q tests/test_bernie_meta_grid_live_local_integration.py tests/test_bernie_functional_meta_grid.py
node --check docs/diary/office-bootstrap.js
node --check docs/diary/meta-grid.js
git diff --check
```

Do not rerun browser/database acceptance.

## Output format

```text
Decision: pass | revision_required | blocked

Findings:
- [severity] path:line — concise evidence-backed finding
  (or `None`)

Boundary audit:
- API/write/event/provider/PII/Stage3B/deployment/release: intact | not intact

Evidence calibration:
- accurate | overstated | understated

Rationale:
<concise independent reasoning>
```
