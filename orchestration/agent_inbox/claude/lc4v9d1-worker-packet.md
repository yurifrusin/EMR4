# LC4V9D1 DeepSeek Bounded Development Diagnostic Packet

Date: 2026-07-16
Model: DeepSeek V4 Flash/high through Claude Code `--bare`
Worktree: `C:\Users\sarashera\EMR4-worktrees\lc4v9d1-dw1`
Branch: `claude/lc4v9d1-noncreate-identity-diagnostic`
Source head: exact dispatch commit containing this packet

Implement the diagnostic-only candidate frozen in
`orchestration/agent_inbox/codex/lc4v9d1-sol-contract.md`.

Read only these named files:

- `AGENTS.md`;
- `orchestration/agent_inbox/codex/lc4v9d1-sol-contract.md`;
- `app/services/bernie/semantic_extraction.py`;
- `app/services/bernie/language_normalization.py`;
- `app/services/bernie/lc4v4d3_policy_resolution.py`;
- `app/services/bernie/lc4v8d1_development_evidence.py`;
- `tests/test_bernie_lc4v8d1_development.py`; and
- `tests/fixtures/bernie_lc4v8d1_development/probes.json`.

The LC4V8D1 files are ordinary synthetic development evidence. All holdouts
v1-v9 and their fixtures, evaluator/authoring/support code, manifests, seals,
markers, reports, tests, and case evidence are forbidden. Do not open the V9
aggregate report or closeout. Do not use recursive listing, broad filename
search, or repository-wide grep.

Create only:

- `app/services/bernie/lc4v9d1_development_evidence.py`;
- `tests/fixtures/bernie_lc4v9d1_development/probes.json`;
- `tests/test_bernie_lc4v9d1_development.py`; and
- `orchestration/agent_inbox/claude/lc4v9d1-worker-closeout.md`.

Requirements:

1. exactly 30 fresh inspectable probes, six per frozen non-create action and
   one per frozen language structure;
2. no utterance copied from the ordinary D1 example fixture; use new synthetic
   names and wording while remaining parser-independent Gold;
3. fail-closed schema and cross-field Gold validation before evaluation;
4. observe extraction and policy before reading/comparing Gold;
5. exact 14-field policy projection and independent outcome/safety derivation;
6. exactly two repeats, 60 observations, zero-variance calculation, and one
   mutually exclusive class per probe;
7. deterministic aggregate report with fixture/report SHA-256 and explicit
   probe IDs by class;
8. diagnostic only—do not edit parser/policy/product code or repair gaps;
9. tests for counts, action/form balance, Gold contradictions, oracle
   separation, projection drift, variance, hash determinism, and safety; and
10. run focused tests serially with the main worktree Python executable.

Do not claim acceptance. In the closeout record exact source head, owned files,
commands/counts, fixture/report hashes, observed class counts, any scope
incident, and end with exactly `DECISION: candidate_ready` or
`DECISION: revision_required`.

Commit only the four owned surfaces on the disposable branch. Do not push or
move master/handoff.
