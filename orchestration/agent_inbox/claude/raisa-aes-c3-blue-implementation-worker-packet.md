# Worker packet: AES-C3 provider-free hostile containment rehearsal

Task ID: `raisa-aes-c3-blue-implementation-001`

Role: DeepSeek V4 Flash/high blue implementation and defensive test worker
through Claude Code `--bare`

Authorized worktree:
`C:\Users\sarashera\EMR4-worktrees\aes-c3-deepseek-blue`

Authorized branch: `codex/aes-c3-blue-deepseek`

Required source HEAD: `d44be5cbe0774b6340c7e4f6ca76075242b2f156`

## Mandatory rehydration before editing

1. Read `AGENTS.md` completely.
2. Read the frozen AES-C3 plan and threat-model delta completely.
3. Read the accepted AES-C2 plan, threat delta, closeout, Sol acceptance,
   contract/schema/scenarios/evidence and implementation needed by C3.
4. Read the accepted AES-C1 and AES-C0 exact contract/schema/scenario and pure
   validator surfaces reached by C3. Do not change any predecessor.
5. Restore AGENTS Sections 5 and 6 protected-evidence and user-decision
   boundaries.
6. Verify the exact branch, clean worktree and source HEAD. Verify but never
   move local/origin `master` or `handoff/current`, which must remain
   `2e34bdad732fdab32fbf778280b3d3c70d66d602`.
7. Use only exact named paths. Do not enumerate, search, open or infer any
   protected fixture/support/authoring/manifest/seal/receipt/per-case surface.

## Mission

Implement the frozen AES-C3 authored-synthetic, provider-free pure hostile-
containment rehearsal and defensive tests. Challenge hostile-looking strings,
forbidden structure, invalid adapter results, cumulative stops, stale replay,
context binding and supply-chain identity only through copied closed C1/C2
objects and pure functions.

Nothing may decode, decompress, deserialize, interpret, dereference, fetch,
execute or relay a hostile value. First challenge the plan against exact
accepted C0/C1/C2 behavior. If a conceptual or authority contradiction remains,
stop without implementation and write `decision: revision_required` in the
durable closeout. Do not silently reinterpret the plan.

## Owned paths

You may create or edit only:

- `orchestration/continuity/raisa-agent-execution-surface-containment-gate-aes-c3/containment-rehearsal-contract.json`
- `orchestration/continuity/raisa-agent-execution-surface-containment-gate-aes-c3/containment-rehearsal-contract.schema.json`
- `orchestration/continuity/raisa-agent-execution-surface-containment-gate-aes-c3/authored-synthetic-hostile-containment-scenarios.json`
- `orchestration/continuity/raisa-agent-execution-surface-containment-gate-aes-c3/provider-free-hostile-containment-evidence.json`
- `scripts/raisa_agent_execution_surface_containment_gate_aes_c3_hostile_containment.py`
- `tests/test_raisa_agent_execution_surface_containment_gate_aes_c3.py`
- `orchestration/agent_inbox/claude/raisa-aes-c3-blue-implementation-closeout.md`

Do not modify the frozen plan or threat delta, C0/C1/C2 artifacts, global
Continuity/Compass, AGENTS, `implementation_plan.md`, API Spine artifacts,
fast-profile configuration or any pre-existing test.

## Exact implementation contract

- Verify every inherited SHA-256 from the plan before scenario evaluation.
  Import only the exact pure C1/C2 validation, digest and simulation functions
  needed by the rehearsal.
- Freeze a closed wrapper whose fields, enums, mutation IDs and reason/status
  vocabulary exactly match the plan. JSON data may not name a Python path,
  module, attribute, callable, URL, expression or cleanup target.
- Implement exactly 61 scenarios with the exact IDs/order/outcomes in the plan:
  21 `contained`, 15 `reject`, 25 `stop`, 28 actual pure calls and 21 digest-
  only releases.
- Use one canonical exact C2 success attempt for opaque content. Replace only
  the already allowlisted `typed-context` string, synchronously in work-cell
  and broker-admission candidate copies. The C2 invocation must contain only
  the candidate digest and fixed C2 inputs, never the hostile value.
- Assert each raw hostile payload is absent from invocation, adapter result,
  returned C2 result, C3 evidence and any normalized failure surface. The
  scenarios fixture is allowed to hold its newly authored synthetic payload;
  final evidence is not.
- Add exactly one forbidden/undeclared field in each structured scenario and
  require closed validation rejection before the pure call. Do not repair or
  remap it into an allowed field.
- Reuse only the exact C2 malformed-result seam for five result-carrier
  scenarios. The fixed pure function must actually run once; the invalid result
  must release nowhere.
- Implement both cumulative sequences exactly: two boundary-probe denials reach
  ceiling 2 and a third attempt stops before dispatch; two invalid adapter
  results reach repeated-failure ceiling 2 and a third attempt makes no call.
  State is broker-owned, terminal, and generation-bound.
- Implement five carrier-independent egress budget scenarios with exact 256-
  byte synthetic values, cumulative total 12,033, ceiling 12,288 and proposed
  total 12,289. No carrier-specific decoder or accounting rule exists.
- Implement stale generation, restart, cross-Bureau lease and inert alias/token
  fixtures with `synthetic_noncredential: true`. None enters the work-cell view.
  Revocation/kill after admission stops before the C2 call.
- Bind candidate, proofreader and broker to one authored-synthetic ContextFrameSet
  digest. Challenge candidate and proofreader mismatches. Challenge exact
  manifest, adapter-artifact, runtime-image and model/provider identities.
- Keep all command-shaped GraphQL, REST, SQL, provider-tool, product-adapter,
  executable and policy content structural and rejected before a pure call.
- Generate hostile mutations for every closed wrapper field/enum/count/digest/
  identity plus unknown mutation ID, target path and carrier. Mutate every
  nested frozen C3 contract rule. Every mutation rejects or stops with zero
  release.
- Static inspection must fail any networking/HTTP/DNS, process/environment,
  database/SQL, template/HDF5/deserialization, archive/compression,
  encoding/decoding, dynamic import/execution/reflection or plugin-loading path.
  Do not import the forbidden facilities named in the plan.
- Emit closed minimized evidence with IDs, statuses, reasons, counts and
  digests only. Record zero real runtime, provider, credential, network,
  database/source, filesystem capability, decoder/interpreter, executable/
  tool/command or product/patient operation.

## Forbidden surfaces

- protected evidence and historical Diary PHI;
- patient, clinical, product-derived, financial or licensed data;
- real broker, work-cell process, container, adapter, plugin, route, listener,
  watcher or source connection;
- provider/model calls, credentials, IAM, metadata, network, HTTP, DNS, relay
  or external retrieval;
- database/source access, persistence, migration or SQL;
- hostile-content decode/decompression/deserialization, template execution,
  work-cell filesystem capability, executable tool, shell/process, dynamic
  import, product command/write or cleanup actuator;
- deployment, production, release, Pages or protected refs;
- package installation or environment mutation;
- edits outside the seven owned paths;
- `git add .`, `git add -A`, force operations or pushing any ref.

Exact repository artifact reads and owned generated-artifact writes are normal
acceptance-harness actions, not simulated work-cell capability. Keep commands
exact and within the authorized worktree.

## Verification

Use the existing interpreter only:
`C:\Users\sarashera\emr4\.venv\Scripts\python.exe`.

Run at minimum, serially where pytest loads repository `conftest.py`:

```powershell
C:\Users\sarashera\emr4\.venv\Scripts\python.exe scripts\raisa_agent_execution_surface_containment_gate_aes_c3_hostile_containment.py
C:\Users\sarashera\emr4\.venv\Scripts\python.exe scripts\ariadne_serial_pytest.py --timeout-seconds 240 tests\test_raisa_agent_execution_surface_containment_gate_aes_c3.py tests\test_raisa_agent_execution_surface_containment_gate_aes_c2.py tests\test_raisa_agent_execution_surface_containment_gate_aes_c1.py tests\test_raisa_agent_execution_surface_containment_gate_aes_c0.py tests\test_api_spine_artifacts.py -q
C:\Users\sarashera\emr4\.venv\Scripts\python.exe -m ruff check scripts\raisa_agent_execution_surface_containment_gate_aes_c3_hostile_containment.py tests\test_raisa_agent_execution_surface_containment_gate_aes_c3.py
git diff --check
```

Inspect the final diff against the required source HEAD and fail if any path
outside the seven owned paths changed. Do not run repository-wide discovery
over protected paths. Leave broader exact safe-path checks to Sol.

## Commit and completion

Write the durable closeout at the owned closeout path. It must state:

- `decision: pass` or `decision: revision_required`;
- exact source and final candidate HEADs;
- exact changed files;
- 61-scenario status/reason/call/release accounting;
- hostile attempt and contract-mutation counts;
- tests and exact results;
- raw-payload non-release and static-boundary evidence;
- issues found, unfulfilled criteria and residual risks; and
- explicit zero-runtime/provider/data/credential/network/database/filesystem-
  capability/decoder/tool/command evidence.

If all requested checks pass, stage only the seven owned paths explicitly and
commit on `codex/aes-c3-blue-deepseek` with message
`Implement AES-C3 hostile containment rehearsal`. Do not push.

Your terminal response must be concise and must not claim acceptance. Sol alone
decides whether to adopt the candidate.
