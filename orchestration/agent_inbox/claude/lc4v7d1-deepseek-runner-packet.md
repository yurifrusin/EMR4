# LC4V7D1 bounded baseline runner candidate

You are DeepSeek V4 Flash/high operating only through Claude Code `--bare`.
GPT Sol is conductor, semantic owner, acceptance/recovery owner, and protected
integrator. You have bounded implementation/test authority for the two named
source files plus your closeout. You may commit only to `claude/current`; do
not push or move any protected ref. DeepSeek Pro is forbidden.

## Workspace and source receipt

- Worktree: `C:\Users\sarashera\EMR4-worktrees\claude`
- Branch: `claude/current`
- Exact source head: `be5eeceb5e9f203de61cfadd9ac45e3c80184306`
- Commit message: `Implement LC4V7D1 development evidence runner`
- Decision: exactly `CANDIDATE: ready` or `CANDIDATE: blocked`

Read `AGENTS.md` completely and generate the required five-source Ariadne
receipt before work. Confirm exact head, branch, clean worktree, and source
scope. Preserve GPT Sol as conductor and Gemini as independent reviewer.

## Protected boundary

Holdouts v1-v7 are sealed. Do not open, enumerate, list, search, import, run,
regenerate, hash-check, infer labels from, or inspect any protected fixture,
support/authoring module, manifest, seal, receipt, test, filename, or per-case
evidence. Do not use broad discovery or preservation suites. Work only from
the fresh ordinary D1 fixture and the exact named files below.

## Exact files allowed to read

- `AGENTS.md`
- `orchestration/agent_inbox/codex/lc4v7d1-sol-contract.md`
- `tests/fixtures/bernie_lc4v7d1_development/probes.json`
- `app/services/bernie/certification_decision_taxonomy.py`
- `tests/test_bernie_certification_decision_taxonomy.py`
- `app/services/bernie/semantic_extraction.py`
- `app/services/bernie/language_normalization.py`
- `app/services/bernie/lc4v4d3_policy_resolution.py`
- `app/services/bernie/lc4v6d1_development_evidence.py` solely as an ordinary
  runner-shape reference
- `tests/test_bernie_lc4v6d1_development.py` solely as an ordinary test-shape
  reference

## Exact files allowed to create/edit

- `app/services/bernie/lc4v7d1_development_evidence.py`
- `tests/test_bernie_lc4v7d1_development.py`
- `orchestration/agent_inbox/claude/lc4v7d1-deepseek-candidate.md`

Do not edit the contract, fixture, taxonomy, parser, normalizer, policy,
AGENTS.md, any historical report, route, API, UI, database/provider file, or
any other surface.

## Runner contract

Implement a deterministic ordinary-development runner that:

1. fail-closed validates the exact top-level and case schemas, schema version,
   reference date, provenance, population 24, family counts 6/6/6/6, unique
   IDs, nonempty utterances, exact expected field population, and the
   family-specific normalization item schema;
2. computes deterministic fixture, complete report, and non-pass selection
   SHA-256 hashes using sorted compact UTF-8 JSON;
3. calls `extract_semantics(utterances, reference_date)` and then
   `resolve_policy(...)` with the extraction only, diary state `empty`, empty
   appointments, and no expected value passed downstream;
4. observes normalization time forms and proves each expected fragment has
   its exact canonical value plus a source span in the correct turn;
5. compares normalization, extraction, and policy independently, including
   no false completion claim, deltas, audit deltas, and simulated-write state;
6. runs each case twice, exposes both observations, and counts any variance;
7. classifies in precedence order as `normalization_gap`, `parser_gap`,
   `policy_gap`, `contract_layer_gap`, or `pass`; reserve
   `authoring_invalid` for fixture validation failure rather than guessing at
   Sol's semantic adjudication;
8. returns aggregate totals, layer pass counts, composed pass, safety,
   variance, family counts, classifications, exact per-case mismatches, and
   hashes in JSON-serializable form; and
9. contains no probe-ID branches, expected-value imports into interpretation,
   hidden tolerance, scenario repair, product mutation, or protected access.

For a case with `safe_no_mutation: true`, safety requires no completion claim,
no appointment/audit deltas, no simulated write, and exact expected policy
tools/outcome. For the ordinary replay-only create controls, safety requires
the exact expected policy result and a resolved practitioner ID; it does not
mean suppressing the expected simulated create evidence.

## Tests and closeout

Tests must cover exact fixture hash, fail-closed mutations for every structural
gate, exact two-repeat accounting, zero variance, classification accounting,
selection/report hash determinism, safety invariants, and absence of probe-ID
branching. Do not assert that baseline gaps pass. Run serially:

```powershell
C:\Users\sarashera\emr4\.venv\Scripts\python.exe -m pytest tests\test_bernie_lc4v7d1_development.py tests\test_bernie_certification_decision_taxonomy.py -q
```

Also run `git diff --check`. In the closeout record exact fixture hash,
aggregate/layer/family/classification counts, non-pass selection hash, report
hash, tests, changed files, exact commit, and any suspected authored defect.
Do not remediate any observed product gap. Commit only the three owned files.
