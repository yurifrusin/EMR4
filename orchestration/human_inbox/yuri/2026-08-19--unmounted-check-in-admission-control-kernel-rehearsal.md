# Yuri update — unmounted check-in admission-control kernel rehearsal

Date: 2026-08-19

Timestamp: 2026-08-19T04:37:32.9752761+10:00 (Australia/Brisbane)

Attention required: no

## Lay summary

The future safety gate for ordinary-practice check-in now has a working
clockwork model, but it is deliberately disconnected from the product. In the
accepted rehearsal there are no enabled ordinary practices and no path that
can accidentally create one. The emergency stop always wins, rollback can
only turn capability off, and even a fully formed ordinary-practice record is
denied because activation authority is absent.

The independent review passed after one useful fail-closed incident. Our first
review command list accidentally included a report generator, which changed
line endings in two review files. Nothing substantive or product-facing
changed, and the review was rejected automatically. We removed the writing
command, restored the exact files, taught the launcher to preserve a sanitized
failure receipt at this boundary, and the fresh review then passed cleanly.

This gives us unusually concrete evidence for your clockwork idea. The
mechanism should generate each bureaucratic reading from one typed event,
including the full Git identity, allowed side effect, stage, evidence identity
and attempt identity. The DeepSeek broker can then mesh with that clock by
accepting only the exact parent tick and returning a digest-bound result.

## Technical summary

- reviewed kernel source: `4204ec6348abb0f92b1a30314699d4a469fa860a`;
- 63 deterministic scenarios and 341 hostile mutations pass;
- zero active ordinary records and zero ordinary admission releases;
- five accepted disable-biased transitions; no accepted activation or resume;
- one-way global kill switch;
- full 40-character Git object required in the command envelope;
- unknown commit: no success, readback required, no retry;
- Gemini 3.7 Flash/high corrected veto: `pass`, 9/9 commands, clean unchanged
  candidate;
- Antigravity postcondition failures now persist digest-only evidence; and
- register revision 521: 602 incidents, none open.

For the clockwork efficacy baseline, this closeout observed one provider retry,
four rejected register/pre-verifier drafts, four failure-induced closeout or
transition verification reruns, two stale mutable-latch fixtures and zero
uncaught escapes. Manual-field count is not yet instrumented; making it a
derived reading is part of the successor architecture.

## Deliberately closed

No ordinary practice, feature flag, allowlist, route, API, database, client,
generic-status `Arrived`, waiting area, product/patient/clinical data, occupied
DeepSeek call, provider, production, deployment, release, Pages, live clockwork
or protected ref is opened.

## Next tranche

The next dependency-satisfied tranche is a provider-free shadow architecture
for the causal clock and its DeepSeek-broker gear. It will freeze the typed
tick/result protocol, atomic projection and measurable efficacy boundary. It
will not call DeepSeek or replace the current controls. Yuri's attention is not
required unless that architecture exposes a genuinely non-inferable outcome
choice.

The usual non-PHI continuing Pushover closeout notification succeeded with
request `0bc371f5-1b5d-46e3-977d-462c0f154928`.
