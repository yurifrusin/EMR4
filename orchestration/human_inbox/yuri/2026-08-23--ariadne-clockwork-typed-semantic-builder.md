# Yuri closeout — typed clockwork interface

Date: 2026-08-23

Timestamp: 2026-08-23T17:17:00.4614783+10:00 (Australia/Brisbane)

## Lay summary

The clockwork has become substantially easier to drive without becoming less
strict. For this class of tooling closeout, I now provide the decisions and
meaning; the clock fills in the fixed repository mechanics. The frozen
representative form shrank from 151 scalar entries to 64; the actual first
closeout contains 61, removing 90 opportunities for a purely clerical lapse.

It also stamps five human documents from one timestamp and runs one named
verification profile, rather than asking the orchestrator to rewrite command
arrays and test paths. Conflicts are returned together, and a failed command or
source drift stops before the clock moves.

## Technical summary

The exact committed candidate expands
`ariadne.governance_semantic_closeout_intent.v1` plus the
`provider_free_governance_tooling_v1` selector into the unchanged admitted
legacy transaction. It derives the fixed Baton acceptance, evidence paths,
provider-free broker, three shell-free commands and four whole governance test
files. Seven focused semantic/hostile fixtures and all 115 governance tests
pass, with byte-idempotent header preparation and byte-exact restoration after
an injected write failure.

Two test-only correction rounds were needed to align new fixtures with existing
schema and projection shapes. There was no live clockwork publication or
rollback during implementation, no product rerun and no provider/model call.

## Boundaries and direction

This does not make narrative judgment automatic, qualify the native DeepSeek
Harness, authorise Claude fallback or open product, data, runtime, production,
deployment, release, Pages or protected refs. The useful Harness coupling
remains three coarse readings: `prepared`, `terminal`, and
`accepted_or_recovered`.

The next tranche is a provider-free read-only matched efficacy review of this
first real semantic closeout. It will decide from actual measurements whether
to retain, adjust or roll back the interface. Yuri's attention is not required.
