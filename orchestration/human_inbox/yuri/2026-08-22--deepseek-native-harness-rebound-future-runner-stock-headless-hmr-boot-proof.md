# DeepSeek native Harness rebound-runner boot proof — paired closeout

Date: 2026-08-22

Timestamp: 2026-08-22T01:02:14.2919121+10:00 (Australia/Brisbane)

Yuri attention required: **no**

## Lay summary

The new Harness control has crossed an important threshold. We ran one real
local native Harness process, watched the stock headless system become ready,
switched in our exact future worker runner, and received the precise deliberate
pre-request stop that we had designed. The independent broker gauge stayed at
zero throughout. In plain terms, we can now prove that our runner was genuinely
mounted and reached the chosen safety barrier, rather than merely inferring
what happened from an opaque exit.

The two earlier package-install failures were both before the native attempt.
They were contained and did not waste the one-attempt authority. The final
recovery removed npm from this path entirely and used a fully measured local
package seed. That seed took about three minutes to copy and verify on this
machine, but then the actual native Harness lifecycle reached its typed stop in
under a second. For a long series of future runs, this is a predictable build
cost rather than an untraceable failure mode.

No DeepSeek model request was made, no worker edited anything, and no product or
patient data was involved. Nothing was deployed or released, and protected
branches did not move.

The first bureaucratic closeout tick also demonstrated the clockwork's value:
it published a successor latch containing one paraphrased boundary, the full
post-publication suite rejected that finite-vocabulary mismatch, and the
clockwork restored the previous generation byte-for-byte. The corrected tick
uses the registered boundary terms exactly. The native Harness result itself
was unaffected.

## Technical summary

- Accepted native execution source:
  `560f471b72bef0b9790120238657dc7afd4d602b`.
- One rc.7 native process; one HMR mutation; zero retry/resume.
- Exact readiness: `sentinel_activated` then `stock_headless_hmr_ready`.
- Exact terminal: `post_hmr_pre_request_failure` from
  `preset_root_roster_admission` / `preset_root_roster_mismatch`, joined to a
  canonical identity-bound all-zero broker reading.
- Zero agent/session/turn/broker/worker/model/provider/network/database/Docker
  and target counts.
- Runner/helper copies matched; bundle manifest unchanged; process and root
  cleanup passed.
- Process-free package seed: 32,744 files, 219,364,530 bytes, zero reparse
  points, exact seed/copy digest equality, 180673 ms materialisation.
- Verification: 27 focused tests, 206 inherited native-chain tests, deterministic
  check, Ruff, compilation, schema validation and typed evidence readback all
  passed.
- AER-0857 and AER-0858 preserve the two corrected prelaunch materialisation
  failures in prospective register revision 600.
- AER-0859 preserves the rejected paraphrased successor boundary; AER-0860
  preserves the contained rollback/republication sequencing errors. The first
  published generation was rolled back byte-for-byte before correction.

## Place in the Raisa direction

This gives the Ariadne clockwork a trustworthy gear on the native Harness side:
it can distinguish stock readiness, runner activation, the exact pre-request
stop and broker silence using bounded machine fields. That is the foundation
for controlling a DeepSeek worker as a subordinate actuator rather than hoping
that a long-running wrapper eventually explains itself.

## Planned next tranche

Next is
`deepseek-native-harness-provider-free-rebound-future-runner-agent-creation-boundary-rehearsal`.
It will use admitted preset roots to create exactly one inert DeepSeek worker
agent and emit a typed agent-created sidecar, while still stopping before a
session, turn, broker/model/provider request or target use. This is the smallest
safe step from proven runner activation toward an occupied, broker-controlled
Raisa development worker.

All product, configuration, API/database, ordinary-practice, clinical data,
production, deployment, release, Pages and protected-ref surfaces remain
closed.
