# Yuri update — typed serial continuation-state projection

Date: 2026-08-23

Timestamp: 2026-08-23T19:41:32.8076827+10:00 (Australia/Brisbane)

Attention required: `no`

## Lay summary

The clockwork can now fill its own ordinary serial continuation form from a
short typed card. In the first real use, the model supplied 9 values instead of
114. The full card-and-receipt record became about 61 percent smaller by bytes,
yet the safety decision was identical.

This is the practical lesson from the extended DeepSeek Harness work: connect
systems at a few coarse lifecycle readings, and make each side derive its own
internal bureaucracy. Do not try to synchronize every conversational move.

## Technical summary

- the typed intent is 14 lines / 741 bytes;
- caller leaves fell 114 to 9 (92.1 percent);
- the pair fell 327 to 135 lines and 16,191 to 6,330 bytes;
- the complete runtime state is derived in memory and not written as another
  artifact;
- worker dispatch, positive assignments and free-form lane rationales are
  impossible through this preset;
- all existing receipt/latch/five-source/Git safety projections are retained;
- 42 focused and 162 combined tests plus Ruff pass first time; and
- the one-time source implementation is roughly recovered after two ordinary
  serial receipt events by line volume, before counting avoided form errors.

## Deliberately closed

This does not qualify the native DeepSeek Harness, reopen Claude fallback,
change worker allocation, reduce test cadence, touch product or patient data,
or open runtime, deployment, release, Pages or protected refs.

## Place in the Raisa direction and next work

The mechanism moves the LLM closer to selecting typed control positions while
the deterministic system owns expansion and validation. The next observation
uses it for three ordinary serial continuation events, then measures whether
the preset needs adjustment before any further workflow reduction.
