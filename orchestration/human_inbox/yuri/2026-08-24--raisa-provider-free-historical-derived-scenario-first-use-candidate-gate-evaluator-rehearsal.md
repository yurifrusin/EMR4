# Raisa historical-scenario first-use gate — lay and technical closeout

Date: 2026-08-24

Timestamp: 2026-08-24T10:07:35.8928123+10:00 (Australia/Brisbane)

Attention required: **no**

## Lay summary

The first-use gate is now executable. It behaves like a tightly constrained
form, not a writing task: only known structural event types, bounded numbers,
one full Git source and one exact fingerprint can enter. It has only three
buttons—block, require revision, or admit this one exact artifact—and an
admission cannot spread to another artifact or purpose.

The test used invented candidates, so no historical-derived scenario exists
yet. The clockwork's current private-data mode expressly forbids fixture writes,
so the next short tranche adds one exact gate-wrapped materialisation mode.
After that, the gate can be used once on a proposed small local scenario and
write one reusable test fixture only if every box passes.

## Technical summary

Exact reviewed source is `abcd4206a363b0c565c070e0f2cb9c54d627b3b3`.
One positive, five blocked, two revision-required and five schema-rejected cases
pass. The form forbids unknown keys and free text, recomputes canonical SHA-256
and utility, and emits a non-transitive exact binding while keeping write,
archive, provider, product, runtime and ordinary-practice authority false.

Seven focused and 222 existing controls pass—229 across 25 files—with Ruff,
compileall, typed-schema and source-boundary checks.

The first preplanning receipt used the wrong event token and one test used a
slightly different verb from the frozen threat text. Each failed closed and
caused one deterministic rerun; neither touched private data or a provider.
They are recorded as AER-1150 and AER-1151.

Deliberately closed: actual candidate admission, reusable historical-derived
content, archive re-access, product/database/client/runtime use,
ordinary-practice, provider/model, production, deployment, release, Pages,
protected evidence and protected refs.

Next: add and prove the closed clockwork subgate without private access. Then
run one local-only, no-retry materialisation attempt in which the candidate
remains in memory until this gate admits its exact digest; block or revision
writes no fixture.

Pushover delivery succeeded under request
`4175a37a-e044-44fb-ae8a-ad75ca8fa74f`.
