# Complete package-unloaded runner — paired closeout summary

Date: 2026-08-22

## Lay summary

The full controlled runner worked. In its one allowed local rehearsal it
assembled the fake worker correctly, applied the exact preset and tools,
installed the chosen DeepSeek settings, stopped publication at the intended
safety barrier and left no live agent or session behind. It did this without
contacting DeepSeek or loading the real Harness.

Our checker then made an avoidable paperwork mistake: it expected the three
small result fields alphabetically, although the frozen fixture emitted them
in its declared order. The runner's successful sidecar was exact and the
process had already exited cleanly. We preserved the rejection, proved from
the recorded byte counts and hashes that both outputs were exactly the expected
ones, and accepted the result without rerunning anything.

This is real forward progress: the complete runner boundary is now passed. It
is also a useful warning that AER-0936's earlier serializer fix was too local;
the same general mistake recurred in the real validator. The stronger control
now separates wire serialization from evidence serialization structurally.

The first clockwork closeout also tried to add a descriptive acceptance row
instead of replacing its one closed DeepSeek slot. The broad governance test
caught the duplicate; the previous generation was restored byte exactly. The
corrected gear now refuses any live acceptance label outside its closed list,
so the same paperwork choice cannot create another row.

The next projection revealed the same class of issue in a boundary token: a
broader sentence was present, but the exact closed ordinary-practice token was
not. That generation was also restored byte exactly. The clockwork validator
now owns the exact ordinary-practice and product/data floor before any write,
rather than relying on closeout authors to remember it.

Next we go directly to one tightly bounded real DeepSeek Harness worker doing
useful Raisa development work. There is no further ceremonial local rehearsal
unless that occupied preflight exposes a concrete new hazard.

## Technical summary

- Accepted result:
  `complete_package_unloaded_runner_evaluation_reconciled_pass`.
- Derived runner: 12,950 bytes,
  SHA-256 `5ef3b25babad23f4851faf7981cbdd6e77bf04701e91bf9ed80387df53f93ab9`.
- Closed graph: four relative edges, four exact local ESM package stubs and
  three admitted Node built-ins in a thirteen-file disposable inventory.
- Consumed process: one Node process, exit 0, stderr 0, stdout 129 bytes,
  sidecar 1,567 bytes, disposable root absent.
- Runner terminal: `prepublication_veto_diagnosed`; preset mounted, model
  selection installed, exact veto observed/rejected, registries/events empty,
  request/provider/model/target counters zero.
- Immutable controller terminal: `complete_runner_result_rejected`.
- Recovery: zero-process, zero-retry exact-preimage hash reconciliation.
- Register: revision 614, 943 bounded incidents, none open; AER-0939 is the
  material wire/evidence serializer recurrence and AER-0942 records the
  duplicate acceptance-row projection, byte-exact rollback and closed
  rolling-label guard; AER-0943 records the exact-boundary-floor recovery.
- Protected refs remain exact at
  `2e34bdad732fdab32fbf778280b3d3c70d66d602`; no Pages or product/data action.
