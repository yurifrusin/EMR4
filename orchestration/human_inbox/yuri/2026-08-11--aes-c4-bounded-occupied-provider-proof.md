# AES-C4 bounded occupied provider proof closeout

Date: 2026-08-11

Result: passed

Attention required: yes — AES-C5 needs the first real product source/purpose and privacy boundary

## Lay summary

Raisa's containment boundary has now carried one harmless synthetic request to
the Sydney Vertex model and back without giving the model a tool, credential,
product connection or command. The broker—not the model—chose the provider,
project, identity, region and operation. The returned text was treated as
untrusted until ordinary code proved the exact four expected fields.

Exactly one call was made and no retry was possible. The result said only that
the broker boundary was contained and explicitly carried no command authority.
The call ledger is permanently consumed, and no reusable token, lease, process,
listener or temporary capability remains.

## Technical summary

Exact independently reviewed source
`e569da0a9081117b799e9437d8b7025230e2162b` passed a fresh Gemini 3.6
Flash/high veto with 368 serial tests, Ruff, compilation, exact Git checks and
an unchanged clean worktree. Fresh public-source and read-only cloud checks
confirmed GA/Sydney availability, current 2026-10-20 retirement, current
Standard pricing, exact Bernie project/quota/impersonated identity, billing,
API, prediction-only permission, audit, logging/cache posture, zero keys and no
fallback.

The one `gemini-2.5-flash` call returned HTTP 200/`STOP` in 1,726 ms with 363
prompt, 40 candidate, 139 thought and 542 total tokens. Deterministic release
was exactly `contained`, the frozen synthetic nonce,
`broker_boundary_confirmed`, and `command_authority: false`. Every cleanup and
zero-external-product-operation assertion passed.

## Issue resolved

The first regenerated zero-call evidence used a non-existent full commit hash
copied from restored summary state. Rehydration caught it before review or any
call. The invalid pair is preserved as AER-0261, a corrected pair was generated
from Git's exact verified HEAD, and the corrected source received a fresh
independent veto. It caused no provider call and supplied no authority.

## Deliberately still closed

No patient, clinical, product-derived, practice, licensed or external-corpus
data was used. Product reads, database/source adapters, filesystem capability,
provider tools, generic network, commands/writes, reusable runtime, IAM or
credential changes, deployment, production, release, Pages and protected refs
remain closed. The Sydney endpoint proves the configured and observed path,
not physical or sovereign processing in Australia.

## Place in the Raisa direction

AES-C0 froze the authority contract; C1 proved admission; C2 proved broker-owned
inert dispatch; C3 proved hostile-content and cumulative-stop containment; and
C4 now proves one real provider crossing under those same external controls.
The chain is complete through authored-synthetic occupied inference.

## Next tranche and attention fork

AES-C5 is the first one-source, one-purpose product-runtime admission. Selecting
the real source and purpose, and freezing its privacy, identity, retention and
product-data posture, would materially determine what EMR4 data is exposed.
AES-C4 does not choose or authorize that. Please nominate the first source and
purpose—or explicitly ask Sol to present bounded alternatives—before AES-C5
planning proceeds. No AES-C5 read or provider call has started.
