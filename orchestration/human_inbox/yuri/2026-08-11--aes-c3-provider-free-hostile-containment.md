# AES-C3 provider-free hostile containment rehearsal closeout

Date: 2026-08-11

Result: passed

Attention required: yes — AES-C4 requires a new exact occupied-provider envelope

## Lay summary

Raisa's safety boundary can now rehearse hostile-looking content without
opening or following it. File paths, credential and metadata references,
template or serialized-object text, URLs and encoded-looking content remain
inert strings. If those ideas arrive as new structural fields, they are refused
before the harmless test function runs. Invalid output, repeated probing,
stale authority and cross-generation reuse all stop without a released result.

We exercised 61 authored-synthetic cases. Twenty-one harmless opaque cases
released digests, 15 structural attacks were rejected and 25 cases stopped.
Only 28 calls reached one ordinary pure Python function. No referenced file,
credential, network, database, provider, product, patient record, tool or
command was opened or used.

## Technical summary

AES-C3 binds every scenario to an exact AES-C1/C2 base and current generation,
manifest, Bureau, work-cell, authority, ContextFrameSet and supply-chain
identity. It proves exact replay-fixture comparison, dispatch-time revocation
and kill precedence, carrier-independent byte charging, cumulative terminal
latching and digest-only egress. All 33 hostile attempt mutations and 18
contract mutations fail closed. A fresh Gemini 3.6 Flash/high veto passed 120
focused tests at unchanged clean reviewed HEAD
`c45ff191af420b801e9917a7efc69c17aeb5698b`.

## Issues

The first worker candidate matched the planned totals but could label stale or
contradictory inner results as the expected outer stop. It also raised on some
malformed public inputs and under-checked cumulative transitions. Sol rejected
that self-pass, preserved the failure as AER-0257 and recovered the candidate
under the formal recovery lease. Exact adversarial regressions and the fresh
independent veto now pass. A wrong Sol test filename was separately recorded;
that command produced no evidence and the corrected full packet passed.

## Deliberately still closed

This is not semantic prompt-injection detection or real operating-system,
process or container isolation. There is still no real broker, adapter,
credential, provider call, product or patient context, database/source access,
filesystem capability, generic network, executable tool, command/write,
deployment, production, release, Pages or protected-ref authority.

## Place in the Raisa direction

AES-C0 froze authority, AES-C1 proved admission, AES-C2 proved broker-owned
inert dispatch, and AES-C3 now proves the hostile-content, cumulative-stop and
stale-authority seam over those same closed objects. The prerequisite chain is
complete up to—but not including—an occupied provider capability.

## Next tranche and attention fork

AES-C4 is the planned bounded occupied authored-synthetic provider proof. It
requires a newly frozen exact provider/model, region, identity, authored-
synthetic data, call and cost ceilings, isolation, proofreader, cleanup and
no-fallback envelope. The provider lane is currently paused and all historical
call envelopes are consumed, so the standing authority cannot infer those
user-owned choices. AES-C3 is complete; Yuri's attention is genuinely required
before AES-C4 planning or any provider call.
