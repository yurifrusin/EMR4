# Verification-envelope phase and runner-admission repair

Date: 2026-08-23

Timestamp: 2026-08-23T05:57:19.6577887+10:00 (Australia/Brisbane)

Yuri attention required: `no`

## Lay summary

This is a meaningful move inward rather than another widening circle. Two
choices that previously depended on the orchestrator remembering the rules now
behave like keyed mechanical selectors. When database access is closed, the
form cannot select either database-capable test runner. And every verification
command must be placed before or after publication using one of two exact
positions; the mechanism will not accept a descriptive substitute or reverse
the order.

The result is not presented as flawless. Two small mistakes in the new tests
and conformance harness were caught on their first runs, and one broad test
packet was repeated because its terminal transcript was unavailable after
context restoration. None of those corrections reached a database, provider,
product or canonical record. The useful measure is that the two recurring
workflow mistakes this tranche targeted were rejected eight times before any
child process could start.

## Technical summary

Exact implementation source is
`aab7dcf2e408f05f57935c1ee1800cff04be6cdf`; canonical evidence is committed at
`e80d2839a20d9bfff5b65133862cbca9aa00cb45`. One shared module supplies finite
database-authority and verification-phase vocabularies plus runner
classification to verifier manifest v2 and governance manifest v2. V1 replay
is unchanged. The validation runner requires `--phase` for v2 and emits the
phase, authority and full manifest digest in its bounded receipt.

Focused and broader provider-free packets, direct conformance, Ruff, Python
compilation and diff hygiene passed. The reusable implementation cost is 218
net lines, with 872 supporting contract/conformance/test lines. V2 leaves zero
free-form runner-authority or phase choices.

## Deliberately closed

No database, attempt 008, provider/worker, product route/client/configuration,
ordinary-practice admission, patient/clinical data, production, deployment,
release, Pages or protected ref moved. All unrelated untracked files,
especially `docs/branding/`, remain preserved.

## Next

The engine will make a read-only, provider-free decision about whether the
accepted evidence is now sufficient to freeze a separately bounded attempt-008
plan. That decision tranche cannot plan or execute the occupied attempt. No
decision from Yuri is needed.
