# DeepSeek native Harness safe-terminal bridge — paired closeout

Date: 2026-08-22

Timestamp: 2026-08-22T04:31:24.1598131+10:00 (Australia/Brisbane)

Yuri attention required: **no**

## Lay summary

The control mechanism worked again and became substantially more informative.
The DeepSeek Harness did not merely stop somewhere inside its agent factory: it
reported, in a small safe vocabulary, that the failure occurred specifically
while mounting our three-tool preset.

Nothing was published, no model was contacted, no target file was touched, and
the temporary installation cleaned up completely. This does not yet give us a
working DeepSeek worker, but it proves that the Harness can be made to fail in
a way the orchestrator can classify and reason about instead of losing the
failure in opaque process behavior.

The next step is deliberately not another attempt. We will first turn the
pinned Harness source into a finite map of the remaining internal mount
locations. That should let any later probe ask one narrow typed question rather
than spending a process on another broad diagnosis.

## Technical summary

- Exact execution source:
  `c66eb82cccd64961f0d99bf9f67803e1a69ebd8a`.
- Terminal: `preset_composition_failure_attributed`.
- Safe coordinate: `EFFECTIVE_TOOL_COMPOSITION_PRESET_MOUNT_FAILED`.
- Last admitted stage: `private_identity_admitted`.
- Factory/private Agent/private Session: `1/1/1`.
- Public registries and lifecycle announcements: all zero.
- Turn/request/broker/worker/model/provider/network/database/Docker/target:
  all zero.
- Exit code 3 after one typed sidecar; no retry/resume.
- Process/root absent; bundle and package seed unchanged.
- Current focused/evidence and selected inherited verification: 26 and 192
  tests passed respectively; deterministic check remains process-free.

## Deliberately closed

No worker or occupied model, product/data/API/database/route/configuration
change, ordinary-practice enablement, production runtime, deployment, release,
Pages or protected-ref action opened.

## Place in Raisa

This is a small but significant control result: the orchestrator can now
receive a finite, non-sensitive terminal from a failure inside the DeepSeek
Harness agent factory. Work continues under standing authority through the
source-coordinate reconciliation tranche; Yuri's attention is not required.
