# Lay and technical summary — native Harness accepted guard graph

Date: 2026-08-22

## Lay summary

We moved forward but did not obtain a formally accepted pass. The corrected
guard system loaded with the unchanged runner and the local process completed
cleanly. Our form rejected the result because two internal activity counts were
different from the numbers we predicted. We did not rerun it and no DeepSeek or
provider call occurred.

The retained byte count and cryptographic hash appear to identify the exact
two-count difference. The next step will prove that deterministically without
starting another process. This is precisely where the clockwork is useful: it
turns a failed form into a finite reading rather than another blind attempt.

## Technical summary

- exact runner/accepted guard graph and all 15 import edges passed;
- one installed-AgentRegistry Node fixture exited 0;
- stdout/stderr: 756/0 bytes, raw streams not retained;
- terminal: `fixture_result_rejected`, attempt consumed, retry/resume/fallback
  zero;
- provisional hash match: `preset_root_reads=4`,
  `hook_installations=5`;
- Harness/broker/worker/model/provider/network/database/Docker/product-target
  counts zero;
- disposable root absent and protected refs unchanged; and
- next: process-free finite-grammar uniqueness proof.
