# EMR4 model-required Bureau A3/B3 terminal-reconciliation closeout

Date: 2026-08-04

Result: `model_required_bureau_a3_b3_occupied_terminal_rejection`

## Accepted outcome

The bounded A3/B3 occupied tranche did not establish a combined pass. After
the restored existing Bernie impersonated ADC passed fresh read-only controls,
the exact reviewed Rayleen A3 primary sent one newly authored synthetic request
through Vertex AI `gemini-2.5-flash`, project `bernie-emr4-dev` and the
`australia-southeast1` endpoint. The broker ended at
`provider_content_invalid` before candidate extraction or proofreading.

Exactly one provider call and USD 0.25 reservation were consumed. No candidate,
correction ticket or release exists. Davida B3 did not start. No patient,
clinical, product-derived, protected or production data was used; no product or
database read/write, command, actuator, deployment, release, Pages or protected
ref action occurred.

## Evidence recovery

AER-0017 preserves the interrupted outer-harness split. The provider-free
terminal finalizer validates the immutable preflight, child ledger, hash-
chained audit and interruption evidence; binds canonical tracked exact-source
inputs; reconciles the parent to one reserved and consumed call; emits exact
no-release attempt/tranche evidence; proves current exact runtime absence; and
stops before correction or Davida. Finalizer and acceptance provider-call count
is zero. The evidence separately reports the one historical candidate-runtime
call.

Review 7's duplicate terminal envelope was rejected and contained as AER-0018.
Review 8 correctly found that the hash-bound audit JSONL was not checkout-
stable under Windows `core.autocrlf=true`. AER-0019 pins the scoped A3/B3 JSONL
namespace to LF and mechanically verifies the Git attribute, bytes and
preserved SHA-256.

## Acceptance

Fresh exact candidate HEAD `063153b9a799b32d125084fb77134588c9a6ac76`
passed in a genuinely fresh Windows worktree:

- audit `eol: lf` and SHA-256 `27d665f162ead5ee70d9db9cb39500bbe621e63b5bc0168b91ec6fb43d82bcad`;
- provider-free A3/B3 acceptance with zero acceptance-runtime calls;
- 305 Review 9 tests;
- Ruff and both diff checks; and
- one fresh Gemini 3.6 Flash/high `pass` with no findings and a clean unchanged worktree.

AER-0017 and AER-0019 are corrected. AER-0018 remains contained. No raw
provider response, raw verifier output, credential or token is retained.

## Closed successor boundary

Current authority is consumed. It grants no Rayleen repeat, correction turn,
Davida start or request-contract change. A possible `thinkingConfig` or output-
budget interaction remains inference only. Any further provider call or request
change is a new material Yuri decision. A4/A5/B4, C4, product/runtime/data/write
surfaces, cloud/IAM, deployment, production, release, Pages and protected refs
remain closed.
