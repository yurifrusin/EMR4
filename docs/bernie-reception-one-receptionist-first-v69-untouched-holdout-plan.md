# Reception One Receptionist-first v6.9 Untouched Holdout Plan

Status: authorised conditional evaluation
Recorded: 2026-07-30

## Purpose

Evaluate the frozen v6.8 receptionist/form/proofreader lane against twelve
newly authored synthetic requests that have never been sent to the provider
and will not be used to alter the prompt, schemas, operator catalogue,
proofreader, desk context or generation settings.

This is a new descendant. It does not revise or rescore v6.8.

## Freeze

Before any occupied call, record hashes for:

- the complete v6.8 system instruction;
- model form, turn-input, desk-context and correction-ticket schemas;
- provider response schema;
- typed operator catalogue and deterministic proofreader implementation;
- temperature zero, 1024 thinking tokens, hidden thoughts and 3072 maximum
  output tokens; and
- this twelve-case manifest and its deterministic oracle.

The holdout may be corrected only for a provider-free authoring defect, such
as an invalid schema or an expected label that disagrees with the already
frozen deterministic semantic adapter. Once the provider-free gate seals the
manifest, no case, label, prompt, schema or proofreader change is permitted.
A failed occupied result closes v6.9; it cannot become training data while
retaining holdout status.

## Cohort

The twelve cases cover create, move, resize, cancel, status, squeeze-in and
clarification outcomes using new exact wording and combinations. All data are
deliberately authored synthetic. The ordinary Margaret/Dr Shera blocking
booking path remains present without reusing an exact v6 utterance.

Each case receives one primary call and at most one proofreader correction.
The absolute ceiling is twenty-four calls and USD 1. No third call, prompt
change, fallback or post-success call is allowed.

## Exact provider boundary

- Google Cloud Vertex AI `gemini-2.5-flash`;
- project `bernie-emr4-dev`;
- exact Bernie service account through existing keyless impersonated ADC;
- `australia-southeast1`;
- `australia-southeast1-aiplatform.googleapis.com`;
- credential-free isolated cells and one-use broker ledgers;
- no API key, static key, global endpoint or fallback;
- no tools, grounding, retrieval or cache creation; and
- no product/database access, confirmation, write or delivery.

## Acceptance

The holdout passes only if all twelve terminal outcomes match their sealed
oracle, every ledger and audit chain closes, no call follows the terminal
cohort result and residue is zero.

Only a pass may unlock the separately bounded read-only authored-synthetic
Diary-context integration. Any mismatch closes this holdout candidly.

## Claim limit

A pass is stronger than the repeatedly observed v6 development cohort because
the cases are frozen before first provider exposure and are not used for
tuning. It remains a small authored-synthetic development holdout, not
representative receptionist, real-data, production or sovereign-processing
evidence.
