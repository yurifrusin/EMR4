# Provider-free check-in native Harness preset-validation subcoordinate recovery plan

Date: 2026-08-20

Timestamp: 2026-08-20T16:36:42.2544702+10:00 (Australia/Brisbane)

Status: `frozen`

Operation:
`raisa-provider-free-check-in-native-harness-preset-validation-subcoordinate-recovery`

## Purpose

Replace the opaque `PRESET_VALIDATION_PASSED` coordinate with three closed,
independently testable readings:

1. exact `emr4-bounded-worker` roster-row discovery and health;
2. exact composition-byte read plus package-dialect parse/shape admission; and
3. exact 158-byte length plus SHA-256 binding.

The accepted lifecycle process is immutable negative evidence. This is not a
retry of it and does not test DeepSeek model quality. Static package-source
proofs and a provider-free package-only characterization must pass before a
separate checkpoint can admit one new provider-disabled native Harness
validation process. That process must stop before `agents.create`, preset
mount, any turn, or any broker/model/provider request.

## Exact predecessor and evidence binding

- Accepted lifecycle closeout source:
  `290923ef7b068b4b61f1bf41fff84fe4f47e3049`.
- Pinned package: `@deepseek-ai/dsh-agent-presets@0.1.0-rc.7` with lockfile
  integrity
  `sha512-T/VcMV7lrXCFmRKrtoMTAz5DAdUmku6hz95wbikRvRc0WizIwQ3R04ke9KIeDiQcxK8xkE8cx+IYqEWa9C5gPg==`.
- Exact preset: `emr4-bounded-worker`, path
  `.agent-presets/emr4-bounded-worker/agent.cordis.yml`, 158 bytes, SHA-256
  `3de182eb702e6f2b397941c73393b87f65acb9b401565f966059d2bd46f649d1`.
- The retained rc.7 installation is read-only package evidence. No install,
  update, registry access or network request is authorised.
- The consumed lifecycle probe, its terminal, attempts 001 through 005 and all
  earlier Harness terminals remain byte-immutable.

## First workflow self-correction

The terminal prepush draft in the predecessor manually completed a short Git
abbreviation to a wrong full object ID and was rejected before publication.
The existing object-resolution guard would reject a non-object, but narrative
Git fields still allowed human transcription and therefore a valid-but-wrong
object role.

This tranche first makes exact ref ownership mechanical:

- `source_evidence.git_refs_and_worktree` may contain prose only and no full
  40-character Git object ID;
- `git_refs_snapshot` remains the sole owner of exact HEAD, task-origin and
  protected-ref values in an orchestrator receipt;
- `active_operation.source_head` remains a structured full object ID and keeps
  its existing commit/ancestry resolution;
- historical IDs in other acceptance-evidence fields remain permitted; and
- the rejected predecessor draft is preserved and enters the successor error
  register as AER-0691 at closeout.

This removes an operator memory requirement rather than adding one.

## Frozen stages

### Stage 1 — deterministic source and byte characterization

Create one provider-free controller and focused tests that read only the exact
pinned installation, canonical preset and predecessor contract. They must
prove:

- lockfile version, package version and registry integrity;
- the exact `AgentPresets.list()` to `discoverPresets()` to `scanRoot()` path;
- `scanRoot()` constructs `<root>/<id>/agent.cordis.yml`, reads the composition,
  parses with `js-yaml` plus `entryListSchema`, and reports an unusable file as
  a `broken` roster row;
- `resolveMountable()` rejects a row carrying `broken` before standing mount;
- the canonical preset's exact path, bytes, length and digest; and
- no Node, native Harness, agent, turn, broker, model, provider, network,
  Docker or database process/request has occurred.

Static proof failure stops the tranche before package execution.

### Stage 2 — package-only discovery characterization

After Stage 1 passes, one repeatable offline package-only command may import
the exact pinned discovery module and scan a newly materialised disposable
preset root. It must not assemble or boot the Harness service graph.

Its closed result separates:

1. `row_discovery`: exact ID found once, exact path, exact trust, and
   `broken` absent;
2. `byte_read_and_parse`: regular file readable and exact package-dialect
   parse/entry-list shape admitted; and
3. `digest_and_length_binding`: exactly 158 bytes and exact SHA-256.

Only enumerated status/reason codes may persist; raw exception text and raw
stdout/stderr are not evidence. The disposable root and process must be absent
at terminal. No network-capable package command (`npm`, `npx`, install,
update, audit or registry query) is allowed.

If this characterization fails, preserve the closed negative reading and make
the narrowest controller or fixture correction allowed by the exact source.
It does not consume the later native-process allowance.

### Stage 3 — separate native-process checkpoint

Only after Stages 1 and 2, schemas, focused tests, static checks and a fresh
five-source preexecution receipt pass may the active latch advance to a
separate checkpoint. That checkpoint must bind the exact clean candidate,
package files, preset bytes, controller, contract, terminal path, timeout,
cleanup and process count.

Before the checkpoint, native Harness process count must remain zero.

### Stage 4 — one provider-disabled native validation process

The checkpoint may admit exactly one newly named provider-disabled native
Harness process. It may start the already pinned local service graph only far
enough to obtain `agentPresets` and emit these ordered marker families:

1. `PRESET_ROW_DISCOVERY_ENTERED`, `PRESET_ROW_FOUND`,
   `PRESET_ROW_HEALTHY`;
2. `PRESET_BYTES_READ`, `PRESET_PACKAGE_PARSE_PASSED`; and
3. `PRESET_LENGTH_BOUND_PASSED`, `PRESET_DIGEST_BOUND_PASSED`.

Success terminates immediately after the last marker. The runner must contain
no `agents.create`, preset mount, session, turn or model call. Failure writes
one sanitized terminal naming the first absent/failed subcoordinate, consumes
the process allowance, performs bounded cleanup and forbids retry.

## Evidence and acceptance

The tranche may be accepted only when:

- the machine-snapshot-only Git guard and focused preflight tests pass;
- the contract and schemas fail closed on missing, duplicate, reordered,
  malformed, raw-text or broadened fields;
- deterministic source/byte characterization passes;
- the package-only discovery characterization passes with all three families;
- a fresh preexecution receipt proves the distinct native checkpoint;
- the sole native process either reaches the exact digest marker or produces
  one accurately labelled closed negative terminal;
- process, disposable root, agent/session/turn/broker/model/provider/network/
  Docker/database counts and cleanup are exact;
- a risk-weighted independent veto is performed if the deterministic candidate
  changes the native controller or evidence meaning; and
- AER-0691 and any new qualifying incidents are contained in the successor
  register revision before acceptance.

A negative native terminal can be accepted as bounded diagnostic evidence but
cannot be called native preset-validation success.

## Parallelism assessment

- **DeepSeek Flash:** declined with negative leverage through planning and
  deterministic characterization. The native Harness is the evidence subject,
  and no DeepSeek model request is admitted.
- **Gemini 3.7 Flash/high:** declined at planning with neutral leverage;
  reassess only after an exact deterministic candidate exists. It may own one
  fresh isolated read-only veto, never implementation or acceptance.
- **Native subagents:** declined. Current developer policy prohibits proactive
  delegation, and the source, checkpoint and one-process boundary are serial.
- **GPT Sol:** owns the Git guard, plan, deterministic/package characterization,
  checkpoint, monitoring, cleanup, recovery, acceptance and Git.

## Deliberately closed

No retry/reclassification of consumed processes; no DeepSeek, Gemini or other
provider request; no attempt 006; no agent creation, preset mount or occupied
worker; no Docker, PostgreSQL, SQL or transaction execution; no product source,
configuration, API/OpenAPI/GraphQL/schema/migration, route, feature flag,
allowlist, grammar, client or waiting-area change; no ordinary-practice
enablement or generic-status `Arrived`; no product, patient, appointment,
clinical, historical or protected data; no production runtime, deployment,
release, Pages or protected-ref movement.

Preserve `docs/branding/` and every unrelated untracked file. Stage only exact
paths; never use `git add .` or `git add -A`.
