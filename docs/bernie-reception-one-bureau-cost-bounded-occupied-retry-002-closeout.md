# Reception One Bureau cost-bounded occupied retry 002 closeout

Status: passed
Recorded: 2026-07-31

The real Reception One Bureau visibly selected the exact disposable
authored-synthetic appointment and verified `aria-selected=true` before one
isolated-planner submission. The corrected inner pre-call gate accepted the
exact Gemini 2.5 Flash, Bernie project/service-account, keyless impersonated
ADC and Sydney Vertex boundary.

Vertex returned HTTP 200 through
`australia-southeast1-aiplatform.googleapis.com` in 6,039 ms. Usage was 4,820
prompt, 236 response and 1,036 reasoning tokens. The deterministic proofreader
admitted `prepare_resize_proposal` with no violation or repair and atomically
released a 45-minute `proposeAppointmentUpdate` proposal requiring human
confirmation. No appointment was confirmed or written.

The retained receptionist response was:

> I can prepare a resize proposal for the requested appointment and duration
> for staff review. No booking was changed.

The separately audited operator note was not parsed into the typed plan or
delivered to product.

Exactly one provider call occurred. The one-use ledger is consumed, both audit
chains validate, the database before/after hashes are identical, and all
broker, cell, relay, image, internal-network, temporary-token, runtime-process,
port and disposable-database residue checks pass.

The cumulative account conservatively retains the earlier USD 0.02
preprovider reservation. Including the historical call and this successful
call, it totals USD 0.0284309 against the USD 1 ceiling. No provider call
followed admission.

The provider-free projection overflow repair also passes: result-heavy
desktop sheets grow to 756 px in the evidence viewport and later results remain
reachable through bounded internal scrolling while the request bar stays
visible.

This establishes one configured and observed Sydney locational request path
and typed proposal release. It does not establish Australian physical or
sovereign processing, production fitness, general reliability or authority
for real patient, health, clinical, product-derived or historical data.

Continuity graph revision 166 and Compass map revision 147 bind the accepted
result and passed preacceptance receipt. The rendered Compass is exact.
Focused Reception One, API Spine, cost, overflow, continuity and Compass
verification passes 100 tests; the repository-only Ariadne verifier passes
266 tests. JSON/schema validation, Python compilation, Ruff, JavaScript syntax
and `git diff --check` pass. A fresh read-only residue inspection independently
confirmed that the task-owned containers, images, network, runtime directory,
processes and disposable database are absent, both ports are available, the
single-use ledger is consumed and no cost reservation remains.
