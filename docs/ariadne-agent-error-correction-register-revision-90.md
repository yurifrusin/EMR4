# Ariadne agent-error register revision 90

Date: 2026-08-08

Status: disposable PostgreSQL parse/catalogue recovery completed; thirteen
bounded incident families corrected

Revision 90 adds AER-0096 through AER-0109 and brings the register to 109
bounded incidents. No incident is open.

## Review and containment corrections

AER-0096 and AER-0097 preserve two Gemini pass envelopes that Sol correctly
refused to admit. The first missed same-name catalogue-definition drift,
incomplete container readback and the aggregate deadline. The second called
retrospective `communicate()` size checks a bounded output collector. The
accepted harness now has exact value-bearing catalogue digests, full closed
containment assertions, an aggregate deadline with cleanup reserve, concurrent
hard-capped pipe draining and hostile coverage.

AER-0103 records the r54 Antigravity read-only violation: the verifier changed
`uv.lock`. That lane was isolated and rejected; a fresh exact-HEAD worktree
using fixed primary-venv commands passed cleanly. AER-0104 records two smaller
verifier test-accounting misreports. Exact pytest collection, rather than
narrative arithmetic, remained the acceptance authority.

## Server-discovered repository and harness corrections

AER-0098 records the two closed Docker Desktop readback variants for exact
absence and tmpfs projection. AER-0099 groups the four renderer
representability defects exposed only by real PostgreSQL parsing: composite
forward reference, SQL-alias catalog names, explicit modeled `xmin` and
interleaved foreign keys. AER-0100 closes authenticated readiness and exact
version-output admission. AER-0101 closes false rollback attribution by
requiring the fixed suffix line. AER-0102 closes the PL/pgSQL physical-symbol
gap for logical `primary`.

Every owned failed attempt remained evidence, invoked no application behavior,
and completed automatic exact-ID cleanup except the tmpfs-readback case, where
Sol first reverified exact ownership and containment, removed only that ID and
proved absence. The terminal attempt later reproduced all exact catalogue
digests and cleanup.

## Orchestrator controls

AER-0105 closes one JSON trailing comma and one invalid continuation-event
name; neither passed a receipt before correction. AER-0106 records two
pre-dispatch constraint-filter mistakes, both caught before review or runtime.
AER-0107 records the repeated guessed-HEAD preflight error and the separate
mistake of chaining receipt and verifier commands; r69 used literal Git output
and separate receipt generation, inspection and dispatch calls. AER-0108
records one invented test path; exact path discovery and the corrected 109-test
packet passed without repository mutation. AER-0109 records the closeout
updater's direct-script import mistake and two incomplete Continuity evidence
links; repository-module invocation and idempotent Compass validation now pass
at Continuity 232 / Compass 214.

## Register posture

These rows are workflow-improvement evidence only. They do not compare model or
provider quality and grant no provider, product, patient, clinical, operational
database, application-runtime, command, deployment, Pages or protected-ref
authority.
