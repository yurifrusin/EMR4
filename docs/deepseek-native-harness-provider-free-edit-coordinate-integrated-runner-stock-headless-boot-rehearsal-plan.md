# DeepSeek native Harness provider-free edit-coordinate integrated-runner stock-headless boot rehearsal plan

Date: 2026-08-22

Timestamp: 2026-08-22T17:21:28.4758726+10:00 (Australia/Brisbane)

Status: frozen for provider-free implementation

Operation: `deepseek-native-harness-provider-free-edit-coordinate-integrated-runner-stock-headless-boot-rehearsal`

Reasoning level: high

## Purpose

Prove that the exact accepted 14,077-byte edit-coordinate integrated runner can
be materialised and activated through the real rc.7 stock-headless/HMR loading
path, not merely imported by a fixture. The proof will stop at an intentionally
selected preset-root mismatch after the module and both typed edit-control
exports are loaded, but before `agents.create`, a session, turn, broker, model or
provider request.

This is a provider-free loading-path proof. It is not candidate/runbook adoption
and cannot become an occupied worker attempt.

## Accepted inputs

- integrated runner:
  `orchestration/continuity/deepseek-native-harness-provider-free-edit-coordinate-future-runner-integration-rehearsal/integrated-future-runner.mjs`,
  14,077 bytes and SHA-256
  `115cbf245ca6a2e218b2f2989093cea651bf4fe0aed796204dce1f83826e6be0`;
- its contract, deterministic evidence, accepted source and closeout;
- the accepted provider-free rebound future-runner stock-headless/HMR proof at
  source `560f471b72bef0b9790120238657dc7afd4d602b`;
- the accepted offline rc.7 package seed and stock-headless readiness sentinel;
- the existing network guard, exact effective-tool guard and typed pre-request
  controller primitives; and
- the active latch plus Yuri's standing uninterrupted-development authority.

All accepted predecessors remain byte-immutable.

## One disposable assembly

The controller creates one exact child beneath the existing worker-root parent.
It copies the already verified offline package seed without npm or network use,
then materialises an execution directory containing:

- the exact integrated runner;
- a minimal boot-probe adapter that imports that runner, proves
  `preflightEditArguments` and `classifyEditArgumentResult` are functions and
  delegates to its `apply` export without altering it;
- the accepted readiness sentinel and effective-tool guard; and
- initial and changed stock-headless HMR patches.

The canonical integrated runner is never edited. Execution copies must be
byte-identical and hash-equal.

## Frozen lifecycle

1. Deterministic preflight validates every source/package/seed digest, exact
   path, patch row and zero-request boundary.
2. The initial patch disables the ordinary headless runner and mounts only the
   readiness sentinel.
3. Exactly one native rc.7 process reaches
   `sentinel_activated` then `stock_headless_hmr_ready`.
4. The controller performs exactly one atomic HMR mutation adding the boot-probe
   adapter with the integrated runner and the exact injected services.
5. The adapter proves all three exports (`apply` plus both typed edit controls)
   and records only `integrated_edit_controls_loaded`.
6. Deliberately mismatched preset roots make the integrated runner stop at its
   `roots` stage before agent creation. Its exclusive generic terminal is
   accepted only when all request/tool/session/turn observations remain zero or
   unobserved as contracted.
7. The controller terminates the still-live native process once if necessary,
   proves process/root absence and only then publishes canonical evidence.

Stdout and stderr are discarded and never parsed or retained. Readiness,
control-load and runner-terminal files are the only admitted communication
surfaces.

## Required typed result

Acceptance releases one controller coordinate:
`integrated_runner_post_hmr_pre_request_hold`.

It requires:

- readiness order exact and one HMR mutation;
- integrated runner/adapter copy equality;
- `apply`, `preflightEditArguments` and `classifyEditArgumentResult` loaded;
- runner terminal status `failed`, failure stage `roots`, request count `0`, no
  tool call/result and no session/turn continuation;
- agent/session/turn/broker/model/provider/network/database/Docker counts zero;
- retry/resume/fallback counts zero; and
- process and disposable-root absence before evidence release.

Unknown files, fields, stages, coordinates, request activity or raw text fail
closed.

## Owned paths

- this plan and its threat-model delta;
- one operation controller under `scripts/`;
- one operation continuity root containing contract, schemas, boot-probe
  adapter, typed evidence and report;
- two focused operation tests;
- operation-specific Ariadne receipts and eventual closeout artifacts.

No accepted runner, package seed, prior controller, candidate, runbook or
product path is modified.

## Acceptance

The tranche passes only when all are true:

1. all accepted inputs and their full Git ancestry match exactly;
2. package-seed and disposable-copy file count, byte count, tree digest and
   zero-reparse-point readings match the accepted proof;
3. the boot-probe adapter is a closed import/export/delegate shim and contains
   no worker, request, fallback or result-reinterpretation logic;
4. the integrated runner execution copy equals the canonical accepted bytes;
5. exactly one native process, one readiness sequence and one HMR mutation
   occur;
6. the exact typed control-load record precedes the exact roots-stage terminal;
7. no agent, session, turn, target, broker/model/provider request or network
   attempt occurs;
8. stdout/stderr and raw errors are neither read nor retained;
9. process/root absence and package/canonical-source immutability pass before
   evidence publication; and
10. focused, inherited boot/HMR, latch, Baton, API Spine, Ruff, compilation and
    schema tests pass.

## Parallelism efficacy assessment

- DeepSeek: declined because the proof must stop before any worker/model/provider
  request.
- Gemini: reserved only if exact process, readiness, HMR, terminal and zero-gauge
  evidence leaves a material loading ambiguity.
- Native subagents: declined because developer policy prohibits delegation and
  the single mutable process is a serial custody boundary.
- GPT Sol owns plan, implementation, deterministic proof, recovery, acceptance,
  clockwork closeout and task-branch publication.
- Parallel work packages: none.

## Recovery

Any binding, package-seed, adapter, patch, readiness, HMR, terminal, counter,
process or cleanup mismatch rejects before canonical pass evidence. A failure
before native launch does not consume the one native attempt. A failure after
launch consumes it and grants no retry/resume/fallback absent a separately
recorded recovery boundary.

## Explicitly closed

No DeepSeek worker, model/provider request, candidate/runbook adoption, target
creation/use, product/API/schema/route/database/configuration change,
ordinary-practice enablement, feature flag, allowlist, generic-status `Arrived`,
action grammar, first-party client, waiting-area movement, product/patient/
appointment/clinical/historical/protected data, production runtime, deployment,
release, Pages, protected evidence or protected-ref movement. Local/origin
`master` and `handoff/current` remain exactly
`2e34bdad732fdab32fbf778280b3d3c70d66d602`. Preserve `docs/branding/` and
every unrelated untracked file. Use explicit-path staging only; never use
`git add .` or `git add -A`.
