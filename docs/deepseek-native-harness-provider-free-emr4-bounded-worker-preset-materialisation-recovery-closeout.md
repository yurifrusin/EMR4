# DeepSeek native Harness provider-free emr4-bounded-worker preset materialisation recovery closeout

Date: 2026-08-20

Timestamp: 2026-08-20T10:52:45.8367551+10:00 (Australia/Brisbane)

Status: `accepted`

Exact reviewed candidate:
`962e91d9af90fbe977177d251f9f3e3134aaad19`

The accepted result materialises the missing `emr4-bounded-worker` preset as
an exact 158-byte UTF-8/LF payload at the future Harness-home relative path
`.agent-presets/emr4-bounded-worker/agent.cordis.yml`. Six exact rc.7 packages
were resolved only from the owned local npm cache and verified by registry
identity, tar digest and retained member digest without npm, Node, extraction
to disk, registry access or external network.

The preset deliberately contains the official filesystem and filesystem-search
rows. Those plugins inherit a broader raw surface: `edit`, `glob`, `grep`,
`read` and `write`, plus conditional `read_image`. The preset is therefore not
an authority boundary. Symbolic application of the already accepted post-mount
guard reduces both admitted attachment postures to exactly sorted `edit`,
`glob`, `read`; the outer broker allowlist remains independently mandatory.
Twenty-one hostile payload, path and mapping variants fail closed.

The deterministic candidate passed 24 focused tests and the exact isolated
review manifest passed 101 provider-free tests. One fresh Gemini 3.7 Flash/high
review then passed all ten prescribed commands with no P0-P2 finding, exact
candidate HEAD before and after, and a clean worktree. No semantic candidate
correction or verifier retry was required after commit.

The efficacy record retains the actual mechanical cost: two malformed
read-only inspection invocations, one test-assertion narrowing, two pre-write
source-token alignments, one test rerun whose first terminal summary was lost,
one pre-verifier schema correction and one commentary byte-count correction.
None changed the accepted claim, caused a provider/native retry or qualifies
for the incident register.

No Node or native Harness process, worker, agent, session, turn, broker,
DeepSeek model, provider, network, Docker or database action occurred. No
product, API, client, feature flag, allowlist, grammar, ordinary-practice,
waiting-area, data, production, deployment, release, Pages or protected-ref
boundary changed.

The next eligible tranche is one separately frozen provider-free native
composition boot. It may combine the accepted required-service declaration,
this exact preset payload, preterminal observability and the accepted guard in
exactly one network-denied process, observe the effective `edit`, `glob`,
`read` schema, emit one sanitized terminal and clean up. It does not authorise
an agent, session, turn, broker, DeepSeek model or provider request.
