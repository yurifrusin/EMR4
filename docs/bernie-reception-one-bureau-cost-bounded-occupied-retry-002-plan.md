# Reception One Bureau cost-bounded occupied retry 002 plan

Status: authorised and active
Recorded: 2026-07-31
Predecessor: `reception-one-bureau-cost-bounded-occupied-retry`

## Objective

Run one distinct corrected occupied Bureau dialogue after the predecessor
failed before provider transport. The visible browser must select the exact
authored-synthetic appointment, the inner v6.8 pre-call gate must accept the
exact authority, and only a proofreader-admitted 45-minute update proposal may
return.

## Exact repair

The predecessor authority remains immutable. This successor adds only the two
legacy compatibility fields required by the existing inner gate:

- `decision=authorised_by_yuri`; and
- `requested_exact_boundary`, repeating the exact frozen provider, model,
  project, Bernie service account, keyless ADC, Sydney hostname and all closed
  capability flags.

A focused test invokes the actual inner `_precall_gate` with the successor
authority and preflight. The occupied harness now also preserves an allowlisted
local failure code before attempting runtime-audit validation of an HTTP error.
No prompt, response, exception text or credential material may enter that
diagnostic.

## Cost continuation

The predecessor cumulative ledger is immutable and terminal. Although the
diagnosis proves zero provider calls, this successor claims no cost credit and
begins at the full conservative amount:

- original HTTP 200 estimate: USD 0.0038049;
- predecessor failed-attempt reservation: USD 0.02;
- successor opening account: USD 0.0238049; and
- successor reservation: USD 0.02.

The maximum before the call is therefore USD 0.0438049, below the unchanged
USD 1 ceiling. The successor ledger must hash-bind the predecessor terminal
cost hash and record that the carried reservation was not refunded.

## Frozen lane

- provider: Google Cloud Vertex AI;
- model: `gemini-2.5-flash`;
- project: `bernie-emr4-dev`;
- service account:
  `emr4-bernie-ai-dev@bernie-emr4-dev.iam.gserviceaccount.com`;
- authentication: `keyless_impersonated_service_account_adc`;
- region: `australia-southeast1`;
- hostname: `australia-southeast1-aiplatform.googleapis.com`;
- authored-synthetic data only;
- credential-free cell and broker-only ADC;
- deterministic proofreader as sole egress;
- one provider call in this dialogue;
- no provider, model, identity, project, region, endpoint or deterministic
  fallback;
- no API key, static key, provider tool, grounding, retrieval or cache
  creation; and
- no confirmation, write, product delivery, production, deployment or
  release.

## Gates and stop conditions

Before the call require a distinct authority, cost policy, cost ledger,
attempt/ledger identifiers, output directory and disposable database; fresh
provider-blocked, ADC, residue and five-source receipts; the inherited real
isolation pass; the corrected projection overflow pass; focused/API Spine,
repository-only Ariadne, Continuity/Compass, schema, syntax and static checks.

Stop after the first admitted proposal. Fail closed and request intervention
only if progress would require an external IAM, ADC, API, billing, provider,
model, identity, project, region, data-class, cost, isolation, audit,
confirmation, write, production, deployment or release change.

The evidence may establish only the configured and observed Sydney locational
request path, never Australian physical or sovereign processing.
