# Reception One Receptionist-first v6.8 Closeout

Status: complete; occupied cohort passed at 24/24 exact outcomes
Recorded: 2026-07-30

## Result

The complete twenty-four-request authored-synthetic regression ran through the
exact bounded Sydney Vertex lane. All twenty-four terminal results matched
their frozen deterministic oracle on the primary call. The tranche result is
`reception_one_receptionist_first_v68_full_cohort_pass`.

All twenty-four opened ledgers are consumed, every turn and case audit chain
verifies, no call followed success and task-scoped runtime residue is clear.

## What passed

- The sole v6.7 mismatch, `b-move-shift`, passed on its primary call.
- Its typed move form was exact and its natural response satisfied the
  unchanged frozen move-vocabulary proofreader.
- All create, move, resize, cancel, status, squeeze-in and clarification cases
  matched their frozen oracle.
- No case used its correction turn.
- Every admitted value remained an in-memory authored-synthetic typed
  proposal, assessment, status proposal or clarification behind the human
  gate; no product delivery or write occurred.

## Calls and controls

- 24 primary calls and 0 terminal second calls; 24 total against a ceiling of
  48;
- all 24 provider responses completed with HTTP 200;
- 111,962 prompt, 12,260 visible candidate, 16,428 thinking and 140,650 total
  tokens reported by Vertex;
- exact `gemini-2.5-flash`, `bernie-emr4-dev`, Bernie impersonated service
  account, `australia-southeast1` and regional hostname throughout;
- no API key, service-account key, global endpoint, fallback, provider tool,
  grounding, retrieval, cache creation, product/database access, write or
  delivery; and
- raw prompts, raw provider responses, credentials, API-key information and
  hidden chain-of-thought were not retained.

## Next evidence

The v6.8 prompt, typed form and proofreader should now be frozen. A future
evaluation should use an untouched authored-synthetic holdout rather than
claiming independent generalisation from this repeatedly observed development
cohort. Product/database context, real data, appointment writes, voice, Word,
production, deployment and release remain separately closed.

## Claim limit

This evidence proves twenty-four exact safe outcomes over a reused
authored-synthetic development cohort through the configured and observed
Sydney locational path. It does not prove independent generalisation,
production fitness, Australian physical or sovereign processing, or safety
for real, product-derived, patient, health, clinical, protected or historical
data.
