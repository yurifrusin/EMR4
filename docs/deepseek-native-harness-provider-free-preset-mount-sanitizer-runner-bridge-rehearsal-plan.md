# DeepSeek native Harness preset-mount sanitizer runner-bridge rehearsal plan

Date: 2026-08-22

Timestamp: 2026-08-22T06:03:42.6115665+10:00 (Australia/Brisbane)

Status: **frozen before implementation**

Reasoning level: **Extra High** for the first runner connection of the admitted
preset-mount sanitizer.

## Objective

Derive one provider-free runner descendant that imports the exact admitted
`sanitizePresetMountError` implementation once and invokes it only at the
accepted preset-mount failure boundary. Prove by deterministic source and
fixture checks that the bridge emits only the closed preset-mount terminal
grammar and retains no dynamic detail.

## Accepted basis

The Windows minimum-environment recovery admits the pure sanitizer at exact
execution candidate `ceac8b2600530bf858394bb84e66a42ec3d016f4`. The earlier
native safe-terminal bridge reached the real private factory but classified
the mount failure only as
`EFFECTIVE_TOOL_COMPOSITION_PRESET_MOUNT_FAILED`. Source reconciliation then
identified the narrow source-reachable mount subcoordinates. This tranche
connects those facts without running the native Harness.

## Frozen implementation

- Preserve the admitted sanitizer bytes and SHA-256 exactly.
- Derive from the accepted preset-composition safe-terminal runner generator;
  do not edit an unbound or ambient runner.
- Import `sanitizePresetMountError` exactly once within the guarded local
  composition boundary.
- Catch the exact `await presets.mount(...)` failure boundary once and pass only
  the caught value plus the resolved `PresetMountError` constructor to the
  sanitizer.
- Emit a typed sidecar terminal containing only `stage: preset_mount`, one of
  the seven admitted codes and `detail: null`.
- Retain the accepted broader fallback only for failures outside that exact
  boundary; the two grammars must be disjoint and precedence-tested.
- Prove import count, call count, catch location, closed vocabulary, null detail,
  no raw message/stack/cause/path/stream/environment, and unchanged provider/
  product zero counters through Python and authored-synthetic Node fixtures.

## Process and authority budget

No DSH import and no native Harness process are authorised. Deterministic local
Node fixtures may exercise only the pure generated bridge and sanitizer, with
an exact process count frozen before execution. The tranche cannot launch a
worker, model or provider request. Any later native run requires a new plan,
new latch and fresh preexecution receipt.

## Parallelism assessment

- DeepSeek lane: **declined**. The runner connection governing DeepSeek is the
  object under construction; worker use would be circular.
- Gemini lane: **declined provisionally**. The exact source anchors and closed
  terminal vector are deterministic; reassess if implementation creates a
  genuine semantic ambiguity.
- Native-subagent lane: **declined**. Developer policy prohibits delegation and
  the bridge has one serial source owner.
- GPT Sol owns implementation, deterministic execution, review and closeout.

## Acceptance

Accept only an exact-hash sanitizer connection at one exact mount catch, a
finite typed terminal with null detail, no raw runtime material, no DSH/native
Harness process, no worker/model/provider activity, no target, and passing
deterministic tests. Ambiguous catch coverage, overlapping grammars, any dynamic
detail or any native process fails closed.

## Explicit exclusions

No native Harness execution, occupied worker, model/provider request, retry,
repair selection, target edit, product/configuration/API/database/route/adapter/
feature-flag/allowlist/action-grammar/first-party-client/waiting-area change,
ordinary-practice enablement, generic-status `Arrived` change, patient/product/
clinical/historical/protected data, production, deployment, release, Pages,
protected evidence or protected-ref movement is authorised.
