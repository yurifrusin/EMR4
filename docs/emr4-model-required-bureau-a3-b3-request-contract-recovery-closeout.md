# EMR4 model-required Bureau A3/B3 request-contract recovery closeout

Date: 2026-08-04

Status: accepted occupied authored-synthetic advisory rehearsal

Terminal result:
`model_required_bureau_a3_b3_request_contract_recovery_pass`

Source HEAD exercised:
`a70d06fd047733bac9a72921d0fd2f81e1b946db`

## Outcome

The bounded request-contract descendant repaired the previous pre-parser
terminal rejection without reopening or overwriting its historical evidence.
The exact Vertex AI `gemini-2.5-flash` lane used project `bernie-emr4-dev`, the
named Bernie service account through existing keyless impersonated ADC, and the
`australia-southeast1` regional endpoint with newly authored synthetic context.

The request preserved the parent prompt, response schema, temperature and
candidate count while setting `thinkingBudget: 1024` and
`maxOutputTokens: 2048`. Rayleen admitted first; Davida remained closed until
that deterministic release, then admitted second. Both lanes returned HTTP
200, one `STOP` candidate with one non-thought text part, valid closed JSON and
one deterministic proofreader admission. No correction or retry call occurred.

| Lane | Calls | Latency | Prompt | Candidate | Reported thinking | Total |
|---|---:|---:|---:|---:|---:|---:|
| Rayleen A3 | 1 | 5,967 ms | 1,369 | 204 | 1,070 | 2,643 |
| Davida B3 | 1 | 3,089 ms | 901 | 138 | 539 | 1,578 |

The provider-reported thinking counts are safe usage metadata. They demonstrate
that the accepted path exercised positive model reasoning; they do not change
the deterministic authority boundary or prove a retrospective cause for the
historical rejection.

## Accounting, isolation and cleanup

- exactly two of four allowed calls were reserved and consumed;
- reserved cost was USD 0.50 inside the USD 1 ceiling;
- both per-lane ledgers and the parent ledger are consumed;
- fallback remained disabled;
- raw prompt, raw provider response, provider text, thought content, headers,
  credentials and tokens were not retained;
- both one-shot cells used the distinct recovery namespace, read-only roots,
  no mounts or ambient credentials, bounded resources and internal networks;
- relay/cell containers, images, internal networks, broker processes and
  temporary contexts are absent; no daemon-wide prune occurred; and
- patient/clinical/product data, product/database reads, commands, writes,
  actuators, cloud/IAM mutation, deployment and protected-ref movement were all
  zero.

## Verification

Fresh Gemini 3.6 Flash/high Review 2 returned one exact terminal `pass`, zero
findings and a clean unchanged worktree at the exercised source HEAD. The
occupied acceptance now verifies the review, request diff, preflights, safe
metadata, exact Rayleen-before-Davida order, proofreader releases, audit hash
chains, single-use ledgers, cost ceiling, cleanup and zero-authority counters.

AER-0020 is corrected by the fresh exact-single-decision review under register
revision 14. Its rejected predecessor envelope remains immutable.

## Claim boundary and next gate

This result proves two bounded, proofreader-admitted, authored-synthetic
occupied advisory rehearsals through the configured and observed Sydney
regional request path. It does not prove Australian physical or sovereign
processing, production suitability, patient/clinical-data safety, product
runtime behavior, a command, a write, an actuator or a clinical decision.

The current planned block is complete. A4 product read/UI projection, A5/B4
writes, C4 actuator rehearsal and actual update/import/activation each remain a
separate material boundary. Deployment, production, release, Pages, protected
refs and protected evidence remain closed.
