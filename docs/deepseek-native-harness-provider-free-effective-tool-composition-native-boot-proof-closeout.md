# DeepSeek native Harness provider-free effective-tool composition native-boot proof closeout

Date: 2026-08-20

Timestamp: 2026-08-20T05:47:54.6367968+10:00 (Australia/Brisbane)

Status: accepted fail-closed result; native composition proof not accepted

## Outcome

The frozen controller admitted exactly one offline, network-denied rc.7 native
Harness process as `native-composition-attempt-001`. It verified the exact
cached package and predecessor identities, completed offline materialisation,
started the stock headless profile, observed the in-process sentinel and both
stock HMR watches, then applied the changed patch.

The attempt did not retain `effective_tool_guard_started` or any guard terminal.
The controller closed the attempt as `NATIVE_COMPOSITION_EXECUTION_FAILED`,
terminated and waited for the owned process, removed its exact disposable root
and retained no raw environment or log body. The effective `edit`, `glob`,
`read` view was therefore not proved. The attempt is immutable and will not be
retried, resumed or reclassified.

## Exact retained reading

- deterministic candidate:
  `b8331387120ade3634e73ba799f4a9fb48389f5b`;
- admitted execution receipt descendant:
  `95a985224bec7af6ad09cf2a6d6b8366f43eee96`;
- immutable failed-evidence descendant:
  `296f35ff696361c63a4e018e5ae9317c79cb6213`;
- package materialisation: exit `0`, `115532 ms`, six required installed
  packages all exact `0.1.0-rc.7`;
- retained lifecycle: `sentinel_activated`,
  `stock_headless_hmr_ready`;
- terminal: absent;
- network, agent-session, turn, broker, model, provider, occupied-worker,
  Docker and database counts: all zero;
- process absence and disposable-root absence: both true.

The evidence records `duration_ms: 0` because the controller assigned launch
duration only after its polling/terminal path completed. That value is not a
credible elapsed-time reading for the started process. The evidence also
collapses the controller exception to a generic closed coordinate. With raw
logs correctly destroyed and no bounded preterminal diagnostic retained, the
exact cause cannot honestly be attributed to package import, HMR activation,
service injection, event parsing or the guard itself.

## Acceptance and non-acceptance

Accepted:

- the plan, exact cache/source binding, one-run latch and deterministic
  controller candidate;
- 13 focused tests and the 139-test neighbouring Harness/broker/no-database/
  latch/clockwork/baton suite; the provider-free closeout wrapper required two
  recorded test-only reruns after its intentional environment scrub removed
  both `LOCALAPPDATA` and home discovery, and the corrected test derives the
  explicit cache root from the repository parent;
- the immutable failed attempt, zero prohibited calls and complete cleanup;
- the conclusion that preterminal observability is insufficient and must be
  repaired before any separately named recovery execution.

Not accepted:

- no effective-tool composition result;
- no successful safe terminal, exit-zero native path or occupied worker;
- no conclusion about DeepSeek reasoning quality, coding quality or provider
  reliability;
- no same-attempt retry and no automatic new native process.

Gemini was reserved only for a passing exact native terminal and was therefore
not eligible. DeepSeek remained the provider-free subject and received no
model request. Native subagents remained declined under current policy and the
serial lifecycle lease.

## Successor

Continue to
`deepseek-native-harness-provider-free-preterminal-activation-observability-recovery`.
That tranche is deterministic and provider-free first. It must preserve this
failed attempt, identify every post-readiness preterminal branch that can avoid
a terminal, add exact closed controller coordinates plus reliable duration and
event capture, and prove module/load/service/guard activation shapes without a
native Harness boot. A later process can be considered only as a separately
frozen successor with a new attempt identity; it is never a retry of this one.

No product, API, configuration, ordinary-practice, feature-flag, allowlist,
action-grammar, first-party-client, waiting-area, data, Docker/database,
production, deployment, release, Pages or protected-ref authority changed.
Local/origin `master` and `handoff/current` remain exact
`2e34bdad732fdab32fbf778280b3d3c70d66d602`. All unrelated untracked files,
including `docs/branding/`, remain preserved.
