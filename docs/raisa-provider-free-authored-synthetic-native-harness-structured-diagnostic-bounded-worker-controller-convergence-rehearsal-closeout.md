# Raisa provider-free authored-synthetic native Harness structured diagnostic bounded-worker controller convergence rehearsal closeout

Date: 2026-08-21

Status: `accepted`

Exact implementation candidate:
`ba2e8b1c06acfe88f9f11afa1a58c1371d0cfa3c`

Acceptance binding HEAD:
`cd58628310478bede424fbe764e03d54efa2b5dd`

## Result

The provider-free convergence passes. A new descendant controller adapter binds
the accepted authored-synthetic task vocabulary to the canonical rc.7 wrapper,
exact launch command, structured sidecar reader, v2 terminal builder and safe
v1/v2 terminal writer. The consumed controller file and attempts 001-003 remain
byte-identical.

Valid canonical sidecar evidence with the exact operation, attempt and full Git
identity selects v2 `structured_entrypoint_import_rejected`. An absent sidecar
retains v1 and returns only `structured_diagnostic_absent`; malformed,
noncanonical, wrong-identity and escaped sidecars retain v1 and return only
`structured_diagnostic_invalid`. Each fallback is explicitly ineligible for
future occupied acceptance.

The lifecycle envelope requires one wrapper build, one future process launch,
sidecar selection before safe terminal persistence, safe persistence before
owned-process termination and exact disposable-root removal last. The adapter
contains no process launcher, provider key or retry path.

## Verification

The focused controller, native-boot and structured-seam allowlist passes 43
tests. The deterministic `--check`, Ruff, Python compilation, JSON/schema
validation and diff hygiene pass. The proof created only disposable local
fixtures: Node, Harness, broker, worker, session, prompt, tool, model, provider
and network counts are all zero, and the fixture root is absent.

Two older startup-terminal tests remain red on the unchanged incoming branch
because their historical validator compares recorded source hashes with later
mutable working-tree controller/test files. The adapter did not create that
drift; its first direct-edit design exposed the coupling and was reverted. The
next provider-free repair is therefore to bind that validator to the immutable
Git snapshot it actually describes before any fresh occupied attempt.

## Boundaries

This result proves controller composition only. It authorises no occupied
attempt, retry, resume or reclassification; no raw historical stream read; no
product source/configuration, API, database, route, adapter, flag, allowlist,
grammar, client or waiting-area change; no ordinary-practice enablement or
generic-status `Arrived`; no product, patient, appointment, clinical,
historical or protected data; and no production runtime, deployment, release,
Pages or protected-ref movement.

## Workflow observations

AER-0750 records the caught direct-edit/historical-binding conflict and the
descendant-adapter correction. AER-0751 records the pre-verifier receipt run
before task-branch push and its clean rerun after alignment. AER-0752 records
the first read-only clockwork rejection of an overlong incident tranche label
and its bounded-vocabulary correction. All were contained without occupied or
provider activity.
