# DeepSeek native Harness exact-tool-view recovery and second monitored development admission closeout

Date: 2026-08-18

Timestamp: 2026-08-18T20:37:23.2818490+10:00 (Australia/Brisbane)

Status: accepted provider-free exact-tool-view proof and attributable pre-provider negative worker evidence; no candidate admitted

Reasoning level: high

Accepted evidence source: `00d4f8d6065ab09b5faf5501c979edd2fa59943c`

Result:
`provider_free_exact_tool_view_passed_occupied_headless_hmr_boot_failed_zero_provider_zero_candidate`

## Outcome

The exact pinned `@deepseek-ai/dsh@0.1.0-rc.7` package-native restriction is
now proven. The preset is mounted inside `agents.create({setup})`, then
`ctx.tools.restrict({allow: ["read", "glob", "edit"]})` removes every
unselected schema before request composition. A fresh provider-free isolated
request exposed exactly sorted `edit`, `glob`, `read`, with one local request
and zero external/provider calls. The normalized profile contract, schema,
evidence and deterministic tests preserve that result.

The one conditionally admitted occupied worker then terminaled with exit 1 in
under one second while the stock headless profile applied its HMR service.
rc.7 HMR requires Node `--expose-internals`; the package-declared CLI launcher
did not supply it. The custom runner, exact-tool-view runtime check, Harness
session and provider boundary were never reached. The broker recorded only its
ready event: zero requests, zero provider calls and zero responses. The sparse
worktree stayed clean and neither owned output path was created.

The occupied attempt is consumed without retry or resume. This accepts the
exact provider-free tool-view proof and the attributable startup failure, not
an EMR4 candidate or unrestricted Harness promotion.

## Controls and cleanup

AER-0473 through AER-0515 preserve the bounded recovery, predispatch,
occupied-terminal and closeout corrections. Register revision 445 contains
515 incidents, all corrected or explicitly contained and none open. Its
complete register/latch packet passes.

The worker and broker containers, package/home/capability volumes, internal
network, sparse worktree and worker branch are absent. The exact disposable
root is in the Windows Recycle Bin and is recoverable until that bin is
emptied. No raw prompt, reasoning, response, tool payload, credential,
environment dump, package cache or session transcript was committed.

## Parallelism closeout

- DeepSeek native-Harness lane: completed with mixed leverage. The provider-
  free exact-tool proof passed; the occupied implementation lane failed at a
  deterministic pre-session HMR prerequisite with zero provider cost and no
  candidate.
- Gemini lane: declined with neutral leverage. No candidate existed to veto;
  the terminal, broker, Git and cleanup evidence was deterministic.
- Native-subagent lane: declined with neutral leverage. Current developer
  policy prohibits proactive delegation and the enclosure was serial.

Sol alone admits the result.

## Protected boundaries

No product, patient, clinical, historical Diary or protected data was used.
No application source, route, API, database, status, grammar, client,
waiting-area, ordinary-practice, live runtime, deployment, release, Pages or
protected ref changed. `docs/branding/` and every unrelated untracked file
remain preserved. Local/origin `master` and `handoff/current` remain exact
`2e34bdad732fdab32fbf778280b3d3c70d66d602`.

The usual non-PHI Pushover closeout notification succeeded with request
`2585170d-71b3-40ee-a8bd-777da80a6d58`.

## Next tranche

The default-off canonical check-in route-adapter convergence already passed at
reviewed source `c82c3a741053a9c8da260aa62e1a968af22bb54e` and must not be
repeated. Next is
`raisa-provider-free-read-only-ordinary-practice-canonical-check-in-admission-readiness-review`.
It may inventory the current default-off feature flag and empty ordinary-
practice posture plus the API Spine command, authorization, tenant,
idempotency, audit, rollback, rollout and observability prerequisites. It may
not enable a practice, edit product code or configuration, exercise a live
route or use product data. The native Harness is not eligible for occupied work
in that tranche; a future DeepSeek worker first requires a separately frozen
provider-free stock-headless-to-custom-runner HMR startup proof.
