# DeepSeek native Harness agent-factory closed-subcoordinate diagnostic closeout

Date: 2026-08-22

Timestamp: 2026-08-22T03:42:19.3682524+10:00 (Australia/Brisbane)

Status: **accepted diagnostic pass; native attempt consumed; no retry**

## Result

The single provider-free rc.7 diagnostic attempt passed its attribution
contract at exact execution source
`33b4e061b1385abc91ecd170e4abdb563396c3ef`.

The process reached exact stock-headless readiness, accepted one HMR mutation,
loaded the package dependencies inside the runner's guarded transaction,
resolved the required services and preset roots/roster, proved the registries
empty, constructed the fixed session identity, invoked the real factory once,
entered setup and admitted the private Agent/Session identity.

It then emitted one valid `closed_subcoordinate_failure` sidecar at
`private_identity_admitted` with `unclassified_error`, before
`preset_composition_admitted`. The failure interval is therefore limited to
the accepted effective-tool guard call or its exact-result validation. The
internal guard coordinate is not yet observed.

This is a diagnostic pass, not an agent-creation or worker-readiness pass.

## Containment

- Native processes / retries / resumes: `1 / 0 / 0`.
- Exit: code `2`, `self_exited_after_typed_sidecar`, 962 ms native duration.
- Factory invocations / private Agent / private Session: `1 / 1 / 1`.
- Published Agent / Session / lifecycle announcements: `0 / 0 / 0`.
- Turns, requests, broker processes/requests, workers, model/provider requests,
  network, database, Docker and target use: all zero.
- The target remained absent, bundle manifest and package seed were unchanged,
  and the process plus disposable root were absent before evidence publication.
- No raw stream, error message, stack, code path, cause, prompt, response,
  session content, path or credential was retained.

## Verification

The 14 focused contract/controller tests and 8 immutable-evidence tests pass.
The exact 219-test focused-plus-inherited native-Harness collection previously
completed without failure; Ruff, compilation, deterministic package/source
checking and Git whitespace also pass. The clockwork command manifest repeats
the risk-proportional focused, inherited and governance gates before canonical
publication.

## Metadata correction

The generated Markdown report used a guessed static timestamp that was still
in the future at attempt completion. Its exact bytes remain preserved, but the
timestamp field is rejected by
`generated-report-metadata-rejection.json`. The machine-recorded launch time
and all terminal/counter/cleanup claims remain authoritative. AER-0868 adds the
creation-clock-or-typed-evidence timestamp rule; no native retry was made.

The first prospective closeout also routed ordinary file paths through the
graph's structured `contract_evidence` field. The clockwork rejected before
commands or publication. AER-0869 keeps those paths in the artifact inventory
and reserves `contract_evidence` for schema-valid structured objects.

The second prospective closeout confused the register's per-transaction
revision with its per-observation incident population. The clockwork again
rejected before commands or publication. AER-0870 records the corrected
reading: canonical revision 601 plus this one transaction gives revision 602,
while the three observations give incident population 870 and latest incident
AER-0870.

The first combined provider-free focused/evidence run then exposed that the
selected wrapper removed `LOCALAPPDATA`, which the hash-bound deterministic
test requires to locate the accepted cache; no native process was created.
AER-0871 moves the correction to the no-conftest verifier invocation and
preserves the accepted execution source unchanged.

That first correction itself touched the focused test bound by
`implementation_bytes`, and the deterministic source rejected it before any
native process was created. AER-0872 restores the test byte-for-byte and moves
the verification-environment correction to the mutable no-conftest closeout
command. Revision 602 therefore carries incident population 872 and latest
incident AER-0872.

The third prospective clockwork check then rejected two descriptive incident
categories outside the register's closed vocabulary. AER-0873 replaces both
with the admitted `operator_error` coordinate and makes runtime-map lookup the
selection rule. Revision 602 therefore carries incident population 873 and
latest incident AER-0873.

## Parallelism efficacy

DeepSeek was correctly declined as a worker because its native factory and
preset composition were the diagnostic objects. Gemini had no missing runtime
fact to reconstruct and no semantic ambiguity after the typed readback. Native
subagents remained prohibited. GPT Sol owned the serial implementation,
process, readback, correction and closeout.

## Successor

The frozen successor is
`deepseek-native-harness-provider-free-preset-composition-safe-terminal-bridge-rehearsal`.
It connects the accepted guard's existing sanitizer to the runner sidecar and
may run one distinct provider-free process. It can expose only one of nine safe
guard coordinates or proceed to the already-frozen publication veto. It is not
a retry and cannot correct itself in the occupied attempt.

No worker turn, model/provider request, target change, product/configuration/
API/database/route/adapter/flag/allowlist/grammar/client/waiting-area change,
ordinary-practice or generic `Arrived` change, data, production, deployment,
release, Pages, protected evidence or protected-ref movement is authorised or
claimed.
