# DeepSeek native Harness provider-free integrated-runner accepted guard-graph materialization recovery plan

Date: 2026-08-22

Timestamp: 2026-08-22T19:37:15.5000864+10:00 (Australia/Brisbane)

Status: frozen for one provider-free installed-registry fixture

Operation: `deepseek-native-harness-provider-free-integrated-runner-accepted-guard-graph-materialization-recovery`

Reasoning level: high

## Purpose

Remove the exact occupied runner/guard interface mismatch dynamically proved by
the accepted import-path recovery. The operation keeps the 14,210-byte
integrated runner byte-identical and replaces only its adjacent old guard with
the already accepted four-argument guard, bridge and sanitizer graph.

This is materialization of accepted bytes, not a new guard design, runner
rewrite or diagnostic taxonomy.

## Exact accepted source inventory

The controller must machine-bind and materialize exactly:

- integrated runner: 14,210 bytes, SHA-256
  `017394e3f86a3efdf5eba0745c254a8b561615fb6ab923978b81bb5941e8e3f4`;
- accepted four-argument guard: 4,501 bytes, SHA-256
  `76029da0f9c030651fd10c0df16f4e75e86b2269d7560af7f94c74680f8598b9`;
- accepted bridge: 1,661 bytes, SHA-256
  `3a49b28174eeefd77d7efe0a00498901ac6636b637ed9dfe60aba46980df1d0b`;
  and
- accepted sanitizer: 2,439 bytes, SHA-256
  `12552925a600dc951afc30b9a738746499c7e2f4cefc9962bc05fb06780f158f`.

The guard must occupy `effective-tool-guard.mjs`; the bridge and sanitizer must
occupy their source-owned relative targets. The occupied old 4,009-byte guard
is neither rewritten nor reused in the corrected materialization.

## Complete import closure

Before process admission, parse every static and literal dynamic import in the
runner, guard, bridge, sanitizer and fixture. Every relative specifier must
resolve one-to-one inside the five-file module inventory. Every admitted
`@deepseek-ai/*` bare import must resolve to an existing installed rc.7 package
entry beneath exact `node_modules/@deepseek-ai`. Only the exact `node:crypto`
and `node:fs` builtins are admitted.

Absolute, file-URL, HTTP(S), unknown bare, nonliteral dynamic, CommonJS,
escaping, missing, duplicate or surplus edges reject before Node and consume no
process.

## One bounded installed-registry fixture

After all source, schema and import-closure gates pass, the controller may
persist attempt `accepted-guard-graph-materialization-001` and start exactly
one provider-free Node process. It imports installed rc.7 Cordis,
`AgentRegistry` and `createScope`, then imports the byte-identical runner and
accepted graph.

The inert factory supplies one real scoped Cordis agent context, one exact
three-tool view, one root preset-service mount and no agent loop. It invokes the
runner setup callback once. If setup completes the accepted guard, restriction
and schema checks, the factory records
`EFFECTIVE_TOOL_COMPOSITION_PASSED` and deliberately throws one controlled
post-guard sentinel before agent publication, target resolution or a model
request.

## Exact acceptance

The sole passing result is
`accepted_guard_graph_passed_old_factory_coordinate`. It requires:

- exact materialized source hashes and complete import closure;
- one installed `AgentRegistry.create` transit and one setup invocation;
- setup resolved through the accepted graph;
- coordinate `EFFECTIVE_TOOL_COMPOSITION_PASSED` and explicit absence of
  `EFFECTIVE_TOOL_COMPOSITION_INPUT_INVALID`;
- one preset mount, one tool view, one restriction and one schema view;
- generic runner terminal `failed` at `factory` only because of the controlled
  post-guard sentinel;
- zero request, tool-result, turn, agent publication, target, broker, worker,
  model, provider, network, database and Docker activity;
- zero retry, resume and fallback; and
- process, Cordis scope and disposable root absent after cleanup.

Raw stdout and stderr are reduced to byte counts and SHA-256 digests before
semantic admission and are never retained. Any other coordinate, terminal,
counter, source, schema or cleanup state is an immutable bounded negative and
stops without retry.

## Decision after terminal

A pass accepts the corrected materializer for the next separately frozen
provider-free stock-headless/custom-runner boot proof. It does not authorize a
DeepSeek turn. A non-pass selects no new correction and closes on the exact
observed terminal.

## Owned paths

- this plan and its threat-model delta;
- one operation controller under `scripts/`;
- one focused test under `tests/`;
- one operation continuity root containing contract, schemas, terminal
  evidence and report; and
- operation-specific Ariadne receipts and closeout artifacts.

Accepted predecessor sources and evidence, consumed attempt identities,
product source and protected refs remain immutable.

## Parallelism efficacy assessment

- DeepSeek: declined because every worker, native-Harness, model and provider
  request is forbidden.
- Gemini: declined because the operation reuses an already reviewed immutable
  correction graph and its provider-free boundary forbids model review.
- Native subagents: declined under current developer policy and because graph
  binding, import closure and the one process form one serial attribution
  chain.
- GPT Sol owns exact materialization, the one process, deterministic
  verification, acceptance and closeout.
- Parallel work packages: none.

## Recovery

Pre-process binding, schema, source or import-closure rejection may be corrected
mechanically because it consumes no process. Once Node starts, attempt 001 is
consumed. There is no retry, resume, fallback, second fixture or native Harness
launch in this tranche.

## Explicitly closed

No native Harness process, DeepSeek worker/turn, broker, model/provider request,
runner mutation, new guard/bridge/sanitizer design, product/configuration/API/
database/route/adapter/flag/allowlist/grammar/client/waiting-area change,
ordinary-practice enablement, generic-status `Arrived` change, patient/product/
appointment/clinical/historical/protected data, production runtime, deployment,
release, Pages, protected evidence or protected-ref movement. Local/origin
`master` and `handoff/current` remain exactly
`2e34bdad732fdab32fbf778280b3d3c70d66d602`. Preserve `docs/branding/` and all
unrelated untracked files. Stage explicit paths only; never use `git add .` or
`git add -A`.
