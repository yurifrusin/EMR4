# Closeout: provider-free native-Diary application-session practitioner runtime

Date: 2026-08-03

Result: `provider_free_native_diary_application_session_practitioner_runtime_pass`

## Accepted result

The bounded Diary implementation is accepted as an unmounted, default-off,
provider-free application-session composition for the existing active-only,
display-safe practitioner directory. A strict ASGI pre-auth guard admits only
one fixed POST/JSON GraphQL request and exact variables before the unchanged
shared bridge performs session, origin, CSRF, policy and product-read checks.
The server binds `Surface.NATIVE_DIARY`; callers cannot choose practice, surface,
policy, action, resource, projection or pagination.

The task-local enabled app passed two sequential reads on one long-lived native
session and denied the next read after revocation. The default-off app exposes no
product route, docs or OpenAPI and opens no bridge or database session. No native
Diary asset or `app.main` route was changed.

## Deterministic evidence and repair history

- DeepSeek V4 Flash/high produced the five-file candidate at
  `b2b64eee537eb9e2be23045c9510a1ef5ade219f`.
- Root reproduced Ruff and 76 focused tests. The first real loopback
  HTTP/PostgreSQL run failed closed at synthetic seed admission because two
  authored-synthetic identifiers were 21 characters for `String(20)` columns;
  listener, database and four-role cleanup still passed completely.
- The bounded repair `bf0d5592c6365684ccb51c28f946567b603f266c`
  derives seed and non-release markers from one constant and adds a regression
  against the actual SQLAlchemy column lengths.
- Root reproduced Ruff and 77 focused tests. The live-local proof then passed as
  `live_local_backend_postgres`, and an independent read-only residue query found
  zero matching disposable databases or roles. Evidence was committed at
  `ef38162afbf89f96aacc9f255bdced9a15934bf3`.
- Two fresh Gemini 3.6 Flash/high reports returned `pass` but were rejected by
  Sol because their narrative source values were inaccurate. They are retained
  as consumed negative reviewer evidence and have no acceptance authority.
- A third genuinely fresh Gemini project reproduced 77 tests and Ruff, emitted
  the exact machine-derived four-column/four-marker JSON, found no defect, and
  left `ef38162a` clean and unchanged.
- Root replayed the accepted commits, made a two-word documentation-only
  reconciliation for a legacy whole-`app/graphql` substring test, and reproduced
  the combined 170-test/Ruff gate at `74ee7f93c9b43a7d34644b58b2584817e7e9cc00`.

## Claims not made

This does not establish a mounted native-Diary consumer, stale in-flight UI
response rejection, real identity or Microsoft federation, patient/clinical
data, provider/model execution, any command/write, deployment, production or
release. Request-time freshness and post-revocation denial do not prove that an
already-returned response will be rejected before rendering.

Protected refs/evidence and `docs/branding/` remained untouched. The product
Continuity/Compass map remains 206/187 because this result is task-local,
default-off and unmounted.

## Next bounded lane step

Build a provider-free, unmounted native-Diary client reconciliation adapter and
deterministic UI harness that binds an authorized session generation/revision to
each request and rejects an already-returned stale or superseded response before
render. Keep product assets and `app.main` unchanged; opening a mounted/default-on
consumer, real identity, broader product reads or writes remains a later gate.
