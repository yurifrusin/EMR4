# Reception One v5 Broad Language ADC-restored Continuation

Status: active — occupied cohort pending
Recorded: 2026-07-30
Authority: Yuri restored the existing approved Bernie keyless impersonated ADC
and reported, “Should be good now.”

## Objective

Resume the already frozen twenty-four-case authored-synthetic v5 broad language
cohort after a fresh read-only cloud-control preflight. This is a continuity
descendant of the closed ADC-blocked result; it does not revise that result or
change any case, prompt, response schema, compiler, proofreader, model or cloud
binding.

## Frozen boundary

- provider: Google Cloud Vertex AI;
- model: `gemini-2.5-flash`;
- project: `bernie-emr4-dev`;
- service account:
  `emr4-bernie-ai-dev@bernie-emr4-dev.iam.gserviceaccount.com`;
- authentication: `keyless_impersonated_service_account_adc`;
- location: `australia-southeast1`;
- hostname: `australia-southeast1-aiplatform.googleapis.com`;
- authored-synthetic input only;
- credential-free isolated work cell and one-use host broker;
- one primary and at most one proofreader-ticket correction per case;
- forty-eight-call absolute ceiling and USD 1 application-cost ceiling;
- no API key, service-account key, fallback, tools, grounding, retrieval or
  cache creation; and
- no product, database, patient, clinical, confirmation, command or write
  authority.

The broker may interpret only the exact typed form. The deterministic
proofreader remains the sole release authority. Any admitted result remains an
in-memory proposal, squeeze-in assessment or clarification; no REST command is
invoked.

## Continuation gates

1. Five-source rehydration records the current baton, authority, active plan,
   protected boundaries and exact Git/worktree state.
2. The existing ADC passes the unchanged read-only credential, project, IAM,
   audit, logging, cache, key and Sydney-model preflight.
3. Continuity revision 102 and Compass revision 89 bind this descendant, and
   schema, lineage and exact rendered Compass validation pass.
4. The already frozen provider-blocked evidence, real-isolation evidence,
   focused tests, API Spine tests and repository-only Ariadne verifier remain
   passing.
5. Independent pre-call container, network, image, credential and process
   residue is clear.

If all gates pass, run the unchanged cohort and stop each case after its first
admitted or terminal safe result. Every opened ledger must be consumed and all
owned resources removed whether the cohort passes or fails.

## Claim limit

This continuation may establish only bounded form-filling behaviour for the
twenty-four frozen authored-synthetic cases through the configured and observed
Sydney locational request path. It cannot prove Australian physical or
sovereign processing, exhaustive language coverage, production reliability or
safety for real, product-derived, patient, health, clinical, protected or
historical data.
