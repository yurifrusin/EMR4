# Ariadne Gemini Repaired-Request Rehearsal — Attempt 004

Date: 2026-07-24
Owner: GPT Sol
Authority: Yuri's explicit instruction to run Gemini again after the
provider-blocked request-contract repair
Status: closed
`ariadne_gemini_repaired_request_attempt4_revision_required`

## Purpose

Exercise the repaired shared Gemini request constructor exactly once against
the unchanged authored-synthetic Ariadne work-cell task. Determine whether
`gemini-3.5-flash` now presents a typed five-draft candidate to the existing
full-schema and deterministic proofreader gates, while producing a complete,
hash-chained audit outside the sandbox.

This is a bounded cognition and containment rehearsal. It is not provider
selection, production admission or evidence that processing occurred in
Australia.

## Exact occupied authority

After a fresh five-source receipt, repository-only verification, a
provider-free real-isolation preflight and a Gemini credential-presence gate
pass:

1. consume the new Gemini attempt-004 ledger immediately before starting the
   work cell;
2. mount the Gemini credential only into the purpose-built one-use broker;
3. transmit the sealed authored-synthetic task through the repaired
   constructor;
4. make at most one `gemini-3.5-flash` Developer API `generateContent`
   request;
5. run the provider schema, full release schema and deterministic proofreader
   if a candidate is presented; and
6. remove and verify the exact work cell, broker, private network and
   lane-specific image tags.

There is no retry, fallback, Terra/OpenAI/DeepSeek call, model substitution,
cross-model input, voting, tool use, self-acceptance or downstream delivery.

## Residency label

This attempt uses the existing direct Gemini Developer API endpoint at
`generativelanguage.googleapis.com`, authenticated by `GEMINI_API_KEY`.
Google's current terms do not give this route an Australian-only processing
guarantee. The task is therefore strictly authored-synthetic and contains no
PII, clinical data, database copy, product data, protected evidence or
historical Diary material.

A later Australian-local evaluation must be a separately authorised Vertex
AI / Gemini Enterprise Agent Platform tranche using an exact
`australia-southeast1` endpoint, an eligible model and independently verified
Google Cloud data-location controls. No such substitution is authorised here.

## Request and audit contracts

The request must:

- omit the Gemini 3.x-unsupported `candidateCount`;
- preserve the accepted provider-schema hash;
- request JSON with the locked provider schema;
- use `MEDIUM` thinking without returned thoughts;
- set the 2,048-token ceiling and `store: false`; and
- expose no tools.

The trusted broker writes a monotonically sequenced SHA-256-linked audit
outside the work cell. The durable exporter must preserve every allowlisted
field exactly and fail closed on any unknown field. The audit records provider
contact, hashes, status, typed field manifests and deterministic dispositions,
but never credentials, prompts, raw responses, draft values, provider error
messages or hidden reasoning.

## Closed surfaces

The attempt may not access PostgreSQL or any database, event feed, product API,
product data, live mailbox, EMR command, provider tool, PII, protected holdout,
historical Diary material, deployment, production or release surface. It may
not read or use `OPENAI_API_KEY`.

## Result rule

Pass:
`ariadne_gemini_repaired_request_attempt4_pass`.

Any provider, schema, proofreader, audit, containment or cleanup failure:
`ariadne_gemini_repaired_request_attempt4_revision_required`.

The single-use ledger is consumed before the occupied work cell starts and is
never rolled back. A failed occupied attempt is not retried under this plan.
