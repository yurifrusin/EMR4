# DeepSeek native Harness package-unloaded guard–bridge module-graph rehearsal plan

Date: 2026-08-22

Timestamp: 2026-08-22T10:08:50.8671885+10:00 (Australia/Brisbane)

Status: **frozen before implementation or process execution**

Reasoning level: **Extra High** for the first execution of the complete exact
derived guard–bridge–sanitizer graph.

## Objective

Materialize the exact accepted derived effective-tool guard, root-service
bridge and safe-subcoordinate sanitizer only inside one disposable
authored-synthetic module root. Supply minimal local package stubs for
`@deepseek-ai/dsh-scope` and `@deepseek-ai/dsh-agent-presets`, then run exactly
one isolated Node process containing three cases:

1. successful mount and exact `edit`, `glob`, `read` effective-tool projection;
2. missing root preset service; and
3. missing mount handle.

Admit only the exact typed case vector or fail closed. The derived runner,
installed package seed, native Harness, DeepSeek worker, model and provider
must not load or start.

## Accepted basis

The accepted predecessor at Continuity 388 / Compass 370 proves the exact
derived bridge in one isolated Node fixture. It binds:

- derived guard: 4,501 bytes, SHA-256
  `76029da0f9c030651fd10c0df16f4e75e86b2269d7560af7f94c74680f8598b9`;
- derived bridge: 1,661 bytes, SHA-256
  `3a49b28174eeefd77d7efe0a00498901ac6636b637ed9dfe60aba46980df1d0b`;
- accepted sanitizer: 2,439 bytes, SHA-256
  `12552925a600dc951afc30b9a738746499c7e2f4cefc9962bc05fb06780f158f`.

That predecessor proves bridge receiver/argument forwarding and the two exact
mount terminals, but deliberately does not execute the guard, scope lookup,
tool view, restriction or schema projection. Its one Node attempt is consumed
and immutable.

## Frozen materialization boundary

The controller may read only:

- the accepted process-free correction controller and v2 evidence;
- the accepted isolated-fixture plan, threat delta, evidence and controller;
- the accepted sanitizer source and repository Git-object resolver;
- this plan, its threat-model delta and tranche-owned schemas and contract.

It may recompute only the exact derived guard and bridge and select the exact
accepted sanitizer. The disposable root may contain exactly these executable
sources:

1. the byte-exact derived guard as `effective-tool-guard.mjs`;
2. the byte-exact derived bridge under its source-owned basename;
3. the byte-exact accepted sanitizer under its source-owned basename;
4. one authored-synthetic fixture module;
5. one minimal local `@deepseek-ai/dsh-scope` ESM stub;
6. one minimal local `@deepseek-ai/dsh-agent-presets` ESM stub; and
7. the two exact stub package manifests required for local ESM resolution.

The scope stub exports only `scopeOf(value)` and returns the fixture-owned
synthetic scope carried by that exact context. The preset stub exports only a
local `PresetMountError` class. Neither stub may read the filesystem,
environment, network, process, package cache, prompt, response or installed
package.

No generated JavaScript or package manifest survives cleanup.

## Exact single-process matrix

All cases use preset ID `emr4-bounded-worker` and required tools exactly
`["edit", "glob", "read"]`, in that order.

### Success

The synthetic preset service owns one async `mount` method. It records:

- receiver identity is the preset service;
- context identity is the exact case context;
- preset ID is exact; and
- call count is exactly one.

The synthetic context owns an inert scope coordinate and a tool surface whose:

- initial view exposes zero local tool names and exactly the three inherited
  restrictable names;
- `restrict` receives exactly `{ allow: ["edit", "glob", "read"] }` once; and
- schema view returns exactly three objects with those names.

The guard result must be exactly:

```json
{
  "coordinate": "EFFECTIVE_TOOL_COMPOSITION_PASSED",
  "presetId": "emr4-bounded-worker",
  "effectiveToolNames": ["edit", "glob", "read"],
  "effectiveToolCount": 3
}
```

The admitted success case additionally records only booleans and counts for the
receiver, context, preset, scope, view, restrict and schema observations.

### Missing service and missing mount

The second case supplies `null` as the service. The third supplies an inert
object with no `mount` member. Each must be caught only as the guard-exported
`PresetMountSanitizedTerminalError` and reduce to exactly:

```json
{
  "stage": "preset_mount",
  "code": "PRESET_MOUNT_UNCLASSIFIED",
  "detail": null
}
```

Neither failure case may reach scope lookup, tool view, restriction or schema
projection. No case may release an exception, message, stack, cause, path,
stream fragment or environment value.

The fixture writes exactly one canonical JSON line to stdout and zero bytes to
stderr. The outer controller admits exact schema, key order, case order and
values only.

## One-process envelope and cleanup order

Before launch, the controller must:

- resolve one absolute existing Node executable without executing a version
  probe;
- build the accepted five-key Windows child environment: `SystemRoot`,
  `WINDIR`, `ComSpec`, `TEMP` and `TMP`;
- prove `PATH` and `NODE_OPTIONS` absent from the child environment;
- prove no installed package or package-cache path appears in the module root
  or launch arguments; and
- prove the derived runner is neither materialized nor imported.

After the one process exits, the controller must delete the entire disposable
root and prove its absence. Before parsing either stream, it must persist a
schema-valid content-free process envelope containing only the numeric exit
code, byte counts, SHA-256 stream digests, environment-key projection, process
counts, package-load counts and cleanup reading. Raw stdout, stderr, paths and
environment values must never be persisted.

Only after that envelope exists may the controller require exit zero, zero
stderr and parse the exact stdout object.

## Machine-only Git and push binding

The caller-authored contract contains no Git object identity and rejects any
embedded 40-character hexadecimal identity. At evidence time, the controller
asks Git for the last commit changing this frozen plan and the last commit
changing its own controller path, passes both through the repository resolver,
and records only the full machine-resolved commits. Push ancestry derives
directly from current Git state with `git rev-parse HEAD^`; no displayed
abbreviation may be expanded manually.

## Closed result vocabulary

- `package_unloaded_guard_bridge_module_graph_pass`
- `module_graph_preflight_rejected`
- `module_graph_process_terminal`
- `module_graph_result_rejected`

A pre-process rejection consumes no process and may be corrected within this
frozen plan. Once the single Node process starts, the attempt is consumed
whether it passes or fails. No automatic or manual retry is authorised. A
failure preserves the content-free process envelope and one typed failure
terminal, then stops for a distinct evidence-led successor.

## Acceptance

Acceptance requires:

- schema-valid contract, process envelope and evidence;
- exact accepted guard, bridge and sanitizer byte/hash bindings;
- zero caller-authored Git object IDs and full machine-resolved plan and
  implementation-owner bindings;
- exactly one Node process and the exact three-case vector;
- success with one correctly bound mount, exact scope lookup, one exact view,
  one exact restriction and one exact schema projection;
- exact successful effective-tool result and order;
- exact `PRESET_MOUNT_UNCLASSIFIED` terminals for both missing coordinates,
  with zero later tool-surface calls;
- local package stubs loaded and installed packages unloaded;
- exit zero, zero stderr and one schema-owned stdout line;
- content-free stream evidence persisted before interpretation;
- disposable-root absence after process exit;
- focused hostile tests for source/stub drift, extra process, case/key/order
  drift, receiver/context/preset/scope mismatch, tool-view mismatch, missing
  short circuit, raw detail, environment expansion, installed-package access,
  runner import and cleanup failure;
- direct CLI, Ruff, `py_compile`, JSON Schema and relevant governance suites
  passing; and
- zero derived-runner, native-Harness, DeepSeek-worker, model, provider,
  network, database, Docker, target, retry and resume activity.

This proves only the exact package-unloaded guard–bridge module graph in an
isolated authored-synthetic Node process.

## Parallelism assessment

- DeepSeek: **declined**. The native Harness and worker are governed objects;
  this tranche authorises one package-unloaded Node graph only and no
  self-review.
- Gemini: **declined provisionally**. The three-case graph, outer process
  envelope and cleanup are deterministic. Reassess before acceptance only if
  the single consumed execution leaves a material semantic ambiguity.
- Native subagents: **declined**. Current developer policy prohibits
  delegation, and one process plus cleanup is inherently serial.
- GPT Sol owns planning, implementation, execution, deterministic verification
  and acceptance.

## Convergence and anti-orbit rule

A pass advances directly to one separately frozen complete package-unloaded
runner evaluation, followed by one bounded occupied native-Harness DeepSeek
worker on useful Raisa development work. No additional narrower rehearsal is
created merely for a conceivable risk. An intervening control is admissible
only when this consumed execution or the subsequent runner preflight exposes a
specific new failure mode that the next step cannot safely contain.

The clockwork, receipts and register are measurement and recovery instruments;
their successful operation is not itself evidence that the DeepSeek control
goal has been reached.

## Explicit exclusions

No second Node process, retry, derived-runner materialization or execution,
installed-package import or mutation, native Harness, DeepSeek worker,
model/provider request, target creation or use, product source/configuration/
API/database/route/adapter/flag/allowlist/grammar/client/waiting-area change,
ordinary-practice enablement, generic-status `Arrived` change, patient/product/
clinical/historical/protected data, production, deployment, release, Pages,
protected evidence or protected-ref movement is authorised.
