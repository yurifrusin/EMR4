# Ariadne agent error and correction register — revision 601

Date: 2026-08-22

Timestamp: 2026-08-22T03:05:18.6792181+10:00 (Australia/Brisbane)

<!-- ariadne-agent-error-register-reading
revision: 601
incident_count: 867
new_incident_ids: AER-0861,AER-0862,AER-0863,AER-0864,AER-0865,AER-0866,AER-0867
open_incident_count: 0
-->

This revision binds six corrected or contained incidents from the failed-closed
native agent-creation boundary. The clockwork owns the canonical JSON register
and pattern report; this note supplies the exact human-readable prospective
reading required for one atomic projection.

## AER-0861

The first implementation pre-push runtime state embedded a Git object identity
in free-form prose. The preflight rejected it before publication. The preserved
corrected receipt uses machine-snapshot-only prose and reports zero manually
supplied object identities.

## AER-0862

The first controller invocation supplied the accepted rebinding contract to the
effective-tool preset builder. Its schema mismatch stopped before native launch,
left no canonical attempt output and consumed no native attempt. The corrected
pre-root guard loads the owner contract, checks preset id, ordered tools, bytes
and digest, and passes the already checked payload to execution.

## AER-0863

The one native process reached exact readiness and one HMR mutation, then exited
before a typed runner sidecar. The attempt is consumed without retry, all
downstream gauges remain zero and cleanup is complete. Factory progress is
unknown. The distinct successor adds dynamic imports and a closed stage/error
terminal rather than repeating this attempt.

## AER-0864

The failed attempt's generated report and efficacy object emitted unconditional
factory-success claims despite `result: fail` and no sidecar. Exact bytes remain
preserved and hash-bound as rejected; the typed failure interpretation is
authoritative and the successor forbids success or observed factory counts
without a valid sidecar.

## AER-0865

The orchestrator manually advanced the live latch during prelaunch recovery
after clockwork adoption. The first closeout check rejected canonical drift
before commands or publication. The selected latch generation was restored
byte-for-byte; canonical governance surfaces now remain clockwork-only outputs.

## AER-0866

The first corrected closeout intent used the truthful Sol non-acceptance as a
Continuity node acceptance pointer. The prospective graph rejected the form.
Failed tranches now use paired semantics: a Sol acceptance of the exact negative
containment result plus a distinct non-acceptance of every unproved capability
claim.

## AER-0867

The first semantically valid prospective node duplicated its detailed evidence
inventory into the live acceptance row and exceeded the compact handover byte
budget. The clockwork rejected before publication. The full inventory remains
in Continuity while the live baton now carries only nine routing artifacts.
