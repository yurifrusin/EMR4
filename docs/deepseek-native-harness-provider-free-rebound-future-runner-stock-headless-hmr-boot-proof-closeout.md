# DeepSeek native Harness provider-free rebound future-runner stock-headless HMR boot proof closeout

Date: 2026-08-22

Timestamp: 2026-08-22T01:02:14.2919121+10:00 (Australia/Brisbane)

Status: **accepted**

## Result

The provider-free native execution candidate is accepted at exact source
`560f471b72bef0b9790120238657dc7afd4d602b`.

One locally materialised `@deepseek-ai/dsh@0.1.0-rc.7` process activated the
exact accepted rebound runner and helper through stock-headless HMR. The
readiness ledger recorded exactly `sentinel_activated` then
`stock_headless_hmr_ready`; the controller made one HMR mutation; and the exact
helper emitted stage `preset_root_roster_admission`, cause
`preset_root_roster_mismatch` and error kind `error`. The independent all-zero
broker reading joined that sidecar into controller coordinate
`post_hmr_pre_request_failure` with `diagnostic_accepted`, `broker_zero` and
`pre_request_supported` all true.

This is the intended positive result: the deliberate preset-root mismatch
proved that the exact future runner was mounted and executed, then stopped it
before `agents.create` or any request boundary.

## Exact lifecycle and containment

- Native process count: `1`.
- HMR mutation count: `1`.
- Native lifecycle duration: `851 ms`.
- Retry and resume counts: `0 / 0`.
- Agent creation, session, turn, broker process/request, worker, model request,
  provider request, database, Docker and network counts: all `0`.
- Target creation and use: `false / false`; the inert target remained absent.
- Stdout/stderr retention and raw stream reading: all `false`.
- Runner and helper execution copies match their frozen SHA-256 bindings.
- The canonical bundle manifest remained byte-identical and no runner fallback
  terminal was written.
- The native process and disposable root were absent before evidence
  publication.

## Package-seed recovery

The first two invocations rejected before native launch. Generation 1 exposed a
Windows npm-wrapper descendant that outlived its parent; generation 2 directly
owned Node/npm-CLI but reached its 600-second deadline. Both paths terminated
owned processes, removed their exact disposable roots, wrote no canonical
attempt evidence and did not consume native attempt 001.

Generation 3 removed npm from prelaunch. It independently verified and copied a
package-only seed containing exactly `package.json`, `package-lock.json` and
`node_modules`: 588 lockfile packages, 32,744 files, 219,364,530 bytes, zero
reparse points and canonical tree SHA-256
`d84e73067c8dbbf4836969eb948012fd364ee454bb07744cfe486995a256084d`.
Seed and disposable-copy readings matched exactly. The copy-and-readback cost
was `180673 ms`; materialiser process and retry counts were both zero. AER-0857
and AER-0858 preserve the two prelaunch failures and their corrections in
prospective register revision 600.

## Verification

- The deterministic package/source, runner/helper, HMR-patch and schema check
  passed without a native process.
- 27 mechanically collected focused tests passed.
- 206 mechanically collected inherited native boot, HMR, tool-guard,
  diagnostic, recovery, materialisation, controller and rebinding tests passed.
- Ruff, Python compilation, strict JSON-schema validation and Git whitespace
  checks passed.
- An independent post-execution readback proved the expected terminal, every
  zero counter, target absence, process/root absence, bundle immutability and
  seed/copy equality.

## Parallelism efficacy

DeepSeek remained declined because the tranche deliberately stopped before
worker creation. Gemini remained declined because the observed terminal exactly
matched the closed typed contract and every independent zero gauge agreed;
there was no semantic ambiguity requiring an external veto. Native subagents
remained declined under developer policy and because one mutable process plus
cleanup formed a serial trust boundary. GPT Sol owned recovery, execution,
evidence join, verification and acceptance.

## Claim boundary and successor

This proves local rc.7 lifecycle control, exact rebound-runner HMR activation,
typed pre-request attribution and independent broker-zero traceability. It does
not prove agent creation, a session or turn, DeepSeek transport, model/provider
behavior, coding quality, target editing, product integration or ordinary-
practice readiness.

No product, configuration, API, database, route, adapter, feature flag,
allowlist, grammar, first-party client, waiting-area, ordinary-practice,
generic-status `Arrived`, patient/clinical/product data, production, deployment,
release, Pages or protected-ref surface changed.

The narrow dependency-satisfied successor is
`deepseek-native-harness-provider-free-rebound-future-runner-agent-creation-boundary-rehearsal`.
It may derive and mount one exact runner descendant with admitted preset roots,
create exactly one inert DeepSeek worker agent, emit one typed agent-created
sidecar and stop before session creation, a turn, broker/model/provider request
or target creation/use. It remains provider-free and must preserve the same
one-process, no-retry, closed-product and protected-ref boundaries.
