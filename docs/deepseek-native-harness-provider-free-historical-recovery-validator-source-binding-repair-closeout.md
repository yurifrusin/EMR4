# DeepSeek native Harness provider-free historical recovery validator source-binding repair closeout

Date: 2026-08-21

Result:
`deepseek_native_harness_provider_free_historical_recovery_validator_source_binding_repair_pass`

Exact reviewed implementation source:
`89e57921fccce34e69e61f1aa6c7a87659fac223`

Pre-verifier acceptance binding:
`bd0ba6182c3a04dc28ac9f78672b989bf2bb5abd`

## Outcome

The accepted pre-HMR startup-terminal recovery validator no longer mistakes
mutable descendant controller and test files for its historical reviewed
source. It now projects the seven accepted SHA-256 values as immutable
historical coordinates. Its schema, behavioral scenario, hostile-mutation,
controller-ordering and consumed-attempt checks remain live and the accepted
historical evidence still compares exactly.

A separate provider-free checker resolves full commit
`12d8758fee2504435ca2b4ccf6225b9d7a86a6a1`, proves it is an ancestor of
current `HEAD`, reads the seven exact Git blobs and matches every accepted hash.
It also proves eight named historical recovery artifacts remain byte-identical.
The old validator itself passes while the shared `subprocess.run` and
`subprocess.Popen` entry points are forbidden.

## Verification

- deterministic repair `--check`: passed;
- repaired focused set, including both unchanged predecessor checks: `61/61`;
- maintained native-Harness functional surface: `352/352`;
- Ruff, Python compilation, closed JSON-schema validation and diff hygiene:
  passed;
- local Git subprocesses per independent repair check: `9`;
- old-validator subprocesses: `0`;
- Harness / broker / worker / session / prompt / tool processes: all `0`;
- model / provider requests: `0 / 0`;
- occupied attempts: `0`;
- reconstructed or retained raw startup bytes: `0`.

An intentionally broader filename-based sweep also found eight pre-existing
legacy governance assertions that hard-code obsolete Continuity revisions
318/319/322 or former Compass/latch roles. The incoming branch is already at
Continuity 359, so those tests were false before this repair. They do not
exercise the historical source projection or maintained Harness behavior and
remain unchanged outside this tranche.

## Evidence preservation

The accepted recovery evidence, report, contract, schemas, efficacy reading,
legacy-boundary receipt, attempt-001 baseline and consumed attempt-002 files
remain unchanged. Attempts 001-003 were not retried, resumed, overwritten,
reclassified or inspected for deleted raw output.

## Parallelism result

- DeepSeek: `not_applicable`; invoking the Harness or model would violate the
  provider-free repair boundary.
- Gemini: `not_applicable`; exact local Git blobs, hashes and unchanged tests
  supplied the complete decision evidence.
- Native subagents: `declined`; developer policy and the single serial semantic
  seam admitted no delegation.

No worker or verifier agent was launched.

## Workflow observation

AER-0753 records one contained plan-vocabulary correction. The first frozen
acceptance row said generic process counts were zero even though the design
explicitly admits bounded local Git subprocesses, and the plan prose did not
repeat the exact ordinary-practice token. Both were corrected before candidate
acceptance; a focused plan test now binds the distinction. No Harness, provider,
product or protected effect occurred.

## Boundaries and next step

This result proves historical validator stability only. It does not identify
deleted attempt output, prove DeepSeek quality or Harness reliability, or
authorize an occupied attempt by itself. Product source/configuration, API,
database, route, adapter, flag, allowlist, grammar, client, waiting-area,
ordinary-practice, generic `Arrived`, patient/appointment/clinical data,
production, deployment, release, Pages and protected refs remain unchanged.

The narrow successor is a provider-free attempt-004 readiness and preexecution
decision. It may freeze a fresh exact attempt identity and one-execution gate
only after deterministic controller, preset, broker, clockwork, cleanup and
safe-terminal checks pass. It may not launch the Harness, worker, model or
provider under that readiness tranche.
