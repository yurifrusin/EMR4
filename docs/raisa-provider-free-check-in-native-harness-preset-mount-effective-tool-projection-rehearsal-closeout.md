# Provider-free native Harness preset-mount/effective-tool projection closeout

Date: 2026-08-20

Status: accepted

## Result

The exact healthy `emr4-bounded-worker` preset now mounts through the accepted
rc.7 guard inside one real provider-disabled native Harness lifecycle and
projects exactly `edit`, `glob`, `read` before any agent or provider boundary.

Accepted attempt `check-in-preset-mount-effective-tool-native-003` produced all
nine ordered lifecycle events and terminal
`EFFECTIVE_TOOL_COMPOSITION_PASSED`. It used one native process, zero automatic
retries, removed three credential environment names, and recorded zero agent
sessions, turns, broker requests, model requests, provider requests, network
attempts, occupied workers, Docker invocations and database invocations.
Stdout and stderr were empty. The process and disposable root are absent, and
no raw log or environment was retained.

## Fail-closed recovery evidence

- Attempt 001 stopped before checkpoint consumption or any subprocess because
  the controller pre-created the offline installer's `installation` root.
- Attempt 002 stopped before checkpoint consumption or native launch after the
  bounded offline npm materialiser timed out. Its one exact package PID was
  terminated, its verified disposable root was removed, and no raw log remains.
- Neither failed identity was retried or reclassified. Both have zero native
  Harness processes and zero model/provider activity.
- Attempt 003 removed npm from this proof. It bound the accepted package source
  by three content/lock digests, revalidated six critical rc.7 packages and the
  installed source before and after a zero-process copy, and passed a disposable
  copy/cleanup fixture twice under the deterministic ledger.

## Review and verification

The final exact candidate is
`c8b0e3a587191b65da212edda36b8b833a2ecc2c`. The shell-free final validation
ledger passed all twelve commands. A fresh Gemini 3.7 Flash/high semantic veto
passed at that full source and left the review worktree clean. Earlier rejected
receipts and failed ledgers remain immutable and have no acceptance authority.

Focused and neighbouring provider-free tests, Ruff, Python compilation, JSON
Schema validation, deterministic source binding, package materialisation,
checkpoint binding, terminal validation and cleanup checks pass. Protected
`master` and `handoff/current` refs remain unchanged at
`2e34bdad732fdab32fbf778280b3d3c70d66d602`.

## Claim boundary

This proves native preset mounting and exact effective-tool projection only. It
does not prove an agent session, DeepSeek model quality, a completed worker work
package, product behavior, ordinary-practice enablement, generic-status
`Arrived`, action grammar, first-party clients, waiting-area behavior, product,
patient or clinical data, a live provider, production runtime, deployment,
release, Pages or protected integration.

## Next dependency frontier

The next tranche may freeze one authored-synthetic, non-product monitored
DeepSeek worker rehearsal through the accepted native preset, exact tool guard
and Ariadne broker gears. Any occupied call requires its own exact work package,
pre-dispatch receipt, request limit, immutable terminal and zero silent fallback.
