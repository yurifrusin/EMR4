# Reception One Bureau Model Text Lane — Provider-Blocked Closeout

## Result

`reception_one_bureau_model_text_lane_provider_blocked_pass`

The provider-blocked descendant passes. Trusted host code now constructs a
minimal authored-synthetic model input, an untrusted candidate can compose only
from fourteen closed typed operators and allowlisted source handles, and the
existing deterministic proofreader retains schema, grounding, topology,
freshness, effect and authority control.

Known create and novel squeeze-in candidates are admitted and executed only in
memory as proposal-shaped results. Invented writes, free literals, forged
scope, forward references, excess steps and duplicate arguments fail closed.
One bounded correction dialogue exposes path/code feedback only.

The disposable fixture cell ran as uid/gid 65532, with a read-only root
filesystem, loopback only, no mounts, no forwarded credential environment,
dropped capabilities and bounded CPU, memory and processes. Its candidate
passed the same host proofreader, and the container, image and temporary
context were removed.

## Evidence boundary

This closeout records zero provider calls, credential reads, API-key use,
database access, product data, historical Diary access, appointment writes,
confirmation or product delivery.

It proves the provider-blocked contract and isolation baseline. It does not
prove Vertex request acceptance, model inference, generated-plan quality,
Australian physical or sovereign processing, product-data safety, runtime
delivery, production or release.

Yuri has separately authorised one exact conditional occupied descendant. That
call may proceed only after the frozen ADC, entitlement, regional endpoint,
cache, request-response logging, Data Access audit, isolation, one-use ledger,
cost and call-ceiling gates pass.
