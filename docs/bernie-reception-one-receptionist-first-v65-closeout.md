# Reception One Receptionist-first v6.5 Closeout

Status: complete; occupied cohort did not pass at 23/24 exact outcomes
Recorded: 2026-07-30

## Result

The complete twenty-four-request authored-synthetic regression ran through the
exact bounded Sydney Vertex lane. Twenty-three terminal results matched their
frozen deterministic oracle. One did not, so the tranche result is
`reception_one_receptionist_first_v65_full_cohort_fail_closed`.

All twenty-eight opened ledgers are consumed, every turn and case audit chain
verifies, no call followed the terminal result and task-scoped residue is
clear. The mismatch released no value.

## What improved

- Every create, move, resize, cancel and status case passed.
- All six v6.4 defects are repaired, including the four exact resize/cancel
  forms and both clarification cases.
- The explicit 3:00 PM create remained exact.
- The unstamped no-show request still safely clarified; no `dna` alias was
  invented.
- One squeeze-in case used its complete exact typed form on both turns.

## Remaining mismatch

`b-squeeze-without` selected the exact `squeeze_in_assessment` goal and typed
program on both calls. Its audit-only operator note passed. The deterministic
proofreader rejected only `decision_note_identifier` on the primary and again
on the terminal correction. The correction ticket named the field and
violation but the prompt did not clearly explain that the internal decision
note must omit all patient and practitioner display names and aliases. No
typed fact, proposal or policy error occurred, and nothing was released.

The bounded descendant repair is to teach that exact de-identified
decision-note rule and its correction code. It must not redact by safe repair,
change the output schema, alter the oracle, add a no-show alias or loosen the
typed proofreader.

## Calls and controls

- 24 primary calls and 4 terminal second calls; 28 total against a ceiling of
  48;
- all 28 provider responses completed with HTTP 200;
- 121,968 prompt, 14,532 visible candidate, 20,384 thinking and 156,884 total
  tokens reported by Vertex;
- exact `gemini-2.5-flash`, `bernie-emr4-dev`, Bernie impersonated service
  account, `australia-southeast1` and regional hostname throughout;
- no API key, service-account key, global endpoint, fallback, provider tool,
  grounding, retrieval, cache creation, product/database access, write or
  delivery; and
- raw prompts, raw provider responses, credentials, API-key information and
  hidden chain-of-thought were not retained.

The first launch failed locally before any provider call or ledger because the
new authority manifest omitted the inherited live-gate shape. Its audit is
closed, the manifest repair is regression-tested and the successful occupied
run used the fully regated revision.

## Claim limit

This evidence proves twenty-three exact safe outcomes and one bounded
decision-note de-identification defect over a reused authored-synthetic cohort
through the configured and observed Sydney locational path. It does not prove
independent generalisation, production fitness, Australian physical or
sovereign processing, or safety for real, product-derived, patient, health,
clinical, protected or historical data.
