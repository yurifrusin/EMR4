# DeepSeek native Harness root-service-forwarding isolated Node-fixture closeout

Date: 2026-08-22

Timestamp: 2026-08-22T09:52:21.2973747+10:00 (Australia/Brisbane)

Status: **accepted isolated bridge behavior**

Reasoning level: **Extra High**

## Result

At exact implementation source
`4a99a8b5f9ee45210a635e8b18281ca5cf143695`, exactly one isolated
authored-synthetic Node process evaluated the byte-exact derived bridge and
accepted sanitizer. The closed result is
`isolated_node_fixture_pass`.

The three cases passed exactly:

- `success` called the service once with the service as receiver, the exact
  synthetic context and the exact preset ID;
- `missing_service` returned
  `PRESET_MOUNT_UNCLASSIFIED` with null detail; and
- `missing_mount` returned the same exact content-free terminal.

The disposable root and all materialized JavaScript were absent before
semantic admission.

## Typed process evidence

The process envelope records exit code zero, stdout 526 bytes with SHA-256
`36486f9f5c5e2c0d1ae680a8edfad53881d0bd6488f4444988a9ee47f14041b4`,
zero stderr with the empty-stream SHA-256, one Node process and zero native
Harness or worker/model/provider processes. It retains no stream content,
executable path, fixture path, raw runtime detail or environment value.

The envelope was persisted before stdout interpretation. The admitted evidence
then retains only the exact schema-owned case vector.

## Source and clockwork binding

The caller-authored contract contains no Git object identity. The repository
resolver derived planning source
`61ed0c2c35970f76c6facc07c35333bb6bb4c950` and implementation source
`4a99a8b5f9ee45210a635e8b18281ca5cf143695`. After evidence commit
`4d2396eb454ef529f1415ebf4fb4287b2e9482d6` advanced `HEAD`, the
read-only check remained byte-equal and retained the implementation owner.

The bridge and sanitizer bindings remained respectively
`3a49b28174eeefd77d7efe0a00498901ac6636b637ed9dfe60aba46980df1d0b`
and
`12552925a600dc951afc30b9a738746499c7e2f4cefc9962bc05fb06780f158f`.

## Verification

- focused hostile fixture tests: 24 passed;
- exact broader bridge/sanitizer/governance collection: 126 passed;
- direct post-evidence-commit check: passed without a process;
- Ruff, `py_compile` and all four JSON schemas: passed;
- failure terminal: absent;
- Node process count: exactly one and consumed;
- native Harness, runner, guard, installed package, DeepSeek worker, model,
  provider, network, database, Docker, target, retry and resume counts: zero.

## Workflow-cost reading

Clockwork records AER-0920 through AER-0922. The first is the short-Git-display
manual-expansion recurrence caught before notification push. The second is an
over-broad test assertion that rejected the sanitizer's legitimate typed
terminal constructor. The third is a manually composed clockwork timestamp
caught before validation and replaced by the exact creation-clock reading. All
three were corrected without consuming external resources.

## Parallelism disposition

- DeepSeek: declined because the Harness and worker were the governed objects
  and could not self-review this predecessor fixture.
- Gemini: declined because the exact single-process vector, cleanup, immutable
  evidence and 126-test gate passed without semantic ambiguity.
- Native subagents: declined under current developer policy.
- GPT Sol owned the serial plan, controller, one execution, verification and
  acceptance.

## Next tranche

Proceed with
`deepseek-native-harness-provider-free-preset-mount-root-service-forwarding-package-unloaded-guard-bridge-module-graph-rehearsal`.
It may run one separately frozen isolated Node graph containing the exact
derived guard, bridge and sanitizer plus authored-synthetic local stubs for
scope and `PresetMountError`. It must prove the successful effective-tool
projection and the same two mount terminals without loading the installed
package, runner, native Harness, worker, model or provider.

The full runner remains a later, separately bounded coordinate because it adds
filesystem, target and lifecycle behavior not needed for the next proof.

## Boundaries

This acceptance proves isolated bridge behavior only. It does not prove the
guard module graph, runner, installed package, native-Harness boot, DeepSeek
turn, worker quality, model/provider access, retry, product/configuration/API/
database/route/adapter/flag/allowlist/grammar/client/waiting-area behavior,
ordinary-practice enablement, generic-status `Arrived`, patient/product/
clinical/historical/protected data, production, deployment, release, Pages,
protected evidence or protected-ref movement.
