# Provider-free native Harness preset-row service-path recovery closeout

Date: 2026-08-20

Timestamp: 2026-08-20T18:41:10.4055454+10:00 (Australia/Brisbane)

Status: `accepted_provider_disabled_native_preset_row_service_convergence`

Operation:
`raisa-provider-free-check-in-native-harness-preset-row-service-path-recovery`

Exact semantic candidate:
`da52514548ef1c1253e379f983165545482592a6`

Exact split-veto executor candidate:
`a6b3fc9ea582677419b5053952336792fd088d0c`

Exact committed preexecution source:
`e295441de4272135f70f712e964293e623fec43f`

Exact immutable terminal and post-terminal repair source:
`3369c92e315e65b64243204dd225265987246a12`

## Result

The rc.7 source proof confirms the predecessor discrepancy. Native
`composeProfile()` appends a final shipped-preset overlay that replaces earlier
configured roots, while `AgentPresets` appends the derived
`$DSH_HOME/.agent-presets` root only when `includeUserRoot` is true. The
predecessor explicitly disabled that user root, so its native service could see
only the shipped `code`, `cordis`, `minimal` and `standard` presets. The direct
package scan passed because it was handed the otherwise excluded user root.

The five-scenario provider-free fixture proves the minimal correction: retain
the shipped system root, re-enable the derived user root second, and expect one
healthy `emr4-bounded-worker` row with `user` trust, 158 bytes and the exact
canonical digest. Missing roots, earlier duplicate shadowing and arbitrary
configured-root displacement all fail closed.

The separately checkpointed native process then passed all eight coordinates:
effective roots entered and passed, row discovery entered, exact row found and
healthy, bytes read, length bound and digest bound. It ran once for 718 ms,
exited zero, retried zero times, retained no raw output and left both process
and disposable root absent. Agent/session/turn/broker/model/provider/network/
Docker/database counts are all zero.

This proves rc.7 provider-disabled native preset-row service convergence only.
It does not prove preset mount, effective tools, agent creation, occupied
DeepSeek work, coding quality, attempt 006, database or product behavior.

## Review and clockwork result

The semantic candidate passed a clean Gemini 3.7 Flash/high veto. The executor
gate initially exposed a workflow defect: two zero-exit, clean Antigravity
transports admitted no structured decision because the response contract made
the LLM reproduce the complete deterministic command-results ledger. Neither
failure was a candidate verdict.

The recovery split the gears. The existing shell-free validation runner sealed
all twelve exact commands and every command passed; Gemini then supplied only
the independent semantic decision and review, which passed cleanly. This keeps
the independent veto while removing exact transcription from the probabilistic
component. The checkpoint and active latch were published only by clockwork,
with zero canonical drift and one live publication.

A post-terminal focused test found that the missing-checkpoint test path was
captured in a Python default argument. The path is now resolved at call time.
This did not alter or rerun the consumed process. The final focused service-path
and preflight suite passes 56 tests; the complete incident-register suite passes
449 tests; Ruff and Python compilation pass. Register revision 574 contains
six corrected incidents, none open, including the caught direct-register-edit
ownership violation and the rejected attempt to repeat an already accepted
successor.

The first continuing non-PHI Pushover notification succeeded with request
`d65c2006-6c3e-4ce1-81f1-0e20ccfa3fc5` but named the stale route successor.
After the clockwork rejected that duplicate, the correction notification
succeeded with request `432db1da-c8ac-4744-9633-e578612f8072` and names the
provider-free preset-mount/effective-tool projection successor.

## Next boundary

Proceed to the dependency-satisfied provider-free Harness tranche:
`raisa-provider-free-check-in-native-harness-preset-mount-effective-tool-projection-rehearsal`.
It may freeze only the narrowest no-model proof that the now-visible exact
`emr4-bounded-worker` preset can mount through the accepted rc.7 guard and
project exactly `edit`, `glob` and `read`. Any native process requires a
separate exact checkpoint, one new attempt identity, zero retries and a
terminal before any agent session, turn, broker, model or provider request.

The first drafted successor incorrectly repeated the already accepted
default-off route-adapter operation. Clockwork rejected that chronology before
publication; the existing route at `c82c3a741053a9c8da260aa62e1a968af22bb54e`
remains unchanged.

## Closed surfaces

No DeepSeek request, agent creation, preset mount, attempt 006, Docker/
PostgreSQL/SQL, ordinary-practice enablement, product data, generic-status
`Arrived` change, action grammar, first-party client, waiting-area movement,
production runtime, deployment, release, Pages or protected ref changed.
Local/origin `master` and `handoff/current` remain exact
`2e34bdad732fdab32fbf778280b3d3c70d66d602`. `docs/branding/` and every
unrelated untracked path remain preserved.
