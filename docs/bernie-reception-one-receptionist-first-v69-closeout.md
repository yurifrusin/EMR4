# Reception One v6.9 untouched authored-synthetic holdout closeout

## Result

`reception_one_receptionist_first_v69_untouched_holdout_pass`

The byte-frozen v6.8 Reception One model lane passed all twelve newly authored,
sealed, authored-synthetic holdout cases. Eleven cases passed on their primary
call. `h-resize-extend` required the one permitted terminal proofreader
correction after the primary receptionist sentence used the wrong goal word;
the corrected form and sentence then passed without broker interpretation or
repair.

The cohort used thirteen of the authorised twenty-four calls. Every call
returned HTTP 200 from the configured
`australia-southeast1-aiplatform.googleapis.com` Vertex endpoint with
`gemini-2.5-flash`, project `bernie-emr4-dev`, and the exact Bernie service
account through keyless impersonated ADC. No API key, fallback, provider tool,
grounding, retrieval, cache, database, product delivery or appointment write
path was used.

## Interruption and recovery

The first local shell control window ended after eight cases and nine calls.
All nine ledgers were already consumed and their cases were durably closed.
The existing ledger-aware cohort resume loaded those immutable results and ran
only the four remaining cases. No completed case was repeated, no call was
discarded, and the prompt, schemas, proofreader, oracle and generation settings
remained frozen.

## External audit

- 12/12 exact safe outcomes;
- 13 provider calls: 12 primary and one terminal correction;
- 60,857 prompt tokens, 6,694 visible candidate tokens, 8,417 thinking tokens
  and 75,968 total tokens;
- all thirteen single-use ledgers consumed;
- thirteen turn audit chains and twelve case audit chains verify;
- zero residual owned containers, networks, images, brokers or work-cell
  processes;
- no raw prompt, raw provider response, credential, token, API-key information
  or hidden thought content retained.

## What this proves

This is evidence that the frozen narrow receptionist/form-filling lane
generalised across these twelve previously unexposed synthetic appointment
requests while preserving the deterministic proposal-only release boundary.
It is meaningfully stronger than another pass over the repeatedly observed
development cohort.

It does not prove production fitness, exhaustive natural-language coverage,
voice performance, safety for real or product-derived information, autonomous
booking authority, or Australian physical or sovereign processing. The
regional claim remains limited to Google’s published locational endpoint
contract and the configured and observed Sydney request path.

## Next bounded descendant

Yuri pre-authorised a minimal read-only authored-synthetic Diary-context
integration after a complete holdout pass. That descendant may let a trusted
backend construct a minimal, source-labelled and freshness-bound synthetic
context frame for the frozen cell. The cell receives no database credentials,
performs no database reads, and may release only a typed proposal candidate for
human review. Real/product data, confirmation, mutation, Word, voice,
production, deployment and release remain closed.
