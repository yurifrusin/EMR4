# Raisa authored-synthetic native-Harness integrated-runner first controlled development rehearsal plan

Date: 2026-08-22

Status: `frozen`

Reasoning level: `high`

## Objective

Measure whether the accepted DeepSeek native-Harness integrated runner can do
one genuinely useful, fully traceable development job. Give DeepSeek V4
Flash/high one authored-synthetic Python defect, permit at most one provider
request and one direct literal edit, and judge the resulting candidate with
exact bytes plus public and independent holdback behavior. A failed attempt is
terminal and cannot be retried or moved to Claude Code.

## Exact work package

The disposable Git workspace contains one non-product file:
`synthetic_window_coalescer.py`. Its maintenance-window coalescer already
handles disjoint, overlapping and touching windows but incorrectly shortens a
span when a later window is nested inside it.

The worker must make one literal edit spanning both related changes:

1. replace the incoming end assignment with the maximum of the current and
   incoming ends, and update the adjacent comment; and
2. append one visible nested-window regression row.

The candidate is useful only if it equals the frozen expected bytes, passes all
four public cases and all three controller-owned holdback cases, changes only
the one synthetic file and leaves no disposable state behind. This reuses the
accepted task that earlier occupied attempts never reached successfully; those
attempt identities and evidence remain immutable.

## Authority and identity

- operation:
  `raisa-authored-synthetic-native-harness-integrated-runner-first-controlled-development-rehearsal`
- attempt: `deepseek-native-integrated-window-worker-001`
- work order: `wo-deepseek-native-integrated-window-worker-001`
- lease: `lease-deepseek-native-integrated-window-worker-001`
- disposable root:
  `C:/Users/sarashera/EMR4-worktrees/deepseek-native-integrated-window-worker-001`
- evidence root:
  `orchestration/continuity/raisa-authored-synthetic-native-harness-integrated-runner-first-controlled-development-rehearsal/`
- worker: `deepseek-v4-flash`, reasoning effort `high`, through the pinned
  native Harness only;
- effective tools: exactly `edit`, `glob`, `read`;
- occupied ceiling: one process, one session, one turn, at most one provider
  request and exactly one direct top-level literal `edit` if any tool is used.

The accepted integrated runner and typed edit controls are mandatory. The
derived execution copy may change only its single target-path literal and add a
redundant `conclusion_marked` compatibility reading for the already accepted
outer terminal reducer. It may not alter the tool allowlist, model selection,
argument preflight, result classification, lifecycle classification, request
count, conclusion timing or fail-closed stages.

## Parallelism assessment

- DeepSeek Flash: `planned`, positive leverage. It owns exactly the one
  authored-synthetic repair after every provider-free gate and checkpoint.
- Gemini 3.7 Flash/high: `reserved`, required independence. If an exact
  candidate survives, it owns a fresh postterminal veto of the candidate and
  evidence; it is not an auxiliary model inside the occupied attempt.
- Native subagents: `declined`, negative leverage. Developer policy prohibits
  proactive delegation and the identity-bound transitions are serial.
- GPT Sol: owns plan freeze, controller adaptation, deterministic verification,
  checkpoint, launch custody, terminal reduction, cleanup and acceptance.

## Fail-closed sequence

1. Complete five-source rehydration, latch validation, exact Git/ref/worktree
   resolution and a fresh Ariadne receipt.
2. Freeze this plan, the threat delta, one contract, closed schemas, the exact
   integrated-runner derivation and focused tests.
3. Prove provider-free that the runner derivation is limited to the exact
   target binding and compatibility reading; the typed edit controls, preset,
   one-request broker, synthetic baseline/expected bytes, public/holdback
   cases, process absence and protected boundaries must all pass.
4. Commit and origin-align the clean candidate, then issue a full-40-character
   deterministic admission receipt.
5. Materialize exactly one disposable workspace and pinned rc.7 native-Harness
   envelope. Preparation may not start the Harness, broker, model or provider.
6. Bind the prepared root, work order, runner, prompt, one-request ceiling and
   cleanup path to one fresh clockwork checkpoint and occupied-prelaunch
   receipt.
7. Consume the attempt before launch. Run exactly one Harness process and
   broker allowance. No automatic/manual retry, resume, fallback, auxiliary
   model or second worker is permitted.
8. Reduce the result to typed lifecycle/edit coordinates, provider counts,
   changed-path and exact-byte readings, seven behavioral cases, timing and
   cleanup. Destroy raw prompt/response/reasoning, streams, environment,
   session, credential material and the exact disposable root.
9. If a candidate survives deterministic admission, obtain the reserved fresh
   Gemini veto. Otherwise decline it because there is no candidate to review.
10. Close transactionally through the governance clockwork, write the paired
    lay/technical Yuri summary, send the usual non-PHI Pushover notification
    and advance only if the terminal evidence justifies doing so.

## Efficacy decision

This tranche distinguishes forward motion from procedural circulation:

- `useful_worker_pass`: one completed provider request, one admitted edit,
  exact expected bytes, 4/4 public and 3/3 holdback cases, accepted independent
  veto, complete traceability and cleanup. The native Harness becomes eligible
  for another separately bounded useful package.
- `traceable_worker_failure`: the exact failure coordinate and costs are
  retained with no retry. The Harness is not admitted for further EMR4
  development until the failed coordinate is redesigned provider-free.
- `prelaunch_rejection`: correct within this tranche only before lease
  consumption; count every rejected local publication or validation so the
  clockwork's own correction burden remains visible.

Success is not inferred from paperwork volume, provider completion or a
plausible diff. It requires the useful candidate and behavioral proof.

## Protected boundaries

- one authored-synthetic non-product file only;
- at most one DeepSeek request after provider-free gates;
- no weakening of the accepted typed controls;
- no Claude Code or other transport fallback;
- no retry, resume, second worker or auxiliary model inside the attempt;
- no ordinary-practice enablement, flag, allowlist or command mounting;
- no product/configuration/API/database/route/adapter/grammar/client or
  waiting-area change;
- no generic-status `Arrived` change;
- no product, patient, appointment, clinical, historical or protected data;
- no production runtime, deployment, release or Pages action;
- no protected evidence access or protected-ref movement;
- local/origin `master` and `handoff/current` remain exactly
  `2e34bdad732fdab32fbf778280b3d3c70d66d602`;
- preserve `docs/branding/` and every unrelated untracked file; and
- explicit-path staging only, never `git add .` or `git add -A`.
