# DeepSeek native Harness provider-free preterminal-observable composition recovery boot closeout

Date: 2026-08-20

Timestamp: 2026-08-20T08:51:16.4963085+10:00 (Australia/Brisbane)

Status: accepted immutable fail-closed result; native composition not accepted

## Outcome

The frozen controller admitted exactly one new offline, network-denied rc.7
native Harness process as
`preterminal-observable-composition-recovery-boot-attempt-001`. It materialised
the exact cache, observed the sentinel and both stock HMR watches, applied the
changed patch and entered the corrected runner.

The runner retained the exact terminal `SERVICES_UNAVAILABLE`, followed by
`EXIT_REQUESTED`, and the process exited `2`. It did not create a scope or
enter the effective-tool guard, so the `edit`, `glob`, `read` view remains
unproved. The attempt is consumed and will not be retried, resumed,
reclassified or reused.

## Exact retained reading

- deterministic execution candidate:
  `e4dddfcb48cd236e147bca2560e54f42d84017df`;
- admitted preexecution receipt descendant:
  `0596788e61db20850b74a4fcb43e0cb93daa2784`;
- new attempt:
  `preterminal-observable-composition-recovery-boot-attempt-001`;
- package materialisation: exit `0`, `119731 ms`, with all six required
  installed packages exact `0.1.0-rc.7`;
- reliable native launch duration: `10025 ms`, assigned in `finally` before
  termination and cleanup;
- readiness: `sentinel_activated`, `stock_headless_hmr_ready`;
- activation: `BOOTSTRAP_APPLY_ENTERED`, `SERVICES_UNAVAILABLE`,
  `EXIT_REQUESTED`;
- terminal: `SERVICES_UNAVAILABLE`, no effective tool names;
- native process count `1`, retry count `0`, exit code `2`;
- network, agent-session, turn, broker, model, provider, occupied-worker,
  Docker and database counts: all zero; and
- process absence and disposable-root absence: both true.

This is the intended observability improvement: unlike the predecessor's
opaque failure and unreliable zero duration, the consumed attempt now has an
exact bounded preterminal coordinate and credible elapsed time. It does not by
itself prove whether the missing services arise from module declarations,
patch-level injection, Cordis context visibility or another rc.7 service
binding rule.

## Acceptance and non-acceptance

Accepted:

- the frozen plan, exact cache/source/controller binding and one-process latch;
- 12 focused and 97 combined provider-free tests, Ruff, compile and the
  deterministic cache-only check before process start, plus the 170-test
  postterminal Harness/latch/clockwork/baton suite;
- exact separate readiness and activation ledgers;
- the immutable `SERVICES_UNAVAILABLE` terminal and reliable duration;
- zero prohibited calls and complete owned cleanup; and
- the conclusion that service-injection availability must be resolved
  deterministically before any separately named future native process.

Not accepted:

- no scope creation, effective-tool guard entry or `edit`, `glob`, `read`
  composition result;
- no occupied worker and no DeepSeek model-quality or provider-reliability
  conclusion;
- no same-attempt retry or automatic successor process.

Gemini was reserved only for a passing native terminal and was therefore not
eligible. DeepSeek remained the provider-free subject and received no model
request. Native subagents remained declined under current developer policy and
the serial lifecycle lease.

## Successor

Continue to
`deepseek-native-harness-provider-free-required-service-injection-recovery`.
That tranche is deterministic and provider-free. It must preserve both
consumed native attempts, bind the exact rc.7 service-injection semantics,
identify why `agentPresets` or `tools` was unavailable, and construct a future
runner/patch declaration that requires exactly `hmr`, `agentPresets` and
`tools` without starting a native Harness process, agent, broker, model or
provider.

Any later native execution requires its own separately frozen operation and
new attempt identity. No product, API, configuration, ordinary-practice,
feature-flag, allowlist, action-grammar, first-party-client, waiting-area,
data, Docker/database, production, deployment, release, Pages or protected-ref
authority changed. Local/origin `master` and `handoff/current` remain exact
`2e34bdad732fdab32fbf778280b3d3c70d66d602`. All unrelated untracked files,
including `docs/branding/`, remain preserved.
