# DeepSeek native Harness provider-free preterminal-observable composition recovery boot plan

Date: 2026-08-20

Timestamp: 2026-08-20T08:32:23.8107285+10:00 (Australia/Brisbane)

Status: `frozen`

Planning source HEAD:
`b3df372968b6f9eca3ba270e691a0c7420c55ba8`

Accepted deterministic observability source:
`b5f0bc0d823a1c8009f3bb49efcc9a588b9703ab`

Immutable failed native attempt source:
`296f35ff696361c63a4e018e5ae9317c79cb6213`

Operation:
`deepseek-native-harness-provider-free-preterminal-observable-composition-recovery-boot`

New immutable attempt:
`preterminal-observable-composition-recovery-boot-attempt-001`

Reasoning level: Extra High freezes the attempt identity, native-process,
activation, terminal, timing, network-denial and cleanup envelope. High is
sufficient for deterministic implementation, the single execution, exact
review and clockwork closeout while this plan remains unchanged.

## Objective and authority

Yuri's standing uninterrupted-development authority admits exactly one new
offline, provider-free `@deepseek-ai/dsh@0.1.0-rc.7` native Harness process.
It must use the accepted HMR readiness lifecycle, corrected preterminal
activation vocabulary and finally-path timing design, reach the accepted
effective-tool guard, observe exactly `edit`, `glob` and `read`, emit one
sanitized guard terminal, exit cleanly and leave no owned process or disposable
root.

The process creates no agent, session or turn and makes no broker, model or
provider request. It is not a retry, resume or reclassification of
`native-composition-attempt-001`, whose bytes and causal classification remain
immutable.

## Exact owned surface

The tranche owns only:

- this plan and its threat-model delta;
- `scripts/deepseek_native_harness_provider_free_preterminal_observable_composition_recovery_boot.py`;
- `tests/test_deepseek_native_harness_provider_free_preterminal_observable_composition_recovery_boot.py`;
- the closed contract, schemas, single-run evidence, report and efficacy
  reading under
  `orchestration/continuity/deepseek-native-harness-provider-free-preterminal-observable-composition-recovery-boot/`;
- the normal Ariadne receipts, verifier packet, closeout, acceptance and Yuri
  summary for this operation.

The controller may import accepted provider-free lifecycle, guard and corrected
runner construction helpers. It may not modify those predecessors or any EMR4
product source.

## Frozen prelaunch construction

Before native launch the controller must:

1. require exact full 40-character planning, failed-attempt and accepted-source
   Git object IDs and prove each is an ancestor of the current task HEAD;
2. bind the exact accepted predecessor script, contract, failed-evidence,
   generated guard and corrected-runner bytes;
3. verify the exact local npm cache only, with online fallback, lifecycle
   scripts, audit and funding requests disabled;
4. create one collision-free controller-owned disposable root outside the
   repository;
5. materialize the pinned rc.7 package offline and verify its declared
   `lib/bin.js`, `--profile headless`, `--expose-internals`, HMR registration
   and two watched stock patch paths;
6. author exactly one `emr4-bounded-worker` preset containing one
   `@deepseek-ai/dsh-tool-fs` row and one
   `@deepseek-ai/dsh-tool-fs-search` row with
   `sampleOverCapGlobResults: false`;
7. write the accepted generated effective-tool guard and corrected runner as
   local ESM modules and verify their exact digests before use;
8. give the sentinel exclusive write ownership of a readiness ledger and the
   corrected runner exclusive write ownership of a separate activation ledger
   and terminal file;
9. write an initial profile patch that disables `headless-runner`,
   `code-runtime` and `session-telemetry-otel` and contains only the readiness
   sentinel; and
10. write but do not mount the changed patch, whose only additional row is the
    preterminal-observable composition runner.

No deterministic check or test may execute `lib/bin.js`, `--profile headless`
or an equivalent native Harness entry.

## Single native execution

The admitted execution is one package-declared native Harness process with:

- attempt ID
  `preterminal-observable-composition-recovery-boot-attempt-001`;
- exact Node `--expose-internals` and Harness `--profile headless` invocation;
- scrubbed credential and proxy environment;
- the accepted network guard preloaded through `NODE_OPTIONS`;
- telemetry, stock model runner and code runtime disabled;
- one sentinel-owned readiness ledger as the sole HMR readiness source;
- one atomic profile-patch replacement only after both watched stock paths are
  observed; and
- no broker process, provider credential, task prompt, product data or session
  request.

The corrected runner must remain byte-exact to the accepted deterministic
observability candidate. Its successful activation sequence is exactly:

1. `BOOTSTRAP_APPLY_ENTERED`;
2. `RUNTIME_MODULES_IMPORTED`;
3. `SCOPE_CREATED`;
4. `GUARD_ENTRY_REACHED`;
5. `GUARD_TERMINAL_REACHED`;
6. `SCOPE_DISPOSED`; and
7. `EXIT_REQUESTED`.

The readiness sequence is independently exactly:

1. `sentinel_activated`; and
2. `stock_headless_hmr_ready`.

The successful terminal is exactly:

- schema `ariadne.deepseek_native_harness_effective_tool_native_boot_terminal.v1`;
- stage `preterminal_activation`;
- code `EFFECTIVE_TOOL_COMPOSITION_PASSED`;
- detail `null`;
- effective tool names `edit`, `glob`, `read` in sorted order; and
- effective tool count `3`.

Any admitted activation or guard failure coordinate is useful bounded failure
evidence but not acceptance. Unknown text, exception messages, stacks, prompts,
payloads, paths, environment values and credentials are never retained.

## One-run and cleanup latch

All defects found before process start may be corrected through deterministic
tests. The first native process start consumes
`preterminal-observable-composition-recovery-boot-attempt-001`. There is no
automatic or manual second native process, retry, resume, same-attempt
reclassification or reuse of its output namespace. Any mismatch or non-passing
terminal stops this attempt after bounded termination and cleanup.

Elapsed time must be assigned in a `finally` path for every started process,
before termination escalation and cleanup. The controller must wait for exact
process absence, remove only its verified disposable root and prove that root
absent. It retains bounded digests, versions, readiness and activation names,
terminal projection, counts, duration, exit code and cleanup booleans. Raw
stdout/stderr bodies, environment, npm cache, package source, disposable paths
and transient preset/plugin files are not retained.

## Deterministic admission and acceptance

Before the native process can start, tests must prove:

1. exact full predecessor object IDs, script/contract/evidence digests, package
   identities, guard digest and corrected-runner digest;
2. exact preset, separate ledger ownership, initial/changed patch and generated
   module shapes;
3. one process launch, one atomic post-readiness mutation, one guard call, one
   exclusive terminal write, one scope disposal and one exit request;
4. rejection of attempt-ID reuse, own-layer tools, missing inherited tools,
   surplus/missing/duplicated schema, reordered selected input, unsafe terminal
   data, duplicate/reordered/partial ledgers, early patch mutation, duplicate
   process, ambiguous exit, network attempt and incomplete cleanup;
5. duration assignment in `finally` before termination and root cleanup;
6. zero agent/session/turn, broker, model, provider, occupied-worker, Docker and
   database paths; and
7. focused and neighbouring provider-free tests, Ruff, compile, JSON/schema
   validation and Git whitespace.

Acceptance requires the sole new native terminal to pass, exit `0`, record the
exact readiness and activation sequences, record a reliable non-negative
duration, prove all prohibited-call counts zero and complete cleanup. Only
after that exact clean candidate exists may one fresh Gemini 3.7 Flash/high
isolated read-only veto run. A P0-P2 finding requires bounded source or evidence
repair, but it can never authorize a second native process. Any repair after a
consumed passing terminal must preserve every byte whose digest the terminal
evidence binds.

## Explicit parallelism assessment

- **DeepSeek native Harness/model lane:** `declined` as worker and reviewer,
  negative leverage. rc.7 is the provider-free subject; a model/provider call
  or occupied worker would violate the boundary under proof.
- **Gemini:** `reserved`, required independent leverage. It owns one fresh
  exact-candidate read-only veto only after deterministic admission, the single
  passing native terminal and complete cleanup.
- **Native subagents:** `declined`, negative leverage. Current developer policy
  prohibits proactive delegation, and launch, readiness, activation, terminal,
  duration, process absence and cleanup are one serial ownership chain.

Sol owns planning, implementation, native-process execution, monitoring,
cleanup, acceptance, recovery, clockwork and Git. Reassess all lanes at plan
freeze, preexecution, native terminal, pre-verifier and closeout.

## Claim, governance and protected boundaries

This tranche can prove only the pinned local rc.7 native pre-provider
composition path and its bounded observability. It cannot prove DeepSeek
reasoning, coding quality, provider reliability, occupied-worker completion,
future Harness versions, production isolation or product suitability.

The clockwork remains the sole canonical governance writer. Run a separate
`--check` before `--publish`; never manually edit canonical continuity,
Compass, baton, latch or error-register projections.

No attempt 004 retry/resume/reclassification or attempt 005; no agent/session/
turn, broker/model/provider call, credential, external network, Docker,
PostgreSQL, SQL or database execution; no product source/configuration/API/
OpenAPI/GraphQL/schema/client/waiting-area change; no ordinary-practice
enablement, feature flag, allowlist, command mount, generic-status `Arrived`,
action grammar or first-party client change; no product, patient, appointment,
clinical, historical or protected data; no production, deployment, release,
Pages, protected evidence or protected-ref movement.

Local/origin `master` and `handoff/current` remain exactly
`2e34bdad732fdab32fbf778280b3d3c70d66d602`. Preserve `docs/branding/` and
every unrelated untracked file. Stage explicit paths only; `git add .` and
`git add -A` are forbidden. At closeout write the paired Yuri summary, send the
usual non-PHI Pushover notification and continue under standing authority.
