# DeepSeek native Harness provider-free rebound future-runner stock-headless HMR boot proof plan

Date: 2026-08-21

Timestamp: 2026-08-21T23:42:28.7565979+10:00 (Australia/Brisbane)

Status: **frozen before execution**

Reasoning level: **Extra High** for the single-process containment and evidence join.

## Objective

Run exactly one locally materialised `@deepseek-ai/dsh@0.1.0-rc.7` native
Harness process and prove that the exact accepted rebound future runner and
helper traverse the stock-headless HMR boundary to the deliberately selected
typed `preset_root_roster_mismatch` subcoordinate. Join that sidecar with an
independently written, identity-bound, all-zero broker reading and emit exactly
one canonical controller terminal.

This is a provider-free control proof. It must stop before `agents.create`,
session creation, a turn, broker traffic, a model/provider request, or creation
or use of the inert target.

## Frozen identities

- Operation: `deepseek-native-harness-provider-free-rebound-future-runner-stock-headless-hmr-boot-proof`
- Native execution attempt: `rebound-stock-headless-hmr-boot-attempt-001`
- Accepted rebinding review: `5f30c69d8cd0ad6f4c01f33b465fe7b541ef1f47`
- Accepted planning source embedded by the rebound bundle: `deaf0b3ccc23adb2f2f17b275a6a7faa4d2ae2ac`
- Embedded bundle operation: `deepseek-native-harness-provider-free-future-attempt-identity-and-target-rebinding-rehearsal`
- Embedded bundle attempt: `future-identity-target-rebinding-fixture-001`
- Rebound runner SHA-256: `d025d1929f20589e86cbd66e983bda17435c1a21e937468532c38bb4895efb1a`
- Rebound helper SHA-256: `7ff2630b1523c66bb9e0cb5d649879cfa97d0340b791a85255c7dc5ad1fb66fd`
- Accepted controller SHA-256: `29495710c5e95a12fba92a2ef34c28ed6b9ce5e034402e5551767e19b843014c`
- Target: `workspace/authored_synthetic_control_probe.py`
- Target-coordinate SHA-256: `f1a4634e24e1cde4bcddfcc88e932a44d75843f6c8a47c1568599775f219c72d`

The native execution-attempt identity describes this one process. The embedded
bundle identity remains unchanged because changing it would change the exact
accepted helper and violate the subject of the proof.

## Pinned Harness package

- Package: `@deepseek-ai/dsh`
- Version: `0.1.0-rc.7`
- Bin: `lib/bin.js`
- Tarball SHA-1: `8a69013c06179d7af437de92fb4a9a2e1fd7d410`
- Tarball integrity: `sha512-ZceDCJ8FAywih+USW/OMk9jEhunlvJBGEz4kqrhau23hPzbciOazZrywH0nBRsaalSeAJ1JGBmjtw4OSjToStw==`
- Tarball SHA-256: `2f8f0b763d611ac536f7a9411ee43c0afc067c1b8732c3102c04dbe398bcacc5`
- Source: the already verified local npm cache only; installation is offline.

## One-root layout

The controller creates one fresh disposable parent outside the repository. It
contains two purpose-separated descendants:

1. `bundle/<embedded-attempt-id>/` is produced only through the accepted rebound
   materialiser. Its closed six-path roster remains authoritative and no extra
   execution module may enter it.
2. `installation/proof/` holds execution copies of the exact runner and helper,
   plus the accepted effective-tool guard and readiness sentinel. The two copies
   must be byte-for-byte and hash equal to their canonical bundle counterparts.

This separation is mandatory. It permits native module resolution while keeping
the canonical bundle readable by the accepted controller after the process.

## Deterministic prelaunch gates

Before `Popen`, the controller must:

1. validate this frozen plan and the tranche contract against their schemas;
2. resolve every Git source as a full 40-character commit and prove the accepted
   sources are ancestors of `HEAD`;
3. verify the exact package cache identity, materialise it offline, and verify
   installed source and dependency versions;
4. reproduce the exact accepted rebound runner and helper through the accepted
   rebinding path, materialise the canonical bundle, and read it back;
5. prove execution-copy equality for runner and helper and validate the accepted
   effective-tool guard and readiness sentinel;
6. write the canonical identity-bound all-zero broker reading exclusively;
7. prove the inert target is absent;
8. scrub credential and proxy variables and preload the accepted network guard;
9. validate initial and changed HMR patches, including exact service injection,
   paths, and the deliberate mismatch values; and
10. prove that neither canonical evidence output already exists.

Any prelaunch rejection consumes no native attempt and produces no canonical
attempt evidence.

## Single native lifecycle

1. The initial headless patch disables `headless-runner`, `code-runtime`, and
   `session-telemetry-otel`, and mounts only the accepted readiness sentinel.
2. The controller starts exactly one native Harness process with stock-headless
   HMR enabled and with stdout and stderr discarded, not retained or parsed.
3. The sentinel must exclusively emit `sentinel_activated`, then
   `stock_headless_hmr_ready`, after observing both exact watched patch paths.
4. Only after that exact prefix does the controller atomically replace the patch
   with one adding the official `@deepseek-ai/dsh-agent-presets` service and the
   exact rebound runner with injection
   `hmr, headlessStartup, agents, sessions, agentPresets`.
5. Runner configuration supplies existing absolute `diagnosticPath` and
   intentionally nonmatching absolute `shippedRoot` and `userRoot` coordinates.
   Therefore the exact runner must pass service lookup and fail at
   `preset_root_roster_admission` with
   `preset_root_roster_mismatch`, before `agents.create`.
6. `terminalPath` is deliberately bound to the existing canonical bundle
   manifest. The exact runner's exclusive generic terminal write must fail closed
   and must not alter the manifest. No runner-authored fallback terminal is
   accepted.
7. Once the exact typed sidecar is fully validated, the controller terminates the
   still-live native process once if needed, waits for process absence, and does
   not retry or resume.
8. The controller verifies the bundle manifest digest is unchanged, verifies the
   target remains absent, and assembles the sole canonical typed controller
   terminal from the sidecar plus broker-zero reading.
9. Only bounded typed projections and digests enter canonical evidence. The
   disposable root and any native artifacts are removed completely.

## Required terminal

The accepted terminal must have:

- coordinate `post_hmr_pre_request_failure`;
- `diagnostic_accepted: true`;
- `broker_zero: true`;
- `pre_request_supported: true`;
- stage `preset_root_roster_admission`;
- cause `preset_root_roster_mismatch`;
- error kind `error`;
- the exact rebound source and target bindings;
- `occupied_launch_authorized: false`;
- `raw_stream_read: false`; and
- `raw_error_retained: false`.

No other terminal or free-form failure coordinate may satisfy acceptance.

## Acceptance checks

Acceptance requires all of the following:

- exactly one fresh disposable root and one native process;
- no retry, resume, fallback process, alternate runner, worker, broker process,
  model request, provider request, database or Docker invocation;
- exact readiness order and exactly one post-readiness HMR mutation;
- exact runner/helper execution-copy and canonical-bundle hash equality;
- exact sidecar and controller-terminal relationship above;
- independently canonical all-zero broker counters;
- zero network attempts under the preloaded guard;
- zero target-file creation or use;
- runner fallback-terminal absence and unchanged bundle manifest;
- process absence and disposable-root absence before evidence publication;
- focused deterministic tests and the relevant complete regression suite pass;
- exact test counts are reported only from completed machine output;
- no protected-ref movement and all unrelated untracked files remain preserved.

## Parallelism assessment

- DeepSeek lane: **declined**. The operation exists to prove the Harness boundary
  before a DeepSeek worker or provider request can be admitted.
- Gemini lane: **declined** unless the native terminal is semantically ambiguous.
  The frozen outcome is an exact local package, hash, lifecycle and typed-terminal
  relationship.
- Native-subagent lane: **declined**. Developer policy prohibits delegation, and
  the one mutable process plus its canonical terminal is a serial trust boundary.
- GPT Sol owns the plan, controller, single execution, evidence join, verification,
  acceptance, clockwork closeout and next-tranche reorientation.

## Explicit exclusions

This tranche authorises no occupied DeepSeek worker, model/provider call, target
creation, product source or configuration change, API, database, route, adapter,
feature flag, allowlist, action grammar, first-party client, waiting-area or
ordinary-practice change. It authorises no generic-status `Arrived` change; no
product, patient, appointment, clinical, historical or protected data; and no
production runtime, deployment, release, Pages rebuild or protected-ref movement.

