# DeepSeek native Harness preset-mount sanitized-terminal native rehearsal plan

Date: 2026-08-22

Timestamp: 2026-08-22T06:36:06.5611095+10:00 (Australia/Brisbane)

Status: **frozen before implementation or execution**

Reasoning level: **Extra High** for the first native process carrying the exact
admitted preset-mount bridge.

## Objective

Materialise the exact accepted preset-composition runner, derived guard,
preset-mount bridge and sanitizer into one disposable package-only rc.7 root.
Run exactly one provider-free native Harness process and retain one finite typed
terminal before any turn, model selection capable of request, worker action or
provider call.

## Accepted basis

The runner-bridge rehearsal passes at exact execution candidate
`993a3b383aa79afba857bb53af29177bffacd566`. The earlier native safe-terminal
bridge reached the private factory and returned only
`EFFECTIVE_TOOL_COMPOSITION_PRESET_MOUNT_FAILED`. The new exact-hash bridge can
reduce the mount failure to seven closed stage/code/null-detail coordinates and
gives that grammar precedence over the broader fallback.

## Frozen implementation

- Reuse the accepted package-only seed, independent disposable copy, stock
  headless readiness sequence, HMR mutation count, private identity, target
  absence, broker-zero reading, network ledger and cleanup controls.
- Preserve the accepted runner-generator, guard-generator, sanitizer and bridge
  hashes exactly; materialise the derived runner and guard hashes from the
  accepted contract.
- Use the accepted five-key Windows minimum for any separately controlled local
  helper process. Never persist environment values.
- Admit exactly one native rc.7 process after a committed/origin-aligned
  candidate and fresh preexecution receipt.
- Persist the typed sidecar and a content-free outer process envelope before
  interpretation. Retain no raw message, stack, cause, path, stream, prompt,
  response, environment or credential.

## Finite terminals

The native process may end only at:

- `preset_mount_failure_attributed` with one of the seven admitted
  `PRESET_MOUNT_*` codes and null detail;
- `preset_composition_failure_attributed` with one accepted broader safe guard
  coordinate if the failure is outside mount;
- `prepublication_veto_diagnosed` if mount and composition pass to the already
  accepted exact publication stop; or
- `closed_subcoordinate_failure` / `runner_link_or_apply_absence` under the
  existing closed controller grammar.

Only the first terminal proves the new bridge's native runtime path. The other
finite terminals remain bounded diagnostic evidence and direct the next
source-owned recovery without retry under this plan.

## Process, request and stop rule

Native process count: one. Automatic/manual retry and resume: zero. Turns,
requests, broker requests, workers, model/provider requests, network, database,
Docker and target actions: zero. Any missing/invalid sidecar, unexpected exit,
raw detail, publication, request activity, seed/bundle mutation or incomplete
cleanup stops the tranche without another native process.

## Parallelism assessment

- DeepSeek lane: **declined**. The native Harness boot/terminal path remains the
  object under test; no worker or model request is authorised.
- Gemini lane: **declined provisionally**. Exact hashes, finite terminals and
  zero counters are deterministic; reassess only if the one-process evidence
  creates a genuine semantic ambiguity.
- Native-subagent lane: **declined**. Developer policy prohibits delegation and
  the single disposable process is serial.
- GPT Sol owns implementation, preexecution, the one native process, readback
  and closeout.

## Explicit exclusions

No occupied worker turn, DeepSeek/model/provider request, retry, target edit,
product/configuration/API/database/route/adapter/feature-flag/allowlist/action-
grammar/first-party-client/waiting-area change, ordinary-practice enablement,
generic-status `Arrived` change, patient/product/clinical/historical/protected
data, production, deployment, release, Pages, protected evidence or protected-
ref movement is authorised.
