# Raisa Word Online authenticated companion verification plan

Date: 2026-07-31

Status: `executed_terminal_pass`

## Authority

This is the terminal continuation of the authorised
`raisa-cloud-run-public-access-word-online-verification` node. Yuri enabled the
documented Chrome file-upload prerequisite and manually selected the already
validated task-specific manifest. The existing authority permits one
supervised, authored-synthetic Word Online companion check and ordinary
repository-local repair on only:

- project `bernie-emr4-dev`;
- region `australia-southeast1`;
- service `raisa-office-web-dev`; and
- public origin
  `https://raisa-office-web-dev-nnbntbx5yq-ts.a.run.app`.

No other service, IAM policy, organisation policy, identity, region or Office
tenant setting may change.

## Frozen boundary

The exercise must remain:

- authored-synthetic and provider-free;
- backend-free, database-free and credential-free;
- read-only with no document-body read or write;
- appointment-command-free and confirmation-free;
- bound to the existing typed companion request and summary contracts;
- detailed only in the native Diary, with Word receiving a generic admitted
  summary; and
- default-off behind the exact companion capability.

The manifest remains `ReadDocument`. It grants no backend, provider,
credential, microphone, command, appointment-write, document-write,
production or release authority.

## Deterministic repair

The first uploaded-manifest run reached the Office dialog but failed closed:
the public taskpane admitted the exact zero-authority hosting policy while the
Diary still admitted `smoke` and `reception_one_companion_demo` only on
loopback.

The repair:

1. loads the same origin-bound hosting policy in the Diary;
2. requires the exact `public_https_development` policy, authored-synthetic
   classification, exact current origin and all seven authority fields false;
3. allows only `smoke` and `reception_one_companion_demo` on that hosted
   boundary; and
4. keeps every other local harness, product-context, provider, backend and
   command capability closed.

No synthetic authentication token is invented. The typed request, proofreader
and generic summary contract are unchanged.

## Acceptance

- The validated manifest loads in the existing signed-in Word Online session.
- One short authored-synthetic request reaches the native Diary through the
  Office dialog.
- The Diary admits the exact typed request and retains all appointment detail.
- Word receives only a proofreader-admitted generic summary.
- The request text and synthetic patient detail are absent from the Word
  summary.
- Taskpane and Diary observe zero API, credential and provider requests.
- No document body, backend, database, command or confirmation path runs.
- Closing the dialog leaves the generic Word summary intact.
- Both task-created blank documents are removed from active OneDrive storage
  and remain recoverable in the Recycle Bin.
- All task-owned local contexts, image tags, containers, networks and temporary
  Docker credential directories are absent.

## Claim boundary

This can prove task-specific manifest execution and the bounded companion
exchange in one signed-in personal Word Online session. It cannot prove EMR4
application authentication, clinician-role authorization, organisational
tenant deployment, safety for real or product-derived data, provider behavior,
physical processing geography, production fitness or release readiness.
