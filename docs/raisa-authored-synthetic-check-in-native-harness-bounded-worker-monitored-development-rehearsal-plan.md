# Authored-synthetic check-in native Harness bounded-worker monitored development rehearsal plan

Date: 2026-08-20

Timestamp: 2026-08-20T20:46:14.4461645+10:00 (Australia/Brisbane)

Status: `frozen`

Operation:
`raisa-authored-synthetic-check-in-native-harness-bounded-worker-monitored-development-rehearsal`

Reasoning level: Extra High freezes the provider, one-request, tool, evidence
and cleanup boundaries. High is sufficient for bounded implementation,
deterministic verification, independent veto, checkpoint, occupied execution
and closeout while this plan remains unchanged.

Machine-bound planning source:
`orchestration/agent_inbox/codex/raisa-authored-synthetic-check-in-native-harness-bounded-worker-preplanning-receipt.json`.

## Purpose

Run the first occupied descendant of the accepted provider-disabled rc.7
preset-mount and exact effective-tool projection proof. One newly identified
DeepSeek Flash worker receives one authored-synthetic, non-product coding work
package in a disposable workspace through the accepted `emr4-bounded-worker`
preset, exact `edit`, `glob`, `read` model-facing view and Ariadne broker gears.

The work package is deliberately shaped for one provider-admitted model
request. It supplies the complete small source context and asks for one exact
literal `edit`. A successful edit must carry rc.7's `concludesTurn` marker so
the Harness ends the turn after committing that tool result, without a second
model request for a summary. Deterministic controller evidence, rather than
model prose, judges the changed file and runs the authored-synthetic tests.

This is a monitored development rehearsal, not ordinary EMR4 implementation.
The words “check-in” name the programme lineage only; no product check-in
route, adapter, status, patient or appointment material enters the worker
workspace or provider request.

## Exact accepted floor

- Accepted preset-mount/effective-tool candidate:
  `c8b0e3a587191b65da212edda36b8b833a2ecc2c`.
- Accepted closeout source:
  `109aeed0b4794fa8c4f98f78c71021d7b4562c32`.
- Accepted clockwork source and present planning parent:
  `1307c5f0fc22b86c999616afa4f0c4e2055b8908`.
- Pinned official Harness package: `@deepseek-ai/dsh@0.1.0-rc.7`, with the
  accepted retained package-lock, installed-source and package-member digests.
- Canonical preset: `emr4-bounded-worker`, 158 bytes, SHA-256
  `3de182eb702e6f2b397941c73393b87f65acb9b401565f966059d2bd46f649d1`.
- Exact effective model-facing names: sorted `edit`, `glob`, `read` and no
  others; maximum parallel tool calls: one.
- Existing native, broker and occupied-attempt evidence remains immutable.

## Frozen work package

The disposable workspace contains one tracked file,
`synthetic_window_coalescer.py`, plus repository metadata and no other source.
It models generic maintenance-window intervals and contains its own executable
authored-synthetic case table. The seeded implementation incorrectly shrinks a
previously extended interval when a nested interval is processed. The worker
must make one literal edit that:

1. preserves the public function name and input/output shape;
2. replaces the shrinking end assignment with the maximum of the existing and
   incoming ends;
3. updates the adjacent explanation so it describes the invariant accurately;
4. appends exactly one named nested-window regression row to the case table;
5. changes no other path and creates no file; and
6. uses neither product vocabulary nor repository content.

The exact baseline bytes, expected changed bytes, public case table, private
authored-synthetic holdback cases and their SHA-256 digests are controller-owned
contract data. The prompt includes the complete baseline source and literal
edit requirement, so no preliminary read is necessary. A `read` or `glob` call
is allowed by the accepted tool view but cannot satisfy the one-request success
path because a tool result would conclude the turn without the required edit.

## Frozen stages

### Stage 1 — source-proved one-request turn conclusion

Build one provider-free controller and focused tests. Before any occupied
process, prove from the pinned rc.7 source and deterministic fixtures that:

1. the accepted preset mounts and projects exactly `edit`, `glob`, `read`;
2. the official `edit` tool performs a literal filesystem edit and produces a
   normalized success result;
3. the runtime execution object can mark that success with `concludesTurn`;
4. the agent loop commits the tool call and result, observes the marker and
   completes the turn without assembling a second request;
5. only one direct model-requested tool call is admitted and parallel width is
   one; and
6. failure, non-edit selection, multiple tool calls or an unsuccessful edit
   cannot acquire the conclusion-success coordinate.

The concluding listener may observe and classify only the exact tool name,
call identity, safe result state and owned target path. It may not alter tool
arguments, file content, output value or error handling.

### Stage 2 — deterministic work-package and broker gears

Materialize the exact one-file baseline in a disposable Git repository under
`C:/Users/sarashera/EMR4-worktrees/`. Generate a WorkOrder v2 and clockwork
event seed binding the full 40-character candidate source, authority,
forbidden surfaces, exact disposable root, profile/preset/tool identities,
prompt digest, baseline and expected digests, one worker identity, one admitted
model request, zero automatic retries, zero fallback and zero auxiliary model
calls.

The occupied broker descendant must retain exact authentication, session,
path, model, streaming, request/response byte, output-token, concurrency and
`edit`/`glob`/`read` schema checks. For this rehearsal it additionally admits
provider-call ordinal one only. Any later request is rejected before provider
I/O and makes the attempt a bounded negative result; it is never reclassified
as success. The worker receives the broker capability but never the provider
credential. The broker receives no worker filesystem mount.

### Stage 3 — provider-free hostile characterization

With provider credentials absent and external I/O denied, exercise exact
success doubles and hostile variants covering:

- wrong work order, digest, source, root, worker, session, model or tool view;
- missing, duplicate or surplus tool schemas;
- malformed, multiple, non-edit or wrong-path tool calls;
- edit failure, no-op, create, delete, rename or unexpected path change;
- absent or forged `concludesTurn` evidence;
- attempted request ordinal two, retry, fallback or auxiliary model route;
- raw prompt, response, reasoning, credential or environment leakage;
- output, event, terminal or cleanup path already present; and
- escaped disposable root, surviving process or incomplete cleanup.

These tests must prove zero provider requests. Durable failure evidence uses
closed coordinates, counts and digests only.

### Stage 4 — exact candidate review

Run schemas, focused and neighbouring provider-free tests, Ruff, Python
compilation, Node syntax checks, deterministic work-package tests and the
hostile matrix. Use deterministic code for exact argv, results and digests.

One fresh Gemini 3.7 Flash/high isolated read-only semantic veto is required
over the exact clean candidate. Gemini owns technical judgment only and has no
implementation, occupied-execution, recovery, acceptance, Git or product
authority.

### Stage 5 — separate clockwork occupied checkpoint

Only after the exact candidate and Gemini veto pass may the exclusive
clockwork publish a checkpoint binding:

- full candidate and review Git object identities;
- package, preset, profile, guard, listener, controller and work-package
  digests;
- exact WorkOrder, broker and event-chain identities;
- one worker process, one session, one turn and one provider-admitted model
  request;
- one model-requested tool call, which must be `edit` on the exact owned file;
- zero automatic retry, fallback, auxiliary model call or second worker;
- timeout, terminal, owned-process and disposable-root cleanup requirements;
  and
- the exact non-product, provider and protected-data boundaries.

Before this checkpoint, provider request count remains zero.

### Stage 6 — one occupied native worker attempt

The checkpoint admits exactly one new native Harness process and worker
identity. The ordered success path is:

1. clockwork and broker ready under the admitted WorkOrder;
2. preset mounted and exact effective tools verified;
3. one DeepSeek Flash request accepted and completed through the broker;
4. its response contains exactly one `edit` call for the owned file;
5. the official tool records one successful call/result pair;
6. the concluding listener marks that successful edit terminal for the turn;
7. the agent completes without a second model request;
8. controller readback matches the exact changed-path and behavior contract;
9. public and holdback authored-synthetic cases pass; and
10. immutable sanitized terminal, process absence and literal-root cleanup are
    recorded.

First occupied process creation consumes the allowance. Any provider error,
timeout, missing or surplus tool call, unsuccessful edit, request ordinal two,
unexpected change, test failure or cleanup failure writes one failed-closed
terminal and forbids retry. A negative result remains useful harness evidence
but cannot be described as successful worker execution.

## Acceptance and claim boundary

The tranche passes only if one DeepSeek provider request produces one exact
successful native `edit`, rc.7 concludes the turn from that tool result with no
second request, deterministic code verifies the exact one-file candidate and
all authored-synthetic cases, the broker/event/tool/session accounts agree,
and cleanup completes. There are zero retries, fallbacks, auxiliary model
calls, extra workers or caller-derived Git identifiers.

A pass proves only one carefully shaped authored-synthetic non-product coding
edit under the pinned native Harness, accepted preset/tool projection and
Ariadne broker gears. It does not prove general worker reliability, multi-turn
development, ordinary EMR4 implementation, product or database behavior,
production isolation, security accreditation or deployment suitability.

## Parallelism assessment

- **DeepSeek Flash:** planned with positive leverage. It owns exactly the one
  authored-synthetic non-product coding edit after deterministic gates, review
  and checkpoint.
- **Gemini 3.7 Flash/high:** reserved with required independence for one exact
  candidate semantic veto before occupied dispatch.
- **Native subagents:** declined with negative leverage. Current developer
  policy prohibits proactive delegation, and source binding, one-request
  lifecycle, evidence ordering and cleanup are irreducibly serial.
- **GPT Sol:** owns plan, controller/tests, candidate, verifier dispatch,
  checkpoint, process monitoring, deterministic judgment, recovery,
  acceptance, clockwork and Git.

## Deliberately closed

No second worker or retry without a distinct Yuri attention decision; no
attempt 006; no Docker, PostgreSQL, SQL, transaction or database work; no
product source or configuration; no API, OpenAPI, GraphQL, schema, migration,
route, adapter, feature flag, allowlist, command grammar, first-party client or
waiting-area change; no ordinary-practice enablement or generic-status
`Arrived`; no product, patient, appointment, clinical, historical or protected
data; no production runtime, deployment, release, Pages or protected-ref
movement.

Local/origin `master` and `handoff/current` remain exact
`2e34bdad732fdab32fbf778280b3d3c70d66d602`. Preserve `docs/branding/` and
every unrelated untracked file. Stage explicit paths only; never use
`git add .` or `git add -A`.
