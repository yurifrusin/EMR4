# Ariadne agent-error register revision 85

Date: 2026-08-07

Status: disposable PostgreSQL planning active

## AER-0092 is contained

During plan-only environment orientation, Sol issued one `rg` command over
broad `scripts`, `tests`, `docs` and `orchestration` roots. The query sought
ordinary PostgreSQL harness and executable-location facts. It returned only
ordinary repository paths and no protected content, and it contacted no
database, SQL runtime, network or provider. The search scope itself nevertheless
repeated the exact-path containment failure recorded as AER-0054.

The result is excluded from architectural authority. The one independently
obtained exact `Get-Command` fact establishes only that the Docker client
executable is locally present; it does not prove daemon or image readiness.
Every further source read in this tranche is limited to a path named by AGENTS,
the accepted parent documents or the new plan. Runtime readiness becomes a
fail-closed preflight inside the frozen plan rather than a reason for broader
discovery.

## Register posture

Revision 85 contains 92 bounded incidents: 74 agent-behavior observations,
five harness failures, five repository defects and eight transport timeouts.
No incident is open. The recurring
`orchestrator.overbroad_repository_content_search` signal now binds AER-0054
and AER-0092 and requires exact-path allowlists before any future environment
discovery.

This register change supplies no PostgreSQL, SQL execution, migration,
database/source, product/patient data, application/runtime, command, provider,
deployment, production, release, Pages or protected-ref authority.
