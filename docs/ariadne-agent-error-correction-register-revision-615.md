# Ariadne agent error and correction register — revision 615

Date: 2026-08-22

Timestamp: 2026-08-22T13:30:09.9126535+10:00 (Australia/Brisbane)

<!-- ariadne-agent-error-register-reading
revision: 615
incident_count: 953
new_incident_ids: AER-0944,AER-0945,AER-0946,AER-0947,AER-0948,AER-0949,AER-0950,AER-0951,AER-0952,AER-0953
open_incident_count: 0
-->

This revision adds ten bounded incidents from the first useful occupied
native-Harness worker tranche. Six were contained before the provider turn,
one is the immutable failed-closed occupied result and two were rejected
closeout drafts before publication. The tenth is a byte-budget rejection of an
overlong rolling acceptance path list. None is open and none authorises a retry.

## AER-0944 — Ariadne Git-ref prose manually restated machine identities

The first preplanning receipt rejected hand-authored Git-ref prose containing
object identities. The corrected runtime leaves every Git identity to the
receipt builder's machine snapshot. No worker or provider process started.

## AER-0945 — controller expected an obsolete deterministic receipt shape

The preparation controller expected direct `head_before`, `head_after` and
`dirty_after` fields while the admitted validation runner emits a hashed
command ledger. Preparation was not attempted with a fabricated compatibility
receipt. The controller now validates the manifest/result ledger, resolves the
full source object from its hash and binds the passing tracked-clean command.

## AER-0946 — checkpoint equality could not accommodate its own evidence commit

The first checkpoint rule required the clockwork source to equal the reviewed
candidate even though committing the immutable review receipt necessarily
creates one evidence-only descendant. The rule now admits only that exact
single-path descendant and rejects every product or control-source delta.

## AER-0947 — cleanup delegated to a foreign attempt-name guard

The controller reused a predecessor cleanup helper whose exact allowed attempt
name differed. A preexecution cleanup rehearsal rejected the mismatch. The
tranche now owns and tests its exact cleanup name and containment boundary.

## AER-0948 — persisted checkpoint loader omitted its new source field

The first loader expectation omitted `checkpoint_source` after checkpoint
ancestry was strengthened. A persisted-checkpoint test rejected the mismatch
before attempt consumption. The loader now revalidates and includes the exact
field.

## AER-0949 — process-absence query counted its own PowerShell command

The first generic command-line query returned one match because its own
PowerShell command contained the searched literals. Readback identified the
match as `pwsh.exe`; the corrected control filters the owned runtime class to
`node.exe` and proved zero prelaunch processes.

## AER-0950 — occupied edit result did not produce or conclude a candidate

The one consumed native-Harness turn reached both HMR coordinates, completed
one DeepSeek request and returned one `edit` tool result, but changed no path,
did not mark conclusion and ended with `turn_kind: error`. The broker rejected
the later request at the one-request ceiling. No candidate was retained; retry,
resume and fallback stayed zero; cleanup completed. The separately named
provider-free successor will type the tool-result/conclusion seam before any
new occupied authority is considered.

## AER-0951 — closeout node carried two parent relationships

The first closeout intent named both the immediate Harness predecessor and a
product-readiness ancestor. The transaction manifest permits exactly one
`builds_on` parent. The clockwork rejected the intent before commands or
canonical mutation; the corrected node retains only its immediate predecessor.

## AER-0952 — closeout invented a descriptive Baton acceptance label

The second closeout intent used a descriptive result label instead of the
manifest-listed rolling `Current DeepSeek native Harness acceptance` slot. The
clockwork rejected it before publication. The corrected intent writes only the
closed rolling slot and leaves result detail in the node and closeout artifacts.

## AER-0953 — rolling acceptance path list exceeded the live Baton budget

The third closeout intent put the complete evidence inventory into the compact
live rolling acceptance row. The clockwork rejected the prospective Baton at
its 80,000-byte ceiling before publication. The corrected rolling row names
only eight controlling artifacts; the complete inventory remains in the
Continuity node and closeout intent.

## Register reading

These incidents reinforce the intended clockwork direction. Git identities,
receipt compatibility, evidence-only ancestry, exact cleanup and persisted
checkpoint shape now have deterministic controls. The occupied failure remains
honest negative evidence: containment and traceability improved, but useful
worker efficacy did not yet pass.
