# Provider-free Office reload and terminal reconciliation plan

Date: 2026-08-03

Status: authorised first lifecycle descendant

Parent: `provider_free_office_practitioner_directory_consumer_pass`

## Contract

The first taskpane delivery may carry one session, CSRF value and result nonce.
Every later delivery for that surface must revoke any surviving session before
returning an explicit inert page with no endpoint or opaque launch material and
expired task cookies. A page restored from browser history clears its in-memory
material and remains inert. The visible action is one-use and in-flight guarded.

## Acceptance

Fresh authored-synthetic PostgreSQL evidence must show one ready delivery, two
inert replays, one successful revocation, zero revocation failures, denial of a
request carrying the stale page's original cookie/CSRF pair, zero product reads,
and complete disposable cleanup.

## Limits

No new route outside the task harness, product resource, provider, identity,
patient/clinical/document access, command, deployment or release is authorised.
