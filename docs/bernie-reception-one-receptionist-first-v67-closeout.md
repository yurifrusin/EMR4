# Reception One Receptionist-first v6.7 Closeout

Status: complete; occupied cohort did not pass at 23/24 exact outcomes
Recorded: 2026-07-30

## Result

The complete twenty-four-request authored-synthetic regression ran through the
exact bounded Sydney Vertex lane. Twenty-three terminal results matched their
frozen deterministic oracle. One did not, so the tranche result is
`reception_one_receptionist_first_v67_full_cohort_fail_closed`.

All twenty-five opened ledgers are consumed, every turn and case audit chain
verifies, no call followed the terminal result and task-scoped residue is
clear. The mismatch released no value.

## What improved

- The sole v6.6 mismatch, `b-clarify-fit`, passed on its primary call.
- Its natural response used the newly taught exact clarification pattern while
  the typed clarification form and deterministic proofreader remained frozen.
- Twenty-three create, move, resize, cancel, status, squeeze-in and
  clarification cases passed.
- The occupied run used only one correction call across the whole cohort.

## Remaining mismatch

`b-move-shift` used the exact move typed form and admitted operator note on
both calls. Its natural response used none of the frozen deterministic move
markers, so the proofreader first requested revision and then edge-aborted.
The discarded response text was not retained and no stronger wording
diagnosis is claimed.

The bounded descendant repair is to teach one explicit
proofreader-compatible move sentence pattern while retaining the exact
no-booking boundary. It must not loosen the proofreader, change the typed form
or oracle, alter the provider lane, or special-case the raw utterance.

## Calls and controls

- 24 primary calls and 1 terminal second call; 25 total against a ceiling of
  48;
- all 25 provider responses completed with HTTP 200;
- 115,173 prompt, 12,627 visible candidate, 15,237 thinking and 143,037 total
  tokens reported by Vertex;
- exact `gemini-2.5-flash`, `bernie-emr4-dev`, Bernie impersonated service
  account, `australia-southeast1` and regional hostname throughout;
- no API key, service-account key, global endpoint, fallback, provider tool,
  grounding, retrieval, cache creation, product/database access, write or
  delivery; and
- raw prompts, raw provider responses, credentials, API-key information and
  hidden chain-of-thought were not retained.

## Claim limit

This evidence proves twenty-three exact safe outcomes and one bounded
move-channel vocabulary mismatch over a reused authored-synthetic cohort
through the configured and observed Sydney locational path. It does not prove
independent generalisation, production fitness, Australian physical or
sovereign processing, or safety for real, product-derived, patient, health,
clinical, protected or historical data.
