# DeepSeek native Harness guard–bridge import-closure recovery rehearsal plan

Date: 2026-08-22

Timestamp: 2026-08-22T11:01:16.5622642+10:00 (Australia/Brisbane)

Status: **frozen before implementation or process execution**

Reasoning level: **Extra High** for correction of the accepted negative-evidence
diagnosis and the one distinct recovery process.

## Objective

Recover the demonstrated package-unloaded guard–bridge graph without retrying
its consumed process. Before one distinct Node process may start, derive every
static relative module specifier from the exact accepted guard, bridge,
sanitizer and corrected authored-synthetic fixture; resolve each target against
the disposable inventory; and prove the resulting graph is closed.

Materialize the unchanged accepted bridge bytes under the exact guard-owned
target `preset-mount-sanitizer-runner-bridge.mjs`, then exercise the same exact
success, missing-service and missing-mount matrix. A pass advances directly to
the complete package-unloaded runner evaluation.

## Accepted basis and corrected source reading

The predecessor at Continuity 389 / Compass 371 preserves one immutable
content-free process terminal and complete cleanup. Its source diagnosis
correctly found that the guard imports
`./preset-mount-sanitizer-runner-bridge.mjs` while the accepted bridge bytes
were materialized under a different basename.

Fresh preplanning source inspection also found literal leading `+` patch
markers embedded in the predecessor fixture generator. Because raw stderr was
deliberately unavailable, the absent import target remains a proved closure
defect but is not accepted as the exclusive process cause. The recovery must
therefore correct both source-proved defects before its process boundary:

- materialize the bridge at the import-owned target; and
- generate the fixture with no patch-marker prefix or other source-coordinate
  mutation.

This correction narrows the predecessor's causal claim; it does not alter its
accepted terminal, cleanup, process count, source hashes or closed authority.

The accepted executable module identities remain unchanged:

- derived guard: 4,501 bytes, SHA-256
  `76029da0f9c030651fd10c0df16f4e75e86b2269d7560af7f94c74680f8598b9`;
- derived bridge: 1,661 bytes, SHA-256
  `3a49b28174eeefd77d7efe0a00498901ac6636b637ed9dfe60aba46980df1d0b`;
- accepted sanitizer: 2,439 bytes, SHA-256
  `12552925a600dc951afc30b9a738746499c7e2f4cefc9962bc05fb06780f158f`.

## Frozen static closure gate

The controller must decode every executable source as strict UTF-8 and extract
only static ESM `import ... from`, side-effect `import`, and `export ... from`
specifiers. Dynamic import, CommonJS `require`, URL, absolute-path, drive-letter
or backslash specifiers fail closed.

For every relative specifier, the controller must:

1. resolve it lexically against the importing module's relative parent;
2. reject traversal outside the disposable root;
3. require an exact file target already present in the frozen materialization
   map; and
4. record only importer, literal specifier, resolved relative target and target
   source identity.

The exact required relative edge set is:

1. fixture → `effective-tool-guard.mjs`;
2. guard → `preset-mount-sanitizer-runner-bridge.mjs`; and
3. bridge →
   `deepseek_native_harness_provider_free_preset_mount_safe_subcoordinate_sanitizer.mjs`.

The guard's two bare package specifiers must resolve only to the exact local
`@deepseek-ai/dsh-scope` and `@deepseek-ai/dsh-agent-presets` stubs and their
frozen manifests. Any other bare specifier fails closed.

The corrected fixture source must additionally satisfy exact source identity,
contain no line beginning with `+`, no carriage return and none of the existing
forbidden filesystem, environment, child-process, network, dynamic-import,
exception-message, stack or cause coordinates. These static gates run before
Node executable resolution and consume no process.

## Disposable inventory

Exactly eight files may exist:

1. `effective-tool-guard.mjs` with the accepted guard bytes;
2. `preset-mount-sanitizer-runner-bridge.mjs` with the accepted bridge bytes;
3. the accepted sanitizer under its exact relative target basename;
4. one corrected authored-synthetic fixture;
5. the local scope stub manifest and source; and
6. the local presets stub manifest and source.

No predecessor bridge basename, derived runner, installed package source,
package cache, symlink or additional JavaScript may exist.

## One distinct process and exact cases

After the static closure gate passes, launch exactly one absolute-path Node
process in a fresh disposable root with the accepted five-key Windows child
environment and no `PATH` or `NODE_OPTIONS`. It is recovery attempt 001 for this
operation and is not a retry of the consumed predecessor attempt.

The process must emit exactly one canonical JSON line and zero stderr bytes.
Its cases and admitted observations remain exactly those frozen by the
predecessor plan:

- success mounts once with exact receiver, context and preset identity, resolves
  the exact synthetic scope, views once, restricts once to `edit`, `glob`,
  `read`, and returns the exact four-field passed coordinate;
- missing service and missing mount each yield only the exported typed terminal
  `{stage: preset_mount, code: PRESET_MOUNT_UNCLASSIFIED, detail: null}`; and
- both failures make zero scope, view, restriction or schema calls.

After the process exits, delete the complete root and prove its absence. Persist
the content-free process envelope before decoding either stream. Never retain
raw stdout, stderr, message, stack, cause, path, environment value, prompt,
response or credential.

## Closed results and no retry

- `package_unloaded_guard_bridge_import_closure_recovery_pass`
- `import_closure_preflight_rejected`
- `import_closure_process_terminal`
- `import_closure_result_rejected`

A static rejection consumes no process and may be corrected within this frozen
plan. Once the distinct Node process starts, it is consumed whether it passes
or fails. Automatic and manual retry and resume counts are zero.

## Acceptance

Acceptance requires:

- unchanged accepted guard, bridge and sanitizer bytes and hashes;
- exact corrected fixture identity and explicit patch-marker absence;
- machine-derived exact relative edge set and complete resolved-target closure;
- exact bare-package allowlist and two local stubs only;
- the accepted bridge bytes materialized only at the guard-owned target;
- exactly eight disposable files and exactly one distinct Node process;
- exact three-case outcome, key order and values;
- exit zero, zero stderr, one canonical stdout line and a content-free envelope
  recorded before interpretation;
- complete disposable-root cleanup;
- focused hostile tests for missing, extra, traversing, absolute, URL, dynamic,
  CommonJS, backslash and reordered specifiers; missing or substituted targets;
  patch markers; source/hash drift; extra files/processes; result drift; raw
  detail; environment expansion; installed-package access; runner import; and
  cleanup failure;
- direct CLI, Ruff, `py_compile`, JSON Schema and exact relevant governance
  suites passing; and
- zero derived-runner, installed-package, native-Harness, DeepSeek-worker,
  model, provider, network, database, Docker, target, retry and resume activity.

This proves only the exact corrected package-unloaded guard–bridge graph and its
pre-process import-closure control.

## Parallelism assessment

- DeepSeek: **declined**. Its native Harness and worker are the governed
  destination and cannot implement or certify this provider-free prerequisite.
- Gemini: **declined provisionally**. Source identities, closure edges, cases,
  envelope and cleanup are deterministic. Reassess only if the consumed result
  leaves a material semantic ambiguity.
- Native subagents: **declined**. Current developer policy prohibits
  delegation, and the gate plus one process is serial.
- GPT Sol owns planning, implementation, execution, deterministic verification
  and acceptance.

## Convergence and stopping rule

A pass advances directly to one complete package-unloaded runner evaluation,
then to one bounded occupied native-Harness DeepSeek worker on useful Raisa
development work. No new intermediate tranche may be introduced for a merely
conceivable risk. Only a specific failure observed here or in the full-runner
preflight can justify another recovery boundary.

Governance generation, register maintenance and receipts are measurement and
recovery costs, not Harness capability gains, and must not be counted as
substantive progress.

## Explicit exclusions

No predecessor retry, second recovery process, derived-runner materialization
or execution, installed-package import or mutation, native Harness, DeepSeek
worker, model/provider request, target creation or use, product source or
configuration change, API/database/route/adapter/flag/allowlist/grammar/client/
waiting-area change, ordinary-practice enablement, generic-status `Arrived`
change, patient/product/clinical/historical/protected data, production,
deployment, release, Pages, protected evidence or protected-ref movement is
authorised.
