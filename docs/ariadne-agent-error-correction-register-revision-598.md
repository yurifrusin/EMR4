# Ariadne agent error and correction register — revision 598

<!-- ariadne-agent-error-register-reading
revision: 598
incident_count: 851
new_incident_ids: AER-0845,AER-0846,AER-0847,AER-0848,AER-0849,AER-0850,AER-0851
open_incident_count: 0
-->

This revision note binds seven corrected provider-free materialisation and
workflow observations to the prospective clockwork-projected register. The
canonical JSON register and pattern report remain clockwork-owned, and one tick
advances the register once.

## AER-0845

The first explicit candidate-staging invocation named `emr-compass.json` and
`emr-continuity-graph.json` rather than the canonical `emr4-*` paths. Git
rejected the complete invocation before staging. A corrected exact roster then
passed. Future closeout staging consumes the clockwork-owned path roster rather
than manually recalling canonical graph names.

## AER-0846

The accepted predecessor report, closeout, Sol acceptance and Yuri summary
omitted the mandatory explicit `Timestamp:` line. They remain immutable. The
successor plan carried the observation forward, and every newly authored plan,
threat delta, recovery, report, closeout, acceptance and Yuri summary includes
an ISO 8601 Australia/Brisbane timestamp.

## AER-0847

The first materialisation contract reused the new outer tranche identity while
generating the accepted diagnostic helper. Because the helper embeds operation,
attempt and candidate values, its hash changed and the generator rejected
`source_binding_mismatch` before evidence output. The recovery separates outer
evidence identity from the exact accepted embedded bundle identity.

## AER-0848

The new generator compiled but its first direct file-path invocation could not
import `orchestration_harness` because the repository root was not placed on
`sys.path`. It failed before artifact generation. The standalone entry point now
derives and inserts the exact repository root before local imports; write and
idempotent check then passed.

## AER-0849

The first shortened-candidate hostile fixture failed closed with the generic
`bundle_runtime_identity_mismatch` because runtime identity comparison preceded
typed candidate validation. The reader now validates operation, attempt and the
full 40-character candidate identity before tree or manifest comparison. The
hostile test then received the exact `candidate_source_invalid` coordinate and
the complete focused suite passed.

## AER-0850

The first clockwork closeout intent used the descriptive incident-stage labels
`planning` and `verification`, which are outside the register's closed enum.
The read-only validator rejected `tick_incident_stage` before any command or
canonical write. Both observations now use the registered
`deterministic_verification` value. Future intent authoring must select incident
stage directly from the clockwork-owned finite vocabulary.

## AER-0851

The first published closeout generation omitted the exact successor protected
boundary `no_ordinary_practice_enablement_feature_flag_allowlist_or_command_mounting`.
The completed postpublication governance suite rejected the current latch in
`test_current_rows_preserve_closed_surface_boundary`. The uncommitted clockwork
generation rolled back byte-exactly to its predecessor, the missing boundary is
now restored in the intent and successor latch projection, and republication
must pass the complete governance suite before acceptance.
