# DeepSeek native Harness provider-free unclassified pre-HMR structured diagnostic seam recovery closeout

Date: 2026-08-21

Timestamp: 2026-08-21T08:24:15.9013739+10:00 (Australia/Brisbane)

Reviewed source: `af22b3e7b2ce3ece15dd6d8530688d19380c1403`.

Clockwork binding source: `4b09bc3665bed77ed7d8cd6f50dc793d1e35d2de`.

## Result

Accepted. The pinned rc.7 source exposes a useful future diagnostic seam
around the rejected dynamic import of `@deepseek-ai/dsh/lib/bin.js`. The
generated wrapper preserves argv, awaits the exact launcher, emits at most one
closed structured sidecar with `wx`, and rethrows the identical rejected value
even if sanitization or writing fails.

The sidecar contains no raw message, stack, path, error property, environment,
prompt, response or credential. It retains only a six-node bounded cause chain
of source-backed error, code, config-stage, message and aggregate-shape
coordinates plus cycle/truncation and explicit false retention flags.

The accepted v1 pre-HMR terminal and consumed controller remain byte-identical.
The new component separately supplies a v2 terminal and an exact future
controller envelope: generate wrapper, launch once, read and validate sidecar,
write the safe terminal outside the disposable root, then remove that root.
Absent or invalid structured evidence leaves the existing v1 fallback in
control.

## Evidence

- six installed rc.7 files are exact SHA-256 bound;
- source inspection proves the launcher/profile/boot/error-object interval;
- 11 hostile fixtures pass without dynamic-value retention;
- the wrapper has one import, one exclusive writer, one identical rethrow,
  zero `process.exit`, zero stack read and no network module;
- the historical attempt-003 terminal is byte-canonical and remains
  `unclassified_nonzero_exit`;
- 52 focused provider-free tests pass across the new seam, the accepted
  monitored controller and attempt-003 compatibility tests;
- Ruff, Python compilation, deterministic re-check and `git diff --check`
  pass; and
- Node, Harness, broker, worker, model, provider and raw-attempt-stream counts
  are all zero for the recovery probe.

## Corrected workflow observations

The first implementation draft modified the accepted v1 component and
consumed controller. The compatibility gate correctly exposed the historical
source-evidence mismatch before staging; the draft was replaced by the
isolated v2 seam and both accepted files were restored byte-for-byte.

That first over-broad suite also repeated a known historical-equality selection
mistake: two immutable predecessor equality tests compare the controller to its
pre-attempt-003 digest. The focused descendant suite replaces them and passes.
The first clockwork check then rejected an invalid Continuity node kind and
untyped contract-evidence strings before canonical mutation. After context
restoration, the first new receipt also rejected two prose-level Git object
repetitions because the machine snapshot alone owns them. The corrected intent
then used two descriptive incident-stage labels outside the fixed clockwork
vocabulary; the next read-only validation rejected those labels before command
execution or canonical mutation. The corrected intent maps them to `closeout`,
and the distinct corrected five-source receipt passes. The first publication
then omitted one explicit ordinary-practice closed-boundary token; the
post-publication baton test rejected it and the clockwork restored the previous
generation byte-exactly before the input was corrected.

All six observations are contained in register revision 579. The one published
rejection was rolled back byte-exactly; no protected evidence or ref changed.

## Honest conclusion

The mechanism is more promising than another stderr-vocabulary expansion. A
future failure at this source boundary can become traceable by structured
object coordinates before serialization, while retaining the original native
failure semantics. The tranche still does not execute the JavaScript wrapper,
launch Harness or DeepSeek, identify attempt 003's destroyed error, or establish
Harness operational readiness.

The narrow dependency-satisfied next step is a provider-free Node fixture
rehearsal of the generated wrapper against authored failing modules. It may
prove actual exclusive sidecar creation, sanitization, identical rejection and
cleanup without importing DSH or launching Harness, broker, worker, model or
provider activity.
