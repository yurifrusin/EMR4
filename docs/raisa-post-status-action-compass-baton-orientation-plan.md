# Post-status-action Compass and baton orientation plan

Date: 2026-08-13

Timestamp: 2026-08-13T23:16:04+10:00 (Australia/Brisbane)

Status: `frozen_for_read_only_execution`

Task baseline: `aefe6938002eae2b7c420e973912fa7973a5d901`

Target result: `raisa_post_status_action_compass_baton_orientation_pass`

Reasoning level: Extra High. This orientation interprets a genuine product-
architecture milestone and selects the next programme direction, while making
no product or authority change itself.

## Objective

Reorient the programme after the first complete Reception One status action and
select the narrowest dependency-satisfied successor already supported by exact
repository evidence.

The orientation must explicitly test Yuri's milestone proposition:

- the conventional grid and the intent-projected meta-grid now have **truth
  parity** for one complete command family because both submit intention to the
  same backend-owned kernel and reconstruct their visible result from that
  kernel;
- this is not yet feature parity across every Diary command; and
- the kernel, not any visual, conversational, email, SMS, voice or future bot
  modality, establishes identity, authority, current source truth, command
  meaning, commit, audit and receipt.

## Exact evidence sources

Read only:

1. the accepted selected-appointment status-action plan, threat delta,
   implementation, typed evidence, closeout and Sol acceptance;
2. the ordinary Diary and Reception One bridge/action surfaces in
   `docs/diary/diary.js` and `docs/diary/meta-grid.js`;
3. the API Spine read/command separation and existing appointment command
   families;
4. the accepted meta-grid, conversational Diary, Context Fabric source-truth,
   patient-channel-neutral and Stage 3B directions;
5. `implementation_plan.md`, the current Continuity graph and Compass; and
6. the current user-owned decision and protected-evidence boundaries in
   `AGENTS.md`.

Do not open protected holdouts, historical Diary/PHI, product data or any live
runtime/source/provider.

## Candidate comparison

Compare at least these directions without implementing any of them:

1. a provider-free projection-neutral truth-parity contract and conformance
   harness that makes the newly proved kernel/projection relationship explicit;
2. composing another already-existing Diary command into Reception One;
3. revising and executing representative Stage 3B sessions;
4. beginning an external patient channel or identity flow;
5. adding another event family or operational watcher/durability runtime; and
6. general visual polish without a new semantic capability.

Each candidate must be classified as `dependency_satisfied`, `user_decision`,
`human_action`, `authority_closed`, or `lower_leverage`, with exact evidence
and the smallest truthful claim.

## Acceptance

The tranche passes only if it:

1. distinguishes truth parity from feature parity and explains why projection
   equality does not elevate a client above the kernel;
2. inventories the exact current read, proposal, confirmation, reconciliation
   and command ownership seams without claiming runtime beyond the evidence;
3. compares every candidate against current dependencies and user-owned gates;
4. selects exactly one narrow successor or reports a genuine user-attention
   fork if no single direction is inferable;
5. freezes the selected successor's maximum eligible scope and explicit closed
   surfaces without implementing it;
6. updates only repository-local plan/orientation/evidence, Continuity, Compass,
   handover and closeout artifacts; and
7. passes focused orientation, latch, baton, Compass and canonical fast checks.

## Stop and recovery

Correct source citations, classification wording, stale Compass assertions and
other mechanical evidence defects inside this read-only boundary. Stop only if
the comparison produces materially different user-owned outcomes that cannot
be resolved from current decisions, or if the selected direction would require
protected evidence, real data, a provider, a new command/event family, a human
participant/channel choice, runtime, deployment or release authority.

## Closed surfaces

No product behavior, FastAPI, GraphQL, OpenAPI, database, source, watcher,
event runtime, persistence, UI implementation, patient channel, identity flow,
provider/ADC, credential/IAM/network, tool, command/write, product/patient/
clinical data, protected evidence, deployment, production, release, Pages or
protected-ref action is authorised. Preserve `docs/branding/` and every
unrelated untracked file; use explicit-path staging only.
