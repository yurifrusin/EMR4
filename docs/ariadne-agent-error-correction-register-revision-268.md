# Ariadne agent error and correction register — revision 268

Date: 2026-08-14

Timestamp: 2026-08-14T20:27:43+10:00 (Australia/Brisbane)

Revision 268 records AER-0306 and AER-0307. The register now contains 307
bounded known incidents, all corrected or contained by an explicit control.

AER-0306 records that the native contract-map analyst used filename discovery
over directory roots rather than its exact ordinary-file allowlist. The
reported output contained only ordinary test filenames and no protected path or
content, but the broad enumeration itself was inadmissible. The worker was
stopped and none of its findings may enter this tranche.

AER-0307 records the distinct native adapter-authority analyst's directory-root
content search. That search emitted protected-fixture content into local tool
output. The analyst self-reported immediately, Sol interrupted the lane and the
entire output is quarantined. No raw protected content is repeated or retained
in the sanitized receipt, register or this revision. No file, Git ref, runtime,
database, provider, credential, product datum or external system changed.

Both native work packages are abandoned. Sol will reconcile the already-known
ordinary sources using one exact literal-file operand per read, and the later
Gemini veto will receive only an explicit non-protected path allowlist. Future
native packets in protected-evidence-adjacent work must prohibit every search,
glob, recursive discovery and directory-root operand—not merely ask for an
exact final source set.
