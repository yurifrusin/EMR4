# Ariadne provider-free shadow clockwork / DeepSeek broker gear rehearsal plan

Date: 2026-08-19  
Operation: `ariadne-provider-free-shadow-clockwork-deepseek-broker-gear-rehearsal`  
Planning source: `159301c3ef84c3f274971df9ef0776312b99f7af`

## Purpose

Build and measure the smallest executable form of the accepted shadow-clockwork architecture. Ariadne reads the existing authorities once, derives every binding field, lends exactly one causal sequence to a provider-free broker, accepts exactly one terminal result, and acknowledges that result before the clock can advance.

This is a private-shadow rehearsal. It does not replace or weaken any current control, invoke DeepSeek, or change EMR4 product behaviour.

## Frozen authority and source relation

- The accepted architecture is exact reviewed source `f6cbd33fd3322754e06ac6dafa1503f5200e0803` and published task source `159301c3ef84c3f274971df9ef0776312b99f7af`.
- The task branch is `codex/ariadne-bernie-davida-parallel-seam`.
- Local/origin `master` and `handoff/current` remain exactly `2e34bdad732fdab32fbf778280b3d3c70d66d602`.
- Eleven accepted architecture and closeout files are content-bound by canonical-LF SHA-256 in the rehearsal contract.
- The live latch is read only through structural validation. No test copies its mutable operation, stage, source or status values.

## One shadow tick

1. Read and validate the live operation latch, fixed Git/ref snapshot, immutable stage and policy contract, content-bound evidence, incident fixtures, and the previous acknowledged tip or genesis.
2. Derive the request tick. No caller may supply a source OID, branch/ref digest, operation, stage, effect, attempt, sequence, previous digest, evidence digest, policy digest, WorkOrder binding or result binding.
3. Admit only `shadow_generation_write`; all candidate, provider, Git, protected-ref, product and runtime effects remain denied.
4. Transfer one sequence lease in a digest-bound WorkOrder to the provider-free broker simulation.
5. Accept exactly one terminal result bound to the WorkOrder, session, ordered tool/result digest, candidate-tree/diff digest, test digest, cleanup state and closed terminal class. The result records zero provider calls.
6. Return the lease only through an Ariadne acknowledgement that binds the terminal-result digest.
7. Validate the complete four-event generation and every projection against the acknowledged tip before one atomic private-shadow rename.

Wall time is diagnostic metadata only. Sequence and digests are the causal clock.

## Frozen efficacy gauges

The comparator is fourteen failure-induced reruns retained from AER-0603 through AER-0615. The fixture ledger represents each failure as a distinct malformed prospective reading. The engine must reject all fourteen before publication and preserve the named control covered by each.

The candidate passes only when one deterministic run establishes:

- caller-supplied derived binding fields: `0`;
- frozen comparator reruns: `14`;
- candidate failure-induced reruns: at most `7` (at least 50% reduction);
- covered frozen failure gauges: `14/14`;
- new mutable-current fixtures: `0`;
- partial publications: `0`;
- uncaught invalid-state escapes: `0`;
- provider calls: `0`;
- shared engine, runner, contract, fixture and test line growth reported without amortisation;
- median clean-run overhead reported but excluded from admission.

A rejected hostile probe is coverage, not a rerun. A rerun is counted only when an invalid generated state survives its owning validation phase and forces a later execution.

## Frozen files

The tranche may add only:

- `orchestration_harness/shadow_clockwork.py`;
- the rehearsal runner under `scripts/`;
- this plan and its threat-model delta;
- the rehearsal contract, schema, immutable failure fixtures, private-shadow evidence and report;
- focused engine and plan tests;
- the eventual immutable review, closeout, acceptance, Continuity/Compass, register and Yuri-summary artifacts required by the standing workflow.

The accepted architecture and `orchestration_harness/transactional_closeout.py` remain read-only predecessor evidence.

## Parallelism disposition

- DeepSeek native Harness: declined for this tranche. The rehearsal is provider-free and the separately required stock-headless-to-custom-runner HMR boot proof is not yet accepted. There is no Claude Code fallback.
- Gemini: reserved for one fresh Gemini 3.7 Flash/high read-only veto of the exact clean candidate after deterministic admission.
- Native subagents: declined under the active developer constraint. Sol owns planning, implementation, verification and integration serially.

## Fail-closed boundaries

No live clockwork adoption, current-control retirement, occupied Harness call, provider call, ordinary-practice enablement, product configuration, API, route, database, client, patient, clinical or historical data, production runtime, deployment, release, Pages, protected evidence or protected-ref movement is authorised. All unrelated untracked files, especially `docs/branding/`, remain preserved. Staging is explicit-path only.

## Acceptance sequence

1. Validate this plan, threat delta, contract, schema and all fourteen immutable fixtures.
2. Implement the shared engine and the provider-free runner.
3. Prove canonical generation, deterministic rerun, exact lease transfer/return, hostile rejection and atomic-publication rollback.
4. Generate the tracked evidence and report from the engine.
5. Run focused tests, complete relevant suites, Ruff, compile and diff checks.
6. Commit an exact candidate and obtain the reserved clean-worktree Gemini veto.
7. Close out with honest efficacy readings, update Continuity/Compass and the error register, write Yuri's paired summary, notify through the usual non-PHI Pushover channel, explicitly stage, commit and push.

