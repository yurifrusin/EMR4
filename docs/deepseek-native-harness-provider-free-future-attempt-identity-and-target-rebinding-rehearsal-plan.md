# DeepSeek native Harness provider-free future-attempt identity and target rebinding rehearsal plan

Date: 2026-08-21

Timestamp: 2026-08-21T22:49:59.5125472+10:00 (Australia/Brisbane)

Status: `frozen`

Reasoning level: `high`

## Starting fact

The accepted materialisation descendant at exact reviewed source
`9b59c6dd7dac9a77030118e83bc81ebf74c3289c` proves that the accepted future
runner, diagnostic helper and controller can be written into one closed
six-path disposable tree and can produce one typed controller terminal without
executing JavaScript. Its fixture deliberately retains the consumed attempt-005
absolute target and predecessor embedded identity. It therefore remains
ineligible for a new occupied Harness attempt.

## Narrow objective

Create one provider-free deterministic source transformation that derives a
single fresh fixture identity and a single inert authored-synthetic relative
target from the exact accepted materialised bundle, then proves that:

1. the future runner contains the new target coordinate and no consumed target;
2. the diagnostic helper contains the new operation, attempt and full
   40-character candidate identity and no predecessor fixture identity;
3. one rebound bundle binds the separately computed runner, helper and
   controller hashes to the exact new identity and target; and
4. one rebound controller terminal copies those exact bindings without adding
   free-form state or execution authority.

No transformed JavaScript is parsed or executed. The target is a coordinate
only and no target file is created.

## Frozen fresh identity and target

- operation:
  `deepseek-native-harness-provider-free-future-attempt-identity-and-target-rebinding-rehearsal`;
- attempt: `future-identity-target-rebinding-fixture-001`;
- candidate source: the machine-resolved full Git commit containing this frozen
  plan, copied into the contract only after that commit exists;
- target: `workspace/authored_synthetic_control_probe.py`;
- classification: `inert_authored_synthetic_relative_python_fixture`.

The target must be a canonical forward-slash relative path with exactly the
two frozen components above. It may not be absolute, contain traversal,
backslashes, drive or UNC syntax, identify a repository or host path, or be
derived from an environment variable. Its SHA-256 binding is computed from its
exact UTF-8 bytes. `occupied_launch_authorized` and
`occupied_target_use_authorized` remain false.

## Exact accepted inputs

- accepted materialisation source:
  `9b59c6dd7dac9a77030118e83bc81ebf74c3289c`;
- accepted materialisation candidate:
  `55906f55dfd82474f095acaa9dc436013db77411`;
- future runner SHA-256:
  `98cf8db1803c7dbfde867044504a820518e448278faf220c8ec3c5dbb8818805`;
- generated helper SHA-256:
  `bd2995a62c7d8bbf37b29c0cd5b5a88c3570341255d1644d9cb98cbef0bd490e`;
- controller SHA-256:
  `29495710c5e95a12fba92a2ef34c28ed6b9ce5e034402e5551767e19b843014c`;
- consumed target:
  `C:/Users/sarashera/EMR4-worktrees/deepseek-native-synthetic-window-worker-005/workspace/synthetic_window_coalescer.py`;
- predecessor fixture operation, attempt and candidate are the exact three
  values in the accepted materialisation contract.

Every input is validated from its separately named source before transformation.
No accepted source or evidence file is modified.

## Deterministic transformation

The rebinder must:

1. rederive the exact accepted future runner and helper and reject any hash
   drift before transformation;
2. require the consumed target JSON literal exactly once in the runner, replace
   it exactly once with the frozen relative target and reject any other source
   change;
3. regenerate the helper through the accepted helper builder using only the
   new operation, attempt and full candidate identity;
4. validate the runner transformation by reversing exactly the target literal
   replacement and recovering the accepted future-runner hash;
5. prove the consumed target and predecessor operation/attempt/candidate bytes
   are absent from their transformed semantic surfaces;
6. compute new runner, helper, target and controller bindings independently;
7. create/read back one exclusive temporary six-path tree using the accepted
   relative path roster; and
8. assemble one canonical typed terminal only after bundle, sidecar and broker
   identity readback succeeds.

The bundle and terminal use new strict schemas. Both carry the exact identity,
source bindings, target binding, false raw-retention flags and false execution
authority. The terminal coordinate remains one of only
`native_harness_terminal_failure`, `post_hmr_pre_request_failure`, or
`post_hmr_request_boundary_unresolved`; no descriptive substitute is admitted.

## Owned artifacts

- this plan and its threat-model delta;
- `orchestration_harness/native_post_hmr_future_attempt_rebinding.py`;
- `scripts/deepseek_native_harness_provider_free_future_attempt_identity_and_target_rebinding_rehearsal.py`;
- strict contract, bundle, terminal and evidence schemas plus generated
  contract/evidence/report/efficacy artifacts under the operation Continuity
  directory; and
- `tests/test_deepseek_native_harness_provider_free_future_attempt_identity_and_target_rebinding_rehearsal.py`.

The accepted materialiser, runner builder, diagnostic helper, controller,
accepted evidence, consumed attempts, product source, configuration and
clockwork are not implementation-owned.

## Deterministic proof

Fixtures must cover exact forward and reverse source transformation; independent
hashes; old-coordinate absence and new-coordinate exact counts; target absolute,
drive, UNC, traversal, backslash, component, Unicode and substitution attacks;
invalid/short candidate identities; operation/attempt/candidate partial rebinding;
runner/helper/controller substitution; bundle and terminal key, type, vocabulary,
relationship and authority mutations; non-canonical/malformed/oversized bytes;
missing/extra/replaced/symlinked members; exclusive-write collision; all three
terminal coordinates; every independently non-zero broker counter; complete
cleanup; unchanged accepted source hashes; and exact full-Git-object resolution.

Python fixture execution is permitted. Node, Harness, broker, worker, model,
provider, network, database, Docker and raw-attempt-stream counts remain zero.
Exact test counts and Git identities may be stated only from completed machine
readings.

## Acceptance

- The accepted materialised source bindings validate before transformation.
- The rebound runner and helper are fresh, deterministic, reversible where
  applicable and contain no consumed semantic coordinates.
- Bundle and terminal copy the same exact identity, target and independently
  computed hashes and remain canonical, closed and write-once.
- The old consumed target cannot pass as the new inert target.
- Generated artifacts, schemas, Ruff, compilation, focused tests and
  risk-proportional inherited/governance tests pass.
- Every prohibited process, request, data and effect count remains zero.

## Parallelism assessment

- DeepSeek Flash: `declined`, negative leverage. This tranche freezes the fresh
  identity and target boundary that must govern a later worker. Running the
  worker now would precede its own admission envelope and violate the tranche.
- Gemini 3.7 Flash/high: `declined`, negative leverage at plan freeze. This is
  an exact provider-free source, schema and hash transformation; reassess only
  if deterministic evidence leaves a genuine semantic veto.
- Native subagents: `declined`, negative leverage. Developer policy prohibits
  delegation and operation, attempt, target, runner, helper, bundle and terminal
  form one serial trust boundary.
- GPT Sol owns planning, implementation, fixtures, review and acceptance.

## Explicit non-authority

No Node/Harness/broker/worker/model/provider process or request, occupied
attempt, retry, resume, fallback, second worker, target-file creation, raw error
or stream read, product/configuration/API/database/route/adapter/flag/allowlist/
grammar/client/waiting-area change, ordinary-practice enablement, generic-status
`Arrived` change, product/patient/appointment/clinical/historical/protected data,
production runtime, deployment, release, Pages or protected-ref movement is
authorised. In particular there is no ordinary-practice feature-flag, allowlist
or command mounting. Preserve `docs/branding/` and every unrelated untracked
file; stage explicit paths only.
