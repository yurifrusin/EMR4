# Reception One visual and interaction synthesis plan

Date: 2026-07-24

Owner: Yuri / GPT Sol

Status: `approved_scope_frozen_active`

## 1. Authority and purpose

Yuri authorised the deferred Reception One visual and interaction synthesis and
said that live Vertex may be involved where it is needed because it is a vital
architectural element. After GPT Sol clarified that the agent would occupy only
a disposable synthesis work cell, not the UI node, Yuri replied: `OK sounds
good so carry on.`

This descendant turns the accepted functional, combined-scope and
committed-reschedule reconciliation behaviour into a coherent high-fidelity
Reception One experience. It may implement visual and interaction changes in
the existing native Diary client, but it may not add product authority.

The user-facing UI remains deterministic and unoccupied. No agent controls the
browser, renders product state, receives Diary events, or persists in the
product. A bounded model work cell may emit a typed design candidate only:

`isolated design work cell -> deterministic proofreader -> human design gate
-> ordinary HTML/CSS/JavaScript implementation`

## 2. Product contract

The synthesis must preserve:

- many views, one authoritative Diary;
- conversation scopes the projection and touch or keyboard selects within it;
- scope, state, omissions, freshness and the route back remain visible;
- answer, selection, proposal, notice, block and receipt are distinct;
- `Proposal - not committed` remains unmistakable;
- ordinary Diary remains the immediate reversible fallback;
- new roots clear unrelated selection, proposal and attention state;
- privacy masking, interruption recovery and keyboard parity;
- the accepted combined patient/practitioner/date/time/duration flow;
- the accepted committed-reschedule availability reconciliation rules; and
- all backend-owned identity, availability, conflict, confirmation, write,
  idempotency, audit and receipt authority.

The target is a calm, legible, tablet-first reception workspace that also
holds together at desktop and phone widths. It may establish typography,
colour, density, composition, component styling and restrained state motion.
It must not claim final public brand or trademark clearance.

## 3. Exact live Vertex boundary

Live Vertex is limited to one authored-synthetic design-synthesis work cell.

- Provider: Google Vertex AI only.
- Model: `gemini-2.5-flash`.
- Project: `bernie-emr4-dev`.
- Target service account:
  `emr4-bernie-ai-dev@bernie-emr4-dev.iam.gserviceaccount.com`.
- Authentication: `keyless_impersonated_service_account_adc`.
- OAuth scope: `https://www.googleapis.com/auth/cloud-platform`.
- Required permission: `aiplatform.endpoints.predict`.
- Location: `australia-southeast1`.
- Data-plane host:
  `australia-southeast1-aiplatform.googleapis.com`.
- Input: a newly authored fictional scheduling-workspace brief with no
  repository excerpts, product data, patient data, clinical data or historical
  material.
- Output: one typed design candidate routed to a deterministic proofreader and
  human design gate.
- Primary call ceiling: one.
- Repair ceiling: one additional call only for a deterministic request-contract
  defect proved locally and repaired without changing any frozen binding.
- Absolute Vertex call ceiling: two.
- Application cost ceiling: USD 1.
- Stop on the first admitted candidate; no critique call follows success.

The occupied cell receives no ADC, OAuth credential, API key, Google Cloud CLI
configuration, product/database mount, Docker socket or unrestricted network.
Only the host one-use broker may use the existing impersonated ADC and call the
exact Sydney Vertex data-plane path. API-key authentication, global endpoints,
regional fallback, provider tools, function calling, grounding, retrieval,
context-cache creation and model-to-product delivery are prohibited.

Before a call, repeat the exact read-only credential/control preflight and the
provider-free real-isolation lifecycle. No cloud, IAM, API, billing, project,
service-account, cache or logging state may be changed.

The configured locational endpoint and observed request path do not prove
Australian physical or sovereign processing.

## 4. Visual concept boundary

Before implementation, create and inspect a complete high-fidelity concept for
the primary Reception One screen. A second generated visual is permitted only
to repair a concrete responsive or hierarchy defect in the first concept.

The built-in ImageGen design tool is the exact visual-generation surface for
this tranche, with an absolute ceiling of two generated images. It is separate
from the Vertex reasoning work cell, receives only an authored-synthetic visual
brief, and has no product/runtime authority. No Australian-processing or other
residency claim is made for this visual-generation surface.

Generated visuals may contain only fictional authored-synthetic identities and
times. They are design artifacts, not product evidence, and provide no runtime
model or provider authority.

## 5. Implementation surface

Implementation is confined to the existing native Diary presentation layer:

- `docs/diary/diary.html`;
- `docs/diary/diary.css` only where the outer Diary shell needs compatible
  styling;
- `docs/diary/meta-grid.css`;
- `docs/diary/meta-grid.js` only for presentation-state or accessible
  interaction changes that do not alter data or command authority; and
- task-scoped deterministic/browser tests and evidence.

No FastAPI route, GraphQL field or mutation, Pydantic/OpenAPI command, database
model or migration, appointment action, event family, provider runtime,
product AI invocation path, deployment or release surface is opened.

## 6. Acceptance gates

### Gate A - rehydration and revision binding

- Complete the five-source rehydration and preserve the dirty worktree.
- Record this descendant without revising historical nodes.
- Increment Continuity and Compass together.
- Validate the graph, map, revision binding and exact rendered Compass before
  any occupied Vertex call.

### Gate B - typed design contract

- The fictional design brief, request constructor, provider schema and
  proofreader are deterministic and repository-tested.
- The proofreader verifies exact schema, enum and responsive-state coverage,
  evidence references, authority class and human-gate disposition.
- Safe repair is limited to whitespace, enum casing and deterministic ordering.
- A rejected draft is never sent to the UI or implementation surface.

### Gate C - credential and isolation preflight

- Repeat the existing read-only exact Bernie ADC/control preflight.
- Repeat the disposable non-root, read-only, capability-dropped, bounded,
  exact-path, no-credential work-cell lifecycle with a zero-call ledger.
- Require independent zero-residue evidence.

### Gate D - bounded synthesis

- Make at most one primary call and only the conditionally eligible repair
  call described above.
- Consume every opened ledger and stop after the first admitted candidate.
- Retain only bounded API metadata, hashes, token/cost fields when supplied,
  proofreader findings and the typed design candidate. Do not retain a raw
  prompt, raw response, credential, token, API-key information, hidden
  reasoning or chain-of-thought.

### Gate E - concept before code

- Generate and inspect the complete visual concept before presentation code is
  changed.
- Record the human design selection and any bounded visual revision.

### Gate F - implementation and rendered evidence

- Preserve the accepted projection and API Spine contracts.
- Verify desktop 1440x900, tablet landscape 1024x768, tablet portrait
  768x1024, phone portrait 390x844 and phone landscape 844x390.
- Verify keyboard, focus, privacy, interruption, Back, ordinary Diary,
  proposal-not-committed and quiet-change states.
- Require no page overflow, no enabled target below 44 CSS pixels, no relevant
  console/page error and no forbidden provider or command request.
- Compare the implemented primary screen with the accepted concept.

### Gate G - closeout

- Run focused Reception One, API Spine, Continuity/Compass, JSON/schema,
  Python, JavaScript, static and Git whitespace checks.
- Produce provider evidence, visual evidence, independent audit analysis, Sol
  review, closeout, pre-acceptance receipt, Continuity/Compass terminal update
  and accurate AGENTS/orchestration-ledger baton.
- Clean every task container, network, image, temporary token/context and
  broker process.

## 7. Evidence labels

- Vertex synthesis: `live_vertex_authored_synthetic_design_candidate`.
- Generated visual concept: `authored_synthetic_generated_design_artifact`.
- Local rendered smoke UI: `authored_synthetic_client_fixture_browser`.
- Non-intercepted local UI/backend evidence, only if separately exercised:
  `live_local_browser_backend_postgres`.
- Deterministic checks: `deterministic_artifact_test`.

No generated concept or fixture browser run may be called production,
representative-usability, live-backend or release evidence.

## 8. Stop conditions

Stop for Yuri if the exact ADC is unusable, an external cloud/IAM/API/billing/
cache/logging change is required, a provider/model/project/identity/region/
credential substitution is required, the Sydney endpoint or no-fallback
boundary cannot be maintained, non-synthetic or sensitive data is required, a
credential may have been exposed, the USD 1 or two-call Vertex ceiling would be
exceeded, isolation or bounded audit cannot be maintained, a material product
policy or command-authority choice is required, or any action exceeds this
plan.

Routine repository-local styling, interaction, request-contract and test
defects may be repaired and rerun within the frozen boundaries.
