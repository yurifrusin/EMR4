# DeepSeek native Harness provider-free preterminal activation observability recovery closeout

Date: 2026-08-20

Timestamp: 2026-08-20T07:22:35.5977084+10:00 (Australia/Brisbane)

Status: `accepted`

Exact reviewed candidate:
`b5f0bc0d823a1c8009f3bb49efcc9a588b9703ab`

## Result

The deterministic recovery is accepted. It leaves
`native-composition-attempt-001` immutable, classifies its retained state only
as `indeterminate_preterminal_failure`, and replaces the future runner's
generic preterminal collapse with finite sanitized activation coordinates,
separate readiness/activation writers and elapsed-time assignment in a
`finally` path before termination and cleanup.

Ten hostile scenarios pass. The exact cached rc.7 package was materialised
once offline in 114,765 ms and one non-Harness Node import probe passed in
118 ms. The tranche started zero native Harness processes, agent sessions,
turns, brokers, models, providers, Docker containers or databases, attempted
zero network calls and removed its disposable root.

The exact reviewer suite passes 85 tests. The broader owned and neighbouring
provider-free suite passed 168 tests before the blocked transition and 167
compatible tests after it. Ruff, compile, schema and deterministic checks pass.

## Review and recovery chronology

The first Gemini review returned `pass`, but its packet falsely stated 137
tests although the exact command contained 85. Sol rejected that receipt as
`revision_required`; it is not acceptance evidence. Three corrected transports
then ended before a model decision with HTTP 403, so the tranche closed blocked
without fallback. The first blocked clockwork publication was rolled back
byte-exactly after two legacy tests proved unable to represent a supported
`blocked` latch; the corrected blocked generation passed.

After Yuri completed fresh interactive authentication directly, one initial
CLI attempt failed before model admission because silent authentication was
unavailable. That event has a non-transferable no-model disposition, and no
authorization code was retained or submitted by Codex. The subsequent fresh
Gemini 3.7 Flash/high review passed all ten commands, explicitly acknowledged
85 tests as 12 + 13 + 21 + 13 + 8 + 8 + 10, found zero P0-P2 issues and left
the exact worktree clean.

The qualifying rejected packet is recorded as AER-0657 through the new
clockwork-owned incident-intake gear. That gear independently passed 480 tests
and a clean Gemini 3.7 Flash/high veto at exact candidate
`7c7ce52a6380637d54dc5ae2d6a778ccd300dd2f`.

## Claim boundary and successor

This accepts a deterministic future-runner observability design. It does not
accept native edit/glob/read composition, DeepSeek model behavior, an occupied
worker, a retry of the consumed attempt, product or practice behavior, data,
runtime, deployment or release.

The successor may be one separately named provider-free native recovery boot
with a new attempt ID and an exact no-retry latch. It cannot resume, retry or
reclassify `native-composition-attempt-001`.

Protected refs remain
`2e34bdad732fdab32fbf778280b3d3c70d66d602`; `docs/branding/` and every
unrelated untracked file remain preserved.
