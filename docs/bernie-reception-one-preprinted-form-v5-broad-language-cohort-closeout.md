# Reception One Pre-printed Form v5 Broad Language Cohort Closeout

Status: blocked pending Yuri intervention
Recorded: 2026-07-30
Result:
`reception_one_preprinted_form_v5_broad_language_adc_preflight_blocked`

## Completed preparation

The unchanged v5 form is frozen against twenty-four new authored-synthetic
utterance cases:

- nineteen proposal or squeeze-in assessment cases; and
- five clarification-only cases.

The cohort covers paraphrases, aliases, minimal wording changes, two-turn
prefaces and corrections, squeeze-in formulations and action-tempting
ambiguities. All twenty-four deterministic oracles pass the unchanged
proofreader. The accepted six-case cohort was not replayed.

The deliberate `no-show` case records a current typed-language boundary: under
the frozen extractor its safe oracle is clarification, not status change. This
will let a later occupied result distinguish model form filling from
deterministic language coverage without retrospective rescoring.

An initial provider-free identifier check found that several descriptive case
codes exceeded the existing thirty-two-character one-use identifier bound.
The repository-local repair shortened only the opaque case codes, added no
provider call and changed no utterance, oracle, prompt, schema or proofreader.
All focused and inherited tests then passed.

## Occupied gate

The fresh read-only cloud-control preflight stopped at
`impersonated_adc_refresh_failed`. No occupied cell or ledger was opened, no
prompt was transmitted and no Vertex inference call occurred.

The prescribed boundary requires Yuri to restore the existing Bernie keyless
impersonated ADC. Codex did not attempt an interactive login, change the active
human account, reconfigure credentials, use an API key, substitute a provider
or region, or modify cloud state.

## Verification

- 158 focused typed-language, v5, Continuity and Compass tests passed;
- 111 relevant API Spine and appointment-proposal tests passed;
- the fixed 266-test repository-only Ariadne verifier passed;
- the inherited two-cell real-isolation rehearsal passed;
- Python compilation and focused Ruff checks passed;
- thirty-one task JSON artifacts parsed at final verification;
- Continuity graph revision 100 and Compass revision 87 validated before the
  attempted cloud preflight;
- the rendered Compass report matched its deterministic renderer;
- `git diff --check` passed apart from pre-existing line-ending warnings; and
- independent container, network, image, broker, cell and occupied-ledger
  residue checks passed.

## Audit and cleanup

Provider call count, HTTP response count, token usage, model cost, occupied
ledger count and typed release count are all zero. No task-owned container,
network, image, broker process, cell process or temporary credential remains.

## Candid result

The broader repository-local cohort is ready and its deterministic safety
oracles pass. The model evaluation has not happened, so no conclusion can yet
be drawn about Gemini's broader form-filling reliability, repeated error
clusters or prompt-teaching need.

This result proves no Sydney inference request for the new cohort and makes no
claim of Australian physical or sovereign processing. Product/database data,
appointment writes, representative sessions, Word, voice, production,
deployment and release remain closed.
