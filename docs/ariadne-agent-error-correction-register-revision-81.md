# Ariadne agent-error register revision 81

Date: 2026-08-08

Status: Continuity closeout baseline defects corrected

Revision 81 adds AER-0082 and AER-0083 from the completed R7 continuity-bound
packet. Neither incident changes the accepted body candidate or its contract.

AER-0082 records that eight already accepted Continuity nodes carried optional
`artifacts` path arrays while the closed JSON Schema omitted that property. The
correction adds the optional property with the existing safe repository-path
type; it does not make artifacts a required inherited contract evidence class
or open file contents.

AER-0083 records that the general Compass report test still hard-coded the
durability state-machine node even though the pre-R7 canonical map had already
advanced to migration/transaction architecture. The test now binds the new
accepted function/trigger-body node while the tranche-specific continuity test
also checks exact graph and map revisions.

The same run also found one exact plan wording expectation changed during
closeout. The binding claim-boundary sentence was restored and followed by a
new sentence recording that independent acceptance now exists; no incident is
added because this was an ordinary uncommitted documentation-test correction.

Revision 81 contains 83 bounded incidents. Both new repository defects are
corrected and no incident is open.
