# Ariadne Sydney Vertex Bounded Work-Cell Rehearsal — Closeout

Date: 2026-07-24
Owner: GPT Sol
Result: `ariadne_vertex_sydney_provider_admission_blocked`

## Outcome

The conditionally authorised sequence stopped at Tranche 1. The current
official Google documentation resolves the stable model ID as
`gemini-3.5-flash`, but does not publish that model for
`australia-southeast1`.

The Gemini 3.5 Flash model card lists model availability and ML processing for
global/multi-region and selected European/Asian locations, including
`asia-southeast1` (Singapore), but not `australia-southeast1` (Sydney). The
current per-model data-residency matrix likewise leaves the Gemini 3.5 Flash
Australia cell unsupported.

The Sydney Vertex service hostname exists. That is an API service-endpoint
fact, not evidence that this model is available there. Yuri explicitly
required a stop instead of model, provider or region substitution, so
Tranches 2-7 did not open.

## Work completed

- preserved Yuri's complete authority and exact stop conditions;
- created a new Continuity descendant without changing consumed historical
  nodes;
- bound Continuity revision 30 and Compass revision 18 before Tranche 1;
- formalised the provider-neutral data, geography, retention, audit and claim
  policy;
- recorded the current official model card, per-model residency matrix,
  lifecycle, retention/training and endpoint observations;
- added a pure provider-free admission validator and focused negative tests;
  and
- produced a fail-closed evidence packet with exact zero-action accounting.

## Work deliberately not started

No launcher, broker, request constructor, ADC preflight, token refresh, Cloud
control-plane read, entitlement check, container, network, model request,
prompt transmission, proofreader run, generated draft, repair retry or provider
audit-log read occurred.

No API-key presence or information was inspected or recorded. No
`GOOGLE_APPLICATION_CREDENTIALS` value or credential file was inspected. No
Google Cloud project, billing, API, IAM, service account, ADC, logging or
external configuration was changed.

## Call, ledger and cleanup accounting

- occupied calls authorised conditionally: at most two;
- occupied calls consumed: zero;
- occupied calls remaining under this closed sequence: zero;
- provider retries: zero;
- provider or regional fallbacks: zero;
- occupied single-use ledgers opened: zero;
- occupied single-use ledgers requiring consumption: zero;
- containers, networks and lane images created by this tranche: zero; and
- temporary credentials created or mounted: zero.

Independent residue checks must still confirm that no task-scoped container,
network, image, credential or process exists; no cleanup action is expected.

## Claim boundary

This result proves that the current official documentary model-location gate
fails and that the repository policy stops before external access.

It does not prove project entitlement, ADC usability, the current Cloud control
posture, provider request acceptance, an occupied work cell, a typed model
response, proofreader behavior, Australian physical processing or Australian
sovereign processing.

## Next decision

Yuri intervention is required. The current authority does not permit another
Gemini model, another model family, another provider, another region, a global
endpoint or a Cloud/IAM/account change.

A future documentary change adding `gemini-3.5-flash` to the Australia
`australia-southeast1` support matrix would still require a fresh, exact Yuri
continuation decision before the remaining tranches resume.
