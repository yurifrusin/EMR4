# DeepSeek native Harness provider-free complete package-unloaded runner evaluation rehearsal plan

Date: 2026-08-22

Status: frozen

Operation: `deepseek-native-harness-provider-free-complete-package-unloaded-runner-evaluation-rehearsal`

## Purpose

Evaluate the complete accepted derived runner against the accepted closed
guard/bridge/sanitizer graph in exactly one disposable, package-unloaded Node
process. This is the last provider-free composition checkpoint before one
bounded occupied native-Harness DeepSeek worker is permitted to perform useful
Raisa development work.

The predecessor import-closure recovery passed all three isolated guard cases.
It did not import or execute the complete runner. This tranche closes only that
remaining composition boundary.

## Frozen source identities

The controller must derive and bind these exact bytes from the accepted source
lineage; it may not copy, edit or infer replacements:

| Artifact | Bytes | SHA-256 |
|---|---:|---|
| derived runner | 12,950 | `5ef3b25babad23f4851faf7981cbdd6e77bf04701e91bf9ed80387df53f93ab9` |
| derived guard | 4,501 | `76029da0f9c030651fd10c0df16f4e75e86b2269d7560af7f94c74680f8598b9` |
| derived bridge | 1,661 | `3a49b28174eeefd77d7efe0a00498901ac6636b637ed9dfe60aba46980df1d0b` |
| accepted sanitizer | 2,439 | `12552925a600dc951afc30b9a738746499c7e2f4cefc9962bc05fb06780f158f` |

The bridge is materialized only at the guard-owned relative target
`preset-mount-sanitizer-runner-bridge.mjs`. The installed DeepSeek Harness
package tree is immutable and must remain unloaded.

## Exact disposable composition

The pre-process closure consists of:

1. the exact derived runner, guard, bridge and sanitizer;
2. one authored-synthetic host fixture;
3. minimal local ESM stubs for exactly
   `@deepseek-ai/dsh-agent`, `@deepseek-ai/dsh-session`,
   `@deepseek-ai/dsh-scope` and `@deepseek-ai/dsh-agent-presets`; and
4. the runner's exact three admitted Node built-ins: `node:crypto`, `node:fs`
   and `node:path`.

Every static and dynamic import must be parsed and resolved before execution.
Relative traversal, absolute/file/URL/backslash specifiers, CommonJS,
unallowlisted packages, missing/extra targets and installed-package resolution
fail closed. The disposable inventory is exact; only the runner-created
sidecar may appear after launch.

## One-process behavior

The fixture supplies authored-synthetic implementations of the injected
loader, agent/session registries, preset service, event subscription and
`appExit` boundary. It exercises the runner's full admitted path:

1. package imports and injected services resolve;
2. both preset roots and the exact mountable roster are admitted;
3. live registries begin empty;
4. one private agent factory setup runs without publication;
5. the root preset service is passed to the accepted guard;
6. mount receiver, context, preset, scope, tool view and restriction are exact;
7. model selection is installed locally without a model or provider request;
8. the exact commit-stage publication veto is observed; and
9. registries and lifecycle events remain empty before the runner writes its
   exact successful sidecar and calls `appExit(0)`.

The fixture emits one schema-owned completion line. Python retains only the
raw stdout/stderr byte counts and hashes until the disposable root has been
removed and the content-free process envelope has been persisted. Only then
may it interpret the fixture line and runner sidecar against closed schemas.

## Closed results

- `complete_package_unloaded_runner_evaluation_pass`
- `complete_runner_preflight_rejected`
- `complete_runner_process_terminal`
- `complete_runner_result_rejected`

There is no automatic or manual retry. A consumed terminal is evidence. Any
successor recovery must be justified by a specific observed failure that the
occupied-worker boundary cannot safely contain.

## Acceptance

Acceptance requires all of the following:

- the five-source Ariadne receipt passes and retains the explicit DeepSeek,
  Gemini and native-subagent dispositions;
- the four exact source identities and complete import graph are bound before
  launch;
- the exact thirteen-file pre-process inventory is materialized in a fresh
  disposable root, with four local stub packages and no installed-package
  access;
- exactly one isolated Node process runs in the five-key minimum Windows
  environment;
- exit is zero, stderr is empty and stdout is one exact canonical line;
- the runner sidecar has its exact ordered key set and values, including one
  private factory preparation, successful preset/tool composition, local model
  selection, exact publication veto, empty registries/lifecycle, zero request
  counters and no raw error;
- the content-free outer envelope is persisted before either output is
  interpreted;
- the disposable root and materialized JavaScript, manifests and sidecar are
  absent after execution;
- focused hostile tests reject source, closure, stub, inventory, environment,
  process-count, key/order/value, terminal, raw-detail and cleanup drift;
- direct contract/check commands, Ruff, `py_compile`, JSON Schema and relevant
  governance suites pass; and
- native Harness, DeepSeek worker, model, provider, broker, network, database,
  Docker, target, retry and resume counts remain zero.

This proves only the complete derived runner's package-unloaded composition
and terminal contract. It does not prove native Harness boot, an occupied
worker turn or worker quality.

## Parallelism assessment

- DeepSeek: **declined** with negative leverage. Its native Harness and worker
  are the governed destination and are excluded from this prerequisite.
- Gemini: **declined provisionally** with neutral leverage. The exact source,
  graph, sidecar and cleanup are deterministic; reassess only if the one
  consumed result leaves material semantic ambiguity.
- Native subagents: **declined** with negative leverage. Current developer
  policy prohibits delegation, and the closure/process sequence is serial.
- GPT Sol owns the plan, implementation, single execution, deterministic
  verification and acceptance.

## Convergence rule

A pass advances directly to one separately frozen, bounded occupied native-
Harness DeepSeek worker on useful Raisa development work. No further
provider-free intermediate is authorised for a merely conceivable risk.

## Explicit exclusions

No second Node process, installed-package import or mutation, native-Harness
boot, DeepSeek worker turn, model/provider/broker/network request, target
creation or use, product source/configuration/API/database/route/adapter/flag/
allowlist/grammar/client/waiting-area change, ordinary-practice enablement,
generic-status `Arrived` change, patient/product/clinical/historical/protected
data, production runtime, deployment, release, Pages, protected evidence or
protected-ref movement is authorised. All unrelated untracked files,
especially `docs/branding/`, remain preserved; staging is explicit-path only.
