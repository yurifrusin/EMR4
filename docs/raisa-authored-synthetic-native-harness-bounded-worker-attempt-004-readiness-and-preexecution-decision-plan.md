# Raisa Authored-Synthetic Native Harness Bounded-Worker Attempt-004 Readiness and Preexecution Decision Plan

Date: 2026-08-21
Timestamp: 2026-08-21T11:48:44+10:00
Status: `frozen`
Reasoning level: `high`

## Objective

Determine, without starting Node, the native DeepSeek Harness, the broker, a
worker, a session, a prompt, a tool, a model or a provider request, whether one
fresh bounded occupied attempt 004 can be represented safely. Freeze a
one-execution plan only when every deterministic gate passes.

## Fixed authority

Yuri's standing uninterrupted-development authority permits this provider-free
readiness decision. It does not itself execute attempt 004. Any occupied action
must begin under the successor operation with a fresh five-source rehydration,
a fresh clockwork checkpoint and an exact machine-resolved source binding.

The protected refs remain exactly
`2e34bdad732fdab32fbf778280b3d3c70d66d602`. Historical attempts 001, 002 and
003 and their retained bytes are immutable and consumed.

## Parallelism assessment

- **DeepSeek Flash:** `not_applicable` in this tranche. The gate decides whether
  a later single DeepSeek execution may exist; this tranche may make no model or
  provider request.
- **Gemini 3.7 Flash/high:** `not_applicable`. Exact local identity, source,
  schema, lease and cleanup checks are deterministic, while provider review is
  forbidden here.
- **Native subagents:** `declined`. One serial attempt identity, broker lease and
  work-order gate has no separable write package, and active team policy does not
  permit proactive delegation.
- **GPT Sol:** owns rehydration, planning, deterministic implementation,
  verification, acceptance and clockwork publication.

## Attempt-004 identity

- operation: `raisa-authored-synthetic-check-in-native-harness-bounded-worker-attempt-004`
- attempt: `deepseek-native-synthetic-window-worker-004`
- work order: `wo-synthetic-native-window-worker-004`
- lease: `lease-synthetic-native-window-worker-004`
- disposable root: `C:/Users/sarashera/EMR4-worktrees/deepseek-native-synthetic-window-worker-004`
- owned file: `synthetic_window_coalescer.py`
- profile: `headless` with preset `emr4-bounded-worker`
- model: `deepseek-v4-flash`, reasoning effort `high`
- tools: exactly `edit`, `glob`, `read`

No attempt-004 preparation, work order, authority, forbidden-surface,
checkpoint, consumed, terminal or report artifact may already exist.

## Deterministic readiness gates

1. Validate this plan, its threat delta, the closed contract and schemas.
2. Re-resolve the full 40-character Git source and all task/protected refs from
   Git; reject abbreviations and caller-authored object IDs.
3. Prove attempts 001-003 are consumed, non-resumable and byte-identical to the
   accepted controller-convergence inventory.
4. Prove the accepted controller, structured diagnostic, legacy terminal,
   broker, work-order schema, pinned native Harness package, exact preset,
   baseline task and expected edit remain digest-bound.
5. Prove attempt 004's identity is disjoint, its disposable root and every
   output path are absent, and its terminal will be written outside that root.
6. Take a read-only clockwork reading that binds the selected generation, lease
   sequence, transaction, journal tip and previous event digest. Do not transfer
   or increment the lease.
7. Freeze a future one-execution envelope: one native process, one session, one
   turn, at most one provider request, at most one model tool call, at most 4,096
   output tokens, 300-second upstream timeout, 420-second native deadline, zero
   automatic retries, zero resumes, zero fallbacks, zero second workers.
8. Require the occupied successor to rehydrate and take a new machine-derived
   clockwork reading after this readiness closeout. The readiness reading must
   never be reused as the execution parent.
9. Exercise absent, malformed, noncanonical, wrong-identity and valid canonical
   diagnostic projections provider-free. Every ambiguous condition fails
   closed and still produces one bounded terminal before cleanup.
10. Verify zero process/request counters and publish only deterministic
    readiness evidence.

## Frozen one-execution decision rule

`ready_for_one_separately_checkpointed_occupied_attempt_004` is permitted only
when every gate passes. It is permission to establish a successor latch and
preexecution checkpoint, not permission to execute from this tranche. Any
identity collision, consumed-history drift, stale Git source, stale clockwork
parent, missing digest, schema variance, root/output residue or ambiguous
terminal rule returns `not_ready` with no retry.

The eventual execution retains the user's existing prepaid DeepSeek balance as
the financial boundary. The broker must not claim an enforced dollar cap and
must not top up that balance.

## Protected boundaries

- no native Harness, broker, worker, session, prompt, tool, model or provider
  process or request;
- no raw startup stream reconstruction or retention;
- no product source, configuration, API, database, route, adapter, feature flag,
  allowlist, action grammar, first-party client or waiting-area change;
- `no_ordinary_practice_enablement_feature_flag_allowlist_or_command_mounting`;
- no ordinary-practice enablement and no generic-status `Arrived` change;
- no product, patient, appointment, clinical, historical or protected data;
- no production runtime, deployment, release, Pages or protected-ref movement;
- preserve `docs/branding/` and every unrelated untracked file;
- explicit-path staging only; never `git add .` or `git add -A`.

## Acceptance

Accept only a schema-valid deterministic result with exact identity and digest
bindings, all gates passed, all process/request counts zero, no attempt-004
artifacts/root present, and `occupied_attempt_authorized: false` for this
tranche. Closeout must publish a paired lay/technical Yuri summary and the usual
non-PHI Pushover notification.
