# Ariadne agent error and correction register — revision 172

Date: 2026-08-10

Revision 172 records AER-0198 and raises the bounded incident population to
198. The incident is an Antigravity authentication transport timeout, not a
model decision, source defect, database failure or operator error.

The first r159 generation-lock parse-reproduction evidence veto timed out
waiting for Google OAuth before model admission. It produced no reviewer
receipt or decision, retained no authorization URL or code, left exact
candidate `fb10f2555a246a2918d84c82e31ea5ea87f32a79` unchanged and clean, and
opened no database or behavior run.

One bounded same-head, same-model, same-packet retry used a distinct passed
orchestrator receipt. It reached Gemini 3.6 Flash/high, returned one `pass`
after 141 focused tests and tracked immutable evidence reconciliation, made no
primary-only mutable path claim, and left r159 unchanged and clean. No fallback
or model substitution occurred.

Counts after this revision:

- origin: 124 agent behavior, 22 harness, 43 repository and nine transport;
- category: 20 command-scope violations, 25 evidence misreports, 22 harness
  failures, 51 output-contract violations, three read-only violations, 25
  reasoning-claim errors, 43 repository defects and nine transport timeouts;
- candidate state: 64 accepted-candidate changes, 112 canonical-unchanged and
  22 untrusted partial worktrees.

No incident remains open. The successful retry is eligible for Sol evidence
admission after exact receipt and primary-worktree reconciliation.
