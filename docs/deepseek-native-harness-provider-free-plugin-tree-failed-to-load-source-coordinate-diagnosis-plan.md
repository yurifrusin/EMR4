# DeepSeek Native Harness Provider-Free `plugin_tree_failed_to_load` Source-Coordinate Diagnosis Plan

Date: 2026-08-21
Timestamp: 2026-08-21T13:08:01.6272499+10:00 (Australia/Brisbane)
Status: `frozen`
Reasoning level: `high`

## Objective

Identify the narrowest failure coordinate that the pinned
`@deepseek-ai/dsh@0.1.0-rc.7` source can support for the immutable attempt-004
structured terminal, then decide whether a separate provider-free repair
tranche is justified. This tranche is source-only: it may not launch Node, the
Harness, the broker, a worker, a model, a provider or an occupied retry.

## Controlling evidence

- the complete live handover, current authority allocation and protected
  boundaries;
- the accepted attempt-004 plan, threat delta, terminal, diagnosis, efficacy,
  closeout and Sol acceptance;
- the active-operation latch for this exact diagnosis;
- the accepted `emr4-bounded-worker` preset materialisation and attempt-004
  profile-template digests; and
- locally available source whose package identity proves exactly
  `@deepseek-ai/dsh@0.1.0-rc.7` without network retrieval.

Raw attempt stderr, error messages, stacks, paths, streams, sessions and
credentials are excluded. The immutable structured terminal is the only
attempt-004 diagnostic input.

## Parallelism assessment

- **DeepSeek Flash:** `declined`, negative leverage. The latch forbids Harness,
  worker, model and provider activity; DeepSeek receives no work package.
- **Gemini 3.7 Flash/high:** `declined`, negative leverage. Provider review is
  outside this source-only tranche. Reassess in a separately frozen repair
  tranche only if a deterministic candidate survives.
- **Native subagents:** `declined`, negative leverage. Developer policy
  prohibits proactive delegation, and the terminal-to-source binding is a
  short serial diagnosis with one evidence owner.
- **GPT Sol:** owns the plan, exact source selection, deterministic analysis,
  evidence, acceptance and clockwork closeout.

## Frozen source-selection rule

The analyzer may read only:

1. the immutable attempt-004 `pre-hmr-startup-terminal.json`;
2. the accepted repository profile templates, preset materialisation and the
   attempt-004 script portions that deterministically authored those profiles;
3. an already-local package tree whose `package.json` proves the exact scoped
   package name and rc.7 version; and
4. this tranche's contract and schemas.

No network/package install, package execution, dynamic import, source-map
resolution, raw terminal reconstruction or recursive search outside the exact
selected local package root is allowed. If no exact local rc.7 source exists,
the verdict is `insufficient_local_source`, not permission to download it.

## Diagnosis method

1. Validate the fresh five-source receipt, active latch and exact Git snapshot.
2. Bind the immutable terminal and admitted profile artifacts by SHA-256.
3. Select one exact already-local rc.7 package root through `package.json`.
4. Inventory only package files statically referenced by the entrypoint,
   plugin-tree construction and their error-wrapping path.
5. Locate every literal or statically provable wrapper capable of producing
   `plugin_tree_failed_to_load`.
6. Compare each candidate's wrapper depth, error kind and stable code/message
   vocabulary with the four-node sanitized attempt-004 cause chain.
7. Name a deeper coordinate only when one source branch uniquely satisfies all
   admitted observations. Never infer from equal byte counts, timing, an
   unrecognized code alone or historical raw output.
8. Classify the narrowest supported owner as one of
   `profile_input`, `package_plugin_tree`, `launcher_environment`,
   `dependency_boundary` or `insufficient_source_coordinate`.
9. Decide whether a separate provider-free repair is justified. A repair is
   justified only when it can target one exact non-product source boundary,
   preserve the accepted profile/tool contract and be proven first with static
   or fixture evidence. This tranche implements no repair.

## Fail-closed decision rule

The diagnosis may return `unique_supported_coordinate` only when:

- package identity is exactly rc.7;
- all admitted inputs match their expected digests;
- exactly one source branch matches the top message coordinate and complete
  sanitized cause-chain shape;
- the reported coordinate is a stable source literal or structural branch,
  not reconstructed raw output; and
- the result is reproducible provider-free.

Zero or multiple matching branches return `insufficient_source_coordinate`.
Package or input drift returns `source_binding_failed`. Neither verdict opens a
Harness retry.

## Acceptance

Accept only schema-valid provider-free evidence that records:

- exact input/package identities and digests;
- the bounded candidate-branch inventory;
- match and ambiguity counts;
- the narrowest supported coordinate and owner classification;
- an explicit `repair_justified` boolean with bounded rationale;
- zero Node, Harness, broker, worker, model, provider and network activity; and
- an explicit successor recommendation that cannot itself execute a repair or
  occupied attempt.

Focused tests must cover missing/wrong package identity, input drift, zero
matches, ambiguous matches, one unique match, forbidden raw fields and false
repair widening. `git diff --check`, lint/compile and exact ref/worktree checks
must pass.

## Protected boundaries

- provider-free static reading only;
- no Harness, broker, worker, model, provider, network, retry, resume, fallback
  or second worker;
- attempts 001-004 and their terminal/consumed evidence remain immutable;
- no raw message, code, stack, path, stream, session or credential
  reconstruction;
- no product source, configuration, API, database, route, adapter, feature
  flag, allowlist, grammar, first-party client or waiting-area change;
- no ordinary-practice enablement or generic-status `Arrived` change;
- no product, patient, appointment, clinical, historical or protected data;
- no production runtime, deployment, release, Pages or protected-ref movement;
- local/origin `master` and `handoff/current` remain fixed at the configured
  protected commit;
- preserve `docs/branding/` and every unrelated untracked file; and
- explicit-path staging only; never `git add .` or `git add -A`.
