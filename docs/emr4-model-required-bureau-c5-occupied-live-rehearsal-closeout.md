# EMR4 Bureau C5 occupied live-rehearsal closeout

Date: 2026-08-06

Status: accepted bounded authored-synthetic development result

## Result

The frozen serial Bureau C5 rehearsal passes as
`model_required_bureau_c5_disposable_live_development_recovery_pass` at exact
source HEAD `dff672049ab5ce47058d7340525e63589fefc5c1`.

One task-owned IPv4-loopback service completed the full controlled lifecycle:

1. generation 1 started and returned the exact nonce, generation, target and
   artifact health tuple;
2. the controller terminated only its owned generation-1 process;
3. Windows post-fault truth was established by exact owned-process absence and
   exclusive reacquisition of the same `127.0.0.1:port`;
4. the controller retained that exact reservation through inherited-socket
   handoff to generation 2;
5. one bounded Sydney Vertex diagnosis was deterministically admitted;
6. one-use authority admitted only the fixed recovery command;
7. generation 2 returned a fresh exact process-plus-HTTP readback; and
8. cleanup proved that no owned process, listener, task directory, open ledger
   or reusable capability remained.

## Exact model and accounting result

- Provider: Google Vertex AI.
- Project: `bernie-emr4-dev`.
- Identity: keyless impersonated
  `emr4-bernie-ai-dev@bernie-emr4-dev.iam.gserviceaccount.com`.
- Region and endpoint: `australia-southeast1` and
  `australia-southeast1-aiplatform.googleapis.com`.
- Model: `gemini-2.5-flash`.
- Request controls: temperature 0, one candidate, `thinkingBudget: 1024`,
  `maxOutputTokens: 2048`, no tools, no retrieval, no redirect, no proxy and no
  fallback.
- Calls: one primary, zero correction calls.
- Reserved-cost ceiling: USD 0.50; maximum reservation consumed: USD 0.25.
- Provider response: HTTP 200 in 6049 ms.
- Usage: 1301 prompt, 445 candidate, 830 thinking and 2576 total tokens.
- Proofreader: admitted with no reason code and no correction ticket.

The durable ledger is closed. No call followed admission.

## Deterministic and independent gates

The first exact-HEAD veto of Windows recovery commit
`88b330870bd559b5276ae8191a41d152c48e9d7b` correctly required revision because
the controller inferred a worktree-local virtual environment that a clean
linked worktree does not contain. The distinct descendant binds the child to
the exact absolute process-owned active interpreter, rejects missing, relative
or alternative executable identity, hashes and rechecks it before spawn, and
accepts no caller path or fallback.

A genuinely fresh Gemini 3.6 Flash/high Antigravity review then passed exact
HEAD `dff672049ab5ce47058d7340525e63589fefc5c1` with one terminal decision, 132
provider-free tests, exact reproduction of all 24 provider-free artifact hashes
and a clean unchanged worktree. The fresh five-source Ariadne runtime state,
generic receipt and custom C5 pre-execution receipt bound exact branch, HEAD,
all four protected refs, source-review digest and a short-lived authority
interval before any live capability.

## Operation and cleanup accounting

The occupied run recorded:

- directory creates/removals: 1 / 1;
- port allocations: 3;
- socket binds/connects: 4 / 2;
- process starts/stops: 2 / 2;
- provider calls: 1; and
- database, SQL, product, deployment, cloud-IAM, external-event, protected,
  shell, dynamic-import, reflection and eval/exec operations: all 0.

The sanitized evidence validates against
`emr4.c5_occupied_rehearsal_evidence.v1`. It retains no raw prompt, raw
response, provider text, model thoughts, credential/token material or patient
or product data. Only the allowlisted metadata, hashes, counters, typed
dispositions and cleanup proof persist.

## Claim boundary

This result proves one source-bound authored-synthetic development loop in
which a fixed model diagnosis could be independently proofread, admitted under
current standing authority, executed once against one disposable target,
verified by fresh readback and completely cleaned up.

It does not prove Australian physical or sovereign processing, production
suitability, model correctness for arbitrary failures, a general repair agent,
patient safety, clinical reasoning, product runtime recovery, real-service or
database recovery, provider-model memory, Context Fabric behavior, or any new
read/write/command/deployment/release authority. The documentation-only API
Spine artifact remains unmounted with empty servers, paths and security.

## Durable evidence

- `orchestration/agent_inbox/antigravity/model-required-bureau-c5-interpreter-binding-review-receipt.json`
- `orchestration/agent_inbox/codex/model-required-bureau-c5-interpreter-binding-live-cloud-preflight.json`
- `orchestration/agent_inbox/codex/model-required-bureau-c5-interpreter-binding-live-preexecution-ariadne-runtime-state.json`
- `orchestration/agent_inbox/codex/model-required-bureau-c5-interpreter-binding-live-preexecution-ariadne-receipt.json`
- `orchestration/agent_inbox/codex/model-required-bureau-c5-interpreter-binding-live-preexecution-receipt.json`
- `orchestration/continuity/model-required-bureau-c5-disposable-live-development-recovery/occupied-rehearsal-interpreter-binding-cost-ledger.json`
- `orchestration/continuity/model-required-bureau-c5-disposable-live-development-recovery/occupied-rehearsal-interpreter-binding-evidence.json`

## Next implementation

The accepted successor is the first provider-free authored-synthetic Practice
Context Fabric and Bureau Memory Bank contract. It may define typed read-only
context requests, deterministic scope grants, expiring frames, memory
projections and weave traces only. Product routes, persistence, real practice
or patient data, provider calls, external retrieval, temporal retention,
commands, writes, deployment, production, release, Pages and protected-ref
movement remain closed.
