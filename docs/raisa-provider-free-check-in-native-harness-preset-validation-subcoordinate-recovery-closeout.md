# Provider-free native Harness preset-validation subcoordinate recovery closeout

Date: 2026-08-20

Timestamp: 2026-08-20T17:18:14.3713624+10:00 (Australia/Brisbane)

Status: `accepted_bounded_negative_native_row_discovery_evidence`

Operation:
`raisa-provider-free-check-in-native-harness-preset-validation-subcoordinate-recovery`

Exact deterministic/package candidate:
`e013bfc0725be62c30b1af2e3ab120a3ef820616`

Exact reviewed no-agent runner candidate:
`8b42760cf68bc2fcc09432de9bd42a8d80b50317`

Exact immutable terminal evidence source:
`9e4d13029e4ea3eb1b511602cf52cd1c863ea69e`

## Result

The deterministic rc.7 package-source chain passes from `AgentPresets.list()`
through `discoverPresets`, `scanRoot`, `compositionProblem` and
`entryListProblem`. A direct pinned-package-only process finds exactly one
healthy `emr4-bounded-worker` row, reads the canonical 158 bytes, parses the
package and binds the expected SHA-256 digest. It uses no native Harness,
provider, network, Docker or database process.

The sole separately checkpointed provider-disabled native process did not pass.
Its injected no-agent runner reached `PRESET_ROW_DISCOVERY_ENTERED` but stopped
before `PRESET_ROW_FOUND`. The closed terminal cannot distinguish a failing
`agentPresets.list()` call from a zero, duplicate or nonmatching native row set,
so no narrower cause is claimed. The discrepancy is between the passing direct
package discovery path and the native Harness service path.

The process ran once for 650 ms, retained no raw output, retried zero times,
created no agent or turn, made zero broker/model/provider/network/Docker/
database requests, and left both its process and disposable root absent. It is
immutable negative Harness evidence, not a DeepSeek model or coding result.

## Verification and review

The deterministic/package candidate passed 42 focused provider-free tests and
one clean Gemini 3.7 Flash/high veto over eleven bound commands. The subsequently
authored native runner passed 15 focused tests, Ruff, Python compilation,
static/package execution and one distinct clean Gemini veto over twelve bound
commands. That runner injects only `agentPresets`, calls `presets.list()` once,
cannot create an agent, mount a preset, create a session or turn, or make an
external request, and writes only ordered markers plus one sanitized terminal.

The separate committed checkpoint binds the exact runner source, passing review
receipt digest, seven markers, 60-second timeout, one process and zero retries.
The terminal validates against the closed schema and cleanup/count contract.

## Workflow efficacy

The result cost one provider-disabled native process and zero provider requests.
It replaced a collapsed preset-validation failure with a useful first-missing
native service coordinate. The machine-snapshot-only Git-ref narrative guard
also removes a recurring transcription requirement.

Four qualifying incidents are closed in register revision 573. Clockwork caught
a recurrence of direct latch editing before acceptance; the latch was restored
from its byte-recoverable generation and the terminal checkpoint was republished
by the exclusive writer with zero canonical drift. This correction matters:
the closeout does not claim that a manually maintained latch was acceptable.

## Next boundary

The narrow successor is
`raisa-provider-free-check-in-native-harness-preset-row-service-path-recovery`.
It will compare the exact home/root/profile/discovery inputs that make direct
package scanning expose one healthy row while the native `agentPresets` service
does not reach the row-found coordinate. It begins with deterministic source and
fixture proofs. The consumed process is not retried, and another native process
would require a separate plan, review and checkpoint.

## Closed surfaces

No DeepSeek request, agent creation, preset mount, occupied worker, attempt 006,
Docker/PostgreSQL/SQL, product source/configuration/API/client/route/flag/
allowlist/grammar/waiting-area change, ordinary-practice enablement, generic
status `Arrived`, product or protected data, production runtime, deployment,
release, Pages or protected ref changed. Local/origin `master` and
`handoff/current` remain exact
`2e34bdad732fdab32fbf778280b3d3c70d66d602`. `docs/branding/` and every
unrelated untracked path remain preserved.
