# Provider-free unmounted default-off canonical check-in environment-evidence admission-input seam rehearsal — closeout

Date: 2026-08-23

Timestamp: 2026-08-23T13:38:13.5091803+10:00 (Australia/Brisbane)

Status: `accepted_pending_clockwork_publication`

Exact reviewed product source:
`1fd1d5f77a02c671528dd0a5f18de4da2f070eaa`

## Product outcome

The unmounted fail-closed seam is complete. It calls the accepted admission
kernel first and changes no existing result except the exact denied ordinary-
practice `ordinary_activation_closed` posture. At that boundary it requires an
exact, satisfied and snapshot-bound `EnvironmentEvidenceGateReading`.

Missing, denied, malformed, replayed, subclassed or duck-typed evidence returns
the existing `ordinary_evidence_missing` denial. A valid reading still returns
`ordinary_activation_closed`; it never admits. The prior operational-evidence
Boolean remains independently mandatory. Authored-synthetic-only admission,
feature flag, kill switch, lane isolation, record binding/state and command
envelope are unchanged.

Two optional snapshot binding fields default to `None`, preserving historical
constructors while making the new seam deny until a future signed snapshot
contains both exact bindings.

## Verification

- 278 focused and surrounding check-in/API Spine tests passed;
- Ruff, Python compilation and `git diff --check` passed;
- exact-type, schema/outcome/reason, environment, generation and manifest-
  digest attacks fail closed;
- ordinary admission release count is zero; and
- one fresh Gemini 3.7 Flash/high veto passed 8/8 commands and 135 tests at
  unchanged clean exact HEAD.

## Native DeepSeek Harness conclusion

This was a real product assignment under the pragmatic adoption plan, not a
generic Harness test. The prepared boundary succeeded twice with an exact
three-tool view, fresh sparse worktrees, zero retry/fallback and exact owned
paths. Both occupied attempts then ended before provider delivery and produced
no candidate. The final exact coordinate was
`EFFECTIVE_TOOL_COMPOSITION_PRESET_MOUNT_FAILED`.

That is materially more traceable than the retained opaque Claude-Code
failures: the failure is localized to the custom-runner preset mount, not
DeepSeek model behavior. It is not useful worker performance, however.
DeepSeek received no task and contributed no code.

The honest cost was high: a 1,018-line reusable coordinator, 96 focused adapter
tests, four adapter/scaffold correction commits, two occupied processes and Sol
recovery, for zero provider calls and zero native candidates. The three-reading
clockwork interface worked as intended, but the development yield does not
justify another diagnostic or generic interoperability tranche.

The rolling allocation therefore narrows native Harness use. Preserve the
traceable broker/profile evidence, but do not select this exact profile for the
immediate next EMR4 tranche. Reconsider only after an upstream or naturally
in-scope non-diagnostic change supplies evidence that preset mounting is fixed,
or a bounded task fits a previously working exact runner. Claude Code is not a
validated reliability control and is not a silent fallback.

## Incidents and workflow efficacy

The clockwork will register the contained form/path/preflight, two native
terminal, Sol predicate-evaluation, Git-expression and verifier-cleanup
observations. None changed protected refs or released product authority. The
typed preflight and terminal readings caught every failure before provider or
acceptance, while Sol and Gemini independently caught the one product defect
before publication.

This supports the user's requested lower-perfection regime: keep coarse typed
boundaries and ordinary product tests; stop multiplying Harness subcoordinate
tranches. It also supplies a negative efficacy reading rather than declaring
the traceability gain to be useful completion.

## Boundary and successor

No ordinary practice is enabled. No feature flag, allowlist, admission
transition, command, route, public API, client, generic-status `Arrived`,
waiting-area movement, environment configuration, secret, database,
product/patient/appointment/clinical data, production runtime, deployment,
release, Pages or protected ref changed.

The dependency-satisfied successor is
`raisa-provider-free-read-only-canonical-check-in-ordinary-practice-admission-readiness-post-evidence-seam-convergence-review`.
It may inspect the now-composed unmounted admission/evidence lineage, identify
the narrowest remaining blocker and select one next provider-free tranche. It
may not enable ordinary practice, reopen Harness diagnostics, mount a command,
change an API/client/status/waiting area, use product data or move protected
refs.
