# Raisa AES-C3 provider-free hostile containment rehearsal closeout

Date: 2026-08-11

Result: `raisa_agent_execution_surface_containment_gate_aes_c3_provider_free_hostile_containment_pass`

Reviewed source HEAD: `c45ff191af420b801e9917a7efc69c17aeb5698b`

## Accepted result

AES-C3 passes as an authored-synthetic, provider-free, pure hostile-
containment rehearsal beneath the exact accepted AES-C0/C1/C2 contracts. Its
closed 61-scenario catalogue resolves as 21 `contained`, 15 `reject` and 25
terminal `stop` results. Exactly 28 calls reach the one statically selected
pure Python adapter and exactly 21 digest-only results release; every recorded
external-operation count is zero.

Opaque hostile-looking strings remain bounded values. The broker-created
invocation contains their digest, never their raw content, and no decoder,
deserializer, template interpreter, dereference, dynamic selector or transport
path exists. Structural local-file, template/deserialization, metadata,
credential, relay, GraphQL/REST/SQL, provider-tool, product-adapter, executable
and policy-amendment fields reject before a call.

Invalid clear, encoded, compressed, chunked and exception-labelled result
carriers each make the required fixed pure call and release nothing. All five
egress-budget carriers receive the same 256-byte charge to the same fixed
destination and stop at 12,289 against the 12,288 ceiling. Denial-probe and
repeated-failure sequences verify each exact transition and the terminal third
stop. Generation, manifest, Bureau, work-cell, authority, lease/alias/token,
context and supply-chain bindings all fail closed when stale or mismatched.

## Exact artifacts

- contract SHA-256: `4b4e94b07823576b469921308fd46f741d02834f860759db56985285ceb67d3e`;
- schema SHA-256: `fc9e1ae2e42e6178586c51faabf1dabc7ae292d9889091b310a8fd9c021e3de1`;
- scenarios SHA-256: `dd589b02e336b0096936edda43f4409bb57c1ea3fbfc954c1540e73621f3877f`;
- generated evidence SHA-256: `3ba7ebe9d823eeb58bbb0ac377e627d6c1ab5450b0cc3956d890550dd05944fb`; and
- implementation SHA-256: `0732208df1a70cd9438980a32952ec62fc389132b65b6184de0bed1019d7b39c`.

All 33 generated attempt mutations and all 18 nested contract mutations fail
closed with zero release. The eleven inherited AES-C0/C1/C2 hashes remain
exact.

## API Spine boundary

The API Steward classification remains `internal_containment_evidence`.
GraphQL stays read-only and was not called; committed events remain signals for
fresh authorized reads and were not consumed; provider failure remains explicit
`intelligence_unavailable`; and product mutations remain separately authorized,
human/policy-gated, idempotent, audited and read-back REST/OpenAPI commands.
AES-C3 adds no route, event, context source, capability, tool or command.

## Deterministic and independent evidence

- the standalone report passes the exact 61 scenarios and aggregate totals;
- all 33 hostile attempts and 18 contract mutations reject without release;
- Sol's recovered focused AES-C3 tests pass 25/25;
- the recovered C3/C2/C1/C0/API packet passes 106/106;
- the final maintained static packet passes 184/184;
- the final canonical fast profile passes 190/190, together with Ruff,
  compilation of 202 maintained Python source files plus 35 exact verification
  files, Diary JavaScript syntax and Git whitespace; and
- a fresh Gemini 3.6 Flash/high exact-HEAD veto independently passes 120
  focused tests, Ruff and whitespace and leaves the clean review worktree at
  the unchanged reviewed HEAD.

Evidence mode is
`authored_synthetic_provider_free_pure_hostile_containment_rehearsal`: no real
runtime, adapter, provider, credential, network, filesystem capability,
database/source, executable tool, command or patient/product operation ran.

## Issues exposed and resolved

The DeepSeek blue candidate correctly reproduced the expected catalogue totals
but hard-coded outer outcomes around incomplete evidence. Removing a replay
fixture or changing the declared inherited base did not invalidate the result;
malformed public input could raise `KeyError`; contradictory inherited C2
results could be relabelled as zero-call outer stops; and the cumulative paths
did not prove every returned transition. Sol rejected the self-pass as
conceptual, preserved it as AER-0257 and applied the explicit recovery lease.

The recovered implementation binds each scenario to its exact base, family,
carrier, mutation and result; binds every replay fixture to current identity;
validates before indexing; checks exact inner results; retains contradictory
actual calls while withholding their digests; and verifies every cumulative
transition. Fresh Gemini review passed, so AER-0257 is corrected without
erasing its immutable failure evidence. AER-0258 separately preserves a wrong
focused-test filename; the invalid command supplied no evidence and the exact
complete packet was rerun successfully.

## Claim and authority boundary

AES-C3 proves only the tested structural rejection, opaque digest-only
handling, invalid-result non-release, carrier-independent byte accounting,
cumulative terminal latching, stale-replay rejection and exact context/supply
binding for these authored-synthetic pure objects. It does not prove semantic
prompt-injection detection, operating-system/process/container isolation, real
credential or adapter safety, provider behaviour, concurrency, product-data
safety, command safety, deployment or production readiness.

Protected evidence, historical Diary PHI, patient/clinical/product data,
licensed content, provider calls, real credentials, IAM/metadata, network,
database/source, migration, watcher/listener, filesystem capability,
executable tools, commands/writes, deployment, production, release, Pages and
protected refs remain closed. User-owned `docs/branding/` and every unrelated
untracked file remain preserved and excluded.

## Next planned descendant

AES-C4 is the next named programme descendant, but it is a bounded occupied
authored-synthetic provider proof. The controlling sequence requires a newly
frozen exact provider/model, region, identity, data, call, cost, isolation,
proofreader, cleanup and no-fallback envelope. No current authority supplies
that envelope: the provider lane is paused and all historical call envelopes
are consumed. AES-C3 therefore closes successfully at a genuine Yuri-attention
fork and grants no AES-C4 provider call.
