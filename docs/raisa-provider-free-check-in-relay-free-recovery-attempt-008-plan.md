# Provider-free check-in relay-free recovery attempt 008 plan

Date: 2026-08-23

Timestamp: 2026-08-23T06:48:58.0480610+10:00 (Australia/Brisbane)

Status: `frozen`

Planning source HEAD:
`2027c2252685c73772de6c60a0f5d11f82ab2c9d`

Accepted attempt-007 terminal source:
`6657ee5061265d732096e9987f327d82feed800c`

Accepted attempt-007 diagnosis closeout source:
`5d93380060f31bab21bddc9ffdd5580754eb4fc6`

Accepted deterministic repair closeout source:
`a33a4ccc7619fcae5cdd45a48a2312ab0c0384a4`

Accepted verification-envelope closeout source:
`d01ef2f3afe16ccdb9a8f2077d5e76688397adb6`

Accepted plan-admissibility decision source:
`bcdd7fc25f745ade62cb145ead73c4a1ad6f4e83`

Protected source:
`2e34bdad732fdab32fbf778280b3d3c70d66d602`

Operation:
`raisa-provider-free-check-in-relay-free-recovery-attempt-008`

Target result:
`raisa_provider_free_check_in_relay_free_recovery_attempt_008_pass`

Reasoning level: Extra High freezes this distinct one-run database authority,
the immutable attempt-007 negative lineage, the repaired prospective-success
and typed-terminal controls, and the exact cleanup boundary. High is
sufficient for mechanical construction, deterministic admission, the sole
occupied execution and closeout while this plan remains unchanged.

## Authority and objective

The accepted fourteen-row decision is exactly
`admissible_for_separate_plan_freeze`, not execution ready. This plan freezes
P06 through P11 now and retains P12 through P14 as hard preexecution locks.
It authorises deterministic construction first and, only after a separately
published database-preexecution clockwork checkpoint, exactly one local
provider-free disposable PostgreSQL 16 invocation.

The sole occupied invocation must prove in one collision-free authored-
synthetic environment that:

1. one effect/receipt/audit packet is staged and explicitly rolled back, after
   which fresh restricted-role readback observes zero members;
2. a disjoint packet commits once and the caller loses its complete terminal
   response through the accepted relay-free path;
3. the caller releases neither success nor retry;
4. fresh restricted-role authoritative readback observes one exact consistent
   packet, zero duplicates and zero cross-practice visibility; and
5. the ephemeral role, attachments, sidecars, server, network and every
   matching labelled resource are exactly absent at close.

The first database-process start consumes attempt 008 regardless of pass,
failure, interruption or unexpected terminal form. There is no retry, resume
or fallback. Any later occupied run requires a new operation, namespace, plan
and checkpoint.

## P06-P14 hard-condition register

| ID | Frozen condition | Attempt-008 disposition |
|---|---|---|
| `P06` | Fresh identity, collision-free namespace and no-retry/no-resume/no-fallback rule. | Frozen by this plan. |
| `P07` | New wrapper and closed terminal schema bind the current repaired base and immutable attempt-007 lineage without altering either. | Construction authorised; modification of either source forbidden. |
| `P08` | Every source digest and Git ancestry binding is regenerated mechanically at the exact clean candidate. | Required in static admission. |
| `P09` | One authorised command, invocation count one and terminal consumption semantics are exact. | Frozen below. |
| `P10` | Rollback-zero, unknown-response no-success/no-retry, authoritative exactly-once readback, isolation and cleanup acceptance remain exact. | Frozen unchanged from the base contract. |
| `P11` | Product, API, ordinary-practice, provider, data, production and protected boundaries remain closed. | Frozen below. |
| `P12` | Complete deterministic provider-free tests, static admission, Ruff, compilation, schema and diff gates pass on one committed candidate. | Must pass before Docker inspection or checkpoint. |
| `P13` | Read-only Docker inspection proves the exact cached image and zero matching resources, then a fresh five-source receipt repeats all lane dispositions. | Must pass after the committed candidate and before checkpoint. |
| `P14` | A distinct clockwork checkpoint check/publish advances the latch to exactly one occupied execution only after P06-P13 pass. | Must publish before the one command. |

No interpretation, partial satisfaction or later prose may weaken a row.

## Immutable lineage and exact inputs

Attempt 007 is consumed once, retry zero and immutable. Attempt 008 binds its
terminal pair byte-for-byte:

| SHA-256 | Exact source |
|---|---|
| `86e5e1342eb54e062e35d73390ebceb141d097d03e180e4fe3c0ed64b465f422` | `orchestration/continuity/raisa-provider-free-check-in-relay-free-recovery-attempt-007/rehearsal-failure-evidence.json` |
| `3338c58054dea96b3845827dacfe184889ee328e5a4463966464b560d0a2c2c5` | `orchestration/continuity/raisa-provider-free-check-in-relay-free-recovery-attempt-007/attempt-007-execution-envelope.json` |

The current repaired base harness is exact SHA-256
`c4372d443206c2a39351667b6d599c6911d575059955e6c615358d379355ae78`:
`scripts/raisa_provider_free_disposable_postgresql_default_off_check_in_relay_free_rollback_unknown_commit_recovery_rehearsal.py`.
It is accepted at implementation source
`8a82a8184cc66efbe31769eda88e299887f798bc` and exposes the frozen
`PostFinalizationTerminal` bridge. Its start vector remains exactly
`<executable> start --attach --interactive <container_id>`.

Additional byte bindings are exact:

| SHA-256 | Exact source |
|---|---|
| `bcad86a4607c12ef5a3f98394b6e65e03c7ff8be8b3d83de2ba39884ea63bbda` | base transaction `contract.json` |
| `d2c186b0d30419e0459d93d92af1f84907125becdeb75c7e1890dce597d3e72c` | base transaction attestation schema |
| `662e055f9011aa5574127503b46dd3ca9c6c3113ce6dad3b6e3ca35728930658` | accepted prospective/typed-terminal repair contract |
| `47f422e7b8ad072c9f4912fe6269cfc85f44eb75808419182c75e19d41157eaa` | accepted prospective/typed-terminal repair evidence |
| `47e6fd567e773c69fd7867e30a56f69e0d9346f1f6216fd37aebbdbaec96aa0d` | accepted verification-envelope evidence |
| `88a6763d325b9cf8801503daec12f6b53e21cb047419d47d904e0450fb5598dd` | accepted attempt-008 plan-admissibility decision evidence |

Every Git binding must be a machine-resolved full 40-character commit object
and an ancestor of the executing candidate. The wrapper performs those
readings. Caller-authored receipt prose must not repeat machine-owned Git
snapshots.

## Narrow construction boundary

The base harness, attempt-007 terminal and accepted repair artifacts remain
byte-for-byte unchanged. Construction may add only:

- `scripts/raisa_provider_free_check_in_relay_free_recovery_attempt_008.py`,
  exposing only `--check` and `--execute`, with no caller output path;
- one closed attempt-008 execution-envelope schema in the fresh Continuity
  namespace; and
- focused pure tests for full-object ancestry, byte bindings, CLI closure,
  namespace collision denial, prospective-success admission, exact terminal
  routing, finalized-cleanup retention, unconditional base-path restoration,
  predecessor immutability and sanitised failure.

The wrapper binds the base attestation, evidence and failure paths for one
runner call and restores them unconditionally in `finally`. It refuses
execution if any attempt-008 terminal exists and never deletes, rotates,
renames or overwrites a terminal. Pass writes the base attestation/evidence
pair and one attempt-008 envelope. Failure writes a base-owned sanitised
failure retaining its finalized cleanup plus the envelope. Construction
creates no Docker or database resource.

The complete prospective success projection, attempt-008 envelope example and
hostile forbidden-field mutations must pass unchanged redaction and schema
admission before a Docker-capable function can be reached. The wrapper may not
substitute `not_started` for a finalized base cleanup projection.

## Fixed transaction, API and containment boundary

This is a security, audit and idempotency rehearsal of an accepted REST command
shape, not an API change. GraphQL remains read-only. Explicit actor/practice
scope, authorisation, command/idempotency identity, exact request digest,
atomic effect/receipt/audit membership, default denial and fresh authoritative
readback remain fixed. Events are post-commit hints, never commands or truth.

Containment remains cached `postgres:16-bookworm` with pull policy `never`, one
captured internal network, no published port, host relay, Docker-exec bridge,
external network, bind, volume, multiprocessing process or queue. Random
controller-memory credentials are delivered only by post-inspection attached
stdin. The three admin-owned non-product relations retain forced RLS. The
ephemeral login is a non-owner with `NOBYPASSRLS`. Exact terminal OCI state and
the observer predicate classify the incomplete response; restricted
authoritative readback alone can prove committed exactly once.

One Sol controller owns acquisition and cleanup, reinspects captured identity
before removal and proves role, attachment, sidecars, server, network and
matching labels absent. Cleanup uncertainty is terminal failure without retry.

## Deterministic and preexecution admission

Before any Docker object, credential, PostgreSQL process, SQL or database
action, one committed candidate must pass:

1. this plan, threat delta, preplanning receipt and all three lane dispositions;
2. the attempt-008 wrapper, closed schema and focused tests with exact digest
   and full-Git ancestry bindings;
3. the repaired base prospective-success/redaction and typed-terminal static
   gates, including 67 normalized/runtime paths, 66 hostile rejections, zero
   late escape and retained finalized cleanup;
4. focused attempt-008, repair, verification-envelope, base-harness, API Spine,
   no-database interlock, latch, Baton and clockwork tests through
   `scripts.ariadne_provider_free_pytest` under typed closed database authority;
5. direct wrapper `--check`, Ruff, Python compilation, JSON/schema validation
   and `git diff --check`; and
6. exact absence of attempt-008 terminal artifacts.

Ordinary or serial pytest is forbidden in this closed phase. Passing P12 does
not itself open Docker or database authority.

After the candidate is committed and tracked-clean, P13 permits only read-only
Docker version, exact cached-image and exact owned-label resource inspection.
It must create no object and record zero matching attempt-008 resources. A
fresh five-source preexecution receipt must bind that exact candidate and
repeat DeepSeek, Gemini and native-subagent dispositions.

P14 then requires a distinct database-preexecution clockwork `--check`, and
only after that succeeds a separate `--publish`, advancing the latch to exactly
one database execution. Only then may this one command run:

`.venv/Scripts/python.exe -m scripts.raisa_provider_free_check_in_relay_free_recovery_attempt_008 --execute`

The invocation count is one even if the command exits nonzero, output is
absent, or bounded ownership-checked cleanup is required. No automatic or
manual retry is permitted.

## Acceptance, efficacy and workflow accounting

Pass requires a closed attempt-008 envelope, explicit rollback zero effect,
committed-exactly-once authoritative readback, no success from the incomplete
response, retry count zero, other-practice visibility zero, product/ordinary
effect zero, schema-valid sanitised evidence, finalized cleanup retained and
zero owned residue. Failure preserves one immutable failed-closed terminal and
closes the attempt without retry.

The closeout efficacy reading must compare one-shot database invocations,
prevented preexecution violations, correction iterations and any form/test
reruns against the previous attempts. It must include the phase-sensitive
postpublication assertion lapse observed after the preceding decision
publication as the next qualifying register incident; it may not silently
disappear merely because the corrected packet passed.

## Parallelism assessment

- **DeepSeek native Harness:** `declined`, negative leverage. The latch forbids
  worker/provider use and this stateful one-shot lifecycle has no separable
  implementation package.
- **Gemini:** `declined`, neutral leverage. Provider use is forbidden and
  deterministic schemas, bindings and lifecycle postconditions own admission.
- **Native subagents:** `declined`, negative leverage. Developer policy forbids
  proactive delegation and splitting the lifecycle would fragment the
  non-transferable cleanup lease.
- **GPT Sol:** owns plan, construction, admission, one execution, cleanup,
  acceptance, clockwork and Git closeout.

Reassess at deterministic-candidate admission, preexecution checkpoint,
postterminal and closeout without inferring new provider or delegation
authority.

## Protected and continuation boundaries

No REST/OpenAPI, GraphQL, route, application schema, migration, feature flag,
authored-synthetic allowlist, action grammar, first-party client, waiting-area
behaviour or product configuration may change. Dedicated check-in remains
default-off; generic status does not gain `Arrived`; no product record or
ordinary-practice admission is written.

No product, patient, appointment, clinical, historical or protected data;
worker/provider call; production runtime; deployment; release; Pages;
protected evidence; or protected-ref movement is authorised. Local/origin
`master` and `handoff/current` remain exact
`2e34bdad732fdab32fbf778280b3d3c70d66d602`.

At closeout clockwork is the sole canonical governance writer. Sol writes the
paired lay/technical Yuri summary, sends the usual non-PHI Pushover, stages
only explicit paths and preserves `docs/branding/` plus every unrelated
untracked file. `git add .` and `git add -A` are forbidden. Continue to the
narrowest dependency-satisfied successor unless a genuine user-attention fork
arises.
