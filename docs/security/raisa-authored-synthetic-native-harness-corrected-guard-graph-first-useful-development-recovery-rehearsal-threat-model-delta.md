# Threat-model delta: corrected-guard-graph first useful-development recovery rehearsal

Date: 2026-08-22

Timestamp: 2026-08-22T21:44:49.4684132+10:00 (Australia/Brisbane)

Status: `frozen`

Operation: `raisa-authored-synthetic-native-harness-corrected-guard-graph-first-useful-development-recovery-rehearsal`

## Added surface

One disposable local Git workspace, one pinned rc.7 native-Harness process,
one loopback one-request DeepSeek broker and one authored-synthetic Python file
are temporarily introduced. The accepted corrected runner/guard graph is
rebound only to the new disposable target.

## Threats and controls

| Threat | Fail-closed control |
|---|---|
| A consumed attempt is silently retried | Use a new operation-specific attempt, work order, lease, root, checkpoint and evidence namespace. Consume it before launch; never resume or reuse predecessor identities. |
| The worker sees product or sensitive data | Synthesize the one-file workspace from frozen non-product bytes. Validate its only path and hashes before launch; transmit no repository, product, patient, appointment, clinical, historical or protected content. |
| The runner correction is substituted or widened | Obtain runner, guard, bridge and sanitizer only from the accepted materializer; permit only one target-literal replacement and prove reverse substitution restores the accepted 14,210 bytes exactly. |
| Preset or effective tools drift | Bind the accepted two-root/four-read/five-hook boot evidence and require effective tools exactly `edit`, `glob`, `read`. The occupied pre-execute hook admits only one direct top-level edit. |
| A read, glob, nested call, second call or `replace_all` expands scope | Deny every call except the first exact-path literal edit with non-empty unequal strings, no parent and no `replace_all`. One parallel tool maximum. |
| A plausible edit is mistaken for useful output | Require exact frozen bytes, exactly one changed path, four public cases, three controller-held cases and a later independent veto. |
| The provider or Harness retries invisibly | One-request broker, disabled retry path, consumed lease and terminal ceilings require one native process, at most one provider request and zero retry/resume/fallback. |
| Raw model or credential material persists | Destroy prompt, response, reasoning, arguments, errors, streams, session, environment and credential-bearing process state. Retain only closed coordinates, counts and hashes. |
| A candidate becomes product code by implication | Retain it only as authored-synthetic evidence outside the removed root. No product path or runtime mount is owned. |
| Gemini becomes fallback implementation | Dispatch it only after deterministic candidate admission, in a fresh read-only exact-candidate worktree, for one terminal veto. It cannot edit, accept, integrate or substitute a different model. |
| Further recovery becomes an unbounded procedural circle | Freeze this as the final occupied recovery in the current sequence. Any occupied non-pass makes the Harness unavailable for EMR4 worker allocation without automatic successor diagnosis or retry. |
| Governance mistakes are hidden | Count every prelaunch and postterminal correction, preserve the rejected full-ID receipt, and allocate it through the next transactional register update. |
| Protected refs or unrelated files move | Verify task/origin and protected refs before and after, stage explicit paths only, and preserve all unrelated untracked files including `docs/branding/`. |

## Residual risk and claim ceiling

One pass would prove controlled usefulness only for one single-file,
single-edit, authored-synthetic maintenance task. It would not prove general
DeepSeek quality, multi-tool or multi-file orchestration, product safety,
ordinary-practice readiness, production isolation or permission to use real
data. One non-pass is negative evidence and ends this Harness recovery sequence
without another occupied attempt.
