# Threat-model delta: guard–bridge import-closure recovery

Date: 2026-08-22

Timestamp: 2026-08-22T11:01:16.5622642+10:00 (Australia/Brisbane)

Status: **frozen with the import-closure recovery plan**

## Boundary

One distinct package-unloaded Node process may evaluate the unchanged accepted
guard, bridge and sanitizer plus one corrected authored-synthetic fixture only
after a complete static ESM import-closure gate. The predecessor attempt is
immutable and cannot be retried. The runner, installed package, native Harness,
DeepSeek worker, models, providers, product source, data and protected refs
remain outside the boundary.

## Threats and controls

| Threat | Fail-closed control |
|---|---|
| The previous absent-target diagnosis is overstated as the exclusive cause | Preserve the terminal and proved absent target, record the newly observed fixture patch markers, withdraw exclusivity, and correct both source-proved defects before the distinct process. |
| Patch syntax or diff markers reach Node again | Generate one exact corrected fixture; reject carriage returns and every line beginning with `+`; bind its bytes and hash in the contract and hostile tests. |
| A relative import target is guessed from a familiar filename | Extract every static specifier from exact source, resolve lexically against the importer, and require the resolved target in the frozen inventory before Node resolution. |
| Traversal or absolute coordinates escape the disposable root | Reject `..` escape, absolute, drive-letter, backslash, URL, dynamic-import and CommonJS specifiers before materialization. |
| The right bridge bytes remain under the wrong name | Materialize the unchanged accepted bridge only at `preset-mount-sanitizer-runner-bridge.mjs`; reject the predecessor source basename from the inventory. |
| Closure validation checks only the guard and misses fixture or bridge edges | Require the exact three-edge graph fixture→guard→bridge→sanitizer and reject missing, extra or reordered edge readings. |
| Bare imports resolve to installed packages | Allow only two exact package names, materialize their minimal local manifests/sources, reject external paths and record installed-package imports as zero. |
| Static checks silently execute Node or another helper | Run closure and source-coordinate checks in the Python controller before Node executable resolution; process count remains zero until the one recovery launch. |
| Several child processes disguise parse checks or retries | Use the one execution itself as the only Node parse/runtime boundary, prohibit child-process coordinates in authored JavaScript and record exactly one process. |
| A process terminal leaks local diagnostics | Persist only exit code, byte counts and stream digests after cleanup and before interpretation; never retain or echo raw streams. |
| Success is admitted without the complete behavior matrix | Require exact case/key order, one mount, scope/view/restrict/schema readings and exact typed short circuits. |
| Temporary source or package data survives | Delete the complete disposable root before stream decoding and require post-cleanup absence. |
| The recovery expands into another speculative control sequence | A pass advances directly to the complete package-unloaded runner; only an observed new failure can justify another recovery. |
| Passing evidence is overstated as Harness or worker proof | Claim only package-unloaded graph behavior plus static closure; withhold runner, package, native boot, worker, provider and useful-development claims. |
| Unrelated worktree material is swept into the tranche | Preserve every untracked path, especially `docs/branding/`, and stage only explicit tranche paths. |

## Residual boundary

A pass proves that the exact accepted modules and corrected fixture form the
required closed package-unloaded graph and produce the exact three-case result
in one isolated process. It does not prove the derived runner, installed
package composition, native-Harness boot, a DeepSeek turn, worker quality,
model/provider access, product authority or production suitability.
