# Threat-model delta — DeepSeek native Harness provider-free edit argument/result coordinate diagnostic recovery

Date: 2026-08-22

Timestamp: 2026-08-22T16:06:16.7328070+10:00 (Australia/Brisbane)

Status: frozen

| Threat | Fail-closed control |
|---|---|
| Consumed occupied evidence is rewritten to fit a new diagnosis | Hash-bind and leave both attempt namespaces unchanged; classify only the new authored-synthetic real-tool variants. |
| A synthetic stand-in falsely proves native edit behavior | Import and execute the exact rc.7 `ToolRuntime`, `dsh-tool-fs` edit registration and `LocalFileSystem`; reject any fixture-owned `edit` definition. |
| Human-readable error prose becomes a routing interface | Route only on authoritative `isError`, stable Harness `error.info.code`, frozen success class and exact hash transition; never parse or retain error text. |
| Plain semantic argument errors are assigned invented specificity | Release the single honest coordinate `edit_error_untyped_argument_constraint` for rc.7 plain errors; do not infer which constraint from result prose. |
| A failed edit mutates the file before returning error | Reset each variant independently and require exact before/after hashes plus target presence; any failure mutation rejects the entire evidence set. |
| A successful edit changes more than the intended target | Use one exact disposable root and target, require the expected canonical hash transition and inventory the root before cleanup. |
| Relative path resolution escapes the fixture | Pass the exact absolute disposable target in every path-bearing variant and independently require every resolved display path to be the expected target. |
| Package drift changes the taxonomy | Bind package names, rc.7 versions, package/runtime/type digests and bounded semantic source markers before execution. |
| Raw authored arguments, content or errors leak into durable evidence | Release only closed IDs/codes, hashes, byte counts and booleans; schema rejects raw content, values, messages, stacks and environments. |
| Provider-free work silently becomes another worker attempt | Native Harness, worker, model, provider and broker counts are fixed at zero; the fixture environment contains no provider credentials and network attempts are forbidden. |
| Disposable state or a child process survives | Resolve one exact child of the fixed worker-root parent, use one bounded Node process, dispose Cordis and require process/root absence before pass evidence. |
| Cleanup removes unrelated user files | Refuse any root outside the exact named child and preserve every repository untracked path, especially `docs/branding/`. |
| A diagnostic result is overstated as a successful DeepSeek worker | Claim only that the rc.7 edit stack can emit the tested provider-free coordinates; useful-worker reliability and candidate production remain unproved. |
| The API Spine or protected refs expand incidentally | Own no product/API/configuration/schema/route/migration/database path and verify all protected refs remain at their frozen full object identity. |

Residual risk: the provider-free matrix cannot recover the occupied model's
discarded arguments and cannot prove that a future DeepSeek worker will choose a
valid edit. It can make a future failure mechanically narrower and therefore
avoid reruns whose only purpose is to rediscover its class.
