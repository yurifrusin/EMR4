# Threat-model delta — DeepSeek native Harness provider-free edit-coordinate integrated-runner stock-headless boot rehearsal

Date: 2026-08-22

Timestamp: 2026-08-22T17:21:28.4758726+10:00 (Australia/Brisbane)

Status: frozen

| Threat | Fail-closed control |
|---|---|
| A different runner is proved than the accepted integrated artifact | Bind canonical and execution-copy byte counts/SHA-256 and require equality before launch and after teardown. |
| A probe shim becomes a substitute runner | Limit it to exact imports, function-type checks, one typed load record and delegation to the unchanged `apply` export. |
| HMR readiness is inferred from process liveness | Require the exact exclusive sentinel sequence before the single atomic patch mutation. |
| The proof accidentally crosses into agent/model execution | Select a deliberate preset-root mismatch and require roots-stage failure plus zero agent/session/turn/broker/model/provider gauges. |
| Console text becomes evidence | Discard stdout/stderr; accept only schema-closed exclusive readiness, load and terminal files. |
| Package installation reaches the network or leaves descendants | Copy the accepted offline seed without npm, preload the network guard and require zero child/network observations. |
| The one process or disposable assembly survives | Own one exact PID/tree, terminate once if necessary and require process/root absence before pass publication. |
| A failed occupied-shaped launch is silently retried | One launched process consumes the attempt; retry/resume/fallback counters are fixed at zero. |
| Historical failures or accepted inputs are overwritten | Treat every predecessor and consumed attempt as immutable and write only beneath this operation root plus its disposable child. |
| Loading proof is overstated as adoption or worker reliability | Keep candidate/runbook, occupied request and useful-worker claims explicitly closed. |
| Product or protected scope expands | Own no product/API/configuration/schema/database path and verify all protected refs and unrelated untracked paths remain unchanged. |

Residual risk: a provider-free roots-stage hold proves the exact integrated
runner loads and its controls are present, but does not prove agent creation,
session/turn behavior, DeepSeek transport, edit choice or useful development.
