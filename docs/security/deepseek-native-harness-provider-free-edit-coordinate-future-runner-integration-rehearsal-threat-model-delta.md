# Threat-model delta — DeepSeek native Harness provider-free edit-coordinate future-runner integration rehearsal

Date: 2026-08-22

Timestamp: 2026-08-22T16:47:32.4934943+10:00 (Australia/Brisbane)

Status: frozen

| Threat | Fail-closed control |
|---|---|
| The accepted runner or classifier is silently rewritten | Hash-bind every accepted input and emit only a derived artifact under the new continuity root. |
| Human-readable rc.7 semantic errors remain a routing interface | Check blank path, empty search and equal search/replacement before dispatch and return only closed decisions plus the shared accepted coordinate. |
| Schema-invalid arguments are confused with semantic denials | Use `defer_to_tool_schema` unless all three relevant values are strings; require the real tool to emit `INVALID_ARGS`. |
| A pre-dispatch denial still reaches the edit tool | Count real `ToolRuntime.execute` calls per variant and require zero for each semantic denial. |
| A synthetic classifier drifts from the accepted mechanism | Cross-check every JavaScript release against the accepted Python classifier and its coordinate schema. |
| A real edit failure is classified from prose | Route only on `isError`, stable `error.info.code`, frozen success class and exact hash transition; never parse or retain messages. |
| A failure mutates the target | Require exact before/after presence, bytes and SHA-256 equality for all seven failure variants. |
| A successful edit is accepted without the intended state transition | Require the exact frozen before/after hash pair for unique and replace-all success. |
| The derived runner changes unrelated lifecycle or authority behavior | Use unique exact source transformations, require closed insertion/use counts and reject any base or derived digest drift. |
| Free-form output enters the terminal | JSON Schemas close every key and enum; retain no arguments, content, values, messages, stacks or environment. |
| The rehearsal silently becomes an occupied worker attempt | Native Harness worker, model, provider and broker counts are fixed at zero; the fixture gets no provider credentials and makes no network request. |
| Disposable state escapes or cleanup removes unrelated data | Resolve one exact child beneath the fixed worker-root parent, dispose Cordis, remove only that child and require absence before pass release. |
| The result is overstated as candidate adoption or useful-worker reliability | Keep the derived runner under continuity evidence only and explicitly leave candidate, runbook and occupied authority closed. |
| Product/API or protected authority expands incidentally | Own no product/API/configuration/schema/route/migration/database path; verify protected refs and preserve every unrelated untracked path. |

Residual risk: this rehearsal can prove that the deterministic controls fit the
accepted future runner and the real edit stack. It cannot prove that a future
DeepSeek turn will choose valid arguments, perform useful development work or
avoid failures outside the closed edit boundary.
