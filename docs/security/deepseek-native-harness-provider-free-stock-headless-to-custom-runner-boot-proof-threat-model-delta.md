# Threat-model delta: provider-free stock-headless-to-custom-runner boot proof

Date: 2026-08-22

Timestamp: 2026-08-22T20:35:32.8728340+10:00 (Australia/Brisbane)

Status: frozen one-process delta

Operation: `deepseek-native-harness-provider-free-stock-headless-to-custom-runner-boot-proof`

## Changed surface

The tranche combines two already accepted provider-free mechanisms inside one
separately identified native process: the pinned rc.7 stock-headless/HMR entry
path and the corrected integrated-runner guard graph. New risk exists only at
their handoff, controlled local factory, typed terminal and cleanup boundary.

Principal threats are source substitution, confusing four root reads with four
roots, loading the obsolete three-argument guard, incomplete import closure,
premature or synthetic readiness, an additional HMR mutation or process,
accidental target/agent/model/provider progress, untyped exception admission,
network or credential escape, raw-stream reliance, retrying a consumed identity
and incomplete cleanup.

## Controls

- Full source IDs, byte lengths and SHA-256 digests bind every accepted
  predecessor and runner/guard/bridge/sanitizer source before launch.
- The contract states two distinct preset roots, four `presets.roots` reads and
  five hook installations as separate typed facts.
- Complete static and literal-dynamic import closure is recalculated against
  the exact disposable module inventory and installed rc.7 package scope.
- The initial HMR patch disables the stock runner, code runtime and telemetry;
  the corrected custom runner cannot appear until the in-process sentinel has
  observed both stock patch registrations.
- Only one atomic patch replacement and one native process are admitted.
- The child environment is credential-scrubbed and preloads the accepted
  fail-closed network guard over socket, TLS, HTTP(S), DNS, datagram, fetch and
  WebSocket primitives.
- The boot probe owns a local scoped `AgentRegistry`, inert sessions, an exact
  two-root preset service and an exact three-tool view. The controlled factory
  throws immediately after the accepted guard coordinate and before agent
  publication, target resolution or request creation.
- Child output is admitted only through exact-schema exclusive sidecars.
  Stdout/stderr and raw exception/environment/path content are neither parsed
  nor retained.
- Attempt identity is written before launch. Every post-launch terminal is
  consumed and there is no retry, resume or fallback.
- Controller acceptance requires source and seed immutability, native-process
  absence and exact disposable-root absence.

## Claim ceiling

A pass proves only that the pinned stock-headless entry point loaded the exact
corrected integrated runner and accepted graph, which reached the four-root-
read/five-hook effective-tool coordinate under one provider-free process. It
does not prove DeepSeek model access, worker usefulness, provider reliability,
target handling, product behavior, production isolation or deployment
suitability.

The consumed materialization fixture remains rejected and is not retried or
reclassified. Product/patient/clinical/protected data, ordinary-practice
changes, provider/model calls, deployment, release, Pages and protected-ref
movement remain closed.
