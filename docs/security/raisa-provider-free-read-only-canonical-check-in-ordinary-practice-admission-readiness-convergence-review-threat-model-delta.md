# Threat-model delta: canonical check-in admission-readiness convergence review

Date: 2026-08-23

Timestamp: 2026-08-23T00:55:04.6617862+10:00 (Australia/Brisbane)

Status: frozen narrow threat delta

## Scope

This tranche is a provider-free, repository-static evidence join. It adds no product feature, command path, admission record, configuration, database object, runtime instrumentation or external connection.

## Assets protected

- default-off A5.1 posture and empty ordinary-practice admission state;
- authored-synthetic allowlist and exact request/response contract;
- the closed twelve-dimension vocabulary and original accepted evidence;
- immutable failed attempt evidence and exact accepted descendant lineage;
- full 40-character Git-object traceability;
- product, patient, clinical and protected evidence boundaries;
- protected refs and every unrelated untracked file.

## Threats and controls

| Threat | Control |
|---|---|
| A design artifact is mistaken for ordinary-practice authority. | Every advanced dimension carries a bounded reason; evidence and report retain `not_ready_for_ordinary_practice_admission`, zero active records and zero enablement. |
| A repaired harness or server lifecycle is mistaken for unknown-commit recovery evidence. | Dimension 7 remains an operational gap because attempt 005 never reached transaction setup, ambiguous response or readback. |
| A schema for future environment bindings is mistaken for operational secret posture. | Dimension 11 remains an operational gap because canonical manifest, secret reference, rotation and current role-binding counts are all zero. |
| An abbreviated or invented Git identifier binds acceptance. | Contract admits only exact lowercase 40-character IDs; validation requires each object to exist and be an ancestor of reviewed HEAD. |
| Free-form model vocabulary drifts the matrix. | Contract fixes twelve ordered IDs and a three-value classification enum; deterministic validation and hostile mutations reject drift. |
| A source file changes after review. | Strict UTF-8, canonical-LF, bare-CR-rejecting SHA-256 bindings cover every input. |
| Reviewer execution reaches product/runtime surfaces. | Reviewer uses standard-library file parsing and local Git object checks only; tests reject `app` imports and network/database/provider tooling. |
| Closeout bureaucracy becomes another widening control loop. | This tranche adds one evidence join only, reuses the clockwork closeout, and creates no reusable control layer beyond its deterministic reviewer. |
| Product or protected state changes incidentally. | Explicit-path staging, before/after protected-ref assertions and preservation of all untracked paths are mandatory. |

## Residual risk

The result is an evidence-based readiness classification, not an operational admission. Unknown-commit recovery and environment/secret posture require later separately authorised evidence. Actual ordinary-practice enablement, client cutover, production deployment and lasting external effects remain closed.
