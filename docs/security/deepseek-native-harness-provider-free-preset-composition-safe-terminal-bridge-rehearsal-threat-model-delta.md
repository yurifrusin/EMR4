# Threat-model delta: preset-composition safe-terminal bridge

Date: 2026-08-22

Timestamp: 2026-08-22T03:42:19.3682524+10:00 (Australia/Brisbane)

Status: **frozen before implementation or execution**

## Scope delta

The accepted predecessor reached private identity admission and then retained
only `unclassified_error`. This tranche connects the already accepted guard's
sanitizer to the runner's typed sidecar and permits one distinct provider-free
process. It adds no new tool, request, target, product or data authority.

## Controls

| Threat | Fail-closed control |
|---|---|
| Raw guard errors escape | Only `sanitizeEffectiveToolTerminal` output is admitted; its coordinate and optional safe tool-name detail are schema-enumerated. |
| A new process is treated as a retry | Distinct operation, attempt, identity and terminal objective; both predecessors remain consumed and immutable. |
| Sanitizer output silently broadens vocabulary | Deterministic tests bind the exact accepted nine-code set and reject unknown fields or values. |
| Linkage absence projects factory state | Controller-only absence retains nullable stage/error/factory fields. |
| Composition publishes an agent | Synchronous commit veto, registry/lifecycle counters and postrollback reads remain mandatory. |
| Configured provider route produces traffic | No turn/message exists; broker and network gauges remain independent and zero. |
| Failure residue survives | Evidence is read before exact process termination and root deletion; publication requires complete cleanup. |

## Residual risk

The safe coordinate may identify a preset-mount, scope, inherited-tool,
restriction, schema-view or effective-view mismatch without repairing it. That
would justify a later source reconciliation, not an in-attempt correction or
retry.

## Security acceptance

Accept only one finite terminal joined to exact readiness/HMR, downstream zero
gauges, immutable package/bundle, target absence and cleanup. Preserve every
provider, product, data, production, Pages and protected-ref closure.
