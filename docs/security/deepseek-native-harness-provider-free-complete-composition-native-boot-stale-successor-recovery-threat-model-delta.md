# Threat-model delta — complete-composition stale-successor recovery

Date: 2026-08-20

Timestamp: 2026-08-20T12:29:30.8678914+10:00 (Australia/Brisbane)

Status: `frozen`

## Boundary

This recovery changes only governance successor selection and its deterministic
interlock. The accepted complete-composition terminal and every accepted
product source remain immutable. No provider, worker, Docker, database, product
route or data path executes.

| Threat | Fail-closed control |
|---|---|
| An already accepted operation is selected again from stale prose | Before any clean-closeout or user-decision projection, reject a next operation ID that already occurs in the validated Continuity graph. |
| A filename/prose guard misses the accepted operation | Use exact graph node identity, not Markdown filenames, plan-path formatting or a hard-coded successor. |
| Invalid incident vocabulary creates serial check reruns | Validate every register-bound date, identifier, enum and const in the caller intent before graph, register or canonical projection; parameterized hostile tests cover each field class. |
| Recovery erases the accepted Harness result or its incidents | Roll back only the selected clockwork generation, preserve its Git source and rejected receipts, then republish the same accepted result with all earlier and new incidents. |
| A rollback selects arbitrary history | The existing clockwork may restore only the immediately previous full-Git-bound generation and must report byte-exact readback. |
| Correcting the successor silently authorises attempt 005 execution | The new latch authorises fresh rehydration and plan freeze only; occupied execution requires a distinct plan, preexecution receipt, WorkOrder and one-run latch. |
| A worker or provider is invoked during the control repair | DeepSeek is declined; Gemini remains ineligible absent a materially changed candidate; all acceptance is deterministic and provider-free. |
| Product or practice authority expands with the corrected product successor | Retain default-off, no ordinary admission, no product/data, no client/grammar/waiting-area and no production/deployment boundaries explicitly. |
| Protected refs or user-owned files move | Verify all four protected refs remain exact, preserve `docs/branding/` and unrelated untracked files, and stage explicit paths only. |

## Residual claim

Passing proves only that the clockwork now rejects recorded operation IDs and
that the accepted Harness closeout points to one unrepeated planning successor.
It does not prove attempt-005 execution, DeepSeek reasoning or coding quality,
database behavior, ordinary-practice readiness, production isolation or future
Harness versions.
