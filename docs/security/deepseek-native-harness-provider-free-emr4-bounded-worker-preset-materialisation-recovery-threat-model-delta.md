# Threat-model delta: DeepSeek native Harness emr4-bounded-worker preset materialisation recovery

Date: 2026-08-20

Timestamp: 2026-08-20T10:30:02.1366272+10:00 (Australia/Brisbane)

Status: `frozen`

Operation:
`deepseek-native-harness-provider-free-emr4-bounded-worker-preset-materialisation-recovery`

## Delta

The accepted service-injection recovery proves a future host declaration but
leaves the selected custom preset absent. This tranche adds one exact
repository-retained preset payload and an offline semantic proof. It starts no
Harness, Node, agent, broker, model or provider process.

| Threat | Fail-closed control |
|---|---|
| Treating the preset as the authority boundary | Record the raw inherited plugin surface separately from the accepted post-mount restriction. Require the guard and outer broker allowlist to remain independently mandatory. |
| Silently exposing `write`, `grep` or `read_image` | Prove exact unconditional and conditional registrations from pinned rc.7 source, then require the accepted restriction to yield only sorted `edit`, `glob`, `read` for minimal and attachment-present cases. |
| Guessing a custom-preset layout | Bind rc.7 discovery semantics for `.agent-presets/<id>/agent.cordis.yml`, exact preset-id grammar, user-root inclusion and first-root-wins behavior. |
| Path traversal or installation ambiguity | Require exact relative components, reject absolute paths, separators outside the frozen path, invalid ids, symlinks and unexpected path types. Retain the payload under the operation continuity directory only. |
| Malformed or surprising YAML | Require exact UTF-8/LF bytes, safe parse, exact row/key order and values, no aliases, tags, groups, dynamic expressions, metadata or surplus fields. |
| Plugin substitution | Bind exact local-cache registry SHA-1, SHA-512 integrity, tar SHA-256 and `package.json`/`lib/index.js` member byte/digests before reading plugin names, injection or tool registration semantics. |
| Missing required plugin configuration | Require `sampleOverCapGlobResults: false` as the search row's sole config; missing, true, non-boolean or surplus config fails closed. |
| Assuming tool activation despite missing services | Retain exact plugin injection declarations and accepted base/headless service-row evidence; label live activation and mount as unproved until a separately authorised native proof. |
| Replacing or weakening the accepted guard | Bind its exact source/contract digest and causal order. Reject selected-name drift, missing inherited names, own-layer names, bypass or reordered restriction. |
| Writing a real Harness home accidentally | The controller may write only the repository continuity payload and named evidence. `%USERPROFILE%/.agent-presets`, external roots and disposable installations are forbidden. |
| Source leakage | Inspect tar members in memory and retain only identities, digests, safe names, booleans, counts and the exact small preset payload. |
| Accidental runtime/provider action | The controller has no subprocess or network path; acceptance requires zero Node/Harness/agent/session/turn/broker/model/provider/network/Docker/database counts. |
| Corrupting immutable failure evidence | Bind both consumed attempts by exact source and artifact digests; never write their paths and never retry, resume or reclassify them. |
| Unreviewed semantic drift | Run deterministic gates first, then one fresh Gemini 3.7 Flash/high isolated read-only veto on the exact clean candidate. |
| Governance drift | Use clockwork as sole canonical writer with separate check, publish, postpublication verification and idempotent check. |

## Residual risk and claim ceiling

Passing evidence proves the exact materialised payload and its deterministic
composition with the already accepted guard. It does not prove live preset
discovery, mount, service activation, scope creation, effective schema in a
native process, occupied worker completion, DeepSeek reasoning quality or
provider reliability. Those claims remain closed behind a separately frozen
successor.

No product/data/runtime, ordinary-practice, deployment, Pages or protected-ref
authority changes. Local/origin `master` and `handoff/current` remain exact
`2e34bdad732fdab32fbf778280b3d3c70d66d602`; `docs/branding/` and every
unrelated untracked file remain preserved.
