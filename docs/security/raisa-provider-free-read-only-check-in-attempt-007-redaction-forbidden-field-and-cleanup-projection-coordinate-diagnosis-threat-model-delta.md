# Threat-model delta — check-in attempt-007 redaction and cleanup-projection coordinate diagnosis

Date: 2026-08-23

Timestamp: 2026-08-23T03:47:43.2463123+10:00 (Australia/Brisbane)

Status: `frozen`

Reasoning level: `high`

## Scope

This delta covers exact immutable-terminal, local source, AST and JSON/schema
inspection plus deterministic in-process projection fakes. It adds no Docker
object, PostgreSQL process, SQL, database operation, attempt 008, worker,
provider, product, data, ordinary-practice, production, deployment, release,
Pages or protected-ref surface.

## Threats and controls

| Threat | Control | Fail-closed result |
|---|---|---|
| A plausible source narrative is substituted for exact proof | Byte-exact source bindings, closed AST predicates and exact terminal hashes | `insufficient_closed_evidence` or reject |
| Only the known bad key is tested and another prospective success key conflict is missed | Enumerate the complete final evidence key-path surface, including every contract-derived projection, through the exact redactor | Reject diagnosis |
| The forbidden-key vocabulary is weakened to make the rehearsal pass | Future repair may safely rename/project the boundary field but may not alter redactor coverage | Reject repair boundary |
| AST line numbers drift while semantic control flow changes | Bind exact source digest and structural AST relations, not prose line numbers alone | Reject source mapping |
| A fake accidentally starts a subprocess, Docker or database | Dependency-injected pure functions and tests asserting zero external calls except Git resolution | Stop before evidence write |
| The wrapper invents successful cleanup from external zero-residue inspection | Preserve immutable `not_started`; diagnose the projection and require a future typed base-owned cleanup bridge | Reject reclassification |
| The wrapper loses valid cleanup again after a late error | Future bridge carries only the base-finalized, schema-validated cleanup object into sanitised failure evidence | Reject future repair |
| Raw exception, source, path or sensitive value leaks into retained evidence | Closed schema, bounded enums/counts/digests and the existing redaction predicate | Reject evidence |
| Attempt 007 is retried, overwritten or relabelled | Exact predecessor hashes, exclusive new namespace and no occupied execution path | Reject tranche |
| Diagnosis silently becomes an attempt-008 plan | Contract forbids attempt number, checkpoint, Docker/database command and occupied authority | Reject closeout |
| Product or API authority expands through rehearsal language | Exact path allowlist and boundary tests deny product/API/configuration/ordinary-practice mutations | Stop before commit |
| Unrelated worktree data is damaged | Explicit-path staging, no broad cleanup, and untracked preservation readings | Stop without mutation |

## Residual boundary

The diagnosis can prove the exact prospective forbidden-field collision, its
post-cleanup escape coordinate and the wrapper's deterministic cleanup-
projection collapse. It cannot prove that attempt 007 completed rollback,
unknown-response commit recovery, role cleanup before teardown or successful
transaction semantics. External zero Docker residue remains separate evidence.
The selected two-gear repair remains unimplemented, and no database successor
is authorised.
