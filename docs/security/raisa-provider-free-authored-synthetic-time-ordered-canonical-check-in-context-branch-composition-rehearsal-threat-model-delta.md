# Threat-model delta — authored-synthetic time-ordered canonical check-in-context branch composition rehearsal

Date: 2026-08-24

Timestamp: 2026-08-24T15:55:06.2330342+10:00 (Australia/Brisbane)

Status: `frozen_for_provider_free_rehearsal`

Task baseline: `d9114b3e9a72fa94acc0a7ab3657f17043c6be0a`

## Assets protected

- the unchanged canonical check-in adapter and its default-off mounting policy;
- the separation between authored-synthetic hypotheses and historical or
  ordinary-practice claims;
- current authority, freshness, signed-evidence, waiting-area, idempotency,
  transaction and readback fail-closed ordering;
- patient-free evidence and the absence of provider/runtime/data access; and
- protected refs, clockwork-owned surfaces, `docs/branding/` and unrelated
  untracked files.

## Threats and fail-closed controls

| Threat | Fail-closed control |
|---|---|
| Calling a 30-cell table minimal without proof | Bind all 5×6 source/outcome pairs exactly once; require every row and column to contain all four middle-axis values and verify all 74 cross-family pairs. |
| Pairwise bookkeeping that never reaches a claimed branch | Freeze unmasked witness cells for every axis value and each outcome; report masked axes separately from causally observed axes. |
| Confusing precedence masking with failed coverage | Record the earlier typed stop and exact callback boundary; do not claim a masked later transition was executed. |
| Making test code define new product behaviour | Derive every expected reason/outcome/callback prefix from the unchanged adapter contract and fail if its exact Git blob changes. |
| Treating synthetic timing or sequence as historical frequency | Label every state and transition authored-synthetic; make no occurrence, prevalence, representativeness or clinical claim. |
| Reopening the diary trove to enrich a scenario | Forbid all historical, fixture, control, archive and `local_data` access; consume only the tracked frozen axis contract. |
| Exact replay releasing arbitrary stored content | Seed replay only from an isolated successful call through the same unchanged adapter and require its exact patient-free response. |
| Precommit failure leaving a false mutation | Snapshot the authored-synthetic state, inject a failure before commit, require rollback and exact state restoration. |
| Unknown commit or missing readback being called success | Require `committed: null`, `outcome_unknown`, no receipt and the exact stop reason; describe simulated memory state without claiming durable truth. |
| Leaking synthetic secrets or patient-shaped fields into evidence | Reject evidence tokens, idempotency material, patient IDs, names, reasons and notes from every serialized generated artifact. |
| Expanding into product, database or route work | Restrict writes to the plan, threat delta, scenario contract, harness, focused tests and evidence/closeout artifacts. |
| Bypassing the clockwork single writer | Publish live governance surfaces only through the accepted closeout driver and its explicit-stage manifest. |

## Residual risk and claim boundary

Passing can show that the existing adapter deterministically composes the
frozen authored-synthetic transitions with the expected fail-closed
precedence. It cannot show real-practice frequency, historical occurrence,
database transaction durability, mounted-route behavior, ordinary-practice
readiness or product correctness beyond the exercised in-memory contract.

No historical data, patient/clinical/product data, provider/model/network,
database, route, client, runtime, configuration, ordinary-practice activation,
production, deployment, release, Pages or protected-ref authority opens.

