# Threat-model delta — check-in prospective-success redaction and typed cleanup-projection conformance repair

Date: 2026-08-23

Timestamp: 2026-08-23T04:33:54.0223195+10:00 (Australia/Brisbane)

Status: `frozen`

Reasoning level: `high`

## Scope

This delta covers a local contract field rename, deterministic prospective-
evidence projection, source AST parity, redaction/schema admission and a typed
post-cleanup terminal bridge. It grants no occupied execution, Docker object,
PostgreSQL process, SQL, database, attempt 008, worker, provider, product,
ordinary-practice, production, deployment, release, Pages or protected-ref
surface.

## Threats and controls

| Threat | Control | Fail-closed result |
|---|---|---|
| The known key is renamed but another result key conflicts | Complete prospective/runtime key-path parity and whole-projection redaction in static admission | Reject before any occupied work |
| The projection omits a future runtime field | Current-source AST surface must equal the normalized prospective surface exactly | `prospective_projection_shape_mismatch` |
| The redactor is weakened to admit the old field | Bind and test unchanged forbidden vocabulary and predicate AST; repair only the contract key | Reject candidate |
| The new name silently opens the boundary | Contract/schema validation requires ten fields and every value false | `closed_boundary_open` or schema reject |
| A redaction/schema failure still escapes after cleanup | One typed base-owned post-finalization helper catches and converts every intended late failure | Sanitised typed failure terminal |
| Cleanup is invented or collapsed | Helper accepts only the finalized cleanup object; tests require structural identity in failure evidence | Reject terminal construction |
| Success artifacts are written before late validation | Terminal helper validates redaction and schema before mutually exclusive writes | No success release |
| Failure evidence leaks the bad field, value or raw exception | Failure projection contains bounded stage/code/predicates only and is itself redacted | Reject write |
| Static checking accidentally starts Docker or a process | Ordering AST assertions and process fakes prohibit Docker-capable calls in projection admission | Stop before mutation |
| Attempt 007 is rewritten to fit the descendant | Exact immutable artifact digests and excluded historical namespace | Reject tranche |
| A deterministic repair is treated as transaction proof | Result vocabulary states conformance repair only; database semantics remain unproved | Reject acceptance overclaim |
| Workflow controls add more cost without preventing reruns | Efficacy compares the two diagnosed failure coordinates with their new pre-occupation/typed controls | Reject closeout efficacy claim |
| Product/API authority expands through shared harness language | Exact allowed-path set and negative source scans exclude product/application surfaces | Stop before commit |
| Unrelated worktree data is damaged | Explicit-path staging, no broad cleanup, machine worktree readings | Stop without mutation |

## Residual boundary

The repair can prove that the complete currently representable success surface
passes the unchanged redactor before occupied work and that deterministic late
terminal failures retain base-finalized cleanup. It cannot prove attempt 007's
transaction semantics, internal role cleanup, successful unknown-response
recovery, or any future occupied attempt. A later attempt-008 plan and any
database execution remain separately gated.
