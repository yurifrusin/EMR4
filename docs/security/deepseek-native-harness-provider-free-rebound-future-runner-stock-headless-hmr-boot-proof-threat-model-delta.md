# Threat-model delta: rebound future-runner stock-headless HMR boot proof

Date: 2026-08-21

Timestamp: 2026-08-21T23:42:28.7565979+10:00 (Australia/Brisbane)

Status: **frozen before execution**

## Scope delta

This tranche adds one bounded local native Harness process to the previously
accepted provider-free rebound future-attempt fixture. It grants no DeepSeek
worker, model/provider, network, product, data, runtime or integration authority.

## Assets

- exact accepted rebound runner, helper, controller and inert-target bindings;
- exact cached `@deepseek-ai/dsh@0.1.0-rc.7` package identity;
- the closed canonical bundle roster and its unchanged manifest;
- the readiness ledger, post-HMR typed sidecar, all-zero broker reading and sole
  controller terminal;
- process absence, target absence and complete disposable-root cleanup.

## New trust boundaries and controls

| Threat | Fail-closed control |
|---|---|
| Execution modules contaminate the accepted bundle | Canonical bundle and execution-copy subtrees are siblings; the accepted reader rejects any extra bundle member; runner/helper copies must be hash equal. |
| Native loading advances to worker creation | Deliberately mismatched frozen preset-root coordinates force the exact runner to fail before `agents.create`; only the exact typed stage/cause is accepted. |
| Missing service masquerades as the intended proof | `required_service_missing` and every stage/cause other than `preset_root_roster_admission` / `preset_root_roster_mismatch` fail acceptance. |
| Runner emits an uncontrolled generic terminal | Its terminal path is an already existing canonical manifest; exclusive `wx` fails closed; the manifest digest must remain unchanged and no runner fallback terminal may exist. |
| Controller fabricates pre-request status | Acceptance requires the helper-authored sidecar and a separately built, identity-bound all-zero broker reading; the accepted controller performs the join. |
| Raw errors or streams escape typed bounds | Helper uses closed stage/cause/error vocabularies; stdout/stderr go to the null device and are neither retained nor read; evidence stores no raw message, stack, path, cause or stream. |
| Package installation reaches the network | Exact cached tarball is verified and installed offline; proxy/credential variables are removed; a preloaded network guard records attempts; any attempt rejects acceptance. |
| More than one attempt occurs | One absent canonical output gate, one `Popen`, no retry/resume path, immediate post-sidecar termination if live, and exact process/root cleanup checks. |
| HMR races before stock readiness | Changed patch is written atomically only after the exact two-event readiness prefix, once. |
| Target or product state is touched | Target is absent before and after the process; failure precedes agent creation; product/config/API/database and all data authorities remain closed. |
| Evidence survives without cleanup | Canonical evidence is accepted only after process absence, unchanged bundle validation and disposable-root removal. |

## Residual risk

The proof establishes only local rc.7 lifecycle controllability at one deliberate
pre-request subcoordinate. It does not establish an occupied worker's coding
quality, DeepSeek provider reliability, successful model transport, budget
behavior, or product-development fitness. Those require separately frozen later
tranches.

## Security acceptance

Security acceptance requires exact source/package bindings, one process, the
exact typed sidecar-plus-broker-zero terminal, no network or target activity,
unchanged canonical bundle, no raw retention, complete cleanup, preserved
protected refs and preservation of every unrelated untracked file.

