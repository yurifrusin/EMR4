# Threat-model delta: integrated-runner accepted guard-graph materialization recovery

Date: 2026-08-22

Timestamp: 2026-08-22T19:37:15.5000864+10:00 (Australia/Brisbane)

Status: frozen provider-free delta

Operation: `deepseek-native-harness-provider-free-integrated-runner-accepted-guard-graph-materialization-recovery`

## Changed surface

The tranche adds one corrected disposable module graph and one provider-free
installed-registry fixture. Its risks are accepted-source substitution,
incomplete import closure, an uncontrolled post-guard continuation, dynamic
exception leakage and accidental retry.

## Controls

- Runner, guard, bridge and sanitizer bytes must match four exact source-owned
  size/hash pairs before materialization and after readback.
- The relative and bare import graph is parsed from every executable module;
  every edge must match a frozen finite inventory and every file target must
  exist before the attempt identity is persisted.
- The installed factory uses one real Cordis scope and a closed three-tool
  fixture. A controlled sentinel stops immediately after setup passes and
  before agent publication, target resolution, an agent loop or model access.
- The old input-invalid coordinate is forbidden in a passing result.
- Only an allowlisted success coordinate, fixed counters and booleans may enter
  structured evidence. Raw message, stack, cause, path, environment, prompt,
  response, reasoning, session content and exception objects are forbidden.
- Native Harness, provider, broker, network, database, Docker and product-target
  counts must remain zero.
- The process receives the minimum Windows environment plus the exact Node
  directory in `PATH`; no provider key is supplied.
- Cleanup validates the exact disposable child before removal and rejects
  parent/root deletion, symlink traversal or cleanup outside that child.
- One process is permitted. Once started, any result consumes the distinct
  identity; retry, resume, fallback, second fixture and native Harness launch
  remain forbidden.

## Claim ceiling

A pass proves only that the accepted corrected graph, beside the byte-identical
runner, crosses the installed registry setup boundary beyond the demonstrated
old coordinate and stops at the controlled sentinel. It does not prove native
Harness boot, a DeepSeek turn, provider reachability, useful worker output,
product behavior, runtime safety, deployment or production suitability.

Protected evidence, product/patient/clinical data, ordinary-practice changes,
deployment, release, Pages and protected-ref movement remain closed.
