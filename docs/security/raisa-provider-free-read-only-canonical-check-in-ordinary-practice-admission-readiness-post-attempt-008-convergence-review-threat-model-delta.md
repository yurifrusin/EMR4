# Threat-model delta: post-attempt-008 check-in admission-readiness convergence

Date: 2026-08-23

Timestamp: 2026-08-23T07:40:10.6116676+10:00 (Australia/Brisbane)

Status: `frozen_narrow_threat_delta`

## Scope

This tranche is a provider-free, repository-static classification of two exact
accepted evidence packets. It performs no product, route, configuration,
database, Docker, SQL, provider, network or deployment action.

## Protected assets

- the prior twelve-dimension order, vocabulary, evidence and not-ready verdict;
- the immutable attempt-008 terminal, attestation and one-shot semantics;
- default-off and zero-ordinary-admission posture;
- the independent `environment_manifest_and_operational_secret_posture` gap;
- API-Spine read/command/event separation;
- product, patient, clinical, historical and protected evidence;
- protected refs and every unrelated untracked file.

## Threats and controls

| Threat | Fail-closed control |
|---|---|
| A successful disposable rehearsal is mistaken for ordinary-practice authority. | The output can change only dimension 7, retains dimension 11, retains verdict `not_ready_for_ordinary_practice_admission` and records zero ordinary admission. |
| Partial transaction evidence is overclaimed. | Nine exact predicates jointly require rollback zero, incomplete response without success/retry, authoritative exactly-once readback, restricted forced-RLS posture and complete cleanup. |
| The environment/secret gap is silently folded into transaction evidence. | Dimension 11 and its accepted `architecture_has_zero_operational_instances` basis are immutable inputs and required outputs. |
| A model invents a new classification or basis label. | Contract and schema admit only the three existing classifications and exact frozen row-specific bases. |
| An abbreviated or invented source binds acceptance. | Lowercase 40-character commit syntax, object existence and ancestor checks are mandatory. |
| Source bytes drift after acceptance. | Strict UTF-8 canonical-LF SHA-256 bindings cover every input; bare CR and digest mismatch reject. |
| API compatibility is widened into product admission. | Read-only API-steward review preserves GraphQL as read-only and the existing explicit REST command, confirmation, practice, idempotency and audit boundaries without changing an API artifact. |
| Another bureaucratic layer is created. | The tranche adds one disposable evidence join and its tests only; no reusable form, ledger or runtime control is authorised. |
| Product or protected state changes incidentally. | No `app` import or product-path diff; explicit-path staging; protected-ref and untracked-path checks before publication. |

## Residual risk

Even if dimension 7 becomes satisfied, the result is still authored-synthetic
disposable PostgreSQL evidence. It does not prove a canonical operational
environment manifest, live runtime-role binding, secret-reference custody,
rotation evidence, ordinary-practice activation, client cutover, production
deployment or real-world behavior. Those remain separately closed.
