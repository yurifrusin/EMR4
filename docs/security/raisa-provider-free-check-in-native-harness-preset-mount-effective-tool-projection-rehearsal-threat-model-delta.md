# Threat-model delta — provider-free native preset mount and effective-tool projection

Date: 2026-08-20

Timestamp: 2026-08-20T19:06:25.3877564+10:00 (Australia/Brisbane)

Status: `frozen`

Operation:
`raisa-provider-free-check-in-native-harness-preset-mount-effective-tool-projection-rehearsal`

## Changed attack and failure surface

This tranche joins two already accepted provider-free components: the native
service now sees the exact user-trust EMR4 preset row, and the effective-tool
guard already constrains a mounted child scope to `edit`, `glob` and `read`.
The only newly admitted runtime surface, after separate review and checkpoint,
is one disposable provider-disabled preset mount and projection process.

| Risk | Fail-closed control |
|---|---|
| Re-enabling the derived user root broadens preset authority | Require exactly shipped `system` then disposable user `user`; select exactly one canonical id/path/digest and reject missing, duplicate, broken, shadowed or surplus EMR4 rows. |
| A user-trust preset is silently elevated to system trust | Bind the selected row to exact `user` trust in deterministic evidence, native terminal and hostile fixtures. |
| Mount resolves a different or mutable composition | Bind exact 158 bytes, digest and two package entries before mount; re-read package and preset digests before and after any native process. |
| Preset mount registers an unapproved child-scope tool | The accepted guard rejects any `knownNames - restrictableNames` before restriction and writes no success terminal. |
| An expected tool is not inherited but restriction proceeds | Prove all three selected names exist in `restrictableNames`; a missing name closes before `restrict()`. |
| Surplus inherited tools leak into the model-facing view | Permit surplus only before restriction; require final schemas to equal the sorted three-name set exactly. |
| A malformed, duplicate or unsafe schema name leaks into evidence | Reject the view; durable evidence permits only stable coordinates and sorted names matching the safe tool-name grammar. |
| The native runner drifts into agent or provider work | It injects no agents/session/model/broker surface, creates only a disposable scope, contains no turn or tool execution and runs with provider/network denial. |
| The consumed failed native composition is retried or rewritten | Use one newly named attempt and new evidence namespace; all predecessor terminals remain immutable. |
| Preterminal failure again produces no useful coordinate | Reuse the accepted bounded event writer and exclusive terminal path; first missing event or closed guard code is retained without raw logs. |
| Failure triggers an automatic retry | First process creation consumes the checkpoint allowance; retry count is structurally zero and fallback is absent. |
| A disposable Harness home or process survives | Exact owned-process termination/wait and literal disposable-root cleanup are terminal requirements; absence is checked before acceptance. |
| Model-generated review output becomes the exact command ledger | Deterministic code owns argv, exit codes, counts and digests; Gemini owns only the bounded semantic veto. |
| Provider-disabled proof is overstated as worker reliability | Claims explicitly exclude agent session, tool execution, DeepSeek response, occupied coding work, product/database behavior and production suitability. |

## Protected boundaries

No DeepSeek/model/provider/broker/network request, agent session, turn, tool
execution, attempt 006, Docker, PostgreSQL, SQL, database, product source,
configuration, API, schema, route, feature flag, allowlist, ordinary-practice,
generic-status `Arrived`, grammar, client, waiting-area, product/patient/
clinical data, production, deployment, release, Pages or protected-ref action
is authorised.

Passing evidence may prove only one provider-disabled disposable preset mount
and exact effective-tool projection under pinned rc.7 semantics.
