# Threat-model delta: check-in relay-free recovery attempt 004

Date: 2026-08-20

Timestamp: 2026-08-20T02:54:58.3293864+10:00 (Australia/Brisbane)

Status: `frozen`

This delta authorises construction and, only after exact preexecution admission,
one separately named provider-free disposable PostgreSQL attempt using the
accepted corrected harness. It grants no product, ordinary-practice or model
authority.

## Assets and trust boundaries

- Immutable attempts 001-003 failure, envelope, cleanup and execution-count
  evidence.
- Full 40-character plan, repair, transport, runtime-role, no-database and
  native-Harness proof bindings.
- One fixed attempt-004 namespace and one terminal envelope.
- One internal Docker network, server and fixed-action sidecars.
- In-memory ephemeral credentials delivered only after exact inspection.
- Admin-owned forced-RLS probe relations and a non-owner `NOBYPASSRLS` role.
- Exact terminal OCI state, restricted authoritative readback and sanitized
  evidence.

Trust crosses only the host Docker API, post-inspection attached stdin, the
captured internal network, PostgreSQL authentication/RLS and closed validators.
Attachment output/status, Docker logs, model prose and process timing are not
outcome evidence.

## Threats and controls

| Threat | Fail-closed control |
|---|---|
| Attempt 004 overwrites or launders a predecessor failure | A distinct hard-coded namespace is collision-checked; attempts 001-003 are hash-bound and immutable. |
| An abbreviated or guessed Git source enters the envelope | Every source is resolved as a full 40-character commit and must be an ancestor of the execution candidate. |
| The accepted lifecycle repair drifts before execution | Static admission binds the repaired harness digest and executes both real-call and pre-registry cleanup tests. |
| A path argument redirects or overwrites evidence | The adapter exposes only `--check` and `--execute`; terminal paths are a closed code allowlist and any collision denies. |
| More than one occupied execution occurs | One clockwork checkpoint admits one invocation; the terminal namespace permanently blocks reuse; no retry path exists. |
| A post-create exception strands a never-started object | The accepted helper owns the full pre-registry interval, verifies exact Created-state ownership, removes only the resolved full ID and proves absence. |
| Host relay or hidden result-channel ambiguity returns | Listeners, published ports, relays, Docker exec, multiprocessing and queues are forbidden; OCI state plus prior observer proof is the only caller classification. |
| Credential, nonce or raw runtime output enters evidence | Credential delivery follows exact configuration inspection; recursive redaction rejects secrets, nonces outside the ownership label, raw output, logs, SQL, paths and object IDs. |
| Rollback leaves a partial packet | Fresh same-practice restricted readback must observe zero receipt, effect and audit members. |
| Incomplete response is retried or released as success | Retry and success-release counts are structurally zero; only restricted authoritative readback may classify committed exactly once. |
| Cross-practice inference or privilege escalation occurs | Forced RLS, transaction-local scope, non-owner `NOBYPASSRLS`, closed grants and other-practice zero visibility. |
| Partial or contradictory committed state passes | Exact packet cardinality, identities, digest, action/outcome and cross-references are required; every other shape denies. |
| Cleanup touches unrelated objects or leaves residue | One owner acts only on captured IDs after full reinspection; pass/failure requires role, attachments, containers, network and matching labels absent. |
| DeepSeek or another worker silently enters the proof | Attempt 004 contains no model role; any occupied worker requires a separate v2 WorkOrder, no-database admission and one-run latch; Claude Code fallback is forbidden. |
| Rehearsal evidence is mistaken for API/product admission | Evidence is authored-synthetic, provider-free and disposable; no route/config/client/flag/allowlist or product record changes. |

## API Spine boundary

The exercise preserves one explicit practice-scoped, idempotent and auditable
REST-command transaction shape. GraphQL remains read-only, events are not
command authority, default denial remains exact, and no API Spine artifact is
modified. No model may write to the database.

## Residual limits

One serial authored-synthetic rollback and caller-level incomplete-terminal-
response run does not prove in-WAL crash behavior, distributed partition,
driver/pool behavior, concurrency, restart recovery, product-schema
compatibility, ordinary-practice safety or production availability.

## Closed boundaries

No live/existing/cloud/product database, external network, provider/model call,
product/patient/appointment/clinical/historical/protected data,
ordinary-practice enablement, feature-flag or allowlist change, route or schema
change, generic-status `Arrived`, action grammar, first-party client,
waiting-area movement, REST/OpenAPI, GraphQL, async contract, production,
deployment, release, Pages, protected evidence access or protected-ref movement
is authorised.
