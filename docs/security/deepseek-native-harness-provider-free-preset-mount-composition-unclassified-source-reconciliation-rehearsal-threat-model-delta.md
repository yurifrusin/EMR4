# Threat-model delta: DeepSeek native Harness preset-mount composition-unclassified source reconciliation

Date: 2026-08-22

Timestamp: 2026-08-22T08:10:09.7608886+10:00 (Australia/Brisbane)

Status: **frozen with the source-reconciliation plan**

## Boundary

This tranche reads exact accepted controller bytes and exact installed rc.7
package-seed members. It starts no Node, native Harness, worker, model or
provider process and changes no package seed, product source or protected ref.

## Threats and controls

| Threat | Fail-closed control |
|---|---|
| A changed package seed is mistaken for the consumed runtime | Require the accepted member byte counts and SHA-256 bindings before interpreting any source anchor. |
| A descriptive diagnosis is promoted without a complete source chain | Admit only the two closed semantic results; every required anchor and ordering relation must pass or the result is `source_evidence_insufficient`. |
| A likely source route is misreported as the exact retained exception | Record that no raw exception, message, stack, cause or direct private-context value was observed; claim only a sufficient deterministic source explanation. |
| The bridge is credited with sanitizing an error raised before entry | Prove the mount-handle dereference precedes the bridge call and separately prove the bridge `try` begins after input validation. |
| Source inspection becomes execution | Permit Python/PowerShell file and Git reads only; prohibit Node, the native Harness, workers, models, providers and dynamic package import. |
| A diagnosis silently becomes a repair or retry | The evidence may name one prospective correction coordinate but cannot modify the accepted runner/guard/bridge/sanitizer or launch another attempt. |
| Unrelated worktree material is swept into the tranche | Preserve every existing untracked path, especially `docs/branding/`, and stage only explicit tranche paths. |

## Residual boundary

Even a passing attribution does not prove a repaired native runtime, occupied
DeepSeek quality, preset mounting, provider access, product authority or
production suitability. A separately frozen process-free correction tranche
must precede any separately authorised native retry.
