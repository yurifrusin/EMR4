# Threat-model delta: check-in relay-free recovery attempt 006

Date: 2026-08-23

Timestamp: 2026-08-23T01:29:01.5099111+10:00 (Australia/Brisbane)

Status: `frozen`

This delta authorises deterministic construction and, only after a separate
exact preexecution checkpoint, one provider-free disposable PostgreSQL
attempt. It grants no model-provider, product or ordinary-practice authority.

## Assets and trust boundaries

- Immutable attempts 001-005 evidence, envelopes, cleanup and execution counts.
- Full 40-character plan, lifecycle, transport, runtime-role and protected
  bindings.
- One fixed attempt-006 namespace and one terminal envelope.
- One internal Docker network, server, retained attachment and fixed sidecars.
- In-memory ephemeral credentials delivered only after exact inspection.
- Admin-owned forced-RLS probe relations and a non-owner `NOBYPASSRLS` role.
- Exact terminal OCI state, restricted authoritative readback and sanitised
  evidence.

The occupied rehearsal crosses the host Docker API, post-inspection attached
stdin, a captured internal network, PostgreSQL authentication/RLS and closed
validators. Model prose, attachment output, Docker logs and timing are not
acceptance evidence.

## Threats and controls

| Threat | Fail-closed control |
|---|---|
| Attempt 006 overwrites or launders a predecessor | A distinct hard-coded namespace is collision-checked; attempts 001-005 are hash-bound and immutable. |
| An abbreviated or guessed Git source enters evidence | Every source is a full 40-character commit resolved and ancestry-checked by machine. |
| The repaired lifecycle drifts | Static admission binds the exact repaired harness, repair contract, evidence and accepted Git sources. |
| A path argument redirects evidence | The adapter exposes only `--check` and `--execute`; terminal paths are code-owned and collision-denied. |
| More than one occupied execution occurs | A one-run checkpoint admits one invocation; terminal collision blocks reuse; no retry, resume or fallback exists. |
| Early stdin release stops the server | `_start_attached` leaves stdin open; post-readiness liveness is checked while attached; final teardown is the sole close owner. |
| A stopped or malformed server state leaks detail | Only the accepted nine-key sanitised projection survives; malformed values collapse fail closed. |
| Host relay or hidden result channel returns | Published ports, listeners, relays, Docker exec, multiprocessing and queues remain forbidden; OCI state plus observer proof is the caller classifier. |
| Credentials or raw runtime values leak | Credentials are post-inspection stdin values; recursive redaction rejects secrets, raw output, logs, SQL, paths and object IDs. |
| Rollback leaves partial state | Fresh same-practice restricted readback must observe zero effect, receipt and audit members. |
| Incomplete response is retried or released | Retry and success-release counts are structurally zero; authoritative readback alone may classify committed exactly once. |
| Cross-practice inference or privilege escalation occurs | Forced RLS, transaction-local scope, non-owner `NOBYPASSRLS`, closed grants and other-practice zero visibility. |
| Cleanup touches unrelated resources or leaves residue | One owner uses captured IDs after full reinspection; pass/failure requires role, attachments, containers, network and matching labels absent. |
| Rehearsal evidence is mistaken for API admission | Evidence is authored-synthetic, provider-free and disposable; no route/config/client/flag/allowlist or product record changes. |

## API Spine boundary

GraphQL remains read-only. The accepted REST-command shape retains explicit
actor and practice scope, authorisation, idempotency identity, exact request
digest, atomic effect/receipt/audit membership, default denial and fresh
authoritative readback. Events remain post-commit evidence hints, not command
authority. No API Spine artifact or product database schema changes.

## Residual limits and closed boundaries

One serial authored-synthetic rollback/unknown-response run does not prove WAL
crash behaviour, distributed partition, driver/pool behaviour, concurrency,
restart recovery, product-schema compatibility, ordinary-practice safety or
production availability.

No live/existing/cloud/product database, external network, model provider,
product/patient/appointment/clinical/historical/protected data,
ordinary-practice enablement, generic-status `Arrived`, feature-flag or
allowlist change, route, action grammar, first-party client, waiting-area
movement, REST/OpenAPI, GraphQL, async contract, production, deployment,
release, Pages, protected evidence access or protected-ref movement is
authorised.
