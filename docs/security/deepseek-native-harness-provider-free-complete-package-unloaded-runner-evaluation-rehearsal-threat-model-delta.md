# Threat-model delta — complete package-unloaded runner evaluation

Date: 2026-08-22

Operation: `deepseek-native-harness-provider-free-complete-package-unloaded-runner-evaluation-rehearsal`

This delta covers only one provider-free disposable Node evaluation of the
complete exact derived runner over the accepted closed guard graph.

| Threat | Control |
|---|---|
| Installed package or native Harness is loaded accidentally | Resolve every runner import to an exact local module, admitted Node built-in or exact local stub before launch; reject installed-package paths and keep native-Harness count zero. |
| A partial graph reproduces an opaque module failure | Prove the full runner-to-guard-to-bridge-to-sanitizer closure plus all four bare-package stubs before consuming the process. |
| A stub silently becomes a behavioral substitute for occupied evidence | Bind minimal stub source and claim only package-unloaded composition; native behavior and worker quality remain unproved. |
| The runner publishes a private agent or lifecycle event | The authored-synthetic factory never inserts into registries, propagates the exact commit veto and requires empty registries and zero events afterward. |
| Model selection is confused with a model request | The local stub records selection only; model/provider/broker/request counters remain zero. |
| Raw runtime or filesystem detail escapes | Persist only byte counts/hashes and closed coordinates before interpretation; forbid raw error, stack, cause, paths, prompts, responses, environment and credentials from evidence. |
| Output is interpreted before cleanup and envelope capture | Retain bytes without parsing, remove the disposable root, persist and validate the content-free envelope, then interpret both schema-owned outputs. |
| A retry hides a consumed terminal | Preflight requires fresh outputs and fixes process count at one; every result sets further-process authority false. |
| The proof expands into product or operational authority | Product, ordinary-practice, data, production, release, Pages and protected refs remain explicitly closed. |
| Clockwork work continues without increasing Harness capability | A pass advances directly to one bounded occupied useful Raisa worker; only a concrete observed failure can justify a recovery. |
| Unrelated user files are captured or changed | Use exact disposable paths and explicit-path staging; preserve `docs/branding/` and every unrelated untracked file. |

Residual risk: local stubs prove runner composition, not the installed native
Harness or DeepSeek worker. That residual is accepted only for this final
provider-free checkpoint and is addressed by the bounded occupied successor.
