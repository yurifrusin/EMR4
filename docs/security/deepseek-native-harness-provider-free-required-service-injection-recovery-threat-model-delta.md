# Threat-model delta: DeepSeek native Harness required-service injection recovery

Date: 2026-08-20

Timestamp: 2026-08-20T09:14:31.1452892+10:00 (Australia/Brisbane)

Status: `frozen`

Operation:
`deepseek-native-harness-provider-free-required-service-injection-recovery`

## Delta

The accepted native attempt proved that its patched runner activated before
`agentPresets` or `tools` was available. This tranche adds no runtime. It
examines exact cached rc.7 source in memory and freezes a dependency-gated
future declaration.

| Threat | Fail-closed control |
|---|---|
| Guessing which service was absent | Prove the effective base + headless row set: `tools` exists and `agent-presets` does not. Retain the aggregate native terminal unchanged. |
| Starting the runner before required services | Require the same exact ordered `hmr`, `agentPresets`, `tools` list in both module and loader-row declarations; bind Cordis inactive-until-complete semantics. |
| Adding an invalid roster row | Copy only the exact rc.7 official Web-bundle row shape: `@deepseek-ai/dsh-agent-presets` with `default: standard`; bind its service/config source. |
| Confusing service injection with preset availability | Record `emr4-bounded-worker` materialisation as unproved and keep every native boot closed. |
| Package or source substitution | Verify registry SHA-1, SHA-512 integrity, tar SHA-256 and exact member byte/digest bindings from the local cache before semantics. |
| Source leakage | Inspect tar members in memory; retain only exact identities, digests, safe names, booleans, counts and generated declarations. |
| Accidental Harness/provider execution | The controller has no subprocess or network path; acceptance requires zero Node/Harness/agent/broker/model/provider/network counts. |
| Corrupting immutable negative evidence | Bind both consumed attempts by exact source and artifact digests; never write their paths. |
| Expanding product authority | Reject changes outside the named orchestration docs, deterministic script/tests/evidence and normal governance artifacts. |
| Unreviewed semantic drift | Run deterministic gates first, then one fresh Gemini 3.7 Flash/high read-only veto on the exact clean candidate. |
| Governance drift | Use clockwork as sole canonical writer with separate `--check` and `--publish`; require zero drift before publication. |

## Residual risk and claim ceiling

Passing evidence proves the exact rc.7 dependency declaration and host-row
repair shape only. It does not prove that `emr4-bounded-worker` exists, that a
preset mounts, that a scope or effective-tool view can be constructed, that
Harness boots successfully, or that an occupied DeepSeek worker is reliable.
Those claims remain closed behind separately frozen deterministic and native
stages.

No product/data/runtime, ordinary-practice, deployment, Pages or protected-ref
authority changes. Local/origin `master` and `handoff/current` remain exact
`2e34bdad732fdab32fbf778280b3d3c70d66d602`; `docs/branding/` and every
unrelated untracked file remain preserved.
