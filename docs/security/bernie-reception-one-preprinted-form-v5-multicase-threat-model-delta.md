# Reception One Pre-printed Form v5 Multi-case Threat-model Delta

Status: closed — controls passed
Parent: Reception One Pre-printed Form v5

| Threat | Control |
|---|---|
| Prompt changes hide case-to-case weakness | Freeze and hash the instruction, schemas and request constructor before the first call; no mid-cohort repair. |
| Reusing the successful move case inflates results | Treat it only as a historical anchor; run exactly six new case identities. |
| A correction becomes implicit training | Permit only the unchanged closed proofreader ticket and one complete replacement per case. |
| Aggregate success hides a dangerous case | Preserve per-case ledgers, audit chains, dispositions, findings and residue evidence. |
| A failed case consumes another case's authority | Use distinct case-bound attempt and ledger identifiers with independent one-use consumption. |
| A model-selected action exceeds the request | Require compiler and semantic proofreader admission against the frozen authored-synthetic frame and deterministic oracle. |
| Ambiguity becomes a proposal | Require only the exact typed clarification output, with no API operation, confirmation claim or write. |
| Multiple calls expand cost unexpectedly | Six primaries, at most six eligible corrections, absolute USD 1 ceiling and no extra protocol-repair call. |
| Batch orchestration leaks credentials into cells | Preserve the v5 credential-free work cell; only the one-use host broker may use existing impersonated ADC. |
| One dirty cleanup contaminates later cases | Verify teardown after every case and stop before the next call if owned residue remains. |
| Typed results acquire product authority | Keep execution in memory, default-off and authored-synthetic; invoke no API route, database or confirmation path. |
| Results overclaim residency | Record only the configured and observed Sydney locational request path and Google's contractual posture, not physical or sovereign locality. |

Prompt examples, tuning, product data, historical Diary material, real patient
data, product delivery, appointment mutation, voice, production, deployment and
release remain outside this tranche.
