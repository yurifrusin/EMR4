# Threat-model delta — native Harness bounded occupied useful worker

Date: 2026-08-22

Timestamp: 2026-08-22T12:50:14.0608610+10:00 (Australia/Brisbane)

Operation: `raisa-native-harness-bounded-occupied-useful-worker-rehearsal`

| Threat | Control |
|---|---|
| The worker turns a runbook into activation authority | Freeze the unchanged default-off header outside its editable member; schema and semantic validator require every activation/flag/allowlist/route/effect field false and no active stage. |
| Free-form model text escapes a closed form | Permit one literal edit into one JSON member, then parse with duplicate-key rejection, validate against a closed JSON Schema and reject every unknown key/value. |
| The worker reaches product or unrelated files | Use one disposable Git workspace containing one baseline file; enforce the absolute target in pre/post tool hooks, one tool call, workspace sandbox and exact changed-path inventory. |
| A read/glob/shell/subagent or second edit expands authority | The preset exposes only `edit`, `glob`, `read`; the runner admits only one direct literal `edit`; shell, PowerShell, web, jobs, skills, goals, subagents and workflow plugins remain disabled. |
| A second request or retry hides failure | The broker and runner cap the process at one request; retry/resume/fallback are zero and the lease is consumed before launch. |
| Published agent/session state survives | Publication is process-local, the process exits after terminalization, every owned process is terminated and the exact disposable root is removed before candidate interpretation. |
| Raw provider or credential material is retained | Keep only stream byte counts/hashes and typed broker/runner coordinates; delete raw files, session/environment material and credential-bearing process state. |
| Malicious candidate JSON escapes cleanup | Read bytes only from the exact target before cleanup, cap size, reject invalid UTF-8/duplicate keys/schema drift, canonicalize and persist only the admitted non-PHI object plus digest outside the disposable root. |
| A valid-looking candidate is self-accepted | Treat it as untrusted until independent GPT Sol readback, exact diff review and deterministic validation; the worker has no acceptance or Git authority. |
| The new runner silently weakens accepted composition controls | Derive it from exact accepted guard/bridge/sanitizer source identities and test the minimal publication/followup delta, tool boundary, terminal vocabulary and no-raw-error surface before launch. |
| The occupied result is overstated as Harness reliability | Claim only one bounded turn and one runbook candidate; retain terminal origin and distinguish Harness transport, DeepSeek output and Sol adoption. |
| Git IDs or refs are recalled manually | All operational Git bindings and receipts use machine resolution and full 40-character objects; protected refs are checked before and after launch. |
| Unrelated user files are captured | Prepare only the exact disposable root, retain only named evidence paths, stage explicit repository paths and preserve every unrelated untracked file including `docs/branding/`. |

Residual risk: one successful constrained edit does not establish general native
Harness reliability or unconstrained DeepSeek coding quality. It establishes
only that the accepted control chain can govern one useful typed artifact with
attributable terminal evidence and complete cleanup.
