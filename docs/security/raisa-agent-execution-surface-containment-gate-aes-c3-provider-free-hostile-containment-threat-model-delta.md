# Threat-model delta: Raisa AES-C3 provider-free hostile containment rehearsal

Date: 2026-08-11

Status: frozen authored-synthetic, provider-free, pure containment boundary

## Security delta

AES-C3 challenges whether hostile-looking content or stale authority can turn
the accepted C1/C2 pure path into a dereference, interpreter, transport,
credential, operation selector, egress bypass or post-revocation call. The
security objective is fail-closed structural rejection and inert digest-only
handling enforced outside candidate content.

There is no real model, broker process, container, adapter, credential,
provider, network, source, filesystem capability, executable tool or command.
The rehearsal cannot establish operating-system isolation or universal
semantic attack detection.

## Assets and data

- exact accepted AES-C0/C1/C2 artifact and implementation identities;
- one fixed pure inert adapter and its exact broker-owned identity;
- closed generation, manifest, authority, context and supply-chain bindings;
- independent cumulative denial, repeated-failure and egress budget state;
- inert authored-synthetic replay fixtures explicitly marked noncredentials;
- short authored-synthetic hostile payloads and their digests; and
- minimized status/reason/count/digest evidence only.

No patient, clinical, product-derived, licensed, credential or protected value
is an asset or permitted input.

## Trust boundaries

1. immutable accepted C0/C1/C2 artifacts to the C3 harness;
2. untrusted candidate fields and opaque values to closed C1/C2 validation;
3. broker-owned generation/authority/context/supply state to the inherited pure
   admission and dispatch functions;
4. one broker-created invocation to one statically bound pure function;
5. untrusted adapter-result fixtures to exact result validation;
6. cumulative state and replay fixtures to the external terminal latch; and
7. minimized C3 evidence to repository-local acceptance.

## Attacker-controlled inputs

- every allowed candidate string value;
- forbidden/undeclared candidate or work-cell fields;
- clear, encoded, compressed, chunked and exception-labelled result content;
- stale generation, manifest, Bureau, work-cell, lease, alias or token fixture
  identities;
- mismatched candidate/proofreader context bindings;
- changed manifest, adapter, runtime-image or model/provider identities; and
- malformed contract/scenario/evidence objects.

No attacker-controlled value may select a mutation function, Python attribute,
module, destination, operation, executable, decoder or cleanup target.

## Abuse cases and required controls

| Abuse case | Control | Required evidence |
|---|---|---|
| Reference an environment value, file, source tree, Docker socket, HDF5 external store or metadata service | allowed content is an opaque bounded string; invocation receives only its digest; no dereference/import path exists | raw fixture absent outside the scenario/candidate copy; one fixed pure call only |
| Trigger template or object deserialization | no template/deserializer/encoding/archive imports; structural fields reject | opaque case stays inert and structured case calls nothing |
| Enumerate credentials or inject custody fields | no real credential exists; candidate/work-cell custody fields are closed | structural rejection, zero release and no credential-like value in evidence |
| Create arbitrary URL, redirect, DNS, registry, webhook, paste or CORS-proxy egress | destination/method come only from the exact one-entry C2 registry; content is never parsed as a locator | all relay references stay opaque or reject; zero network operations |
| Exfiltrate through clear, encoded, compressed, chunked or exception result | exact result schema, digest-only release and carrier-independent wire-byte charge | invalid results release nothing; all five overflow at 12,289/12,288 |
| Probe repeatedly until a boundary is found | independent cumulative denial/boundary/failure ceilings plus external terminal latch | exact sequences reach ceiling and next attempt makes no call |
| Reuse authority after supersession, restart or another Bureau | immutable generation/Bureau binding and fresh pre-dispatch current-state check | stale lease/alias/token fixtures and cross-Bureau replay stop |
| Revoke after a prior allow | dispatch-time revocation/kill recheck outranks admission | no pure call after revocation or kill |
| Change ContextFrameSet between model, proofreader and broker | exact digest equality required before inherited simulation | candidate and proofreader mismatch scenarios stop |
| Swap manifest, adapter, runtime image or model/provider contract | exact inherited supply-chain digests checked before admission/dispatch | each identity mismatch stops with zero call |
| Emit GraphQL/REST/SQL/provider-tool/product-adapter shaped content | closed candidate/work-cell schema and broker-owned operation identity | all command-shaped cases reject before call; command count remains zero |
| Turn the scenario document into an instruction language | closed scenario schema plus static mutation-ID map | unknown mutation/path/callable attempts reject |
| Leak raw attack material through evidence or exceptions | minimized closed evidence; recursive payload absence check; exceptions fail tests | only IDs, reasons, counts and digests persist |

## Verification evidence

- exact inherited artifact hashes and full C2/C1/C0 regressions;
- exact closed C3 contract/schema and 61-scenario catalogue;
- 21 contained, 15 rejected and 25 stopped outcomes;
- exact 28 pure-call and 21 digest-only-release totals;
- recursive payload non-occurrence outside approved scenario/candidate fixtures;
- five identical carrier budget charges and overflow decisions;
- cumulative denial/boundary/failure state-transition assertions;
- stale replay, context and supply-chain binding assertions;
- generated hostile attempt and contract mutations;
- AST/static forbidden-import/call and exact-callable checks;
- focused API Spine compatibility tests;
- DeepSeek blue implementation/test evidence; and
- fresh exact-head Gemini red/veto after deterministic gates.

## Residual risk owner

GPT Sol owns acceptance of this pure rehearsal. Later separately authorised
gates own real process/container isolation, TOCTOU concurrency, real
credential/broker/adapter defects, provider behavior, product data, operations,
deployment and production. No C3 result may be represented as universal prompt-
injection prevention or real-runtime escape resistance.

## Stop boundary

Any raw payload outside its approved scenario/candidate fixture, decoder or
interpreter path, candidate-selected operation, external effect, budget bypass,
post-terminal call, stale-authority acceptance, context/supply mismatch
acceptance, command invocation, open evidence or unresolved critical/high
finding returns `revision_required`. This delta grants no real runtime,
credential, provider, data, network, metadata, database/source, filesystem,
executable tool, command, deployment, release, Pages or protected-ref authority.
