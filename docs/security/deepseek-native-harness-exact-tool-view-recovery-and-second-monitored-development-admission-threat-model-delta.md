# DeepSeek native Harness exact-tool-view recovery and second monitored development admission threat-model delta

Date: 2026-08-18

Timestamp: 2026-08-18T18:11:35.4041914+10:00 (Australia/Brisbane)

Status: frozen

Reasoning level: high

## Scope

This delta covers exact pinned rc.7 scoped-tool discovery, normalized contract
binding, one provider-free composed-request capture and, only after that gate,
one fresh second monitored DeepSeek worker session over two provider-free
harness-validator paths. It changes no EMR4 application or clinical boundary.

## Assets

- exact task-branch source and accepted predecessor negative evidence;
- pinned package identity, scoped-tool source mapping and normalized digest;
- sanitized provider-free request-header proof;
- exact sparse worker packet and two owned output paths;
- provider credential held only by the isolated occupied broker;
- raw Harness/package/session material held only in disposable roots;
- protected refs and preserved user-owned untracked files.

## Threats and controls

### Cosmetic scoping or hidden schemas

Threat: configuration appears to select three tools while the model request
still contains surplus schemas.

Controls: accept only an rc.7 package-native scope mechanism verified in exact
source; capture the actual provider-shaped request before external forwarding;
assert exact sorted names `edit`, `glob`, `read`; reject duplicates, omissions
or additions; keep the outer broker's identical allowlist unchanged.

### Package patching or identity drift

Threat: a local patch or dependency/version drift makes the proof
non-representative.

Controls: verify accepted registry name/version/shasum/integrity; use a fresh
disposable package root; prohibit global install and package-source edits;
record exact selected packages/source anchors; fail closed on mismatch.

### Accidental provider call during proof

Threat: the structural probe transmits its composed request externally.

Controls: worker and capture endpoint share an isolated internal Docker
network with no egress member; no provider credential is present; capture
terminates locally; prove zero external/provider calls and exact cleanup.

### Sensitive trace retention

Threat: request messages, prompt, reasoning, response, tool schemas,
credentials or environment data enter the repository.

Controls: capture only structural allowlisted metadata and hashes/counts; raw
package/session material remains in disposable roots; scan retained evidence
for secret-shaped values; remove and verify every disposable resource.

### Scope bypass in occupied execution

Threat: the worker gains more authority than provider-free proof established.

Controls: use the same committed normalized profile, package-native scope and
three-tool broker contract; exact sparse bind mount; worker has no provider
credential or direct egress; broker has no worker mount; reject any effective
configuration, mount, network, route or request-header difference.

### Candidate workspace overreach

Threat: the model reads or changes unrelated repository or user-owned files.

Controls: a fresh sparse non-protected worktree contains exact packet inputs
and two owned output paths; only `read`, `glob`, `edit`; exact changed-path and
Git readback; reject any unexpected path. Main-worktree `docs/branding/` never
enters the worker mount.

### Runaway, retry or stale-session confusion

Threat: the trial loops, retries, falls back or resumes consumed authority.

Controls: one fresh occupied session, zero automatic retry/fallback, one
parallel tool call, 15-minute wall clock, session/source/profile bindings and
no resume after terminal. Yuri's prepaid balance remains the monetary ceiling.

### Candidate self-certification

Threat: worker claims substitute for independent evidence.

Controls: Sol runs tests outside the worker, reads the complete diff and trace,
and alone admits/rejects/recovers. DeepSeek receives no Git publication,
acceptance, recovery, baton or protected-ref authority.

## Deliberately closed

No product, patient, clinical, historical-Diary, real-person or protected data;
no application source/API/database/route/client/status/grammar/waiting-area or
ordinary-practice change; no live product runtime, provider-executed product
tool, durable user Harness profile, global install, credential/IAM mutation,
deployment, production, release, Pages or protected ref.
