# Threat-model delta: check-in relay-free recovery attempt 005

Date: 2026-08-20

Timestamp: 2026-08-20T13:21:08.5249660+10:00 (Australia/Brisbane)

Status: `frozen`

This delta authorises deterministic construction, one bounded native-Harness
DeepSeek adapter/test worker and, only after separate exact preexecution
admission, one provider-free disposable PostgreSQL attempt. It grants no
product or ordinary-practice authority.

## Assets and trust boundaries

- Immutable attempts 001-004 evidence, envelopes, cleanup and execution counts.
- Full 40-character plan, repair, transport, runtime-role, no-database and
  native-Harness bindings.
- One WorkOrder v2, inner/outer exact tool views, journal and consumed worker.
- One fixed attempt-005 namespace and one terminal envelope.
- One internal Docker network, server, retained attachment and fixed sidecars.
- In-memory ephemeral credentials delivered only after exact inspection.
- Admin-owned forced-RLS probe relations and a non-owner `NOBYPASSRLS` role.
- Exact terminal OCI state, restricted authoritative readback and sanitized
  evidence.

Trust crosses the native Harness, local broker and exact edit/glob/read surfaces
for the worker. The occupied rehearsal separately crosses the host Docker API,
post-inspection attached stdin, captured internal network, PostgreSQL
authentication/RLS and closed validators. Model prose, attachment output,
Docker logs and process timing are not acceptance evidence.

## Threats and controls

| Threat | Fail-closed control |
|---|---|
| Attempt 005 overwrites or launders a predecessor | A distinct hard-coded namespace is collision-checked; attempts 001-004 are hash-bound and immutable. |
| An abbreviated or guessed Git source enters evidence | Every source is a full 40-character commit resolved and ancestry-checked by machine. |
| The repaired lifecycle drifts | Static admission binds the repaired harness digest and exact repair sources; focused tests preserve attachment lifetime and separated diagnostics. |
| A path argument redirects evidence | The adapter exposes only `--check` and `--execute`; terminal paths are code-owned and collision-denied. |
| More than one worker request occurs | A distinct worker one-run checkpoint, immutable terminal namespace, zero retry/resume and no fallback consume the first launch. |
| The worker escapes its package | WorkOrder v2, sparse worktree, exact owned paths, inner and outer edit/glob/read checks, forbidden-surface digest and broker journal all deny surplus authority. |
| The model executes tests or database work | The worker has no shell/test/Docker/database tools; no-database admission binds exact commands for Sol to run outside the session. |
| Model output is mistaken for acceptance | Sol reads every changed byte, validates path scope and runs all admitted deterministic gates. |
| More than one occupied database execution occurs | A separate database one-run checkpoint admits one invocation; the terminal namespace blocks reuse; no retry path exists. |
| Early attachment release destabilizes the server | The accepted attachment remains under the sole controller until final teardown; liveness and failure-branch tests remain exact. |
| Runtime identity drift remains opaque | Stopped-server and identity-mismatch coordinates remain distinct; only sorted sanitized failed-predicate names may be retained. |
| Host relay or hidden result channel returns | Published ports, listeners, relays, Docker exec, multiprocessing and queues remain forbidden; OCI state plus observer proof is the caller classifier. |
| Credentials or raw runtime values leak | Credentials are delivered after exact inspection; recursive redaction rejects secrets, nonce values outside the ownership label, raw output, logs, SQL, paths and object IDs. |
| Rollback leaves partial state | Fresh same-practice restricted readback must observe zero receipt, effect and audit members. |
| Incomplete response is retried or released | Retry and success-release counts are structurally zero; restricted authoritative readback alone may classify committed exactly once. |
| Cross-practice inference or privilege escalation occurs | Forced RLS, transaction-local scope, non-owner `NOBYPASSRLS`, closed grants and other-practice zero visibility. |
| Cleanup touches unrelated resources or leaves residue | One owner acts on captured IDs only after full reinspection; pass/failure requires role, attachment, containers, network and matching labels absent. |
| Rehearsal evidence is mistaken for API admission | Evidence is authored-synthetic, provider-free and disposable; no route/config/client/flag/allowlist or product record changes. |

## API Spine boundary

GraphQL remains read-only. The accepted REST-command shape retains explicit
actor and practice scope, authorization, idempotency identity, exact request
digest, atomic effect/receipt/audit membership, default denial and authoritative
readback. Events remain post-commit evidence hints, not command authority. No
API Spine artifact or database schema changes.

## Residual limits

One worker and one serial authored-synthetic rollback/unknown-response run do
not prove general DeepSeek reliability, WAL crash behavior, distributed
partition, driver/pool behavior, concurrency, restart recovery, product-schema
compatibility, ordinary-practice safety or production availability.

## Closed boundaries

No live/existing/cloud/product database, external network beyond the exact
model-provider path, product/patient/appointment/clinical/historical/protected
data, ordinary-practice enablement, generic-status `Arrived`, feature-flag or
allowlist change, route, action grammar, first-party client, waiting-area
movement, REST/OpenAPI, GraphQL, async contract, production, deployment,
release, Pages, protected evidence access or protected-ref movement is
authorised.
