# Threat-model delta — native Harness bounded-worker attempt 003

Date: 2026-08-21

Timestamp: 2026-08-21T07:09:40.7240740+10:00 (Australia/Brisbane)

Status: `frozen`

Operation:
`raisa-authored-synthetic-check-in-native-harness-bounded-worker-attempt-003`

## Changed surface

This tranche admits one official DeepSeek request and one native filesystem
edit inside a newly materialized one-file synthetic root. It reuses the
accepted loopback broker, rc.7 preset/tool projection, one-result turn
conclusion and recovered sanitized pre-HMR terminal. It opens no product,
database, real-data, deployment or production surface.

| Risk | Fail-closed control |
|---|---|
| A consumed attempt is silently retried | Use new attempt-003 identities and paths. Process creation writes a new exclusive consumed record; attempts 001/002 remain immutable and are never selected. |
| The user-controlled prepaid balance is treated as an unlimited budget | Admit provider ordinal one only, cap output/bytes/time, forbid top-up or balance mutation, and state honestly that the broker enforces no currency cap. |
| Product or protected material reaches the provider | Create a new generic one-file repository from controller constants; bind prompt/workspace digests and scan the closed vocabulary before launch. |
| The worker receives the provider credential | Put the key only in the broker environment. The worker receives a one-session loopback capability and the broker receives no workspace mount. |
| Preset/tool drift expands authority | Verify exact rc.7 package, preset digest and sorted `edit`, `glob`, `read` projection immediately before execution; parallel width remains one. |
| A second request or summary crosses the provider boundary | Broker rejects ordinal two before provider I/O; successful official edit sets rc.7 `concludesTurn`; any second-request attempt makes the terminal negative. |
| Model changes the wrong surface | Permit only the existing synthetic path; exact Git inventory and expected-byte digest reject create/delete/rename/no-op/surplus changes. |
| Outer startup failure is again untraceable | Before raw root removal, derive and exclusively validate the accepted bounded pre-HMR stage/cause sidecar; ambiguity, over-limit and unclassified results make no stronger claim. |
| Raw prompt, response, reasoning, key or startup stream becomes durable | Retain only digests, counts, safe enums and terminal coordinates. Remove raw session/stream material with the exact disposable root after safe terminal validation. |
| Cleanup targets unrelated user files | Resolve one literal descendant of `C:/Users/sarashera/EMR4-worktrees/`, reject symlink/escape/parent targets and preserve all unrelated untracked files including `docs/branding/`. |
| A shaped success is overstated | Claim only the exact one-request one-edit synthetic result; general worker quality, multi-turn reliability and EMR4/production readiness remain unproved. |

## Protected boundaries

No retry, resume, fallback, second worker, product/database/real-data access,
ordinary-practice enablement, generic-status `Arrived`, route/adapter/flag/
allowlist/grammar/client/waiting-area change, production, deployment, release,
Pages, protected evidence or protected-ref action is authorised.
