# DeepSeek native Harness provider-free complete-composition native-boot recovery plan

Date: 2026-08-20

Timestamp: 2026-08-20T11:24:06.3744524+10:00 (Australia/Brisbane)

Status: `frozen`

Planning source HEAD:
`d64a816aa422b45706b7cc154f989a6f83bc6468`

Accepted required-service recovery source:
`ae608b4a8cc4004693813a83e518c71eb2ff06f3`

Accepted bounded-preset recovery source:
`4ffc8482934bed754e149acea005d7f8f444a782`

Accepted effective-tool guard source:
`dc167c20f5b54b783a57fccb7843f434136c8ca8`

Accepted preterminal-observability source:
`b5f0bc0d823a1c8009f3bb49efcc9a588b9703ab`

Immutable failed complete-composition predecessor source:
`8155a941a28b3d22a7dbb4132c4b0ada8558482e`

Operation:
`deepseek-native-harness-provider-free-complete-composition-native-boot-recovery`

New immutable attempt:
`complete-composition-native-boot-recovery-attempt-001`

Reasoning level: Extra High freezes the native-process, service-activation,
preset, guard, timing, terminal, network-denial and cleanup envelope. High is
sufficient for bounded implementation, deterministic verification, the one
execution, independent veto and clockwork closeout while this plan is
unchanged.

## Objective and exact claim

Perform exactly one offline, network-denied
`@deepseek-ai/dsh@0.1.0-rc.7` native Harness boot that composes four accepted
pieces without broadening any of them:

1. the stock-headless HMR readiness lifecycle and Node
   `--expose-internals` prerequisite;
2. the official `agent-presets` host row and exact ordered runner injection
   `hmr`, `agentPresets`, `tools`;
3. the exact 158-byte `emr4-bounded-worker` preset payload; and
4. the accepted post-mount effective-tool guard and preterminal activation
   vocabulary.

Success requires the native process to prove stock HMR readiness, activate the
custom runner only through Cordis's three-service dependency gate, discover
and mount the exact custom preset, expose exactly sorted `edit`, `glob`,
`read`, emit one sanitized success terminal, request exit zero, terminate
within the bound and leave no process or disposable root.

This is a pre-provider composition proof. It creates no agent, session or turn
and makes no broker, DeepSeek model or provider request. It is not a retry,
resume or reclassification of either consumed native failure.

## Immutable inputs and exact executable boundary

Before any process start the controller must bind by full 40-character Git
object id and exact file digest:

- the accepted required-service controller, contract and evidence, including
  its exact future patch fragment and runner whose only source change is
  `export const inject = ["hmr", "agentPresets", "tools"]`;
- the accepted bounded-preset controller, contract, evidence and exact
  repository payload at
  `materialised-home/.agent-presets/emr4-bounded-worker/agent.cordis.yml`;
- the accepted effective-tool guard controller, contract, evidence and
  generated ESM helper;
- the accepted preterminal observability controller, contract, evidence and
  corrected activation/terminal runner lineage;
- the accepted stock-headless HMR controller and contract; and
- both consumed native attempts, including their failed terminals and exact
  source bindings.

The only executable external boundary is one package-declared local Node/native
Harness process. Package materialisation uses the existing exact npm cache
only, with offline mode, lifecycle scripts, audit, funding and online fallback
disabled. No deterministic check or unit test may execute Node, `lib/bin.js`,
`--profile headless` or an equivalent Harness entry.

## Frozen disposable composition

The controller creates one collision-free direct child of the accepted
disposable parent outside the repository. It writes only into that root until
bounded sanitized evidence is committed after cleanup.

The disposable home must contain the accepted preset at exactly:

`.agent-presets/emr4-bounded-worker/agent.cordis.yml`

The bytes must be copied from the accepted repository payload and match
SHA-256
`3de182eb702e6f2b397941c73393b87f65acb9b401565f966059d2bd46f649d1`.
No generated approximation or alternative preset row is admitted.

The initial `profiles/headless/cordis.patch.yml` must:

- disable `headless-runner`, `code-runtime` and `session-telemetry-otel`;
- insert only the accepted HMR readiness sentinel; and
- contain neither `agent-presets` nor the complete-composition runner.

Only after the sentinel exclusively records exact readiness
`sentinel_activated`, `stock_headless_hmr_ready` and both watched stock patch
paths are present may the controller atomically replace that file. The changed
patch preserves every initial row and adds exactly, in order:

```yaml
- id: agent-presets
  name: '@deepseek-ai/dsh-agent-presets'
  config:
    default: standard
- id: provider-free-complete-composition-runner
  name: ../../../installation/proof/runner.mjs
  inject: [hmr, agentPresets, tools]
```

The runner module is the accepted corrected preterminal runner with only the
accepted injection declaration replacement. The patch row and module export
must declare the same ordered three-service list. Successful entry into
`BOOTSTRAP_APPLY_ENTERED` is runtime evidence that Cordis held runner
activation until all three injected providers were active; there is no
controller-authored service-readiness shortcut.

## Single process and terminal contract

The sole admitted execution has attempt id
`complete-composition-native-boot-recovery-attempt-001` and exact shape:

- one resolved Node executable;
- exact `--expose-internals`;
- the package-declared `lib/bin.js`;
- exact `--profile headless` arguments and one fixed non-sensitive label;
- a scrubbed credential and proxy environment;
- the accepted fail-closed network guard preloaded with `NODE_OPTIONS`;
- no task prompt, work order, broker, provider credential or product data; and
- one bounded timeout with no retry path.

The runner exclusively owns the activation ledger and terminal. The exact
success activation sequence remains:

1. `BOOTSTRAP_APPLY_ENTERED`;
2. `RUNTIME_MODULES_IMPORTED`;
3. `SCOPE_CREATED`;
4. `GUARD_ENTRY_REACHED`;
5. `GUARD_TERMINAL_REACHED`;
6. `SCOPE_DISPOSED`; and
7. `EXIT_REQUESTED`.

The only accepted terminal has schema
`ariadne.deepseek_native_harness_effective_tool_native_boot_terminal.v1`,
stage `preterminal_activation`, code
`EFFECTIVE_TOOL_COMPOSITION_PASSED`, `detail: null`, exact sorted tool names
`edit`, `glob`, `read` and count `3`. The retained service projection is
exactly `hmr`, `agentPresets`, `tools`, bound to identical loader-row and
module declarations plus successful dependency-gated runner activation.

Any non-passing terminal, incomplete/disordered ledger, missing service,
preset mount failure, tool-view mismatch, network record, nonzero or ambiguous
exit, timeout or cleanup failure is an immutable failed result. Unknown text,
exception messages, stack traces, prompts, payloads, paths, environment values
and credentials are never retained.

## One-run latch, monitoring and cleanup

Construction defects discovered before process start may be corrected through
deterministic tests. The first `Popen` consumes the new attempt id. There is no
automatic or manual second process, same-attempt retry, resume,
reclassification or reuse of its output namespace.

The process result is monitored through a dedicated execution call whose full
result envelope retains any yielded session identifier and polls it to a final
exit. Elapsed time is assigned in `finally` for every started process before
termination escalation and cleanup. Cleanup waits for exact process absence,
removes only the verified disposable root and proves it absent. Raw
stdout/stderr bodies are destroyed after their digests and byte counts are
captured.

## Deterministic admission

Before the one native start, deterministic checks and tests must prove:

1. all planning and predecessor object ids are full 40-character commits and
   ancestors of the task HEAD;
2. all accepted scripts, contracts, evidence, payload and immutable-attempt
   bytes match frozen digests;
3. the exact cached rc.7 package and every required installed package/member
   match registry identity and accepted source predicates;
4. the preset bytes and install-relative path are exact;
5. initial and changed patches have exact rows, ordering and ownership, with
   one atomic mutation only after real sentinel readiness;
6. loader-row and runner injection are both exactly ordered `hmr`,
   `agentPresets`, `tools`, and the runner differs from the accepted corrected
   source only by that injection declaration;
7. one launch site, one preset mount, one pre-restriction view, one
   restriction, one final schema view, one terminal writer, one scope disposal
   and one exit request;
8. hostile variants reject absent/renamed/reordered/duplicated/surplus service
   rows, wrong preset bytes/path, early patch mutation, controller-authored
   readiness, missing or own-layer tools, schema drift, unsafe terminal data,
   duplicate process, network activity, ambiguous exit and incomplete cleanup;
9. duration assignment precedes termination and root cleanup in `finally`;
10. agent/session/turn, broker/model/provider, occupied-worker, registry/network,
    Docker and database paths remain zero; and
11. focused and neighbouring provider-free Harness tests, Ruff, Python
    compilation, JSON/schema validation and Git whitespace pass.

Immediately before execution, a fresh preexecution Ariadne receipt must repeat
the five sources, exact attempt id, zero prior output, exact executable/package
identity, empty worker slots, no fallback and the three-lane assessment. Any
failure forbids process start.

## Acceptance and independent veto

Acceptance requires the sole process to pass with exact readiness and
activation sequences, identical three-service injection declarations, exact
three-tool terminal, exit zero, reliable non-negative duration, zero prohibited
request/action counts and complete cleanup.

Only after that immutable terminal and its deterministic tests are committed
may one fresh Gemini 3.7 Flash/high isolated read-only veto inspect the exact
candidate. Gemini receives no implementation, execution, repair, acceptance,
cleanup or Git authority. A P0-P2 finding permits bounded source/evidence repair
but never a second native process; terminal-bound bytes must remain exact.

## Explicit parallelism assessment

- **DeepSeek:** `declined`, negative leverage. The native Harness is the
  provider-free evidence subject; an agent/model/provider call or Claude Code
  fallback violates the claim.
- **Gemini:** `reserved`, required independent leverage. It owns one fresh
  exact-candidate and exact-terminal read-only veto after deterministic
  admission and the sole native result.
- **Native subagents:** `declined`, negative leverage. Current developer policy
  prohibits proactive delegation, and disposable home, readiness, mutation,
  terminal, process absence and cleanup are one serial ownership chain.

Sol owns plan, implementation, deterministic admission, the one process,
monitoring, cleanup, recovery, acceptance, clockwork and Git. Reassess all
three lanes at plan freeze, preexecution, terminal capture, pre-verifier and
closeout.

## Claim ceiling, protected boundaries and closeout

A pass proves only one pinned local rc.7 provider-free pre-provider composition
path. It does not prove DeepSeek reasoning, coding quality, occupied-worker
completion, model/provider reliability, future Harness versions, production
isolation or product suitability.

No second native process; no agent/session/turn, WorkOrder, broker, DeepSeek
model/provider call, fallback, credential, registry/external network, Docker,
PostgreSQL, SQL or database execution; no product source/configuration/API/
OpenAPI/GraphQL/schema/client/waiting-area change; no ordinary-practice
enablement, feature flag, allowlist, command mount, generic-status `Arrived`,
action grammar or first-party client change; no product, patient, appointment,
clinical, historical or protected data; and no production, deployment,
release, Pages, protected evidence or protected-ref movement is authorised.

Local/origin `master` and `handoff/current` remain exactly
`2e34bdad732fdab32fbf778280b3d3c70d66d602`. Preserve `docs/branding/` and
every unrelated untracked file. Stage explicit paths only; `git add .` and
`git add -A` are forbidden.

At closeout the clockwork is the sole canonical governance writer and must run
a separate `--check` before `--publish`, followed by a dedicated full-envelope
postpublication suite and an idempotent check. Sol writes the paired Yuri
summary, sends the usual non-PHI Pushover and continues under standing
authority. A later occupied native DeepSeek worker, if eligible, requires its
own freshly frozen work order and grants no authority here.
