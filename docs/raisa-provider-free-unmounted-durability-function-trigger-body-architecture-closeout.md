# Provider-free unmounted durability function-and-trigger-body architecture closeout

Date: 2026-08-08

Result: `raisa_provider_free_unmounted_durability_function_trigger_body_architecture_pass`

Accepted source HEAD: `a93d07405ad35d7d6c0603065625c17ec14ab23e`

Canonical contract:
`sha256:f71287f266a3252d2a0736e511287600939a40bc70397710600c12581e24d4f3`

## Accepted result

The provider-free, unmounted body architecture now freezes the exact
machine-readable programs for all nine security-definer entry points and all
thirteen trigger functions deliberately omitted by the accepted durability
migration-and-transaction architecture. The closed typed instruction and
expression trees bind exact relations, columns, symbols, branches, locks,
effects, failures, returns, call edges and renderer order without containing or
executing SQL.

The final fourth-veto recovery adds one exact `F_ANCHOR` fence to the new key-
rotation path. Immediately after the current recovery anchor is locked, and
before the prior key, digest derivation or any effect, it compares four
checkpoint fields and seven controlling generation digests field by field.
Identical-key replay remains earlier, inert and independent of that new-effect
fence. The earlier R6 source-independent replay, complete recovery-anchor,
duplicate-free set-pair and contiguous lock-order repairs remain binding.

## Evidence

- Sol's complete inherited-plus-R7 packet passed 339/339 tests, including 34
  focused hostile anchor-fence attacks.
- The exact builder self-check returned the canonical contract digest; Ruff and
  `git diff --check` passed.
- The first Antigravity verifier process returned no verdict after a transport
  exit. It remained immutable negative transport evidence and did not affect
  the candidate.
- One distinct fresh `gemini-3.6-flash-high`/high focused exact-HEAD veto then
  returned one schema-constrained `pass`, ran 44/44 focused tests and reported
  no P0-P3 finding.
- The reviewer independently confirmed the exact eleven equality pairs, fence
  ordering, inert replay path, bounded executable delta and survival of R6A-R6D.
- Review postflight remained clean at the exact accepted source HEAD; local and
  origin protected refs remained
  `2e34bdad732fdab32fbf778280b3d3c70d66d602`.
- AER-0081 is corrected by the fresh focused attempt in register revision 80;
  the original no-decision failure remains preserved.
- The continuity-bound packet exposed two pre-existing repository baselines:
  accepted nodes emitted optional artifact pointers omitted by the graph schema,
  and the general Compass test hard-coded a superseded current node. AER-0082
  and AER-0083 preserve and correct both in revision 81 without changing the
  accepted body candidate.

## Closed boundaries

No SQL or DDL was rendered or executed. No migration, database object,
database/source/outbox/feed/watcher/listener contact, operational persistence,
product or patient data, provider product path, command/write authority,
application/API/Diary change, runtime wiring, credential change, deployment,
production, release, Pages rebuild or protected-ref movement was performed or
authorised.

## Next safe descendant

The next safe descendant is a provider-free unmounted inert DDL rehearsal. It
may mechanically lower only the accepted parent structure/signatures and this
exact child body contract into repository-local SQL artifacts, with static
PostgreSQL-16 grammar, ordering, catalogue and privilege checks. It may not
execute or apply the SQL, create a migration or database object, contact a
database or source, use product/patient data, add runtime or command authority,
deploy or move protected refs.
