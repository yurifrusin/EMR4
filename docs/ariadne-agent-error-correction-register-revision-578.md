# Ariadne agent error and correction register — revision 578

Date: 2026-08-21

Timestamp: 2026-08-21T07:35:11.0925861+10:00 (Australia/Brisbane)

Status: `prospective_clockwork_reading`

This revision adds five contained observations from the native-Harness
attempt-003 tranche to revision 577's 725 incidents. The exclusive clockwork
derives the canonical register; this human-readable reading must match 730
total incidents, latest `AER-0730`, and zero open incidents.

- `AER-0726`: the preplanning receipt rejected the recalled `deferred` lane
  disposition; the schema-owned `reserved` coordinate passed before planning.
- `AER-0727`: the first candidate precommit receipt omitted required adapter
  observations; the complete declared adapter inventory passed before commit.
- `AER-0728`: an over-broad descendant test selection ran two immutable
  historical-report equality checks that correctly rejected controller-source
  drift; the frozen plan now excludes regeneration and uses focused descendant
  ordering tests.
- `AER-0729`: the first occupied prelaunch runtime state treated the synthetic
  workspace as an assigned Ariadne handoff worker; preflight rejected the
  incompatible receipt shape and the corrected native-process lane remained
  planned until controller dispatch.
- `AER-0730`: exactly one native Harness attempt exited before the first HMR
  event with zero provider requests. The recovered sidecar safely recorded
  `unclassified_nonzero_exit`; the attempt is consumed, cleaned up and routed
  to provider-disabled source-static diagnosis without retry.

All five observations are corrected or contained. None confer comparative
model scoring, occupied retry, product, data, database, production,
deployment, Pages or protected-ref authority.
