# Ariadne and Agent Runtime Ledger

The live authority allocation in root `AGENTS.md` overrides all historical
allocation language.

## Current allocation

- GPT Sol: Conductor, planner, architecture and acceptance owner, recovery
  owner, and protected integrator.
- DeepSeek V4 Flash/high through Claude Code `--bare`: economical bounded
  implementation and test worker.
- Gemini 3.5 Flash through a fresh Antigravity project: economical peer worker
  and independent veto reviewer.
- DeepSeek Pro: not a routine or fallback Conductor and not authorized for
  planning, sprint allocation, or acceptance revision without a new explicit
  user instruction.
- Claude/Fable/Opus and native Codex workers: availability- and leverage-gated
  options, never automatic integration authorities.

Preferred launchers are `scripts/ariadne_deepseek_claude.py` and
`scripts/ariadne_antigravity.py`. Deep Code is a real-TTY fallback only.
Durable packet artifacts, not terminal prose, carry worker authority.

## 2026-07-30 Reception One shared typed language closeout

The provider-free shared PlanProgram, deterministic compiler/proofreader and
separate bounded audit-only operator note pass. Two actual authored-synthetic
Gemini 2.5 Flash calls used the exact Bernie keyless-ADC Sydney Vertex lane and
returned HTTP 200, but both selected invalid prior-output coordinates. The
proofreader released nothing. The repaired audit retains the integer-only form
beside the independently admitted note without retaining raw provider output
or hidden reasoning. All four lifecycle ledgers are consumed: two at zero
calls and two at one call each. The absolute two-call ceiling is exhausted,
cleanup passes, and no further provider call is authorised. Continuity graph
revision 83 and Compass map revision 70 bind the fail-closed closeout.

## Rehydration and receipts

New sessions, post-compaction continuations, restored conversations, and
model/provider changes require a fresh orchestrator receipt. Before planning,
dispatch, verifier acceptance, integration, commit, or push, reread and name:

1. live Current Baton;
2. current authority allocation;
3. active plan and acceptance evidence;
4. protected-evidence and user-decision boundaries; and
5. Git refs and worktree state.

Conversation summaries are continuity aids only. They cannot establish model
allocation, provider transport, holdout rules, or user authority.

## Recovery and integration

Worker closeout provenance is non-transferable. Sol may adopt failed worker
source only as an untrusted candidate under
`docs/ariadne-orchestrator-recovery-lease.md`, must preserve failures, own and
record amendments, and run risk-proportional independent verification.

Only the protected integrator advances `master` and `handoff/current`. Worker
branches do not push or integrate. Test processes that load the shared
PostgreSQL schema must run serially.

## Primary documents

- `docs/ariadne-operating-model-v2.md`
- `docs/ariadne-autonomous-continuation.md`
- `docs/ariadne-orchestrator-receipts.md`
- `docs/ariadne-orchestrator-recovery-lease.md`
- `docs/ariadne-economical-deepseek-execution.md`
- `docs/ariadne-deepcode-runtime-observability.md`
- `orchestration/protocol_alerts.md`

For historical transport experiments, rejected Conductor plans, old model
allocations, and worker-pool incidents, consult the immutable snapshot rather
than copying those superseded instructions into a live sprint plan.

## 2026-07-17 Secure SDLC review protocol

Material sprints now carry a Sol-owned security delta. Named sensitive
triggers require asymmetric blue and fresh-context red evidence; cross-layer,
four-sprint cadence, provider, and deployment triggers require Sol purple
synthesis. The executable gate binds repository-contained artifacts to hashes,
candidate heads, decisions, recovery authority, and canonical unresolved-
finding severity. Workers still cannot accept, integrate, or push protected
refs. See `docs/ariadne-secure-sdlc-red-blue-protocol.md` and the 2026-07-17
security closeout.

## 2026-07-20 Ariadne Continuity Engine Increment 1

Yuri authorised an incremental continuity layer so agentic branch exploration
would not lose EMR4 product obligations. Increment 1 passes as repository-local,
metadata-only tooling: a valid Codex plugin/skill, a standard-library CLI, JSON
Schema, canonical directed graph and deterministic tests. It records evidence
pointers and multi-parent lineage; inherits behavioural contracts through
`builds_on`, `implements`, `validates`, `forked_from` and `synthesizes`; keeps
harvests candidate-only; and globally preserves closed authority boundaries.

The canonical six-node lineage keeps the active Reception One review connected
to the Stage 1 combined appointment-intent foundation. It reports the functional
client and live-local evaluation as historically accepted for their original
scopes while returning an exact open contract for the not-yet-proved combined
patient, practitioner, duration and bounded-time request. The tool's separate
`protects` relationship does not inherit product contracts or displace the
review baton.

Increment 1 does not install the plugin, add task/thread hooks, create Git refs,
import transcripts, call a provider or alter product code. A possible Increment
2 read-only pre-plan continuity packet and the separate Reception One combined-
scope proof both require fresh bounded decisions. See
`docs/ariadne-continuity-engine-increment1-closeout.md`.

## 2026-07-21 Ariadne Compass Increment 2

Yuri authorised a navigation-first Compass to make EMR4's strategic position
and the relationship among accepted tranches continuously legible. Increment 2
passes as repository-local, read-only metadata navigation over the Continuity
Engine. A revision-bound JSON map and deterministic human report state the EMR4
north star, place Reception One in Phase 2B, preserve the real seven-step
product journey, explain the current capability and distinguish candidates from
Yuri-owned decisions.

The canonical map is deliberately partial: it covers the active Reception One
lineage and Ariadne tooling, not every EMR4 domain. Validation fails closed on
stale graph revisions, fabricated lineage, unsafe evidence, sensitive fields,
unknown boundaries, an unauditable current node or malformed decision metadata.
The frozen 43-test population and all validators pass; fresh Gemini 3.5 Flash
High independently returns `pass`. The wider 236-pass/14-failure Deep Code PTY
observation remains an explicitly labelled clean-worktree Node dependency
baseline.

The Compass has no plan compiler, hook, model/provider call, dispatch, Git,
worktree, PR or product-command actuator. Its product horizon lists visual/
interaction synthesis and another exact typed Diary event family as candidates;
workflow-executive maturation is a separate programme-support candidate. All
three require fresh Yuri decisions. See `docs/ariadne-compass-current.md` and
`docs/ariadne-compass-increment2-closeout.md`.

## 2026-07-22 Ariadne sandbox-DAG first fork

Yuri authorised the first real Continuity Engine fork to explore a DAG of
sandboxed leaves without changing accepted Reception One or Compass work. The
fork passes as a repository-local, authored-synthetic and non-executing
protocol. It records typed frames, provenance, freshness, immutable attempts,
fan-out/fan-in and one terminal human-authority gate.

During implementation Yuri refined the topology: the orchestrator is the
control plane, not a compulsory relay. A leaf may send typed data directly to a
peer when both immutable start-up policies name the peer, channel and frame.
Unilateral, ambient or peer-control communication fails closed. A policy change
requires a later container generation, higher revision and explicit restart
lineage; the prototype describes but does not perform that restart.

The 12-test focused and 55-test combined populations and all validators pass;
fresh Gemini 3.5 Flash High returns `pass`. The continuity graph records the
accepted exploration through `forked_from` while the mechanically rebound
Compass retains the same seven-step Reception One journey and current product
position. No model, real container, API, database, event runtime, EMR command,
provider, PII, protected/historical, production, deployment or release surface
opened. See `docs/ariadne-sandbox-dag-fork-closeout.md`.

## 2026-07-22 Ariadne Synaptic Event Router candidate

Yuri asked to preserve a possible scope-aware bridge between committed practice
events and a future operational sandbox DAG before deciding whether to begin a
new tranche. The recorded Synaptic Event Router would deterministically compare
a minimal committed domain event with declared live node-scope leases and send
bounded conversation-style steering notices only to intersecting nodes. The
orchestrator remains the control plane and need not relay every event or value.

The candidate explicitly separates the development Continuity graph from any
future per-practice operational DAG. Events remain hints to invalidate context,
not row truth or command authority; a receiving node must reconcile at a
declared boundary through fresh authorized reads, deduplicate replay and prevent
stale generations from overwriting newer work. The first recommended tranche is
repository-local and non-executing and would incorporate the earlier dry-run
policy compiler as inspectable scope, subscription and restart manifests.

This planning record opens no database watcher, event consumer, event family,
listener, broker, node mailbox, container, model, product read, API mutation,
PII, autonomous action, production, deployment or release gate. The existing
default-off local `diary.appointment_rescheduled` exception remains unchanged.
See `docs/ariadne-synaptic-event-router-candidate-plan.md`.

## 2026-07-22 Ariadne Synaptic Event Router protocol

Yuri authorised the candidate's bounded repository-local, non-executing first
tranche. It passes as `ariadne_synaptic_event_router_protocol_pass` with a
separate operational-DAG schema, one authored-synthetic event, versioned scope
leases, deterministic route decisions, typed mailbox notices, exact fresh-read
grants, forward-only supersession and source-hashed dry-run start-up,
subscription and restart-policy manifests.

The implementation explicitly extends the accepted Sandbox DAG exchange
grammar. Router outbound policy and node inbound lease must bilaterally name the
router, control channel, notice kind and frame; every notice carries
workflow/graph revision, immutable sender/recipient, correlation, provenance
and freshness. The predecessor 13-node/13-exchange trace validates unchanged.

The canonical extension evaluates 11 route attempts: two deliveries fan out to
availability and ranking mailboxes, while nine replay, cross-practice,
non-intersecting, superseded, expired, non-newer, sensitivity and undeclared
cases are suppressed. Same-generation lease changes only narrow. A
cancel-and-supersede trace creates a later immutable attempt and rejects stale
completion. Fresh-read grants execute no read and return no data.

Fifteen focused and 106 combined tests pass with API Spine, Continuity, Compass,
Ruff, compilation, JSON and whitespace gates. No external review model was used
because Yuri closed model connections for the tranche; no independent-veto
claim is made. The Continuity graph advances to revision 18 and Compass revision
6 replaces the consumed protocol decision with a separately closed candidate
runtime-adapter decision while preserving the Reception One journey and current
product position.

PostgreSQL, the existing event feed, product APIs, operational persistence,
live mailboxes, models, containers, commands, additional event families, PII,
protected/historical data, Stage 3B, production, deployment, release and
autonomous action remain closed. See
`docs/ariadne-synaptic-event-router-protocol-closeout.md`.

## 2026-07-23 Ariadne Bounded Cognitive Work Cell and Proofreader Gate

Yuri authorised the repository-local authored-synthetic non-executing protocol
tranche to formalise the architecture discussion about node granularity and
deterministic egress. It passes as
`ariadne_bounded_cognitive_work_cell_protocol_pass`.

The accepted rule is `coarse-cognition-fine-authority`: node, topological leaf,
container and agent are independent concepts. Deterministic leaves are not
pre-containerised merely because they might later change implementation. One
agent-eligible but unoccupied booking work cell can retain a coherent bounded
request and emit five typed UX, human-review, audit, orchestrator and advisory
drafts while practice, principal, sensitivity, freshness and authority remain
constant. Material boundary changes still force a node split.

The deterministic proofreader is the sole egress route. It proves exact schema,
scope, source, freshness, authority, grounding and atomic consistency; its only
repairs are duplicate removal and stable sorting of opaque references with
immutable before/after hashes. The canonical proof covers all eight verdicts,
bounded retry and repeated-failure edge abortion, inert fresh-read
supersession, stale-completion rejection, all-or-none UX/human output and a
verified but non-command human gate.

The first draft-PR CodeQL aggregate check then caught a document-derived trace
echo path and one unused assignment. Yuri approved the contained repair: the
public trace now exposes fixed verdict labels and aggregate counts only, while
caller-selected identifiers, values and rejection details remain internal.
The repaired security and regression gates pass.

Twenty focused and 126 combined tests pass with Sandbox DAG, Event Router, API
Spine, Continuity, Compass, Ruff, compilation, JSON and whitespace gates. No
external worker, subagent or model reviewer was used because the semantics are
tightly coupled and Yuri closed model/provider connections; no independent-
veto claim is made. The Continuity graph advances to revision 19 and Compass
revision 7 records a separately closed in-memory scripted-rehearsal decision
without changing the seven-step Reception One product journey.

No PostgreSQL, event feed, product API, model, provider, real container, live
mailbox, persistent retry, human-gate UI, command, PII, protected/historical
data, Stage 3B, production, deployment, release or autonomous-action gate
opened. See
`docs/ariadne-bounded-cognitive-work-cell-protocol-closeout.md`.

## 2026-07-23 Ariadne scripted cognitive work-cell rehearsal

Yuri authorised the smallest executable descendant of the accepted Bounded
Cognitive Work Cell protocol: a finite repository-local process-memory
rehearsal over pre-authored synthetic attempts and drafts. It passes as
`ariadne_scripted_cognitive_work_cell_rehearsal_pass`.

Eight independent scenarios advance through 53 immutable hash-chained
transitions. The runner reuses the accepted deterministic proofreader and
cannot override its verdict or disposition. It records eight process-local
verified releases, four inert human-gate deliveries, two observed allowlisted
repairs, three bounded correction requests, two edge abortions, one inert
fresh-context supersession and one stale-completion rejection. Retry follows
declared attempt lineage; atomic output releases all-or-none; stale and rejected
frames cannot be routed to the gate.

Twenty-three focused and 149 combined tests pass with the predecessor work-cell
protocol, Event Router, Sandbox DAG, Continuity, Compass, orchestrator,
operating-model, API Spine and handover populations. Draft 2020-12 schema,
semantic validation, deterministic evidence, Ruff, compilation, JSON and
whitespace gates pass. No external worker, subagent or model reviewer was used;
the result claims deterministic local Sol acceptance, not an independent veto.

The Continuity graph advances to revision 20 and Compass revision 8 replaces
the consumed in-memory-rehearsal decision with a separately closed disposable
real-isolation candidate. The seven-step Reception One journey and current
product node remain unchanged.

No draft was generated and no adaptive agent, model, provider, real container,
network sandbox, persistence, PostgreSQL, event feed, product API, live mailbox,
human-gate UI, command, PII, protected/historical data, Stage 3B, production,
deployment, release or autonomous-action gate opened. See
`docs/ariadne-scripted-cognitive-work-cell-rehearsal-closeout.md`.

## 2026-07-23 Ariadne real-isolation rehearsal

Yuri authorised the recorded one-shot real-isolation candidate. It passes as
`ariadne_real_isolation_rehearsal_pass` after one local Docker lifecycle around
the unchanged accepted authored-synthetic tape.

The Docker Official Python 3.12.13 Alpine 3.22 image is pinned to OCI index
`sha256:a190708a2dec1bd18b1decb539f8e8f5407abaa9bf39cacda583f7f8c11db322`
and linux/amd64 manifest
`sha256:9381e50cc82f4279b949fcd2d2f5e57cf97b1da2399eb956502364ceea2f4e83`.
The repository was neither build context nor mount: a temporary context
contained exactly 14 hash-bound authored-synthetic sources plus its fixed
manifest, and the Dockerfile ran no build command.

Pre-start effective inspection proved network none, a read-only root,
UID/GID 65532, all capabilities dropped, no-new-privileges, no mounts,
devices, secrets, forwarded host environment or ports, and exact 128 MiB
memory/swap, 0.5 CPU, 32-PID and 64-file limits. Inside the container the
payload observed loopback only and an `EROFS` write stop with no residue. It
ran the accepted eight-scenario, 53-transition tape twice through the unchanged
proofreader and exactly reproduced the predecessor evidence. The container
exited zero without OOM or engine error.

The fixed container, derived image, run-acquired base reference and temporary
context were explicitly removed and independently rechecked absent. No
daemon-wide prune was run; possible unreferenced Docker layer/BuildKit cache
contains only the official base and allowlisted synthetic bundle and is
recorded as residual rather than called erased.

Twenty-eight focused and 177 combined tests pass with predecessor work-cell,
Event Router, Sandbox DAG, Continuity, Compass, orchestrator, operating-model,
API Spine and handover populations. Schema/semantic validation, Ruff,
compilation, JSON and whitespace gates pass. Sol retained the tightly coupled
image/provenance/run/cleanup surface; no worker, subagent or model reviewer was
used and no independent-veto claim is made.

The first protected-PR CodeQL review flagged the structural `CommandRunner`
protocol ellipsis as a no-effect statement. Replacing it with `pass` was a
mechanical, behaviour-neutral correction; focused and required PR gates were
rerun.

The Continuity graph advances to revision 21 and Compass revision 9 replaces
the consumed isolation decision with a separately closed bounded
agent-admission design candidate. The seven-step Reception One product journey
and current product node remain unchanged.

No adaptive agent, generated draft, model, provider, networking, writable or
mounted runtime input, secret/workload identity, persistence, PostgreSQL,
event feed, product API, live mailbox, human-gate UI, command, PII,
protected/historical data, Stage 3B, production, deployment, release or
autonomous-action gate remains open. See
`docs/ariadne-real-isolation-rehearsal-closeout.md`.

## 2026-07-23 Ariadne bounded agent-admission design

Yuri authorised the provider-neutral design descendant of the accepted work
cell, scripted rehearsal and one-shot isolation proof. It passes as
`ariadne_bounded_agent_admission_design_pass` without occupying the cognition
adapter or selecting a model, provider or transport.

The admission envelope binds six typed, minimal, source-labelled and
freshness-bound authored-synthetic frames to the exact five accepted draft
ports. The canonical payload is 660 bytes under finite frame/input/output and
two-attempt caps. Token accounting remains explicitly unresolved until a model
and tokenizer are selected. Tool, secret, network, filesystem, process,
database, event, mailbox, product and command capabilities are empty or false;
cancellation and supersession are terminal; and every draft must pass through
the accepted deterministic proofreader.

Fifteen cases treat embedded policy-bypass text as evidence data while
rejecting transport selection, guessed token authority, capability expansion,
scope drift, unknown or secret-class frames, stale context, budget overflow,
late completion, proofreader bypass and command-shaped output. Six source-
hashed dry-run manifests remain default-deny and non-executing. The three
catalogued topologies - in-cell local, host-brokered local and remote-provider
broker - are all unselected and unconfigured.

Forty-two focused and 219 combined tests pass with the real isolation,
scripted/bounded work-cell, Event Router, Sandbox DAG, Continuity, Compass,
orchestrator, operating-model, API Spine and handover populations. Draft
2020-12 schema, semantic validation, artifact equality, Ruff, compilation,
JSON and whitespace gates pass. Sol retained the tightly coupled authority and
negative-case design; no worker, subagent or external model reviewer was used
and no independent-veto claim is made.

The Continuity graph advances to revision 22 and Compass revision 10 replaces
the consumed design decision with a separately closed first generated-draft
rehearsal candidate. The seven-step Reception One product journey and current
product node remain unchanged.

No model selection/call/mount, prompt/context transmission, provider/broker,
container action, network, secret, generated draft, persistence, PostgreSQL,
event feed, product API, live mailbox, human-gate UI, command, PII,
protected/historical data, Stage 3B, production, deployment, release or
autonomous-action gate opened. See
`docs/ariadne-bounded-agent-admission-design-closeout.md`.

## 2026-07-23 Ariadne DeepSeek in-cell generated-draft rehearsal

Yuri selected the first admission topology: Claude Code 2.1.201 running
tool-less and sessionless inside a disposable work-cell container, with
authored-synthetic context only, DeepSeek V4 Flash behind a separate one-use
credential broker, and deterministic-proofreader-only egress.

The inspected cell was read-only, unmounted, non-root, capability-free,
resource-capped and attached only to an internal network. The provider key
existed in the broker environment, not the cell. The exact five-file build
context excluded the repository and the product, database, event, mailbox and
command surfaces remained disconnected.

The single authorised model process started and consumed its attempt. Claude
Code then made a preliminary request whose method or path was outside the
frozen broker allowlist. The broker rejected it before forwarding; DeepSeek
received zero requests, so no provider inference or cost occurred, no
generated draft existed, and the deterministic proofreader received nothing.
The sanitised broker retained the rejection reason but deliberately did not
retain the method or path, so the exact gateway prerequisite remains unproved.

All work-cell, broker, internal-network and derived-image-tag cleanup checks
passed. The authoritative result is
`ariadne_deepseek_in_cell_generated_draft_rehearsal_revision_required`, not an
acceptance. The attempted architecture proved only that the inspected
cell/broker boundary failed closed and cleaned up.

The Continuity graph advances to revision 23 with a rejected exploration node.
Compass revision 11 marks the generated-draft direction blocked. Any
provider-blocked gateway-shape diagnostic, broker allowlist change or second
occupied model/provider attempt requires a fresh Yuri decision; the consumed
authority cannot be retried.

The first protected-PR Python Security run later reported two Bandit B108
findings on the literal `/tmp` targets in the Docker `--tmpfs` arguments.
These are container-internal memory-only scratch mounts, not host temporary
paths. Yuri approved the focused CI-only correction; two justified
`nosec B108` annotations make the boundary explicit, and the exact Bandit
profile plus focused checks pass without changing the executed runtime or its
evidence.

## 2026-07-24 Ariadne Terra/Gemini comparative rehearsals

Yuri authorised a provider-neutral direct-API comparison using one byte-
identical authored-synthetic task, a locked five-port draft schema, separate
tool-less work cells and broker-only credentials. Terra was first, followed by
complete teardown and Gemini. Each lane had one process and one provider-call
maximum with no retry, fallback, tools, cross-model input or voting.

Attempt 001 consumed Terra when its internal broker rejected the sealed full
schema before provider forwarding. The host had hashed explicit UTF-8/LF bytes
but then used a Windows text write that translated the derived schema to CRLF.
Gemini was suppressed by the boundary-stop rule. No provider received a
request. The local preparation was corrected to explicit bytes and a focused
regression proved all five actual build-context hashes.

Yuri then explicitly authorised fresh attempt 002 with new single-use ledgers.
Both credential, test, corrected-hash and real-isolation preattempt gates
passed. Terra made exactly one `gpt-5.6-terra` Responses API request and
received HTTP 500. After complete Terra teardown, Gemini made exactly one
`gemini-3.5-flash` GenerateContent request and received HTTP 400. Neither
provider presented a draft, so provider-schema, full-schema and deterministic
proofreader gates were not reached.

The broker retained status, byte count and response hash but no raw error body,
error type/code/parameter or provider request identifier. The exact causes are
therefore unresolved and no model-quality conclusion is permitted. Both fresh
process ledgers are consumed, no retry or fallback occurred, and final
container, network and derived image-tag residue is zero.

The authoritative latest result is
`ariadne_terra_gemini_comparative_rehearsal_attempt2_revision_required`.
Continuity graph revision 25 and Compass revision 13 pause the branch behind a
fresh provider-contract diagnostic-design decision. No diagnostic, envelope
change, new occupied process, provider call, credential mount, prompt
transmission, product/database/event/mailbox/command surface, PII,
protected/historical evidence, production, deployment, release or autonomous
action is authorised.

## 2026-07-24 Ariadne provider-contract diagnostic hardening

Yuri authorised a provider-free follow-up to diagnose the comparative request
contracts without retrying either consumed lane. The repository now carries
explicit Terra and Gemini schema profiles, typed enums, a Gemini-safe boolean
shape, bounded allowlisted provider error metadata and a broker-owned
hash-chained external observer track. The observer records typed field, port,
hash and proofreader mechanics, not hidden reasoning, raw prompts, provider
responses or sensitive values.

The implementation gates passed, but its first verification invocation
accidentally inherited the repository PostgreSQL session fixture. That attempt
remains immutable
`ariadne_terra_gemini_provider_contract_diagnostic_hardening_revision_required`;
later clean commands do not erase the boundary incident.

Yuri then authorised the necessary follow-up. A new fixed verifier disables
repository conftest and plugin autoload, accepts no arbitrary test paths, and
forwards no database or provider environment. Its eleven-file, 255-test
population passes. A historical DeepSeek source-hash failure was traced to
Windows CRLF checkout materialisation; logical LF hashes match all immutable
execution evidence without regeneration.

The separate result is
`ariadne_terra_gemini_provider_contract_diagnostic_hardening_fresh_closeout_pass`.
Continuity graph revision 26 accepts only the repository-local diagnostic
descendant; Compass revision 14 removes the completed diagnostic decision.
Both provider ledgers remain consumed. No retry, provider call, credential
read, container, database, product runtime or durable audit-sink authority is
opened.

## 2026-07-24 Ariadne Terra/Gemini attempt 003 and external audit

Yuri authorised one fresh audited run with distinct single-use ledgers: Terra
first, exact teardown, then Gemini, with at most one provider call per lane and
no retry, fallback, tools, cross-model input, voting or downstream delivery.
The unchanged task was authored-synthetic.

The credential, fixed 259-test, static and real-isolation preflight gates
passed. Terra made one `gpt-5.6-terra` Responses API request and received HTTP
200. It returned five typed drafts which passed the provider envelope, full
release schema and deterministic proofreader. Three edges were released to
their declared typed recipients and two to human gates; no safe repair was
needed.

After complete Terra teardown, Gemini made one `gemini-3.5-flash`
GenerateContent request and received HTTP 400 `INVALID_ARGUMENT` before a
typed candidate. The schema and proofreader gates were not reached. The audit
intentionally retained no raw provider error message, so the exact rejected
request field is not proved and the lane is not retried.

The broker's live hash chains passed host verification. They record one call
per lane, request/response hashes, allowlisted status metadata, Terra's five
typed IDs/ports/types/field-name manifests and its proofreader disposition,
without prompts, responses, draft values, hidden reasoning or credentials.
Both lane cleanups passed with no container or private-network residue.

A post-run regression found that the original durable sanitized copies omitted
four non-sensitive fields that participated in each broker-ready event hash:
`allowed_path`, `upstream_host`, `upstream_path` and
`maximum_provider_calls`. The original evidence remains immutable and cannot
independently revalidate its first event. A separate analysis reconstructs
only those frozen manifest/broker values and verifies both complete chains.
Future sanitized exports retain the four fields. The audit therefore closes
`ariadne_terra_gemini_attempt3_external_audit_revision_required`; it is not
retrofitted to pass.

The overall result is
`ariadne_terra_gemini_comparative_rehearsal_attempt3_revision_required`
because Gemini produced no candidate and the original durable audit copies
were not self-verifying. Continuity graph revision 27 and Compass revision 15
record the consumed result and a blocked Gemini request-contract diagnostic.
No retry, provider call, request repair, additional error disclosure,
credential mount, prompt transmission, occupied process, container lifecycle,
product connection or durable practice audit-store authority remains open.

## 2026-07-24 Gemini provider-blocked contract diagnostic

Yuri authorised a repository-local, no-call Gemini request-contract
diagnostic and repair of the durable audit-export defect revealed by Terra's
successful attempt-003 lane. The diagnostic used authored-synthetic sentinels
only and read no provider credential or provider environment value.

Current official Gemini 3.5 migration guidance says `candidate_count` is not
supported in Gemini 3.x. Source inspection proved that attempt 003 sent
`generationConfig.candidateCount: 1`. The exact request constructor is now a
pure shared provider-contract function used by the broker and diagnostic, and
the unsupported field is absent. The provider schema remains unchanged at
`sha256:39a0f6da352a8da5f8291291a8f87abaa6aa9daa6b7359a8221341ebb756f1e2`.

The unsupported field is capable of the observed HTTP 400
`INVALID_ARGUMENT`, but the raw provider error message was intentionally not
retained. The diagnostic therefore does not claim exact historical causation
or exclude schema complexity and other provider-side checks.

The future sanitized audit exporter is also repaired structurally: every
allowlisted field is copied losslessly, while any unallowlisted field fails
closed before export. A complete synthetic broker-ready event revalidates
after export and a synthetic new field is rejected. Original attempt-003
evidence remains immutable and revision-required.

The result is
`ariadne_gemini_provider_blocked_request_contract_diagnostic_pass`.
Continuity graph revision 28 and Compass revision 16 close the diagnostic
decision and leave only a separately authorised occupied Gemini retry at
Yuri's gate. No provider/model call, credential read, prompt transmission,
container, network, database, product API, event feed, mailbox or command
surface was opened.

## 2026-07-24 Gemini repaired-request attempt 004

Yuri authorised one Gemini-only occupied authored-synthetic attempt using the
repaired shared request constructor, a distinct single-use ledger, the
fail-closed durable audit exporter and the existing disposable work-cell
isolation. Terra, OpenAI, DeepSeek, fallback, retry, tools, cross-model input,
voting and downstream delivery were excluded.

The 43-test focused population, fixed 266-test repository-only population,
static validation and provider-free real-isolation preflight passed. The
attempt-004 ledger was then consumed immediately before the work cell started.
The broker made exactly one `gemini-3.5-flash` Developer API
`generateContent` request. Google returned HTTP 400 `INVALID_ARGUMENT` before
a candidate, so neither provider/full schema nor deterministic proofreader ran.
Removing `candidateCount` did not admit this exact request; the remaining
rejected constraint is unknown because the reviewed audit contract still
excludes the free-form provider message and raw response.

The separate external audit passes as
`ariadne_gemini_attempt4_external_audit_pass`. Its durable chain directly
verifies one broker-ready event, one call start, one call completion, HTTP 400
metadata and complete work-cell, broker, private-network and image-tag cleanup.
No prompt, credential, raw response, draft value, hidden reasoning, PII,
database/product data, protected evidence or historical Diary material was
retained or accessed.

The occupied result is
`ariadne_gemini_repaired_request_attempt4_revision_required`. The direct
Gemini Developer API route has no Australian-processing claim. Current Google
Cloud documentation separately lists `gemini-3.5-flash` for Sydney
`australia-southeast1` ML processing, while current OpenAI API documentation
offers Australian regional storage but not regional processing. Any bounded
error-detail diagnostic, further direct call, Interactions migration or
Sydney-bound Vertex design/run is a fresh Yuri decision. Continuity graph
revision 29 and Compass revision 17 record that gate.

## 2026-07-24 Sydney Vertex Gemini 2.5 Flash bounded rehearsal

Yuri authorised a conditional seven-tranche descendant of the consumed
Terra/Gemini work, initially selecting Gemini 3.5 Flash through the exact
`australia-southeast1` Vertex endpoint and existing Bernie keyless
impersonated-ADC boundary. The complete pasted authority was preserved in a
new plan and receipt before Tranche 1. Current official documentation did not
publish Gemini 3.5 Flash for Sydney, so that node failed closed with zero calls
and remains immutable.

Yuri then reoriented only the model to exact `gemini-2.5-flash`. A fresh
authority delta, plan, receipts, Continuity node and Compass state preserve
every other project, service-account, authentication, endpoint,
authored-synthetic, isolation, audit, call, cost, cleanup and stop boundary.
Current official documentation publishes Gemini 2.5 Flash as GA and includes
`australia-southeast1` for model availability and ML processing, so the
documentary admission gate passes. The model's 2026-10-16 retirement is
accepted only for this bounded rehearsal.

The provider-blocked tranche also passes: a typed authored-synthetic cell
request, exact regional Vertex request constructor, four-field typed output,
mechanical-only proofreader, bounded provider-error reducer, single-use broker,
relay, isolation policy and hash-chain audit contract pass 24 focused tests
without ADC, Google Cloud or provider contact.

An independent repository-only audit found and repaired one pre-acceptance
defect: the initial error reducer could preserve a neutral free-form provider
message. No provider response existed and no content leaked. The accepted
reducer discards all free-form messages, bounds the provider code and status,
admits only allowlisted field paths and retains raw error bytes only as a hash.
The provider-blocked unit run opened and consumed one temporary test ledger;
no durable rehearsal or occupied ledger was opened.

The first Tranche 3 gate then fails closed. Local inspection identifies the
exact impersonated credential type, project
`bernie-emr4-dev`, target service account
`emr4-bernie-ai-dev@bernie-emr4-dev.iam.gserviceaccount.com` and cloud-platform
scope, but neither `google.auth.default()` nor the existing gcloud ADC path can
refresh a usable short-lived token non-interactively. No token or raw auth
response is recorded. No cloud-control metadata, model endpoint, prompt,
durable rehearsal/occupied ledger, container, network or image follows the
failure.

The result is
`ariadne_vertex_sydney_gemini_25_adc_preflight_blocked`. Occupied calls and
retries are both zero. The seven-event sanitized external audit and
independent Sol review pass after repository-only hardening. Continuity graph
revision 34 and Compass revision 21 record the closed result and exact rendered
report. Yuri intervention is
required to restore or make usable the existing exact impersonated ADC. Codex
does not revoke, replace, interactively recreate or reconfigure it, and no
API-key, static-key, provider, model, project, identity, region or endpoint
fallback is permitted. A future continuation requires fresh five-source
rehydration and repetition of the complete read-only preflight.

## 2026-07-24 Gemini 2.5 Flash ADC-restored continuation

Yuri reported that the exact Bernie impersonated ADC had been authenticated
externally and directed Tranche 3 and conditionally gated successors to be
tried again. A fresh five-source rehydration, continuation plan and pre-plan
receipt preserved the exact model, project, service account, keyless ADC,
cloud-platform scope, prediction permission, Sydney endpoint,
authored-synthetic classification, isolation, audit, USD 1, two-call and stop
boundaries. The earlier ADC-refresh-blocked result remains immutable.

The repeated `google.auth.default()` check returned exact impersonated
credentials and refreshed non-interactively. Read-only inspection also passed
the existing billing entitlement, enabled Vertex AI API, exact enabled service
account, prediction-only custom role and sole
`aiplatform.endpoints.predict` permission, exact binding, Vertex `DATA_READ`
and `DATA_WRITE` audit, request-response logging disabled/absent, zero
user-managed service-account keys, Sydney endpoint, no fallback and current
`gemini-2.5-flash` publisher-catalogue presence.

The first publisher-catalogue read exposed a repository-local preflight defect:
the read did not bind its request quota to the exact target project. The local
reader was repaired to add only the per-command target quota-project argument,
and a focused regression was added. No external Cloud configuration, billing,
API, IAM, project, service account or credential state changed.

The terminal cache check did not verify explicit `disableCache: true`. The
read-only result does not establish that caching was positively enabled; it
establishes only that the frozen explicit-disable gate did not pass. Codex did
not change the external setting.

The continuation therefore closes as
`ariadne_vertex_sydney_gemini_25_cache_control_blocked`. Tranches 4–6 never
opened. No prompt or model payload was transmitted, no provider inference call
occurred, no durable rehearsal or occupied ledger opened, and no container,
network or task image started. Calls, retries and model-call cost are zero.
Independent residue inspection reports zero task containers, networks, images,
broker processes and temporary credential files.

The continuation-specific eight-event external audit verifies directly.
Independent repository-only review passes 36 focused tests after binding the
restored audit into Continuity and Compass, correcting cache-state wording and
distinguishing key inventory metadata from key material. Sol accepts the
fail-closed result. Continuity graph revision 38 and Compass map revision 25
bind the final artifacts and exact rendered report.

An externally authorised operator must determine the current Vertex
provider-managed in-memory cache state for `bernie-emr4-dev` and establish
explicit `disableCache: true` if needed without changing any other frozen
boundary. Another continuation then requires fresh five-source rehydration and
the complete read-only Tranche 3 preflight. No provider inference, real
isolation, typed release, Australian physical processing or sovereign
processing is proved.

## 2026-07-24 Gemini 2.5 Flash cache-disabled continuation

An authorised operator supplied a sanitized observation of the documented
project-level Vertex cache-control resource for `bernie-emr4-dev` with
`disableCache` explicitly present and true. A fresh continuity descendant,
five-source receipt and plan preserved the exact Google Vertex AI,
`gemini-2.5-flash`, Bernie project and service account, keyless impersonated
ADC, cloud-platform scope, `aiplatform.endpoints.predict`,
`australia-southeast1`, authored-synthetic, isolation, audit, one-primary/
one-conditional-retry, absolute two-call, USD 1, no-fallback and stop
boundaries. The earlier ADC and cache-control stops remain immutable.

The complete read-only Tranche 3 preflight passed twice, including exact
impersonated credential type and refresh, project, service account, scope,
billing/API, prediction-only role/binding/permission, Vertex Data Access
audit, disabled or absent request-response logging, explicit project cache
disablement, zero user-managed service-account keys, Sydney endpoint and
current publisher catalogue. The documented `us-central1` project cache
resource was used only as a prompt-free management read; no external
configuration changed.

The provider-free real-isolation lifecycle then passed. The non-root,
read-only, capability-dropped and resource-bounded cell had no mounts,
credentials, published ports, Docker socket, product data or database. It
could reach only the exact-path relay on the internal task network; only the
host broker held the one-use relay token and ADC boundary. One interrupted
host run and six setup diagnostics preceded the accepted dry run. All eight
provider-free ledgers are consumed with zero calls and valid audit chains.
The accepted dry fixture passed the typed exchange, deterministic proofreader,
mechanical safe-repair, atomic release and cleanup gates.

After Continuity graph revision 41, Compass map revision 28, the exact rendered
report, repository-only verifier, focused rehearsal tests and API Spine tests
passed, one distinct occupied ledger opened. Exactly one primary
authored-synthetic request used the exact Bernie impersonated ADC and:

`https://australia-southeast1-aiplatform.googleapis.com/v1/projects/bernie-emr4-dev/locations/australia-southeast1/publishers/google/models/gemini-2.5-flash:generateContent`

Vertex returned HTTP 400 `INVALID_ARGUMENT` before a candidate. The bounded
error retained only status/code, normalized status, an empty allowlisted field
path list, a constant redaction marker and a hash of discarded raw bytes. It
did not identify a deterministic request-contract defect. The conditional
retry is therefore ineligible and was not used. No raw prompt, response,
provider message, credential, token, reasoning, proofreader input, release or
fallback is retained or claimed.

The result is
`ariadne_vertex_sydney_gemini_25_occupied_rehearsal_revision_required`.
The occupied ledger is consumed with one call. The seven-event occupied chain,
consolidated external audit, independent repository-only audit and Sol review
pass for that fail-closed result. Task containers, networks, images, broker
processes, tokens and temporary contexts are zero; no daemon-wide prune or
external state mutation occurred. Continuity graph revision 42 and Compass map
revision 29 bind the terminal state and exact rendered report.

This evidence proves the configured and observed Sydney locational request
path, exact keyless identity boundary, credential-free cell containment,
single-use accounting, bounded error handling and cleanup. It does not prove
provider request-contract acceptance, model inference, model-generated typed
output, actual token use or billing, production suitability, Australian
physical processing or sovereign processing. A container constrains local
capability; it does not determine remote provider processing geography.

The descendant is closed. The unused numerical call allowance grants no
authority. Any provider-blocked diagnostic, broader error disclosure, request
revision, further occupied call or provider/model/region change requires a
fresh Yuri decision.

## 2026-07-24 Gemini 2.5 Flash post-failure provider-blocked diagnostic

After the occupied rehearsal closeout, Yuri separately authorised diagnosis
of the HTTP 400 failure. A fresh five-source rehydration, provider-blocked plan
and pre-plan receipt opened only local retained-evidence inspection and public
official Google contract review. Credential discovery or refresh, cloud-control
reads, provider calls, prompt transmission, occupied ledgers, request changes,
retries, fallbacks and product/API Spine surfaces remained closed.

The local constructor reproduced the immutable occupied provider-request hash
exactly:

`sha256:6710af194aba6a2731008475e5509fa27ba12c8b58e995e1952faa036ecba61c`

The exact response schema encodes
`generationConfig.responseSchema.properties.total_tiles.enum[0]` as JSON
integer `5`. Current official Vertex `Schema` documentation defines enum
members as strings, current structured-output guidance supports only string
enums, and the installed official Vertex v1 protobuf exposes the field as a
repeated string. The complete reconstructed request fails the official local
protobuf parser. A deep-copied in-memory counterfactual changing only the enum
representation to string `"5"` parses completely.

The result is
`ariadne_vertex_sydney_gemini_25_postfailure_diagnostic_pass`. The defect is
deterministic and sufficient to invalidate the retained request, making it the
leading explanation for the HTTP 400. The raw provider message was
deliberately discarded, so the diagnosis cannot prove which field the server
named or that no later provider/model validation would fail after correction.
The counterfactual was not sent and the request constructor was not changed.

The external audit and Sol acceptance record zero credential, cloud-control,
provider, prompt, ledger, retry, fallback and external-state operations. The
closed rehearsal and consumed ledgers remain unchanged. Continuity graph
revision 44 and Compass map revision 31 bind the accepted diagnostic and exact
rendered report without revising historical nodes.

This diagnostic authority is consumed. Any request-constructor repair, future
wire regression, new occupied ledger, further provider call or broader error
disclosure requires a fresh Yuri decision.

## 2026-07-24 Gemini 2.5 Flash iterative repaired run

Yuri first authorised the proved numeric-enum repair and one fresh occupied
call, then explicitly broadened only the numerical retry limit for the same
bounded lane: continue with necessary evidence-backed diagnose-repair-rerun
cycles until success or bounded-option exhaustion. The provider, exact
`gemini-2.5-flash` model, Bernie project and service account, existing keyless
impersonated ADC, cloud-platform scope, prediction-only permission, Sydney
locational endpoint, authored-synthetic data class, isolation, audit,
no-fallback, no-product and USD 1 boundaries remained frozen.

The initial repair represented the INTEGER enum member as string `"5"` and
passed the installed official Vertex protobuf, but occupied attempt
`gemini-25-repair-001` still returned bounded HTTP 400 before a candidate. Its
ledger is consumed and cleanup passed. Official structured-output guidance
supports enums only for strings, so the next evidence-backed repair removed
the enum from the INTEGER field and used exact numeric minimum and maximum
constraints while the deterministic release proofreader retained the exact
integer requirement.

The first provider-free run of that request failed before broker contact
because the relay container was running before its listener was ready. Its
zero-call ledger was consumed and residue cleared. A regression-tested,
connection-refused-only pre-connect retry repaired the local race without
permitting replay after connection. The next distinct provider-free lifecycle
passed the isolated typed exchange, proofreader, seven-event audit chain,
consumed zero-call ledger and cleanup gates.

After Continuity graph revision 48, Compass map revision 35 and the rendered
Compass report validated exactly, occupied attempt
`gemini-25-repair-002` used the exact Sydney Vertex endpoint and existing
Bernie impersonated ADC. Vertex returned HTTP 200 in 1108 ms and supplied
usage of 176 prompt tokens, 50 candidate tokens and 226 total tokens. The
deterministic proofreader found no defect and atomically released exactly:

- `summary`: `Project Lark has 5 tiles: 3 blue and 2 green.`
- `total_tiles`: `5`
- `risk_level`: `none`
- `evidence_ids`: `fact_alpha`, `fact_beta`

No API-key path, service-account key, global endpoint, regional fallback,
provider tool, grounding, retrieval, explicit cache, product data, database
access or command authority was used. The successful ledger consumed one
call; no provider call followed success. Under the frozen application prices,
the successful-call usage estimate is USD 0.0000564; actual provider billing
was not supplied.

All opened repair-run ledgers are consumed. Independent residue checks found
zero task containers, networks, images, broker processes, temporary tokens,
contexts or roots. Unrelated pre-existing local development servers were
preserved. The external audit, separate repository-only audit analysis, Sol
review and Sol acceptance pass. Continuity graph revision 49 and Compass map
revision 36 bind the terminal success and exact rendered report.

The result is
`ariadne_vertex_sydney_gemini_25_occupied_rehearsal_pass`. It proves one small
authored-synthetic typed release through the configured and observed Sydney
Vertex locational path and exact keyless identity boundary. It does not prove
Australian physical or sovereign processing, exact billing, general model
reliability, production suitability, or safety for product-derived, patient,
health, clinical or historical data. The authority is consumed and opens no
further provider call, product, database, command, production, deployment or
release path.

## 2026-07-24 Reception One visual and interaction synthesis

Yuri authorised a bounded high-fidelity Reception One product synthesis and
permitted live Vertex only inside a disposable authored-synthetic design cell.
The product UI node remained deterministic, unoccupied and disconnected from
provider runtime. The exact project, Bernie service account, keyless
impersonated ADC, `gemini-2.5-flash`, Sydney locational endpoint, USD 1 ceiling,
proofreader-only egress and no-fallback boundaries remained frozen.

Two provider-free ledgers were consumed after ordinary generic isolation
verifier defects; a third distinct lifecycle passed. The complete repeated
read-only ADC/control preflight passed. One primary Sydney Vertex call returned
HTTP 200 with 693 prompt, 493 candidate and 1,186 total tokens. Its draft failed
the deterministic proofreader on `priority_order_invalid`, released no field
and was not used. The request-contract-only retry was ineligible, so no second
call occurred.

One complete authored-synthetic visual concept was generated and inspected.
The implementation adopted its calm hierarchy but rejected invented
three-practitioner lanes and busy blocks. The deterministic Reception One
client now presents an integrated intent rail, truthful candidate-time
projection, explicit selected-but-not-reserved boundary and responsive
desktop/tablet/phone/keyboard behavior over the existing read and proposal
contracts. Exact four-viewport raster evidence, focused and inherited tests,
the repository-only verifier, static and schema checks and independent final
residue checks pass.

The result is
`reception_one_visual_interaction_synthesis_pass_with_vertex_candidate_rejected`.
Continuity graph revision 54 and Compass map revision 41 bind it. This proves
authored-synthetic responsive product behavior and one configured/observed
Sydney locational provider path. It does not prove an admissible Vertex design,
Australian physical or sovereign processing, representative usability,
patient/clinical-data safety, production readiness, public brand adoption,
deployment or release. All provider and product-authority expansions remain
closed.

## 2026-07-24 Reception One Stage 3B study readiness

Yuri authorised the proposed provider-free representative-staff evaluation
sequence. Sol kept the serial readiness work local and added a consent-gated
study sidecar, closed anonymous export schema, eight authored-synthetic tasks,
responsive browser evidence and a disposable live-local product-task
acceptance runner.

Rendered QA found and repaired a pre-consent visibility defect, an unreliable
scripted popup result, undersized controls, an ambiguity task not grounded in
the accepted fixture, reference-date wording and reuse of a pre-existing
review runtime. The final sidecar evidence passes at desktop, tablet and phone
sizes. The product-task runner uses a marker-owned disposable IPv6
FastAPI/PostgreSQL runtime and proves the patient timeline, practitioner
ambiguity, combined availability, proposal-only boundary and ordinary-Diary
fallback without API interception.

Database counts and hashes remain identical, appointment confirmation is not
activated, all inspected write-surface counts remain zero, and the exact
owned database and processes are removed. The repository-only audit is
explicitly not an independent-agent review.

The result is `stage3b_study_ready_awaiting_participants`, bound by Continuity
graph revision 56 and Compass map revision 43. It proves study readiness only.
No representative participant session has occurred, no usability threshold is
claimed, and low-interruption notice precision/recall remains `not_measured`.
Yuri must nominate or schedule five to eight voluntary current or recent
Australian general-practice reception staff before execution; Codex is not
authorised to recruit or contact them.

## 2026-07-29 Reception One Yuri-only internal walkthrough

Yuri reviewed the Stage 3B surface briefly and paused independent receptionist
execution. The accepted readiness result above remains intact, but the current
instrument was judged premature and too dominated by provenance language to
test the intended product value fairly.

The durable product direction now retains the ordinary Diary behind a
modeless foreground projection, moves the Diary to a requested day before
opening that projection, and treats the Bureau as three separate desks: a
bounded interpreting agent, a deterministic proofreader/sub-editor and a
deterministic typesetter. One concise fictional-data/no-write acknowledgement
replaces governance ceremony in the product demonstration; operational
proposal, ambiguity and stale-state warnings remain local to the state they
explain. Text is the first future integrated path, with explicit push-to-talk
voice separately gated.

A distinct Yuri-only walkthrough is ready. It uses the accepted eight
authored-synthetic tasks without participant demographics, counterbalancing or
research thresholds, records structured product criticism plus optional
bounded notes in memory, and exports JSON only after an explicit local action.
Desktop `1440 x 900` and phone `390 x 844` browser checks pass the gate,
progress, responsive layout and console conditions. The provider-disabled
disposable runner starts on IPv6 loopback, changes no database truth and
removes its owned database and processes.

The result is `reception_one_yuri_internal_walkthrough_ready`, bound by
Continuity graph revision 57 and Compass map revision 44. Yuri has not
completed the walkthrough, so no internal observation, participant result,
usability threshold or production-readiness claim exists. External
participants remain deferred. The integrated Bureau implementation, any
product-connected provider/model path, design-partner session, representative
cohort, voice, real or product-derived data, writes, deployment and release
all require later bounded decisions.

## 2026-07-29 Reception One Yuri-only walkthrough result

Yuri returned the exact local export
`yuri-walkthrough-20260728232225`. It validates against the closed Yuri
walkthrough schema and contains all eight task reviews plus the final review.
Its SHA-256 is
`12bc0b8cbcb1b4df2011d6a4c5ac406778b1a285944fd583f55525a6ba11089a`.

The session lasted 1,715 seconds. Five tasks worked, three partly worked and
none failed or were skipped. Five tasks rated Reception One better than the
ordinary Diary and three were not compared. Back-path uncertainty, Diary
context loss and technical wording each occurred three times.

The result is
`reception_one_yuri_internal_walkthrough_completed_promising_needs_revision`.
Yuri supports the Bureau workflow but records concerns for the foreground
projection and date-first movement, does not yet assess text-before-voice, and
marks the product `not_ready` for a design partner.

The accepted interpretation preserves the technically successful
availability-to-proposal flow while requiring a provider-free UX revision:
open on the intended reference day; keep the Diary visible behind a
content-sized modeless projection; make close, return and focus restoration
obvious; replace technical/legalistic wording with conversational copy; add
restrained visual character; and avoid `grid` in final user-facing language.

Continuity graph revision 58 and Compass map revision 45 bind the result. It is
one informed owner's internal authored-synthetic critique, not representative
participant evidence, a usability threshold, a model or voice assessment, or
production evidence. The next implementation remains a fresh Yuri decision.

Yuri then stopped the interactive runner. A fresh residue check found no owned
Python process, no listener on the task's loopback ports and no owned
disposable database. The disconnected user Chrome tab was preserved.
Continuity graph revision 59 and Compass map revision 46 bind the completed
session cleanup.

## 2026-07-29 Reception One Bureau Typed Plan Protocol

After the provider-free integrated Bureau baseline passed, Yuri authorised
implementation of the proposed typed dialogue between a future sandboxed
interpreter and the deterministic proofreader.

The accepted protocol contains fourteen closed operators with only `pure`,
`authorised_read` and `proposal_only` effects. It reuses the deterministic
semantic extractor and existing API Spine proposal operation identifiers for
create, move, resize, cancel and status requests. One pre-authored novel
squeeze-in request composes schedule read and squeeze-in assessment, producing
candidate information and warnings for human review without a new primitive,
confirmation or write.

The proofreader enforces exact schemas, request/practice/correlation scope,
context freshness, grounding, operator signatures, typed topological
dataflow, semantic action and effect ceiling. One bounded correction dialogue
rejects a missing duration then admits a corrected second and final attempt.
Six positive cases pass; six adversarial cases fail closed. The in-memory
executor independently rechecks plan hash and context revision before
returning an inert typed result.

No provider, credential, network, database, product-data, historical Diary,
event-family, API-route, confirmation or write path was opened. No task-owned
container, network, image, service or background process was created. No
commit, push, pull request or protected-ref change occurred.

The result is
`reception_one_bureau_typed_plan_protocol_provider_free_pass`, bound by
Continuity graph revision 64 and Compass map revision 51. It proves a
repository-local authored-synthetic typed language and deterministic egress
gate, not live-model interpretation, exhaustive receptionist-language
coverage, product-runtime behavior, representative usability, provider
residency, appointment mutation, voice, production, deployment or release.
One exact occupied model text lane requires fresh authority; product-context
reads and runtime proposal delivery remain a separate later decision.

## 2026-07-29 Reception One Bureau Model Text Lane

Yuri authorised the next typed-language descendant and subsequently granted
one exact occupied `gemini-2.5-flash` Sydney Vertex call after authenticating
the Bernie impersonated ADC. The provider-blocked model input exposes only the
authored-synthetic utterance, closed fourteen-operator catalogue and source
handles. Trusted host code attaches request/practice/correlation scope and
context revision; the model cannot author identity, literals, writes or
confirmation authority.

Known create and novel squeeze-in candidates pass the unchanged deterministic
proofreader and proposal-only in-memory executor. Invented writes, free
literals, forged scope, forward references, excess steps and duplicate
arguments fail closed. A non-root, read-only, no-network fixture cell passed
and left zero owned residue.

Every exact ADC, prediction-role, API, entitlement, Data Access audit,
request-response logging, in-memory cache, regional endpoint and isolation
preflight passed. Exactly one request reached
`australia-southeast1-aiplatform.googleapis.com`. Vertex returned bounded HTTP
400 `INVALID_ARGUMENT`, explicitly identifying excessive structured-schema
serving states. No candidate or proofreader result exists, nothing was
released, the ledger is consumed, no retry/fallback occurred and cleanup is
complete.

A zero-call provider-blocked diagnostic replaced the deeply nested provider
response schema with a compact `goal` plus plan-line string array. Trusted code
decodes that wire form into the unchanged exact typed candidate and unchanged
proofreader. Focused regressions pass, but the repair is not provider-validated.

Continuity graph revision 66 and Compass map revision 53 bind the result. It
proves the configured/observed Sydney request path and fail-closed controls,
not model interpretation, generated-plan quality, Australian physical or
sovereign processing, product-data safety, production, deployment or release.
A further occupied call requires fresh exact authority and a distinct ledger.

## 2026-07-29 Reception One Bureau Model Text Lane iterative retry

Yuri granted bounded continuing diagnose-repair-rerun authority for the exact
authored-synthetic `gemini-2.5-flash` Sydney Vertex lane until the first
admitted result or bounded-option exhaustion. The authority preserved project
`bernie-emr4-dev`, the exact Bernie service account, existing keyless
impersonated ADC, `australia-southeast1`, the regional Vertex hostname,
credential-free isolated work cell, deterministic proofreader, external audit,
no fallback, no product or database data, no write, and the cumulative USD 1
ceiling. Each provider attempt required a distinct ledger and complete
fail-closed audit.

Retry 001 failed before proofreader admission because provider output did not
match the compact goal/plan-line wire contract. A focused provider-blocked
repair constrained goal vocabulary and admitted only mechanical goal-casing
and separator-whitespace normalization. Retry 002 returned HTTP 200 but failed
closed at four allowlisted source-handle paths; no field values were retained
or released. The next focused repair gave the model a canonical prior-step
source form while explicitly prohibiting guessing or semantic source
transformation.

Retry 003 returned HTTP 200 in 1435 ms using the same exact Sydney Vertex and
Bernie ADC boundary. The deterministic proofreader admitted five operators:
patient resolution, practitioner resolution, date resolution, practitioner
schedule read and squeeze-in assessment. It released twelve grounded,
authored-synthetic, proposal-only fields with no wire or proofreader repair.
The admitted result retained `human_gate=true` and
`write_performed=false`; it did not confirm, mutate or deliver anything to a
product user.

All three ledgers are consumed. No provider call followed the first admitted
result. No fallback, IAM/configuration mutation or credential exposure
occurred, and independent container, network, image, broker, process and
temporary-root residue checks pass. The conservative published-price upper
bound is USD 0.0313344; invoice cost was not observed.

The terminal result is
`reception_one_bureau_model_text_iterative_retry_pass`, bound by Continuity
graph revision 70 and Compass map revision 57. It proves bounded model
interpretation, deterministic typed release, and the configured and observed
Sydney locational request path for authored-synthetic input. It does not prove
Australian physical or sovereign processing, product-derived or patient-data
safety, representative usefulness, production suitability, appointment
mutation, deployment or release. The completed retry authority is closed.
Product-context reads and runtime proposal delivery remain a separate
Yuri-owned decision.

## 2026-07-29 Reception One Product-Context Proposal Runtime

Yuri authorised continuing through the necessary bounded runtime tranches.
The accepted descendant adds a separate default-off, development-only
product-context proposal route without changing the blocked legacy Bernie
interpreter gate.

Trusted FastAPI code now owns authentication, practice scope, minimum
authored-synthetic patient/practitioner reads, request-scoped opaque handles,
fresh availability search and the proposal adapter. The planner remains an
untrusted closed-operator composer, and the deterministic proofreader remains
the only release gate. The Bureau receives only typed proposal or
clarification fields and retains the selected-not-reserved boundary.

A disposable IPv6-loopback acceptance made one genuine, non-intercepted
browser-to-FastAPI-to-PostgreSQL request. It returned eight current 15-minute
candidate slots while appointment, audit, idempotency and Bernie-session
counts/hashes remained unchanged. Provider calls and writes were zero. The
exact database, processes and temporary root were removed without touching
Yuri's IPv4 preview.

One separate authored-synthetic occupied cell then stands as the model proof:
`gemini-2.5-flash`, project `bernie-emr4-dev`, exact Bernie service account,
keyless impersonated ADC, `australia-southeast1` and
`australia-southeast1-aiplatform.googleapis.com`. HTTP 200 arrived in 1634 ms
with 1507 total tokens. The proofreader admitted five operators without repair
and atomically released only twelve proposal fields with
`write_performed=false`. The ledger is consumed, the nine-event hash chain
validates, no call followed success and cleanup is complete.

The occupied call was made before the exact uninterrupted live-local proof
existed. Earlier evidence split the real API/PostgreSQL test from the browser
fixture. This sequencing deviation is permanently retained; the later
provider-free proof closes the missing evidence but does not rewrite the
historical order. The occupied cell received only its static
authored-synthetic frame and was not repeated.

The terminal result is
`reception_one_product_context_proposal_runtime_pass_with_closed_sequencing_deviation`,
bound by Continuity graph revision 72 and Compass map revision 59. It does not
connect the model to product context or open real/product-derived data,
appointment confirmation or writes, voice, production, deployment or release.

## 2026-07-30 Reception One Extended Proposal Runtime

Yuri authorised the next default-off authored-synthetic runtime section through
its bounded live Vertex denouement. The local Bureau runtime now passes move,
duration change, cancellation review and squeeze-in assessment. Four genuine
browser requests crossed FastAPI and PostgreSQL, used request-scoped opaque
appointment handling and existing non-mutating update/delete proposal safety
adapters, preserved every forbidden-write hash and cleaned up completely.

The exact Bernie keyless impersonated-ADC preflight passed. Occupied attempt
001 returned HTTP 200 in 921 ms but failed the strict local wire contract at
`$.goal` before the proofreader. A focused instruction-only mapping repair and
complete provider-blocked rerun passed. The sole repaired retry returned HTTP
200 in 1,114 ms but failed closed at `$.goal` and `$.plan_lines[0]`. No
candidate reached the proofreader and no value was released in either attempt.

Both distinct ledgers are consumed, the absolute two-call ceiling is
exhausted, no fallback occurred and independent task residue checks are clear.
The terminal result is
`reception_one_extended_proposal_runtime_local_pass_live_vertex_unadmitted`,
bound by Continuity graph revision 74 and Compass map revision 61. The
deterministic authored-synthetic runtime is accepted; live model interpretation
of the extended families remains closed pending a newly authorised
request/wire-contract design. The observed Sydney locational path is not
evidence of Australian physical or sovereign processing.

## 2026-07-30 Reception One Pre-printed Form v5

Yuri authorised an untaught prompt-context baseline before any training work.
The new descendant keeps PlanProgram v3 unchanged but makes `version_code` a
broker-owned pre-printed field. Gemini may fill only `operator_note`,
`goal_code` and `steps`; the broker may validate and assemble the form but may
not infer, repair or reinterpret its meaning.

Provider-blocked and real-isolation gates passed. One exact
`gemini-2.5-flash` occupied call used the existing Bernie keyless impersonated
ADC and the Sydney locational Vertex endpoint. It returned HTTP 200 in 2,574
ms and filled the bounded form on the first attempt. The deterministic
proofreader admitted six grounded authored-synthetic operations without
violation or safe repair and released only a non-writing move proposal that
requires human confirmation. The bounded operator note is audit-only. No
correction turn or retry was needed; the sole ledger is consumed and cleanup
is complete.

The accepted result is `reception_one_preprinted_form_v5_occupied_pass`,
bound by Continuity graph revision 94 and Compass map revision 81. It is one
positive familiar-case baseline, not evidence that prompt teaching or tuning is
unnecessary. The next defensible step is a frozen authored-synthetic multi-case
evaluation across the existing typed goal and operator catalogue, followed by
the smallest prompt-context intervention only if repeatable error clusters are
observed. The Sydney endpoint evidence does not prove Australian physical or
sovereign processing, and no product data, appointment write, Word, voice,
production, deployment or release authority is opened.

## 2026-07-30 Reception One Pre-printed Form v5 Multi-case Evaluation

Yuri authorised continued evaluation of the untaught v5 form before any
prompt-context teaching. The successful move remained a historical anchor and
was not replayed. The new frozen cohort covered create, resize, cancellation,
status change, squeeze-in assessment and an ambiguous clarification request.

Provider-blocked oracles, the unchanged two-cell real-isolation proof, relevant
API Spine and repository verification, exact Continuity/Compass rendering,
fresh Bernie impersonated-ADC/cloud controls and pre-call residue all passed.
The occupied cohort then completed in seven HTTP 200 calls through the Sydney
locational endpoint. The five actionable cases were admitted first-turn. The
ambiguous case's first form had one correction-eligible operator-arity defect;
the closed proofreader ticket led to a complete replacement that released only
a typed clarification with no API operation or write.

After the valid create lifecycle had closed, the batch coordinator initially
failed because the external-audit renderer did not recognise the new
case-bound attempt identifier. The repository-local repair regenerated only
the external audit from immutable evidence and allowed resume only from a
fully closed exact-bound case. It did not replay the call, reopen the ledger or
change the prompt, schema, proofreader, frame or provider binding.

All seven ledgers are consumed, all turn and parent audit chains validate and
all task-owned runtime residue is absent. The terminal result is
`reception_one_preprinted_form_v5_multicase_occupied_pass`, bound by
Continuity graph revision 99 and Compass map revision 86. The predeclared
training disposition is `diagnose_without_teaching`: one isolated correctable
error does not meet the repeated-error threshold for prompt examples.

This proves only six frozen authored-synthetic tasks and the configured and
observed Sydney locational request path. It opens no real/product data,
database access, appointment write, Word, voice, production, deployment or
release authority and does not prove Australian physical or sovereign
processing.

## 2026-07-30 Reception One v5 Broad Language Cohort

Yuri authorised a broader authored-synthetic utterance cohort before any
prompt-context teaching. Twenty-four new cases were frozen across create, move,
resize, cancel, status, squeeze-in and clarification families, including
single-turn paraphrases, two-turn corrections, minimal pairs and
action-tempting ambiguity.

All twenty-four provider-free oracles passed the unchanged v5 compiler and
proofreader: nineteen yield only the expected in-memory proposal or assessment
and five yield clarification only. The hyphenated `no-show` request is
deliberately frozen as a current deterministic-language gap, so it cannot be
retrospectively rescored after a model observation. An initial repository-only
identifier defect was repaired by shortening opaque case codes to the existing
one-use identifier bound; no utterance, prompt, schema or proofreader changed.

The two-cell isolation proof, 158 focused tests, 111 relevant API Spine tests,
the fixed 266-test repository-only verifier, static checks, JSON checks and
Continuity/Compass 100/87 binding passed. The fresh read-only cloud-control
preflight then stopped at `impersonated_adc_refresh_failed`.

No occupied cell or ledger was opened, no prompt was transmitted and no Vertex
call occurred. Provider call, HTTP response, token, cost and typed-release
counts are zero; there was no fallback or cloud mutation and final residue is
clear. The terminal result is
`reception_one_preprinted_form_v5_broad_language_adc_preflight_blocked`, bound
by Continuity graph revision 101 and Compass map revision 88.

Yuri must restore the existing approved Bernie keyless impersonated ADC before
a fresh read-only preflight. Codex may not recreate, replace or reconfigure
credentials. No model or training conclusion exists for this broader cohort.

## 2026-07-30 Reception One v5 Broad Language ADC-restored Continuation

Yuri restored the existing approved Bernie keyless impersonated ADC. The fresh
read-only preflight then passed the exact project, target service account,
impersonated-credential type, cloud-platform scope, prediction-only role,
`aiplatform.endpoints.predict` permission, Vertex Data Access audit logging,
disabled-or-absent request-response logging, disabled provider in-memory cache,
no service-account key and Sydney model-entitlement gates.

The frozen twenty-four-case cohort resumed without changing any utterance,
prompt, response schema, compiler, proofreader, model, provider, project,
identity, region or credential mechanism. After five completed provider calls,
the first correction turn stopped before Vertex. Eight distinct failed relay
ledgers were consumed at zero calls while the local transport was diagnosed.
The exact cause was a 9,095-byte typed correction packet crossing an 8,192-byte
relay limit even though the broker already admitted 32,768 bytes. The relay
ceiling now matches the broker ceiling; readiness no longer consumes the
one-use connection, and both transport hops retry only pre-connect refusal.
No sent request is replayed.

All twenty-four cases then closed in 32 HTTP 200 Sydney Vertex calls, below the
48-call and USD 1 ceilings. Provider usage was 74,390 prompt tokens, 10,766
candidate tokens and 85,156 total tokens, with an estimated frozen-price cost
of USD 0.0176181. Every one of the forty opened ledgers is consumed and owned
container, network, image, temporary-context and process residue is absent.

The capability threshold fails closed. Fourteen cases matched their exact
typed oracle. Eight used the single proofreader correction turn. Nineteen
released an in-memory, human-gated typed value and five released nothing. Ten
missed their frozen oracle; critically, five semantically wrong typed values
passed the current deterministic proofreader. The accepted disposition is
therefore `nominate_minimal_prompt_context_teaching`, coupled with stronger
semantic proofreader assertions and an untouched evaluation holdout.

Continuity graph revision 103 and Compass map revision 90 bind this result.
No further provider call followed the completed cohort. No API key, fallback,
product/database access, real or historical data, confirmation, write, human
delivery, deployment or release occurred. This proves the configured and
observed Sydney locational request path, not Australian physical or sovereign
processing.

## 2026-07-30 Reception One Receptionist-first v6.5

The complete original twenty-four-request authored-synthetic development
cohort ran through the exact `gemini-2.5-flash` Bernie keyless-ADC Sydney lane.
The first launch rejected locally before any provider call or ledger because
the new authority manifest omitted the inherited live-gate shape. Its
three-event audit is closed; the manifest and focused regression were repaired
and every provider-free, isolation, API Spine, Continuity, Compass, exact
read-only cloud-preflight and zero-residue gate passed again.

The occupied cohort then closed at twenty-three of twenty-four exact safe
outcomes in twenty-eight HTTP 200 calls. All six v6.4 defects passed. The sole
mismatch selected the exact squeeze-in goal and typed form on both calls but
repeated a person identifier in its internal decision note after correction.
The proofreader edge-aborted and released nothing. All twenty-eight ledgers are
consumed, turn and case audit chains verify, no call followed the terminal
result and owned residue is zero.

Continuity graph revision 132 and Compass map revision 114 bind the candid
closed result. v6.5 is not a capability pass and may not be called again. No
API key, fallback, product/database access, real or historical data,
confirmation, write, product delivery, deployment or release occurred. The
evidence proves only the configured and observed Sydney locational request
path, not Australian physical or sovereign processing.

## 2026-07-30 Reception One Receptionist-first v6.6

The complete original twenty-four-request authored-synthetic cohort ran
through the exact `gemini-2.5-flash` Bernie keyless-ADC Sydney lane. The v6.5
squeeze-in internal-note defect passed after one correction with an exact
typed form and a de-identified decision note.

The occupied cohort closed at twenty-three of twenty-four exact outcomes in
twenty-eight HTTP 200 calls. `b-clarify-fit` retained the exact clarification
typed form on both calls, but its terminal natural response used none of the
frozen clarification markers. The deterministic proofreader edge-aborted and
released nothing. All twenty-eight ledgers were consumed, all turn and case
audit chains verified and task-owned residue was zero.

Continuity graph revision 137 and Compass map revision 118 bind the candid
closed result. v6.6 is immutable and may not be called again. No API key,
fallback, product/database access, real or historical data, confirmation,
write, product delivery, deployment or release occurred.

## 2026-07-30 Reception One Receptionist-first v6.7

The v6.7 descendant taught one exact proofreader-compatible clarification
sentence pattern without changing the typed form, deterministic proofreader
or oracle. All focused, provider-blocked, real-isolation, API Spine,
Continuity, Compass, repository and exact read-only cloud-control gates passed
before the complete original twenty-four-request cohort ran.

`b-clarify-fit` passed on its primary call. The cohort closed at twenty-three
of twenty-four exact outcomes in twenty-five HTTP 200 calls. `b-move-shift`
retained the exact move typed form and admitted operator note on both calls,
but its natural response used none of the frozen move markers. The proofreader
edge-aborted and released nothing. All twenty-five ledgers are consumed, all
turn and case audit chains verify and task-owned residue is zero.

Continuity graph revision 141 and Compass map revision 122 bind the candid
closed result. v6.7 is immutable and may not be called again. A new descendant
may teach one exact move-response pattern without weakening the proofreader.
No API key, fallback, product/database access, real or historical data,
confirmation, write, product delivery, deployment or release occurred.

## 2026-07-30 Reception One Receptionist-first v6.8

The v6.8 descendant taught one exact proofreader-compatible move response
without changing the typed form, semantic oracle, deterministic proofreader,
desk context or generation settings. All focused, provider-blocked,
real-isolation, API Spine, Continuity, Compass, repository and exact read-only
cloud-control gates passed before the complete original twenty-four-request
cohort ran.

All twenty-four cases matched their frozen oracle on the primary call. The
sole v6.7 mismatch, `b-move-shift`, passed with the exact typed move form and
the taught bounded natural response. All twenty-four ledgers are consumed,
all turn and case audit chains verify, no call followed success and task-owned
residue is zero.

Continuity graph revision 144 and Compass map revision 125 bind the accepted
closed result. v6.8 is frozen and may not be called again. The evidence remains
a repeatedly observed authored-synthetic development cohort, not an
independent holdout. Yuri subsequently authorised a separately versioned
untouched authored-synthetic holdout and, only after a pass, a minimal
read-only authored-synthetic Diary-context integration. Real or
product-derived data, confirmation, writes, Word, voice, production,
deployment and release remain closed.

## 2026-07-30 Reception One v6.9 untouched holdout

The byte-frozen v6.8 contract passed a separately authored and sealed
twelve-case holdout through the exact Bernie keyless-ADC Gemini 2.5 Flash
Sydney lane. Eleven cases passed on the primary call and one passed after its
single authorised terminal proofreader correction. The local control process
ended after eight durably closed cases, but ledger-aware resume loaded those
immutable results and called only the four outstanding cases.

All thirteen ledgers are consumed, every audit chain verifies and residue is
zero. Continuity graph revision 147 and Compass map revision 128 bind the
accepted result. The holdout remains small authored-synthetic evidence; it
does not establish production fitness, real-data safety or Australian physical
or sovereign processing.

## 2026-07-30 Reception One read-only synthetic Diary-context bridge

The existing trusted product-context service alone read one exactly owned
disposable PostgreSQL fixture and built the minimal source-labelled frame. The
isolated cell received only request-scoped opaque handles and the selected
synthetic context; it received no database credential, raw UUID, handle map,
full Diary or unselected appointment. The database was removed before provider
contact.

One occupied Gemini 2.5 Flash call returned HTTP 200 through the exact Sydney
Vertex lane. The frozen proofreader admitted a 45-minute appointment-update
proposal requiring human confirmation. No correction, write, confirmation,
delivery, fallback or provider tool occurred. The ledger is consumed and
container, network, image, process and database residue is zero. Continuity
graph revision 149 and Compass map revision 130 bind the accepted result. The
next authorised descendant is a default-off authored-synthetic dual-planner
route with explicit wall-clock freshness; real/product data, writes,
production, deployment and release remain closed.

## 2026-07-30 Reception One default-off dual-planner runtime

The existing authenticated product-context proposal route now has a closed
planner selector. `deterministic` remains the zero-provider default;
`isolated_vertex` is separately default-off, development-only and restricted
to the exact authored-synthetic practice lane. Both planners terminate at the
same deterministic proofreader and proposal-only API Spine adapter.

Exactly one occupied Gemini 2.5 Flash request used the exact Bernie keyless-ADC
Sydney Vertex lane and returned HTTP 200. The proofreader admitted one grounded
45-minute resize proposal without violation or repair. The ledger consumed one
call and no retry, fallback, confirmation or write followed.

Windows retained a closed backend log handle during the outer harness cleanup,
so the first HTTP response was not durably retained. The completed
provider/cell/proofreader audit remained valid. A bounded cleanup retry repair
and zero-provider authenticated deterministic replay re-proved the same route,
fresh proposal adapter and unchanged database truth without making a second
provider call. All owned residue is absent.

Continuity graph revision 153 and Compass map revision 134 bind the accepted
composed result. It is not evidence of a second occupied response, production
fitness, real-data safety or Australian physical or sovereign processing. The
next descendant is provider-free, development-only Bureau UI wiring.

## 2026-07-30 Reception One Bureau runtime UI wiring

The provider-free development Bureau now exposes Standard and Isolated model
selection only behind the explicit authored-synthetic UI gate. Standard
remains the deterministic default. The browser can send only the closed
planner mode and can display only proofreader-admitted proposal fields plus
bounded non-secret provenance.

The non-intercepted browser/FastAPI/PostgreSQL Standard request returned HTTP
200 with proofreader admission and zero provider calls. The disabled isolated
gate returned HTTP 403 before context or provider use, cleared earlier
provenance and did not fall back. Database truth was unchanged and all exact
disposable state was removed.

Continuity graph revision 155 and Compass map revision 136 bind the accepted
result. A complete occupied request initiated through this visible UI remains
a fresh provider-call result and requires fresh exact authority and
pre-attempt gates.

## 2026-07-31 Reception One Bureau cost-bounded occupied successor

After the visible Bureau's first isolated evaluation failed closed because no
exact appointment was selected, the provider-free repair made the disposable
authored-synthetic appointment visibly selected and verified. A later
preprovider attempt exposed a local authority-compatibility mismatch; it made
zero provider calls, retained its full USD 0.02 reservation conservatively and
was closed without altering its immutable evidence.

The distinct corrected successor then passed every repository-only,
provider-blocked, ADC, entitlement, cost, isolation and residue gate. Exactly
one request reached `gemini-2.5-flash` through the Bernie keyless impersonated
ADC and `australia-southeast1-aiplatform.googleapis.com`. Vertex returned HTTP
200. The deterministic proofreader admitted one grounded 45-minute resize
proposal without repair, and no confirmation, command or write followed.
Database truth remained unchanged, the one-use ledger is consumed, no retry or
fallback occurred, cleanup is complete and cumulative conservative cost closed
at USD 0.0284309.

The related provider-free projection repair expands the appointment sheet
within the viewport and gives its results canvas a bounded internal scrollbar,
so all matching times remain reachable while the request bar stays visible.
Continuity graph revision 166 and Compass map revision 147 bind the accepted
result and passed preacceptance receipt. This is evidence of the configured and observed Sydney locational
request path and bounded typed release, not Australian physical or sovereign
processing, production fitness, general reliability or real-data safety. The
next active increment is provider-free post-admission runtime hardening; no
further provider call is required.

## 2026-07-31 Reception One Bureau post-admission runtime hardening

The accepted Standard and occupied Isolated proposal paths now converge on one
exact client admission tuple. The Bureau displays a proposal only when the
returned planner matches the requested planner, the deterministic proofreader
disposition is `admit`, and the call-count/audit-reference combination matches
the planner: zero calls and no audit reference for Standard, or one call and
one bounded opaque audit reference for Isolated model.

Six route-intercepted authored-synthetic Chromium cases passed. Valid Standard
and Isolated fixtures used the same renderer; planner, proofreader, call-count
and audit-reference mismatches all failed closed. Changing planner after
either valid result immediately removed the previous proposal, provenance and
audit reference and required a fresh exact selection. No provider call, ADC
read, database access, confirmation or write occurred.

Continuity graph revision 167 and Compass map revision 148 bind
`reception_one_bureau_post_admission_runtime_hardening_pass`. This proves
provider-free client binding and stale-state clearing, not a new provider,
backend, production or real-data result. The next material choice is whether
Word Online should open the native Diary/Bureau, embed a bounded companion
surface, or provide only a launch link; Word wiring remains closed pending
Yuri's direction.

## 2026-07-31 Reception One Word Hybrid contextual-launch foundation

Yuri resolved the access-surface choice in favour of the Hybrid model. A new
accepted descendant records the provider-free typed handoff from the existing
Word taskpane to the native Diary/Bureau. The context is exact, in-memory,
separate from authentication, absent from the URL and explicitly grants no
patient, command or provider authority. The native Diary observes a verified
read for the requested date before opening the projection.

The route-intercepted browser harness substituted only the Office host shim;
all unexpected external hosts failed closed. It recorded zero provider calls,
credential reads, database accesses, confirmations or writes and no console
errors. Source and published taskpane copies remain synchronized, and the
responsive request-input repair passed desktop and 390×844 inspection.

One pre-plan read-only search crossed into a protected fixture path. It was
stopped immediately; the output was not analysed, reused, copied, transmitted
or admitted into evidence. Subsequent work used explicit non-protected paths.
No historical continuity node was revised.

Continuity graph revision 168 and Compass map revision 149 bind
`reception_one_word_hybrid_contextual_launch_pass`. The next recommended
provider-free descendant is the compact Word companion shell. An authenticated
Word Online exercise, patient/product data, provider use, voice, writes,
production, deployment and release remain closed.

## 2026-07-31 Reception One Word compact companion shell

The accepted descendant establishes a small typed edge from Word into the
existing native-Diary Bureau and a smaller proofread edge back. Authentication,
zero-authority navigation context and request remain separate messages. The
Diary consumes each request id once, awaits date verification, applies the
existing deterministic projection, checks freshness and zero command/write
authority, and emits only a closed generic summary. Word performs a second
exact validation before rendering allowlisted local copy.

The repeatable Chromium exchange passed with three detailed authored-synthetic
appointments visible only in the Diary and a generic `results_ready` count in
Word. Provider, credential, backend, database, confirmation, command and write
counts are zero. Continuity graph revision 169 and Compass map revision 150
bind `reception_one_word_compact_companion_shell_pass`.

The shell remains loopback-only, default-off and stubbed-Office-host evidence.
Authenticated Word Online behavior, provider connection, live backend/product
context, patient or clinical data, voice, writes, production, deployment and
release remain closed.

## 2026-07-31 Reception One supervised Word desktop dialog check

After the authenticated Word Online localhost route failed closed under
Chromium Local Network Access subframe policy, Yuri selected the installed Word
desktop route. One disposable sideload opened one task-created blank document
and exercised the accepted companion request and generic summary seam.

The first observation exposed a repository-local initialization-order race:
`Office.onReady` replaced exact companion mode with the normal login view. The
repaired branch now preserves the loopback capability before token/login
handling and does not call normal backend initialization. The rerun opened the
native Diary, retained three detailed authored-synthetic results there and
returned only `3 results are ready in the Diary.` to Word.

The sideload stopped and all task Word, listener, server and temporary-file
residue is absent. Continuity graph revision 170 and Compass map revision 151
bind `reception_one_word_desktop_supervised_dialog_check_pass`. Word Online
remains platform-blocked; Office tenant identity, providers, backend/product
context, real data, voice, writes, production, deployment and release remain
closed.

## 2026-07-31 Raisa Cloud Run deployment blocked at public access

The frozen deployment ran through repository creation, immutable image push
and a ready private Cloud Run revision. The returned hash-based service origin
was materialised into the closed context before the final revision. Runtime
limits, zero-role/keyless identity and absence of secrets, volumes and network
attachments were independently rechecked.

The `allUsers` invoker binding failed closed under effective Domain Restricted
Sharing. No workaround was inferred from the earlier deployment authority.
Public HTTP, browser, manifest and Word Online evidence remains absent. The
accepted blocked closeout is bound by Continuity graph revision 178 and Compass
map revision 159; the next action is a Yuri-owned access-control choice.

## 2026-07-31 Raisa exact-service public access and Word handoff

The authorised exact-service Invoker IAM check change and all public static-host
gates passed without adding an `allUsers` binding or changing any other service,
IAM policy or organisation policy. Two narrow repository-local repairs produced
revision `raisa-office-web-dev-00005-w82`: a non-reserved `/health` probe and
exact-origin authored-synthetic companion admission. All task-owned local build
directories, repair image tags, containers and networks were removed.

The Word Online sequence is stopped before manifest transmission at the local
Chrome extension file chooser. One blank Word tab is preserved for the exact
retry after Yuri enables file-URL access. The cloud service and manifest require
no further change. Continuity graph revision 179 and Compass map revision 160
record the accepted partial result and explicit unresolved gate.

## 2026-07-31 Raisa Word Online terminal companion closeout

After Yuri enabled the documented file-upload prerequisite and manually
selected the validated manifest, the first Office-dialog run failed closed on
a hosted-Diary capability mismatch. It released no Word summary and performed
no fallback. Sol repaired only the exact origin-bound authored-synthetic
capability gate, added a focused regression test, rebuilt the closed context
and redeployed only `raisa-office-web-dev`.

The terminal rerun passed: the native Diary retained three synthetic matches
and Word received only the exact generic proofreader-admitted result count.
No provider, backend, database, credential, document-body, command,
confirmation or microphone path ran. Browser-extension asynchronous channel
errors were observed in the Diary console but did not affect the result; the
taskpane reported no application warning or error.

Both task-created blank Word documents were moved to the recoverable OneDrive
Recycle Bin. Task-local containers, networks, image tags, contexts, listeners
and temporary Docker credential directories are absent. The authorised
min-zero Cloud Run service and immutable image remain. Continuity graph
revision 180 and Compass map revision 161 bind the accepted terminal
descendant. No worker was dispatched and no protected Git ref moved.
