# Reception One shared typed language — closeout

## Result

`reception_one_shared_typed_language_fail_closed_call_ceiling_exhausted`

The provider-free implementation passes. Agent and proofreader now exchange
the same closed PlanProgram: integer goal, operator and source codes compile
mechanically into the accepted typed proposal language, after which the
unchanged semantic proofreader and proposal-only executor remain mandatory.
All six authored-synthetic proposal families pass local regression.

The agent also has a bounded speaking channel. An `operator_note` is short,
single-line, independently scanned, audit-only, never parsed into the plan and
never product-delivered. The live attempts demonstrated two admitted notes.
The audit records the note alongside the PlanProgram's integer-only form while
excluding raw responses, prompts and hidden reasoning.

## Occupied sequence

- Lifecycle 001 stopped locally because pretty JSON exceeded the relay cap. Its
  ledger is consumed at zero calls. Compact canonical transport repaired it.
- Lifecycle 002 made the first actual Sydney Vertex call and received HTTP 200.
  The note passed; the PlanProgram failed `output_index_invalid`; nothing was
  released.
- Model input v3 added concrete zero-based output examples, closed typed
  proofreader feedback and integer-form audit retention.
- Lifecycle 003 stopped before broker readiness because host validation omitted
  the new feedback packet when reconstructing expected input. Its ledger is
  consumed at zero calls. The exact broker defect was repaired.
- Lifecycle 004 made the second and final actual call and received HTTP 200.
  Its note passed. The typed audit proves the final step selected source code
  `1040`, an invalid output index on the date-resolution step. The proofreader
  released nothing.

No semantic repair was made. Replacing `1040` with the candidate-slot output
would alter the model's selected dependency and would turn the proofreader
into a planner. That would defeat the boundary being tested.

## Verification

The provider-blocked positive and negative suites, legacy model lane, typed
plan protocol, extended and product-context runtimes, relevant API Spine,
appointment proposal/update, Continuity, Compass and functional meta-grid
tests pass. The repository-only Ariadne verifier, JSON/schema validation,
Python compilation, Ruff, real container isolation, rendered Compass binding,
`git diff --check` and final residue checks pass.

Every opened ledger is consumed. Actual provider calls total two. No call
followed the final failure, no provider/region/model/identity/credential
fallback occurred, and all task-scoped containers, networks, images, temporary
material and broker processes are absent.

## What the evidence establishes

It establishes:

- a mechanically lossless shared coded language locally;
- a distinct, strictly bounded audit-only natural-language channel;
- deterministic rejection of invalid model-selected dependencies;
- two observed requests to the configured Sydney Vertex locational endpoint
  through the exact Bernie keyless impersonated-ADC boundary; and
- complete fail-closed cleanup and external audit.

It does not establish:

- a proofreader-admitted live model plan in this descendant;
- general Gemini reliability for this planning grammar;
- Australian physical or sovereign processing;
- safety for product-derived, patient, health, clinical or historical data;
- appointment confirmation or write authority;
- Word, voice, production, deployment or release readiness.

The next technical decision belongs to Yuri: retain the current arithmetic
source language and study model-guidance improvements in a separately
authorised call budget, or redesign prior-output references into a more
provider-constrainable representation before any further occupied work.
