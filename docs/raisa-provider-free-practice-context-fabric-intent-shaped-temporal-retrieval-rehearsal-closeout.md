# Provider-free Practice Context Fabric intent-shaped temporal retrieval closeout

Date: 2026-08-06

Status: accepted

Result:
`raisa_provider_free_practice_context_fabric_intent_shaped_temporal_retrieval_rehearsal_pass`

Reviewed source HEAD:
`b24b56bda296f3713b5e2c0e52545c749e71540a`

## Outcome

The first intent-shaped Context Fabric retrieval contract passes. One closed,
non-authoritative candidate now selects the smallest authorised combination of
the already accepted Current operational weave, Bureau Memory Bank and
patient-free bitemporal History without opening a source, route, provider or
command.

The pure provider-free path is:

`closed IntentRetrievalCandidate -> backend IntentRetrievalAuthorityBinding -> exact intent template and vocabulary mapping -> proofread upstream component catalog -> deterministic scope and bilateral-sharing intersection -> minimal IntentContextFrameSet or safe alternatives/unavailable result -> same-packet proofreader`

## Deterministic retrieval result

- Five closed intent codes cover Current-only, Memory-only, Historical-only,
  Current-plus-Historical comparison and ambiguous recent-reference rehearsal.
- Candidate fields cannot contain principal, role, practice, location, session,
  retention, provider, patient, prompt, SQL/vector query, source truth or command
  authority.
- The backend binding owns exact requesting Bureau, intent, components, field
  profiles, time, session generation, cardinality, bytes, alternatives and
  bilateral-sharing pairs.
- Accepted lowercase Memory and uppercase Current/Historical vocabularies are
  joined by one explicit mapping. There is no case folding or guessed alias.
- Each upstream proofreader is recomputed before facts are projected. The
  backend binding seals the exact resulting catalog digest.
- Current remains one atomic four-source coherence component. One minimal fact
  is projected from each Diary, waiting-room, practitioner-directory and private
  application-session frame; no source may be silently dropped.
- `REASSEMBLY_REQUIRED`, expiry, missing required scope, catalog substitution or
  historical coverage gaps return safe `NOT_AVAILABLE` without partial facts.
- `valid_at` plus `known_at` reproduces both the originally known waiting count
  and its later correction. Missing coverage remains a gap, not evidence of
  absence.
- Two equally admissible opaque recent-work references return two canonical
  alternatives with `identity_asserted: false`; no person, patient or event is
  guessed.
- Same-packet proofreading reconstructs the plan, selection, projection,
  temporal state, ambiguity result and digest tree. Content or provenance
  changes block release.

## Branded workspaces and Bureau authority

The rehearsal gives operational form to Yuri's durable architecture direction:

- `RECEPTION ONE(TM)` and candidate `Clinician One` are branded workspace and
  projection families, not security or authority domains;
- a Bernie request can receive a Rayleen recent-work component only through the
  exact bilateral `requesting<-origin:purpose` grant;
- Rayleen's Current component cannot cross because it contains private
  application-session state; and
- Consultant, requests/referrals, medicines/prescribing and billing/claims may
  later interweave context through the Fabric, but each retains independent
  capability, proofreader, professional/human and command gates.

No future clinical or practice-administration command is opened by this result.

## API Spine classification

This is an unmounted pure read-context protocol. No GraphQL root, resolver,
route, mutation or subscription; REST/OpenAPI command; application import or
runtime source was added. GraphQL remains the future read plane and REST/OpenAPI
the command plane. A context frame is never command evidence.

## Evidence and verification

The committed evidence contains the closed schema and canonical example,
canonical-LF hashes for the seven exact implementation artifacts and zero
side-effect counters.

Sol verification passed focused and inherited Memory, Current, temporal,
retrieval and API Spine gates, plus Ruff, compileall, JSON and whitespace
checks. The new focused test file passes 31 tests.

One fresh Gemini 3.6 Flash/high Antigravity project independently reviewed the
exact clean source HEAD and returned one schema-constrained `pass`. It ran the
eight-file provider-free suite as 199/199 passing and left the review branch and
HEAD unchanged. It confirmed the closed candidate, brand-independent binding,
explicit vocabulary mapping, four-source Current atomicity, bilateral Memory
sharing, private-session containment, upstream proofreader recomputation,
catalog binding, bitemporal selection, ambiguity handling, same-packet
proofreading and zero new actuator/API surface. No review correction or agent-
error entry is required. The dedicated clean review worktree and its local
non-protected review branch were removed after the unchanged-HEAD receipt was
preserved.

## Claim and authority boundary

Passing proves only an unmounted pure authored-synthetic intent-to-context
selection contract. It does not prove natural-language intent classification,
provider behaviour, patient/product privacy, live source retrieval, database
watching, persistence, production retention, clinical evidence retrieval,
cross-Bureau clinical workflow, runtime performance or command safety.

No patient, clinical, product-derived, financial, protected or historical-PHI
data; raw audit; live source/database/session/feed/watcher; persistence or
retention choice; model/provider or external retrieval; API/runtime wiring;
clinical, prescribing, referral or billing command; deployment, production,
release, Pages, protected evidence or protected-ref movement was opened.
`docs/branding/` and unrelated untracked artifacts remain preserved and
excluded.

## Next descendant

Under Yuri's standing uninterrupted planned-gate authority, the next safe
descendant is a separately frozen authored-synthetic model-required intent-
shaping rehearsal over this exact deterministic contract. Its plan must bind
the already selected `gemini-2.5-flash` model to the Bernie Vertex development
project and Sydney region, retain a thinking budget, exact schema, proofreader,
call/cost ledger, no fallback and zero patient/product/runtime/command scope.

This closeout does not itself execute or authorise a provider call. The next
plan must derive and freeze the exact occupied envelope before dispatch.
