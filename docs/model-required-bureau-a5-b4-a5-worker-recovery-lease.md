# A5.1 worker recovery lease

Date: 2026-08-05

Worker candidate: `52370189dac575bbbaa59a2458c6ea583edb8d6c`

Worker receipt:
`orchestration/agent_inbox/deepseek/model-required-bureau-a5-1-check-in-worker-receipt.json`

Incident: `AER-0021`

## Lease boundary

Sol adopts the complete A5.1 worker commit as an untrusted candidate, not as an
accepted worker result. The worker's out-of-packet
`app/schemas/diary_events.py` edit is explicitly transferred to Sol ownership
because the frozen plan requires a closed patient-free schema for the new
checked-in committed-event family. This transfer does not rewrite the worker
receipt, erase the scope breach or expand A5.1 authority.

Sol additionally owns:

- reconciling the three frozen route/model inventory tests identified by the
  worker;
- resolving the A5/B4 provisional migrations into one sequential Alembic chain;
- resolving only the shared `app/config.py` integration seam;
- repairing deterministic defects without widening the frozen plan; and
- producing separate integration, test and independent-review evidence.

No same-lane worker correction is requested. The lease permits local source
repair only inside the frozen A5.1/B4.1 plan and retains all closed provider,
data, GraphQL, autonomous-action, external-transport, deployment, release,
Pages, protected-evidence and protected-ref boundaries.

## Closure condition

The lease closes only after exact path review, focused and relevant regression
tests, migration upgrade/downgrade/head checks, full serial acceptance
proportionate to the runtime/database boundary, and a fresh Gemini 3.6
Flash/high read-only independent veto over the integrated candidate. Until then,
AER-0021 remains open and neither the worker pass nor this lease is acceptance.
