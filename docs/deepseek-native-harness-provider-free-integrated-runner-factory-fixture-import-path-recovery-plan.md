# DeepSeek native Harness provider-free integrated-runner factory fixture import-path recovery plan

Date: 2026-08-22

Timestamp: 2026-08-22T19:15:38.4788019+10:00 (Australia/Brisbane)

Status: frozen for one provider-free fixture

Operation: `deepseek-native-harness-provider-free-integrated-runner-factory-fixture-import-path-recovery`

Reasoning level: high

## Purpose

Remove the one demonstrated local fixture defect that prevented the accepted
factory-subcoordinate diagnostic from reaching installed
`AgentRegistry.create`. This is a distinct recovery attempt, not a retry of the
consumed fixture.

The recovery changes only the package-scope projection from
`package_root.parents[1]` to `package_root.parent`, proves both resulting
absolute import targets are existing files before process admission, and then
permits one separately identified provider-free Node process.

## Frozen source relation

The successor controller is a proof-carrying derivative of the accepted
diagnostic controller. Its JavaScript fixture body, installed registry route,
runner, occupied guard, factory, structured output and validation matrix must
be byte-equivalent after normalizing exactly the single package-scope
projection line. No second semantic source difference is admitted.

Before launch the controller must derive from the materialized package root:

- exact scope `node_modules/@deepseek-ai` through `package_root.parent`;
- exact Cordis target `cordis/lib/index.js`; and
- exact agent target `dsh-agent/lib/index.js`.

Both targets must resolve strictly as regular files beneath the exact scoped
root. Absence, escape, symlink ambiguity, a different scope basename or any
source-equivalence difference stops before Node and consumes no process.

## One distinct process

After all provider-free gates pass, the controller may persist the distinct
attempt identity `factory-fixture-import-path-recovery-001` and start exactly
one local Node process. It may import the installed rc.7 `Context` and
`AgentRegistry`, the exact occupied runner and the exact occupied old guard,
then invoke only the runner-supplied setup callback through the inert local
factory.

No native Harness or agent loop is started. No broker, worker, model, provider,
network, database, Docker or product target is reachable.

## Exact acceptance

The only passing result remains
`occupied_guard_graph_signature_mismatch_reproduced` with:

- one installed `AgentRegistry.create` transit and one setup invocation;
- structured coordinate `EFFECTIVE_TOOL_COMPOSITION_INPUT_INVALID`;
- generic runner terminal `failed` at `factory`;
- zero request, tool-result and turn observations;
- zero preset mount, model-selection, hook, session publication, target,
  broker, model, provider, network, database and Docker activity;
- zero retry, resume and fallback; and
- Node process, Cordis context and disposable root absent at admission.

Raw stdout and stderr are reduced to byte counts and SHA-256 digests before
semantic admission and are never retained. Any non-pass coordinate, terminal,
counter, schema, cleanup state or process exit is an immutable bounded negative
and stops without retry.

## Decision after terminal

If the exact coordinate passes, the evidence confirms the occupied
four-argument runner/three-parameter guard interface mismatch. The next
eligible correction may materialize only the already accepted four-argument
root-service-forwarding guard, bridge and sanitizer graph beside the unchanged
runner.

If it does not pass, no speculative runner or guard change is selected. The
operation closes on the exact observed provider-free terminal.

## Owned paths

- this plan and its threat-model delta;
- one operation controller under `scripts/`;
- one focused test under `tests/`;
- one operation continuity root containing contract, schemas, terminal
  evidence and report; and
- operation-specific Ariadne receipts and closeout artifacts.

The predecessor evidence and attempt identity, accepted runner/guard/bridge/
sanitizer bytes, package seed, product source and protected refs remain
immutable.

## Parallelism efficacy assessment

- DeepSeek: declined because every worker, model and provider request is
  forbidden and its factory boundary is the diagnostic subject.
- Gemini: reserved only if the one fixture yields a material runner/guard
  correction candidate after deterministic admission; it has no leverage on
  the mechanical path projection.
- Native subagents: declined under current developer policy and because source
  equivalence, prelaunch binding and one process form one serial attribution
  chain.
- GPT Sol owns implementation, the one process, deterministic verification,
  acceptance and closeout.
- Parallel work packages: none.

## Recovery

A pre-process source-equivalence, binding, schema or file-existence rejection
may be corrected mechanically because it consumes no process. Once Node starts,
attempt 001 is consumed. There is no retry, resume, fallback, second fixture or
native Harness launch in this tranche.

## Explicitly closed

No native Harness process, DeepSeek worker, broker, model/provider request,
accepted runner/guard/bridge/sanitizer mutation, product/configuration/API/
database/route/adapter/flag/allowlist/grammar/client/waiting-area change,
ordinary-practice enablement, generic-status `Arrived` change, patient/product/
appointment/clinical/historical/protected data, production runtime, deployment,
release, Pages, protected evidence or protected-ref movement. Local/origin
`master` and `handoff/current` remain exactly
`2e34bdad732fdab32fbf778280b3d3c70d66d602`. Preserve `docs/branding/` and all
unrelated untracked files. Stage explicit paths only; never use `git add .` or
`git add -A`.
