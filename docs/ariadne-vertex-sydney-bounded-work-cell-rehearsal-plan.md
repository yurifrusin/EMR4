# Ariadne Sydney Vertex Bounded Work-Cell Rehearsal — Conditional Tranche Plan

Date: 2026-07-24
Owner: GPT Sol
Authority: `docs/ariadne-vertex-sydney-bounded-work-cell-authority.md`
Status: active, conditional gates frozen
Target result: `ariadne_vertex_sydney_bounded_work_cell_rehearsal_pass`

## Purpose and lineage

This is a new Continuity descendant of the consumed
`ariadne-terra-gemini-comparative-rehearsal` node. It does not amend attempt
001-004 evidence. It replaces neither the accepted provider-blocked diagnostic
nor the consumed direct Gemini Developer API attempt.

The intended endpoint is one authored-synthetic occupied cognitive work cell
using an officially supported Gemini 3.5 Flash identifier or stable alias
through the Sydney `australia-southeast1` Vertex locational endpoint and the
existing Bernie keyless impersonated-ADC boundary.

## Frozen binding

| Control | Exact binding |
|---|---|
| Provider | Google Vertex AI |
| Model family | Gemini 3.5 Flash; official identifier resolved in Tranche 1 |
| Project | `bernie-emr4-dev` |
| Service account | `emr4-bernie-ai-dev@bernie-emr4-dev.iam.gserviceaccount.com` |
| Authentication | `keyless_impersonated_service_account_adc` |
| Required permission | `aiplatform.endpoints.predict` |
| Region | `australia-southeast1` |
| Base URL | `https://australia-southeast1-aiplatform.googleapis.com` |
| Data class | authored-synthetic only |
| Primary call ceiling | 1 |
| Conditional repair retry | at most 1, exact contract-defect conditions only |
| Absolute occupied calls | 2 |
| Application cost ceiling | USD 1 |
| Provider fallback | prohibited |
| Regional/global fallback | prohibited |
| API-key/static-key path | prohibited |
| Product/database/command authority | none |

## Boundary classification and API Spine

Classification:
`developer_only_authored_synthetic_access_ai_provider_rehearsal`.

The work cell receives one minimal typed authored-synthetic evidence frame and
cannot source data. The broker is an external-effect boundary with a
single-use ledger, exact endpoint/identity/project/model policy, bounded cost
and hash-chained audit. The model may emit draft-only typed content.
Deterministic verification is the only egress path. No GraphQL, REST command,
database, product API, event feed, appointment, clinical, deployment or release
surface is opened.

## Tranche gates

1. **Residency and provider admission:** official Google evidence must support
   the Gemini 3.5 Flash family through the Sydney locational endpoint. A
   provider-neutral policy must reject Developer API, global endpoint, fallback,
   API-key, provider/model/identity/project/region mismatch and overclaim.
2. **Provider-blocked contracts:** the typed request/response, broker,
   launcher, policy manifest, bounded error exporter and external audit verify
   without model contact or rehearsal-prompt transmission.
3. **Existing entitlement:** existing ADC, API, billing entitlement,
   prediction-only role/binding, exact permission, Data Access audit,
   disabled/absent request-response logging, no key and regional model access
   pass read-only checks. External mutations are forbidden.
4. **Provider-free real isolation:** the complete disposable work-cell and
   broker path proves non-root/read-only/bounded/no-host-network/no-Docker-
   socket/no-product-mount/default-deny containment, exact allowlisting,
   one-use ledger consumption, teardown and independent residue checks.
5. **Occupied call:** one primary request only after gates 1-4 and the bound
   Continuity/Compass revisions plus rendered Compass report validate.
6. **Proofreader/release:** exact schema, grounding, policy, authority,
   freshness, retry, model/project/region binding, allowed repairs and atomic
   release pass; otherwise the frozen human-gate/abort/retry disposition
   applies.
7. **Audit/closeout:** the external observer verifies the durable hash chain,
   typed admitted fields, supplied status/usage, cleanup and exclusions without
   retaining raw prompt, raw response, credentials or reasoning.

Each gate automatically opens the next only when it passes. Any failed gate
fails closed and still receives evidence and closeout.

## Retry contract

The only possible second occupied call is a repair retry after a primary
failure caused solely by a deterministic request-contract defect. Before the
retry the first ledger must be consumed and independently audited, the bounded
error contract must support the diagnosis, a focused regression must pass, all
provider-blocked gates must rerun, and a distinct ledger must bind the
unchanged provider, model family, project, service account, credential class,
region, endpoint, data class, output contract, isolation, residency assurance
and USD 1 ceiling.

Any other failure is terminal. No fallback or substitution exists.

## Audit contract

The external observer records attempt/ledger IDs, provider/model, configured
project and service account, authentication class, no-API-key-use assertion,
region/hostname, request/schema hashes, event sequence/hash chain, supplied
status/latency/usage, typed output manifest, proofreader/repair/retry/
freshness/supersession disposition, released authored-synthetic values if
admitted, cleanup/residue and Vertex control posture.

It never records credential material, access tokens, API-key presence or
information, raw prompts, raw responses, discarded drafts, hidden reasoning or
chain-of-thought.

## Stop boundary

Stop for Yuri on any missing/unrefreshable impersonated ADC; external account,
IAM, API, billing, project or service-account change; broader role; missing
audit control; unverified disabled request-response logging; unavailable
Gemini 3.5 Flash Sydney support; provider/model/project/identity/region/
credential substitution; global endpoint or fallback; API/static key need;
non-synthetic data; possible ceiling breach; credential exposure; containment
or audit failure; unexpected charge; or any authority expansion.

## Required durable artifacts

- authority, plan, residency/provider-admission policy and threat-model delta;
- typed request, response, policy-manifest, evidence and audit schemas;
- launcher, one-use broker and isolated payload;
- focused tests and repository-only verifier;
- pre-plan, rehydration, pre-attempt and pre-acceptance receipts;
- provider-free dry-run and occupied evidence;
- independent external audit analysis and Sol review;
- closeout, Continuity/Compass/handover/ledger updates; and
- a candid claim-limit statement.

No commit, push, pull request, protected-ref movement or external-worker
dispatch is authorised.
