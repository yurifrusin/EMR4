# DeepSeek native Harness preset-mount source-coordinate reconciliation rehearsal plan

Date: 2026-08-22

Timestamp: 2026-08-22T04:31:24.1598131+10:00 (Australia/Brisbane)

Status: **frozen before implementation**

Reasoning level: **Extra High** for a deterministic source-level narrowing of
an accepted provider-free native terminal.

## Objective

Bind the exact installed rc.7 preset-mount state machine, the two preset-row
plugins and their host-service prerequisites to a deterministic, typed source
reconciliation. Reduce `EFFECTIVE_TOOL_COMPOSITION_PRESET_MOUNT_FAILED` to the
narrowest finite internal candidate set supportable without reading or
retaining the discarded raw runtime exception.

This is a provider-free, read-only source rehearsal. It runs no native Harness
process and selects no repair.

## Frozen inputs

- Accepted safe-terminal evidence at candidate source
  `c66eb82cccd64961f0d99bf9f67803e1a69ebd8a`.
- Pinned `@deepseek-ai/dsh-agent-presets` `0.1.0-rc.7` mount source.
- Pinned `@deepseek-ai/dsh-tool-fs` and
  `@deepseek-ai/dsh-tool-fs-search` `0.1.0-rc.7` sources.
- Pinned `@deepseek-ai/dsh-base` composition declaring `tools`, `fs`,
  `systemPrompt` and `subprocess`.
- Exact `emr4-bounded-worker` rows and ordered effective-tool target `edit`,
  `glob`, `read`.

## Required output

The deterministic projection must:

1. verify every source hash and package version;
2. prove the admitted mount chain and plugin injection prerequisites;
3. reject already-eliminated root, roster, discovery, pre-factory import and
   publication coordinates;
4. enumerate only source-reachable internal candidates;
5. state explicitly that no exact runtime subcoordinate or repair is selected;
6. preserve zero native-process, turn, request, provider, target and product
   activity.

The accepted candidate vocabulary is:

- `PRESET_MOUNT_AGENT_SCOPE_ABSENT`
- `PRESET_MOUNT_COMPOSITION_STAMP_UNREADABLE`
- `PRESET_MOUNT_ROW_IMPORT_OR_APPLY_REJECTED`
- `PRESET_MOUNT_SUBTREE_PUBLICATION_ABSENT`
- `PRESET_MOUNT_ROW_INACTIVE_AFTER_AWAIT`
- `PRESET_MOUNT_ROOT_SERVICE_LEAK`

## Parallelism assessment

- DeepSeek lane: **declined**. The DeepSeek Harness package source is the object
  being reconciled; no native process or model turn is allowed.
- Gemini lane: **declined**. Exact local source hashes and finite control-flow
  coordinates are deterministic; no independent semantic ambiguity presently
  justifies a provider lane.
- Native-subagent lane: **declined**. Developer policy prohibits delegation,
  and the hash-bound source walk is serial.
- GPT Sol owns the deterministic implementation, verification and closeout.

## Acceptance and stop rule

Accept only a schema-closed finite candidate set derived from the exact pinned
source, with all input hashes and negative boundaries verified. Stop without a
repair if any source binding drifts or if the exact failure cannot be narrowed
without raw runtime detail. A later tranche may design one safe typed
subcoordinate probe; this plan authorises neither that probe nor another native
attempt.

## Explicit exclusions

No native process, worker turn, model/provider request, raw error recovery,
target edit, product/configuration/API/database/route/adapter/feature-flag/
allowlist/action-grammar/first-party-client/waiting-area change, ordinary-
practice enablement, generic-status `Arrived` change, patient/product/clinical/
historical/protected data, production runtime, deployment, release, Pages,
protected evidence or protected-ref movement is authorised.
