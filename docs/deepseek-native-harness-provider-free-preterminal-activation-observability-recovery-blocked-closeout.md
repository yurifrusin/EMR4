# DeepSeek native Harness provider-free preterminal activation observability recovery — blocked closeout

Date: 2026-08-20

Status: `blocked`; not accepted and not complete

Exact deterministic candidate:
`b5f0bc0d823a1c8009f3bb49efcc9a588b9703ab`

## Result

The provider-free recovery design itself is complete and passes. It preserves
`native-composition-attempt-001` byte-for-byte, gives every admitted
post-readiness preterminal path a finite safe coordinate, separates readiness
from activation evidence, and assigns process duration in a `finally` path
before termination and cleanup.

The published probe materialised exact cached rc.7 bytes once in offline mode
and ran one non-Harness Node module-import probe. It started zero native Harness
processes, sessions, turns, brokers, models or providers; made zero network,
Docker or database attempts; retained no raw logs or environment; and cleaned
the disposable root.

The candidate passes its 85-test review suite and a 168-test provider-free
owned, neighbouring, latch, clockwork and baton-consistency suite, plus Ruff,
compile and the deterministic ten-scenario check.

After the clockwork-authored blocked transition, its corrected compatible
post-publication suite passed 167/167 tests. The first blocked publication was
rolled back byte-exactly because two selected legacy tests cannot represent the
clockwork-supported `blocked` latch state; they were removed from the blocked
manifest, not changed or falsely reported as passes.

## Why acceptance remains closed

The first Gemini 3.7 Flash/high review returned `pass` against the exact clean
candidate and all ten commands exited zero, but the packet incorrectly said
that C03 contained 137 tests. It actually contains 85. Because Gemini did not
challenge that false assertion, Sol rejected the receipt as
`revision_required` and corrected only the packet count.

Three fresh corrected review transports then failed before any decision. Each
immutable failure receipt records exit code 1, no admitted review, unchanged
HEAD and a clean worktree. Antigravity's local diagnostic log safely localised
the terminal condition to `PERMISSION_DENIED (code 403): The caller does not
have permission`. `agy models` still passed, so catalogue liveness does not
substitute for inference authority. No fallback model was used and further
blind retries stopped after the third identical predecision failure.

The optional expanded gate also exposed an inherited, out-of-scope governance
budget mismatch: `AGENTS.md` is 121,619 bytes while
`tests/test_agents_acceptance_index.py` still caps it at 80,000. This candidate
did not create or alter that state, and the failure is not counted as a tranche
pass.

## Resume condition

Restore Antigravity inference permission for `gemini-3.7-flash-high`, then run
one fresh clean-worktree preflight and corrected independent veto against the
unchanged candidate. A clean pass may resume normal clockwork acceptance. It
must not rerun the consumed native composition attempt.

No product/API/configuration/client/flag/grammar/waiting-area change,
ordinary-practice enablement, data access, native Harness run, production,
deployment, release, Pages or protected-ref movement occurred. Local/origin
`master` and `handoff/current` remain
`2e34bdad732fdab32fbf778280b3d3c70d66d602`; all untracked files, including
`docs/branding/`, remain preserved.
