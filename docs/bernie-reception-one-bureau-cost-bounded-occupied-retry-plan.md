# Reception One Bureau cost-bounded occupied retry plan

Status: authorised and active
Recorded: 2026-07-31
Predecessor: `reception-one-bureau-explicit-selection-retry-readiness`

## Objective

Prove that the real Reception One Bureau can visibly select the exact
authored-synthetic appointment, invoke the existing isolated Gemini 2.5 Flash
planner through the exact Sydney Vertex lane, admit the returned typed plan
through the deterministic proofreader, and release only the existing
proposal-only update envelope.

The consumed failed-closed predecessor is immutable. This plan creates a fresh
continuity descendant, distinct attempt identifiers and distinct one-use
ledgers.

## Fresh authority

Yuri authorised:

- the next fresh retry;
- further fresh provider calls when they progress the tranche;
- a cumulative provider-cost ceiling of USD 1; and
- autonomous continuation after an admitted call through the next appropriate
  bounded workflows unless a substantial product-direction choice or an
  existing external-authority/safety boundary is reached.

An unchanged duplicate call does not progress the tranche and is prohibited.
No provider call may follow the first admitted proposal merely to accumulate
evidence.

## Frozen provider and data lane

Every occupied call must retain:

- provider: `google_vertex_ai`;
- model: `gemini-2.5-flash`;
- project: `bernie-emr4-dev`;
- service account:
  `emr4-bernie-ai-dev@bernie-emr4-dev.iam.gserviceaccount.com`;
- authentication: `keyless_impersonated_service_account_adc`;
- location: `australia-southeast1`;
- endpoint hostname:
  `australia-southeast1-aiplatform.googleapis.com`;
- authored-synthetic data only;
- the credential-free cell and one-use broker architecture;
- the deterministic proofreader as the sole egress gate;
- proposal-only release through the existing update adapter;
- no provider, model, project, identity, region or endpoint fallback;
- no API key, service-account key, provider tool, grounding, retrieval, cache
  creation, confirmation, write, production, deployment or release.

The cell receives no ADC, OAuth material, Google Cloud configuration or
service-account identity. API-key environment variables are not inspected and
are omitted from every owned child environment.

## Exact browser repair

Before submission, the non-intercepted browser must:

1. bind the exact disposable appointment through the existing test-only smoke
   parameter;
2. locate and visibly click its appointment row;
3. require `aria-selected=true`;
4. select `isolated_vertex`;
5. submit the authored-synthetic 45-minute resize; and
6. retain only a boolean proof that an appointment identifier was supplied,
   never the identifier itself.

## Pre-attempt projection overflow repair

Before the occupied call, Yuri identified that a result-heavy projection in
the accepted desktop evidence clipped later matching times behind the fixed
request bar. This active descendant therefore also requires:

- result-heavy projections to grow from the former 690 px desktop cap to the
  available viewport, bounded at 760 px;
- the result canvas, rather than the complete window or request bar, to own
  vertical overflow;
- a stable, visually styled scrollbar whenever results exceed the canvas;
- every matching result to remain reachable without covering the request bar;
  and
- zero provider calls, credential reads or database access during the focused
  layout acceptance.

The provider-free authored-synthetic acceptance rendered eleven matching times
at 1440 x 900 and 1024 x 650. It proved a 756 px desktop projection, internal
scroll availability, final-result reachability, a continuously visible
request bar and no horizontal overflow. The repair changes presentation only;
it adds no data, provider, model, proofreader, proposal, confirmation or write
authority.

## Cumulative cost contract

The official Google Cloud Vertex AI pricing page retrieved on 2026-07-31 lists
Gemini 2.5 Flash Standard at:

- USD 0.30 per one million text/image/video input tokens; and
- USD 2.50 per one million text output tokens, including response and
  reasoning.

Source:
`https://cloud.google.com/gemini-enterprise-agent-platform/generative-ai/pricing?hl=en`

Only HTTP 200 requests are token-billed according to that page. The immediately
preceding HTTP 200 call is included in this descendant's conservative
cumulative budget:

- input: 4,633 tokens;
- response: 147 tokens;
- reasoning: 819 tokens; and
- estimated cost: USD 0.0038049.

Before every fresh call, the cost gate reserves USD 0.02. This exceeds the
computed bounded-request maximum using 16,384 input tokens and the frozen 3,072
maximum output-token setting:

`(16384 * 0.30 + 3072 * 2.50) / 1,000,000 = USD 0.0125952`.

The call is forbidden unless:

`accounted_cost + outstanding_reservations + 0.02 <= 1.00`.

After a completed call, the reservation is consumed and the ledger records the
sanitized provider usage and rate-derived cost. Any absent, malformed or
over-limit usage fails closed. The cumulative cost ledger is hash chained and
must include every opened attempt.

## Diagnose-repair-rerun rule

Each route dialogue has an absolute one-call ceiling. If a call fails:

1. consume and independently audit its one-use ledger;
2. classify the bounded error or proofreader finding;
3. make only a repository-local repair that preserves every frozen boundary;
4. add a focused regression test;
5. rerun provider-blocked, isolation, preflight, cost and residue gates; and
6. create distinct attempt, route, audit and ledger identifiers before a fresh
   progress-bearing call.

Stop rather than repair when progress would require a provider/model/project/
identity/region change, IAM or ADC reconfiguration, API enablement, billing
change, broader data, weakened residency assurance, higher cost, weakened
isolation/audit, confirmation/write authority or any other externally material
change.

## Acceptance

Pass only if one fresh occupied attempt proves:

- exact browser appointment selection before submission;
- one non-intercepted authenticated compose request;
- exact frozen Vertex binding and HTTP 200;
- consumed one-use ledger and valid durable hash chains;
- deterministic proofreader admission of `resize`;
- 45-minute proposal-only update adapter release;
- rendered bounded provenance;
- no fallback, confirmation or write;
- unchanged database truth;
- actual cumulative estimated provider cost below USD 1;
- no call after admission; and
- complete independent container, image, network, credential, process,
  temporary-directory, port and database residue cleanup.

The evidence may prove only the configured and observed Sydney locational
request path. It may not claim Australian physical or sovereign processing,
production suitability, general model reliability or safety for real patient,
health, clinical, product-derived or historical data.

## After success

Close and audit this node first. Then continue autonomously through the next
bounded provider-free or already-authorised product/runtime workflows. Any
substantial product-direction choice, new data class, new provider surface,
write/confirmation authority, external participant activity, voice, Word,
production, deployment or release remains a fresh Yuri decision.
