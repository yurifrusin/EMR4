# Ariadne agent error and correction register — revision 391

Date: 2026-08-18

Timestamp: 2026-08-18T14:27:15.6592213+10:00 (Australia/Brisbane)

Status: accepted correction and containment

## Revision

Revision 391 adds AER-0439 through AER-0445 from the tiny native DeepSeek
Harness traceability rehearsal. The package-resolution, output-bounding,
Windows search-scope and pre-dispatch workspace-classification errors were
corrected before provider-capable execution. Static config admission then
missed a stricter adapter runtime invariant: an empty `retryableCodes` list is
rejected even when `maxRetries` is zero. The sole provider-capable process
therefore exited during plugin-tree load before provider I/O. It was contained
without retry or fallback and accurately reduced to a bounded no-call result.

The latch full-SHA draft and direct cleanup-command rejection were corrected
before closeout. Exact `git rev-parse` supplied the source object, and the
verified disposable directory was removed through the recoverable Windows
Recycle Bin path with an independent absence readback.

No product source, accepted result, provider request, credential, deployment,
Pages action or protected ref changed.

## Population

- incidents: 445;
- corrected or explicitly contained: 445;
- open: 0;
- latest id: `AER-0445`.
