# DeepSeek native Harness agent-creation boundary prelaunch contract-family recovery

Date: 2026-08-22

Timestamp: 2026-08-22T02:30:41.4443432+10:00 (Australia/Brisbane)

Status: **frozen before correction or native launch**

## Observed stop

The first controller invocation rejected during disposable preparation, before
the native `@deepseek-ai/dsh@0.1.0-rc.7` process boundary. Preset construction
received the accepted rebinding contract rather than the accepted effective-
tool composition contract required by the preset builder. The two contracts are
individually valid but intentionally have different schemas, so the controller
failed closed instead of inventing a preset.

Post-stop readback proved:

- native Harness processes: `0`;
- native attempt 001 consumed: `false`;
- canonical attempt outputs: `0`;
- matching disposable roots retained: `0`;
- target creation/use: `false / false`;
- retry, resume and alternate transport: `0 / 0 / 0`; and
- tracked source remained clean with every untracked file preserved.

No raw exception message, stack, code path, stream, session, prompt, response or
credential is retained in the typed failure artifact.

## Narrow recovery

The controller will add one deterministic preset-payload binding function that:

1. loads the accepted effective-tool composition contract through its owning
   module;
2. requires exact preset id, exact ordered tools, exact byte count and exact
   digest agreement with the current agent-creation contract;
3. builds the preset only from that owner contract;
4. runs inside `deterministic_check()` before a disposable root is created; and
5. supplies those already checked bytes to execution without a second contract
   selection.

The focused test will prove the owner contract produces the exact frozen preset
and that both deterministic checking and execution consume the one guarded
payload. The implementation and focused-test digests in the typed contract must
be rebound after the repair.

This is a prelaunch controller correction, not a retry of the native attempt.
After the corrected source is tested, committed and pushed, a new five-source
pre-execution receipt and exact clean-source readback are required before the
still-unconsumed attempt 001 may launch once.

## Parallelism and authority

- DeepSeek is declined because its native factory boundary remains unproven.
- Gemini is declined because the two local typed contract shapes determine the
  repair without semantic ambiguity; reassess on typed terminal ambiguity.
- Native subagents are declined because developer policy prohibits delegation
  and this contract handoff is serial.
- GPT Sol owns the correction, deterministic verification, publication and the
  later single native admission.

The recovery grants no published agent/session, turn, worker, broker/model/
provider request, target use, product/configuration/API/database/route/adapter/
flag/allowlist/grammar/client/waiting-area change, ordinary-practice or generic
`Arrived` change, data, production, deployment, release, Pages, protected
evidence or protected-ref movement.
