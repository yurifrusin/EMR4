# Canonical check-in reference-only operational-evidence conformance packet rehearsal — closeout

Date: 2026-08-23

Timestamp: 2026-08-23T15:18:50.3859558+10:00 (Australia/Brisbane)

Status: `accepted_pending_clockwork_publication`

Exact reviewed product source:
`904c626e88af7cd947241eae73d987e9418f1637`

## Lay outcome

The last repository-only check-in prerequisite has passed. One wholly
synthetic packet travelled through the closed environment-manifest reader,
the typed evidence reader, the evidence gate and the still-disabled admission
decision. The evidence gate accepted the internally consistent references,
but ordinary-practice check-in remained denied because activation is closed.

Twelve deliberately hostile variants were also denied. No real environment,
credential, patient, appointment or operational fact was used or created.
The readiness clock therefore remains 11 satisfied, zero blocking and one
operational-evidence gap: the software-side prerequisites are complete, but
real operational evidence and Yuri-owned choices are still absent.

## Technical result

- the immutable packet is `authored_synthetic_opaque_reference_only` and its
  canonical SHA-256 is
  `c81a38d8d1892a7f02574a9f295a1ca507407dc637acb44e43736ac0f1532571`;
- the canonical path produced `manifest_normalized`,
  `evidence_inputs_normalized` and `evidence_gate_satisfied`, then denied at
  `ordinary_activation_closed`;
- all 12 hostile cases produced their frozen fail-closed reasons;
- ordinary-admission releases, external facts, human choices, provider calls
  and product-data records all remained zero;
- focused tests passed 11/11, and the surrounding manifest, evidence, gate,
  admission and API-spine suites passed;
- JSON Schema, Ruff, byte compilation and byte-for-byte evidence reproduction
  passed; and
- no product source, route, API, client, runtime, feature flag, allowlist,
  status, waiting-area, deployment, Pages or protected ref changed.

Nine bounded workflow incidents were corrected without changing the accepted
candidate: one wrong script invocation form, one forbidden manually repeated
Git object in receipt prose, one copied schema-vocabulary value, one guessed
test filename and one descriptive Continuity node kind outside the closed
vocabulary. A second dry check showed that the nominally valid `integration`
kind activates two unrelated inherited availability contracts, so the packet
was classified as provider-free `tooling`. Postpublication verification then
found a missing required Brisbane timestamp in the Yuri summary and restored
the previous generation byte-exactly. Two further CLI-shape lapses—an
unsupported register `--check` flag and passing `--intent` to rollback—were
corrected. They are retained as ergonomics evidence rather than hidden as
incidental noise.

## Parallelism and Harness posture

DeepSeek was declined because this tranche was provider-free and the pinned
native rc.7 occupied profile remains paused after its pre-provider HMR startup
failure. Claude Code was not used as a silent fallback. Gemini was declined
after the exact deterministic packet, hostile matrix and surrounding suites
passed without a semantic fork. Native subagents were declined under the
active serial policy. GPT Sol owned the exact packet and acceptance.

## Next tranche

Proceed under standing authority with
`ariadne-provider-free-read-only-governance-clockwork-ergonomics-and-efficacy-review`.
It will take a measured reading of the clockwork and DeepSeek-gear period,
identify duplicated or manually supplied facts, rejected ticks and recovery
sequences, and rank only changes that eliminate at least one operator choice,
duplicate field or rerun sequence.

That review may propose a narrow later implementation plan. It may not alter
the live clockwork, launch a Harness or provider, reopen check-in operational
choices, enable ordinary practice, use product data, or move protected refs.
