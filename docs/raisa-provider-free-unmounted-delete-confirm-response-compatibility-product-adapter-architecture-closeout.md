# Provider-free unmounted delete-confirm response-compatibility and product-adapter architecture closeout

Date: 2026-08-16

Timestamp: 2026-08-16T18:12:23.8651539+10:00 (Australia/Brisbane)

Result: `raisa_provider_free_unmounted_delete_confirm_response_compatibility_product_adapter_architecture_pass`

Exact independently reviewed candidate: `9f0c166be2276d4e236dbdb4ed5657074ffbd0aa`

Exact reviewed tree: `5216361f49dbfd317751872e905ccd7b49b4786b`

Semantic freeze source: `5aaed2a859c64062d40dd2fe1b419d48dcc5d821`

Corrected mechanical proof source: `9ddda9f53dfda8aea226550f7261271b0c1cd426`

## Accepted outcome

Delete-confirm now has a complete off-route response and product-adapter
architecture. The accepted six-field private canonical receipt remains the
sole persisted command truth. Initial delivery and replay produce the same
versioned, minimal public success envelope by a pure deterministic projection
from those exact validated bytes. No later appointment read, mutable
`AppointmentOut` reconstruction or additional patient/practitioner/schedule
persistence is permitted.

The adapter boundary derives practice, actor, role, positive authority
generation and minimized session identity from authenticated server state. It
accepts no client-selected role, generation, session or capability. Opaque
proposal generation, signed evidence, freshness, warnings and delete state are
checked before the command session and rebuilt from the locked target before
any effect. The accepted physical seam alone owns the exact
`appointment.cancel.confirm` capability, current-authority rechecks and atomic
appointment/audit/private-receipt write set.

Raw `DELETE /appointments/{id}` remains a separate legacy ingress. It cannot
inherit the dedicated adapter, capability, receipt, idempotency or replay
contract. Any later canonical and compatibility proposal-confirm aliases must
share one handler and the same public v1 envelope.

## Evidence

- fourteen strict-UTF-8 canonical-LF source bindings and four semantic-output
  digests match;
- both closed JSON schemas validate;
- 136 contract hostile mutations and 20 evidence hostile mutations fail
  closed;
- exact private bytes reject reordered keys, extra whitespace, CRLF, duplicate
  keys and alternate escaping;
- the replacement provider-free canonical-static profile passes 191 tests;
- the provider-free focused architecture/API/Ariadne population passes 424
  tests;
- Ruff, deterministic validation, whitespace and compilation of 214 maintained
  Python sources pass;
- the current host lacks exact Python 3.11, so no exact target-runtime result is
  claimed; and
- the fresh Gemini 3.7 Flash/high review executes all six admitted commands,
  returns exactly one `pass` and leaves HEAD, tree and worktree unchanged and
  clean.

## Worker correction and harness learning

The first DeepSeek proof candidate is preserved. It accepted semantically
equivalent reordered or whitespace-altered private JSON, which contradicted
the frozen physical-byte authority. The single plan-authorised mechanical
correction reconstructs the exact compact six-field insertion-order bytes and
rejects those alternatives before projection.

AER-0358 separately records that three earlier Sol verification sessions used
ordinary repository pytest entry points and therefore loaded the autouse local
`gp_pms_test` fixture despite this tranche's no-database boundary. No product
database or product/patient/clinical data was accessed and teardown completed.
The accepted replacement control, `scripts/ariadne_provider_free_pytest.py`,
forces `--noconftest`, disables plugin/cache loading, clears inherited options,
omits database and credential environment, restricts test paths and uses
`shell=False`. All accepted counts above come from the provider-free
replacement profiles.

## Parallelism outcome

The planned worker mix was effective. Sol froze and reconciled the material
authority and response semantics; DeepSeek produced the separable mechanical
proof and completed one bounded correction; Gemini supplied the required fresh
independent veto. Native subagents remained declined under the developer
constraint. No further worker or verifier call is required.

## Scope retained closed

No product source, schema or route was edited, mounted or called. No HTTP
transport, application runtime, database, Docker, SQL, migration, product
command, capability provisioning, product/patient/clinical data, protected
evidence, provider, ADC, credential, IAM, browser, network, UI, deployment,
production, release, Pages or protected ref was opened. Raw DELETE remains
isolated. `docs/branding/` and all unrelated untracked paths remain preserved.

## Next tranche

Proceed under standing uninterrupted-development authority to one separately
frozen provider-free unmounted delete-confirm composition and product-adapter
implementation tranche. It may implement only the pure public-envelope
projection, server-owned ingress packet, proposal/locked re-admission
composition and closed outcome mapping against the accepted physical seam. It
still may not edit, mount or call a route, change a schema, execute a database,
provision capability, open product data or perform provider/runtime work.
