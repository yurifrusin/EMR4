# Raisa local-only historical Diary leading explicit time-token bounded measurement rehearsal — plan

Date: 2026-08-24

Timestamp: 2026-08-24T08:25:04.8967052+10:00 (Australia/Brisbane)

Status: `frozen_fail_closed_plan`

Operation: `raisa-local-only-historical-diary-leading-explicit-time-token-bounded-measurement-rehearsal`

Planning source HEAD: `adf444b68053d34ba07d909294967aff8511f718`

Reasoning level: Extra High. This tranche authorizes one narrowly bounded local
read of potentially private historical Diary documents. Scope, parser meaning,
one-run finality, containment, retention and first-use denial are frozen before
metadata binding or content access.

## Objective and inherited evidence

Measure exactly once whether strict leading explicit time tokens recover useful
same-segment scheduling time from the already approved fixed 80-snapshot local
slice.

The accepted parser recovery at exact reviewed source
`5a3c589873a104e948e65eaadacd2397f0621a3b` proved:

- character-zero complete-time parsing after structural cell segmentation;
- ASCII space/tab/hyphen boundary and non-empty payload requirements;
- invalid, embedded, attached, date-like, phone and email/contact denial;
- same-segment direct mapping with no forward fill;
- removal of the token and separator before normalized occupancy and HMAC
  construction;
- unchanged coordinate fallback; and
- first-class explicit-source admission without weakening distinct-minute,
  interval, mapping-ratio, stable-linkage, adjacent-change or leakage gates.

All 219 provider-free controls passed without historical access. This evidence
justifies one measurement; it does not predict a positive result.

## Exact source and fresh attempt

The only source root is:

`local_data/historical-diary-trove/raw/pilot_01`

The only new ignored attempt root is:

`local_data/historical-diary-trove/measured-probes/2026-08-24-leading-token-v3`

The source remains one leaf root, one dense day, non-recursive and read-only.
Selection remains the first 80 chronological regular non-reparse files, capped
at 8 MiB each and 128 MiB total. Symlink/reparse traversal, a second root or
day, broader enumeration and any source mutation are forbidden.

The consumed `story-coordinate-v1` and `story-coordinate-v2` attempts are not
inputs and may not be read, resumed, copied, retried or removed. The literal
`v3` root, its manifest, terminal and aggregate must be absent before Phase A;
unexpected state blocks rather than being reused or deleted.

## Frozen parser and projection

The accepted strict leading parser and all compiled grammar, separator,
contact/date denial, same-segment, token-removal and no-forward-fill behavior
are byte-bound before Phase A. The existing same-page coordinate mapper remains
an unchanged fallback only for segments without a valid leading token.

Story anchors and valid leading tokens are the only explicit time-source forms.
Their combined observation count must be at least three. At least three
distinct mapped minutes, at least 25% mapped structural occupancy, a positive
deterministic interval mode, nonzero stable linkage and adjacent changes, zero
source leakage and complete cleanup remain independently mandatory for a local
candidate. No row, column, ordinal, opening-hour, filename, timestamp or
expected-interval fallback exists.

No parser, threshold, source selection, output, cleanup or decision rule may
change after Phase A begins.

## Deterministic pre-access admission

Before creating `v3`, all 219 or later historical-Diary provider-free controls
must pass, including the 29 leading-token controls and all inherited selection,
schema, time mapping, privacy, stable-linkage, motion, Word containment,
progress and cleanup controls.

Additional source checks must prove:

- Python and the historical PowerShell profile bind only the literal `v3`
  attempt root and contain no active `v2` binding;
- the prior v2 plan and contract remain immutable historical evidence;
- the new root and all four public/generic output names are absent;
- the historical root is unchanged, one leaf and non-reparse;
- the parser and extractor digests are bound before access;
- no provider, network, model, product, database, client or raw logging import
  is introduced; and
- Ruff, compileall, PowerShell parsing, JSON validation, source-boundary scans
  and `git diff --check` pass.

Any deterministic failure prevents Phase A.

## Phase A — one metadata-only bind

Phase A may run exactly once. It creates only the new ignored `v3` root and
binds the fixed 80 regular files, byte caps, source day, controller/extractor
digests and chronological offsets without opening content. Strict readback must
prove the exact source/attempt roots, selection, caps, absence of reparses and
zero archive content reads before Phase B.

A failed or ambiguous bind closes the tranche. There is no second bind.

## Phase B — one content run

After Phase A passes, Phase B may run exactly once. It writes the one-way
content terminal before launch, opens only the bound files in one owned
invisible read-only Word process and gives the child 1,800 seconds.

Count-only progress contains aggregate document, table-cell, structural-
segment, coordinate-attempt and explicit-story-anchor counts plus existing
coarse elapsed/rate buckets. It contains no document identity, path, exact
timestamp, text, time value, page, coordinate, distance, token, key or mapping.

The Python parent performs exact PID/class/start-time cleanup on timeout,
abnormal exit or invalid output. Broad process-name stopping is forbidden and
the user's pre-existing Word process set must remain untouched. Pass,
revision-required, timeout and failure are all terminal. No second content run
is authorized.

## Decision, retention and first use

`locally_restricted_candidate` requires every frozen utility, privacy,
containment and cleanup gate to pass. `revision_required` applies when the sole
run is contained and cleaned but leading-token or overall utility evidence is
insufficient. `blocked` applies to any root, binding, digest, schema, raw
output, leakage, process, cleanup or authority failure.

Only generic aggregate, safe binding, cleanup and content-terminal readings may
remain under the ignored `v3` root. Private manifest, projection, process
control, progress, ephemeral keys, HMAC mappings, times, paths and coordinates
must be absent after every terminal result.

The first-use gate remains `closed_pending_candidate_specific_evaluation`.
Even a passing measurement cannot create or retain a reusable historical-
derived scenario, fixture, benchmark, replay, corpus, memory/RAG input or
product test. A later candidate-specific gate becomes useful only if this run
supports a particular minimized derivative worth promoting.

## Parallelism assessment

- **DeepSeek:** declined with negative leverage. The native Harness remains
  paused, Claude Code is not a silent fallback, and no external worker may
  receive private historical text or the one-run terminal.
- **Gemini:** not applicable with neutral leverage. Provider/model transmission
  is forbidden and the decision is deterministic and local.
- **Native subagents:** declined with negative leverage. Bind, Word process,
  count-only progress, ephemeral projection and terminal form one indivisible
  private state.
- **GPT Sol:** owns planning, deterministic admission, literal-root update, the
  single bind, single content run, cleanup, acceptance, Git and closeout.

Reassess only on deterministic failure, before any worker dispatch, and at the
next named tranche boundary.

## Closed surfaces

No second root/day, recursive or reparse access, cap increase, source mutation,
prior-attempt reuse, second bind or content retry; no raw text, identity, note,
contact value, filename, exact timestamp, time token, page, coordinate,
distance, key or mapping in Git/stdout/conversation; no provider, network,
model, telemetry, clipboard or external release; no first-use admission; no
product runtime, route, API, client, database or configuration; no ordinary-
practice activation; and no production, deployment, release, Pages, protected
evidence or protected-ref movement. Local/origin `master` and
`handoff/current` remain exactly
`2e34bdad732fdab32fbf778280b3d3c70d66d602`. Preserve `docs/branding/` and
all unrelated untracked files. Stage explicit paths only.
