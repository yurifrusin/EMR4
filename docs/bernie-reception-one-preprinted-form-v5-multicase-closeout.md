# Reception One Pre-printed Form v5 Multi-case Evaluation Closeout

Status: passed
Recorded: 2026-07-30
Result: `reception_one_preprinted_form_v5_multicase_occupied_pass`

## Outcome

The frozen untaught v5 form passed its six-case authored-synthetic occupied
cohort. Create, resize, cancel, status change and squeeze-in assessment were
all admitted by the unchanged deterministic proofreader on their primary
calls. Each released only its expected typed in-memory result and recorded
`write_performed: false`.

The deliberately ambiguous request did not become a proposal. Its primary form
was exact JSON but used an invalid operator arity, so the proofreader issued
the one closed correction ticket. The complete replacement was admitted and
released only `kind: clarification`, with no API Spine operation, no
confirmation claim and no write.

Seven calls were made in total. All returned HTTP 200 through the exact
`australia-southeast1-aiplatform.googleapis.com` path using `gemini-2.5-flash`,
project `bernie-emr4-dev`, the exact Bernie service account and keyless
impersonated ADC. The calls used 15,642 prompt, 1,821 candidate and 17,463 total
tokens with 12,540 ms aggregate provider latency. At Google's published
Gemini 2.5 Flash token prices, that is an application estimate of USD
0.0034389, not an observed invoice charge, and is below the USD 1 ceiling.

## Coordinator repair

After the already successful create lifecycle closed, the batch coordinator
failed while reading its external audit because the audit renderer recognised
the historical v5 attempt identifiers but not the new case-bound identifiers.
The create provider call, typed result, proofreader admission, ledger and
cleanup were all valid.

The repository-local repair added the case-bound identifier pattern to the
audit renderer and allowed the coordinator to resume only from a fully closed
case with exact attempt, ledger and parent-chain binding. It regenerated the
bounded external audit from immutable turn evidence and the durable event
chain. It did not replay the create call, reopen its ledger or change the
prompt, provider schema, proofreader, frame, model, project, identity, region,
cost ceiling or fallback boundary.

## Training conclusion

Do not add prompt-context teaching yet.

Five of six primary forms were admitted directly. The sole correctable error,
`operator_arity_invalid`, occurred only in the ambiguous case and was repaired
by the already authorised proofreader dialogue. It therefore fails the
predeclared threshold of the same correctable error recurring in at least two
cases. The correct next action is to diagnose and expand evaluation coverage,
not teach against one observation.

This does not rule out prompt examples later. It establishes that the current
untaught form can already produce the five tested action families and can use
one bounded correction dialogue to reach a safe clarification.

## Audit and cleanup

All seven one-use ledgers are consumed. Seven turn hash chains and six parent
dialogue chains validate. Every external audit records the exact model,
project, service account, authentication class, regional hostname, request and
schema hashes, typed form, bounded operator note, proofreader result, release,
latency, token usage and cleanup without retaining raw prompts, raw provider
responses, credentials, tokens, API-key information or chain-of-thought.

All task-owned containers, networks, images, broker processes, temporary
tokens and temporary contexts are absent. No daemon-wide prune, provider or
regional fallback, product/database access, appointment confirmation or write,
human/product delivery or external cloud-state mutation occurred.

## Verification

- 129 focused and inherited typed-language/Continuity/Compass tests passed;
- 110 relevant API Spine and appointment-proposal tests passed;
- the fixed repository-only Ariadne verifier passed 266 tests;
- the unchanged two-cell real-isolation proof passed;
- Python compilation and focused Ruff checks passed;
- Continuity, Compass and exact rendered-report validation passed;
- JSON and schema validation passed;
- `git diff --check` passed; and
- independent post-run container, network, image, credential and process
  residue checks passed.

## Candid claim boundary

This is evidence for six frozen authored-synthetic form tasks under one typed
language, one deterministic proofreader and the configured and observed Sydney
Vertex locational request path. It is not evidence of Australian physical or
sovereign processing, exhaustive natural-language coverage, receptionist
usability, production fitness or safety for real, product-derived, patient,
health, clinical, protected or historical data.

It opens no product or database integration, appointment write, representative
staff study, voice, Word, production, deployment or release authority.
