# Ariadne S10-S12 Terra Executor Pilot

Status: active bounded pilot

Sol remains at the S10 tranche-entry gate and the final S12 acceptance gate.
GPT 5.6 Terra/high handles routine intra-tranche execution after each sprint is
defined and allocated by the Conductor. Terra may dispatch allocated workers,
request same-lane corrections, run deterministic gates, and integrate accepted
commits onto a staging branch.

Terra cannot define or reallocate sprint work, change acceptance criteria, or
integrate and push protected `master`. Those boundaries prevent economy from
silently changing product direction or executive authority.

The pilot escalates to Sol for scope or authority changes, ambiguous product
policy, repeated worker defects, security/write/deployment/release boundaries,
conflicting evidence, and protected-master integration. Luna is recorded as a
future mechanical receipt coordinator but is disabled for this first pilot.

The closeout records Sol interventions, retries, invalid integrations,
escalations, correction commits, and duplicated-context events. The pilot is
successful only if it reduces gate-owner involvement without increasing final
correction work.
