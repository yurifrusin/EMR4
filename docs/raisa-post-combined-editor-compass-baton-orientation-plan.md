# Post-combined-editor Compass and baton orientation plan

Date: 2026-08-15

Timestamp: 2026-08-15T05:47:03+10:00 (Australia/Brisbane)

Status: `frozen_for_read_only_execution`

Task baseline: `aa2b34573d47e0a81ae689cb20b0461b3585c221`

Target result: `raisa_post_combined_editor_compass_baton_orientation_pass`

Reasoning level: Extra High. The accepted combined editor completes the first
visible Reception One status and update-family set. Selecting what follows is a
programme and authority decision, while this tranche itself remains read-only.

## Objective

Reconcile the accepted Reception One command families with the Context Fabric
and durable-cue horizon, the channel-neutral patient foundation and every
remaining Yuri-owned gate. Select one narrow dependency-satisfied successor,
or identify the exact genuine user-attention fork if all worthwhile outward
directions require a separately recorded choice.

The orientation must preserve the distinction between:

- authority delegated to a future patient channel or assistant, which is
  narrow, expiring and revocable for future acts;
- a single-use confirmation challenge for one proposed command; and
- a command already committed to Diary truth, which can be reversed only by a
  separately authorised cancellation or rescheduling command.

## Exact evidence sources

Read only:

1. the live Current Baton, accepted combined-editor plan, closeout, Sol
   acceptance and terminal active-operation latch;
2. the current Continuity graph, Compass current position, decision horizon,
   user-owned decisions and map limits;
3. the accepted Context Fabric direction, including source-owned truth and the
   optional Durable Event and Cue Delivery extension;
4. the accepted channel-neutral patient-interaction architecture, especially
   identity, assurance, delegation, recovery and revocation boundaries;
5. the current API Spine appointment command inventory and the native Diary /
   Reception One bridge and presentation source; and
6. the current Phase 2/2B/3 programme direction in `implementation_plan.md`.

Protected holdouts, historical Diary/PHI, live source/database state, patient
or product data, provider output and external channel state are ineligible.

## Candidate comparison

Compare at least:

1. a Reception One cancellation/delete-family direction, beginning with a
   provider-free read-only command-path readiness review before any destructive
   UI composition;
2. Reception One check-in/waiting-area composition;
3. a first external patient channel, identity or delegated-assistant
   revocation descendant;
4. representative Stage 3B sessions;
5. another typed Diary event family;
6. operational durable-cue delivery or return to restart/unknown-commit work;
   and
7. visual polish that adds no semantic capability.

Classify each `dependency_satisfied`, `user_decision`, `human_action`,
`authority_closed` or `lower_leverage`. Do not manufacture a dependency-
satisfied result merely to avoid asking Yuri a material question.

## Selection rules

The successor may be selected without a user pause only if it:

1. is already explicitly authorised or is a fully specified descendant of an
   accepted family;
2. introduces no new command, event, identity, channel, participant, product-
   data, database/source or provider authority;
3. has meaningful product or assurance value, not ceremonial documentation;
4. preserves source-owned truth, command-time current-authority checks,
   proposal/confirmation separation, idempotency, audit and fresh readback; and
5. is narrow enough to freeze fail-closed as one tranche.

If no candidate satisfies all five rules, the orientation must make the fork
small: name the recommendation, explain its first safe tranche and retain the
other horizons without activating them.

## Acceptance

The tranche passes only if it:

1. inventories the exact current Reception One command and proposal reach;
2. identifies any unsafe or unresolved seam that must precede the recommended
   command-family composition;
3. distinguishes revoking delegated future authority from reversing a
   committed appointment;
4. compares every candidate against current dependencies and Yuri-owned gates;
5. selects exactly one successor or reports a genuine user-attention fork;
6. changes no product, API, database, event, provider or runtime source;
7. passes focused static orientation, latch, baton, Compass and canonical fast
   checks; and
8. receives one fresh Gemini 3.6 Flash/high read-only exact-candidate veto
   after deterministic admission.

## Parallelism-efficacy allocation

- **DeepSeek V4 Flash/high — declined:** successor selection is coupled
  programme judgment and offers no stable mechanical package.
- **Gemini 3.6 Flash/high — reserved:** one fresh read-only exact-candidate veto
  will challenge the final classification and authority boundary.
- **Native subagents — declined:** no concrete independent package is needed;
  the evidence and decision are tightly coupled.
- **Sol — serial authority owner:** evidence reconciliation, plan, candidate,
  acceptance, continuity and Git.

Reassess at candidate freeze, pre-verifier admission and closeout.

## Stop and recovery

Correct citations, stale repository facts and classification wording inside
this read-only scope. Stop for Yuri if the remaining useful paths require
materially different command, identity/channel, participant, event or runtime
authority. Do not open one by analogy with the status or update families.

## Closed surfaces

No product behavior, FastAPI, GraphQL, OpenAPI, database, source, watcher,
event/runtime persistence, UI implementation, patient channel, identity,
delegation/revocation runtime, provider/ADC, credential/IAM/network, command or
write, patient/product/clinical data, protected evidence, deployment,
production, release, Pages or protected-ref action is authorised. Preserve
`docs/branding/` and every unrelated untracked file; use explicit-path staging
only.
