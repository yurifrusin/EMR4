# Provider-free check-in relay-free recovery attempt 007 plan

Date: 2026-08-23

Timestamp: 2026-08-23T03:08:47.1921091+10:00 (Australia/Brisbane)

Status: `frozen`

Planning source HEAD:
`f30b82ea0b80bdef2fa8d63549ba78d39d14e24d`

Accepted attempt-006 occupied source:
`a9567be36c82bc6d2eebc2488b48cd8bfb9f8d23`

Accepted attempt-006 closeout source:
`53760513c42a380904136eb4ef2f5ffda397e820`

Accepted start-argv repair implementation source:
`022d780726c74cb285d5b626cd004821b4e5ff47`

Accepted start-argv repair reviewed source:
`8814d4b5d62885f8f8eca4cf02fe5a49ccdc013b`

Accepted start-argv repair closeout source:
`f30b82ea0b80bdef2fa8d63549ba78d39d14e24d`

Protected source:
`2e34bdad732fdab32fbf778280b3d3c70d66d602`

Operation:
`raisa-provider-free-disposable-postgresql-default-off-check-in-relay-free-rollback-unknown-commit-recovery-attempt-007`

Target result:
`raisa_provider_free_check_in_relay_free_recovery_attempt_007_pass`

Reasoning level: Extra High freezes the seventh one-run database authority,
consumed negative lineage, repaired start/attach vector and exact cleanup
boundary. High is sufficient for deterministic construction, the sole occupied
execution and clockwork closeout while this plan remains unchanged.

## Authority and objective

Yuri's standing uninterrupted-development authority admits this separately
named descendant only after the accepted attempt-006 failed-closed terminal,
the exact read-only failure-coordinate diagnosis and the accepted one-token
start-argv repair. It authorises deterministic construction first and, only
after a separately published preexecution checkpoint, exactly one local
provider-free disposable PostgreSQL 16 execution.

The sole occupied execution must prove in one collision-free authored-synthetic
environment that:

1. one effect/receipt/audit packet is staged and explicitly rolled back, after
   which fresh restricted-role readback observes zero members;
2. a disjoint packet commits once and the caller loses its complete terminal
   response through the accepted relay-free path;
3. the caller releases neither success nor retry;
4. fresh restricted-role authoritative readback observes one exact consistent
   packet, zero duplicates and zero cross-practice visibility; and
5. the ephemeral role, attachment, sidecars, server, network and every matching
   labelled resource are exactly absent at close.

The first database-process start consumes attempt 007 regardless of pass,
failure, interruption or unexpected terminal form. This plan has no retry,
resume or fallback path. Any later occupied run requires a newly named
operation, namespace, plan and checkpoint.

## Immutable lineage and exact inputs

Attempts 001 through 006 are consumed and immutable. Attempt 007 binds the
attempt-006 terminal pair byte-for-byte:

| SHA-256 | Exact source |
|---|---|
| `3c7049b318fffb28aa70e8b4346f1ed857b7cf34e1780eec21373935f6c88efd` | `orchestration/continuity/raisa-provider-free-check-in-relay-free-recovery-attempt-006/rehearsal-failure-evidence.json` |
| `52470c6c6245f0988dd4f580e68f7a0e21ce5b8636e60119091c089d603bde1c` | `orchestration/continuity/raisa-provider-free-check-in-relay-free-recovery-attempt-006/attempt-006-execution-envelope.json` |

The executable base harness remains byte-for-byte unchanged at exact SHA-256
`1b7ec51cfd97fa6a54398ab0587acf79d3b0b8d34fa5609a2bad2abe17e91c16`:
`scripts/raisa_provider_free_disposable_postgresql_default_off_check_in_relay_free_rollback_unknown_commit_recovery_rehearsal.py`.
Its exact start vector is
`<executable> start --attach --interactive <container_id>`.

The accepted start-argv repair inputs are exact SHA-256:

| SHA-256 | Exact source |
|---|---|
| `73d5773d3662509ec2cdb8d8f109651b77ef79be42f5b641f07e36d7ca8bcf91` | `orchestration/continuity/raisa-provider-free-check-in-server-start-argv-sig-proxy-removal-conformance-repair/repair-attestation.json` |
| `de9106afdd69db62eaaf6888ba780e65596838c162f2f1db5cc2703b893bf8d7` | `orchestration/continuity/raisa-provider-free-check-in-server-start-argv-sig-proxy-removal-conformance-repair/contract.json` |
| `35afc06958477556f9d0689a99fad26babb8e15fb268d90b1cb983321ee9fee4` | `orchestration/continuity/raisa-provider-free-check-in-server-start-argv-sig-proxy-removal-conformance-repair/repair-report.md` |

The base transaction contract remains exact SHA-256
`bed2a89a3814ba9e9ac006d0fdb0c68d204fec53d8c21b6128190605b6ad9ec2`:
`orchestration/continuity/raisa-provider-free-disposable-postgresql-default-off-check-in-relay-free-rollback-unknown-commit-recovery-rehearsal/contract.json`.
The transaction attestation schema remains exact SHA-256
`d2c186b0d30419e0459d93d92af1f84907125becdeb75c7e1890dce597d3e72c`:
`orchestration/continuity/raisa-provider-free-disposable-postgresql-default-off-check-in-relay-free-rollback-unknown-commit-recovery-rehearsal/transaction-attestation.schema.json`.

Every Git binding must be a machine-resolved full 40-character commit object
and an ancestor of the executing candidate. Git readings are generated by the
receipt or wrapper; prose evidence fields may not manually repeat them.

## Narrow construction boundary

The repaired base harness remains byte-for-byte unchanged. Construction may
only add:

- `scripts/raisa_provider_free_check_in_relay_free_recovery_attempt_007.py`,
  exposing only `--check` and `--execute`, with no caller-supplied output path;
- one closed execution-envelope schema in the collision-free attempt-007
  Continuity namespace; and
- focused pure tests for full-object ancestry, byte-exact bindings, start-argv
  repair identity, CLI closure, collision denial, exact terminal routing,
  unconditional global restoration, predecessor immutability and sanitised
  failure.

The wrapper binds base attestation, evidence and failure paths only for one
runner call and restores all three unconditionally in `finally`. It refuses
execution if any attempt-007 terminal already exists and never deletes,
rotates, renames or overwrites a terminal. Pass writes
`transaction-attestation.json`, `rehearsal-evidence.json` and one
`attempt-007-execution-envelope.json`; failure writes
`rehearsal-failure-evidence.json` and the envelope. Construction creates no
Docker or database resource.

## API Spine and product boundary

This is a security, audit and idempotency rehearsal of the accepted REST
command shape, not an API change. GraphQL remains read-only. Explicit actor and
practice scope, authorisation, command and idempotency identity, exact request
digest, atomic effect/receipt/audit membership, default denial and fresh
authoritative readback remain fixed. Events are post-commit evidence hints,
never commands or source truth.

No REST/OpenAPI, GraphQL, async contract, route, application schema, migration,
feature flag, authored-synthetic allowlist, action grammar, first-party client,
waiting-area behaviour or product configuration may change. Dedicated check-in
remains default-off. Generic status does not gain `Arrived`, and the rehearsal
writes no product record or ordinary-practice admission.

## Fixed containment and cleanup boundary

Containment remains the accepted cached `postgres:16-bookworm` image with pull
policy `never`, one captured internal network, no published port, host relay,
Docker-exec byte bridge, external network, bind, volume, multiprocessing
process or queue. Random controller-memory credentials are delivered only by
post-inspection attached stdin. Retained evidence contains no credential or
nonce beyond the exact ownership label.

The three admin-owned non-product relations retain enabled and forced RLS. The
ephemeral login is a non-owner with `NOBYPASSRLS` and exact probe grants. Exact
terminal OCI state plus the prior observer predicate—not attachment output or
timing—classifies the incomplete response. Restricted authoritative readback
alone can prove committed exactly once. All partial, duplicate, mismatched or
cross-practice shapes deny.

One Sol controller owns every acquisition and cleanup. It reinspects captured
identity before removal. Pass or failure requires role, attachment, sidecars,
server, network and matching labels absent. Cleanup uncertainty is terminal
failure and never a reason to retry.

## Deterministic and preexecution admission

Before any Docker object, credential, PostgreSQL process, SQL or database
execution, one committed candidate must pass all of the following:

1. this plan, threat delta, fresh five-source receipt and all three lane
   dispositions validate;
2. the attempt-007 wrapper, schema and focused tests exist, and every frozen
   digest plus full-Git ancestry binding matches exactly;
3. the base static gate rejects all contract, manifest, OCI-state and
   classifier mutations with zero escapes;
4. focused attempt-007, repaired-start, base-harness, API Spine,
   no-database-interlock, latch, Baton and clockwork tests pass serially through
   admitted runners;
5. Ruff, Python compilation, JSON/schema validation and `git diff --check`
   pass;
6. read-only Docker version/image inspection proves the exact cached image and
   zero pre-existing attempt-007 labelled resources without creating an object;
7. a fresh preexecution five-source receipt binds the exact clean candidate and
   repeats the three-lane assessment; and
8. one distinct clockwork checkpoint `--check`, followed only on success by a
   separate `--publish`, advances the latch to exactly one database execution.

Then exactly one command may run:

`.venv/Scripts/python.exe -m scripts.raisa_provider_free_check_in_relay_free_recovery_attempt_007 --execute`

No automatic or manual retry is permitted. Invocation count is one even if the
command exits nonzero, output is absent, or bounded ownership-checked cleanup
is required.

## Acceptance and parallelism assessment

Pass requires the closed attempt-007 envelope, explicit rollback zero effect,
committed-exactly-once authoritative readback, no success from the incomplete
response, retry count zero, other-practice visibility zero, ordinary/product
effect zero, schema-valid sanitised evidence and exact cleanup with zero
residue. Failure preserves one immutable failed-closed terminal and closes the
attempt without retry.

- **DeepSeek native Harness:** `declined`, negative leverage. Its EMR4 worker
  allocation is paused pending a separate boot proof, and this provider-free
  database lifecycle grants no worker or provider call.
- **Gemini:** `declined`, neutral leverage during construction and occupied
  execution. No independent model judgment belongs inside a one-shot database
  lifecycle. Reassess only after a successful immutable terminal exposes a
  material static-veto surface.
- **Native subagents:** `declined`, negative leverage. Developer policy
  prohibits proactive delegation and the database lifecycle plus cleanup lease
  are indivisible and serial.
- **GPT Sol:** alone owns authority freeze, byte validation, execution,
  cleanup, acceptance, clockwork and Git.

## Protected and continuation boundaries

No product, patient, appointment, clinical, historical or protected data;
ordinary-practice enablement; feature-flag or allowlist change; product/API/
OpenAPI/GraphQL/config/client/waiting-area change; live provider; production
runtime; deployment; release; Pages; protected evidence access; or protected-
ref movement is authorised. Local/origin `master` and `handoff/current` remain
exact `2e34bdad732fdab32fbf778280b3d3c70d66d602`.

At closeout clockwork is the sole canonical governance writer. Sol writes the
paired lay/technical Yuri summary, sends the usual non-PHI Pushover, stages
only explicit paths and preserves `docs/branding/` plus every unrelated
untracked file. `git add .` and `git add -A` are forbidden.

After accepted closeout, proceed to the narrowest dependency-satisfied
successor. Pause only for a truly extraordinary, genuinely non-inferable or
safety-critical fork.
