# Raisa provider-free governance clockwork historical-derived first-use materialisation subgate rehearsal — plan

Date: 2026-08-24

Timestamp: 2026-08-24T10:38:41.3497625+10:00 (Australia/Brisbane)

Status: `frozen_fail_closed`

Reasoning level: `extra_high_authority_vocabulary_freeze_then_high_mechanical_implementation`

## Objective

Add one closed historical-data boundary mode to the repository-governance
clockwork so a later, separately authorised first-use materialiser can express
exact gate-wrapped authority without weakening the existing denial or measured
privacy-probe modes.

This tranche reads no historical material, creates no fixture and executes no
writer. It changes only the typed governance vocabulary and its authored-
synthetic tests.

## Accepted inputs

- accepted evaluator implementation source
  `abcd4206a363b0c565c070e0f2cb9c54d627b3b3`;
- accepted evaluator closeout source
  `538b5cb47e11aad77ae794873c5fa99718c9354d`;
- frozen successor contract
  `orchestration/continuity/raisa-provider-free-historical-derived-scenario-first-use-candidate-gate-evaluator-rehearsal/next-tranche-contract.json`;
- exact successor-contract SHA-256
  `93b6a177b73176518dcd94e31e9dfba90c26dca9a4c4a1851e2e858dc7393a1e`;
- existing clockwork implementation and tests at task HEAD
  `eee0bbc1c0a4b0482305038efcf2f6785ddf50a6`; and
- the active latch and its complete protected boundary list.

## Frozen implementation

Add `HISTORICAL_FIRST_USE_MATERIALISATION_SUBGATE_BOUNDARIES` as one exact
`frozenset` containing only:

1. `allow_local_only_historical_derived_first_use_materialisation_after_exact_candidate_gate`;
2. `historical_first_use_materialisation_subgate_contract_sha256_93b6a177b73176518dcd94e31e9dfba90c26dca9a4c4a1851e2e858dc7393a1e`;
3. `historical_first_use_candidate_gate_source_abcd4206a363b0c565c070e0f2cb9c54d627b3b3`;
4. `historical_first_use_exact_digest_bound_gate_receipt_required_before_write`;
5. `historical_first_use_maximum_one_minimised_structural_scenario_fixture`;
6. `historical_first_use_non_admission_or_revision_writes_nothing`;
7. `historical_first_use_written_bytes_must_match_admitted_candidate_digest`; and
8. `historical_first_use_authority_non_transitive`.

The existing stable safety floor and typed product-data denial remain
mandatory. The new allowance becomes the only additional recognised
`allow_...` token. The validator admits the new set only when all eight members
are present and no legacy denial, typed historical denial or measured-probe
member is present.

## Acceptance matrix

- legacy full denial remains accepted unchanged;
- typed product plus historical denial remains accepted unchanged;
- the complete measured privacy-probe mode remains accepted unchanged;
- the complete new materialisation mode is accepted;
- removing any one new member rejects as incomplete;
- mixing the new mode with legacy denial, typed historical denial or any
  measured-probe member rejects as a mode conflict;
- unknown allowance, digest, source or historical token rejects;
- the contract file is byte-bound to the exact SHA-256 above and its source,
  one-fixture, receipt-before-write, non-admission, digest-match and
  non-transitive semantics are asserted;
- closeout and user-decision intent paths use the same validator;
- validation is pure and creates no file; and
- all existing clockwork and latch tests remain green.

## Parallelism assessment

- DeepSeek Flash: `declined_negative`; the pinned native Harness remains
  paused, Claude is not a silent fallback, and this authority-vocabulary seam
  is too small and coupled to amortise a worker packet.
- Gemini verifier: `not_applicable_neutral`; no external judgment is needed
  beyond deterministic byte, vocabulary, conflict and regression tests.
- Native subagents: `declined_negative`; constants, validator ordering and the
  matrix form one serial unit.
- GPT Sol owns plan, implementation, verification and acceptance. Reassess on
  deterministic failure, before any dispatch, and at the next named tranche.

## Forbidden surfaces

No historical archive or ignored attempt output; no fixture materialisation;
no product, patient, appointment, clinical or protected data; no provider,
model, network, database, client, route, runtime or ordinary-practice change;
no deployment, production, release, Pages or protected-ref movement. Preserve
`docs/branding/` and every unrelated untracked file. Stage explicit paths only.

## Closeout and successor

Close only after focused and surrounding governance tests pass, the byte-bound
contract and all four closed modes read back exactly, protected refs remain at
`2e34bdad732fdab32fbf778280b3d3c70d66d602`, the paired Yuri summary and
non-PHI Pushover receipt exist, and the clockwork publishes the result.

The sole successor is
`raisa-local-only-historical-derived-minimised-check-in-context-scenario-first-use-materialisation-rehearsal`.
That later tranche must perform a fresh rehydration and separately freeze its
one-run private-access and writer envelope.
