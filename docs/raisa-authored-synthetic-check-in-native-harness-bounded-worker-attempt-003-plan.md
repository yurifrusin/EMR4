# Authored-synthetic check-in native Harness bounded-worker attempt 003 plan

Date: 2026-08-21

Timestamp: 2026-08-21T07:09:40.7240740+10:00 (Australia/Brisbane)

Status: `frozen`

Operation:
`raisa-authored-synthetic-check-in-native-harness-bounded-worker-attempt-003`

Reasoning level: Extra High freezes the one-use provider, cost, effect,
isolation, terminal and claim envelope. High is sufficient for its mechanical
implementation, deterministic admission, monitored execution, judgment and
closeout while these boundaries remain unchanged.

Machine-bound preplanning source:
`orchestration/agent_inbox/codex/deepseek-native-harness-attempt-003-user-decision-preplanning-corrected-receipt.json`.

## Authority and purpose

Yuri explicitly answered Yes on 2026-08-21 to one new occupied
authored-synthetic native-Harness worker attempt after accepting the
provider-disabled pre-HMR startup terminal recovery. This is a fresh attempt
003, not a retry, continuation, overwrite or reclassification of consumed
attempt 002.

Attempt 003 repeats the unchanged generic maintenance-window one-edit package
through the pinned official `@deepseek-ai/dsh@0.1.0-rc.7` Harness, accepted
`emr4-bounded-worker` preset, exact `edit`, `glob`, `read` model-facing view and
Ariadne broker. Its purpose is to determine whether the repaired outer
controller now yields either the intended exact edit or a materially more
traceable bounded terminal.

The programme word “check-in” is lineage only. No EMR4 product source, route,
adapter, appointment, status, patient, clinical or practice data enters the
workspace, prompt or provider request.

## Frozen single-use envelope

- Worker identity: `deepseek-native-synthetic-window-worker-003`.
- Disposable root:
  `C:/Users/sarashera/EMR4-worktrees/deepseek-native-synthetic-window-worker-003`.
- Evidence root: parent continuity directory `attempt-003/`; attempts 001 and
  002 remain byte-immutable.
- Provider/model: official DeepSeek endpoint through the local loopback
  broker; exact broker model `deepseek-v4-flash`; reasoning effort `high`.
- Data: newly materialized authored-synthetic non-product baseline only.
- Calls: one native process, one session, one turn and at most one provider
  request; one model-requested tool call, which must be exact `edit`.
- Token/byte bounds: at most 4,096 output tokens, 1,048,576 request bytes and
  2,097,152 response bytes.
- Time bounds: 300-second upstream request timeout inside a 420-second native
  process deadline.
- Cost posture: the one request may draw only from Yuri's existing
  user-controlled prepaid DeepSeek balance. The broker has no currency meter,
  top-up, balance mutation or second-call authority. Acceptance records
  observed usage when available and must not claim an enforced dollar cap.
- Tools: exact sorted `edit`, `glob`, `read`; parallel tool width one. Read or
  glob is allowed by the proven preset but cannot pass the one-request result.
- Effects: only the existing disposable
  `synthetic_window_coalescer.py` may change; no create, delete, rename, second
  path or controller repair after launch.
- Retry: zero automatic retries, zero resume, zero fallback, zero auxiliary
  model calls and no second worker. First native process creation consumes the
  authority regardless of outcome.

## Frozen work package and success path

The controller materializes one Git repository containing only
`synthetic_window_coalescer.py`. The baseline incorrectly shrinks a previously
extended interval when a nested interval is processed. The prompt includes the
complete baseline and asks for one literal edit that:

1. changes `current[1] = end` to `current[1] = max(current[1], end)`;
2. updates the adjacent comment to describe preserving the furthest boundary;
3. appends exactly one `nested` public regression row;
4. preserves every other byte and creates no file; and
5. concludes the rc.7 turn from the successful official edit result without a
   second request or prose summary.

Deterministic readback, not model prose, judges the exact expected bytes, one
changed path, four public cases and three private authored-synthetic holdback
cases.

## Required order

1. Extend only the attempt identity/configuration, terminal schema and focused
   tests; retain the accepted parent controller, broker and recovered pre-HMR
   terminal semantics.
2. Commit the frozen plan and provider-disabled candidate.
3. Publish the exclusive clockwork user-decision transition from the blocked
   gate to attempt 003.
4. Generate a fresh five-source preexecution receipt and run deterministic
   admission with zero provider requests.
5. Materialize the fresh disposable root and bind the exact 40-character
   candidate, WorkOrder, authority, forbidden surfaces, prompt, preset,
   package, runner and cleanup identities.
6. Publish a separate exact clockwork checkpoint. Before it, no native process
   or provider request may start.
7. Launch exactly one monitored native Harness process.
8. Stop owned processes, write and validate a safe terminal, remove the exact
   disposable root, then judge the result. A pre-first-HMR failure must use the
   accepted two-stage/eleven-cause sanitized sidecar before raw cleanup.

Any admission failure occurs before provider I/O and may receive only a
mechanical in-plan correction. Once process creation is attempted, the attempt
is consumed and there is no retry.

The accepted pre-HMR recovery evidence binds its historical reviewed source and
must not be regenerated merely because this descendant adds attempt-003
identity wiring. Its committed-report equality tests are therefore excluded
from the transition manifest. Descendant-focused tests instead prove that the
unchanged recovered builder and exclusive sidecar write still precede exact
root cleanup on the attempt-003 path.

## Acceptance and claim boundary

Success requires one completed provider request, one successful exact native
`edit`, no second request, exact expected bytes, all seven authored-synthetic
cases, agreement among broker/runner/tool/process accounts and complete
cleanup. A negative result is accepted only as failed-closed evidence with its
strongest justified lifecycle coordinate, exact call/tool/change counts,
immutable consumed record and cleanup readback.

A pass proves only one shaped non-product one-request edit. A failure proves
only the bounded observed terminal. Neither outcome establishes general
DeepSeek quality, general Harness reliability, multi-turn usefulness, EMR4
product readiness, production isolation, security accreditation or deployment
suitability.

## Parallelism assessment

- **DeepSeek Flash:** `planned`, positive leverage. It owns exactly the one
  occupied synthetic edit after deterministic admission and checkpoint.
- **Gemini 3.7 Flash/high:** `reserved`, neutral leverage. No fresh call is
  included in this envelope because the work package, controller semantics and
  pre-HMR recovery are already accepted; reassess only if a new risk-triggered
  repository candidate is proposed after the occupied terminal.
- **Native subagents:** `declined`, negative leverage. Developer policy
  prohibits proactive delegation and the one process/session/terminal/cleanup
  lifecycle is irreducibly serial.
- **GPT Sol:** owns planning, exact identity wiring, admission, checkpoint,
  monitoring, cleanup, deterministic judgment, acceptance, clockwork and Git.

## Deliberately closed

No retry, resume, fallback, second worker, attempt 006, Docker, PostgreSQL,
SQL, transaction, product source/configuration, API, OpenAPI, GraphQL, schema,
migration, route, adapter, feature flag, allowlist, command grammar,
first-party client or waiting-area change; no ordinary-practice enablement or
generic-status `Arrived`; no product, patient, appointment, clinical,
historical or protected data; no production runtime, deployment, release,
Pages or protected-ref movement.

Local/origin `master` and `handoff/current` remain exact
`2e34bdad732fdab32fbf778280b3d3c70d66d602`. Preserve `docs/branding/` and
every unrelated untracked file. Stage explicit paths only; never use
`git add .` or `git add -A`.
