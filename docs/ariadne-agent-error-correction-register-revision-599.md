# Ariadne agent error and correction register — revision 599

<!-- ariadne-agent-error-register-reading
revision: 599
incident_count: 856
new_incident_ids: AER-0852,AER-0853,AER-0854,AER-0855,AER-0856
open_incident_count: 0
-->

This revision note binds five corrected provider-free identity-rebinding and
workflow observations to the prospective clockwork-projected register. The
canonical JSON register and pattern report remain clockwork-owned, and one tick
advances the register once.

## AER-0852

The first fresh preplanning state placed the protected expected Git object in
human-authored `git_refs_and_worktree` prose. The orchestrator preflight
rejected `git_refs_evidence_manual_object_id_forbidden` before planning or
dispatch. The rejected pair remains evidence; a distinct corrected state
describes the invariant without an object value, leaving the machine snapshot
as the sole source of Git-ref identities.

## AER-0853

The candidate commit directly advanced four fields in the clockwork-owned
active-operation latch. A working-tree restoration was insufficient because
the candidate source still contained those bytes, and the first canonical-path
check rejected `tick_canonical_not_at_source`. The immediately previous latch
generation was restored and committed; intermediate progress remains in
receipts, while the clockwork alone publishes the completed tranche and
successor transition.

## AER-0854

The first closeout intent used the recalled schema name
`ariadne.governance_closeout_intent.v1` instead of the admitted live-tick schema.
The read-only clockwork check rejected `tick_intent_keys` before commands or
canonical writes. The failed intent is preserved and the corrected canonical
intent copies the exact current `ariadne.governance_live_tick_intent.v2` value.

## AER-0855

The corrected intent content was first supplied under
`closeout-intent-v2.json`, outside the clockwork's closed filename. The CLI
rejected `closed_tick_intent_required` before reading the intent or running any
command. The failed copy is preserved and the corrected live intent occupies
the exact operation-local `closeout/closeout-intent.json` path.

## AER-0856

The corrected intent named prospective revision 599 in baton acceptance, but
the orchestrator incorrectly expected the clockwork to render its human
revision document. The read-only check rejected
`tick_incident_revision_reading` before commands or canonical publication
because that input was absent. The exact prospective revision, incident count,
ordered incident IDs and headings are now materialised before the tick; future
incident-bearing ticks treat this document as an explicit pre-check input.
