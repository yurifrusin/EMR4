# Ariadne agent error and correction register — revision 624

Date: 2026-08-22

Status: `accepted_closed_reading`

<!-- ariadne-agent-error-register-reading
revision: 624
incident_count: 982
new_incident_ids: AER-0980,AER-0981,AER-0982
open_incident_count: 0
-->

This revision records two bounded orchestrator lapses in the accepted-guard
graph materialization recovery. Both are contained; neither is open.

## AER-0980 — focused tests copied incidental source and prose forms

Two focused assertions expected a multiline function signature and prose
sentences that were not the exact accepted source/plan forms. Focused pytest
rejected before any Node attempt. The assertions now bind the four-argument
semantic signature and the actual frozen boundary language.

No process identity, Harness, worker, model, provider, product, data,
deployment, Pages or protected-ref boundary was consumed.

## AER-0981 — success vector underestimated installed setup read and hook counts

The frozen fixture vector expected three hook installations and did not freeze
the exact preset-root read count. The installed runner produced a clean
756-byte typed stdout envelope but validation rejected it and retained only its
hash. The one process identity was consumed and cannot be retried.

The correction is to keep the negative immutable and admit a distinct
process-free successor that reconstructs the source-defined finite JSON grammar
against the retained length/hash. Any future process contract must derive its
counter vector from the installed setup graph before launch and retain a
schema-safe typed mismatch coordinate rather than only a generic rejection.

## AER-0982 — clockwork publication invoked with a nonexistent mode

After the closeout dry run passed, the orchestrator invoked the tick with
`--execute` although the closed CLI vocabulary is `--check`, `--publish` and
`--rollback`. Argument parsing rejected immediately with no repository or
clockwork mutation. The corrected publication uses `--publish`; future callers
must read the parser-owned mode vocabulary rather than generalise from other
controller CLIs.
