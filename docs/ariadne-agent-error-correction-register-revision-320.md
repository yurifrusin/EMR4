# Ariadne agent error and correction register — revision 320

Date: 2026-08-17

Timestamp: 2026-08-17T07:17:16.9889734+10:00 (Australia/Brisbane)

Status: corrected and contained

## Revision

Revision 320 records 369 bounded known incidents. No incident is open.

- AER-0369 preserves the delete-confirm HTTP/PostgreSQL preexecution repository
  defect. The route producer duplicated and diverged from accepted adapter
  canonicalization, omitted the nested signed evidence required by adapter
  equality, and the fresh physical command transaction established no
  transaction-local practice context for forced RLS. Provider-free inspection
  and a pure ingress probe stopped these gaps before Docker or SQL.

The correction delegates producer canonicalization to the accepted adapter,
carries the exact signed evidence in the prepared nested proposal, and sets the
server-authenticated practice locally after transaction isolation and before
any authority or target read. Focused route-to-adapter and physical-order guards
pass. The live disposable PostgreSQL rehearsal remains required before the
product tranche can be accepted.

The first focused run also exposed an obsolete historical test assumption that
every scaffold-era source hash must equal its later descendant. The historical
contract remains byte-identical and its paths/hashes stay structurally checked;
the current integration contract separately binds the accepted descendant API
and pre-edit source state. No historical evidence was rewritten.

No product/patient/clinical or protected data, provider, credential, browser,
database, deployment, release, Pages or protected-ref surface opened.
