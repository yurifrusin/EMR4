# Ariadne agent error and correction register — revision 626

Date: 2026-08-22

Timestamp: 2026-08-22T21:03:29.4171606+10:00 (Australia/Brisbane)

Status: `accepted_closed_reading`

<!-- ariadne-agent-error-register-reading
revision: 626
incident_count: 996
new_incident_ids: AER-0985,AER-0986,AER-0987,AER-0988,AER-0989,AER-0990,AER-0991,AER-0992,AER-0993,AER-0994,AER-0995,AER-0996
open_incident_count: 0
-->

This revision records twelve bounded orchestrator control-plane errors around
the passing provider-free corrected-runner boot proof. All were corrected or
contained, none is open, and none caused a second native Harness process or a
model/provider request.

## AER-0985 — preplanning receipt used descriptive values outside closed types

The first receipt used non-admitted lane/reachability descriptions, omitted two
declared adapters and repeated full Git IDs in a machine-owned evidence field.
Preflight rejected before planning. The corrected receipt uses only configured
enums, the complete adapter inventory and machine-generated Git binding.

## AER-0986 — predecessor live-latch equality was replayed before selection

The first inherited suite replayed one predecessor-only assertion that its own
operation remained the active latch. The exact node is now documented as
historical equality while every substantive predecessor assertion remains
selected.

## AER-0987 — one-attempt controller omitted success-terminal readback

The execution controller initially admitted only the no-artifact preflight
state. After its single process passed, review exposed the missing idempotent
success branch. It now validates the consumed identity, schema-valid evidence,
exact observation, cleanup and report without rerunning the Harness.

## AER-0988 — focused-test path was retyped with wrong separators

One read-only hash command substituted hyphens for underscores in the test
filename. The path lookup failed without mutation. The contract was bound from
the corrected exact repository path.

## AER-0989 — PowerShell reinterpreted an unquoted Git revision suffix

A read-only `git rev-parse` command allowed PowerShell to reinterpret the
peeling suffix. Git rejected the resulting arguments. Plain machine resolution
then returned the exact full IDs without state change.

## AER-0990 — closeout broker posture used descriptive prose

The first closeout check supplied a meaningful but non-admitted future broker
posture. Clockwork rejected before any write. The broker block now uses exact
`provider_free_shadow`; future qualifications remain in protected boundaries.

## AER-0991 — next-operation closures paraphrased the mandatory floor

The next-boundary list contained semantically similar closures but initially
missed literal validator-owned floor tokens. The complete required set is now
checked for set membership before clockwork invocation.

## AER-0992 — prospective register reading was referenced before authoring

Transaction preparation rejected because the prospective graph referenced this
revision document before it existed. The reading is now authored before the
clockwork check; canonical register allocation and publication remain owned by
the clockwork.

## AER-0993 — provider-free test wrapper received a pytest node selector

The rehearsed closeout manifest passed a `--deselect` node selector to the
literal-path-only provider-free pytest wrapper. Its closed argument parser
rejected the command before tests or state mutation. Literal-path tests remain
with that wrapper; the one historical-node deselection now uses the admitted
serial pytest runner.

## AER-0994 — completed readback depended on ambient cache-root discovery

The first clean provider-free closeout run removed `LOCALAPPDATA`, exposing that
the completed-attempt readback still resolved the npm cache before consulting
canonical success evidence. No Harness or provider activity occurred. Completed
readback now validates the immutable evidence and recorded package count without
ambient cache discovery; only an unexecuted preflight needs a live cache root.

## AER-0995 — serial pytest remainder lacked its delimiter

The first serial historical-test command placed `-q` directly after the
wrapper's named options. Its closed parser rejected the pytest option before
test collection. Adding the explicit `--` remainder delimiter admitted the
same frozen deselection, which then passed three selected tests.

## AER-0996 — incident observation used descriptive enum labels

The first observation for AER-0994 described candidate and correction state as
`corrected_clean_candidate` and `fixed`. Clockwork rejected the first label and
schema inspection exposed the second before another check. Both now use the
admitted `accepted_candidate_changed` and `control_added` values.
