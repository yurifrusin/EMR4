# EMR4 model-required Bureau Gate-zero closeout

Date: 2026-08-04

Result: `model_required_bureau_gate_zero_shared_contract_pass`

Candidate HEAD: `3727ee83d03d310bbb2f0d52c2ce70d0430ab65d`

## Accepted result

Gate zero freezes one provider-neutral, versioned contract for Bernie, Rayleen,
Davida and controlled recovery/update under the doctrine `model-required
cognition; deterministic authority`.

The accepted contract contains:

- four ordered, separately principalled cognitive, proof, authority and
  execution/readback planes;
- exact non-crossing domain ownership and nine closed candidate kinds;
- mandatory provider admission, explicit
  `PROVIDER_REQUIRED_UNAVAILABLE` outage and no equivalent silent fallback;
- minimized source-labelled data-only context, raw `untrusted_model`
  candidates, monotone provenance/integrity/readers/freshness/authority labels,
  exact source/sink mediation and typed denial receipts;
- one fresh broker-mediated cell per attempt with one input, one output, no
  ambient authority bridge, hostile-byte parsing, exact quotas, kill, teardown
  and zero-residue contracts;
- distinct backend human/dual-review and single-purpose command/readback
  authority;
- mixed API Spine separation for GraphQL reads, REST/OpenAPI commands, committed
  event hints and declarative manifests;
- a closed Access AI invocation boundary and a backend-owned future command
  envelope with actor, practice, correlation, idempotency, freshness,
  confirmation, audit and readback requirements; and
- exact provider-free, occupied-model, live-product, actuator, deployment,
  production and release evidence classes.

## Evidence

The deterministic acceptance result is
`model_required_bureau_gate_zero_provider_free_deterministic_acceptance_pass`.
It validates six closed Draft 2020-12 schemas, five canonical
authored-synthetic examples, exact registry associations, hostile byte cases,
label joins, sink denials, outage behavior, authority ceilings and zero-side-
effect accounting.

Sol Extra High review hardened raw candidate labels, context authority and
domain/kind binding before returning `pass_for_fresh_independent_veto`.

A genuinely fresh Gemini 3.6 Flash/high Antigravity review then ran 137 tests on
the exact clean non-protected candidate and returned `pass` with zero findings.
Its development review transport was non-zero and source-only. Candidate
product/runtime provider calls and other candidate-runtime side effects remained
zero. The review left HEAD unchanged and the worktree clean.

## API Spine stewardship

The API Spine source pass and its complete artifact suite pass. GraphQL remains
read/context only; it cannot invoke a provider or act as a command tunnel.
REST/OpenAPI owns provider/external and state-changing commands. Events do not
bypass commands, and manifests do not become executable policy engines.

## Authority and claim boundary

This acceptance is architecture, schema prototypes and provider-free
deterministic evidence. It does not implement or authorize a provider product
runtime, patient or product data, product read, command/write, actuator,
shell/SQL/cloud/IAM, migration, external update activation, deployment,
production, release, Pages rebuild, protected-ref movement or protected-evidence
access. The schema-level cell contract is not evidence that an operating-system,
container, microVM, broker or provider runtime now satisfies it.

`docs/branding/` and the preserved Consultant and Gate −1 pre-push receipt/state
files remain untouched and excluded.

## Automatic successor

Under Yuri's standing programme authority, Gate-zero closeout is not a
permission handback. A1/A2, B1/B2 and C1/C2 are now open as provider-free
architecture/test lanes. D1/D2 may begin once C1 freezes the shared technical
context and provenance vocabulary it needs. Occupied model rehearsals, live
product reads, every write/actuator and every deployment/release boundary remain
separate genuine attention gates until an active accepted plan makes them exact.
