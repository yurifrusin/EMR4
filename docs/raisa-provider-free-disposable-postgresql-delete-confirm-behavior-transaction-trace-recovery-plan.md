# Delete-confirm behavior/transaction trace-recovery plan

Date: 2026-08-16

Timestamp: 2026-08-16T13:09:34.9753447+10:00 (Australia/Brisbane)

Status: `frozen_for_provider_free_trace_diagnosis`

Risk classification: Tier 2 — database runtime and authority/transaction evidence

## Why this revision exists

The original frozen plan at `2a5042f80941e2bd191999c430ff2517ba7e8cb2`
remains authoritative except for this narrower recovery amendment. Occupied
attempts 001 and 002 failed closed on AER-0351's synthetic identity collision
and cleaned up exactly. Attempt 003 proved all nine authority groups, then
stopped before the eleven-transaction checkpoint with generic
`lock_authority_order_mismatch`; cleanup again passed.

The retained failure evidence does not identify the transaction group or the
expected and observed value-free statement tokens. It therefore cannot decide
whether the harness classifier is wrong or the accepted unmounted service seam
actually differs from the frozen order. No further behavior claim or rerun is
admitted under the original repair allowance.

## Exact recovery surface

Sol may change only:

- `scripts/raisa_provider_free_disposable_postgresql_delete_confirm_behavior_transaction_rehearsal.py`;
- `tests/test_raisa_provider_free_disposable_postgresql_delete_confirm_behavior_transaction_rehearsal.py`;
- `orchestration/continuity/raisa-provider-free-disposable-postgresql-delete-confirm-behavior-transaction-rehearsal/provider-free-behavior-transaction-evidence.schema.json`;
- the current failure/pass evidence generated by that harness; and
- this tranche's incident, semantic-freeze, latch, receipt and closeout metadata.

No migration, model, service, route, public contract or product source may
change during diagnosis.

## Closed diagnostic contract

An order mismatch must retain only:

- one exact group ID from `TX-S01` through `TX-S11`;
- one exact outcome already admitted by the rehearsal contract;
- expected and observed arrays drawn only from the seven existing statement
  token enums; and
- the existing stage, code and detail digest.

No SQL text, parameters, row values, UUIDs, timestamps, exception text or
database content may enter evidence. The failure schema remains a closed whole
document. Static tests must reject unknown group IDs, outcomes, tokens, keys,
reordering of closed fields and raw SQL-like strings.

## Bounded execution rule

After focused tests, Ruff, compilation, schema validation and whitespace pass,
one diagnostic occupied attempt may run with the unchanged internal-only
network, portless tmpfs PostgreSQL 16 container and fixed loopback relay.

- If all twenty groups pass, proceed directly to the existing final gates.
- If the trace proves a harness classifier or frozen expected-token
  representation defect while the service source order remains conformant,
  Sol may make one exact harness/schema/test repair and run one final occupied
  attempt.
- If the trace shows that `app/services/appointment_delete_physical.py` differs
  from the accepted authority/lock order, stop without editing product source
  and raise a genuine architectural decision fork.
- Any different failure, cleanup ambiguity or second post-diagnostic failure
  stops; no further local retry is authorized.

Every attempt must re-prove exact relay shutdown, captured container removal,
captured internal-network removal and absence. Failed evidence is immutable Git
history and cannot be promoted to a pass claim.

## Parallelism efficacy

- DeepSeek V4 Flash/high: `not_applicable`; its implementation lane is closed,
  and reopening it would add transport/context cost to a tiny conceptual trace
  diagnosis.
- Gemini 3.7 Flash/high: `reserved`; exactly one final independent veto remains
  after an occupied pass, never during diagnosis.
- Native subagents: `declined`; the database lifecycle and single trace
  interpretation are serial, and native delegation is not authorized here.

## Acceptance

This recovery passes only if the diagnostic contract is schema-closed and
hostile-tested, the service file remains byte-identical, the bounded execution
rule is observed, all nine authority and eleven transaction groups ultimately
pass, cleanup is exact, focused and canonical gates pass after the final
semantic freeze, and one Gemini 3.7 Flash/high veto passes. Protected refs and
all unrelated untracked files must remain unchanged.

## Forbidden surfaces

No product database or durable storage, product provisioning, mounted/called
route, public API/GraphQL/UI edit, real command, concurrency, restart, unknown
commit, patient/clinical/real-person/product/protected data, provider/ADC/IAM,
external network, deployment, production, release, Pages or protected-ref
movement. Preserve `docs/branding/` and every unrelated untracked file; stage
explicit paths only.
