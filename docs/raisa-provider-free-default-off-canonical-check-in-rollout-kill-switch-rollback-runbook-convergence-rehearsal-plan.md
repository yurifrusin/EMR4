# Provider-free default-off canonical check-in rollout/kill-switch/rollback runbook convergence plan

Date: 2026-08-22

Timestamp: 2026-08-22T23:14:54.9671906+10:00 (Australia/Brisbane)

Status: `frozen_for_execution`

Operation:
`raisa-provider-free-default-off-canonical-check-in-rollout-kill-switch-rollback-runbook-convergence-rehearsal`

Task baseline: `6c57762db71b002a5dd1d7c73a8edbc482aaa40e`

Target result:
`raisa_provider_free_default_off_canonical_check_in_rollout_runbook_convergence_pass`

Reasoning level: High. The accepted orientation already froze the selection,
authority ceiling and exact closed form; this tranche is mechanical API-Spine
artifact convergence with deterministic admission.

## Objective

Materialize exactly one already-frozen declarative API-Spine manifest at:

`docs/api-spine/manifests/canonical-check-in-rollout-kill-switch-rollback-runbook.json`

Its sole byte authority is
`orchestration_harness/check_in_rollout_runbook.py::required_candidate_bytes`.
The exact candidate is 2,331 bytes, has SHA-256
`dbd765ef3afe2ffe283a07befff44f745b21a8ec474c58d5a6d944fe3a9c8448`,
and must pass that module's duplicate-key, UTF-8, newline, size and exact
closed-form validator.

This is artifact convergence, not rollout execution. The manifest remains
default-off, keeps activation authority false, records zero active ordinary
practice records, keeps the kill switch engaged, denies success on unknown
commit state, forbids blind retry and requires source-truth readback.

## API Spine boundary classification

- Existing check-in remains a typed REST/OpenAPI proposal and confirmed
  command family.
- GraphQL remains read-only.
- The new JSON file is declarative input only; it is not a route, command,
  grant, executable policy engine, activation record or recovery proof.
- Server-owned identity, practice scope, role, confirmation, idempotency,
  freshness/revalidation, audit and typed receipt remain unchanged.
- Async events remain observational hints and never confer command or rollback
  authority.

No OpenAPI or runtime source changes because the exact manifest neither changes
the request/response contract nor mounts an operation.

## Exact owned outputs

GPT Sol may create only:

1. this frozen plan;
2. its threat-model delta;
3. the exact canonical JSON manifest;
4. one focused deterministic test module;
5. required five-source receipts; and
6. bounded closeout, efficacy, Sol acceptance, paired Yuri summary and
   transactional clockwork evidence after acceptance.

No `app/**`, configuration, route, OpenAPI, GraphQL, client, feature flag,
synthetic allowlist, migration, database, Harness runner/broker or protected
source is editable.

## Deterministic acceptance

Pass requires:

1. a fresh five-source receipt with all three worker lanes assessed and zero
   caller-supplied Git identities;
2. the target bytes equal `required_candidate_bytes()` exactly;
3. target byte count and SHA-256 equal the frozen 2,331-byte values;
4. `validate_candidate_bytes()` accepts the file and reports activation false,
   ordinary practice disabled and the exact default-off claim;
5. the kill switch, rollback/readback, non-PHI audit, full 40-character Git
   object ID and zero-effect fields remain exact;
6. check-in OpenAPI retains `proposeAppointmentCheckIn` and
   `confirmAppointmentCheckInProposal` without mutation;
7. focused, API-Spine, latch, Baton, Compass and clockwork checks pass with
   Ruff, compilation and `git diff --check`;
8. no product or protected path changes; and
9. local/origin protected refs remain exact while `docs/branding/` and every
   unrelated untracked file remain preserved.

The validator is the semantic authority. A JSON file that parses but differs
in any field, duplicate-key posture, newline form or canonical byte sequence
fails closed.

## Parallelism-efficacy assessment

- DeepSeek V4 Flash/high: `declined`, negative leverage. The native Harness is
  paused, Claude Code is historical only, and briefing a worker costs more than
  materializing one exact precomputed artifact.
- Gemini 3.7 Flash/high: `declined`, neutral leverage. Exact byte equality and
  the existing closed-form validator decide acceptance without a model-judgment
  fork. Reassess only if deterministic API-Spine evidence conflicts.
- Native subagents: `declined`, negative leverage. Developer policy prohibits
  proactive delegation and the artifact has serial plan/manifest/graph custody.
- GPT Sol owns byte materialization, deterministic validation, API-Spine review,
  acceptance, Git and closeout.

Parallel work packages: none. Reassess after focused validation, on any contract
conflict and at closeout.

## Recovery and stop rules

- If generated bytes differ from the frozen digest or exact validator output,
  do not normalize them by hand; diagnose the source/candidate mismatch.
- If API-Spine tests find a P0-P2 authority or contract conflict, stop before
  acceptance and freeze the smallest corrective plan.
- A failed deterministic test permits local correction only within the four
  owned implementation/plan paths. It grants no provider, database or runtime
  retry.
- Do not claim rollback or unknown-commit operational proof merely because the
  declarative policy is present.

## Claim and protected boundary

Passing proves only that the canonical default-off rollout/kill-switch/rollback
manifest is present, byte-exact and machine-checked. It closes the manifest half
of the accepted runbook gap. It does not prove or authorize ordinary admission,
activation, live secrets, monitoring, rollback execution, unknown-commit
readback, client cutover, runtime or production suitability.

No ordinary-practice enablement, feature-flag or allowlist change, application/
configuration/route/OpenAPI/GraphQL/client change, generic-status `Arrived`
change, action grammar, waiting-area movement, product/patient/appointment/
clinical/historical/protected data, DeepSeek/native-Harness work, provider,
database/Docker, production runtime, deployment, release, Pages or protected-ref
movement is authorised. Preserve `docs/branding/` and every unrelated untracked
file. Use explicit-path staging only, never `git add .` or `git add -A`.
