# Reception One Bureau Model Text Lane — Iterative Retry Closeout

## Result

`reception_one_bureau_model_text_iterative_retry_pass`

The compact Reception One model-text wire now passes through the exact
Australian Vertex locational lane and the deterministic proofreader.

Three distinct retry ledgers were used:

1. Retry 001 failed closed on the strict local wire schema. The repair added
   only canonical goal casing and separator-whitespace normalization.
2. Retry 002 received HTTP 200 but failed at four prior-step source paths. The
   repair added a canonical three-segment source example and explicitly
   forbade omitted prefixes and dot notation; it did not transform or guess a
   source.
3. Retry 003 received HTTP 200 in 1435 ms. Vertex supplied 1286 prompt, 148
   candidate and 1434 total tokens. The generated five-step candidate passed
   the unchanged deterministic proofreader with no repair or violation.

The atomic release contained only the admitted authored-synthetic typed
proposal fields:

- squeeze-in assessment for
  `synthetic-patient-margaret` with
  `synthetic-practitioner-shera`;
- one authored-synthetic candidate slot,
  `synthetic-slot-july27-1215`;
- 15-minute duration;
- explicit warnings including no reservation and required staff selection and
  confirmation; and
- `write_performed=false`.

All three retry ledgers are consumed. No call followed the first admitted
result. No provider/model/project/identity/region change, API key, global
endpoint, fallback, tool, grounding, retrieval, cache creation, product
context, database access, command or appointment write occurred. All owned
containers, networks, images, processes, temporary tokens and contexts are
absent.

The deterministic cost guard conservatively bounds the three calls at USD
0.0313344 using the request/output ceilings and current official Gemini 2.5
Flash token rates. This is not a billing invoice, but it is well below the USD
1 application ceiling.

## What the evidence proves

It proves:

- the configured and observed
  `australia-southeast1-aiplatform.googleapis.com` locational request path;
- exact `bernie-emr4-dev` and Bernie service-account impersonation through
  existing keyless ADC;
- the isolated one-use broker/cell lifecycle and external hash-chained audit;
- bounded Gemini composition over the closed operator catalogue;
- deterministic proofreader admission; and
- typed in-memory proposal-only release for this authored-synthetic case.

It does not prove Australian physical or sovereign processing, safety for
product-derived, patient, health or clinical data, general request coverage,
production fitness, user value, appointment mutation, deployment or release.

Product-context reads and runtime proposal delivery remain separate later
decisions.

## Final verification

The final bound state passed:

- the Reception One model-text and Typed Plan Protocol suites;
- relevant API Spine, Continuity, Compass and Sydney Vertex contract,
  preflight, rehearsal and audit suites;
- the repository-only Ariadne verifier;
- Compass validation and rendered-report regeneration at graph revision 70
  and map revision 57;
- JSON parsing for all lane evidence plus JSON Schema validation for the
  Continuity graph and Compass map;
- Ruff and Python compilation for the changed implementation and tests;
- `git diff --check` apart from pre-existing line-ending warnings; and
- an independent post-verification residue check showing no owned container,
  network, image, broker process or temporary root.

No JavaScript changed in this retry tranche, so no JavaScript syntax target
applied. No commit, push, pull request or protected-ref movement occurred.

The terminal pre-acceptance receipt is
`orchestration/agent_inbox/codex/reception-one-bureau-model-text-lane-iterative-retry-preacceptance-receipt.json`.
