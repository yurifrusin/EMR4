# Provider-free default-off canonical check-in non-PHI observability manifest convergence plan

Date: 2026-08-22

Timestamp: 2026-08-22T23:34:18.1886561+10:00 (Australia/Brisbane)

Status: `frozen_for_execution`

Operation:
`raisa-provider-free-default-off-canonical-check-in-non-phi-observability-manifest-convergence-rehearsal`

Task baseline: `422eeecbb29fbef26f55f7de3d91516f39182d1d`

Target result:
`raisa_provider_free_default_off_canonical_check_in_non_phi_observability_manifest_convergence_pass`

Reasoning level: High. The accepted architecture already freezes the complete
observability object and the current latch limits this tranche to mechanical
declarative API-Spine convergence.

## Objective

Materialize exactly one default-off declarative API-Spine manifest at:

`docs/api-spine/manifests/canonical-check-in-non-phi-observability.json`

Its semantic authority is exactly the `observability` object in:

`orchestration/continuity/raisa-provider-free-default-off-ordinary-practice-canonical-check-in-admission-control-architecture/contract.json`

The accepted architecture source is
`752b521c59f5b44bf46de0cf776a33ac74b8134d`. The canonical wrapper is
6,291 bytes with SHA-256
`79d6191e1a499e85bb12be38fd15980c7f1bf7dc54eb15132c607b0c43341d8c`.

The wrapper adds only a schema version, exact source binding, the
`prepared_not_authorized` state, explicit default-off posture, zero-effect
fields and the exact accepted observability sub-object. It is not
instrumentation, telemetry transport, alert delivery, automatic rollback,
command authority or operational evidence.

## Exact projected contract

The manifest must contain exactly:

- five metric families:
  `emr4_check_in_admission_decisions_total`,
  `emr4_check_in_admission_snapshot_age_seconds`,
  `emr4_check_in_admission_kill_switch`,
  `emr4_check_in_unknown_commit_total` and
  `emr4_check_in_control_commands_total`;
- the accepted closed low-cardinality label domains;
- all 15 forbidden identifiers/value classes;
- six critical alerts for kill-switch engagement, invalid/stale snapshot,
  unknown commit, active-record rejection, control-audit failure and rollback
  failure;
- `automatic_control_action: false` and `contains_identifier: false` on
  every alert; and
- false values for raw request/response use, audit-as-metric, telemetry
  feedback and automatic retry/control action.

Instrumentation, alert transport, automatic control action and ordinary
practice all default false.

## API Spine boundary classification

- Existing check-in remains a REST/OpenAPI proposal and confirmed command
  family; no command contract or operation ID changes.
- GraphQL remains read-only.
- Metrics and alerts are observations only and cannot grant admission, retry a
  command, clear a kill switch, activate a record or execute rollback.
- The manifest contains no practice, appointment, patient, practitioner, user,
  actor, correlation, idempotency, command, record, evidence, token, free-text
  or request/response value.
- Append-only attributable control audit remains an authority record and is
  never copied into metric labels.

## Exact owned outputs

GPT Sol may create only:

1. this frozen plan;
2. its threat-model delta;
3. the exact canonical JSON manifest;
4. one focused deterministic test module;
5. required five-source receipts; and
6. bounded closeout, efficacy, Sol acceptance, paired Yuri summary and
   transactional clockwork evidence after acceptance.

No `app/**`, instrumentation/configuration, route, OpenAPI, GraphQL, client,
feature flag, synthetic allowlist, migration, database, Harness runner/broker
or protected source is editable.

## Deterministic acceptance

Pass requires:

1. a fresh five-source receipt with complete worker-lane assessment and zero
   caller-supplied Git identities;
2. manifest `observability` deep-equals the accepted source object's
   `observability` value;
3. canonical bytes and SHA-256 equal the frozen 6,291-byte values;
4. wrapper source, default posture, status, effects and claim are exact;
5. metric family count/name/kind/label domains, forbidden value classes and all
   six alerts are exact;
6. no alert contains an identifier or automatic action, and the four
   non-feedback booleans remain false;
7. check-in OpenAPI retains `proposeAppointmentCheckIn` and
   `confirmAppointmentCheckInProposal`;
8. focused, API-Spine, latch, Baton, Compass and clockwork checks pass with
   Ruff, compilation and `git diff --check`;
9. no product or protected path changes; and
10. protected refs remain exact while `docs/branding/` and all unrelated
    untracked files remain preserved.

## Parallelism-efficacy assessment

- DeepSeek V4 Flash/high: `declined`, negative leverage. The native Harness is
  paused, Claude Code is historical only, and one exact accepted-object
  projection is smaller than its worker briefing and monitoring cost.
- Gemini 3.7 Flash/high: `declined`, neutral leverage. Deep equality, canonical
  bytes and exact API-Spine tests decide the artifact without a material model
  judgment fork.
- Native subagents: `declined`, negative leverage. Developer policy prohibits
  proactive delegation and source-object/wrapper/graph custody is serial.
- GPT Sol owns projection, deterministic admission, API-Spine review,
  acceptance, Git and closeout.

Parallel work packages: none. Reassess after focused validation, on any source
equality/API-Spine conflict and at closeout.

## Recovery and stop rules

- Any difference from the accepted `observability` object is a source
  projection failure, not an invitation to edit the taxonomy.
- A wrapper byte/digest mismatch must be diagnosed and cannot be normalized by
  hand after acceptance.
- A P0-P2 API Spine conflict stops acceptance and requires the narrowest
  corrective plan.
- Deterministic correction remains limited to the four owned plan/candidate
  paths and grants no runtime, provider or database retry.

## Claim and protected boundary

Passing proves only that the accepted non-PHI observability contract is present
as one exact default-off API-Spine manifest. It does not prove emitted metrics,
delivered alerts, automatic controls, live monitoring, ordinary activation,
unknown-commit recovery, secret custody, client cutover, runtime or production
suitability.

No ordinary-practice enablement, feature-flag or allowlist change, application/
configuration/instrumentation/route/OpenAPI/GraphQL/client change, generic-
status `Arrived` change, action grammar, waiting-area movement, product/
patient/appointment/clinical/historical/protected data, DeepSeek/native-Harness
work, provider, database/Docker, production runtime, deployment, release, Pages
or protected-ref movement is authorised. Preserve `docs/branding/` and every
unrelated untracked file. Use explicit-path staging only, never `git add .` or
`git add -A`.
