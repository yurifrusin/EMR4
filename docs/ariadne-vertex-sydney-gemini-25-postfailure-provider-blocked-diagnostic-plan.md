# Ariadne Sydney Vertex Gemini 2.5 Flash — Post-Failure Provider-Blocked Diagnostic Plan

Date: 2026-07-24
Owner: GPT Sol
Authority: Yuri's fresh instruction to diagnose the failure after the occupied
rehearsal closeout
Status: closed — diagnostic accepted

## Historical integrity

The closed cache-disabled rehearsal remains immutable as
`ariadne_vertex_sydney_gemini_25_occupied_rehearsal_revision_required`. Its
primary ledger remains consumed with one call, zero retries, no candidate and
no release. This diagnostic is a new Continuity descendant. It does not
retrofit the historical bounded error, reopen the earlier conditional retry or
revise any historical node.

## Diagnostic boundary

This is provider-blocked diagnosis only. It may:

- reconstruct the exact deterministic request locally from the
  authored-synthetic constructor and verify its retained hash;
- inspect repository code, tests, schemas and immutable audit evidence;
- inspect current public official Google Vertex documentation, discovery
  metadata or installed official client types without authentication and
  without transmitting the rehearsal prompt; and
- add repository-local diagnostic evidence and focused regression tests.

It may not:

- recover or broaden the discarded provider error;
- read or refresh ADC, OAuth credentials, API keys or Google Cloud CLI state;
- call a model, inference endpoint, authenticated control plane or provider;
- transmit any prompt or model payload;
- open an occupied ledger;
- revise the request constructor for a future call;
- perform a retry or fallback;
- change credentials, IAM, APIs, billing, project, service account, logging,
  caching or other cloud state; or
- open product, database, clinical, patient, command, production, deployment
  or release authority.

Any actual request revision or further occupied call remains a fresh Yuri
decision after this diagnostic closes.

## Deterministic questions

1. Does local reconstruction produce the exact retained provider-request hash?
2. Is the exact `generateContent` method, Sydney hostname, project, location,
   publisher, model and JSON media contract supported?
3. Are every retained top-level and nested request field and type admitted by
   the current official Vertex `GenerateContentRequest` contract?
4. Does the configured structured-output schema use only the documented Vertex
   subset and supported property names for Gemini 2.5 Flash?
5. Can one specific deterministic contract defect be proved without the
   discarded provider message or another call?
6. If exact causation cannot be proved, what hypotheses are supported,
   falsified or left unresolved?

## Acceptance contract

The diagnostic passes only if it:

- makes zero credential, cloud-control and provider calls;
- proves reconstruction against the immutable request hash;
- produces a field-path/type manifest without retaining the raw prompt;
- cites only current official Google contracts for provider semantics;
- distinguishes an established defect from a ranked hypothesis;
- adds focused tests for every deterministic conclusion;
- preserves the closed rehearsal and all consumed ledgers unchanged;
- records API Spine closure for product and command surfaces;
- synchronizes Continuity, Compass and the rendered Compass report; and
- closes candidly with no retry or continuation authority.

If no deterministic defect can be proved, that is a valid diagnostic result:
the closeout must say that causation remains unresolved and must not invent a
repair.

## Verification

- focused diagnostic and existing Vertex rehearsal tests;
- repository-only Ariadne verifier;
- relevant API Spine tests;
- Continuity and Compass validation with exact rendered report;
- JSON/schema validation;
- Python compilation and static checks;
- JavaScript syntax checks where applicable;
- `git diff --check`; and
- independent task-residue checks.

No commit, push, pull request, protected-ref movement, external worker,
deployment or release is authorised.
