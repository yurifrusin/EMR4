# Raisa provider-free historical-derived scenario first-use candidate gate evaluator rehearsal — closeout

Date: 2026-08-24

Timestamp: 2026-08-24T10:07:35.8928123+10:00 (Australia/Brisbane)

Status: `accepted_evaluator_first_use_still_closed_pending_clockwork_publication`

Exact reviewed source: `abcd4206a363b0c565c070e0f2cb9c54d627b3b3`

## Lay outcome

The gate is now a real mechanism. It is a box-ticking form rather than a prose
prompt: a candidate can contain only the few structural event types and bounded
numbers the gate knows, and the result can be only blocked, revision required,
or admitted for one exact digest. Unknown fields, made-up labels and shortened
Git IDs cannot be submitted.

The rehearsal used invented candidates only. It therefore proves that the gate
works but does not yet admit or create anything from the historical Diary.

## Technical outcome

- strict Pydantic models use `extra="forbid"`, frozen values and no prose field;
- one exact 40-character accepted source and canonical SHA-256 bind the form;
- candidate digest and structural utility are independently recomputed;
- one synthetic positive case creates only an exact non-transitive binding;
- five hostile cases block and two broader/incomplete cases require revision;
- five invalid forms are schema-rejected before evaluation;
- the evaluator has no archive reader, provider call, product import or writer;
- 7 focused plus 222 existing controls pass, 229 across 25 files; and
- Ruff, compileall, typed-schema, source-boundary and Git-diff checks pass.

AER-1150 records one rejected descriptive preplanning event; AER-1151 records
one literal test/text mismatch. Both were contained before any private or
provider access and each caused one deterministic rerun only.

The actual first-use gate remains
`closed_pending_candidate_specific_evaluation`. The next tranche may perform
no private access: it adds the exact gate-wrapped first-use materialisation mode
to the clockwork's closed historical vocabulary. The existing measurement mode
forbids fixtures and cannot safely represent the intended successor. After the
new mode passes, one local-only no-retry materialisation attempt follows, and a
non-admission writes nothing.

No product, database, client, ordinary-practice, provider/model, production,
deployment, release, Pages, protected-evidence or protected-ref authority is
opened. Local/origin `master` and `handoff/current` remain exactly
`2e34bdad732fdab32fbf778280b3d3c70d66d602`.
