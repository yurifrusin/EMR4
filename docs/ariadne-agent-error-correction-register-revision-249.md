# Ariadne agent error and correction register — revision 249

Date: 2026-08-12

Revision 249 records and corrects AER-0281. The register now contains 281
bounded known incidents with none open.

The CF-D2 recovery focused implementation gate passed, but the canonical fast
profile found one baseline repository defect: the containment-order test still
looked for roadmap items numbered 17 and 18, while the accepted Context Fabric
direction already numbered the same unchanged semantic headings 19 and 20.
Exact baseline readback proves the drift preceded this recovery descendant.

The correction changes only the test's two order comparisons. They now locate
the three unique semantic headings without their mutable display numbers and
retain the same required durability-before-containment-before-product-read
order. No roadmap, authority, CF-D2 contract, SQL, harness or runtime semantic
changes.

This is also direct evidence for Yuri's requested post-CF-D2 workflow incident
diagnosis: a safety gate should bind the invariant it means to protect, not a
ceremonial presentation detail that can drift independently.
