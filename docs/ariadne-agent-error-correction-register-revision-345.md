# Ariadne agent error and correction register — revision 345

Date: 2026-08-18

Timestamp: 2026-08-18T00:19:57.3905182+10:00 (Australia/Brisbane)

Status: corrected

## Revision

Revision 345 retains 395 bounded known incidents. No incident is open.

- AER-0392 preserves the first verifier-worktree preflight's over-specific
  branch-prefix argument. The exact configured `codex/review-` prefix passed
  against the same clean candidate before any model call.
- AER-0393 preserves Gemini's exact combined-browser rejection. The product
  contract was structurally conformant; one immediate focus assertion and one
  short cleanup-reload timeout made the packet nondeterministic across suites.
  The same focus predicate now uses a bounded eventual wait, the one full-page
  cleanup reload uses 30 seconds, and the exact combined packet passes all 170
  tests without changing product code or weakening an assertion.
- AER-0394 preserves the first post-compaction receipt's missing fallback
  adapter observations and invalid Antigravity probe method. The corrected
  runtime state restores the complete allowlisted adapter block and the fresh
  five-source receipt passes.
- AER-0395 preserves the first full revision-345 register run's one stale
  direct agent-origin total. The direct seed count, ordered IDs, aggregate
  counts and existing exact-count recurrence are aligned with the regenerated
  report before the complete file is rerun.

## Boundary

These are verifier-preflight, repository test-harness and continuity-receipt
corrections only. They change no cancellation product semantics, backend, API,
database, provider or protected ref and grant no product data, deployment,
release or Pages authority.
