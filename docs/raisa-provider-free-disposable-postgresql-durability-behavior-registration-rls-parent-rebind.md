# Behavior/transaction registration-RLS parent rebind

Date: 2026-08-08

Status: deterministic candidate; runtime closed pending fresh exact-HEAD veto

The frozen behavior/transaction rehearsal remains exactly twenty ordered
authored-synthetic scenarios with category counts `6/4/3/4/3`. Attempts
001-018 remain immutable predecessor failure evidence. No scenario, principal,
fixture, transaction shape, expected SQLSTATE, readback, forbidden effect,
runtime profile or authority ceiling changes in this rebind.

The six canonical parent bindings now resolve to:

- accepted parse evidence source
  `14d145f02971e56f5751670969cb4153d49c1fb8`;
- inert SQL source `2c22d6f56d0081ebfae5a5585088381e1219d7f8`,
  1,402,659 LF bytes, 412 statements and SHA-256
  `sha256:34d321adce220a94473e3cd74173f7b0ffc37441b2e4dd24699ca18b86c7e760`;
- render manifest SHA-256
  `sha256:4ac9b851796f6460fd55844d4d3634eba62f4302bd68ce80b62b63f69cd541ea`;
- corrected structural source
  `9fb107ab598fba418b42be6d233c4960a6f29840`;
- corrected body source
  `6ae8f2c7bf3df1fe9f89b760e9d3641384848545`; and
- the unchanged synthetic prerequisite contract at accepted parse source
  `14d145f02971e56f5751670969cb4153d49c1fb8`.

Every parent is canonical UTF-8/LF hash-checked and, except the necessarily
later accepted-source ledger, read back from its exact Git source commit before
Docker resolution. The behavior contract receives no new direct Fabric grant;
the structural repair remains the exact six forced-RLS predicates required by
the already frozen `BTR-E01` lifecycle entry point.

After the complete deterministic and hostile packet plus one fresh exact-HEAD
Gemini 3.6 Flash/high veto pass, attempt 019 may run once in one newly owned
pull-never, networkless, mountless, portless, tmpfs-backed local PostgreSQL 16
container. Any mismatch fails closed and preserves evidence before repair.

The active deterministic packet contains 386 collected checks and passes in
full, together with Ruff and `git diff --check`. One broader exploratory run
also selected the frozen historical function/body continuity module; its four
assertions still expect Continuity 230, AER 81 and the then-current next-work
text, while the repository is correctly later at Continuity 233 and AER 118.
That historical equality module is preserved unchanged and excluded from this
candidate exactly because its frozen artifacts must not be regenerated to
masquerade as current acceptance evidence. It revealed no source or runtime
defect.

This rebind opens no applied migration, operational persistence or credential,
watcher/listener/feed, application/API/Diary integration, real Context Fabric
read, patient/product/protected data, provider, tool, command, deployment,
production, release, Pages or protected-ref authority. `docs/branding/` and
all unrelated untracked files remain preserved and unstaged.
