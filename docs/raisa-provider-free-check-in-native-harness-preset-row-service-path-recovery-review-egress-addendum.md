# Preset-row service-path executor review egress recovery addendum

Date: 2026-08-20

Status: `frozen`

Operation:
`raisa-provider-free-check-in-native-harness-preset-row-service-path-recovery`

## Observed failure

Two fresh Gemini 3.7 Flash/high executor-review transports preserved the exact
clean candidate, exited zero, returned non-empty stdout and no stderr, but the
Antigravity egress gate admitted zero schema-valid decision envelopes. The
first and one corrected transport are immutable non-decision evidence. Neither
is a candidate verdict and neither authorises a checkpoint.

The exact corrected twelve-command manifest was then executed once by the
existing shell-free Ariadne validation runner. All commands returned zero and
the worktree remained clean. This isolates the repeated failure to verifier
egress serialization, specifically the unnecessary requirement that an LLM
reproduce the complete exact command-result vocabulary after execution.

## Narrow recovery

Separate deterministic command ownership from independent judgment:

1. retain the immutable two egress-failure receipts;
2. bind the shell-free validation receipt containing exact argv, exit codes,
   output digests and clean pre/post Git readings;
3. dispatch one fresh Gemini 3.7 Flash/high read-only veto without a command
   manifest in its response schema;
4. require Gemini to inspect the exact executor candidate and the deterministic
   validation receipt, returning only `decision` and `review`;
5. admit a checkpoint only when the deterministic validation is `passed`, the
   split-veto receipt is a clean exact-candidate `pass`, and both artifacts are
   bound by digest.

This is not a retry of either missing decision. It removes the failed egress
coupling and assigns exact transcription to deterministic code. The independent
model retains semantic-review responsibility and Sol retains acceptance.

## Closed boundary

No native Harness process, DeepSeek/model request other than the one Gemini
review, agent, preset mount, session, turn, Docker, database, product/runtime
change, protected data, deployment, Pages or protected-ref movement is admitted
by this addendum. The eventual native process still requires its separate
clockwork checkpoint and remains one process with zero retry.
