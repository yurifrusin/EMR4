# DeepSeek native Harness provider-free stock-headless-to-custom-runner boot proof plan

Date: 2026-08-22

Timestamp: 2026-08-22T20:35:32.8728340+10:00 (Australia/Brisbane)

Status: frozen for one provider-free native Harness boot

Operation: `deepseek-native-harness-provider-free-stock-headless-to-custom-runner-boot-proof`

Reasoning level: high

## Purpose

Prove that the pinned `@deepseek-ai/dsh@0.1.0-rc.7` stock headless entry point
hands off, through its real HMR patch path, to the exact corrected 14,210-byte
integrated runner beside the accepted guard, bridge and sanitizer graph. The
runner must cross the accepted effective-tool composition coordinate and stop
at the controlled factory sentinel before any target, DeepSeek turn, worker,
model or provider request.

This is the final provider-free boot prerequisite for a later separately
frozen controlled DeepSeek development attempt. It is not another general
loader exploration and cannot become an occupied worker run.

## Exact accepted inputs

The proof binds these immutable predecessors as ancestors of the execution
candidate:

- accepted stock-headless/HMR boot source
  `560f471b72bef0b9790120238657dc7afd4d602b`;
- accepted corrected-graph materializer implementation source
  `822d487afcf6657027bc66c8d6d9635677e73096`;
- consumed materializer failure source
  `cc6d14257c7ff110a3c51476738e57f5a3135232`; and
- accepted content-free diagnosis source
  `3d82febd6a402337db606ce4767fbd4875698eba`.

The source inventory is exact:

- integrated runner: 14,210 bytes, SHA-256
  `017394e3f86a3efdf5eba0745c254a8b561615fb6ab923978b81bb5941e8e3f4`;
- four-argument guard: 4,501 bytes, SHA-256
  `76029da0f9c030651fd10c0df16f4e75e86b2269d7560af7f94c74680f8598b9`;
- bridge: 1,661 bytes, SHA-256
  `3a49b28174eeefd77d7efe0a00498901ac6636b637ed9dfe60aba46980df1d0b`;
  and
- sanitizer: 2,439 bytes, SHA-256
  `12552925a600dc951afc30b9a738746499c7e2f4cefc9962bc05fb06780f158f`.

The accepted observation means **four reads of `presets.roots` and five hook
installations**, not four distinct roots. The controlled preset roster remains
the exact two-root system/user roster used by the accepted materializer.

The pinned package name, version, package-declared `lib/bin.js`, tarball
integrity, `--profile headless`, Node `--expose-internals`, verified offline
package seed, stock readiness sentinel and network guard remain byte-bound to
their accepted sources. No online package fallback or lifecycle script is
allowed.

## One disposable assembly

The controller creates one exact child beneath the existing worker-root parent
and copies the already verified provider-free rc.7 package seed without npm or
network activity. It materializes only:

- byte-identical copies of the runner, guard, bridge and sanitizer;
- one closed boot-probe adapter derived from the accepted materializer fixture;
- the accepted stock-headless readiness sentinel and network guard; and
- an initial and changed HMR patch.

The initial patch disables `headless-runner`, `code-runtime` and
`session-telemetry-otel` and mounts only the readiness sentinel. The changed
patch adds exactly the closed boot probe. Accepted canonical sources and the
package seed remain immutable.

## Frozen lifecycle

1. Deterministic preflight validates the plan, threat delta, contract, schemas,
   source bytes, complete import closure, installed package tree, patch rows,
   child environment, zero canonical output identity and absent disposable
   root.
2. The attempt identity is consumed immediately before native launch.
3. Exactly one native process invokes the pinned package-declared stock
   headless entry point with `--expose-internals` and a scrubbed credential
   environment.
4. The in-process sentinel alone records `sentinel_activated` and
   `stock_headless_hmr_ready` after both stock watched patches are registered.
5. The controller performs exactly one atomic HMR mutation adding the closed
   boot probe.
6. The boot probe creates one local scoped rc.7 `AgentRegistry`, supplies the
   accepted two-root preset service and exact three-tool view, then calls the
   byte-identical integrated runner beside the corrected graph.
7. The runner completes the guard, restriction and schema checks. Its factory
   records `EFFECTIVE_TOOL_COMPOSITION_PASSED`, throws the controlled
   post-guard sentinel and writes the generic `failed/factory` terminal.
8. The boot probe writes one exclusive typed observation, disposes its local
   scope and requests stock application exit 0.
9. The controller admits only the exact typed observation, validates zero
   activity gauges, waits for process absence, removes the exact disposable
   root and proves source/seed immutability before publishing evidence.

Stdout and stderr are discarded, never parsed and never retained. The readiness
ledger, typed observation and runner terminal are the only admitted child-to-
controller communication surfaces.

## Exact accepted terminal

The only passing controller result is
`stock_headless_handed_off_to_accepted_guard_graph_runner`. It requires:

- readiness order exactly `sentinel_activated`,
  `stock_headless_hmr_ready` and exactly one HMR mutation;
- exactly one native process, one `AgentRegistry.create` transit and one setup
  invocation;
- setup resolved with `preset_root_reads: 4`, `preset_mount_reads: 1`,
  `preset_mount_calls: 1`, `tool_view_calls: 1`,
  `tool_restrict_calls: 1`, `tool_schema_calls: 1` and
  `hook_installations: 5`;
- structured coordinate `EFFECTIVE_TOOL_COMPOSITION_PASSED` and no old
  input-invalid coordinate;
- runner terminal `failed/factory` only from the controlled sentinel, app-exit
  code 1 inside the isolated runner context, then stock process exit 0;
- zero target, live agent, session, turn, tool call/result, broker process or
  request, worker, model, provider, network, database and Docker activity;
- zero retry, resume and fallback; and
- exact execution-copy equality, canonical-source/package-seed immutability,
  process absence and disposable-root absence.

Any other source, import, readiness, mutation, typed field, counter, terminal,
exit or cleanup reading fails closed.

## Owned paths

- this plan and its threat-model delta;
- one operation controller and one focused test;
- one operation continuity root with contract, schemas, boot-probe source,
  consumed identity, typed evidence and report; and
- operation-specific receipts, register correction if required, closeout,
  paired Yuri summary and notification receipt.

No accepted predecessor, package seed, product path or protected ref may be
modified.

## Verification and acceptance

Before native launch, focused hostile tests must reject wrong package/source
bindings, incomplete import closure, altered patch roster, early/synthetic
readiness, an extra process, raw-stream handling, credential exposure, network
activity, counter drift, surplus fields, ambiguous terminal and incomplete
cleanup. Ruff, Python compilation, JSON Schema, Git whitespace and the relevant
accepted stock-boot/materializer/diagnosis/latch/Baton gates must pass.

After launch, the consumed identity and typed terminal are immutable. A pass
admits only the stock-headless handoff and corrected guard-graph boot coordinate.
A failure closes as bounded negative evidence and does not select a retry.

Gemini will be reassessed at the exact deterministic candidate. The active
provider-free latch authorises no model/provider request, so absent a genuine
semantic ambiguity it remains declined and typed deterministic evidence owns
acceptance.

## Parallelism efficacy assessment

- DeepSeek: declined as a worker and model because its native Harness is the
  provider-free subject under test; no DeepSeek turn is authorised.
- Gemini: reserved only for the required pre-verifier reassessment and expected
  to be declined while the absolute provider-free/model-zero boundary remains
  sufficient and unambiguous.
- Native subagents: declined under current developer policy and because one
  process, readiness, terminal and cleanup form a serial custody chain.
- GPT Sol owns the contract, implementation, deterministic verification, one
  native attempt, acceptance and closeout.
- Parallel work packages: none.

## Recovery

A deterministic construction or source-binding rejection before native launch
may be corrected mechanically and rerun because it consumes no process. Once
the native process starts, attempt 001 is consumed. There is no automatic or
manual retry, resume, fallback or second boot in this tranche.

## Decision after closeout

A pass ends provider-free Harness boot testing and selects a separately frozen
small authored-synthetic controlled DeepSeek development rehearsal that measures
useful work, traceability and correction cost. A non-pass preserves its exact
coordinate and requires a distinct evidence-led decision; it does not justify
another generic boot circle.

## Explicitly closed

No target, DeepSeek worker/turn, broker, model/provider request, product source/
configuration/API/database/route/adapter/flag/allowlist/grammar/client/waiting-
area change, ordinary-practice enablement, generic-status `Arrived` change,
patient/product/appointment/clinical/historical/protected data, production
runtime, deployment, release, Pages, protected evidence or protected-ref
movement. Local/origin `master` and `handoff/current` remain exactly
`2e34bdad732fdab32fbf778280b3d3c70d66d602`. Preserve `docs/branding/` and
all unrelated untracked files. Stage explicit paths only; never use
`git add .` or `git add -A`.
