# Provider-free unmounted observation-to-temporal-signal rehearsal closeout

Date: 2026-08-06

Status: accepted

Result:
`raisa_provider_free_unmounted_authored_synthetic_observation_to_temporal_signal_rehearsal_pass`

Exact independently reviewed source HEAD:
`c0502c398df4a56c9558bc68eddedb2adf20d12d`

## Outcome

The first pure observation-to-temporal-signal vertical now passes over closed
authored-synthetic metadata. A default-off, zero-effect synthetic activation
admits one exact committed-change observation, derives a scope-bound keyed
identity without releasing the raw identifier or key, maps through registered
aliases and a backend-owned impact floor, and hands one existing typed temporal
signal to the accepted temporal processor.

The ordinary positive path classifies the immutable prior frame set as
`REASSEMBLY_REQUIRED` and emits an inert reassembly requirement. It neither
reads fresh truth nor mounts or advances a source, listener or durable cursor.

## Acceptance reconciliation

- practice, source system, source contract, observer identity/generation,
  policy, binding, registry, impact and prior coordinate are exact and sealed;
- raw source identifiers become domain-separated HMAC observation identities
  and are discarded with the key;
- alternate grammatical raw identifiers, valid prior seen-ID sets and every
  admitted two-sided clock ordering map from their actual validated inputs;
- exact minus/plus 120-second skew admits and maps, while minus/plus 121
  seconds deterministically rejects;
- duplicate, replay, gap, baseline, revision, restart, overflow, foreign scope,
  schema and activation failures use closed deterministic outcomes;
- source metadata cannot narrow the mandatory Diary and waiting-room frame
  floor, and unresolved impact requires bounded full invalidation;
- coordinated resealing of authority, policy, aliases, impact or observation
  links fails closed;
- admission and mapping remain internal; the sole public signal-bearing builder
  releases only after same-packet deterministic proofreading; and
- no source payload becomes Context Fabric truth and every authority/effect
  ceiling remains false.

The first independent veto rejected the worker candidate for canonical-fixture
overfit and a public proofreader bypass. Sol invoked the named recovery lease.
A second fresh veto then rejected the first recovery's one-sided interpretation
of the two-sided clock-skew contract. Both failures remain immutable in AER-0046
and AER-0047. The final genuinely fresh exact-head veto found no P0-P2 issue.

The final reviewer and orchestrator packets passed 93 focused, 69 inherited
temporal/API/architecture and 65 register/handover checks: 227 in total.
Compilation, Ruff, Draft 2020-12 schema validation, three-artifact byte
reproduction and diff checks passed, with an unchanged clean review worktree.

## Preserved evidence

All user-owned untracked files, especially `docs/branding/`, and every unrelated
Gate -1, Consultant, receipt/state, evidence and cost-ledger artifact remain
preserved and excluded.

## Claim boundary

This proves only a provider-free, unmounted, authored-synthetic contract
rehearsal. It proves no live observation or delivery, database/outbox/feed/
watcher/listener, durable transaction position or checkpoint, restart recovery,
source/product read, patient privacy control, provider behavior, API route,
command/write, runtime, deployment, production, release, Pages operation or
protected-ref authority.

## Next safe descendant

Under Yuri's standing uninterrupted-gate authority, proceed directly to a
provider-free unmounted source-specific durability architecture for the
already-defined patient-free `diary.appointment_rescheduled.v1` committed-event
family. It may freeze the exact integration principal, durable monotonic
transaction-position/outbox coordinate, atomic decision-invalidation-checkpoint
transaction, restart/gap/retention behavior, key rotation and audit fields as
architecture and authored-synthetic contracts only.

It may not mount or contact the database, event source, outbox, feed, watcher or
listener; read product data; handle patient/product/protected data; persist or
advance an operational cursor; add a route; call a provider; perform a command
or write; wire runtime; deploy, release, rebuild Pages or move protected refs.
Any live implementation and exact operational credential remain later gates.
