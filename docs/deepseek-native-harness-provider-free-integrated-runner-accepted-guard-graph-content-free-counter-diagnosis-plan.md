# DeepSeek native Harness provider-free integrated-runner accepted guard-graph content-free counter diagnosis plan

Date: 2026-08-22

Timestamp: 2026-08-22T20:13:02.0990867+10:00 (Australia/Brisbane)

Status: frozen zero-process deterministic diagnosis

Operation: `deepseek-native-harness-provider-free-integrated-runner-accepted-guard-graph-content-free-counter-diagnosis`

Reasoning level: high

## Purpose

Determine what typed JSON the consumed materialization fixture emitted without
rerunning it or recovering its raw stream. The diagnosis treats the retained
stdout byte count and SHA-256 as a clockwork reading over a closed,
source-derived finite grammar.

This is not general SHA-256 inversion. It is exhaustive comparison of a small
finite set of exact JSON serializations admitted by immutable source and the
single-process control flow.

## Exact immutable inputs

The controller must bind:

- the process envelope: 522 bytes, SHA-256
  `e1696656a7088010808be12f82713f67ee22495a81a861f4d7e7d8ee4fc78c48`;
- the failure terminal: 352 bytes, SHA-256
  `3b07178ca74d0c357a7123792a5c21afc7a7a6e3a4c370467ca14b2061739fa5`;
- the consumed identity: 338 bytes, SHA-256
  `bc3d13c9d72a1a612ab213d2755250f051d8beae91fd4faeebfeee5dfc446e7d`;
- the exact emitted fixture source: 5,165 bytes, SHA-256
  `81359ea4357b9f6b18d896f8681651582665ab7855e6bc5bb749937ca228fdc3`;
- the byte-identical runner and accepted graph inventory already bound by the
  predecessor contract; and
- installed rc.7 `dsh-agent/lib/index.js`: 31,560 bytes, SHA-256
  `e7e40c5ca66d9827a5084c5c0c68983f9685842bb9b6d604803d4cb4642bb263`.

The process envelope must say exit 0, stdout 756 bytes with SHA-256
`6e75c083f6b42d5c828d53c7f16a11ae09897023bf0a8139abde615c674225ff`,
stderr 0, raw stream not retained and one Node process. The terminal must be
`fixture_result_rejected`; retry remains zero.

## Source-derived grammar

The exact result-object key order is frozen from `fixture_source()`. Constants,
nulls, booleans and strings use JavaScript `JSON.stringify`-equivalent compact
UTF-8 serialization followed by one LF.

The finite counter ceilings are source-derived:

- one runner creation/setup/scope-disposal/app-exit transit;
- four `presets.roots` reads in the exact runner root predicate;
- one mount handle read/call and one view/restrict/schema call;
- three runner-owned `agentCtx.on` calls plus two exact installed
  `installModelSelection` calls, for at most five hook installations; and
- zero request, tool-result, turn, conclusion and live-agent activity because
  every admitted path stops at or before the controlled factory sentinel.

The grammar conservatively enumerates both the success-coordinate branch and
bounded failure-coordinate shapes over those finite domains. It admits no
free-form string, number, key, ordering, exception, path, prompt, response,
environment or raw stream field.

## Exact acceptance

The controller must enumerate the complete grammar in process and write only
closed aggregate evidence. Acceptance requires:

- zero subprocess, native Harness, broker, worker, model, provider, network,
  database, Docker or product-target activity;
- the complete candidate count equals the contract value;
- exactly one candidate has 756 serialized bytes;
- exactly one candidate has both 756 bytes and the retained SHA-256;
- the matching candidate is the success-coordinate shape with
  `preset_root_reads: 4`, `hook_installations: 5`, setup resolved, one
  mount/view/restrict/schema operation, failed/factory terminal from the
  controlled sentinel, and every request/tool/turn counter zero; and
- an independent direct reconstruction of that candidate produces the same
  bytes and digest.

Zero matches, multiple matches, a source/domain drift or any unexpected field
fails closed.

## Claim boundary

A pass admits only the uniquely reconstructed typed observation and proves
that the accepted guard graph crossed the old input-invalid coordinate before
the controlled factory sentinel. The original materialization attempt remains
an immutable rejected terminal because its frozen validator rejected; the
diagnosis does not rewrite or reclassify it.

A pass may select the separately frozen provider-free stock-headless/custom-
runner boot proof using the corrected five-hook/four-root counter contract. It
does not authorise a DeepSeek turn, model/provider request or product work.

## Owned paths

- this plan and threat-model delta;
- one zero-process controller and focused test;
- one operation continuity root with contract, closed schemas, evidence and
  report; and
- operation-specific receipts and closeout artifacts.

All predecessor sources, consumed identities and protected evidence remain
immutable.

## Parallelism efficacy assessment

- DeepSeek: declined because every worker, Harness, model and provider request
  is forbidden and the work is deterministic acceptance logic.
- Gemini: declined because the provider-free boundary forbids a model request
  and the result is completely determined by immutable bytes and a closed
  grammar.
- Native subagents: declined under current developer policy and because source
  binding, enumeration and uniqueness admission form one serial proof.
- GPT Sol owns grammar construction, exhaustive enumeration, acceptance and
  closeout.
- Parallel work packages: none.

## Recovery

Source, schema, grammar or uniqueness rejection may be corrected mechanically
because the tranche starts no process and consumes no runtime identity. It may
never trigger a materialization retry or raw-stream recovery.

## Explicitly closed

No subprocess, native Harness, DeepSeek worker/turn, broker, model/provider
request, raw stream, predecessor rewrite/reclassification, product/
configuration/API/database/route/adapter/flag/allowlist/grammar/client/
waiting-area change, ordinary-practice enablement, generic-status `Arrived`
change, patient/product/appointment/clinical/historical/protected data,
production runtime, deployment, release, Pages, protected evidence or
protected-ref movement. Local/origin `master` and `handoff/current` remain
exactly `2e34bdad732fdab32fbf778280b3d3c70d66d602`. Preserve `docs/branding/`
and unrelated untracked files. Stage explicit paths only; never use
`git add .` or `git add -A`.
