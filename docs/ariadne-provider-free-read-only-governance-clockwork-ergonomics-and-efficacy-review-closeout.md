# Provider-free read-only governance clockwork ergonomics and efficacy review — closeout

Date: 2026-08-23

Timestamp: 2026-08-23T15:59:03.2163529+10:00 (Australia/Brisbane)

Status: `accepted_pending_clockwork_publication`

Exact reviewed source:
`5971500fcef2de4970fa204a7bb50a94358c0dde`

## Lay outcome

The clockwork is worth keeping. Its safety mechanism is doing exactly what we
wanted: one writer owns the records, mistakes stop safely, accepted state does
not drift, and a bad publication can be restored byte-for-byte.

The control panel is too laborious. The last two closeout forms contain 73,459
bytes, 1,173 lines and 785 scalar entries. Across four accepted reviews, 31
disclosed lapses were procedural and none was a product-semantic defect. In the
latest packet, three rollbacks were caused by facts the repository could have
validated or counted before moving the pointer.

The first ergonomic repair should therefore strengthen the existing tick, not
loosen it or build a parallel system: validate the complete prospective human
evidence set, return all errors together, and make transaction accounting a
machine-owned output.

## Technical result

- the live-clockwork window contains 99 accepted nodes, 66 with
  `native-harness` in the identifier;
- the accepted native synthesis covers 70 nodes and supports better terminal
  attribution than retained Claude failures, but not a comparative reliability
  rate or useful-completion superiority;
- no controlled native EMR4 candidate has been accepted;
- the four-review friction sample contains 31 procedural lapses: 17 command
  shape, seven prospective projection, five closed vocabulary, one derived
  fact and one self-referential accounting;
- 27 were contained before durable acceptance and four required byte-exact
  rollback;
- the last two closeouts used six publication attempts, four rollbacks and no
  bespoke updater;
- incident observations account for 59.0% of the sampled scalar leaves; and
- the latest product interval was 29.05 minutes while its closeout interval was
  29.78 minutes, using commit timestamps only as bounded proxies.

The existing three Harness/clockwork readings—`prepared`, `terminal`, and
`accepted_or_recovered`—are retained. Fine-grained per-turn or subcoordinate
clockwork coupling remains retired. The pinned native occupied profile remains
paused and Claude Code is not a silent fallback.

The review also records a stale-prose inconsistency: the operation-specific
live Baton pauses native occupied use and forbids silent Claude fallback, while
generic Authority Allocation still describes Claude Code as preferred. The
specific active control governs. Reconciliation is not folded into the first
ergonomic repair.

## Next tranche

Proceed under standing authority with
`ariadne-provider-free-governance-clockwork-prospective-current-node-evidence-and-transaction-fact-derivation-repair`.
It may change only the existing clockwork implementation and focused tests to:

- validate the complete prospective plan/closeout/acceptance evidence set
  before live publication;
- return the complete prospective error set in one preparation pass; and
- make attempt, lease, generation and rollback accounting generated output.

It must add zero operator fields, approvals, gates or closeout documents and
must preserve the single writer, full-Git binding, protected-boundary floor,
pointer-last publication and byte-exact rollback.

No Harness/provider, product, check-in operational choice, runtime, deployment,
Pages or protected-ref authority opens.
