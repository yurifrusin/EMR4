# Reception One Bureau live isolated-planner evaluation plan

Status: authorised bounded occupied evaluation
Recorded: 2026-07-31
Predecessor: `reception-one-bureau-runtime-ui-wiring`

## Authority

Yuri explicitly granted the exact authority requested after the provider-free
Bureau UI result:

- one fresh authored-synthetic end-to-end occupied request;
- initiated through the visible **Isolated model** selector;
- Google Vertex AI `gemini-2.5-flash`;
- project `bernie-emr4-dev`;
- exact Bernie service account
  `emr4-bernie-ai-dev@bernie-emr4-dev.iam.gserviceaccount.com`;
- existing keyless impersonated service-account ADC;
- `australia-southeast1` and
  `australia-southeast1-aiplatform.googleapis.com`;
- deterministic proofreader as the sole release gate;
- no provider, model, project, identity, region or deterministic fallback;
- proposal-only output requiring human confirmation;
- no appointment write or confirmation; and
- application cost ceiling USD 1.

This descendant has an absolute occupied provider-call ceiling of one. A
proofreader correction ticket may be recorded, but it must terminate without a
second call. Any retry requires fresh authority.

## Objective

Exercise the complete operator-visible development path as one retained result:

1. load the real local Diary and Reception One Bureau;
2. expose the development planner control only through the existing explicit
   authored-synthetic gates;
3. select **Isolated model** visibly;
4. submit one small authored-synthetic request referring to the current
   synthetic Diary context;
5. traverse the authenticated FastAPI proposal route;
6. let the trusted backend construct the minimal practice-scoped context frame;
7. pass only opaque typed context into the credential-free isolated cell;
8. let the one-use broker make at most one exact Sydney Vertex call;
9. release only a proofreader-admitted typed proposal; and
10. render only the admitted proposal and bounded provenance in Reception One.

The frozen request asks to extend the uniquely resolved authored-synthetic
Margaret Thompson / Dr Alex Shera appointment to 45 minutes. The retained audit
records only its request hash, typed field manifest and admitted synthetic
values, not the raw prompt or provider response.

## API Spine boundary

This remains an authenticated command-style read that prepares a proposal.
The backend owns practice scope, identity resolution, current appointment
truth, freshness, proofreading and proposal adaptation.

The browser and model gain no confirmation or mutation authority. Any admitted
response must retain:

- `requires_confirmation=true`;
- `proposal_only=true`;
- `write_performed=false`;
- `confirmation_performed=false`;
- `model_database_access=false`;
- `planner_mode=isolated_vertex`;
- `provider_calls=1`; and
- an opaque runtime audit reference.

If the model, proofreader or adapter does not admit the exact typed result, the
route fails closed or returns a non-released disposition. It must not call the
model again, fall back to the deterministic planner or display an unverified
draft.

## Credential and provider boundary

Fresh preflight must prove:

- `google.auth.default()` returns impersonated credentials;
- exact project and target service account;
- cloud-platform scope;
- exact prediction-only custom role and
  `aiplatform.endpoints.predict` permission;
- Vertex Data Access read/write audit logging;
- request-response logging disabled or absent;
- provider in-memory cache disabled;
- no user-managed service-account key;
- Vertex AI API and billing enabled;
- `gemini-2.5-flash` present in the publisher catalogue; and
- no API-key or global/fallback path.

No API-key variable is read or reported. Provider-key variables are omitted
from child environments. The cell receives no ADC, OAuth token, Google CLI
configuration, database credential, raw UUID, handle map or product network
path. Only the host broker may use the existing impersonated ADC and regional
Vertex endpoint.

## Deterministic pre-attempt gates

Before the occupied call:

1. provider-free UI, dual-planner, proofreader and API Spine tests pass;
2. the one-call ceiling has a focused regression test;
3. provider-blocked request construction passes;
4. real isolation passes with no credentials or external network in the cell;
5. the fresh read-only cloud-control preflight passes;
6. Continuity revision 158 and Compass revision 139 validate together;
7. the rendered Compass report matches those revisions;
8. the exact disposable database, ports, runtime directory, containers,
   images and network are absent; and
9. a pre-attempt receipt binds the same exact authority and controls.

## Occupied acceptance

The result passes only if:

- the visible selector submitted `isolated_vertex`;
- exactly one non-intercepted UI route request reached the real local backend;
- exactly one provider call used the exact Sydney/Bernie binding;
- the provider response was handled by the frozen typed contract;
- the deterministic proofreader admitted the result without unsafe repair;
- only schema-admitted fields and bounded provenance rendered;
- the result remained a human-confirmation-required proposal;
- database truth was unchanged;
- all ledgers were consumed;
- no retry or fallback occurred; and
- browser, backend, cell, broker, container, image, network, token, temporary
  runtime and disposable database cleanup passed.

A provider, proofreader or request failure still consumes the sole call and
must close out candidly without retry.

## Evidence and exclusions

The external audit may retain attempt/ledger identifiers, exact provider
binding, authentication class, endpoint hostname, request/schema hashes,
status, latency, token usage, hash chain, proofreader disposition, admitted
field manifest, admitted authored-synthetic typed values, cleanup and explicit
exclusions.

It must not retain raw prompts, raw provider responses, credentials, API-key
information, hidden reasoning, rejected drafts or unconstrained provider
messages.

This evaluation does not authorise real, product-derived, patient, health,
clinical or historical data; confirmation; writes; participant sessions;
voice; Word; production; deployment; release; provider substitution; or a
claim of Australian physical or sovereign processing.
