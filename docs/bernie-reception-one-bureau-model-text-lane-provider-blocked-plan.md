# Reception One Bureau Model Text Lane — Provider-Blocked Plan

## Authority and target

Yuri authorised continuing from the accepted Reception One Bureau Typed Plan
Protocol into the next logical tranche on 2026-07-29. This descendant is
limited to an authored-synthetic, repository-local and provider-blocked model
text lane. Its target result is:

`reception_one_bureau_model_text_lane_provider_blocked_pass`

This plan does not authorise an occupied model call. It does not read
credentials, contact Google Cloud, connect to product context, access a
database, invoke an API command, write an appointment, deploy, release, commit,
push or move a protected ref.

## Readiness gate

The mandatory runtime readiness command is:

`scripts/bernie_interpretation_readiness_check.py`

The observed gate remains:

- `runtime_or_provider_wiring_ready=false`
- `raw_trove_access_ready=false`
- `runtime_gate_decision=blocked`

The provider-boundary report remains:

- `default_provider=disabled`
- `live_provider_enabled=false`
- `provider_calls_performed=false`
- `route_behavior_changed=false`
- `database_access_performed=false`
- `memory_or_rag_access_performed=false`
- `historical_diary_material_access_performed=false`

Those are deliberate constraints, not defects to work around.

## Frozen architecture

1. A trusted host constructs a minimal authored-synthetic interpretation frame
   from the accepted typed-plan input contract.
2. The frame contains the receptionist utterance, the closed fourteen-operator
   catalogue and opaque source handles. It contains no credential, Google
   identity, API key, provider configuration or command capability.
3. An untrusted cell may return only a `ModelPlanCandidate`: a goal and a
   bounded ordered list of catalogue operators whose arguments reference
   allowlisted source handles or earlier typed step outputs.
4. Trusted host code attaches the immutable request, practice, correlation and
   context-revision envelope. The cell cannot author those values.
5. The existing deterministic proofreader validates exact schema, grounding,
   typed dataflow, topology, freshness, effect ceiling and authority. Only an
   admitted plan may reach the existing in-memory proposal executor.
6. Revision feedback contains only allowlisted field paths and reason codes.
   It never contains a raw draft, prompt, provider response or provider error.
7. The isolated provider-free rehearsal uses a disposable non-root,
   read-only, no-network container with bounded resources and no credentials.
   Its authored fixture output is treated exactly as untrusted model output.

The model may compose a novel plan from typed operators. It may not invent an
operator, literal value, identity, source handle, completed action or effect.

## Provider-blocked request contract

For compatibility testing only, the inert request constructor is bound to the
current official Vertex AI coordinate:

- provider: Google Cloud Vertex AI
- candidate model: `gemini-2.5-flash`
- location: `australia-southeast1`
- regional hostname:
  `australia-southeast1-aiplatform.googleapis.com`
- authentication class for any later separately authorised occupied call:
  `keyless_impersonated_service_account_adc`

The request enables JSON structured output, deterministic sampling, a bounded
output budget and zero thinking budget. It contains no tools, function calls,
grounding, retrieval, cache reference, fallback or global endpoint.

These coordinates are a blocked compatibility profile only. An occupied call
requires a fresh exact Yuri authority naming the provider, model, project,
service account, authentication class, location, endpoint, isolation, audit,
cost, call ceiling, data class and stop boundaries. The authority request
packet is durable but inert.

## Acceptance

The tranche passes only if:

- the model candidate and model-input schemas are closed and validate;
- host envelope attachment prevents model-authored scope;
- positive create and novel squeeze-in candidates are admitted;
- deterministic execution remains proposal-only and in memory;
- unknown operators, literals, forged scope, stale context, forward
  references, excess steps and write-shaped output fail closed;
- one bounded correction dialogue is path/code-only;
- the Vertex request contract is inert and provider-blocked;
- the isolated fixture cell is non-root, read-only, no-network,
  resource-bounded, credential-free and leaves no owned residue;
- repository-only, API Spine, schema, Python, Continuity, Compass and hygiene
  verification pass; and
- evidence records zero provider calls, zero credential reads, zero database
  access, zero writes and zero product delivery.

## Stop boundary

Stop for Yuri before any occupied provider call, product-derived context,
patient or health information, raw historical Diary material, database/API
runtime connection, command execution, appointment confirmation or write,
voice, participant work, production, deployment, release, commit, push or
protected-ref movement.
