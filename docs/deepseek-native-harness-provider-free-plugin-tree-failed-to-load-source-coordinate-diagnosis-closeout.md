# DeepSeek native Harness provider-free plugin-tree source-coordinate diagnosis closeout

Date: 2026-08-21
Timestamp: 2026-08-21T13:26:54.8506659+10:00 (Australia/Brisbane)
Reviewed source: `f735e6c9f4412aea8e83e410c0292668ebe7853f`

## Outcome

Accepted as `unique_supported_coordinate`.

The exact local `@deepseek-ai/dsh@0.1.0-rc.7` source, the immutable attempt-004
structured terminal, the accepted preset and the already passing provider-free
complete-composition predecessor support exactly one initial failure branch:

`profile_patch.initial.synthetic-worker-hmr-sentinel.name:absolute_windows_path_not_normalized_to_relative_or_file_url_before_loader_import`

The current profile author gives the rc7 loader an absolute Windows filesystem
path for the sentinel module. The later runner row uses the same form. Rc7's
unbased root `Include` takes that supplied name into loader logic whose special
handling is reserved for dot-relative names. The observed four-node sanitized
chain matches the boot wrapper, root-apply wrapper, loader-import wrapper and
closed-coded underlying error. The accepted passing predecessor used the same
sentinel and runner as profile-relative module specifiers.

This is sufficient to justify a separate provider-free repair of those two
specifier rows. It does not prove a repaired boot or DeepSeek reachability and
does not authorise another occupied attempt.

## Evidence and verification

- exact package manifest SHA-256:
  `7a9f356ad1e27c7013b44619bc675b8cb877f995cd0951ab3dfeb10d4edcc361`;
- accepted predecessor evidence SHA-256:
  `9ba784b0726addb5644ac3786def410aed56e5bf9da3e23ec21d8e10f6ba1ea0`;
- deterministic evidence verdict: one matching branch, owner `profile_input`,
  repair justified;
- focused diagnosis tests: 8 passed;
- widened terminal/controller/observability regression: 58 passed after exact
  repository-path resolution and the documented historical-latch deselection;
- JSON Schema, Ruff, Python compilation and staged diff checks passed; and
- Node, Harness, broker, worker, model, provider and network activity: zero.

Three rejected procedure readings are retained as evidence. One
historical attempt-004 test correctly rejected the successor latch, so it was
deselected rather than weakened or made to roll the latch backwards. A later
grouped command used two mnemonic filenames that did not exist; `rg --files`
resolved the exact inventory and the corrected 58-test command passed. The
first read-only closeout check also rejected a bare `contract_evidence` path;
the corrected node uses an empty list because it makes no cross-contract claim.

## Parallelism efficacy

DeepSeek was declined because this latch prohibited Harness, model and provider
activity. Gemini was declined because provider review was forbidden and the
claim is deterministic source binding. Native subagents were declined because
the proof had one serial owner and current developer policy prohibits proactive
delegation. GPT Sol owned the source proof. Reassess independent review after a
separately frozen repair candidate exists.

## Boundaries preserved

No Harness, broker, worker, model, provider or network process was launched.
No raw message, error code, stack, path, stream, session or credential was
reconstructed. Attempts 001-004 remain immutable and consumed. No product,
patient, appointment, clinical or protected data was used. No product source,
route, adapter, flag, allowlist, grammar, client or waiting-area behavior
changed. No production, deployment, release, Pages or protected ref moved.
`docs/branding/` and every unrelated untracked file remain preserved.

## Next operation

Freeze `deepseek-native-harness-provider-free-proof-module-relative-specifier-repair`.
It may change only the generated sentinel and runner module names from
root-specific absolute Windows paths to the exact profile-relative specifiers
already proven by the passing predecessor, plus deterministic regression
evidence. It may not launch Node or Harness and may not retry an occupied
worker. A native boot proof remains a later, separately frozen operation.
