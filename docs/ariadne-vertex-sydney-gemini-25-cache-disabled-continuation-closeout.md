# Ariadne Sydney Vertex Gemini 2.5 Flash — Cache-Disabled Continuation Closeout

Date: 2026-07-24
Result:
`ariadne_vertex_sydney_gemini_25_occupied_rehearsal_revision_required`
Occupied calls: one primary, zero retries
Fallbacks: zero

## Outcome

The conditional sequence advanced through the complete read-only Tranche 3
preflight and the provider-free Tranche 4 real-isolation lifecycle. It then
made exactly one primary authored-synthetic request to:

`https://australia-southeast1-aiplatform.googleapis.com/v1/projects/bernie-emr4-dev/locations/australia-southeast1/publishers/google/models/gemini-2.5-flash:generateContent`

The broker used the existing keyless impersonated Application Default
Credentials for:

`emr4-bernie-ai-dev@bernie-emr4-dev.iam.gserviceaccount.com`

Google Vertex returned HTTP 400 `INVALID_ARGUMENT` before a candidate. The
bounded error record contains no allowlisted field-violation path, so it does
not identify a deterministic request-contract defect. The conditionally
authorised repaired retry is therefore not eligible and was not used.

No proofreader input, model-generated typed value, safe repair or release
exists. No provider, model, project, identity, credential, region or endpoint
fallback occurred.

## Tranche disposition

| Tranche | Result |
|---|---|
| 1. Residency and provider admission | passed for exact `gemini-2.5-flash` and published Sydney locational support |
| 2. Provider-blocked contracts | passed; the relay alias and effective resource bounds were strengthened with focused regressions |
| 3. Existing ADC and entitlement | passed twice at the continuation and pre-attempt boundaries, including explicit project cache disablement |
| 4. Provider-free real isolation | passed with one accepted dry-run lifecycle, deterministic proofreader release and complete cleanup |
| 5. Occupied Australian Vertex rehearsal | one primary call made; HTTP 400 `INVALID_ARGUMENT`; revision required |
| 6. Proofreader and release | not reached; no draft or release |
| 7. External audit and closeout | passed for the fail-closed result |

## Real-isolation evidence

The accepted dry run proves the effective local boundary:

- the cell ran as `65532:65532` with a read-only root filesystem;
- memory, swap, CPU, process and file-descriptor ceilings were enforced;
- all capabilities were dropped and `no-new-privileges` was active;
- the cell used only the task-internal network and had no mounts, published
  ports, Docker socket, product data, database or credential material;
- the exact-path relay alone joined the internal and bridge networks;
- the broker alone held the ephemeral relay token and the host ADC boundary;
- no provider or ADC path was contacted in dry-run mode;
- the one-use dry ledger was consumed;
- the deterministic proofreader admitted the exact four authored-synthetic
  fields and made only enum-casing and deterministic-order repairs; and
- every task container, network, image, token, temporary context and broker
  process was removed.

One host execution interruption and six provider-free setup diagnostics
preceded the accepted dry run. They made zero provider calls. Every opened
zero-call ledger was preserved, consumed and bound to a valid audit chain
before the next provider-free attempt.

## Occupied-call audit

The seven-event durable chain verifies:

1. broker ready under the exact provider/model/project/identity/region;
2. typed cell request admitted;
3. occupied ledger consumed with one call reserved;
4. exact regional provider request constructed;
5. one provider call started;
6. bounded HTTP 400 failure recorded; and
7. broker rejected without retry.

The retained provider-error fields are only HTTP status 400, bounded numeric
code 400, normalized status `INVALID_ARGUMENT`, an empty allowlisted
field-path list, the constant redacted diagnostic and a hash of discarded raw
bytes. The raw error, free-form provider text, prompt and response were not
retained.

Latency, token usage, model version and actual provider billing were not
supplied by the failed response and are not claimed. The request remained
within the frozen 256-output-token and USD 1 application ceilings.

## Authentication and cloud controls

The pre-attempt read-only evidence verifies the exact impersonated credential
type, Bernie project, target service account, cloud-platform scope and
non-interactive refresh. It also verifies the existing billing and Vertex API
state, exact prediction-only role/binding/permission, Vertex Data Access
audit logging, absent or disabled request-response logging, explicit
project-level `disableCache: true`, zero user-managed service-account keys,
current publisher catalogue presence and the exact Sydney endpoint with no
automatic fallback.

No external credential, API, billing, IAM, project, service-account, active
human account, logging or cache configuration was changed by Codex. API-key
authentication was not used.

## Cleanup and accounting

- occupied calls consumed: one;
- occupied retries: zero;
- provider or regional fallbacks: zero;
- occupied ledger: consumed;
- provider-free ledgers: all consumed;
- task containers, networks, images and broker processes: zero;
- temporary contexts and relay tokens: zero;
- model-generated releases: zero; and
- product, database, clinical, patient, command, production, deployment and
  human delivery: none.

## What this proves

The evidence proves that one small authored-synthetic request followed the
verified Bernie impersonated-ADC boundary to the exact configured
`australia-southeast1` Vertex hostname; that Vertex returned a bounded HTTP
400 before a candidate; that the cell remained credential-free and locally
isolated; that no draft was released; and that the ledger, audit and cleanup
boundaries held.

It does not prove provider request-contract acceptance, model inference,
model-generated typed output, proofreader handling of model output, actual
token usage or billing, production suitability, Australian physical
processing or sovereign processing. A container constrains local capability;
it does not determine the provider's remote processing geography. The only
geographic claim is the configured and observed Sydney locational endpoint
together with Google's published contractual documentation.

## Independent audit and continuity

The independent repository-only audit recomputed the occupied seven-event
chain, the request and schema hashes, all eight provider-free ledger states
and chains, the one-call accounting, bounded error projection, no-retry
disposition and recorded cleanup. It found no defect and accepts the accurate
revision-required result. It did not repeat cloud, credential, provider,
network or Docker observations and grants no integration or baton authority.

Continuity graph revision 42 and Compass map revision 29 record the terminal
result together. The rendered Compass report is derived exactly from those
revisions. Historical ADC and cache-control nodes were not revised.

## Continuing gate

This descendant is closed. The unused conditional retry is not transferable:
its deterministic-defect predicate was not satisfied. Any provider-blocked
diagnostic, broader error disclosure, revised request, second call or new
model/provider/region attempt requires a fresh Yuri decision.

No commit, push, pull request, protected-ref movement, deployment or release
was performed.
