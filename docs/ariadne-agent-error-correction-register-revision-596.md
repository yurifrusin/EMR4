# Ariadne agent error and correction register — revision 596

<!-- ariadne-agent-error-register-reading
revision: 596
incident_count: 839
new_incident_ids: AER-0829,AER-0830,AER-0831,AER-0832,AER-0833,AER-0834,AER-0835,AER-0836,AER-0837,AER-0838,AER-0839
open_incident_count: 0
-->

This revision note binds eleven corrected or contained diagnostic and workflow
observations to the prospective clockwork-projected register. The canonical
JSON register and pattern report remain clockwork-owned, and one tick advances
the register once.

## AER-0829

The first preplanning receipt draft included full Git object IDs in caller
descriptive evidence. Receipt admission rejected it before publication. The
preserved rejected draft and corrected receipt prove that exact Git identities
now come only from the machine snapshot.

## AER-0830

The first focused test run derived the npm cache from `Path.home()`, which is
not guaranteed inside the provider-free test environment. The source reader now
uses the exact repository-parent cache coordinate and passes without HOME.

## AER-0831

The first test assertion contained an accidental patch-marker character inside
the expected plan phrase. Immediate focused execution rejected the draft. The
literal was corrected and the focused suite passes.

## AER-0832

The first widened asynchronous pytest launch projected only command output and
discarded its returned session handle. The running process was allowed to end,
then a fresh serialized run retained the complete result object and session ID.

## AER-0833

One exploratory invocation ran the repository package script by path and hit a
package-import failure. All accepted invocations use the declared
`python -m scripts.<module>` entry point, and the help and generation commands
pass in that form.

## AER-0834

Candidate inspection found that generic `Error` identity preceded custom preset
error identities. A subclass with a specific constructor but inherited generic
name could therefore be projected too coarsely. Specific constructor identities
now precede generic `Error`, with a hostile mixed-identity regression test.

## AER-0835

The accepted parent live handover had regrown to 204,718 bytes and 542 lines,
recurring beyond both compactness guards. The acceptance index preserves 222
inactive lookup rows verbatim, retains `Current clockwork relation` as an
explicit active label and supplies one rolling Harness acceptance slot.

## AER-0836

The first compactness repair manually projected `AGENTS.md`, despite the live
clockwork's exclusive ownership of that canonical surface. The clockwork check
rejected the candidate with `canonical_drift` before publication. The selected
generation was restored byte-exactly and compaction was moved into the
clockwork renderer with closed-label, index-integrity, byte-budget and
historical-replay tests. No live generation or protected ref moved during the
rejected draft.

## AER-0837

The first closeout intent put two artifact-path strings in the Continuity
node's `contract_evidence` list. The prospective graph validator requires
typed contract objects and rejected the preparation before publication. This
tooling tranche changes no registered product contract, so the field is now an
explicit empty list and the artifacts remain in their typed evidence
collections.

## AER-0838

The first published successor latch omitted the exact established
`no_ordinary_practice_enablement_feature_flag_allowlist_or_command_mounting`
token while retaining its semantics across two descriptive boundaries. The
post-publication consistency packet rejected the typed subset check. Generation
124 was rolled back byte-exactly; the exact token is restored alongside the
narrower denials before a fresh check and publication.

## AER-0839

The first corrected dry-run was attempted after the byte-exact rollback but
before its incremented pointer lease was Git-bound, recurring after AER-0749.
The check rejected `tick_pointer_physical_drift` without preparation or
execution. Only the lease-125 pointer was committed at exact source
`97c4367cc21c7bf9dad1cbeab6be0da314fec3be`; the next dry-run is derived from
that source. The recurrence supports folding rollback, lease binding and fresh
preparation into one deterministic control rather than another remembered
runbook step.
