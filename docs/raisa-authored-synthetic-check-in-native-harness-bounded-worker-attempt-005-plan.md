# Raisa authored-synthetic native Harness bounded-worker attempt 005 plan

Date: 2026-08-21
Timestamp: 2026-08-21T19:11:54.8870455+10:00 (Australia/Brisbane)
Status: `frozen`
Reasoning level: `high`

## Objective

Prepare, checkpoint, execute and terminalize at most one fresh disjoint native
DeepSeek Harness worker attempt against the already accepted one-file
authored-synthetic window-coalescer task. Use the exact repaired sentinel,
runner, `emr4-bounded-worker` preset, broker, work-order gear and structured
diagnostic controller admitted by the readiness decision. Stop on every
terminal outcome without retry, resume, fallback, auxiliary model or second
worker.

## Authority and identity

Yuri's standing uninterrupted-development authority and the accepted
process-free readiness result permit this exact successor after a separately
published clockwork checkpoint.

- operation:
  `raisa-authored-synthetic-check-in-native-harness-bounded-worker-attempt-005`
- attempt: `deepseek-native-synthetic-window-worker-005`
- work order: `wo-synthetic-native-window-worker-005`
- lease: `lease-synthetic-native-window-worker-005`
- disposable root:
  `C:/Users/sarashera/EMR4-worktrees/deepseek-native-synthetic-window-worker-005`
- evidence root:
  `orchestration/continuity/raisa-authored-synthetic-check-in-native-harness-bounded-worker-monitored-development-rehearsal/attempt-005`
- owned synthetic file: `synthetic_window_coalescer.py`
- model/effort: `deepseek-v4-flash` / `high`
- effective tools: exactly `edit`, `glob`, `read`; the model may make exactly
  one direct literal `edit` and no other tool call.

Attempts 001 through 004 and every retained boot proof are consumed,
immutable and non-resumable.

## Parallelism assessment

- **DeepSeek Flash:** `planned`, positive leverage. It owns the single later
  occupied authored-synthetic literal edit through the pinned native Harness.
- **Gemini 3.7 Flash/high:** `reserved`, neutral leverage before the terminal.
  Reassess only after terminalization if a surviving candidate makes a fresh
  veto proportionate; no auxiliary model is allowed inside the occupied
  attempt.
- **Native subagents:** `declined`, negative leverage. Developer policy
  prohibits proactive delegation and the identity, preparation, checkpoint,
  one-process launch, terminalization and cleanup sequence is serial.
- **GPT Sol:** owns plan freeze, provider-free descendant implementation and
  tests, deterministic admission, preparation, clockwork checkpoint, launch
  monitoring, terminal reduction, cleanup and acceptance.

## Fail-closed sequence

1. Complete the fresh five-source rehydration, validate the active latch and
   resolve the full Git/ref/worktree snapshot.
2. Add only an attempt-005 identity adapter, its closed outer terminal schema,
   focused provider-free tests and this plan/threat delta. The adapter must
   reuse the accepted converged attempt controller and introduce no second
   launcher or retry loop.
3. Run the provider-free check, focused and applicable startup-lineage tests,
   Ruff, compilation, broker syntax, Git hygiene, root/process absence and
   exact protected-ref checks. Node, Harness, broker, model, provider and
   network counts remain zero.
4. Commit and origin-align the exact candidate. Write one full-OID Sol
   deterministic-admission receipt whose head-before/head-after are the exact
   clean candidate and whose provider posture is zero requests.
5. Materialize exactly one disposable root and exact evidence envelope using
   that receipt. Preparation may create the synthetic local Git workspace,
   pinned rc.7 package, profile, preset, sentinel, runner, work order,
   authority, forbidden-surface and provider-free command-admission artifacts;
   it may not start the native Harness or call a provider.
6. Validate the prepared envelope and publish one clockwork checkpoint that
   binds its exact full source, review receipt, preparation, work order,
   runner, prompt, root, process/request ceilings, tool view and expected path.
7. Generate a fresh five-source occupied-prelaunch receipt and recheck exact
   root identity, one unconsumed lease, zero matching processes, task/origin
   alignment and protected refs.
8. Execute exactly one native Harness process/session/turn. The broker may
   forward at most one DeepSeek request. The runner may accept exactly one
   direct literal edit.
9. Consume the attempt before launch, bound execution to 4,096 output tokens,
   a 300-second upstream ceiling and 420-second native deadline, and never
   retry or resume.
10. On success or failure, derive the bounded outer terminal and any sanitized
    pre-HMR terminal outside the disposable root. Retain digests, counts,
    closed coordinates and candidate/test readings only.
11. Terminate the Harness and broker, destroy raw streams, environment/session
    material and the exact disposable root, then prove owned-process and root
    absence. Cleanup failure is a failed-closed terminal, never a retry.
12. Verify the immutable terminal, assess honest Harness/DeepSeek efficacy,
    close through the governance clockwork, write the paired Yuri summary and
    send the usual non-PHI Pushover notification.

## Cost posture

The user's existing prepaid DeepSeek balance is the sole financial boundary.
The broker enforces the one-request ceiling but does not claim a currency cap
and may not top up the balance.

## Acceptance

The tranche completes when exactly one of these terminal classes is preserved:

- `pass`: one process, one completed provider request, one direct literal edit,
  exact expected bytes, 4/4 public and 3/3 holdback synthetic cases, bounded
  evidence and complete cleanup; or
- `failed_closed`: the exact failure coordinate, zero retry/resume/fallback,
  bounded sanitized evidence, consumed identity and complete cleanup.

Neither class alone supports general Harness reliability. A prelaunch
deterministic rejection permits correction inside this plan only before the
occupied lease is consumed. Once preparation is checkpointed and the attempt
is consumed, no retry or replacement attempt is authorised.

## Protected boundaries

- exactly one fresh attempt-005 Node/native-Harness process, session and turn;
- at most one DeepSeek request and exactly one direct literal edit tool call;
- no retry, resume, fallback, auxiliary model or second worker;
- no raw prompt, response, reasoning, stream, environment, session or
  credential retention;
- no mutation or reclassification of attempts 001 through 004 or boot proofs;
- no ordinary-practice enablement, feature-flag/allowlist/command mounting or
  generic-status `Arrived` change;
- no product source/configuration/API/database/route/adapter/grammar/client/
  waiting-area change;
- authored-synthetic non-product one-file input only; no product, patient,
  appointment, clinical, historical or protected data;
- no production runtime, deployment, release, Pages or protected-ref movement;
- local/origin `master` and `handoff/current` remain exactly
  `2e34bdad732fdab32fbf778280b3d3c70d66d602`;
- preserve `docs/branding/` and every unrelated untracked file;
- explicit-path staging only; never `git add .` or `git add -A`.
