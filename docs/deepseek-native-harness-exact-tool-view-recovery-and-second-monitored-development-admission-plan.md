# DeepSeek native Harness exact-tool-view recovery and second monitored development admission plan

Date: 2026-08-18

Timestamp: 2026-08-18T18:11:35.4041914+10:00 (Australia/Brisbane)

Status: frozen

Reasoning level: high

Operation:
`deepseek-native-harness-exact-tool-view-recovery-and-second-monitored-development-admission`

## Objective

Recover the pinned native Harness model-facing tool contract without weakening
the accepted Ariadne broker, prove the recovered contract before provider I/O,
and only then permit one fresh second monitored DeepSeek V4 Flash/high worker
session to receive the unchanged provider-free EMR4 profile-validator package.

The recovery must make the inner Harness present exactly `read`, `glob` and
`edit`, while the outer broker independently retains that same allowlist. A
passing recovery is defence in depth: neither layer substitutes for the other.

## Accepted predecessor and exact negative result

The tranche builds on exact accepted evidence source
`af1a79f93024a7186849e550b4d529c8c601c93f`. That source proves that the first
occupied request declared seven tools and that the broker rejected the four
surplus schemas before provider I/O. It proves zero provider calls, zero
successful model steps, zero tool calls and zero candidate changes. The
session is consumed and cannot be resumed or retried.

The rejected request is immutable negative evidence. This successor must not
make it pass by broadening the broker to `grep`, `read_image`,
`str_replace_editor` or `write`.

## Phase 1: pinned package-native tool-view discovery

Sol will recreate an exact disposable rc.7 package root without a global
installation and verify registry name, version, shasum and integrity against
the accepted profile family. Source inspection is limited to the pinned
Harness and its official tool packages.

The recovery may use only a package-native scoped-tool facility that is
present in exact rc.7 source and that removes unselected schemas from the
model-facing composed request. It may select or wrap official tools, but it may
not patch package source, rename a broader tool, hide a schema only in the UI,
or rely on prompt instructions, broker rejection or post-response filtering as
the primary scope mechanism.

Sol will bind the discovered mechanism into the repository-owned normalized
profile contract and schema. Every package-native field, selected tool name,
plugin/package identity and effective authority value must be represented and
deterministically validated. Unknown or ambiguous scope semantics fail closed.

## Phase 2: provider-free composed-request proof

Before any provider credential or egress route is present, one fresh
disposable Harness boot will compose an initial request against a local capture
endpoint inside an isolated Docker network. The endpoint records only
sanitized structural metadata and terminates the request locally. It never
forwards to DeepSeek or any external upstream.

The durable proof must establish:

- exact pinned package identity and source mapping;
- exact selected profile and normalized profile digest;
- one composed request header with model `deepseek-v4-flash`, high reasoning,
  streaming enabled and output ceiling 4,096;
- exact sorted model-facing tool names `edit`, `glob`, `read` with no duplicate
  or additional schema;
- zero external/provider calls, zero successful model steps and zero tool
  calls;
- worker container has no provider credential and no direct egress;
- no raw prompt, reasoning, response, message body, tool schema body,
  credential or environment content is retained in EMR4; and
- exact disposable package, session, container, volume and network cleanup.

The capture gate is a structural proof only. A missing request, malformed
request, surplus/missing tool, unexpected route or incomplete cleanup ends the
tranche as attributable provider-free negative evidence. It does not admit an
occupied correction loop.

## Phase 3: conditional second monitored worker admission

Only a passing committed Phase 2 proof may unlock one fresh occupied session.
Sol will create a new non-protected worker branch and sparse worktree under
`C:\Users\sarashera\EMR4-worktrees\`. The worktree receives the full root
`AGENTS.md`, this plan and threat delta, the frozen profile contract, exact
neighbouring examples named in the packet, and only these owned output paths:

- `scripts/ariadne_deepseek_native_harness_profiles.py`;
- `tests/test_ariadne_deepseek_native_harness_profiles.py`.

DeepSeek must implement the previously frozen provider-free validator:
schema validation, semantic invariant validation, named-profile selection,
sanitized effective-authority materialization and canonical SHA-256 digest.
It must reject unknown profiles/fields, secret-looking values, retries,
fallbacks, auxiliary model routes, profile/tool mismatches and cross-operation
resume. It must not execute DSH, resolve credentials, contact a provider or
edit global configuration.

The worker receives exactly model-facing `read`, `glob` and `edit`. It receives
no shell; Sol independently runs focused and neighbouring tests. The worker
cannot commit, push, accept, recover or move the baton.

## Occupied enclosure

- One fresh DeepSeek V4 Flash/high native-Harness rc.7 session only.
- Same pinned Node image and two-container worker/broker separation accepted in
  the predecessor enclosure recovery.
- Worker has exact sparse bind mount, internal broker network only, no direct
  egress and no provider credential.
- Broker has no worker mount, holds the provider credential, accepts only its
  one-session capability token, exact path/model and exact three tools.
- Zero automatic retry, zero fallback, zero auxiliary model route, one
  parallel tool call and a 15-minute wall clock.
- Yuri's prepaid balance remains the monetary ceiling; no Harness token/turn
  budget is invented.
- No resume or fresh retry after any occupied terminal.

Retain only sanitized package/profile/session/process/broker/usage/tool/changed-
path/test/cleanup metadata. Never commit raw prompts, reasoning, responses,
messages, tool payloads, credentials, environment dumps, package caches or raw
session transcripts.

## Sol admission and recovery

Sol independently checks exact changed paths, complete diff, hashes, terminal
trace, provider/broker counts, tool order, focused tests, neighbouring tests,
Git state and cleanup. DeepSeek's narrative or test claim is never acceptance.

An incomplete or incorrect candidate may be preserved only as attributable
negative worker evidence. There is no automatic worker retry. A mechanically
bounded candidate defect may be recovered under the existing non-transferable
Sol recovery lease only when the frozen authority meaning and exact tool view
remain unchanged. A conceptual authority or scope defect stops candidate
admission and triggers reassessment before any external verifier.

## Acceptance

The tranche passes as a successful second monitored development admission only
if all of the following hold:

- an exact rc.7 package-native mechanism removes every unselected schema from
  the composed model request;
- the normalized profile contract/schema represent and validate that mechanism;
- the provider-free capture proves exactly `edit`, `glob`, `read` and zero
  external/provider activity;
- the provider-free proof and cleanup are committed before occupied dispatch;
- exactly one new occupied session uses the same exact tool view, model route,
  broker allowlist and two-path sparse package;
- only the two owned output paths change;
- the candidate satisfies the complete frozen validator contract and all
  focused/neighbouring deterministic tests;
- trace, broker, Git, test and cleanup readbacks agree; and
- every protected, untracked-file and ref boundary passes.

If the package-native exact view cannot be proven, acceptance is limited to a
bounded provider-free incompatibility result. If the second worker is rejected
or incomplete, acceptance is limited to attributable worker evidence. Neither
negative outcome promotes the native Harness or permits a retry.

## Parallelism assessment

- DeepSeek native-Harness lane: `planned`, positive leverage, but gated. It
  owns only the unchanged two-path worker package after committed provider-free
  exact-tool-view proof.
- Gemini lane: `declined`, neutral leverage during deterministic recovery.
  Reassess only if a candidate exists and material independent veto is useful,
  or if Sol recovery changes conceptual authority meaning.
- Native-subagent lane: `declined`, neutral leverage. Current developer policy
  prohibits proactive delegation, and the shared package/enclosure evidence is
  serial.

Package discovery, normalized binding, provider-free composition, proof
commit, pre-dispatch rehydration, occupied launch, readback, tests, acceptance,
cleanup and Git closeout remain serial.

## Protected boundaries

No product, patient, clinical, real-person, historical-Diary or protected-
holdout data. No application source, API, database, route, status, action
grammar, client, waiting-area movement, ordinary-practice enablement or live
product runtime. No global npm/DSH installation, durable user profile,
credential/IAM mutation, deployment, production, release or Pages action. No
protected-ref movement. Local/origin `master` and `handoff/current` remain
exact `2e34bdad732fdab32fbf778280b3d3c70d66d602`. Preserve `docs/branding/`
and every unrelated untracked file. Stage explicit paths only.

## Claim boundary

Passing can show that Ariadne deterministically narrowed the pinned inner
Harness to the same exact three-tool contract enforced by the outer broker and
that one fresh second monitored session produced an admissible low-risk EMR4
harness-development candidate. It cannot establish general DeepSeek or Harness
reliability, unrestricted default-worker promotion, production safety,
privacy/sovereignty, product-data authority, runtime, deployment, release or
protected integration.
