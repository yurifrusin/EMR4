# Provider-free unmounted default-off ordinary-practice canonical check-in admission-control kernel rehearsal — closeout

Date: 2026-08-19

Timestamp: 2026-08-19T04:37:32.9752761+10:00 (Australia/Brisbane)

Status: accepted

Reviewed source: `4204ec6348abb0f92b1a30314699d4a469fa860a`

## Outcome

The accepted architecture now has one pure standard-library evaluator and
disable-biased transition kernel. It is unmounted and has no route, database,
configuration, environment, provider, filesystem-write, product or broker
capability.

The canonical contract contains zero active ordinary-practice records and
freezes ordinary activation authority false. Every ordinary input, including
an otherwise valid active record, denies. The unchanged authored-synthetic
lane still admits only when it is the sole matching lane.

The executable record graph accepts exactly five non-activating edges. The
represented `prepared -> active` edge denies, no accepted edge produces
`active`, suspension cannot resume, withdrawal is terminal, and the only kill-
switch transition is `clear -> engaged`.

## Verification

- seven exact source bindings: passed;
- evaluator / record-transition / kill-switch / command / unknown-commit
  scenarios: 17 / 25 / 2 / 18 / 1, 63 total;
- hostile closed-contract mutations: 341 rejected, zero escapes;
- canonical active ordinary records: zero;
- ordinary admission releases: zero;
- focused kernel and plan tests: 23 passed in the independent worktree;
- full register, launcher, latch, API Spine, baton and integrated checks:
  passed;
- Ruff, Python compilation and Git diff checks: passed; and
- corrected fresh Gemini 3.7 Flash/high veto: one schema-constrained `pass`,
  9/9 commands exited zero, exact HEAD unchanged and worktree clean.

The first Gemini attempt was rejected because the orchestrator included an
evidence generator in a read-only manifest. It changed only line endings in
two tracked evidence files. Both paths were restored from exact HEAD and the
candidate's semantic content never changed. The corrected manifest is non-
generative. The Antigravity launcher now writes a digest-only receipt for every
post-transport worktree postcondition failure.

Register revision 521 contains 602 bounded incidents, all corrected or
contained and none open. AER-0592 through AER-0602 preserve the two preflight
arguments, generative read-only command, missing postcondition receipt, false
peer identity, unadmitted disposition, future evidence-path and mutable-latch
fixture, peer-linkage and regression-literal corrections.

## Clockwork consequence

The failures in this closeout materially refine the proposed shared causal
clock. Each tick must carry one full 40-character source object, one configured
stage/disposition, one side-effect class, one already-materialized evidence
identity and one derived attempt identity. A read-only tick cannot carry a
generator. A DeepSeek broker WorkOrder must name the exact Ariadne parent tick,
and the broker result must bind its digest before either harness advances.
Every completed tick yields exactly one admitted result receipt or one
digest-only rejection receipt.

These controls must be evaluated by whether they reduce hand-authored fields,
reruns, maintained fixtures and uncaught escapes. They remain shadow-only; this
tranche does not adopt a live clock or give DeepSeek product authority.

The bounded baseline reading is: manual-field count not yet instrumented; one
provider retry after the rejected read-only manifest; four rejected register
or pre-verifier drafts; four failure-induced closeout/transition verification
reruns; two stale mutable-latch fixtures; and zero uncaught escapes. All were
caught before the closeout commit or any protected movement.

## Closed boundaries and successor

No ordinary practice is enabled. No feature flag, allowlist, product,
configuration, API, route, database, generic-status `Arrived`, grammar,
first-party client, waiting-area, product/patient/clinical data, provider,
production runtime, deployment, release, Pages or protected ref changed.
`docs/branding/` and all unrelated untracked files remain preserved.

The dependency-satisfied successor is
`ariadne-provider-free-shadow-clockwork-deepseek-broker-gear-architecture`.
It may freeze only the typed causal tick, side-effect classes, broker parent/
result binding, atomic journal projections, exhaustive failure receipts and
efficacy measurements. It authorizes no live adoption, occupied DeepSeek call,
product authority, provider, deployment, release or protected-ref movement.

The required non-PHI continuing Pushover notification succeeded with request
`0bc371f5-1b5d-46e3-977d-462c0f154928`.
