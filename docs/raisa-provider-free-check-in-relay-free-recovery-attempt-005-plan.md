# Provider-free check-in relay-free recovery attempt 005 plan

Date: 2026-08-20

Timestamp: 2026-08-20T13:21:08.5249660+10:00 (Australia/Brisbane)

Status: `frozen`

Planning source HEAD:
`f270fd0be764230ca189cd08dabd8135409cad9e`

Accepted attempt-004 execution source:
`932ae6ce02e0e973a22dfe999601087295001d1b`

Accepted attempt-004 retained-evidence source:
`4908bf53265e1356a9c5dac84a05b05702ad6d34`

Accepted server-attachment repair implementation source:
`cfc7eb472aaaa4fdf7ffef35b07a65a2729073c5`

Accepted server-attachment reviewed candidate:
`9f9984e0575beb7b300035fdb74433f5bef32028`

Accepted complete-composition clockwork source:
`f9a4ede953cc496e9b778a6162d77dc7e73121df`

Accepted complete-composition reviewed candidate:
`6ef058b87a2c927efd9d9d2027b59d6ad279fec5`

Accepted no-database clockwork source:
`958ae762e7c6a065b5926f47eb1a2b63115212c7`

Accepted relay-free transport source:
`4f0f54c2b0861828f9994444201b8da1bd54be00`

Accepted runtime-role and tenant-isolation source:
`6a2832575e9b4df5c40a13984db7281e79814a94`

Operation:
`raisa-provider-free-check-in-relay-free-recovery-attempt-005`

Target result:
`raisa_provider_free_check_in_relay_free_recovery_attempt_005_pass`

Reasoning level: Extra High freezes the fifth one-run authority, immutable
negative lineage, repaired server lifetime, transaction identity, forced-RLS,
native-worker transport and exact cleanup boundaries. High is sufficient for
the bounded worker package, deterministic admission, sole occupied execution,
independent veto and clockwork closeout while this plan remains unchanged.

## Authority and objective

Yuri's standing uninterrupted-development authority admits this separately
named descendant after the accepted attempt-004 blocked closeout, accepted
server-attachment repair and accepted native-Harness complete-composition boot.
It authorises planning and deterministic construction first. It may then admit
one bounded DeepSeek adapter/test worker. Only after that result is independently
accepted and a separate preexecution checkpoint passes may exactly one occupied
local disposable PostgreSQL execution begin.

The sole database execution must prove, in one uniquely named provider-free
PostgreSQL 16 environment, that:

1. an authored-synthetic effect/receipt/audit transaction is explicitly rolled
   back and fresh restricted-role readback observes zero members;
2. a disjoint transaction commits once and loses its complete terminal response
   through the accepted relay-free caller path;
3. the caller releases neither success nor retry;
4. fresh restricted-role authoritative readback observes one exact consistent
   packet, zero duplicates and zero cross-practice visibility; and
5. the ephemeral role, live attachment, sidecars, server and network are exactly
   absent at close.

The first database-process start consumes attempt 005 regardless of pass,
failure, interruption or unexpected terminal form. This plan has no retry path.
Any future occupied run requires a new operation, output namespace and frozen
plan.

## Immutable negative and corrective evidence

Attempts 001 through 004 are consumed and immutable. Attempt 005 binds their
accepted chain through the exact attempt-004 terminal pair:

| SHA-256 | Exact source |
|---|---|
| `1ccc86c76826aa805a48a8823186f5b0eee6e0b571f6deff59ece0474f5df4d3` | `orchestration/continuity/raisa-provider-free-check-in-relay-free-recovery-attempt-004/rehearsal-failure-evidence.json` |
| `415f054f10639c2dba2466842ad7b957ce9a66f71f48bf07abe5bfdf4e47e7d5` | `orchestration/continuity/raisa-provider-free-check-in-relay-free-recovery-attempt-004/attempt-004-execution-envelope.json` |

The repaired base harness is executable input at exact SHA-256
`62a18d9ce2a29eb417f491c8ce341416f03183375f042f8c41bcb1f4674df77c`:
`scripts/raisa_provider_free_disposable_postgresql_default_off_check_in_relay_free_rollback_unknown_commit_recovery_rehearsal.py`.
It retains the server attachment until final teardown, separates
`server_not_running_after_readiness` from
`server_identity_mismatch_after_readiness`, and retains only safe sorted failed
predicate names. The accepted repair report is exact SHA-256
`0cf005adab3b6117ec19409aa2ce95bfbe1ec8c285b56bd7d6564c6b97252c88`:
`orchestration/continuity/raisa-provider-free-check-in-server-attachment-lifetime-and-post-readiness-observability-conformance-repair/repair-report.md`.

The transaction contract and attestation schema remain exact SHA-256
`bed2a89a3814ba9e9ac006d0fdb0c68d204fec53d8c21b6128190605b6ad9ec2`
and `d2c186b0d30419e0459d93d92af1f84907125becdeb75c7e1890dce597d3e72c`.
The no-database evidence is exact SHA-256
`9770af5d6d8e4282456e2ddd43ce6359c5dbff13b974c7d37a887fab331476d8`:
`orchestration/continuity/ariadne-provider-free-no-database-manifest-runner-admission-repair/provider-free-no-database-admission-evidence.json`.
The WorkOrder v2 schema is exact SHA-256
`71c87760ff9351b60704b5b2dcf7d3c43c96a36b2c41a53057e3f497b6fc0a5b`:
`orchestration/continuity/ariadne-provider-free-no-database-manifest-runner-admission-repair/work-order-v2.schema.json`.
The accepted complete-composition native-boot evidence is exact SHA-256
`9ba784b0726addb5644ac3786def410aed56e5bf9da3e23ec21d8e10f6ba1ea0`:
`orchestration/continuity/deepseek-native-harness-provider-free-complete-composition-native-boot-recovery/provider-free-complete-composition-native-boot-evidence.json`.

Every Git binding is a machine-resolved full 40-character commit object and
must be an ancestor of the candidate. Abbreviations and caller-completed object
identifiers are inadmissible.

## Narrow implementation boundary

The repaired base harness remains byte-for-byte unchanged. The only permitted
database adapter addition is one fixed fail-closed attempt wrapper:

- add `scripts/raisa_provider_free_check_in_relay_free_recovery_attempt_005.py`
  with only `--check` and `--execute` and no caller-supplied output path;
- use only the closed attempt-005 envelope schema and collision-free Continuity
  namespace frozen with this plan;
- bind the base harness's attestation, evidence and failure globals only for one
  runner call and restore all three unconditionally in `finally`;
- refuse execution if any attempt-005 terminal path already exists and never
  delete, rotate, rename or overwrite a terminal;
- bind the full plan commit, accepted full-Git sources, repaired harness digest,
  attempt-004 terminal pair, no-database evidence, WorkOrder schema and
  complete-composition evidence into static admission and the envelope; and
- add focused pure tests for CLI closure, full-object ancestry, byte-exact
  inputs, collision denial, exact routing, unconditional restoration, closed
  envelope validation, predecessor immutability and sanitized failure.

The exact terminal filenames are `transaction-attestation.json` plus
`rehearsal-evidence.json` on pass, `rehearsal-failure-evidence.json` on failure,
and exactly one `attempt-005-execution-envelope.json` for the occupied run.
Construction creates only deterministic source, tests and schemas; it creates
no terminal file and no Docker or database resource.

## DeepSeek native-Harness worker package

DeepSeek receives one useful but non-executing implementation opportunity. Sol
first commits this plan, threat delta, schema, plan test and fresh five-source
receipt. From that exact full source it must create and validate:

- one `ariadne.deepseek_work_order.v2` with exact source, authority, forbidden
  surfaces, command manifest, no-database admission, branch, sparse worktree,
  journal predecessor and outer-broker tool allowlist;
- one worker branch and sparse worktree containing full root `AGENTS.md`, this
  plan and threat delta, the schema, accepted attempt-004 adapter, repaired base
  harness, and the two worker-owned new paths;
- one exact command manifest whose tests Sol, never the model, will execute;
- one passing no-database admission artifact over those exact test bytes; and
- one predispatch Ariadne receipt plus one distinct one-run checkpoint.

The worker is pinned to official `@deepseek-ai/dsh@0.1.0-rc.7`,
`deepseek-official/deepseek-v4-flash`, high reasoning, the accepted
`emr4-bounded-worker` preset, zero automatic retries, zero fallback, one
parallel tool call and a 15-minute wall clock. The inner effective view and
outer broker must both expose exactly `edit`, `glob`, `read`.

The worker may edit only the new attempt-005 adapter and its focused test. It
has no shell, test, Docker, database, Git, network beyond the local broker, web,
subagent, workflow, skill, telemetry, title or compaction tool and cannot
commit, push, accept, clean up or move a ref. A terminal failure consumes the
worker attempt with no retry or resume. Sol may accept a useful patch or finish
the exact frozen semantics directly. Claude Code is not a fallback.

Retain only sanitized package, profile, process, broker, usage, tool-name,
changed-path, test and cleanup metadata. Never retain raw prompts, reasoning,
responses, messages, tool payloads, credentials, environment dumps, package
caches or raw sessions in EMR4.

## API Spine and product boundary

This is a security, audit and idempotency rehearsal of an accepted REST command
shape, not an API change. GraphQL remains read-only. The closed fixture retains
explicit actor and practice scope, authorisation, command and idempotency
identity, exact request digest, atomic receipt/effect/audit membership,
default denial and fresh authoritative readback. Events remain post-commit
evidence hints, never commands or source truth.

No REST/OpenAPI, GraphQL, async contract, route, schema, migration, feature
flag, authored-synthetic allowlist, action grammar, first-party client,
waiting-area behaviour or product configuration may change. Dedicated check-in
remains default-off. Generic status does not gain `Arrived`, and the rehearsal
writes no product record or ordinary-practice admission.

## Fixed containment, data and cleanup boundary

The accepted containment remains exact: cached `postgres:16-bookworm`, pull
policy `never`, one internal captured network, no published port, host relay,
Docker-exec byte bridge, external network, bind, volume, multiprocessing process
or queue. Credentials remain random controller-memory values delivered only
through post-inspection attached stdin. Docker configuration and retained
evidence contain no credential or nonce beyond the exact ownership label.

The three admin-owned non-product relations retain enabled and forced RLS. The
ephemeral login is non-owner and `NOBYPASSRLS`, receives only exact probe
grants, and is removed before teardown. Exact terminal OCI state plus the prior
observer predicate—not attachment output or status—classifies the incomplete
response. Restricted authoritative readback alone can prove committed exactly
once. All partial, duplicate, mismatched or cross-practice shapes deny.

One Sol controller is the cleanup owner for every acquisition. It reinspects
captured identity, name, image, labels, nonce, network and lifecycle before
removal. Pass or failure requires role, attachment, sidecars, server, network
and matching labelled resources absent. Cleanup uncertainty is terminal
failure and never a reason to retry.

## Deterministic worker and preexecution admission

Before a provider request, the complete WorkOrder v2, exact allowlists,
no-database admission, worker source and collision-free worker namespace must
pass. One clockwork checkpoint `--check`, followed only on success by a separate
`--publish`, admits the single worker launch. Sol then independently reads the
complete diff and runs every admitted provider-free test. Worker output is not
acceptance evidence by itself.

Before any Docker object, credential, PostgreSQL process, SQL or database
execution, one committed candidate must additionally pass all of the following:

1. this plan, threat delta, five-source receipts and all three lane
   dispositions validate;
2. the attempt-005 adapter, schema and focused tests exist, and every frozen
   digest and full-Git ancestry binding matches exactly;
3. the base static gate rejects all contract, manifest, OCI-state and classifier
   mutations with zero escapes;
4. focused attempt-005, repaired-harness, API Spine, no-database interlock,
   latch, Baton and clockwork tests pass through admitted runners;
5. Ruff, compilation, JSON/schema validation and `git diff --check` pass;
6. read-only Docker/version/image inspection proves the exact cached image and
   zero pre-existing attempt-005 labelled resources without creating an object;
7. a fresh preexecution five-source receipt binds the exact clean candidate and
   repeats the three-lane assessment; and
8. a second distinct clockwork checkpoint `--check`, followed only on success
   by `--publish`, advances the latch to exactly one database execution.

Then exactly one command may run:

`.venv/Scripts/python.exe -m scripts.raisa_provider_free_check_in_relay_free_recovery_attempt_005 --execute`

No automatic or manual retry is permitted. Invocation count is one even if the
command exits nonzero, output is absent, or cleanup requires bounded
ownership-checked recovery.

## Acceptance and independent veto

Pass requires the closed attempt-005 envelope, all accepted relay-free
scenarios, explicit rollback zero effect, committed-exactly-once authoritative
readback, no success from the incomplete response, retry count zero,
other-practice visibility zero, ordinary/product effect zero, schema-valid
sanitized evidence and exact cleanup with zero residue.

Only after occupied success and deterministic clean acceptance may one fresh
Gemini 3.7 Flash/high read-only veto run in an isolated unchanged worktree. A
veto or occupied failure closes attempt 005 with immutable evidence and no
retry.

## Explicit parallelism assessment

- **DeepSeek native Harness:** `planned`, positive bounded leverage. It owns one
  exact adapter-and-focused-test package after WorkOrder v2, no-database
  admission, inner/outer tool checks and one-run checkpoint pass. It receives no
  test, database, Git or acceptance authority and has no retry or fallback.
- **Gemini:** `reserved`, required independent leverage. It owns one fresh
  exact-candidate and terminal read-only veto only after deterministic and
  occupied acceptance.
- **Native subagents:** `declined`, negative leverage. Current developer policy
  prohibits proactive delegation, and the database lifecycle plus cleanup lease
  are one serial boundary.

Sol alone freezes authority, validates and accepts worker bytes, executes tests,
owns the database lifecycle and cleanup, commits, publishes clockwork and
chooses any successor. Reassess all lanes at worker predispatch, worker
terminal, preexecution, occupied result, pre-verifier and closeout.

## Protected and continuation boundaries

No product, patient, appointment, clinical, historical or protected data;
ordinary-practice enablement; feature-flag or allowlist change; product/API/
OpenAPI/GraphQL/config/client/waiting-area change; production runtime;
deployment; release; Pages; protected evidence access; or protected-ref
movement is authorised. The only live provider authority is the single bounded
DeepSeek worker and later one Gemini veto. Local/origin `master` and
`handoff/current` remain exact
`2e34bdad732fdab32fbf778280b3d3c70d66d602`.

At closeout clockwork is the sole canonical governance writer. Sol writes the
paired lay/technical Yuri summary, sends the usual non-PHI Pushover, stages only
explicit paths and preserves `docs/branding/` plus every unrelated untracked
file. `git add .` and `git add -A` are forbidden.

After accepted closeout, proceed to the narrowest dependency-satisfied
successor. Pause only for a truly extraordinary, genuinely non-inferable or
safety-critical fork.
