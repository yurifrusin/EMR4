# EMR4 Bureau C5 provider-free implementation-readiness closeout

Date: 2026-08-05

Result: `model_required_bureau_c5_provider_free_implementation_readiness_pass`

Accepted source HEAD: `d82de54ba59071d231adbf45a3aae1bbc0642ff4`

## Accepted result

The provider-free implementation needed for the frozen C5 disposable
live-development recovery rehearsal is ready. Its typed contracts, authority
issuer, shared state, controller, exact process adapter, inherited loopback
socket handoff, readback, rollback, cleanup and acceptance evidence pass without
starting a process, opening a socket or port, creating a task directory or
calling the candidate-runtime provider.

This is an implementation-readiness result, not the C5 live result
`model_required_bureau_c5_disposable_live_development_recovery_pass`. That
result can exist only after the distinct source-bound pre-execution receipt and
one successful serial rehearsal under the already frozen provider, process,
cost, data, authority, rollback and cleanup envelope.

## Recovered safety and authority properties

- Execution evidence binds the complete current approval, canonical anatomy
  frame, admitted candidate, proofreader result, provider-attempt admission,
  inherited port, Python executable, target artifact and command material.
- The frame contains exactly one healthy generation-1 baseline and one
  absent/connection-refused post-fault observation; model diagnosis and
  proofreading cite the same exact packet.
- Approval issuance revalidates the full standing-authority narrowing,
  provider/model/project/identity/region/endpoint, reasoning/output budgets,
  call and cost ceilings, exact fault/runbooks and evidence label.
- Shared store state makes provider correction, execution evidence,
  idempotency, supersession and attempts monotone across runtime objects.
- The parent keeps the exact bound IPv4 loopback socket until the child adopts
  its inherited handle/file descriptor. The child refuses the old direct-bind
  route, closing the port substitution race.
- Only a distinct fresh owned-process observation plus exact generation-2 HTTP
  readback can release success. Every post-launch failure uses only the exact
  stop rollback and fresh absence/refusal proof.
- Cleanup may touch only the exact owned process, socket reservation and
  marker-bound OS-temporary directory, and reports verification only when all
  process, listener, directory, ledger and capability absence proofs pass.
- Live-marked adapters maintain truthful append-only operation accounting;
  provider-free fakes remain exactly zero.

The API Spine command artifact remains explicitly `not_mounted`. It opens no
GraphQL mutation, `app.main` route, product command, database, audit table or
transport idempotency surface. Those fields are deliberately deferred to a
separately authorised mounted descendant.

## Rejected worker and recovery lease

DeepSeek worker commit `45a7a76e0705a2b534779847866507017370c557`
and its self-pass remain rejected as AER-0027. Independent provider-free audit
reproduced fourteen material defects. Sol adopted only that source under the
named recovery lease and added a direct regression for every finding.

The first fresh Gemini review at exact HEAD
`792f6e06a3c91a6684af0b0127c65660fd690b67` reconfirmed all fourteen recovered
controls but returned `revision_required` because durable evidence embedded one
worktree's absolute paths and called the older plan baseline `source_head`.
That veto is preserved. Sol changed only the evidence boundary: runtime paths
are still validated as exact checkout-pinned paths before portable
`repository://` projection, and candidate source is now bound through a
canonical repository-relative artifact hash set while the external receipt
binds exact Git HEAD.

Agent-error register revision 23 marks AER-0027 corrected only through this
completed recovery chain; it does not relabel the failed worker candidate.

## Deterministic and independent evidence

- Focused C5 contract and rehearsal tests, including the alternate-worktree-
  root regression, pass.
- The widened C5/C4/C3/Gate-zero/API-Spine suite passes 269 tests.
- Acceptance evidence reproduces from the fresh review worktree and all 24
  repository-relative artifact hashes plus their canonical set digest match.
- Ruff, compilation, Bandit, JSON/YAML validation and `git diff --check` pass.
- Fresh Gemini 3.6 Flash/high reviewed exact corrected HEAD
  `d82de54ba59071d231adbf45a3aae1bbc0642ff4`, found no material defect, returned
  one terminal `pass` and left the worktree clean and unchanged.

C5 candidate-runtime external effects remained zero. Exactly two authorised
Gemini/Antigravity source-review transports were non-zero: the preserved valid
revision veto and the fresh corrected pass.

## Claim boundary and next action

No patient, clinical, participant, historical-Diary, product-derived,
protected or production data; product API/database/runtime; ordinary service;
provider tool/retrieval; deployment; production; release; Pages; protected
evidence or protected-ref action is established or opened here.

The Practice Context Fabric remains a separate accepted staged direction. C5
does not implement `ContextNeed`, `ContextScopeGrant`, `ContextFrameSet`, weave
trace, temporal retention or cross-Bureau retrieval.

The next dependency-satisfied action is a distinct exact-HEAD pre-execution
receipt for the frozen serial C5 rehearsal. Only that receipt may admit the
task-owned target lifecycle and Sydney Vertex diagnosis call. A human pause is
required only for an indispensable credential restoration/console action,
exhausted bounded recovery or a change outside the frozen envelope.

`docs/branding/` and every unrelated untracked receipt/state artifact remain
preserved and excluded.
