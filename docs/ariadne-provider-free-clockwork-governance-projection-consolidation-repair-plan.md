# Ariadne provider-free clockwork governance projection consolidation repair plan

Date: 2026-08-19
Timestamp: 2026-08-19T08:41:24.0023767+10:00 (Australia/Brisbane)
Operation: `ariadne-provider-free-clockwork-governance-projection-consolidation-repair`
Planning source: `4ac81d4443de6f37d6baa26e390c1e8be7eaede1`
Reasoning level: High within Yuri's selected bounded repair; any live adoption or retirement remains a separate conceptual gate.

## Purpose

Repair the rejected shadow-clockwork rehearsal by bringing the surrounding governance work that caused most reruns inside one deterministic reading. One acknowledged clock tick must derive a prospective incident-register update, recurrence report, revision/count readings, typed command manifest, Continuity/Compass deltas, Current Baton fields and latch transition. No caller may retype their OIDs, counts, vocabulary-specific statuses, attempt/peer identities, output paths or projection revisions.

This remains a provider-free private-shadow rehearsal. Existing controls stay authoritative and are not retired in this tranche.

## Frozen factual baseline

- Accepted architecture: `f6cbd33fd3322754e06ac6dafa1503f5200e0803`.
- Corrected but efficacy-rejected predecessor: `a4044010e9f9319e149660ad889141a32cc8d000`.
- Predecessor result: 14/14 gauges covered, 13 failure-induced reruns, 7.143% reduction, zero provider calls and zero accepted publications.
- Exact rerun decomposition: four clock-engine/test reruns and nine surrounding-governance reruns. The nine surrounding groups are register category/origin, fixed-search command construction, register output/test projection, attempt/resource grouping, peer-link derivation, closeout vocabulary search, Continuity decision vocabulary, atomic final projections and next-work boundary propagation.
- The first repair reading begins with two honestly retained planning retries: an unsupported file-style helper invocation and one rejected multi-operation patch. Construction cost is never folded into steady-state savings.

## One governance reading

The public repair API accepts only paths to already-materialized authorities plus one closed semantic observation:

- a `failure_class` selecting one configured category/origin pair;
- bounded narrative and exact actor/resource facts that cannot be inferred;
- a closed `gate_result` expressing the factual outcome once; and
- already-materialized evidence paths.

The reducer derives:

1. full 40-character source OID and protected-ref digest;
2. next incident ID, attempt identity, same-attempt peer set, revision and every aggregate/count;
3. recurrence identity from the full configured composite;
4. exact executable/argument/output-path command arrays from a closed command catalogue, with no shell string, wildcard operand or optional required output;
5. the correct register, Continuity, Compass, baton and latch vocabulary projections from the single gate result;
6. compact Current Baton register/result/next-work readings and the successor latch checkpoint; and
7. one content digest binding all projections to the acknowledged predecessor clock tip.

Prospective projections are validated together and written only to one private generation followed by one atomic rename. The canonical register, AGENTS, latch, Continuity, Compass and command/update scripts are not rewritten by the rehearsal.

## Immutable replay set

The repair fixture contains exactly thirteen rerun probes derived from AER-0616 through AER-0629. AER-0620 and AER-0621 are one rerun because one complete-suite execution exposed both. Each probe retains its incident IDs, classification (`clock_core` or `surrounding_governance`), failure signature and generated control owner.

All thirteen must be rejected or rendered unrepresentable before publication. Coverage probes are not counted as reruns. The nine surrounding probes are scored separately because that is the surface this repair is intended to remove.

## Frozen efficacy and cost gates

The exact candidate passes only when one deterministic run proves:

- immutable rerun probes covered: `13/13`;
- surrounding-governance probes covered: `9/9`;
- caller-authored derived governance fields: `0`;
- candidate steady-state surrounding reruns per representative closeout: at most `1`;
- new mutable-current fixtures: `0`;
- partial publications and uncaught escapes: `0`;
- provider calls: `0`;
- no coverage loss from the predecessor's fourteen hostile gauges;
- incrementally added shared engine, runner, contract, fixture and focused-test growth: at most `850` physical lines;
- independently hand-maintained governance surfaces in the frozen retirement map fall by at least 50% after hypothetical adoption, with every legacy binding assigned one generated owner;
- `retirement_ready` may be true only when there is no unmapped legacy binding and no proposed long-term dual control plane; and
- repair construction retries, predecessor sunk reruns, steady-state savings, repair-only break-even and cumulative programme break-even are reported separately.

Timing is diagnostic only. A passing private-shadow repair does not itself adopt the reducer or retire a control.

## Frozen maintained-surface meaning

A maintained surface is a file or command shape that currently requires a human to repeat a derived binding. Generated canonical outputs may continue to exist after future adoption but do not count as independently maintained when their values can only come from the journal reducer. Irreducible narrative, factual classification input and acceptance judgment remain explicit human responsibilities and are not claimed as automated.

The retirement map must name each currently hand-maintained binding and its prospective generated owner. A vague wrapper, duplicated assertion or parallel updater does not reduce the surface.

## Owned files

The tranche may add or update only:

- this plan and one threat-model delta;
- a bounded governance projection reducer under `orchestration_harness/` and one runner under `scripts/`;
- one contract, immutable rerun fixture, private-shadow evidence/report and rejected generations under the operation's Continuity directory;
- focused reducer/plan tests;
- the required receipt, latch, error-register, review, closeout, Sol acceptance, Continuity/Compass, Current Baton and Yuri-summary artifacts.

Product, API, route, database, client and deployment files remain outside the owned set. Existing accepted clockwork files may be imported but not weakened.

## Parallelism disposition

- DeepSeek native Harness: declined. This repair is provider-free governance-semantic work and the required stock-headless-to-custom-runner HMR boot proof remains absent. Claude Code is not a fallback.
- Gemini: reserved for one fresh Gemini 3.7 Flash/high read-only veto of the exact clean candidate only after deterministic efficacy and line-budget admission.
- Native subagents: declined under active developer policy; the reducer, vocabulary mappings, replay and cost accounting are one serial acceptance package.

## Acceptance sequence

1. Validate the fresh five-source receipt and successor latch.
2. Freeze this plan, threat delta, closed contract, exact thirteen-probe fixture, surface map and line budget.
3. Implement one deterministic reducer and typed command catalogue.
4. Prove exact derivation, vocabulary separation, attempt/peer grouping, command safety, atomic rollback and complete replay.
5. Generate evidence and report construction, steady-state and break-even readings without amortisation.
6. Run focused and applicable governance suites, Ruff, compilation, schema/static checks and `git diff --check`.
7. If deterministic admission passes, obtain one fresh exact-candidate Gemini veto with unchanged clean HEAD.
8. Close out factually, update the durable records, write Yuri's paired summary, send the usual non-PHI notification, explicitly stage, commit and push only the task branch.

## Fail-closed boundary

No live adoption, current-control retirement, occupied DeepSeek/HMR, Claude Code fallback, provider call, ordinary-practice enablement, product configuration, API, route, database, client, patient, clinical or historical data, production runtime, deployment, release, Pages, protected evidence or protected-ref movement is authorised. Local/origin `master` and `handoff/current` remain exactly `2e34bdad732fdab32fbf778280b3d3c70d66d602`. Preserve all untracked files, especially `docs/branding/`; stage explicit paths only.
