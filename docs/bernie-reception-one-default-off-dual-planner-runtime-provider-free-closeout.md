# Reception One Default-off Dual-planner Runtime — Provider-free Closeout

**Date:** 2026-07-30
**Result:** `reception_one_default_off_dual_planner_provider_free_pass`

## Outcome

The provider-free tranche is complete. The existing authenticated,
development-only, authored-synthetic Reception One proposal route now accepts
an explicit closed planner selector:

- `deterministic` remains the default and performs zero provider calls;
- `isolated_vertex` is separately default-off;
- isolated selection has no provider or deterministic fallback;
- both modes release only through the deterministic proofreader and existing
  proposal-only API Spine adapters;
- no confirmation, appointment write or product-delivery authority was added.

The runtime reuses the byte-equivalent v6.8 system instruction, pre-printed
form, request packet and response schema. Its separate policy binds the
proofreader to request wall time, so an expired context edge-aborts without
release while the historical frozen evidence behavior remains unchanged.

## Verification

- Focused dual-planner tests: 8 passed.
- Inherited route, proofreader and read-only context tests: 23 passed.
- Relevant API Spine tests: 25 passed.
- Python compilation and YAML validation: passed.
- Provider-free real isolation: passed with non-root execution, network none,
  read-only root filesystem, dropped capabilities, bounded CPU, memory and
  process count, no mounts, no credentials, no database access and no residue.
- Provider calls: 0.
- Credential reads: 0.
- Database and appointment truth changes: 0.

## Accepted boundary

The generic model interpreter/provider lane remains blocked. The only candidate
occupied descendant is the exact separately gated authored-synthetic
`composeReceptionOneProductContextProposal` route using Gemini 2.5 Flash,
`bernie-emr4-dev`, the exact Bernie impersonated service account,
`australia-southeast1` and the regional hostname, after fresh Continuity,
Compass, ADC/control and residue preflight.

## Unresolved gates

- No occupied product-route result exists yet.
- Real/product-derived, patient, health, clinical and historical data remain
  prohibited.
- Appointment writes, confirmation, Word, voice, production, deployment and
  release remain closed.
- A Sydney locational request path would not prove Australian physical or
  sovereign processing.
