# DeepSeek native Harness provider-free integrated-runner factory fixture import-path recovery closeout

Date: 2026-08-22

Timestamp: 2026-08-22T19:27:57.8855998+10:00 (Australia/Brisbane)

Status: `accepted_pass`

Reasoning level: high

## Outcome

The one separately identified provider-free Node fixture passed. Before launch,
the controller proved that its JavaScript fixture was identical to the consumed
predecessor after normalizing exactly one source line, changed the package
projection from `package_root.parents[1]` to `package_root.parent`, and resolved
both Cordis and `dsh-agent` imports as regular files beneath exact
`node_modules/@deepseek-ai`.

The process traversed the installed rc.7 `AgentRegistry.create` method once and
invoked the exact integrated runner's setup callback once. The occupied old
three-parameter guard then returned the predicted structured coordinate
`EFFECTIVE_TOOL_COMPOSITION_INPUT_INVALID` when the runner supplied its
four-argument root-service-forwarding call. The runner emitted its generic
`failed` / `factory` terminal with zero request, tool-result or turn activity.

The result dynamically confirms the source diagnosis. The eligible correction
is no longer speculative: keep the runner unchanged and make its materializer
supply the already accepted four-argument guard, bridge and sanitizer graph.

## Exact process reading

- attempt: `factory-fixture-import-path-recovery-001`;
- Node processes: 1; exit: 0;
- installed factory transits: 1; setup invocations: 1;
- coordinate: `EFFECTIVE_TOOL_COMPOSITION_INPUT_INVALID`;
- preset-root reads: 4; preset-mount reads: 0;
- agent-context property reads: 0; live agents after disposal: 0;
- stdout: 641 bytes; stderr: 0 bytes;
- raw stream retained: false;
- retry, resume and fallback: 0;
- native Harness, broker, worker, model and provider requests: 0; and
- process and disposable root absent after cleanup.

## Honest efficacy conclusion

This tranche reached its full narrow objective and recovered forward motion.
It turned a plausible interface mismatch into a traceable dynamic result using
one local process and no model/provider cost. The clockwork did not create a new
diagnostic taxonomy or another exploratory loop; it carried one prior negative
to its exact falsifiable successor and obtained the predicted coordinate.

The initial focused implementation suite did expose one grouped low-severity
test-design lapse: several assertions were coupled to literal URI/prose forms,
and one monkeypatch intercepted the shared subprocess module rather than only a
Node boundary. All failed before process admission and were corrected before
the attempt. AER-0979 records the event so the efficacy reading includes its
correction cost rather than hiding it.

## Verification

- 63 exact operation, predecessor, integrated-runner and accepted-correction
  lineage tests pass on a clean origin-aligned source;
- all contract, evidence and failure schemas validate;
- source equivalence admits exactly the one package projection difference;
- the direct provider-free readback passes without starting Node;
- Ruff, Python compilation and Git whitespace checks pass; and
- protected refs remain unchanged.

The first post-process broad lineage run correctly rejected four historical
Git-source bindings while the new terminal artifacts were still uncommitted.
After the immutable terminal commit and task-branch publication, the exact same
63-test collection passed. This was an expected source-binding gate, not a
second fixture or provider attempt.

## Parallelism closeout

- DeepSeek: declined as planned; every worker/model/provider boundary remained
  closed.
- Gemini: reserved in the plan, then declined because the result selected an
  already accepted immutable correction graph and the provider-free boundary
  admitted no fresh model review.
- Native subagents: declined under current developer policy and the serial
  one-process attribution constraint.
- GPT Sol owned the exact recovery, terminal admission and acceptance.

## Next operation

Proceed under Yuri's standing uninterrupted-development authority with
`deepseek-native-harness-provider-free-integrated-runner-accepted-guard-graph-materialization-recovery`.
It may change only the integrated-runner materializer from the occupied old
guard to the already accepted four-argument guard/bridge/sanitizer graph, keep
the runner byte-identical, prove the complete relative-import closure, and
exercise the narrowest provider-free installed runner boundary needed to show
the `factory` mismatch is removed. It may not start a DeepSeek turn or model/
provider request.

## Protected boundaries

Both consumed fixture identities and all accepted evidence remain immutable.
No DeepSeek turn, model/provider request, product/configuration/API/database/
route/adapter/flag/allowlist/grammar/client/waiting-area change, ordinary-
practice enablement, generic-status `Arrived` change, product/patient/
appointment/clinical/historical/protected data, production, deployment,
release, Pages or protected-ref movement occurred. All unrelated untracked
files, especially `docs/branding/`, remain preserved; staging used explicit
paths only.
