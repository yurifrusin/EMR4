# Ariadne agent-error register revision 25

Date: 2026-08-06

Status: AER-0030 corrected; AER-0031 contained at a human-only authentication gate

## AER-0030: verifier-dispatch observation method was not admitted

The first Context Fabric pre-verifier state used descriptive method
`antigravity_new_project_preflight`, which is not an admitted adapter
observation method. The deterministic Ariadne receipt returned
`revision_required` with worker dispatch forbidden before Antigravity ran.

This recurs from AER-0024's orchestrator runtime-contract pattern. The failure
is preserved and the corrected five-source state uses exact admitted method
`synthetic_fixture`; its receipt passed with the same exact clean candidate and
unchanged protected refs. Adapter method values must be copied from the
versioned transport settings, while descriptive transport evidence remains in
the evidence field.

## AER-0031: Antigravity OAuth expired before review admission

The first independent Context Fabric veto transport required human Google OAuth
restoration and timed out after 60 seconds. The packet did not reach Gemini,
there was no terminal reviewer decision or ordinary worker receipt, and no
provider/model call is claimed. Exact HEAD
`1289f9b822c571341b224ab9c6b5caaeefaf0c71` and the review worktree remained
clean and unchanged.

This is a recurrence of AER-0022's Antigravity transport signature, not a model,
reviewer, operator, ADC or gcloud credential-store error. The sanitized failure
retains no OAuth URL, code, challenge, state, credential or token value. It is
contained at the human-only gate. After authentication is restored, a genuinely
fresh new-project process must repeat exact-head/clean/five-source preflight and
return exactly one decision before the incident can be corrected.

Revision 25 contains 31 bounded incidents: 23 agent-behaviour observations,
three harness failures, two repository defects and three transport timeouts.
AER-0031 is the only open incident. Counts remain workflow-improvement signals
only and do not establish model, provider, transport or role causation.
