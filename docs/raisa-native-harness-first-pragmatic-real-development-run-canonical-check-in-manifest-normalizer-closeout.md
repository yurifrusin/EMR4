# Canonical check-in manifest normalizer closeout

Date: 2026-08-23

Status: `accepted_pending_clockwork_publication`

Exact reviewed source: `ae62faf95b289b369a6eea1793ee4325f33447bc`

## Outcome

The provider-free, unmounted canonical check-in environment-manifest
normalizer is complete. It accepts only exact canonical bytes, a closed YAML
and field vocabulary, full lowercase 40-character Git objects, three ordered
opaque reference slots, exact cross-bindings and structurally ordered RFC3339
times. It returns frozen typed data or one deterministic denial and never
reads a file, process environment, configuration, credential, database,
network, route or current clock.

The final serial verification passed 149 focused and surrounding tests plus
Ruff, Python compilation and `git diff --check`. The older gap-decomposition
tripwire now permits only this exact unmounted service file while continuing to
deny route, API Spine, Diary, sidebar and environment-template movement.

## Native Harness reading

The one occupied native session did not reach DeepSeek. The stock rc.7
headless runner advertised `edit`, `exit_plan_mode`, `glob`, `grep`, `read`,
`read_image` and `write`; the accepted broker permits exactly `edit`, `glob`
and `read`. The broker therefore failed closed before provider I/O with one
HTTP 400 request rejection, zero tool calls and no changed path.

The coordinator retained the terminal, counts and exact request tool header,
but omitted the broker's textual rejection reason. Pinned package and broker
source make the mismatch deterministic, so the trace is sufficient for
attribution but not complete under the matched efficacy definition. No retry
was made because correction would require a new runner restriction hook or a
broader broker contract, both outside the pragmatic run's stop rule. Sol
implemented and verified the frozen source task under explicit recovery
provenance.

This is not a DeepSeek model failure and supplies no comparative model-quality
reading. It shows that native Harness transport remains traceable but that a
stock preset does not itself reduce an agent's globally mounted tool suite.
The practical rule is to use an already compatible accepted runner when one
exists and otherwise decline the native lane without opening another generic
interoperability programme.

## Cleanup and evidence

The worker and broker processes, worktree registration and disposable attempt
root are absent. The attempt root was permanently removed after exact path
validation; raw prompt/response/reasoning/stream material is not recoverable
from the active filesystem or Windows Recycle Bin. No model candidate existed.
The sanitized terminal, structural review, cleanup reading and matched efficacy
reading remain repository evidence.

## Parallelism disposition

- DeepSeek native Harness: attempted; no provider call and no candidate because
  the broker rejected the stock runner's surplus tools.
- Gemini 3.7 Flash/high: declined with neutral leverage; no API meaning changed,
  no worker candidate required an independent veto, and deterministic checks
  closed the unmounted source task.
- Native subagents: declined with negative leverage under the active developer
  policy.
- GPT Sol: completed recovery implementation, review, deterministic
  verification, acceptance, clockwork and Git.

## Boundary and next work

No operational manifest instance, secret value or resolution, environment
variable, configuration, credential store, database, route, API, client,
ordinary-practice enablement, feature flag, allowlist, command mounting,
generic-status `Arrived`, action grammar, waiting-area movement, product data,
production runtime, deployment, release, Pages or protected-ref movement was
authorized or performed.

The next dependency-satisfied repository node is the provider-free unmounted
typed operational-evidence input model. It remains reference-only and supplies
no external operational fact. The pure environment evidence-gate evaluator
still depends on that input model and this accepted normalizer.
