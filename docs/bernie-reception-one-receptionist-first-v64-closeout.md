# Reception One Receptionist-first v6.4 Closeout

Status: complete; occupied cohort did not pass at 18/24 exact outcomes
Recorded: 2026-07-30

## Result

The complete twenty-four-request authored-synthetic regression ran through the
exact bounded Sydney Vertex lane. Eighteen terminal results matched their
frozen deterministic oracle. Six did not, so the tranche result is
`reception_one_receptionist_first_v64_full_cohort_fail_closed`.

All thirty-one opened ledgers are consumed, every case audit chain verifies,
no call followed the terminal result and task-scoped residue is clear. None of
the six mismatches released a value.

## What improved

- Both v6.3 defects are repaired: `b-create-correct` preserved the exact 3:00
  PM bounds, and `b-move-shift` kept its natural and typed channels on the same
  move goal.
- The first eight cases passed on primary calls, including every create and
  move case.
- The unstamped no-show request still safely clarified; no `dna` alias was
  invented.

## Six mismatches

Four cases—two resize and two cancel—were false rejections introduced by an
overly structural v6.4 proofreader rule. Their typed forms carried the exact
recognized appointment and duration facts in the terminal proposal operator,
but v6.4 also demanded the deterministic reference's separate
`read_selected_appointment` operator. A bounded diagnostic replay through the
unchanged semantic proofreader admitted all four exact expected proposals.

Two clarification cases remained model/channel failures. One correction
ticket said the goal was wrong without naming the required goal, after which
the model changed a clarification form into a create form. The other retained
a typed clarification form but its natural receptionist response still
described the wrong goal and omitted the no-change review boundary.

## Calls and controls

- 24 primary calls and 7 terminal second calls; 31 total against a ceiling of
  48;
- all 31 provider responses completed with HTTP 200;
- 126,304 prompt, 15,078 visible candidate, 22,239 thinking and 163,621 total
  tokens reported by Vertex;
- exact `gemini-2.5-flash`, `bernie-emr4-dev`, Bernie impersonated service
  account, `australia-southeast1` and regional hostname throughout;
- no API key, service-account key, global endpoint, fallback, provider tool,
  grounding, retrieval, cache creation, product/database access, write or
  delivery; and
- raw prompts, raw provider responses, credentials, API-key information and
  hidden chain-of-thought were not retained.

## Recommended descendant

A separately versioned v6.5 should:

- require each recognized `(source kind, field, argument role)` anywhere in
  the normalized typed source graph, rather than requiring the deterministic
  reference operator sequence;
- retain the exact earliest/latest corrected-time regression;
- put the required deterministic goal in the existing bounded correction
  ticket feedback when the goal mismatches;
- explicitly teach the clarification desk form and the distinction between
  appointment details and a request to act; and
- rerun all twenty-four raw requests without revising v6.4 or adding the
  no-show-to-`dna` alias.

## Claim limit

This evidence proves eighteen exact safe outcomes, both v6.3 repairs and six
actionable fail-closed defects over a reused authored-synthetic cohort through
the configured and observed Sydney locational path. It does not prove
independent generalisation, production fitness, Australian physical or
sovereign processing, or safety for real, product-derived, patient, health,
clinical, protected or historical data.
