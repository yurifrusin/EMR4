# Ariadne provider-free clockwork live canonical adoption and retirement plan

Date: 2026-08-19
Timestamp: 2026-08-19T12:41:00.9132105+10:00 (Australia/Brisbane)

**Frozen reasoning level:** Extra High
**Operation:** `ariadne-provider-free-clockwork-live-canonical-adoption-retirement`
**Source anchor:** machine-resolved full 40-character Git object ID from the exact task `HEAD`
**Accepted safety floor:** `d03cc6386fdf3e2714881089514380d93824e160`
**Initial authority source:** `a6129d9a0c391314691cb73b28a5f21f1e834654`
**Pre-adoption generation source:** the machine-resolved full Git object ID of the exact clean implementation candidate after its fresh veto and before the live tick
**Protected refs:** local/origin `master` and local/origin `handoff/current` remain `2e34bdad732fdab32fbf778280b3d3c70d66d602`

## Decision and objective

Yuri explicitly selected live repository-governance adoption after the accepted single-owner mirror rehearsal. This tranche must make the accepted clockwork the only canonical closeout publisher without a dual-writer interval, retain the exact pre-adoption generation through Git, refuse ordinary execution of the historical bespoke updater class, detect any manual canonical drift, and prove byte-exact rollback in a disposable clone before publishing the first live clockwork-owned closeout.

"Live" here means the task branch's repository-governance records. It does not mean product runtime, practice enablement, provider traffic, deployment, release, Pages or protected integration.

## Authority ceiling

Authorized:

- provider-free repository-local implementation, tests and deterministic evidence;
- exact read-only inventory of historical governance writers and canonical readers;
- one clockwork publisher for the seven canonical authority files and three transaction/ownership surfaces;
- one exclusive lease, prevalidated staged generation, bounded multi-file materialization, byte-exact pre-pointer rollback and an atomic current-generation pointer;
- an immutable previous-generation manifest bound to the exact full reviewed implementation-candidate Git source rather than duplicated canonical bytes;
- a direct-execution retirement guard for the historical `*_continuity_update.py` class after activation;
- canonical-digest validation that rejects manual baton, latch, register or projection drift before acceptance, commit or push;
- one first live clockwork closeout only after deterministic admission and a fresh Gemini 3.7 Flash/high read-only veto on the exact clean implementation candidate;
- exact rollback exercise only in disposable storage; task-branch commits/push, paired Yuri summary and non-PHI Pushover closeout.

Not authorized:

- deletion or history erasure of the 145 historical continuity updaters or accepted evidence;
- a second live writer, a long-running compatibility writer or any interval with two accepted owners;
- caller-supplied Git object IDs, revisions, counts, output paths, ownership, transaction or projection digests;
- bypass of the clockwork by manual edits to Current Baton, latch, register, graph, Compass or report;
- occupied DeepSeek/HMR, Claude Code fallback or any provider call before the optional Gemini veto;
- product, practice, route, schema, API, database, client, patient, clinical or protected-data change;
- production runtime, deployment, release, Pages, protected evidence or protected-ref movement.

## Writer inventory reading

The pre-adoption inventory found:

- seven canonical authority files: Continuity graph, Compass JSON, Compass Markdown, active latch, error register, recurrence pattern report and `AGENTS.md`;
- 145 historical `scripts/*continuity_update.py` programs, of which 137 route prospective validation through `scripts.ariadne_compass` and eight older programs require the same direct-execution guard explicitly;
- four logical legacy writer classes from the accepted rehearsal: bespoke Continuity updater, manual baton editor, manual latch editor and manual register-fixture editor;
- no existing live canonical pointer or single-owner guard.

Historical programs remain immutable evidence and importable replay fixtures. Their ordinary direct publication role is retired. Historical tests may still import pure builders or exercise isolated fixtures; they receive no canonical publication authority.

## Canonical generation

One live generation owns exactly ten logical surfaces:

1. `orchestration/continuity/emr4-continuity-graph.json`;
2. `orchestration/continuity/emr4-compass.json`;
3. `docs/ariadne-compass-current.md`;
4. `orchestration/continuity/ariadne-active-operation-latch/current.json`;
5. `orchestration/continuity/ariadne-agent-error-register/agent-error-register.json`;
6. `orchestration/continuity/ariadne-agent-error-register/pattern-report.json`;
7. `AGENTS.md` including its typed Current result, Next implementation and acceptance reading;
8. the allowlisted closeout command manifest;
9. the transaction/generation manifest;
10. the ownership/current pointer.

The publisher derives graph, Compass, report and next latch from one validated closeout intent; preserves clean-closeout register and pattern bytes; derives Current Baton numeric and Git bindings; renders the full Compass from the same prospective graph; and produces exact digests for every surface. The human supplies only irreducible authority, narrative, evidence paths, decisions and the selected allowlisted commands.

## Publication and rollback state machine

1. `legacy_oracle`: current canonical bytes are bound to the full reviewed implementation-candidate Git source, which must descend from initial authority source `a6129d9a0c391314691cb73b28a5f21f1e834654`; no live clockwork pointer exists.
2. `prepared`: one exclusive source-bound lease exists; all ten output bytes and their generation digest have been built in private staging and independently reread; canonical bytes remain unchanged.
3. `clockwork_active`: seven authority files have been materialized under the lease, their bytes reread, ownership/transaction files installed, and `current.json` atomically replaced last. The publisher is now the only accepted owner. A post-pointer exception is classified committed only if the whole selected generation rereads exactly.
4. `rolled_back`: only an explicit rollback lease may restore the seven exact bytes from the full previous Git object. The same digest checks and pointer-last rule apply. The live tranche exercises this state only in a disposable Git clone.

Any pre-pointer exception restores every changed canonical byte from the private backup before releasing the lease. No generated staging or backup directory is committed. Git commit remains the externally published atomic boundary; the engine never commits, pushes or moves refs.

## Retirement enforcement

- The clockwork ownership marker lists all four legacy writer classes as `retired`.
- Direct execution of any `*_continuity_update.py` after activation fails with `legacy_governance_writer_retired` before its prospective Compass validation or first canonical write.
- The eight older updater programs that do not use `scripts.ariadne_compass` receive the same one-line central guard at their executable entry.
- Import-only historical replay remains available and cannot claim publication authority.
- The canonical-state validator compares every selected file with the active generation manifest; any manual or stale-writer edit returns `revision_required` before acceptance, commit or push.
- The new tranche has no bespoke Continuity updater. Its accepted closeout is the first clockwork-owned tick.

## Frozen implementation package

- `orchestration_harness/governance_writer_guard.py`
- `orchestration_harness/governance_live_adoption.py`
- `scripts/ariadne_provider_free_clockwork_live_canonical_adoption_retirement.py`
- `orchestration/continuity/ariadne-provider-free-clockwork-live-canonical-adoption-retirement/contract.json`
- `orchestration/continuity/ariadne-provider-free-clockwork-live-canonical-adoption-retirement/closeout-intent.json`
- `orchestration/continuity/ariadne-provider-free-clockwork-live-canonical-adoption-retirement/writer-inventory.json`
- `orchestration/continuity/ariadne-governance-clockwork/current.json`
- `orchestration/continuity/ariadne-governance-clockwork/ownership.json`
- `orchestration/continuity/ariadne-governance-clockwork/transaction.json`
- `orchestration/continuity/ariadne-governance-clockwork/command-manifest.json`
- `orchestration/continuity/ariadne-governance-clockwork/generation-manifest.json`
- focused live-adoption tests and narrow compatibility assertions in `scripts/ariadne_compass.py`, the eight inventoried legacy entry points and `tests/test_current_baton_consistency.py`;
- deterministic evidence/report, fresh verifier artifacts, register revision if needed, closeout, Sol acceptance and paired Yuri summary.

No tranche-specific bespoke Continuity updater or updater test is authorized.

## Deterministic acceptance gates

1. The fresh five-source receipt passes with the exact five named authorities and full 40-character Git bindings.
2. The live latch is `in_progress`; task origin aligns; all four protected refs remain exact.
3. Contract and intent reject unknown keys, path escapes, protected targets, abbreviated object IDs and every caller-authored derived binding.
4. The inventory accounts for all 145 historical continuity updaters and both guard populations without an unclassified executable writer.
5. The prospective generation advances exactly one Continuity and Compass revision, renders the full structurally validated Compass, produces a valid successor latch and preserves register/pattern bytes for a clean closeout; the main workspace separately passes the complete historical evidence-presence gate.
6. All ten surfaces have one owner; dual ownership is zero; all four legacy classes are retired; the previous generation binds the exact full reviewed candidate Git source and exact file digests.
7. Wrong writer, stale lease, stale generation, wrong source, abbreviated source, wrong protected ref, path escape, manual drift and legacy direct execution fail closed.
8. Fault injection before/after every materialized canonical file and before pointer replacement restores the exact previous bytes, leaves no pointer, partial metadata or lease, and exposes no mixed accepted state. A post-pointer exception is accepted as committed only after full reread.
9. Disposable rollback restores every previous byte from the exact full Git object and a validated re-adoption restores the exact active generation.
10. The first live closeout requires zero caller-authored derived fields and no bespoke updater.
11. All 13 predecessor and 9 surrounding rerun probes remain passing; active latch, complete register, Compass, Current Baton, transactional closeout and migration suites remain passing.
12. The canonical-state gate detects each single-surface mutation, and every historical direct updater entry is guarded.
13. Deterministic admission passes before one fresh Gemini 3.7 Flash/high exact-candidate read-only veto; the review worktree remains unchanged.
14. The clockwork performs the canonical closeout tick only after the review pass; final full tests, `git diff --cached --check`, explicit staging, task-origin alignment and protected-ref checks pass.

## Efficacy reading

Report construction and closeout reruns separately. The live adoption passes only if its own accepted closeout uses one validated intent and one clockwork publication, with zero bespoke updater execution and zero manual derived-field repair. It does not claim that future closeouts are cost-free; the next three qualifying closeouts must report clockwork validation failures, rollback events, legacy-guard trips and manual derived-field edits as direct readings.

## Parallelism assessment

- **DeepSeek:** declined. The canonical transition is serial; the native Harness remains behind the separate stock-headless-to-custom-runner HMR boot proof, and Claude Code is not a fallback.
- **Gemini:** reserved for one fresh Gemini 3.7 Flash/high read-only veto after deterministic exact-candidate admission and before the live pointer is installed.
- **Native subagents:** declined under current developer policy and because ownership, materialization, pointer commit and rollback form one inseparable boundary.

Reassess after plan freeze, writer inventory, dry-run/fault proof, pre-verifier admission, live activation and closeout.

## Closeout

Closeout must state pass or rejection, exact reviewed 40-character source, previous and active generation digests, ownership/retirement counts, fault and rollback results, live publication count, construction and closeout reruns, any guard trips, unresolved gates and next clockwork-owned tranche. It must write the paired lay/technical Yuri summary, send the normal non-PHI Pushover notification, stage explicit paths only, commit and push only the task branch, and leave protected refs unchanged.
