# Ariadne Sydney Vertex Gemini 2.5 Flash repaired-request run plan

## Status and authority

Status: closed on accepted success.

Yuri's 2026-07-24 instruction, “Please repair and run again,” is the fresh
authority source for this descendant. It authorises only:

1. the deterministic repair proved by the accepted provider-blocked diagnostic
   at
   `generationConfig.responseSchema.properties.total_tiles.enum[0]`, changing
   the JSON representation from integer `5` to string `"5"` as required by
   Vertex `Schema.enum[]`;
2. repository-local regression and provider-blocked verification of that
   repaired constructor;
3. a complete repeat of the read-only existing-ADC/entitlement and
   provider-free real-isolation gates; and
4. if every predecessor gate passes, exactly one fresh occupied call through a
   distinct single-use ledger.

The later Iterative repair authority update below superseded only item 4's
numerical limit and no-retry disposition. Every other boundary in this plan
remained frozen.

The complete original authorization is preserved in
`docs/ariadne-vertex-sydney-bounded-work-cell-authority.md`; the Gemini 2.5
model change is preserved in
`docs/ariadne-vertex-sydney-gemini-25-flash-authority-delta.md`. This plan
inherits those boundaries without revising any historical node, evidence,
ledger, audit or closeout.

## Exact frozen binding

- Provider: Google Vertex AI only.
- Model: `gemini-2.5-flash` only.
- Project: `bernie-emr4-dev`.
- Target service account:
  `emr4-bernie-ai-dev@bernie-emr4-dev.iam.gserviceaccount.com`.
- Authentication:
  `keyless_impersonated_service_account_adc`.
- OAuth scope: `https://www.googleapis.com/auth/cloud-platform`.
- Required permission: `aiplatform.endpoints.predict` through the existing
  prediction-only custom role and binding.
- Region: `australia-southeast1`.
- Data-plane hostname:
  `australia-southeast1-aiplatform.googleapis.com`.
- Data class: authored-synthetic only.
- Local isolation: the occupied work cell receives no ADC file, OAuth
  credential, API key, Google Cloud CLI configuration, product/database mount,
  Docker socket or unrestricted network route. It can communicate only through
  the exact-path one-use relay and broker.
- Audit: bounded, external, hash-chained “API typewriter keys” only. No raw
  prompt, raw provider response, raw provider error, credential, token,
  API-key information, hidden reasoning or chain-of-thought may be retained.
- Call ceiling: the initial two-call numerical ceiling was later superseded
  only by Yuri's iterative authority below. Each additional call required a
  distinct evidence-backed repair, request, attempt and consumed ledger. The
  loop closed immediately on success after three total occupied lineage calls;
  no continuing call authority remains.
- Cost ceiling: USD 1 application ceiling across the authorised occupied
  sequence.
- Authority exclusions: no provider/model/project/identity/region/credential
  or endpoint fallback; no provider tools, functions, grounding, retrieval or
  cache creation; no product, database, patient, clinical, command, production,
  deployment or release authority.

`GEMINI_API_KEY`, `GOOGLE_API_KEY` and other provider API keys must not be read
or used. Their host presence is not evidence and must not be recorded. The
broker and container child environments omit them. No service-account key may
be created, copied, mounted, inspected or persisted.

## Deterministic tranche gates

### Gate A — historical immutability and repair regression

- Preserve the consumed primary ledger, occupied evidence, external audit,
  closeout and accepted post-failure diagnostic byte-for-byte.
- Freeze a historical request reconstruction inside the diagnostic so it
  continues to prove the exact retained historical hash after the live
  constructor is repaired.
- Change only the proved enum representation in the live constructor.
- Add a focused regression requiring the complete repaired request to parse
  with the installed official Vertex protobuf and requiring the exact current
  request/schema hashes.
- Re-run the repository-only Ariadne verifier, focused Vertex tests, API Spine
  tests, JSON/schema validation, Python compilation/static checks, relevant
  JavaScript syntax checks, Compass validation and `git diff --check`.

Failure closes this descendant with zero new occupied calls.

### Gate B — provider-blocked launch contract

- Use distinct authored-synthetic request identifiers and distinct zero-call
  dry-run and one-call occupied ledgers.
- Verify the exact regional `generateContent` method/path and repaired request
  locally or from public official Google metadata without transmitting a
  rehearsal prompt.
- Verify every provider, identity, project, model, hostname, region,
  no-fallback, no-key and isolation rejection.

Failure closes this descendant with zero new occupied calls.

### Gate C — repeated existing-ADC and entitlement preflight

Repeat the complete read-only preflight for:

- exact impersonated credential type, project, target service account and
  cloud-platform scope;
- non-interactive refresh of the already configured ADC;
- enabled Vertex AI API and billing entitlement;
- exact prediction-only role, binding and
  `aiplatform.endpoints.predict` permission;
- Vertex Data Access audit logging;
- request-response logging disabled or absent;
- project in-memory cache explicitly disabled;
- zero user-managed service-account keys;
- no API-key path, global endpoint or automatic regional fallback; and
- current `gemini-2.5-flash` catalogue support for
  `australia-southeast1`.

No external cloud state may be changed. A failed gate requires Yuri
intervention under the original stop policy.

### Gate D — provider-free real-isolation repeat

Run the full disposable non-root, read-only, capability-dropped,
resource-bounded work-cell, exact-path relay, zero-call broker, typed exchange,
proofreader, ledger consumption, teardown and independent residue sequence.
The dry-run ledger and occupied ledger are distinct.

Failure closes this descendant with zero new occupied calls.

### Gate E — revision binding and occupied call

Before the call, require:

- Continuity graph and Compass revisions to be synchronized;
- the rendered `docs/ariadne-compass-current.md` to equal the current Compass
  rendering;
- a fresh pre-attempt five-source receipt;
- all earlier gates to pass; and
- independent absence of task-scoped container, network, image, token,
  context and broker-process residue.

Then make one small authored-synthetic call through its distinct ledger. The
broker must consume that ledger before transmitting. Success still requires
exact typed schema admission, deterministic grounding, policy and
command-authority checks, and atomic release of only the four admitted
authored-synthetic fields. Failure consumes the ledger and releases nothing;
only the later iterative update can admit another evidence-backed repair and
distinct call.

## Stop conditions

Stop and request Yuri's intervention for any unusable or non-refreshable exact
ADC; credential recreation or reconfiguration; API, billing, IAM, role,
binding, project, service-account, audit, logging or cache change; missing
Sydney model support or entitlement; provider/model/project/identity/region or
credential substitution; global endpoint or fallback; API/static/user-key
authentication; non-synthetic data; weakened isolation/audit/residency
assurance; possible credential exposure; cost above USD 1; exhaustion of the
bounded official-contract repair options; or any broader authority.

The configured Sydney locational endpoint and published Google contract may
support a regional-processing claim within their terms. They do not prove
Australian physical or sovereign processing. A container constrains local
capability; it does not determine the remote provider's processing geography.

## Iterative repair authority update

After the first repaired call still returned bounded HTTP 400, Yuri instructed
Ariadne to diagnose, repair and rerun the tranche until it succeeds or the
bounded official-contract options are exhausted. This fresh instruction
supersedes the earlier two-call/no-retry limit only for this exact
authored-synthetic Sydney Vertex lane.

The unchanged USD 1 application ceiling and every provider, model, project,
identity, keyless credential, Sydney region, data classification, isolation,
audit, no-fallback and no-product boundary continue to apply. Each provider
call requires a distinct request, attempt and consumed single-use ledger. Only
deterministic repairs supported by current official Google contracts or a
focused provider-blocked regression are eligible. No blind identical retry is
permitted.

The bounded option order is:

1. remove the Gemini-structured-output-ineligible INTEGER enum and preserve the
   exact value through supported numeric minimum/maximum constraints;
2. if needed, simplify provider-side constraints while retaining the exact
   typed release schema and deterministic proofreader;
3. if needed, remove only optional generation controls that current official
   model-specific contracts reject, without changing model, endpoint, output
   contract or safety boundary.

Close when one call succeeds or when those evidence-backed options are
exhausted. Batch Continuity, Compass and closeout updates at meaningful
programme-state transitions rather than routine internal retry boundaries.

## Closeout contract

Whether the iterative sequence succeeds or exhausts its bounded options:

- consume every opened ledger;
- perform no blind identical retry, no call after success and no fallback;
- clean all task-scoped containers, networks, images, temporary tokens,
  contexts and broker processes;
- preserve immutable evidence;
- produce a bounded external audit, independent audit analysis, Sol review and
  candid closeout;
- increment Continuity and Compass together for the terminal result;
- update the rendered Compass, AGENTS.md and the orchestration ledger; and
- do not commit, push, open a pull request, dispatch an external worker or move
  protected refs.
