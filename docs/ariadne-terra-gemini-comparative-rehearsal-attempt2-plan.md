# Ariadne Terra/Gemini Comparative Rehearsal — Fresh Attempt 002

Date: 2026-07-24
Owner: GPT Sol
Authority: Yuri's instruction to run a fresh attempt when ready without a
further pause
Status: closed
`ariadne_terra_gemini_comparative_rehearsal_attempt2_revision_required`

## Relationship to attempt 001

Attempt 001 remains immutable
`ariadne_terra_gemini_comparative_rehearsal_revision_required`. Its Terra
process ledger is consumed and its Gemini lane was suppressed. Neither ledger
is reset or reused.

Attempt 002 receives two new single-use process authorities. It reuses the
same provider-neutral authored-synthetic cognition task and typed output
contract, but builds new images from the corrected explicit UTF-8/LF
build-context preparation.

## Authority

After a fresh five-source receipt, both machine credential presence gates,
focused tests, exact five-hash build-context regression, and new real-isolation
preflight pass:

1. run one Terra work-cell process with at most one
   `gpt-5.6-terra` Responses API provider-generating request;
2. verify exact Terra teardown;
3. run one Gemini work-cell process with at most one
   `gemini-3.5-flash` `generateContent` provider-generating request; and
4. verify exact Gemini teardown.

Each new process authority is consumed immediately before its cell starts.
There is no retry, fallback, model substitution, tool, cross-model input, vote,
or self-acceptance.

## Unchanged boundaries

The shared prompt, common provider schema, exact full schema, token/byte/time
budgets, deterministic proofreader, sanitised evidence contract, broker-only
credential mount, product/API/database/event/mailbox/command closure, PII and
protected/historical exclusions, and downstream/human-action prohibitions are
unchanged.

## Result rule

Pass:
`ariadne_terra_gemini_comparative_rehearsal_attempt2_pass`.

Any occupied-process failure:
`ariadne_terra_gemini_comparative_rehearsal_attempt2_revision_required`.

A failure before either new ledger is consumed is a preattempt gate and does
not consume that process authority.

## Closed runtime result

Both preattempt gates passed and both process authorities were consumed.
Terra made exactly one provider request and received HTTP 500. After exact
Terra cleanup, Gemini made exactly one provider request and received HTTP 400.
Neither provider returned a draft candidate, so the full-schema gate and
proofreader were not reached. No retry or fallback occurred or remains
authorised.
