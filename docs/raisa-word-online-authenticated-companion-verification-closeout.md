# Raisa Word Online authenticated companion verification — closeout

## Result

Accepted terminal result:
`raisa_word_online_authenticated_companion_verification_pass`.

The task-specific `ReadDocument` manifest loaded in Word Online. One bounded
authored-synthetic request opened the hosted native Diary, returned three
synthetic matches and released only the generic proofreader-admitted result
count to Word.

## Repair and rerun

The first uploaded-manifest attempt failed closed. The hosted taskpane admitted
the exact public zero-authority policy, but the Diary still limited its smoke
and companion capabilities to loopback.

The repair loads the same dynamic policy in the Diary and admits exactly:

- `smoke`; and
- `reception_one_companion_demo`.

Admission still requires the exact HTTPS `run.app` origin, authored-synthetic
classification and false backend, provider, credential, microphone, command,
document-write and production authority. It does not admit broader harness,
product-context, confirmation or provider flags.

The closed context passed focused tests and a local container route matrix,
then was pushed at immutable digest
`sha256:8e06f07e4efd393f38275348d8bd7b136e664c2797c399a89207b66116839324`
and deployed only to `raisa-office-web-dev` as revision
`raisa-office-web-dev-00006-xf9`.

The exact public origin, runtime identity, min-zero/max-one limits, 1 CPU,
256 MiB memory, concurrency 20, 60-second timeout, no secrets, no volumes, no
VPC connector and zero project roles remain unchanged. The Invoker IAM check
remains disabled on this service only, with zero `allUsers` bindings.

## Word Online result

The completed exchange established:

- the Office dialog opened from the hosted taskpane;
- the native Diary admitted the exact typed request;
- three authored-synthetic appointment matches appeared in the Diary;
- synthetic patient and appointment detail stayed out of Word;
- Word displayed only `3 results are ready in the Diary.`;
- closing the Office dialog left that admitted summary intact; and
- neither taskpane nor Diary observed an API, credential or provider request.

No Word document body was read or written. No backend, database, appointment
command, confirmation, microphone or provider path ran.

Three browser-extension asynchronous message-channel errors appeared in the
Diary console and did not affect the exchange. No taskpane application warning
or error was observed.

## Cleanup

Both task-created blank documents were moved to the recoverable OneDrive
Recycle Bin. The pre-existing named synthetic document was left untouched.
Task-created browser tabs were closed.

Task-owned local build contexts, image tags, containers, networks and temporary
Docker credential directories are absent. The authorised Cloud Run service,
revision and immutable Artifact Registry image remain deployed.

## What the evidence proves

It proves one task-specific manifest and provider-free authored-synthetic
companion exchange in a signed-in personal Word Online session, including
native-Diary-only detail, typed proofreader admission and generic Word release.

It does not prove EMR4 application authentication, clinician-role
authorization, organisational Office deployment, safety for real or
product-derived data, backend or provider readiness, physical or sovereign
processing location, production fitness or release readiness.

## Verification

Continuity revision 180, Compass revision 161 and the rendered Compass report
validate exactly. The focused Word/host/Continuity suite passes 52 tests and
the relevant Word/API Spine regression suite passes 92 tests. JSON parsing,
Python compilation, Ruff static checks, JavaScript syntax checks and Microsoft
Office manifest validation pass. A final read-only Cloud Run check confirms
the exact revision, digest, runtime identity, zero project roles, zero
user-managed keys, zero `allUsers` bindings, min-zero/max-one resource posture
and absence of secrets, volumes, VPC or Cloud SQL configuration. Independent
local residue counts remain zero.

## Notification

The required non-PHI closeout notification was attempted. Pushover returned
`no active devices to send to`, so delivery did not occur.
