# Raisa AES-C5 product-runtime admission closeout

Date: 2026-08-11

Result: `raisa_agent_execution_surface_containment_gate_aes_c5_product_runtime_admission_pass`

Exact independently reviewed and occupied-evidence source HEAD:
`4e5d96ada19c51432fa4db46c76e23c952147c52`

Evidence label:
`occupied_authored_synthetic_product_runtime_route_postgres_brokered_provider_proof`

## Accepted result

AES-C5 passes the final named descendant of the AES-C0 containment contract.
One ordinary bearer-authenticated Receptionist request traversed the existing
FastAPI practitioner-directory route, SQLAlchemy service and local PostgreSQL
path against one fresh task-owned disposable schema. The schema contained only
the five frozen newly authored synthetic records. The route returned exactly
the three active same-practice rows; the inactive same-practice and active
foreign-practice sentinels remained excluded.

The external broker minimized that response to three opaque practitioner
aliases, display names and optional role labels in one fresh expiring
`ContextFrameSet`. UUIDs, practice/user identity, active state, location,
route/source/timing metadata, the alias map, the bearer credential and the
database connection remained broker-side and were absent from the provider
request and retained evidence.

AES-C0 permits one destination per immutable generation, so the proof used two
separately immutable, non-overlapping generations. The source generation held
one `authoritative_read` grant and one local route destination. Only after that
generation exhausted and the fresh minimized frame passed deterministic
admission did the provider generation acquire one `provider_inference` grant,
one Sydney Vertex destination and an immutable manifest binding the frame
digest. No lease or budget transferred between generations.

## Occupied provider result

The provider generation issued exactly one Google Vertex AI
`gemini-2.5-flash` request through project/quota project `bernie-emr4-dev`, the
existing keyless impersonated Bernie service-account ADC and
`australia-southeast1-aiplatform.googleapis.com`. The request used no tool,
function, grounding, retrieval, code execution, explicit context cache,
session resumption, redirect or fallback. No retry or repair call was made.

The provider returned HTTP 200 with finish reason `STOP` in 2,713 ms. Safe
usage was 451 prompt tokens, 105 candidate tokens, 391 reasoning tokens and 947
total tokens. At the rechecked Standard rates, the approximate input plus
response-and-reasoning charge is USD 0.00138, below the frozen USD 0.25
application ceiling.

The deterministic proofreader admitted exactly one closed four-field booking-
context match: the required decision code, `practitioner-choice-002`, the exact
fresh ContextFrameSet digest and `command_authority: false`. Repository
evidence retains only its digest, never the released name, raw prompt,
provider text, raw response or reasoning.

## Accounting and cleanup

- exactly one product route read and one provider call occurred;
- the measured route emitted three read/control SQL statements and no DDL or
  DML;
- practitioner, appointment and appointment-audit counts were unchanged;
- the source and provider admissions both terminated `exhausted` before
  revocation;
- both single-use ledgers are `consumed`, with zero route retries and zero
  provider retries;
- the provider reserved allowance and USD 0.25 ceiling before I/O and left no
  residual reservation;
- every lease, alias and token is revoked, further generation calls are false,
  and no broker process/listener, reusable capability or task temporary root
  remains; and
- the exact generated schema was dropped, its absence was verified, and its
  name is retained only as a digest.

Operation counters are one source operation, one product read, three database
operations and one provider network operation. Command/write, provider-tool,
filesystem-capability, deployment/production and protected-operation counters
are all zero. The lifecycle evidence confirms no patient, clinical or real-
person data and no retained JWT, database URL, route values, prompt or provider
text.

## Deterministic, independent and cloud gates

Before the occupied attempt:

- the provider-free core and actual local-route/fake-provider PostgreSQL proof
  passed with one route read, zero provider calls and exact cleanup;
- fresh Gemini 3.6 Flash/high independently passed 71 focused tests, Ruff,
  compilation and Git checks in an unchanged clean exact-head worktree, while
  making zero provider calls, product reads or database operations;
- official Google lifecycle, pricing, training, request/response logging,
  in-memory caching and regional-endpoint facts were rechecked;
- the sanitized read-only cloud/ADC preflight passed all 22 controls, including
  exact impersonation, project/quota project, scope, billing/API, prediction-
  only role/binding, Vertex Data Access audit, disabled-or-absent request/
  response logging, disabled project in-memory cache, zero user-managed keys,
  current model catalogue, exact Sydney hostname and no fallback; and
- the distinct gcloud CLI credential-store impersonation probe passed without
  printing or persisting a token.

After the occupied attempt, the final AES-C5 focused/continuity packet passed
75/75. The wider serial route, API Spine, baton, acceptance-index, incident-
register and Compass packet passed. The canonical fast profile passed 190/190
together with Ruff, target-Python-3.11 source-state compilation of 202
maintained files plus 35 verification files, Diary JavaScript syntax and Git
whitespace. The exact CI-static packet passed 184/184 with `--noconftest`, and
the historical-Diary leakage lint remained safe. The CI-only wrapper's literal
`--require-target-runtime` step could not run because this workstation has
Python 3.14 but no Python 3.11 interpreter; no test was hidden by that
environmental limitation, because its lint and exact static-test commands ran
and passed independently.

The first CLI-store wrapper promoted gcloud's ordinary impersonation warning
to a PowerShell error. It produced no inference, prompt transmission or state
change and was not accepted as evidence. The corrected zero-inference probe
suppressed stderr, checked only exit/non-empty token shape in memory and
passed. No provider retry was involved.

## Issues exposed and resolved

DeepSeek's initial candidate attempted to place two destinations in one AES-C0
generation and bypassed the complete AES-C1 validator. Sol preserved that
candidate as untrusted worker output, applied the recovery lease and split the
path into the two exact generations above. The same recovery corrected route
nullability/shape assumptions, enforced recursive closed packet keys, removed
broker-only request metadata, prevented a trivial one-value provider choice,
and bound source observation to the real dispatch clock. Fresh independent
review passed the repaired exact source.

AER-0262 through AER-0268 preserve the tranche's orchestration and evidence-
binding corrections, including rejected invented continuation events, an
abandoned broadened search output, expired readiness treatment, the one-
generation conflict, incomplete worker inventory, a fabricated full SHA and
the corrected exact pre-integration event. No rejected or invalid evidence
supplied occupied authority.

## Exact artifact hashes

- product-runtime envelope SHA-256:
  `df6e19dd67129323a232b3c73a73620ec0cefe2fa2f9aca1ebc1838fa80f9ffb`;
- containment core implementation SHA-256:
  `8b36075e8ee4962e59122a1bd47738d0f8eee6bb000e223189b6fa3830d9d597`;
- local-route harness SHA-256:
  `e08dad776a47993363e971232d44deb995df3c3d2f37d0d9c37e0a3ca6e2d361`;
- factual/CLI check SHA-256:
  `8c33a9c23cb8086b8ee2e06302921ff58688db9a003874a7721773a3febb1821`;
- live cloud preflight SHA-256:
  `0c4fe229adb9e0a9e6d39e00fd0b2753ed0643cf627d191a233b8163f3ef7dc4`;
- independent review receipt SHA-256:
  `ac990ca737af678c1ede891787782e713fafbe74997805e87d99170de50fc4fd`;
- occupied core evidence file SHA-256:
  `0e8e8280446f600b656ed79737279b171dadf21681a46bd9f624cdeffec230ae`;
- occupied lifecycle evidence file SHA-256:
  `e8870abfd9b4ec26e24764157453d685a88a0d2e78b12f805d24b9e51d2f4f21`;
- consumed source ledger SHA-256:
  `8f1750cfa974072a599c9d6a4b69891007d5644c1cbf718d9d1971ea54d01c9a`;
- consumed provider ledger SHA-256:
  `f181403e4b26b1bbdae38c216d30c6d563be6e28c99d1d234fb8c1310374b4fd`;
- provider request digest:
  `sha256:2d793b60137e11ec78757668205084f9b5a86aeb673ffcd080125517d15d2b5e`;
- provider response digest:
  `sha256:fd3ba8aae59e863b246fa69e4e53ef04ddee3c9de020da78ebb68a9147d314f1`;
  and
- deterministic release digest:
  `sha256:5cf8d27c139b66fda0ec1d728981f5ea9a5a3429c844ad4798cf3dc66744f69b`.

## Claim and authority boundary

Passing proves only one exact authenticated, practice-scoped, authored-
synthetic application read minimized into one expiring ContextFrameSet and
used by one exact broker-admitted Vertex request to release one inert typed
booking-context match. The configured Sydney endpoint proves the requested and
observed locational path, not Australian physical or sovereign processing.

It does not prove real-person or patient/clinical safety, production identity
or RLS, concurrency, reusable broker/runtime safety, general semantic prompt-
injection detection, command safety, deployment, production or release. No
continuing product read, database/source access, provider call, generic
network, filesystem capability, provider tool, credential/IAM change, command
or write is authorized.

AES-C5 completes the finite AES-C0 through AES-C5 sequence. No AES-C6 or later
tranche is planned or authorized. A future real practice population, new data
class, reusable runtime, tool/command, deployment or production descendant is
a new programme choice for Yuri.
