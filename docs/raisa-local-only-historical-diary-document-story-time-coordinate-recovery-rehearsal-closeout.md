# Raisa local-only historical Diary document-story time-coordinate recovery rehearsal — closeout

Date: 2026-08-24

Timestamp: 2026-08-24T05:28:25.6834754+10:00 (Australia/Brisbane)

Status: `accepted_blocked_pending_clockwork_publication`

Exact reviewed source: `2e6974218f8a133e220d84684af432867d53fcd8`

## Lay outcome

The mapping rules and privacy controls passed, but Word did not finish reading
the rendered positions of the fixed 80-document slice within the predeclared
15-minute ceiling. Because this was deliberately a one-run experiment, the
attempt stopped instead of being tuned and rerun against the same private data.

Nothing private was released or retained. The one Word process created by the
run was identified by its exact start time and removed; the Word process that
was already open before the run remains untouched. The result says the current
extraction shape is too slow for its controller budget. It does not say that
the coordinate mapping idea failed.

## Technical outcome

- Phase A passed on its first metadata-only bind: 80 files, 8,151,040 bytes and
  zero content reads before admission;
- Phase B consumed exactly one content run and hit the fixed 900-second parent
  timeout before a typed PowerShell extraction returned;
- the automatic generic reason was `internal_local_probe_failure`; the reviewed
  factual classification is `fixed_900_second_controller_timeout`;
- the manifest and private projection are absent and no text, key, mapping,
  page, coordinate or distance value was emitted or retained;
- exact post-timeout cleanup removed one owned Word process and preserved the
  one pre-existing user Word process;
- 175 provider-free historical-Diary controls and all static checks pass; and
- the first-use gate remains closed with zero reusable historical-derived
  artifacts.

The same empirical attempt must not be repeated. The dependency-satisfied next
work is authored-synthetic timeout containment and throughput recovery. It may
improve typed failure reporting, exact parent cleanup and performance evidence,
but it may not enumerate or open the historical archive.

No provider, model, product, database, ordinary-practice, production,
deployment, release, Pages, protected-evidence or protected-ref authority is
opened. Local/origin `master` and `handoff/current` remain exactly
`2e34bdad732fdab32fbf778280b3d3c70d66d602`.
