# Reception One Proofreader Dialogue v4 Request-Contract Repair

## Closed primary

Occupied turn one consumed exactly one ledger and one actual call through the
exact Gemini 2.5 Flash, Bernie keyless-ADC, Sydney Vertex lane. Vertex returned
bounded HTTP 400 `INVALID_ARGUMENT` before a candidate because the structured
response constraint had too many serving states. No prompt, raw response,
credential, rejected note or hidden reasoning was retained. The per-turn and
parent audit chains validate, no value was released, and all task-scoped
containers, networks, images, token files and processes are absent.

This is a deterministic provider request-contract defect. It is not a semantic
model-output rejection and cannot open the proofreader correction turn.

## Exact repair

The provider response schema remains the same PlanProgram-v3 object with the
same required fields, types, property ordering and closed string enums. Six
provider-only constraint keywords are removed:

- numeric `minimum` and `maximum`;
- string `minLength` and `maxLength`; and
- array `minItems` and `maxItems`.

The exact local PlanProgram-v3 JSON Schema retains all of those bounds. The
local parser, explicit-reference compiler, semantic proofreader, operator-note
gate, ticket contract and proposal-only executor are unchanged and decisive.
The repaired provider schema therefore reduces serving-state complexity
without weakening the admitted output contract.

## Call budget

The frozen authority permits the remaining actual call for this deterministic
request repair only after:

1. the first ledger and audits are closed;
2. the bounded error is diagnosed;
3. a focused regression passes;
4. all provider-free, API Spine, schema, isolation, Continuity, Compass and
   read-only cloud-control gates pass again; and
5. a distinct one-use repair ledger is prepared.

The repair consumes actual call two whether it passes or fails. It competes
with and therefore eliminates the semantic correction turn. No third call,
fallback, provider/model/project/identity/region/credential/data change or
external mutation is permitted.
