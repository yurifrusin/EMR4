# Provider-free disposable PostgreSQL delete-confirm scaffold parse/catalogue rehearsal plan

Date: 2026-08-16

Timestamp: 2026-08-16T10:31:22+10:00 (Australia/Brisbane)

Source HEAD: `274c42adf575f7f07aea759e79a6bd3f2ec25e54`

Status: `frozen_for_tier_2_provider_free_disposable_postgresql_execution`

Reasoning level: material database execution / Extra High

Risk classification: Tier 2 (`migration_representation_or_execution`,
`database_runtime`)

## Purpose

Ask one locally present, disposable PostgreSQL 16 server whether exact migration
`x3y4z5a6b7c8` lowers, parses and installs atomically over the minimum empty
authored-synthetic prerequisite relations, and whether the resulting columns,
relations, constraints, indexes, functions, triggers and Alembic head exactly
represent the accepted unmounted delete-confirm scaffold.

This tranche deliberately stops at representation. It runs no behavior probe,
inserts no user, grant, receipt, audit or appointment row, invokes no service or
route, and grants no capability. Empty relation counts are evidence that the
migration did not fabricate authority.

## API Spine classification

This is private physical evidence beneath the REST/OpenAPI command
`confirmAppointmentDeleteProposal`. Public OpenAPI and runtime routes do not
change. GraphQL remains read-only. Events remain non-authoritative acceleration
hints and gain no command or durability authority. Catalogue evidence is not
product truth, an authorization decision or an executable command receipt.

## Exact source bindings

Only these exact non-protected authority inputs may be read, hashed or searched
by the rehearsal contract:

| SHA-256 | File |
|---|---|
| `f6e75c7428dc5c1327166bc0e900c2804f3201ea1b32cd5577d1f8134b16c2a8` | `docs/raisa-provider-free-unmounted-delete-confirm-physical-schema-transaction-scaffold-plan.md` |
| `caad279b52f915bbfc1d554c2460725044c34fee945c0601e30a0cde52c4d250` | `docs/raisa-provider-free-unmounted-delete-confirm-physical-schema-transaction-scaffold-closeout.md` |
| `986de83a97449a5aeecd39b37594ab1eca740780f9c4016ae7d5b75f954428b5` | `orchestration/agent_inbox/codex/raisa-delete-confirm-physical-schema-transaction-scaffold-sol-acceptance.md` |
| `4d4ecc83fdb9b9e90067714f4827be6bc007ddd183bc66b0cd95aa207d475f22` | `orchestration/continuity/raisa-provider-free-unmounted-delete-confirm-physical-schema-transaction-scaffold/scaffold-contract.json` |
| `e6542c960a9378cf7c1c3c22dd876a1c9f242b68047a180f9f383c1c62d348bb` | `alembic/versions/x3y4z5a6b7c8_add_delete_confirm_physical_scaffold.py` |
| `8a70641ffa66bb1c228e16a62d8b7a2111810888819c9c662c17ea72e2d70b49` | `alembic.ini` |
| `5f8f7f752287ce47b507555c43065499fe797c37f724ed827c1d43ebfc5fe346` | `alembic/env.py` |
| `c5493c14efd92b3d3fc3d8a0ef33d3e3a266fa1d0961ad90ebbc37e4b4065a3a` | `docs/api-spine/openapi/appointment-commands.yaml` |
| `a4bc7f92d1caecbd0b421cd40438d64916a7945197e02d2f8b9232d2162c2284` | `orchestration/harness_settings/risk_weighted_workflow.yaml` |

The accepted status-confirm parse/catalogue harness supplies a containment
pattern only. Its migration, receipt probes and product rows are not executed
or treated as delete-confirm evidence.

## Exact owned artifacts

This tranche may add or update only:

- this plan and its threat-model delta;
- preplanning, dispatch and verifier receipts plus the active latch;
- one closed rehearsal contract and schema;
- one fixed-path standard-library harness;
- one provider-free minimized evidence document and its schema;
- focused static, hostile-mutation and simulated-lifecycle tests; and
- eventual closeout, Sol acceptance, Continuity, Compass, baton and paired Yuri
  summary artifacts.

Existing migration, application model, service, route, public API, GraphQL and
UI files must not change.

## Risk-weighted gate budget

- The canonical fast baseline passed once at exact source HEAD before the first
  semantic tranche edit: Ruff, maintained compilation, 196 tests, Diary syntax
  and whitespace.
- This plan freezes scope and acceptance meaning. After implementation admission,
  one exact-candidate semantic freeze records HEAD/tree, semantic-binding digest,
  toolchain digest and focused-gate results.
- Deterministic admission uses focused contract/harness tests, API Spine lineage,
  Ruff and whitespace; the occupied rehearsal then produces volatile evidence
  without changing the semantic bindings.
- Exactly one fresh Gemini 3.7 Flash/high final veto is required after the exact
  candidate and occupied evidence pass.
- Post-freeze repairs rerun only gates whose assumptions changed. No redundant
  canonical profile or intermediate independent review is required.

## Parallelism-efficacy decision

- DeepSeek V4 Flash/high: reserved for one bounded post-freeze mechanical package
  comprising the fixed-path harness, closed contract/schema and focused tests.
- Gemini 3.7 Flash/high: reserved for the single final Tier-2 independent veto.
- Native subagents: declined; one stateful container lifecycle is serial, and a
  second analytical review would duplicate the final veto without separate
  authority value.

Sol retains plan semantics, source admission, occupied execution, integration
and acceptance. No worker may change the boundary, execute the occupied run,
accept its own work or move a protected ref.

## Exact executable and containment profile

- Host executable: locally resolved `docker.exe`; no caller override.
- Image: exact locally present `postgres:16-bookworm`, inspected before create;
  `--pull=never` forbids registry access.
- Container: unique prefix `emr4-delete-confirm-pg16-catalogue-` plus a random
  16-hex suffix, with exact harness and cleanup-nonce labels.
- Network: `--network=none`, no published/exposed port and no network join.
- Storage: one container-local tmpfs at `/var/lib/postgresql/data`; no bind,
  named volume, workspace or Docker-socket mount.
- Bounds: one CPU, 512 MiB memory, 128 processes, no restart, 90-second startup,
  30-second commands and 240-second total run.
- Bootstrap identity: fixed authored-synthetic database/user/password values
  used only inside the owned container; they are not operational credentials.
- SQL transport: `docker exec -i` to in-container `psql` over its local Unix
  socket, argument vectors and `shell=False`; no host database URL.

The harness may inspect only the exact image reference, closed container name
and captured container ID. It never lists, prunes, pulls, builds, logs in, starts
a desktop app or removes an object it did not create and reverify.

## Empty authored-synthetic prerequisite relations

One explicit transaction creates only the empty columns and constraints required
by migration `x3y4z5a6b7c8`:

- `public.users`: UUID `id`, UUID `practice_id`, role and active-state columns;
- `public.appointment_command_idempotency`: the status-confirm v1 receipt fields
  and exact named completeness constraint which the migration widens in place;
- `public.appointment_audit_log`: only the pre-existing columns referenced by
  the additive delete-audit constraint; and
- `public.alembic_version` containing exactly parent `w2x3y4z5a6b7`.

No prerequisite relation contains a row. No appointment relation is required or
created because this migration never references one.

## Migration admission

Before SQL, the captured container must pass exact ownership/profile inspection,
then `pg_isready` and authenticated server-major probes continuously for three
observations. The server major must be 16.

The host generates SQL only through the repository Alembic graph:

`python -m alembic upgrade w2x3y4z5a6b7:x3y4z5a6b7c8 --sql`

A fixed synthetic dialect URL satisfies offline Alembic configuration; it cannot
connect. The harness rejects unexpected revision text, source hash, transaction
shape, database-creation or destructive-table statements. It removes only the
exact outer `BEGIN;`/`COMMIT;` wrapper emitted by Alembic and streams the otherwise
byte-identical body through `psql --file=-`, `ON_ERROR_STOP=1` and
`--single-transaction`.

## Exact catalogue assertions

Read-only `pg_catalog` queries must prove:

- Alembic head exactly `x3y4z5a6b7c8`;
- `users.authority_generation` is non-null `int8`, default one, with its positive
  check and exact `(practice_id, id)` uniqueness;
- `user_capability_grants` has exactly its three non-null columns, composite
  primary key, composite user foreign key, closed two-value capability check and
  exact two-column index;
- `appointment_command_idempotency.authority_generation` is nullable `int8` and
  the named v1 completeness constraint contains the unchanged status branch plus
  only the exact `confirmAppointmentDeleteProposal` / `delete-confirm` branch;
- the seven additive appointment-audit columns have their exact types and
  nullability, and the delete-v1 constraint contains the accepted version,
  action, state-version, cancellation, waiting-area and JSON-array predicates;
- the exact three public PL/pgSQL trigger functions are invoker/volatile trigger
  functions and contain their frozen generation, overflow, parent-lock,
  duplicate-insert and update-rejection tokens;
- the exact three non-system row triggers are enabled at the accepted timings and
  call only those public functions;
- there are no unexpected `emr4_user_*` functions or non-system triggers on
  `users` and `user_capability_grants`; and
- `users`, `user_capability_grants`, `appointment_command_idempotency` and
  `appointment_audit_log` each remain at zero rows.

No DML behavior, downgrade, lock, concurrency, rollback, restart or service
transaction assertion is admitted.

## Evidence and cleanup

Evidence stores only allowlisted metadata, exact object names, counts, canonical
digests and bounded lifecycle outcomes—never raw PostgreSQL logs or environment
secrets.

Cleanup runs in `finally`. Before `docker container rm --force <captured-id>`,
the harness re-inspects that exact ID and requires exact ID, name, image, labels,
network, mounts, bounds, environment and no port bindings. Any mismatch refuses
removal and returns `cleanup_ownership_unverified` with the ID for human
attention. Verified removal is followed by exact-ID absence proof. No image,
volume, network, workspace path or unrelated container is removed.

## Acceptance

Pass only if:

1. the fresh five-source receipt passes and every exact source hash matches;
2. the contract and evidence are whole-document schema-valid and at least eighty
   hostile leaf mutations fail closed;
3. Docker/image/profile/readiness and offline revision range are exact;
4. installation succeeds in one transaction;
5. every allowlisted catalogue assertion and every zero-row assertion passes;
6. exact-owned cleanup proves absence;
7. focused harness, plan, API Spine lineage, latch/Compass/baton, Ruff and
   whitespace gates pass; and
8. one fresh Gemini 3.7 Flash/high final veto passes the exact candidate.

## Data, provider, cost and authority boundary

Only repository-authored schema metadata and empty authored-synthetic relations
enter one local disposable PostgreSQL container. No patient, clinical,
real-person, product, historical diary or protected data; provider, ADC,
credential/IAM, browser or external network; behavior probe, route, watcher,
event, command, durable storage, deployment, production, release, Pages or
protected ref is opened. Cloud cost is zero. The locally cached PostgreSQL image
cannot be downloaded by this tranche.

## Recovery and next candidate

Missing Docker/client/daemon/image returns `environment_unavailable` without
fallback. A migration, catalogue or harness defect receives one evidence-backed
repair within this exact representation-only envelope; changed assumptions rerun
only affected gates. Cleanup ownership uncertainty stops destructive action.
Yuri's attention is required only for a human-only environment action or a
materially different unplanned outcome.

Passing would make a provider-free disposable PostgreSQL delete-confirm
behavior/transaction rehearsal the next narrow candidate. It would still not
authorize route mounting, UI, product data, concurrency/restart/unknown-commit,
deployment or production.
