# Provider-free check-in relay-free recovery attempt 007 failed-closed closeout

Date: 2026-08-23

Timestamp: 2026-08-23T03:30:46.2756937+10:00 (Australia/Brisbane)

Status: `accepted_failed_closed_negative_evidence`

Operation:
`raisa-provider-free-disposable-postgresql-default-off-check-in-relay-free-rollback-unknown-commit-recovery-attempt-007`

Exact occupied source:
`b7c37a76c41d399c4b198d3ab6b526c5510b434b`

Exact terminal commit:
`6657ee5061265d732096e9987f327d82feed800c`

## Result

Attempt 007 is consumed once and failed closed at
`redaction/forbidden_field`. Its closed envelope records one occupied
invocation, zero automatic retries, no success, no ordinary admission, no
product record and no transaction attestation. The failure and envelope
SHA-256 values are respectively
`86e5e1342eb54e062e35d73390ebceb141d097d03e180e4fe3c0ed64b465f422`
and
`3338c58054dea96b3845827dacfe184889ee328e5a4463966464b560d0a2c2c5`.

Read-only postterminal inspection found zero matching owned containers and
zero matching owned networks. The attempt-007 namespace now mechanically
denies reuse. No retry, resume or fallback occurred or remains authorised.

## Exact claim boundary

Deterministic source evaluation reproduces `redaction/forbidden_field` when
the accepted success result copies the contract's closed-boundaries projection.
The exact conflicting key is
`live_secret_existing_hosted_or_product_database_used`, because the evidence
redactor rejects keys containing the forbidden `secret` token.

This is a strong source-supported explanation, not transaction acceptance.
The successful-result redaction check is positioned after the base harness's
final cleanup, so the failure is later than attempt 006's server-start
coordinate. However, the attestation and success evidence are absent, and the
wrapper's terminal projection reports `cleanup_status=not_started`. Therefore
rollback, unknown-response commit recovery, internal role absence before
teardown and the intended success result remain unproved. Only external owned
Docker-resource absence is independently accepted.

## Workflow efficacy

The clockwork constrained the expensive part correctly: exactly one occupied
invocation, zero retry, immutable terminal, zero owned Docker residue and no
protected movement. It also caught the preplanning manual-Git-ID recurrence
before planning.

Low-cost construction circles remain. Pure tests caught a forbidden `argv`
substring in new envelope field names, a repeated raw line-wrap assertion and
a contradictory negative assertion. The first broad test selection was
rejected before collection because it included a direct `tests.conftest`
import. These point to generated enum/vocabulary fields, normalized document
predicates and AST-classified test manifests rather than more memory rules.
Two closeout dry runs also rejected caller-authored relationship shapes before
publication; the single immediately projected predecessor now passes, and the
next control is to generate that edge from live projection state.

The occupied failure exposes a more important missing preflight: the complete
prospective success-evidence object was not passed through its final redaction
predicate before the database run. The wrapper also needs a typed terminal
bridge for exceptions raised after base cleanup so that it does not collapse
the cleanup projection to `not_started`.

DeepSeek remained declined because its native Harness lane is paused and no
provider call belonged in the serial database lifecycle. Gemini remained
declined because the result is a deterministic failed-closed terminal, not an
independent model-judgment surface. Native subagents remained declined under
developer policy and the indivisible cleanup lease. GPT Sol retained custody.

## Next boundary

The narrow successor is
`raisa-provider-free-read-only-check-in-attempt-007-redaction-forbidden-field-and-cleanup-projection-coordinate-diagnosis`.
It may bind the immutable terminal, trace the exact redaction/control-flow and
cleanup-projection coordinates, and specify the smallest deterministic repair
without creating a Docker object, starting PostgreSQL, executing SQL or
authorising attempt 008.

Dedicated check-in remains default-off. No route, feature flag, allowlist,
action grammar, first-party client, waiting-area behavior, REST/OpenAPI,
GraphQL, product configuration, product/patient/appointment/clinical/
historical/protected data, provider call, production runtime, deployment,
release, Pages or protected ref changed. Local/origin `master` and
`handoff/current` remain exact
`2e34bdad732fdab32fbf778280b3d3c70d66d602`. `docs/branding/` and all
unrelated untracked paths remain preserved.
