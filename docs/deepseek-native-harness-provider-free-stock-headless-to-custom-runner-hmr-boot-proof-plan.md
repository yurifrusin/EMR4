# DeepSeek native Harness provider-free stock-headless-to-custom-runner HMR boot proof plan

Date: 2026-08-20T02:07:29+10:00 (Australia/Brisbane)

## Authority and exact source

This plan freezes the active operation
`deepseek-native-harness-provider-free-stock-headless-to-custom-runner-hmr-boot-proof`
at exact planning source `f5a00279f428698b5ce789d022404335a54a7d95`.
It follows the accepted no-database manifest/runner admission repair at that
same clockwork source and Yuri's standing uninterrupted-development authority.

The consumed occupied rc.7 attempt is immutable negative evidence. It exited
before the custom runner, Harness session or provider boundary because the
stock headless launcher's watch-only HMR fallback requires Node
`--expose-internals`. This tranche may prove that prerequisite and lifecycle;
it may not retry or resume the consumed attempt.

## Frozen objective

Execute the narrowest provider-free native-Harness proof that:

1. materialises exact `@deepseek-ai/dsh@0.1.0-rc.7` from the existing local npm
   cache with lifecycle scripts, audit and online fallback disabled;
2. invokes the package-declared `lib/bin.js` launcher with the documented
   `--profile headless` surface and the exact Node `--expose-internals`
   prerequisite;
3. disables the stock `headless-runner`, `code-runtime` and session telemetry
   before the tree mounts, so no agent, model request, provider call or worker
   runtime can start;
4. observes the launcher's real post-boot HMR service and both exact stock user-
   patch registrations without supplying readiness from the controller;
5. atomically changes the watched profile patch only after that readiness;
6. requires the HMR-refreshed composition, and no initial layer, to insert a
   local custom runner which records the exact terminal event and requests
   application exit 0; and
7. proves exact event order, zero network attempts, one bounded native process,
   terminal exit 0 and complete process/filesystem cleanup.

The result proves only that the pinned native Harness can boot stock headless
and reach a local custom runner through its own HMR-backed profile-patch path.
It does not prove a DeepSeek model call, coding quality, provider reliability,
occupied-worker admission or production suitability.

## Exact lifecycle and evidence contract

The single execution owns these serial stages:

1. verify the exact package tarball SHA-1
   `8a69013c06179d7af437de92fb4a9a2e1fd7d410` and npm integrity
   `sha512-ZceDCJ8FAywih+USW/OMk9jEhunlvJBGEz4kqrhau23hPzbciOazZrywH0nBRsaalSeAJ1JGBmjtw4OSjToStw==`;
2. create one controller-owned disposable root outside the repository and
   install from that tarball with npm `--offline --ignore-scripts --no-audit
   --no-fund`;
3. initialise the exact shipped headless profile through the package CLI's
   boot-free default-config surface, then write the initial local patch and
   two local proof plugins;
4. launch exactly one native process with a scrubbed credential environment,
   telemetry disabled and a local fail-closed network guard;
5. accept readiness only after the sentinel records that the rc.7 HMR service
   is active and its config registry contains both the exact profile and home
   patch paths;
6. replace the profile patch atomically with a second composition whose only
   new row is the local custom runner;
7. accept success only when the event ledger is exactly
   `sentinel_activated`, `stock_headless_hmr_ready`,
   `custom_runner_reached`, `app_exit_requested`, followed by native exit 0;
8. collect only bounded sanitized facts and digests into repository evidence;
   and
9. terminate then kill on a deadline when necessary, wait for process absence,
   remove the exact disposable root and prove its absence in all terminals.

The initial and changed patch byte digests, plugin byte digests, exact Node/npm/
package versions, invocation-shape digest, event projection, duration, exit
code, network-attempt count and cleanup facts are retained. Raw environment,
credentials, task text beyond the fixed authored-synthetic label, npm cache,
stdout/stderr bodies and disposable paths are not retained.

## Fail-closed rules

Execution must reject or terminal as failure for any of:

- wrong package name/version, tarball SHA-1/integrity or package-declared bin;
- online package fallback, install script execution or a missing cached
  dependency;
- missing `--expose-internals`, wrong profile/launcher, an additional native
  boot process or any occupied-worker input;
- the stock runner, code runtime or telemetry remaining enabled in the initial
  composition;
- the custom runner appearing in the initial patch;
- readiness synthesized by the controller, fewer than both exact stock patch
  registrations, patch mutation before readiness or a non-atomic replacement;
- malformed, duplicate, missing or out-of-order lifecycle events;
- any provider credential in the child environment, provider/broker/model
  boundary, DNS/socket/HTTP/fetch attempt or external network activity;
- nonzero/ambiguous exit, deadline expiry, surviving process or residual
  disposable root; or
- mutation of historical terminal evidence, protected evidence or protected
  refs.

There is no automatic second native boot. A construction defect found before
the frozen execution may be repaired through deterministic unit tests. Once
the native process starts, its terminal is evidence and any further attempt
requires a separately frozen successor.

## Acceptance

Acceptance requires all of the following:

1. static source evidence binds the exact rc.7 launcher, headless bundle, HMR
   prerequisite, `registerConfig` readiness and `watchUserPatches` mechanism;
2. hostile deterministic tests reject wrong package identity/integrity/bin,
   unsafe patch shapes, synthetic readiness, early mutation, extra process,
   credential presence, every network primitive, event disorder, deadline,
   ambiguous terminal and incomplete cleanup;
3. one actual provider-free rc.7 launch reaches the custom runner only after
   the watched patch mutation and exits 0;
4. the retained evidence records zero model, broker, provider and network
   requests plus complete cleanup;
5. focused provider-free tests, Ruff and compile checks pass without ordinary
   pytest, Docker, PostgreSQL or product data;
6. one fresh Gemini 3.7 Flash/high read-only veto reviews the exact clean
   candidate after deterministic success; DeepSeek does not review its own
   harness admission proof; and
7. closeout uses the governance clockwork, explicit-path staging/commit/push,
   a paired Yuri summary and the usual non-PHI Pushover notification.

## Explicit parallelism assessment

- DeepSeek: declined as a worker and reviewer. The native Harness is the
  provider-free subject under test; no model call is authorised and Claude
  Code is not a fallback.
- Gemini: reserved for one fresh exact-candidate read-only veto after the
  deterministic proof exists. It owns no implementation, execution or repair.
- Native subagents: declined. Current session policy does not authorise them,
  and process launch, observed readiness, patch mutation, terminal capture and
  cleanup form one serial ownership chain.

## Protected boundaries

This tranche authorises no occupied DeepSeek attempt, model/provider call,
credential, external network, Docker, PostgreSQL, SQL or product database. It
authorises no EMR4 product/config/API/client/waiting-area change, ordinary-
practice enablement, feature-flag/allowlist change, generic-status `Arrived`
change, action grammar, first-party client, patient/product/clinical data,
production runtime, deployment, release, Pages or protected-ref/evidence
movement. Attempt-004 remains closed pending a separately frozen successor.

Local and origin `master` and `handoff/current` remain exactly
`2e34bdad732fdab32fbf778280b3d3c70d66d602`. All untracked files, especially
`docs/branding/`, remain preserved. Staging is by explicit path only.

## Reasoning level

Extra High for native-process containment, lifecycle attribution and terminal
cleanup; High for implementation and deterministic verification.
