# Reception One Receptionist-first v6.4 Exact-Constraint Plan

Status: active bounded repair plan
Recorded: 2026-07-30

## Purpose

Repair the two v6.3 failures without revising any historical node:

1. strengthen the deterministic proofreader so every independently recognized
   exact semantic or selected-context constraint must survive into the typed
   source graph before a proposal can be admitted; and
2. instruct the model that its bounded natural receptionist response,
   decision note and typed form must describe the same goal, including after a
   correction ticket.

The complete original twenty-four-request authored-synthetic cohort will be
rerun, including every v6.0 pass. v6.3 remains immutable and no v6.3 ledger may
be reopened.

## Exact repair

The proofreader will derive a constraint manifest from the existing
deterministic semantic adapter. For a recognized actionable request, each
`semantic_ref` and `context_ref` in that manifest must appear with the same
field in the model-authored normalized plan. An omitted, replaced or broadened
constraint is a deterministic violation:

- turn one may receive one closed correction ticket;
- turn two must edge-abort if the defect remains; and
- no non-exact proposal may be released.

This does not require an unknown novel composition to copy a deterministic
operator sequence. It constrains only request facts the deterministic adapter
already recognized independently.

The output schema, typed operator catalogue, desk context, response ceiling,
temperature and thinking budget remain unchanged.

## Frozen boundaries

- provider: Google Cloud Vertex AI;
- model: `gemini-2.5-flash`;
- project: `bernie-emr4-dev`;
- identity:
  `emr4-bernie-ai-dev@bernie-emr4-dev.iam.gserviceaccount.com`;
- authentication: existing keyless impersonated service-account ADC;
- location: `australia-southeast1`;
- endpoint: `australia-southeast1-aiplatform.googleapis.com`;
- data: authored-synthetic only;
- 24 primaries and at most one terminal second call per case;
- absolute 48-call and USD 1 ceiling;
- no API key, static key, global endpoint, fallback, provider tool, grounding,
  retrieval, cache creation, product/database access, appointment write,
  product delivery, production, deployment or release; and
- raw prompts, raw provider responses, credentials, API-key information and
  hidden chain-of-thought are not retained.

The no-show-to-`dna` alias remains closed.

## Deterministic gates

Before an occupied call:

1. all twenty-four reference forms pass the new constraint-manifest review;
2. a form that omits corrected 3:00 PM bounds is rejected and receives an
   exact closed correction ticket;
3. the repaired replacement is admitted and releases only the 3:00 PM slot;
4. the existing historical wrong-form set remains rejected;
5. provider-blocked, real-isolation, focused, API Spine, Continuity, Compass,
   JSON, compilation, Ruff and whitespace gates pass;
6. the exact read-only Bernie/Sydney cloud-control preflight passes;
7. pre-run residue is zero; and
8. Continuity and rendered Compass revisions bind the frozen candidate.

Once occupied execution starts, no prompt, schema, proofreader, desk-context
or generation-setting change is permitted.

## Acceptance

Capability acceptance requires all twenty-four terminal outcomes to match
their frozen oracle, all ledgers and audit chains to close, no call after the
terminal result, and zero task residue. Any mismatch closes v6.4 candidly and
releases no product capability.

The evidence remains a reused development cohort, not an independent holdout.
The Sydney locational path does not prove Australian physical or sovereign
processing.
