# Ariadne agent error and correction register revision 123

Date: 2026-08-09

Status: bounded register correction candidate

Revision 123 adds AER-0147 and AER-0148 and brings the register to 148 bounded
incidents with zero open incidents.

## AER-0147 — invalid verifier predispatch receipt shape

The first record-access behavior-veto predispatch state used a descriptive
Antigravity method that was not admitted by the transport contract and placed
the separately preflighted review worktree in a structure whose semantics
require alignment with `handoff/current`. The generic orchestrator preflight
returned `revision_required` with both exact reasons before any verifier call.

The failed receipt remains immutable. The corrected distinct state copies
`agy_cli_observation` from the admitted transport configuration and records the
external review worktree in five-source evidence rather than as a baton worker.
Its receipt passed without changing the candidate or any ref.

## AER-0148 — verifier reported stale behavior attempt 016

The first fresh Gemini 3.6 Flash/high review correctly reported the candidate,
parent hashes, unchanged scenario digest, 73 tests and clean r105 postflight,
but twice described the current failed behavior evidence as attempt 016. The
current protected mutable evidence and immutable failure copy are attempt 021.
Its terminal `pass` is therefore preserved but rejected before runtime.

A corrected packet must mechanically bind attempt 021 and prohibit attempt 016
as current evidence. A distinct fresh exact-HEAD Gemini review is required;
neither the candidate nor the twenty scenarios changes.
