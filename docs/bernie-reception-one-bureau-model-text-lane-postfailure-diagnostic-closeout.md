# Reception One Bureau Model Text Lane — Post-Failure Diagnostic Closeout

## Result

`reception_one_bureau_model_text_schema_state_diagnostic_pass`

The retained HTTP 400 `INVALID_ARGUMENT` explicitly identifies excessive
structured-schema serving states. The former response schema nested step
objects, argument arrays and bounded strings under a twelve-step array. The
repository-local repair replaces only that provider wire representation with:

- one free string `goal`; and
- one bounded array of compact plan-line strings.

Trusted code deterministically decodes each line back into the unchanged
`ModelPlanCandidate`, after which the unchanged exact candidate schema,
trusted scope attachment, typed proofreader and in-memory executor apply.

Focused tests prove one response array, no provider enum constraint, exact wire
round-trip, hostile candidate rejection, one-use broker proofreader release
under a fixture response and bounded audit construction. The diagnostic and
repair made zero provider or credential calls and changed no external state.

This repair is ready for a future separately authorised occupied retry. It does
not prove that Vertex will accept the repaired schema or that a model candidate
will pass the proofreader.
