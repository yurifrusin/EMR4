# DeepSeek native Harness provider-free custom-runner pre-request failure-coordinate diagnosis closeout

Date: 2026-08-21

Status: `accepted`

Reasoning level: `high`

## Result

The provider-free diagnosis passes. The accepted attempt-005 runner is bound
byte-exactly, as are the four relevant cached rc.7 source members. Its generic
post-HMR catch is now mapped to seven ordered runner source coordinates. A
future-only sidecar design admits exactly those seven stages, three cause
coordinates and seven sanitized error kinds; descriptive prose, shortened Git
IDs, extra keys, raw messages, stacks, paths and causes all fail closed.

The diagnostic candidate is exact source
`167f8330216b84f2981469299575f4fa7ad1f7e8`. The reviewed closeout source is
`97c4367cc21c7bf9dad1cbeab6be0da314fec3be`, which additionally moves the
live-handover compaction into the clockwork's sole deterministic writer, binds
the byte-exact rollback lease and installs one rolling Harness acceptance slot
without changing authority.

## What became possible

A future runner can record the exact operation active when a rejection escapes
without asking an LLM to invent a stage label and without retaining dynamic
exception text. The runner owns the stage variable; the helper can only project
one schema-admitted value and rethrows the identical primary rejection.

This is the first concrete form of the broader typed-control-seat principle:
reasoning may choose among admitted controls, but deterministic code owns facts,
state transitions and actuator admission.

## Verification

- 19 focused hostile and source-binding tests, 106 predecessor regressions and
  548 final published-state governance tests pass.
- Ruff, Python compilation and all six generated JSON artifacts pass.
- The selected native-Harness predecessor, orchestrator, latch, clockwork,
  register and handover packets pass after the baseline compactness repair.
- The clockwork projection is 79,195 bytes and 489 lines; 222 historical
  lookup rows remain verbatim in the hash-bound index.
- Node, Harness, broker, worker, model, provider, network, database, Docker and
  raw-attempt-stream counts are zero.

## Honest limits

The sidecar identifies a runner source coordinate, not an internal rc.7
sub-operation. `first_turn_idle_wait` supports a pre-request conclusion only
when joined with an independent broker reading of zero requests. No DeepSeek
request, reasoning, coding or tool behavior was exercised, and native Harness
readiness remains unproved.

The accepted attempt-005 runner and terminal remain immutable and consumed.
No occupied retry, product/configuration change, ordinary-practice enablement,
generic-status `Arrived` change, product or protected data, production,
deployment, release, Pages or protected-ref movement occurred.

## Workflow observations

Eleven corrected or contained observations are recorded in register revision
596. They cover caller-authored Git IDs in a receipt draft, environment-derived
cache lookup, a test-literal typo, loss of an asynchronous test-session handle,
direct-path script invocation, error-kind precedence and recurrent live-baton
compactness drift, plus the rejected manual edit to a clockwork-owned canonical
surface, the rejected legacy `contract_evidence` shape and one rolled-back
successor boundary-token omission, followed by the rejected unbound rollback
lease recurrence. None is open.

## Next operation

Proceed under standing authority with
`deepseek-native-harness-provider-free-post-hmr-pre-request-diagnostic-sidecar-integration-rehearsal`.
It may integrate the accepted helper and code-owned stage variable into a new
future-only runner derived from the accepted runner, then prove controller
ingestion and broker-zero join semantics using provider-free fixtures. It may
not mutate the accepted runner or consumed attempt, launch Node/Harness/broker/
worker/model/provider activity, or open any product, data, production,
deployment, Pages or protected-ref surface.

The continuing non-PHI Pushover notification succeeded with request
`3100bb53-025c-456a-8256-3ddd43bbae71`.
