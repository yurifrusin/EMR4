# Ariadne agent error and correction register — revision 666

Date: 2026-08-24

Timestamp: 2026-08-24T16:22:26.0503913+10:00 (Australia/Brisbane)

Register revision: `666`

Incident count: `1160`

Open incidents: `0`

New incident: `AER-1160`

<!-- ariadne-agent-error-register-reading
revision: 666
incident_count: 1160
new_incident_ids: AER-1160
open_incident_count: 0
-->

## AER-1160

Four contained prepublication workflow lapses were corrected as one bounded
verification incident. The first release privacy check searched for the
generic substring `reason` and therefore rejected the legitimate typed field
`result_reason_counts`. The first candidate-precommit continuation intent then
included script/test paths outside the serial receipt's admitted evidence
roots. Finally, the first long combined pytest invocation yielded without its
session identifier being retained, preventing direct citation of its terminal
exit record. The first semantic closeout draft also used the descriptive
candidate-state label `accepted_candidate_unchanged` outside the register's
closed vocabulary.

The privacy assertion now distinguishes exact patient-shaped keys and opaque
values from admitted adapter reason fields. The receipt intent now uses only
documentation and orchestration evidence roots. The exact 264-node combined
profile was repeated with its session handle retained and reached terminal exit
code zero. The closeout now uses the admitted `canonical_unchanged` state and
passes semantic admission. Fourteen focused tests, Ruff, compileall, idempotent generation and
the unchanged adapter Git blob also pass. No historical data, provider, product
runtime or protected ref was touched.

Durable prevention: use exact-key privacy assertions; keep implementation paths
in deterministic checks and explicit staging rather than serial receipt
evidence roots; always retain and poll the returned session identifier for
commands expected to outlive the initial terminal yield; and select incident
states only from the clockwork's typed vocabulary.
