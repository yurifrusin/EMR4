# Raisa provider-free authored-synthetic historical Diary Word-coordinate timeout containment and throughput recovery — closeout

Date: 2026-08-24

Timestamp: 2026-08-24T06:09:21.0361409+10:00 (Australia/Brisbane)

Status: `accepted_pass_pending_clockwork_publication`

Exact reviewed source: `9cbc39cda3503216b27e81aca4f9199db8561b49`

## Lay outcome

The Word extraction mechanism is now safe and fast enough to justify one new,
bounded local measurement. A deliberately timed-out synthetic Word process was
removed by its exact identity without touching the Word process already open
for the user. A second proof read twelve newly authored generic documents and
completed 2,016 coordinate attempts plus 288 explicit time anchors within the
planned five-minute terminal.

All synthetic documents and temporary controls were removed. No historical
diary file was enumerated or opened in this tranche. The first-use gate is now
an explicit default-deny boundary: historical-derived material may not become
a reusable scenario, fixture, benchmark or product input merely because a
measurement succeeds.

## Technical outcome

- the parent now classifies an interrupted extractor as
  `word_extractor_timeout` only after exact PID, `WINWORD` class and start-time
  cleanup is proved;
- a missing or mismatched identity fails closed and never falls back to broad
  process-name termination;
- count-only progress exposes bounded totals and coarse elapsed/rate buckets,
  with no path, filename, source value, time, page or coordinate value;
- the occupied timeout proof removed exactly one owned Word process and
  preserved the pre-existing process set;
- the occupied throughput proof opened and parsed 12/12 synthetic documents,
  attempted 2,016 coordinates and found 288 explicit story anchors in the
  `30_to_119_seconds` / `16_or_more_per_second` buckets;
- all 187 provider-free historical-Diary controls and all static checks pass;
  and
- generated documents, control/progress files and private extraction products
  are absent after cleanup.

The accepted successor is exactly one fresh 80-snapshot local measurement at
the new `2026-08-24-story-coordinate-v2` attempt root, with one metadata bind,
one content run, no retry, an 1,800-second ceiling, count-only progress and
exact parent cleanup. Its strongest result remains ignored local research
retention. First use requires a later candidate-specific gate decision.

No provider, model, product, database, ordinary-practice, production,
deployment, release, Pages, protected-evidence or protected-ref authority is
opened. Local/origin `master` and `handoff/current` remain exactly
`2e34bdad732fdab32fbf778280b3d3c70d66d602`.
