# Ariadne agent error and correction register — revision 530

Date: 2026-08-19

Timestamp: 2026-08-19T06:42:34.3202175+10:00 (Australia/Brisbane)

Status: accepted correction and containment update

## Change from revision 529

Revision 530 preserves three closeout failures rather than hiding them from the
clockwork efficacy baseline:

- AER-0611 records a read-only Windows wildcard path operand that ripgrep could
  not resolve; the corrected search uses an inventoried directory root and a
  native glob filter.
- AER-0612 preserves the first post-compaction receipt's fail-closed rejection
  because its runtime state omitted the configured `deepcode_cli` and
  `claude_cli_print` observations. The corrected attempt includes the complete
  adapter inventory in a distinct state and receipt.
- AER-0613 records a read-only search that bundled one valid register test with
  three guessed sibling paths; the corrected inspection uses only the exact
  inventoried test path.
- AER-0614 records a read-only latch-test search that bundled one valid path
  with one guessed continuity-test sibling; the corrected inspection discovers
  matching test inventory before using exact operands.

## Register state

Revision 530 contains 614 bounded incidents. All are corrected or contained;
none is open. AER-0611, AER-0613 and AER-0614 recur under
`operator.explicit_repository_path_operand_not_inventory_resolved`.
AER-0612 recurs under
`orchestrator.preplanning_adapter_observation_inventory_incomplete`.

## Clockwork consequence

The architecture closeout's conventional baseline advances from nine to thirteen
failure-induced verification reruns. That is not a retroactive architecture
failure: it is the honest reading of additional conventional procedure after
the architecture candidate was accepted. The successor shadow rehearsal must
compare against all twelve rather than a cosmetically frozen earlier number.

The future reducer should source command operands and the adapter inventory from
typed settings. Until then, inventory discovery and exact-file inspection are
separate readings, and every generated receipt is parsed immediately before its
governed action.

No product, patient, clinical, provider credential, deployment, release,
Pages, protected-evidence or protected-ref authority changed.
