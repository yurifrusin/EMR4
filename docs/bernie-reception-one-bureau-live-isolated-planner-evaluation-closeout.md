# Reception One Bureau live isolated-planner evaluation closeout

Status: closed, failed safely
Recorded: 2026-07-31
Occupied calls: 1 of 1

## Result

The visible Bureau path made exactly one authorised authored-synthetic call to
Gemini 2.5 Flash through the exact Bernie keyless impersonated-ADC Sydney
Vertex lane. Vertex returned HTTP 200 in 5,110 ms. The deterministic
proofreader released nothing.

The terminal result is
`reception_one_bureau_live_isolated_planner_failed_closed`.

## What happened

The browser selected `isolated_vertex` and made one authenticated route
request. The backend constructed a fresh context frame, but the frame recorded
`selected_appointment_absent`. The model therefore filled the form as a
clarification and asked which Margaret Thompson appointment should be
extended. The deterministic intent gate expected `resize` from the utterance
and rejected the typed clarification with
`recognized_intent_goal_mismatch` at `$.typed_form.goal_code`.

This is a local acceptance-harness context-binding defect, not evidence that
the model failed to understand the receptionist language. The harness opened
the overview and submitted the request without first selecting the exact
authored-synthetic appointment. The ordinary overview does not contain a
selectable appointment item, so the route received no selected appointment
identifier.

## Safety outcome

- The one-use ledger consumed exactly one provider call.
- The one-call authority prevented the correction ticket from producing a
  second call.
- No deterministic fallback occurred.
- No unverified draft or bounded provenance was displayed.
- No appointment was confirmed or written.
- Database truth was unchanged.
- The exact provider, model, project, identity, keyless ADC, Sydney region and
  hostname remained bound.
- Every owned container, image, internal network, process, temporary token,
  runtime directory and disposable database was removed.
- Independent residue verification passed.

## Repair direction

The next repository-local step is provider-free:

1. bind the existing disposable Margaret Thompson appointment into the
   authored-synthetic smoke projection;
2. click that exact appointment in the real Bureau UI;
3. verify the row is selected before submission;
4. run the same 45-minute resize through the deterministic planner and
   proofreader with zero provider calls; and
5. retain only a boolean selection proof and request hash, not the raw
   appointment identifier.

Only after that provider-free path passes should a fresh occupied retry be
considered. The current authority is exhausted and grants no further provider
call.

## Claim limit

This result proves one bounded configured and observed Sydney locational
request path and correct fail-closed behavior. It does not prove an admitted
end-to-end proposal, Australian physical or sovereign processing, production
fitness, or authority for real, patient, health or clinical data.
