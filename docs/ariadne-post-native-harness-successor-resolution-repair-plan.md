# Ariadne post-native-Harness successor resolution repair plan

Date: 2026-08-18

Timestamp: 2026-08-18T21:31:22.5921213+10:00 (Australia/Brisbane)

Status: `frozen`

Source HEAD: `5fab227e7a0bf1d308d1373858f490419fee660e`

Reasoning level: Extra High for freezing the product-successor meaning; High is
sufficient for the bounded documentation, continuity, test and Git correction.

## Evidence conflict

The live baton and accepted route closeout prove that
`raisa-provider-free-default-off-canonical-check-in-route-adapter-convergence-rehearsal`
already passed at exact reviewed source
`c82c3a741053a9c8da260aa62e1a968af22bb54e`. The later native-Harness
closeout, Sol acceptance, Yuri summary, Compass current position and live
`Next implementation` row nevertheless direct that same completed tranche
again. Re-executing it would violate fail-closed successor selection and could
create an unnecessary second mutation attempt over already accepted source.

## Objective

Correct only the stale post-Harness successor evidence, register every
qualifying orchestration incident exposed during fresh rehydration, and add a
deterministic guard that rejects a live next-tranche pointer to a tranche the
same baton already records as accepted.

The narrowest true product successor is frozen as
`raisa-provider-free-read-only-ordinary-practice-canonical-check-in-admission-readiness-review`.
That successor may inspect and document the current default-off feature flag,
empty ordinary-practice posture, API Spine command contract, authentication,
tenant, idempotency, audit, rollback, rollout and observability prerequisites.
It may not enable a practice, change configuration, call the route, access
product data, or edit product source.

## Exact accepted anchors

- route plan source: `ad523968713e58f0b42c3a428556c968f61d6d3c`;
- accepted route implementation: `614852c4`;
- accepted reviewed route descendant:
  `c82c3a741053a9c8da260aa62e1a968af22bb54e`;
- route closeout and Sol acceptance both say the Harness rehearsal is the
  immediate successor and ordinary-practice admission remains a later gate;
- accepted Harness evidence:
  `00d4f8d6065ab09b5faf5501c979edd2fa59943c`;
- Harness closeout commit:
  `5fab227e7a0bf1d308d1373858f490419fee660e`;
- protected local/origin `master` and `handoff/current`:
  `2e34bdad732fdab32fbf778280b3d3c70d66d602`.

## Owned repair package

GPT Sol owns the serial repair. It may change only:

- this plan and its threat-model delta;
- the live Current Baton rows that name current/next position;
- the Harness closeout, Sol acceptance and Yuri summary successor wording;
- the Harness Continuity updater, Compass current position/report and their
  exact continuity tests;
- the active-operation latch and its continuity test;
- one new deterministic successor-consistency test;
- the canonical incident register, generated report and correction revisions;
- one closeout, Sol acceptance, paired Yuri summary, Continuity updater/test,
  and the required Ariadne runtime states/receipts.

No route, service, schema, OpenAPI, configuration, database, Diary, Reception
One or first-party client file is editable.

## API Spine posture

The accepted check-in route remains one typed REST/OpenAPI mutation with
practice scope, Receptionist authority, explicit confirmation, idempotency,
audit, committed-event hint and bounded receipt. GraphQL remains read-only.
This repair changes no command, request/response schema, status mapping,
manifest, event contract or runtime. The read-only readiness successor must
retain those boundaries and treat feature admission as a separate future gate.

## Deterministic acceptance

The repair passes only if:

1. Git proves `c82c3a741053a9c8da260aa62e1a968af22bb54e` is an ancestor of current HEAD.
2. The live baton still names the route convergence as accepted and never names
   it as the next implementation.
3. Harness closeout, Sol acceptance, Yuri summary, Compass current position and
   the Harness updater all name the same read-only readiness successor.
4. A deterministic test fails if an exact tranche name appears in both an
   accepted live-baton row and `Next implementation`.
5. The readiness successor is explicitly no-enable, no-product-code,
   no-product-data and no-live-route.
6. AER-0516 preserves the stale-successor evidence conflict; AER-0517 preserves
   the first receipt's omitted adapter observations; AER-0518 preserves its
   inaccurate tracked-clean source wording. All receive corrected descendants.
7. The corrected five-source receipt remains `passed`, the active latch is
   valid, focused continuity/API Spine/baton/register tests pass, and
   `git diff --check` passes.
8. Local/origin `master` and `handoff/current` remain exact protected source and
   `docs/branding/` plus every unrelated untracked file remain preserved.

## Parallelism assessment

- DeepSeek: declined. Native-Harness occupied work is paused; Claude Code is not
  a silent fallback; authority reconciliation is not a bounded worker package.
- Gemini: declined for this repair because no product candidate or ambiguous
  architecture alternative exists. Reassess for the readiness review and
  before any later admission candidate.
- Native subagents: declined under current developer policy and because the
  live baton/latch/Compass repair is one serial state transition.

Sol alone repairs, verifies, accepts, commits and advances continuity. No
external worker receives integration or acceptance authority.

## Claim and closed boundaries

Passing proves only that successor evidence is internally consistent and will
not repeat the accepted route tranche. It does not prove ordinary-practice
readiness and does not authorise admission.

No product source or behavior, feature flag, practice allowlist, generic-status
`Arrived`, action grammar, first-party client, waiting-area movement, product/
patient/clinical/historical/protected data, provider call, Harness retry,
credential/IAM/network, production runtime, deployment, release, Pages or
protected-ref movement is authorised. Preserve `docs/branding/`; use explicit
path staging only.
