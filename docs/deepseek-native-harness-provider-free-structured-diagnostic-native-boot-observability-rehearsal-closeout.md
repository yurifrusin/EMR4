# DeepSeek native Harness provider-free structured diagnostic native-boot observability rehearsal closeout

Date: 2026-08-21

Timestamp: 2026-08-21T10:15:26.7935902+10:00 (Australia/Brisbane)

Status: `accepted`

Exact reviewed result source:
`36f173d3b4b65d9a65a90cecd06d403c3b1492a2`

Operation:
`deepseek-native-harness-provider-free-structured-diagnostic-native-boot-observability-rehearsal`

## Outcome

The single authorised attempt passed. Exactly one pinned
`@deepseek-ai/dsh@0.1.0-rc.7` entrypoint process ran through the accepted
canonical diagnostic wrapper with the authored absent profile and no task
argument. It exited `1` after 3,552 ms before the first HMR event.

The wrapper produced one canonical safe diagnostic. The controller accepted
the exact v2 terminal with:

- phase `entrypoint_import_rejected`;
- cause `structured_entrypoint_import_rejected`;
- one cause node with `error_kind: error` and every other coordinate
  `none`; and
- zero HMR, broker, worker-session, prompt, tool, model, provider, network,
  Docker and database activity.

Raw stdout and stderr were retained only long enough to record their bounded
byte counts and SHA-256 digests. No raw message, stack, path, environment or
credential value became durable. The process, raw streams, copied package tree
and exact disposable root are absent.

## Verification

The controller's deterministic check passes against the exact full Git
candidate. The complete five-file provider-free allowlist passes 64 tests,
including the Python-only accepted-package-tree materialisation fixture.
Ruff, Python compilation, JSON/schema relationships, diff hygiene, zero
matching native processes and all four protected-ref readings pass.

Attempts 001, 002 and 003 and their accepted evidence remain immutable.
`docs/branding/` and every unrelated untracked file remain preserved.

## Workflow observations

The expensive part behaved as intended: one native process, one terminal and
no retry. Three local procedural defects were contained without consuming a
second Harness attempt:

1. the first preexecution receipt repeated the candidate object in prose and
   was rejected before launch; the corrected receipt leaves object ownership
   to the machine snapshot;
2. one composite PowerShell prelaunch reading had a parser error; the
   decomposed fail-closed reading then passed before launch; and
3. the checkpoint test manifest included a pytest `-k` selector that the
   literal-path provider-free wrapper intentionally rejects; the same
   acceptance set then passed as whole files.

The first read-only closeout check also rejected two noncanonical incident-stage
values before transaction preparation; their typed correction is AER-0747.

The first uncommitted successor latch then omitted one exact legacy
closed-surface token. The postpublication baton test rejected it; clockwork
rolled that generation back byte-exactly before corrected publication.
The replacement dry-run initially preceded the required durable rollback-lease
commit and was rejected for pointer drift; only that clockwork-owned pointer
was committed before the replacement transaction.

Register revision 581 records AER-0744 through AER-0749. All are corrected or
contained, none is open, and none caused a provider call, second native
attempt, evidence mutation, product effect or protected-ref movement.

## Conclusion

The structured diagnostic mechanism is promising and now proven at the real
pinned Harness entrypoint boundary, not merely against authored Node fixtures.
It reliably converts this source-backed pre-HMR failure into a bounded,
machine-readable terminal before destroying raw evidence.

This is not evidence of a DeepSeek worker, model reasoning, coding quality,
provider reliability or occupied-session readiness. The next justified step
is to converge the accepted wrapper and terminal reader into the future
bounded-worker controller while remaining provider-free. Only after that
controller convergence is accepted should a separately named fresh occupied
attempt be considered.

## Next tranche

Proceed under standing authority with
`raisa-provider-free-authored-synthetic-native-harness-structured-diagnostic-bounded-worker-controller-convergence-rehearsal`.
It may compose the accepted wrapper and v2 reader into the bounded authored-
synthetic worker controller and prove deterministic pre-provider lifecycle
relationships without launching Harness, broker, worker, model or provider
activity.

It authorises no occupied attempt, retry or reclassification; no product
source/configuration, API, database, route, adapter, flag, allowlist, grammar,
client or waiting-area change; no ordinary-practice enablement or generic
status `Arrived`; no product, patient, appointment, clinical, historical or
protected data; no production runtime, deployment, release, Pages or
protected-ref movement.

Local/origin `master` and `handoff/current` remain exactly
`2e34bdad732fdab32fbf778280b3d3c70d66d602`.
