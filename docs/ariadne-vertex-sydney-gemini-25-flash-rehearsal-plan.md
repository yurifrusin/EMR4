# Ariadne Sydney Vertex Gemini 2.5 Flash Rehearsal — Conditional Tranche Plan

Date: 2026-07-24
Owner: GPT Sol
Authority:
`docs/ariadne-vertex-sydney-bounded-work-cell-authority.md` plus
`docs/ariadne-vertex-sydney-gemini-25-flash-authority-delta.md`
Status: closed at Tranche 3, conditional gates frozen
Target result:
`ariadne_vertex_sydney_gemini_25_bounded_work_cell_rehearsal_pass`

## Purpose and lineage

This is a fresh Continuity descendant of the immutable Gemini 3.5 Flash
provider-admission stop. It does not amend that result or any consumed
Terra/Gemini or T3R7 evidence.

The intended endpoint is one authored-synthetic occupied cognitive work cell
using the exact published GA model ID `gemini-2.5-flash` through the Sydney
`australia-southeast1` Vertex locational endpoint and the existing Bernie
keyless impersonated-ADC boundary.

## Frozen binding

| Control | Exact binding |
|---|---|
| Provider | Google Vertex AI |
| Model | `gemini-2.5-flash` only |
| Project | `bernie-emr4-dev` |
| Service account | `emr4-bernie-ai-dev@bernie-emr4-dev.iam.gserviceaccount.com` |
| Authentication | `keyless_impersonated_service_account_adc` |
| Required permission | `aiplatform.endpoints.predict` |
| Region | `australia-southeast1` |
| Base URL | `https://australia-southeast1-aiplatform.googleapis.com` |
| Data class | authored-synthetic only |
| Primary occupied call ceiling | 1 |
| Conditional repair retry | at most 1, exact request-contract defect only |
| Absolute occupied calls | 2 |
| Application cost ceiling | USD 1 |
| Provider/model/region fallback | prohibited |
| API-key/static-key path | prohibited |
| Product/database/command authority | none |

The 2026-10-16 retirement date is accepted only for this bounded rehearsal.
It supplies no product, production, promotion, long-lived platform or
post-retirement authority.

## Boundary classification

Classification:
`developer_only_authored_synthetic_access_ai_provider_rehearsal`.

The cell receives one minimal typed authored-synthetic evidence frame and no
credential or source-data capability. The one-use broker is the only process
permitted to obtain or refresh a short-lived token from existing impersonated
ADC and the only process permitted to call the exact regional Vertex
data-plane URL. Deterministic proofreader admission is the sole egress.

No GraphQL, REST command, database, product API, event feed, appointment,
clinical, deployment or release surface opens.

## Ordered tranche gates

1. **Residency/provider admission:** current official Google sources must
   publish `gemini-2.5-flash` for model availability and ML processing in
   `australia-southeast1`. Data, geography, retention, cache, audit and
   claim boundaries must pass.
2. **Provider-blocked contracts:** typed request/response, launcher, one-use
   broker, policy manifest, bounded error reducer, audit chain and negative
   endpoint/identity/model tests pass without a model call or rehearsal-prompt
   transmission.
3. **Existing ADC/entitlement:** read-only checks prove exact impersonated
   credentials, project, target service account, cloud-platform scope, enabled
   API, existing billing entitlement, prediction-only custom role and binding,
   exact permission, Data Access audit, absent/disabled request-response
   logging, disabled in-memory caching, no service-account key, no API-key
   path, exact region/model access and no fallback. A required external change
   stops for Yuri.
4. **Provider-free real isolation:** a disposable non-root/read-only/bounded
   cell communicates only with the one-use broker; negative routes fail
   closed; ledger consumption, deterministic teardown and independent residue
   checks pass.
5. **Occupied call:** exactly one primary small authored-synthetic typed
   request, only after gates 1-4 plus revision-bound Continuity/Compass and
   rendered Compass validation pass.
6. **Proofreader/release:** exact schema, grounding, allowed paths/types,
   policy, no command authority, mechanical-only safe repair, freshness,
   supersession, retry/model/project/region binding and atomic typed release
   pass; otherwise frozen abort/human-gate/retry disposition applies.
7. **External audit/closeout:** a durable hash-chained typewriter-key audit
   records admitted typed fields and controls without raw prompt, raw response,
   credential, API-key information or reasoning; every opened ledger is
   consumed and every task-scoped runtime residue is removed.

Each gate opens its successor only on deterministic pass. A failed gate closes
the sequence with complete evidence and no fallback.

## Request and retry contract

The request uses `generateContent` at:

`https://australia-southeast1-aiplatform.googleapis.com/v1/projects/bernie-emr4-dev/locations/australia-southeast1/publishers/google/models/gemini-2.5-flash:generateContent`

It contains one authored-synthetic text instruction, a bounded system
instruction, `responseMimeType: application/json`, a provider-compatible
response schema, deterministic generation parameters, a low output-token
ceiling and no tools, functions, grounding, retrieval, context cache or
provider-executed capability.

One second call is possible only for a deterministically diagnosed
request-contract defect. Before it: consume/audit ledger one, add a focused
regression, rerun repository/provider-blocked gates, open a distinct ledger,
and reverify the exact unchanged provider/model/project/identity/credential/
region/endpoint/data/schema/isolation/residency/cost contract. No other retry
or fallback is authorised.

## Provider-error and audit contracts

Provider errors retain only HTTP status, normalized/provider code,
allowlisted field-violation paths, a scanned diagnostic of at most 2 KiB and a
hash of discarded raw error bytes. Raw prompts, raw responses, credentials,
tokens, unconstrained messages, discarded drafts and reasoning are excluded.

The external audit records attempt/ledger IDs, provider/model/project/service
account/authentication class, API-key-not-used assertion, region/hostname,
request/schema hashes, hash-chained event sequence, supplied latency/usage,
admitted output manifest, proofreader/repair/retry/freshness/supersession
decisions, released authored-synthetic fields, cleanup/residue and audit
control posture.

## Stop boundary

Stop for Yuri on unusable existing ADC; required login or credential
reconfiguration; API/billing/IAM/project/service-account/human-account change;
role broader than exact prediction permission; unavailable Data Access audit;
unverified request-response logging; in-memory caching not verified disabled;
missing Sydney model entitlement; another model/provider/project/identity/
credential/region; global endpoint or fallback; API/static key; non-synthetic
data; possible USD 1 or two-call breach; credential exposure; containment or
audit failure; unexpected charge; or authority expansion.

## Required verification and closeout

Run focused tests, the repository-only Ariadne verifier, relevant API Spine
tests, Continuity/Compass/schema/JSON validation, Python compilation/static
checks, applicable JavaScript syntax checks, `git diff --check`, and
independent container/network/image/credential/process residue checks.

Produce rehydration, pre-plan, pre-attempt and pre-acceptance receipts;
tranche evidence; independent audit; Sol review; closeout; synchronized
Continuity/Compass/rendered report; `AGENTS.md`; and orchestration-ledger
updates.

Do not commit, push, open a pull request, move protected refs or dispatch
external workers.
