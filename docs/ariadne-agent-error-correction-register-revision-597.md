# Ariadne agent error and correction register — revision 597

<!-- ariadne-agent-error-register-reading
revision: 597
incident_count: 844
new_incident_ids: AER-0840,AER-0841,AER-0842,AER-0843,AER-0844
open_incident_count: 0
-->

This revision note binds five corrected provider-free sidecar-integration and
workflow observations to the prospective clockwork-projected register. The
canonical JSON register and pattern report remain clockwork-owned, and one
tick advances the register once.

## AER-0840

The predecessor's final governance packet was called 555 cases in commentary
before completed output established 548. The statement was corrected
immediately and changed no evidence. Exact test counts are now admitted only
from mechanically completed command or collection output.

## AER-0841

The first sidecar-integration contract placed the generated diagnostic-helper
hash in the field intended for the accepted diagnostic Python module. The
generator rejected `source_binding_mismatch` before writing canonical evidence.
A machine hash roster separated all five semantic bindings, the three affected
schema constants were corrected and write plus idempotent check passed.

## AER-0842

After candidate publication, the orchestrator manually expanded the displayed
`3a16f5a0` abbreviation into an incorrect 40-character identity in commentary.
An immediate `git rev-parse` snapshot corrected the candidate to
`3a16f5a00e50259caf993decc67719173348dc0a` and reconfirmed task-origin and
protected-ref alignment. Commit identities may now be stated only by copying a
current machine-resolved 40-character reading.

## AER-0843

The first closeout intent cited revision 597—which the same tick was meant to
generate—as prospective decision evidence. The read-only clockwork preparation
rejected the missing current-node evidence before command execution or
canonical writes. The decision now cites the existing closeout intent; the
derived register revision remains a post-reduction acceptance artifact.

## AER-0844

The corrected closeout intent was checked before its required human-readable
revision-597 intake note existed. The incident-artifact gate rejected the
attempt before command execution or canonical writes. This exact revision,
population, five-ID roster, zero-open reading and ordered headings are now
prepared before the next check. The durable control requires the intake note
to pre-exist preparation unless a future clockwork renderer owns it.
