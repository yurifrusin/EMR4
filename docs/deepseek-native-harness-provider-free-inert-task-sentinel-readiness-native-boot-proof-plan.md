# DeepSeek native Harness provider-free inert-task sentinel-readiness native boot proof plan

Date: 2026-08-21
Timestamp: 2026-08-21T17:30:02.4063086+10:00 (Australia/Brisbane)
Status: `frozen`
Reasoning level: `high`
Planning source: `0c939669bb84eabfb255e8771bb7aae27574e90f`

## Objective

Run at most one fresh pinned `@deepseek-ai/dsh@0.1.0-rc.7` Node/native-Harness
process with the accepted source-repaired sentinel, unchanged sentinel-only
headless profile and exactly one inert authored-synthetic task argument. A pass
requires exactly `sentinel_activated` followed by
`stock_headless_hmr_ready`; any other post-launch result becomes one sanitized
`failed_closed` terminal and cannot be retried.

This proves only that the native Harness can accept a harmless task string and
reach stock-headless HMR readiness before any runner or worker is mounted. It
does not run DeepSeek, create a broker or worker session, send a prompt, call a
model/provider, exercise product code or establish development reliability.

## Frozen lineage and identity

- accepted post-sentinel diagnosis: `5fae13203261be6c8960d1bad599e7f04f749fed`;
- accepted source-repaired boot candidate: `84a9327d98812a9891af0ef5724045f7599eb3a5`;
- accepted sentinel source repair: `eb8913aacb19d823e251731f9393cc54fe71524c`;
- fresh attempt ID: `inert-task-sentinel-readiness-native-boot-attempt-001`;
- exact inert task: `EMR4_PROVIDER_FREE_SENTINEL_READINESS_PROBE`.

The prior consumed terminal and its stream digests remain immutable. The new
operation uses distinct canonical paths and a distinct disposable-root prefix.

## Exact construction and launch

Before the sole process start, the controller validates the contract, full-hash
ancestry, exact component hashes, the accepted diagnosis and the prior terminal.
It copies the accepted rc7 package tree using Python filesystem operations,
materializes the unchanged preset and initial headless profile, writes only the
source-repaired sentinel, rejects every runner row/file, scrubs credential and
proxy names, and preloads the accepted network-denial guard.

The sole command shape is:

`node --expose-internals <pinned rc.7 lib/bin.js> --profile headless EMR4_PROVIDER_FREE_SENTINEL_READINESS_PROBE`

The command contains six arguments and exactly one task argument. The first
`subprocess.Popen` consumes the attempt. The controller polls only the bounded
event ledger, terminates the exact process after readiness or failure, retains
only stream byte counts/digests and bounded terminal fields, then deletes raw
streams, environment, copied package tree and disposable root and proves process
and root absence.

## One-process latch

- Node/native-Harness process limit: one;
- automatic retry, manual retry, resume, fallback and reclassification: false;
- task arguments: exactly one inert authored-synthetic constant;
- changed-profile writes, runner, broker, worker, prompts, tools,
  model/provider requests, network attempts, Docker and database invocations:
  zero;
- raw stdout/stderr, messages, paths, stacks, environment and session state
  retained: false.

Construction defects found before `Popen` may be corrected and rechecked. After
the first `Popen`, no second process is authorised under any terminal.

## Deterministic admission and acceptance

Before execution, the committed candidate must prove exact source/component
bindings, rc7 identity, Python-only materialization, one sentinel row, zero
runner rows/files, the exact six-argument command, one `Popen`, no retry loop,
closed event/failure vocabularies, exclusive attempt outputs, simulated
terminalization/cleanup, compatibility of the generalized launch accounting
with the historical zero-task contract, and zero Node/Harness activity during
checks.

Accept `pass` only for the exact two-event sequence, one started process,
controller termination after readiness, argument count six, task count one,
zero network/provider/worker counters and complete cleanup. A sanitized
`failed_closed` terminal remains an accepted one-attempt result but is not a
passing boot and grants no retry.

## Parallelism assessment

- DeepSeek Flash: declined with negative leverage because a worker/model request
  would breach the provider-free claim and contend with the sole process lease.
- Gemini 3.7 Flash/high: declined with negative leverage because provider review
  is outside this one-process provider-free envelope.
- Native subagents: declined with negative leverage because developer policy
  prohibits proactive delegation and the non-repeatable lifecycle is serial.
- GPT Sol owns contract, admission, sole process, terminalization, cleanup and
  acceptance.

## Protected boundaries

No headless runner, broker, worker, model, provider or network execution; no
retry, resume, fallback, reclassification or second process; no product,
configuration, API, database, route, adapter, flag, allowlist, grammar, client
or waiting-area change; no ordinary-practice enablement; no product, patient,
appointment, clinical, historical or protected data; no production runtime,
deployment, release, Pages or protected-ref movement. Local/origin `master` and
`handoff/current` remain fixed at
`2e34bdad732fdab32fbf778280b3d3c70d66d602`. Preserve `docs/branding/` and
every unrelated untracked path; stage explicit paths only.
