# Reception One Bureau Model Text Lane — Iterative Retry Plan

## Authority and goal

Yuri has authorised the repaired occupied Vertex cell and future retries until
the first positive result. The exact durable authority is
`orchestration/continuity/reception-one-bureau-model-text-lane/iterative-retry-authority.json`.

A positive result means that `gemini-2.5-flash` returns a compact plan-line
candidate which the unchanged deterministic proofreader admits, after which
only the admitted typed fields are released to the in-memory proposal
executor. The sequence stops immediately after that result.

## Frozen boundary

Every occupied attempt is fixed to:

- Google Cloud Vertex AI `gemini-2.5-flash`;
- project `bernie-emr4-dev`;
- service account
  `emr4-bernie-ai-dev@bernie-emr4-dev.iam.gserviceaccount.com`;
- existing keyless impersonated-service-account ADC;
- `australia-southeast1`;
- `australia-southeast1-aiplatform.googleapis.com`;
- authored-synthetic input only;
- the credential-free isolated work cell and one-use host broker;
- no API key, provider tool, function call, grounding, retrieval, cache
  creation, global endpoint, fallback, product context, database, confirmation,
  command, appointment write or product delivery;
- external bounded audit with no raw prompt, response or reasoning; and
- cumulative application cost ceiling USD 1.

The configured and observed locational path must never be described as proof
of Australian physical or sovereign processing.

## Necessary retry loop

Each pass must:

1. close the preceding ledger and retain only bounded provider diagnostics;
2. identify a deterministic request-contract or typed-candidate defect;
3. add or strengthen a focused regression test;
4. rerun the provider-blocked, repository-only, API Spine, schema and isolation
   gates;
5. reverify the exact ADC, entitlement, audit, cache, logging and Sydney
   endpoint posture without external mutation;
6. bind the current Continuity graph and rendered Compass revisions;
7. open a distinct single-use ledger, consume it before exactly one call, and
   audit and clean up that attempt; and
8. either stop on the first admitted proofreader result or repeat within the
   unchanged boundaries.

Ordinary repository-local request repairs do not require another confirmation.
Any stop condition named in the authority JSON does.

## First retry acceptance

The first retry may run only when:

- the former nested-schema regression stays rejected and the compact
  `goal`/`plan_lines` wire passes all focused tests;
- attempt and ledger identifiers are distinct, paired and single-use;
- Continuity revision 67 and Compass map revision 54 bind this authority and
  the rendered Compass validates;
- the fresh read-only external preflight passes; and
- no owned container, network, image, process, credential or temporary-context
  residue exists before the call.

No historical node is revised. Product-context reads and runtime proposal
delivery remain later, separate decisions.
