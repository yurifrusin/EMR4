# DeepSeek native Harness preset-mount composition-unclassified source reconciliation rehearsal plan

Date: 2026-08-22

Timestamp: 2026-08-22T08:10:09.7608886+10:00 (Australia/Brisbane)

Status: **frozen before implementation**

Reasoning level: **Extra High** for causal attribution across the generated
controller and installed rc.7 dependency boundary.

## Objective

Explain the consumed native terminal
`EFFECTIVE_TOOL_COMPOSITION_UNCLASSIFIED` at the narrowest source-owned
coordinate without starting Node, the native Harness, a worker, a model or a
provider process. Emit either one hash-bound closed attribution or an explicit
`source_evidence_insufficient` result. Do not infer or reconstruct raw runtime
error content.

## Accepted basis

The sole native process is consumed at exact candidate
`c1ae13df334dfdffefb229c3ae5a502a7251451c`. Its admitted terminal is
`preset_composition_failure_attributed` at
`EFFECTIVE_TOOL_COMPOSITION_UNCLASSIFIED`, after private identity and before a
turn or request. The zero-process recovery passes at exact reviewed source
`05a721a075a96a5e248818371d7bdfc8e136c792`, binds the exact generated runner,
guard, bridge and sanitizer hashes, and explicitly does not prove the new
preset-mount runtime path.

## Frozen source inventory

The deterministic controller must bind and compare only:

- the generated runner and effective-tool guard from the accepted native
  controller;
- the accepted preset-mount bridge and sanitizer modules;
- the installed rc.7 `dsh-agent-loop/lib/index.js`,
  `dsh-scope/lib/index.js` and `dsh-agent-presets/lib/index.js` members in the
  already accepted package-only seed;
- the accepted native contract, admitted terminal and offline recovery
  evidence; and
- the earlier exact package-source reconciliation contract/evidence.

Every source byte count and SHA-256 must be recorded. The package-only seed is
read-only and must not be copied, mounted, executed or modified.

## Causal proposition to test

The exact sources presently show this candidate control path:

1. the runner declares `agentPresets` in its own injection surface and resolves
   the admitted root service as `presets = ctx.get("agentPresets")`;
2. `agents.create` supplies setup with `prepared.agent.ctx`;
3. the installed agent loop creates that context from its own injected runtime
   context, whose declared dependency list does not include `agentPresets`;
4. installed `createScope` states that a minted scope inherits the minting
   plugin's dependency API;
5. the runner does not forward its already admitted `presets` handle into
   `assertEffectiveToolComposition`;
6. the generated guard instead evaluates
   `agentCtx.agentPresets.mount.bind(agentCtx.agentPresets)` while constructing
   the bridge arguments;
7. that dereference occurs before `mountWithSanitizedTerminal` enters its
   sanitizing `try` block; and
8. the broader sanitizer maps an error without an admitted effective-tool code
   to `EFFECTIVE_TOOL_COMPOSITION_UNCLASSIFIED`.

The controller may return
`root_preset_service_not_forwarded_before_bridge` only if every link above is
present once, in the required order, under the exact accepted hashes. Otherwise
it must return `source_evidence_insufficient` and name only closed missing
coordinates.

## Claim boundary

A passing source attribution proves a deterministic controller/interface gap
that is sufficient to explain the observed broad terminal. It does not recover
the exact runtime exception or its message, prove the observed private context
value directly, prove the preset-mount bridge ran, prove that forwarding the
root service passes natively, or authorise a retry.

The narrowest prospective correction coordinate is to pass the already
admitted root preset service explicitly into the composition guard and place
all mount-handle validation inside the preset-mount bridge. This tranche may
name that correction but may not apply it.

## Closed result vocabulary

- `root_preset_service_not_forwarded_before_bridge`
- `source_evidence_insufficient`
- `source_binding_rejected`

No descriptive substitute is accepted. Missing or changed source bytes, an
ambiguous anchor, contradiction with the admitted terminal, or any prohibited
process returns fail-closed evidence and stops without repair or retry.

## Parallelism assessment

- DeepSeek: **declined**. The Harness/worker is the governed object and no
  process or self-review is authorised.
- Gemini: **declined provisionally**. Exact hashes, anchors and control-flow
  order are deterministic. Reassess before acceptance only if the closed
  controller cannot distinguish attribution from insufficient evidence.
- Native subagents: **declined**. Current developer policy prohibits
  delegation, and the evidence chain is serial.
- GPT Sol owns the plan, deterministic controller, tests, evidence and
  acceptance.

## Explicit exclusions

No Node/native-Harness/worker/model/provider process, retry, resume, raw
message/stack/cause/path/stream/environment/credential, seed mutation, target
or product-source change, configuration/API/database/route/adapter/flag/
allowlist/grammar/client/waiting-area change, ordinary-practice enablement,
generic-status `Arrived` change, patient/product/clinical/historical/protected
data, production, deployment, release, Pages, protected evidence or protected-
ref movement is authorised.
