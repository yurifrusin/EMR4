# Reception One Bureau Model Text Lane — Occupied Closeout

## Result

`reception_one_bureau_model_text_lane_occupied_revision_required`

Exactly one authorised occupied call was made. It used `gemini-2.5-flash`,
project `bernie-emr4-dev`, the exact Bernie impersonated service account,
keyless impersonated ADC and the
`australia-southeast1-aiplatform.googleapis.com` Sydney locational endpoint.
The preflight passed the exact project, identity, scope, prediction-only role,
Vertex Data Access logging, request-response logging, provider in-memory cache,
service-account-key, entitlement and no-fallback controls.

The one-use ledger was consumed before the call. Vertex returned HTTP 400
`INVALID_ARGUMENT` and explicitly reported that the structured response schema
produced too many serving states. No candidate was returned, the deterministic
proofreader was not reached, nothing was released, no retry or fallback
occurred, and the container, relay, network, images, broker process, temporary
context and token were removed.

## What this proves

- the exact configured and observed Sydney regional request path was used;
- the exact keyless Bernie impersonated-ADC boundary remained usable;
- the credential-free cell, exact-path relay, one-use broker, bounded error,
  durable hash chain, consumed ledger and cleanup controls operated fail
  closed; and
- provider rejection cannot bypass the proofreader or release a proposal.

## What this does not prove

It proves no model interpretation, generated-plan quality, proofreader handling
of generated output, token usage, Australian physical or sovereign processing,
product-data safety, production fitness, deployment or release.

The single-call authority is consumed. A further occupied call requires fresh
exact Yuri authority even though the deterministic provider-blocked repair is
now prepared.
