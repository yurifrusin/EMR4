# Reception One Bureau Model Text Lane — Post-Failure Diagnostic Plan

## Authority

The failed occupied call authorises no retry. This follow-on is
provider-blocked and repository-local. It may read only the bounded HTTP status,
normalized provider code, sanitized message, allowlisted field paths and raw
error hash already present in the durable audit.

## Deterministic question

Does the retained provider error identify a request-contract defect that can be
repaired without changing provider, model, project, identity, authentication,
region, endpoint, data class, isolation, proofreader, cost, call ceiling or
residency assurance?

## Acceptance

Pass only if the diagnosis follows directly from retained bounded evidence, a
focused regression test fails for the former schema shape and passes for the
repair, the inner typed candidate and deterministic proofreader remain
unchanged, and provider/credential/network activity remains zero.
