# Ariadne agent-error register revision 55

Date: 2026-08-06

Status: sixth migration/transaction architecture recovery active

## AER-0054 is contained

While checking the local PostgreSQL target, Sol issued one overbroad content
search across `docs`. The only returned result was the immutable handover
archive's existing PostgreSQL 16 environment line; no protected content was
returned, no candidate file changed and no runtime, database, network or
provider was contacted. The search scope itself nevertheless violated the
exact-path protected-evidence rule.

The command is preserved in a sanitized failure receipt and contributes no
authority beyond its one returned environment fact. Every subsequent recovery
read is limited to exact paths already named by the active handover, plan, API
Steward pass or existing-model contract. Broad repository/documentation search,
wildcard discovery and recursive listing are prohibited.

## Continuing recovery posture

AER-0051 remains open until the durability migration-and-transaction
architecture passes a genuinely fresh exact-path veto. AER-0053 and AER-0054
are contained process incidents and cannot supply reviewer or architecture
acceptance. The sixth recovery remains architecture-only and opens no DDL,
migration, database, runtime, provider, product-data, deployment or protected
authority.

The sixth candidate now freezes PostgreSQL 16's exact xid8-to-low-XID32
comparison, forbids savepoints and subtransaction-authored producer members,
derives the exact positive and negative reschedule obligation from appointment
`OLD`/`NEW` time and duration values, and closes every claim, appointment,
audit, event, alias, head and outbox trigger operation. Its normative machine
contract enumerates all 18 relations with exact columns, types, keys, foreign-
key delete actions, RLS policy, role, entry-point, lifecycle, anchor, key and
retention surfaces. The receipt now carries a real composite foreign key to an
explicit `PRIMARY` admission discriminator. Sixty-five focused architecture
and register checks plus the inherited durability and API Spine modules pass;
this remains deterministic pre-veto evidence only.

Revision 55 contains 54 bounded incidents: 42 agent-behaviour observations,
three harness failures, two repository defects and seven transport timeouts.
AER-0051 remains open; AER-0053 and AER-0054 are contained. Counts remain
workflow-improvement signals and do not establish model, provider, transport
or role causation.
