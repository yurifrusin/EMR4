# Provider-free check-in native Harness preset-mount effective-tool projection rehearsal plan

Date: 2026-08-20

Timestamp: 2026-08-20T19:06:25.3877564+10:00 (Australia/Brisbane)

Status: `frozen`

Operation:
`raisa-provider-free-check-in-native-harness-preset-mount-effective-tool-projection-rehearsal`

Reasoning level: Extra High freezes the native composition, provider boundary
and one-process lifecycle. High is sufficient for bounded implementation,
deterministic verification, independent veto, checkpoint, execution and
closeout while this plan remains unchanged.

Machine-bound planning source:
`orchestration/agent_inbox/codex/raisa-check-in-preset-mount-effective-tool-projection-preplanning-receipt.json`.

## Purpose

Prove the next exact pre-provider Harness coordinate after the accepted native
preset-row service result: the healthy `emr4-bounded-worker` row mounts in one
disposable child scope and the accepted rc.7 effective-tool guard projects
exactly `edit`, `glob` and `read`.

This is a new recovery descendant, not a retry or reclassification of
`native-composition-attempt-001`. That consumed process reached stock HMR
readiness but never produced a guard terminal. The later recovery chain proved
preterminal observability, required service injection, exact preset
materialisation, post-readiness lifecycle handling and, finally, the native
service root cause: rc.7 replaces configured roots with its shipped root and
the predecessor disabled the derived user root containing the EMR4 preset.

The accepted guard itself remains the semantic floor. This tranche may compose
it with the corrected shipped-plus-derived-user-root profile; it may not weaken
the guard, broaden the tool view or cross into an agent session or model call.

## Exact accepted floor

- Accepted effective-tool guard candidate:
  `51fe669f7925a9468e036bb242d6f7bcf2613dc9`.
- Consumed failed native composition evidence descendant:
  `296f35ff696361c63a4e018e5ae9317c79cb6213`.
- Accepted canonical preset: `emr4-bounded-worker`, 158 bytes, SHA-256
  `3de182eb702e6f2b397941c73393b87f65acb9b401565f966059d2bd46f649d1`.
- Accepted native preset-row terminal: all eight row, health, byte, length and
  digest coordinates passed with one process, zero retries and exact cleanup.
- Pinned local packages remain `@deepseek-ai/dsh@0.1.0-rc.7`,
  `@deepseek-ai/dsh-agent-presets@0.1.0-rc.7`,
  `@deepseek-ai/dsh-tools@0.1.0-rc.7` and
  `@deepseek-ai/dsh-scope@0.1.0-rc.7`, bound by their retained lockfile and
  installed-source digests.
- Every prior native terminal and attempt 001 through 005 remain immutable.

## Frozen stages

### Stage 1 — deterministic source-to-composition proof

Build one provider-free controller and focused tests that read the accepted
guard, the consumed native-composition controller, the current passing
preset-row controller and the pinned rc.7 package sources. Before any Node or
native process they must prove:

1. the corrected native profile retains the forced shipped root with `system`
   trust and appends the derived disposable `$DSH_HOME/.agent-presets` root
   with `user` trust;
2. the native service exposes exactly one healthy canonical EMR4 row from the
   second root, with no shipped duplicate or arbitrary configured root;
3. `agentPresets.mount()` resolves that exact row, rejects `broken`, ensures
   standing parent services and applies only the preset's exact two package
   entries;
4. the accepted guard still performs one awaited mount, one pre-restriction
   view, one exact restriction and one final schema projection in that order;
5. the child scope begins with no scope-local tools, while the inherited
   restrictable view contains `edit`, `glob` and `read` before restriction;
6. final model-facing schemas are exactly the sorted three-name set; and
7. the native runner imports no `agents`, session, turn, broker, model or
   provider surface and can terminate only through one exclusive sanitized
   terminal plus scope disposal and `appExit`.

Any missing source predicate, digest, trust label, service injection or order
stops the tranche before executable characterization.

### Stage 2 — closed mount/projection fixture matrix

Exercise the accepted guard and exact preset semantics against disposable
authored-synthetic doubles without booting the native Harness. The matrix must
cover:

- corrected two-root success with one user-trust preset and final
  `edit`, `glob`, `read`;
- predecessor shipped-root-only absence;
- missing, duplicate, broken, wrong-path, wrong-trust or wrong-digest preset;
- mount rejection and missing scope;
- a pre-existing child-scope tool;
- one or more expected tools absent from the inherited restrictable set;
- malformed or duplicate schema names;
- surplus inherited tools, which may exist before restriction but must be
  absent from the final exact projection; and
- restriction or schema-projection failure.

Every failure must map to the accepted closed coordinate vocabulary. Durable
evidence may retain only enumerated coordinates, exact safe tool names, counts,
trust labels, byte length and digests. Raw exception text, logs, paths,
environment values, prompt or payload content are forbidden.

### Stage 3 — minimal provider-disabled native candidate

Only after Stages 1 and 2 pass may the controller generate a new native runner
and profile candidate. The candidate must:

- reuse the accepted effective-tool guard byte-for-byte unless a separately
  reviewed defect requires a narrower correction;
- reuse the accepted preterminal event writer, exclusive terminal creation,
  reliable duration, owned-process termination and disposable-root cleanup;
- set `includeUserRoot: true` and require the exact shipped-system then
  derived-user root roster;
- create only one disposable child scope, never an agent;
- call the guard once with exact preset id and exact ordered allowlist;
- write one terminal containing only stage, closed code, safe detail, exact
  tool names and count;
- dispose the child scope and request application exit on every path; and
- statically contain no `agents.create`, session, turn, broker, model, provider,
  network, Docker or database call.

No native process is authorised by candidate construction.

### Stage 4 — deterministic gate and independent veto

Run schemas, focused tests, neighbouring guard/root/profile/lifecycle tests,
Ruff, Python compilation, JSON validation, static installed-source checks and
the closed fixture matrix. Use the existing shell-free deterministic validation
runner for exact argv/results; do not ask the verifier model to retranscribe the
command ledger.

Because this candidate joins the corrected native root roster to executable
preset mount and tool projection, one fresh Gemini 3.7 Flash/high isolated
read-only semantic veto is required over the exact clean candidate. Gemini
owns technical judgment only and receives no implementation, acceptance,
runtime, cleanup, Git or product authority.

### Stage 5 — separate clockwork native-process checkpoint

Only after the exact candidate and veto pass may the exclusive clockwork
publish a checkpoint binding:

- full candidate source and review identities;
- package, guard, preset, profile and runner digests;
- exact marker and terminal schemas;
- one new attempt identity;
- one native-process allowance, zero automatic retries and no fallback;
- timeout, owned-process and disposable-root cleanup; and
- zero agent/session/turn/broker/model/provider/network/Docker/database counts.

Before this checkpoint the tranche's native-process count is zero.

### Stage 6 — one provider-disabled native mount/projection confirmation

The checkpoint may admit exactly one offline, network-denied rc.7 native
Harness process. Its ordered bounded coordinates are:

1. stock sentinel activated and both HMR watches ready;
2. effective-tool guard started;
3. preset mount returns without a closed mount failure;
4. pre-restriction inherited view is admitted with no local child tool;
5. exact restriction succeeds;
6. final effective names are exactly `edit`, `glob`, `read`;
7. terminal code is `EFFECTIVE_TOOL_COMPOSITION_PASSED`;
8. child scope is disposed; and
9. `appExit(0)` is requested and the owned process exits zero.

First process creation consumes the allowance. Success or failure writes one
immutable sanitized terminal, performs cleanup and forbids retry. A failed
terminal may be accepted only as bounded diagnostic evidence and cannot be
called effective-tool convergence.

## Acceptance and claim boundary

The tranche passes only if deterministic source and fixture evidence binds the
corrected root roster to the unchanged accepted guard, the fresh semantic veto
passes, and the separately checkpointed native process reaches the exact
success terminal with one process, zero retries and complete cleanup.

A pass proves only provider-disabled rc.7 preset mount and exact effective-tool
projection in a disposable child scope. It does not prove an agent session,
tool execution, broker request, DeepSeek response, coding quality, occupied
worker reliability, attempt 006, database or product behavior, production
isolation or deployment suitability.

## Parallelism assessment

- **DeepSeek Flash:** declined with negative leverage. No model request is in
  scope and the native Harness is the provider-free system under test.
- **Gemini 3.7 Flash/high:** declined at planning; reserved for one exact
  candidate semantic veto after deterministic acceptance.
- **Native subagents:** declined. Current developer policy prohibits proactive
  delegation, and source binding, checkpoint, one-process monitoring and
  cleanup are irreducibly serial.
- **GPT Sol:** owns plan, deterministic controller/tests, candidate, verifier
  dispatch, checkpoint, process monitoring, recovery, acceptance, clockwork
  and Git.

## Deliberately closed

No retry, resume or reclassification of a consumed native attempt; no DeepSeek
or other model request; no agent creation or agent session; no turn, tool
execution, broker/provider/network request or fallback; no attempt 006; no
Docker, PostgreSQL, SQL or database execution; no product source,
configuration, API/OpenAPI/GraphQL/schema/migration, feature flag, allowlist,
route, grammar, first-party client or waiting-area change; no ordinary-practice
enablement or generic-status `Arrived`; no product, patient, appointment,
clinical, historical or protected data; no production runtime, deployment,
release, Pages or protected-ref movement.

Local/origin `master` and `handoff/current` remain exact
`2e34bdad732fdab32fbf778280b3d3c70d66d602`. Preserve `docs/branding/` and
every unrelated untracked file. Stage explicit paths only; never use
`git add .` or `git add -A`.
