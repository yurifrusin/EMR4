# DeepSeek native Harness provider-free integrated-runner factory-subcoordinate diagnostic recovery plan

Date: 2026-08-22

Timestamp: 2026-08-22T18:51:53.2135364+10:00 (Australia/Brisbane)

Status: frozen for provider-free implementation

Operation: `deepseek-native-harness-provider-free-integrated-runner-factory-subcoordinate-diagnostic-recovery`

Reasoning level: high

## Purpose

Explain the consumed integrated-runner `factory` terminal without another
Harness or provider attempt. The recovery binds the exact occupied runner and
the exact guard bytes that were materialised beside it, then exercises their
real `agents.create` and setup call shape through the installed rc.7
`AgentRegistry` with a closed local factory fixture.

The accepted lineage already proved the broad factory/private-identity
boundary and separately derived a four-argument root-service-forwarding guard.
This tranche reuses that evidence. It creates no new factory taxonomy and does
not repeat any consumed native attempt.

## Frozen source hypothesis

The occupied preparation binds a runner that calls
`assertEffectiveToolComposition` with four arguments: private agent context,
the admitted root preset service, preset id and exact tool list. The same
preparation binds the older three-argument guard, whose second and third
parameters are preset id and tool list and whose mount path dereferences the
private agent context.

JavaScript ignores the extra fourth argument. The three-argument guard
therefore receives the preset-service object as the preset id and the preset-id
string as the tool list, yielding its finite
`EFFECTIVE_TOOL_COMPOSITION_INPUT_INVALID` coordinate before preset mounting.
This is a frozen hypothesis until the exact fixture passes.

## One closed provider-free fixture

The controller may start exactly one local Node process and no Harness process.
It copies the already accepted offline rc.7 package tree into one disposable
root, materialises the exact occupied runner and exact occupied guard bytes, and
imports the real installed `Context`, `AgentRegistry`, `SessionId` and runner.

The fixture supplies:

- the real installed `AgentRegistry.create` method;
- one closed factory whose `createAgent` invokes the exact runner-supplied
  setup callback once and republishes only a schema-admitted error code;
- inert loader, sessions and root preset-service fixtures;
- an exclusive `appExit` observer; and
- proxies that count any preset mount, model-selection, hook, registry or
  target boundary touched before rejection.

The runner must emit the same generic `factory` terminal shape as the consumed
attempt. The factory fixture may release only the structured guard code, fixed
counters and booleans; it retains no message, stack, path, environment,
session content or exception object.

## Finite result matrix

The only accepted diagnostic pass is
`occupied_guard_graph_signature_mismatch_reproduced`. It requires:

- exact runner and guard byte/hash equality with occupied preparation;
- one installed `AgentRegistry.create` transit and one setup invocation;
- exact structured coordinate
  `EFFECTIVE_TOOL_COMPOSITION_INPUT_INVALID`;
- runner terminal `status: failed`, `failure_stage: factory` and zero request,
  tool and turn observations;
- zero preset mount, model-selection, hook-installation, session publication,
  target, broker, model, provider, network, database and Docker activity;
- zero retries, resumes and fallbacks; and
- Cordis, Node process and disposable-root absence before evidence admission.

Any other structured coordinate, counter, terminal, file, schema field or
cleanup state fails closed as `fixture_result_rejected`. There is no second
fixture process in this tranche.

## Correction decision

If the pass coordinate is reproduced, freeze exactly one correction:
materialise the already accepted four-argument root-service-forwarding guard,
bridge and sanitizer graph beside the unchanged integrated runner. Reverting
the runner to the old three-argument call is forbidden because accepted native
evidence already proved that the private agent context does not forward the
root preset service.

If the fixture does not reproduce the coordinate, stop with the immutable
negative result. Do not launch a Harness process or choose a speculative
correction.

## Owned paths

- this plan and its threat-model delta;
- one operation controller under `scripts/`;
- one focused test under `tests/`;
- one operation continuity root containing contract, schemas, typed evidence
  and report;
- operation-specific Ariadne receipts and closeout artifacts.

Accepted runners, guards, package seeds, consumed evidence, product source and
protected refs remain byte-immutable.

## Parallelism efficacy assessment

- DeepSeek: declined because its native factory is the diagnostic object and
  every worker/model/provider request is forbidden.
- Gemini: reserved only if a material correction candidate survives the exact
  fixture and deterministic gates; no provider review is useful for source
  facts alone.
- Native subagents: declined under current developer policy and because the
  exact runner/guard/factory chain has one serial owner.
- GPT Sol owns lineage reconciliation, the single fixture, verification,
  correction selection, acceptance and closeout.
- Parallel work packages: none.

## Recovery

A pre-process binding or schema failure consumes no fixture process and may be
corrected mechanically. Once Node starts, the fixture identity is consumed.
Any non-pass terminal is retained without retry, resume, fallback or Harness
launch. Raw streams are reduced to byte counts and SHA-256 digests before
semantic admission and then deleted with the exact disposable root.

## Explicitly closed

No Harness model turn, broker, DeepSeek worker, model/provider request, accepted
runner mutation, product/configuration/API/database/route/adapter/flag/
allowlist/grammar/client/waiting-area change, ordinary-practice enablement,
generic-status `Arrived` change, patient/product/appointment/clinical/
historical/protected data, production runtime, deployment, release, Pages,
protected evidence or protected-ref movement. Local/origin `master` and
`handoff/current` remain exactly
`2e34bdad732fdab32fbf778280b3d3c70d66d602`. Preserve `docs/branding/` and
all unrelated untracked files. Stage explicit paths only; never use
`git add .` or `git add -A`.
