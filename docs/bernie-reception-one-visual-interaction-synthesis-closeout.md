# Reception One visual and interaction synthesis closeout

## Result

`reception_one_visual_interaction_synthesis_pass_with_vertex_candidate_rejected`

The bounded product tranche passes. Reception One now presents the accepted
intent-projected Diary as a calm, tablet-first scheduling workspace with an
integrated request rail, structured scope tokens, an exact time projection,
quiet freshness/state language, an embedded proposal-only selection panel and
explicit keyboard/ordinary-Diary escape.

The UI remains deterministic and unoccupied. No model is connected to product
reads, events, rendering, selection, proposals, commands or appointment
writes.

## Live Vertex result

The provider-blocked contract, exact read-only Bernie ADC/control preflight and
third distinct provider-free isolated lifecycle passed before the occupied
call. The single primary authored-synthetic request then used:

- provider: Google Vertex AI;
- model: `gemini-2.5-flash`;
- project: `bernie-emr4-dev`;
- impersonated service account:
  `emr4-bernie-ai-dev@bernie-emr4-dev.iam.gserviceaccount.com`;
- authentication: `keyless_impersonated_service_account_adc`;
- location: `australia-southeast1`; and
- hostname: `australia-southeast1-aiplatform.googleapis.com`.

Vertex returned HTTP 200 in 2,986 ms and supplied 693 prompt tokens, 493
candidate tokens and 1,186 total tokens. The deterministic proofreader
edge-aborted the draft for `priority_order_invalid`. It released no field.
Mechanical evidence/risk ordering repairs were permitted, but the semantic
priority defect was not.

The occupied ledger is consumed after one call. No retry was made because the
conditional second call admitted only a deterministic request-construction
defect. No provider/model/region/identity fallback occurred. The eight-event
external audit chain verifies and the independent post-call residue check
found zero task containers, networks, images and processes.

The rejected draft was not retained, reconstructed, used for implementation or
delivered to the product.

## Visual concept and implementation

A fresh authored-synthetic complete visual concept was generated and inspected
before implementation:

`orchestration/prototypes/reception-one-visual-synthesis/reception-one-desktop-concept-1440x900.png`

The implementation retained its strongest ideas:

- one calm navy/eucalyptus Diary shell;
- a scoped request rail rather than a separate chatbot;
- schedule-first information hierarchy;
- a quiet selected/proposal-only edge panel;
- restrained borders, density and state colour; and
- explicit keyboard and ordinary-Diary affordances.

It deliberately did not copy the concept's fictional multi-practitioner busy
blocks. The accepted product request was scoped to one practitioner and the
existing slot-search response supplies candidate availability, not a complete
busy-grid truth. The implementation therefore renders only the eight actual
authored-synthetic candidate slots and does not invent occupancy.

Changed product files are limited to:

- `docs/diary/diary.html`;
- `docs/diary/meta-grid.css`; and
- `docs/diary/meta-grid.js`.

The implementation adds no API route, GraphQL field, OpenAPI artifact,
Pydantic schema, database model or migration. It adds no fetch path, provider
runtime, storage, service worker, voice input, confirmation command or model
authority.

Two ordinary local display defects were repaired during rendered QA:

- candidate times containing seconds (`HH:MM:SS`) now parse for visual
  positioning; and
- repeated keyboard selection replaces, rather than accumulates, the current
  `selected` scope suffix.

## Browser evidence

The repository-local exact-raster acceptance runner used the existing local
authenticated authored-synthetic fixture without route or API interception.
It passed:

- desktop landscape, 1440×900;
- tablet landscape, 1024×768;
- tablet portrait, 768×1024; and
- phone portrait, 390×844.

All four views had zero page or Reception One horizontal overflow, no enabled
control below 44×44 CSS pixels, eight distinct candidate positions, one
selected slot, and a visible proposal-only panel. Arrow keys moved to the next
candidate, Enter selected it, the scope retained exactly one selection suffix,
and Escape returned to the ordinary Diary.

All four raster dimensions and SHA-256 hashes verify against
`orchestration/prototypes/reception-one-visual-synthesis/browser-acceptance-evidence.json`.
The rendered desktop and phone captures were inspected against the concept.

The local server's default-off committed-event feed returned one known,
read-only `GET /api/v1/diary/events/committed` 404 per fresh browser context.
The runner admits only that exact local 404. Unexpected HTTP responses,
unexpected console warnings/errors, page errors, external hosts, confirmation
routes and session-command routes were zero.

## Verification

Passed:

- exact-raster local browser runner: four viewports and four screenshots;
- repository-only Ariadne fixed verifier: 266 tests;
- integrated focused product, API Spine, Vertex contract/rehearsal/audit and
  Compass suite: 142 tests;
- JavaScript syntax;
- Python compilation and static checks;
- JSON/schema validation;
- Compass validation and exact rendered report; and
- `git diff --check`.

Continuity graph revision 54 and Compass map revision 41 bind the accepted
compound result and the exact rendered Compass report.

No commit, push, pull request, protected-ref move or external worker dispatch
was performed.

## What the evidence proves

The evidence proves a bounded authored-synthetic visual synthesis over the
existing deterministic Reception One client, exact local responsive behavior,
the preserved proposal/confirmation boundary, and one fail-closed Sydney
Vertex design-cell attempt through the configured keyless impersonation path.

It proves configuration and observation of the
`australia-southeast1` locational endpoint. It does not prove Australian
physical or sovereign processing.

It does not prove that Vertex supplied an admissible design candidate,
representative-staff usability, patient/clinical-data safety, production
readiness, exact provider billing, deployment, release, public brand adoption
or authority for an agent to inhabit the UI.

## Closed boundaries and next work

Stage 3B, representative users, voice, real/product/patient/health/clinical
data, other event families, new appointment write authority, external workers,
production, deployment, release, public rename, artwork adoption and trademark
claims remain closed.

The unused numerical second-call allowance is not available: its only
admissible defect class did not occur. Any further Vertex design call or
provider/model/region change requires fresh Yuri authority.
