# DeepSeek native Harness provider-free preterminal activation observability recovery plan

Date: 2026-08-20

Timestamp: 2026-08-20T06:01:41.6545833+10:00 (Australia/Brisbane)

Status: `frozen`

Planning source HEAD:
`13d73085eadaa14531c1fb08101332ca575aa3a0`

Immutable failed attempt source:
`296f35ff696361c63a4e018e5ae9317c79cb6213`

Operation:
`deepseek-native-harness-provider-free-preterminal-activation-observability-recovery`

Reasoning level: Extra High freezes the immutable-attempt, no-native-process
and causal-coordinate boundaries. High is sufficient for deterministic
implementation, isolated review and clockwork closeout.

## Objective and authority

Preserve `native-composition-attempt-001` and every retained byte unchanged.
Deterministically identify all post-readiness paths that can avoid the accepted
guard terminal; replace the generic controller failure and unreliable zero
duration in the future runner design with safe exact preterminal coordinates,
reliable timing and separate single-writer readiness/activation evidence.

This tranche may inspect pinned offline rc.7 package bytes, materialise them in
one disposable root and run bounded Node module-import/mock-lifecycle probes.
It may not start the native Harness CLI, an agent/session/turn, a broker, model,
provider or occupied worker.

## Exact owned surface

The tranche owns only:

- this plan and its threat-model delta;
- `scripts/deepseek_native_harness_provider_free_preterminal_observability_recovery.py`;
- `tests/test_deepseek_native_harness_provider_free_preterminal_observability_recovery.py`;
- closed contract, schema, evidence and report under
  `orchestration/continuity/deepseek-native-harness-provider-free-preterminal-activation-observability-recovery/`;
- normal receipts, review, closeout, acceptance and Yuri-summary artifacts.

It may read but not modify the consumed proof evidence, accepted guard/HMR
sources or EMR4 product source.

## Frozen diagnosis

The retained attempt proves only:

1. exact offline rc.7 materialisation passed;
2. one native process started;
3. `sentinel_activated` and `stock_headless_hmr_ready` were retained;
4. the changed patch was written after readiness;
5. no guard-start or terminal record was retained;
6. the controller collapsed its caught exception to
   `NATIVE_COMPOSITION_EXECUTION_FAILED`;
7. launch duration remained its initial zero because assignment occurred only
   after the polling/terminal path; and
8. all prohibited-call counts were zero and cleanup completed.

No retained evidence distinguishes module resolution, plugin activation,
service injection, event-ledger parsing, scope creation or guard entry. The
recovery must label this causal state `indeterminate_preterminal_failure`; it
must not infer a Harness, DeepSeek, guard or controller root cause.

## Corrected future-runner construction

The deterministic recovery must construct and validate, without mounting it:

1. a bootstrap plugin with only Node built-ins and accepted HMR injection;
2. a separate readiness ledger written only by the sentinel;
3. a separate activation snapshot written only by the bootstrap/runner;
4. closed activation coordinates for bootstrap entry, HMR/app-exit checks,
   service checks, dynamic runtime-module import, scope creation, guard entry,
   guard terminal, scope disposal and exit request;
5. dynamic imports of `@deepseek-ai/dsh-scope` and the exact accepted guard
   only after bootstrap entry is durably recorded;
6. the accepted guard success/failure vocabulary unchanged and distinguishable
   from controller activation coordinates;
7. one final sanitized terminal and one independent activation snapshot;
8. elapsed time assigned in a `finally` path for every started process; and
9. caught controller failures mapped to a finite coordinate set without raw
   messages, stacks, paths, environment values, prompts or credentials.

The design must make a future missing bootstrap coordinate mean only “the
changed plugin did not enter apply”; later coordinates must partition all
admitted branches without pretending to identify Cordis internals that were
not observed.

## Deterministic proof

The recovery must:

- bind the exact failed evidence and its source commit;
- statically inventory every throwable controller/runner region before the
  former duration assignment and generic collapse;
- execute hostile mock scenarios for missing HMR, missing app exit, missing
  services, dynamic-import rejection, scope creation rejection, every accepted
  guard failure, terminal-write rejection, disposal rejection and success;
- require one safe coordinate per scenario and no raw exception retention;
- verify the corrected event/snapshot writers are single-owner and reject
  duplicate/reordered/unsafe records;
- offline-materialise the exact cached rc.7 package once, use the accepted
  network guard, run one non-Harness Node import probe for the real scope and
  accepted guard modules, and prove zero network attempts plus full cleanup;
- record Node probe count separately from native Harness process count, which
  must remain zero; and
- pass focused and neighbouring guard/HMR/profile/broker/no-database/latch/
  clockwork/baton checks, Ruff, compile and schema validation.

The materialisation/import probe may be corrected before publication because
it consumes no native attempt. It may not execute `lib/bin.js`, `--profile
headless` or any equivalent native Harness entry.

## Parallelism assessment

- **DeepSeek native Harness/model lane:** `declined`, negative leverage. The
  failed native attempt is the evidence subject and the active boundary admits
  neither another Harness process nor a model/provider call.
- **Gemini:** `reserved`, required independent leverage. One fresh Gemini 3.7
  Flash/high isolated read-only veto may inspect the exact deterministic
  candidate only after all local evidence passes.
- **Native subagents:** `declined`, negative leverage. Current developer policy
  prohibits proactive delegation; exact evidence interpretation, offline probe
  cleanup and candidate freezing are one serial ownership chain.

Sol owns implementation, deterministic probe, cleanup, recovery, acceptance,
clockwork and Git. Reassess all lanes before the offline probe, verifier and
closeout.

## Acceptance and successor gate

Acceptance requires zero native Harness/agent/broker/model/provider processes,
an exact closed coordinate for every deterministic scenario, one successful
real-package non-Harness import probe, zero network attempts, complete cleanup
and one clean Gemini veto.

Only then may closeout select a separately named one-process recovery attempt.
That later operation must receive its own plan, attempt id and preexecution
receipt. It cannot retry, resume or reclassify
`native-composition-attempt-001`.

No Docker/database, product source/configuration/API/OpenAPI/GraphQL/schema/
client/waiting-area change, ordinary-practice enablement, feature flag,
allowlist, command mount, generic-status `Arrived`, action grammar, first-party
client, product/patient/clinical data, production, deployment, release, Pages,
protected evidence or protected-ref movement is authorised.

Local/origin `master` and `handoff/current` remain exactly
`2e34bdad732fdab32fbf778280b3d3c70d66d602`. Preserve `docs/branding/` and
every unrelated untracked file. Stage explicit paths only; `git add .` and
`git add -A` are forbidden.
