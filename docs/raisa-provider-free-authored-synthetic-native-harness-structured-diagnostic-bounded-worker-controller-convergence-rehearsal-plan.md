# Raisa provider-free authored-synthetic native Harness structured diagnostic bounded-worker controller convergence rehearsal plan

Date: 2026-08-21

Timestamp: 2026-08-21T10:37:51.7615490+10:00 (Australia/Brisbane)

Status: `frozen`

Operation:
`raisa-provider-free-authored-synthetic-native-harness-structured-diagnostic-bounded-worker-controller-convergence-rehearsal`

Planning source HEAD:
`516f6bf6a7f6f68fc9a7fa38a5643d6f1bf65d98`

Accepted structured diagnostic native-boot source:
`6f728b2979dffcd089bd79eb73f49d7e447d772f`

Reasoning level: High. The integration changes one serial lifecycle boundary in
the accepted bounded worker controller. Provider-free fixtures and exact source
invariants are sufficient; no Harness, broker, worker, model or provider process
is permitted.

## Objective and exact claim

Compose the accepted canonical structured-diagnostic wrapper, launch-command
builder, sidecar reader and v2 terminal builder into the existing bounded
authored-synthetic worker controller. Prove deterministically that a future,
separately authorised attempt will:

1. write one canonical wrapper and one potential sidecar only beneath its exact
   disposable root;
2. launch its one native process through that wrapper rather than directly
   through `lib/bin.js`;
3. always derive the accepted v1 terminal first for a pre-HMR failure;
4. select v2 only when an exact-identity canonical structured sidecar validates;
5. retain v1 and fail closed when that sidecar is absent or invalid; and
6. persist the safe selected terminal outside the disposable root before exact
   root removal.

This tranche changes controller source and deterministic evidence only. It does
not prepare, consume, launch, retry, resume or reclassify an occupied attempt.

## Narrow controller seam

The controller imports the already accepted
`orchestration_harness.native_pre_hmr_diagnostic` component without changing
that component or the accepted v1 terminal component. Its future execution path
uses fixed leaf names `entrypoint-wrapper.mjs` and
`pre-hmr-structured-diagnostic.json` beneath the verified attempt root.

After the copied rc.7 package root and profile validate, the controller:

- proves the pinned `lib/bin.js` entrypoint exists;
- generates the wrapper with canonical JSON enabled and exact operation,
  attempt and full 40-character candidate Git object identity;
- writes and reads back the wrapper exclusively;
- constructs argv through the accepted launch-command builder with the exact
  `headless` profile and existing authored-synthetic task; and
- invokes the existing single `Popen` site with that argv.

The wrapper performs exactly one identical-value rethrow and cannot start an
extra process, network request, retry or fallback.

## Closed terminal selection

The controller's pure selection function accepts only the fixed runtime facts,
bounded stream readings and exact sidecar path. It first builds v1. It then
returns one of exactly three closed outcomes:

| Sidecar state | Selected terminal | Structured accepted | Controller result |
|---|---|---:|---|
| canonical and exact identity | v2 `structured_entrypoint_import_rejected` | true | eligible for ordinary future acceptance checks |
| absent | unchanged v1 | false | fail closed at `structured_diagnostic_absent` |
| present but invalid, noncanonical, escaped or identity-mismatched | unchanged v1 | false | fail closed at `structured_diagnostic_invalid` |

V2 is inapplicable to process-creation failure, timeout, a zero exit or any path
with an HMR event. Those paths retain v1 and the controller's existing failure
coordinate. No dynamic exception text, raw message, stack, path, environment or
credential may enter the terminal or fallback reason.

The terminal writer validates either accepted schema, exclusively writes beneath
the exact continuity evidence root and outside the disposable root, fsyncs,
reads back and byte-compares before cleanup. Structured selection failure is
never acceptance merely because v1 was safely preserved.

## Provider-free proof

Deterministic fixtures exercise valid, absent, malformed, noncanonical,
wrong-identity and escaped-sidecar cases without monkeypatching or launching a
process. Source inspection proves wrapper build precedes the one `Popen`,
sidecar read precedes terminal persistence, and terminal persistence precedes
exact root removal. It also proves zero retry loops and preservation of the
accepted HMR/profile mutation behavior.

The proof boundary is exactly one already-running Python test/controller
process and zero Node, Harness, broker, worker, session, prompt, tool, model,
provider, network, package-manager, Docker or database activity.

## Immutable evidence

The consumed markers and terminal evidence for attempts 001, 002 and 003 remain
byte-identical to the SHA-256 values frozen in the contract. No historical raw
stream is read, reconstructed or retained. The accepted structured diagnostic
module and legacy terminal module also remain byte-identical.

## Acceptance

Acceptance requires the canonical contract, schema, evidence and report; focused
selection, plan and predecessor-compatibility tests; Ruff; Python compilation;
JSON/schema validation; deterministic re-check; `git diff --check`; exact
protected refs; a clean tracked tree at closeout; and preservation of every
unrelated untracked file.

## Parallelism assessment

- DeepSeek Flash: `not_applicable`, neutral leverage. Any worker/model request
  violates this provider-free controller tranche.
- Gemini 3.7 Flash/high: `not_applicable`, neutral leverage. Exact local source,
  fixtures and lifecycle invariants own acceptance; provider calls are closed.
- Native subagents: `declined`, negative leverage. Developer policy prohibits
  proactive delegation and the integration is one tightly ordered controller
  seam.

GPT Sol owns planning, implementation, deterministic verification, closeout,
clockwork and Git. No work package is dispatched.

## Explicit non-authority

No native Harness, broker, worker, session, prompt, tool, model or provider
process/request; no occupied retry, resume, overwrite or reclassification; no
product source/configuration, API, database, route, adapter, flag, allowlist,
grammar, client or waiting-area change; no ordinary-practice enablement or
generic-status `Arrived`; no product, patient, appointment, clinical, historical
or protected data; no production runtime, deployment, release, Pages, protected
evidence or protected-ref movement.

Local/origin `master` and `handoff/current` remain exactly
`2e34bdad732fdab32fbf778280b3d3c70d66d602`. Preserve `docs/branding/` and
every unrelated untracked file. Stage explicit paths only; `git add .` and
`git add -A` are forbidden.

