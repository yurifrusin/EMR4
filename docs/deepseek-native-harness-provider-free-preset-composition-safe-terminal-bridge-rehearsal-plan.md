# DeepSeek native Harness preset-composition safe-terminal bridge rehearsal plan

Date: 2026-08-22

Timestamp: 2026-08-22T03:42:19.3682524+10:00 (Australia/Brisbane)

Status: **frozen before implementation or execution**

Reasoning level: **Extra High** for one new native-process boundary derived
from an accepted typed failure interval.

## Objective

Derive one runner descendant that imports the accepted effective-tool guard's
existing `sanitizeEffectiveToolTerminal` function inside the guarded dynamic-
import transaction, maps only its closed safe codes into the diagnostic
sidecar, and runs at most one distinct provider-free rc.7 process.

The process may either attribute the preset-composition failure to one accepted
safe guard coordinate or proceed through the already-frozen prepublication
veto path. It is not a retry of either consumed predecessor attempt.

## Identities

- Operation:
  `deepseek-native-harness-provider-free-preset-composition-safe-terminal-bridge-rehearsal`
- Attempt: `preset-composition-safe-terminal-attempt-001`
- Private identity: `session-emr4-preset-composition-terminal-001`
- Preset: `emr4-bounded-worker`
- Ordered tools: `edit`, `glob`, `read`
- Route remains configured but unexercised: `deepseek-official` /
  `deepseek-v4-flash` / `high` / `4096`
- Target remains absent and unused:
  `workspace/authored_synthetic_control_probe.py`

## Safe terminal bridge

The runner may retain only one of the accepted guard's closed coordinates:

- `EFFECTIVE_TOOL_COMPOSITION_INPUT_INVALID`
- `EFFECTIVE_TOOL_COMPOSITION_PRESET_MOUNT_FAILED`
- `EFFECTIVE_TOOL_COMPOSITION_SCOPE_MISSING`
- `EFFECTIVE_TOOL_COMPOSITION_SCOPE_LOCAL_TOOL_PRESENT`
- `EFFECTIVE_TOOL_COMPOSITION_EXPECTED_TOOL_NOT_INHERITED`
- `EFFECTIVE_TOOL_COMPOSITION_RESTRICTION_FAILED`
- `EFFECTIVE_TOOL_COMPOSITION_SCHEMA_VIEW_INVALID`
- `EFFECTIVE_TOOL_COMPOSITION_EFFECTIVE_VIEW_MISMATCH`
- `EFFECTIVE_TOOL_COMPOSITION_UNCLASSIFIED`

The optional sanitized detail remains limited to the guard's sorted safe tool
names. No raw message, stack, code path, cause, stream, prompt, response, path,
session content or credential may be written.

The runner terminal is exactly one of:

- `preset_composition_failure_attributed` with one safe guard coordinate;
- `prepublication_veto_diagnosed` if composition, model selection, commit and
  the exact publication veto all pass; or
- `closed_subcoordinate_failure` for a failure outside the guard bridge.

The controller alone may emit `runner_link_or_apply_absence` when no sidecar
exists. It must retain `factory_boundary: null` in that case.

## Materialisation and execution

Reuse the exact accepted package-only seed, independent copy, HMR readiness,
preset bytes, helper, guard, broker-zero reading, target absence and cleanup
controls. Commit and push the implementation, schemas and deterministic tests,
then require a fresh five-source preexecution receipt before exactly one new
native process. There is no retry, resume, alternate runner or transport.

## Acceptance

Acceptance requires one exact finite terminal, a safe guard coordinate when the
failure lies inside composition, at most one factory invocation, zero published
agents/sessions/lifecycle announcements, zero turns/requests/broker/model/
provider/network/database/Docker/target activity, immutable seed/bundle and
complete cleanup.

An attributed guard failure is useful diagnostic evidence, not worker or agent-
creation acceptance. A prepublication-veto terminal proves only the private
factory boundary; occupied worker development remains a later explicit gate.

## Parallelism assessment

- DeepSeek lane: **declined**. The native factory and preset composition remain
  the objects under test.
- Gemini lane: **declined**. The safe vocabulary and exact package bytes are
  deterministic; reconsider only if the new typed terminal is semantically
  ambiguous.
- Native-subagent lane: **declined**. Developer policy prohibits delegation and
  the one-process transaction is serial.
- GPT Sol owns implementation, the one native process, readback and closeout.

## Explicit exclusions

No occupied worker turn, model/provider request, target change, product/
configuration/API/database/route/adapter/feature-flag/allowlist/action-grammar/
first-party-client/waiting-area change, ordinary-practice enablement, generic-
status `Arrived` change, patient/product/clinical/historical/protected data,
production runtime, deployment, release, Pages rebuild, protected-evidence
access or protected-ref movement is authorised.
