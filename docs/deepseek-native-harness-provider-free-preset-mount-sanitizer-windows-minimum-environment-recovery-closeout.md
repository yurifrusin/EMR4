# DeepSeek native Harness preset-mount sanitizer Windows minimum-environment recovery closeout

Date: 2026-08-22

Timestamp: 2026-08-22T06:03:42.6115665+10:00 (Australia/Brisbane)

Status: **accepted deterministic pass**

Reasoning level: **Extra High**

## Result

At exact execution candidate `ceac8b2600530bf858394bb84e66a42ec3d016f4`,
the separately frozen controller ran exactly one local Node fixture process with
only `SystemRoot`, `WINDIR`, `ComSpec`, `TEMP` and `TMP`. The unchanged
sanitizer and wrapper emitted the exact fifteen-result closed vector with exit
0 and zero stderr bytes.

The content-free process envelope was written before semantic admission. It
retains the numeric exit, byte counts and SHA-256 digests but no stream content
or environment values. The successor used one Node process; the complete
lineage has used four. No further process is authorised by this plan.

## Accepted control gain

- Sanitizer SHA-256:
  `12552925a600dc951afc30b9a738746499c7e2f4cefc9962bc05fb06780f158f`.
- Wrapper SHA-256:
  `601bb41f21916fc836603e54cb5caecec6987e75347609ff1881eef758940788`.
- Environment key count: five; unlisted keys: zero; values retained: false.
- Result count: fifteen; exact stdout: true; forbidden detail absent: true.
- Native Harness, worker, model, provider, broker, network, database, Docker,
  target and product counts: zero.

This admits the pure sanitizer under the exact five-key local fixture boundary.
It does not prove that the earlier empty environment was the sole JavaScript
abort cause, because the accepted evidence deliberately discarded raw runtime
content. It does prove that the minimum environment removes the observed abort
condition for the unchanged fixture.

## Verification

Deterministic `--check`, 33 directly relevant successor and predecessor tests,
Ruff, `py_compile`, schema validation and Git whitespace checks pass after the
single execution. The check path read the persisted artifacts and spawned no
second process.

Four low-severity workflow mistakes were contained without a Node rerun:
one guessed commit identity was removed before receipt generation; one
read-only hash query guessed a JavaScript path before resolving it from the
repository; and one read-only summary query guessed evidence field names before
schema-led inspection. The first clockwork projection also omitted one exact
inherited boundary token and was rolled back byte-exactly after the full baton
consistency suite rejected it. They are recorded as AER-0891 through AER-0894
so the clockwork can measure recurrence rather than hide it.

## Parallelism disposition

- DeepSeek: declined because its native Harness remained the governed object;
  no worker or model process was authorised.
- Gemini: declined because environment projection, hashes and the finite result
  vector were deterministic and produced no semantic ambiguity.
- Native subagents: declined because developer policy prohibits delegation and
  the process budget was serial.
- GPT Sol owned projection, execution, typed readback and acceptance.

## Next tranche

Proceed with a provider-free preset-mount sanitizer runner-bridge rehearsal.
It may derive one runner source that calls the admitted sanitizer at the exact
preset-mount failure boundary and proves the closed terminal grammar without
starting DSH or a native Harness process. A later native execution remains a
separately frozen gate.

## Boundaries

No runner execution, native Harness, worker/model/provider request, repair or
retry, product/data/configuration/API/database/route/adapter/flag/allowlist/
grammar/client/waiting-area change, ordinary-practice enablement, generic-status
`Arrived` change, production runtime, deployment, release, Pages, protected
evidence or protected-ref movement is accepted.
