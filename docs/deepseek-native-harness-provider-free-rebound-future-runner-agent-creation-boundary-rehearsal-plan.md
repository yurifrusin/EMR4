# DeepSeek native Harness provider-free rebound future-runner agent-creation boundary rehearsal plan

Date: 2026-08-22

Timestamp: 2026-08-22T01:46:01.9815058+10:00 (Australia/Brisbane)

Status: **frozen before implementation or execution**

Reasoning level: **Extra High** for the native factory-publication boundary and
the correction of the predecessor successor wording.

## Objective

Run at most one provider-free `@deepseek-ai/dsh@0.1.0-rc.7` native Harness
process and prove that the exact rebound runner descendant can:

1. traverse the already accepted stock-headless HMR boundary;
2. admit the exact `emr4-bounded-worker` preset from the exact two-root roster;
3. invoke `ctx.agents.create()` exactly once with the frozen DeepSeek route;
4. mount the preset and project exactly `edit`, `glob`, `read` inside the
   factory's unpublished agent setup;
5. reach the synchronous setup-commit point immediately before publication;
6. deliberately veto publication with one exact controller-owned stop code;
7. verify that the private prepared agent/session pair was rolled back and that
   no live agent, live session or lifecycle announcement escaped; and
8. emit one bounded typed sidecar and exit before a turn, broker/model/provider
   request or target creation/use.

This is the narrowest real rc.7 state that preserves the active latch's
zero-session and pre-request boundary while advancing beyond runner activation.

## Native lifecycle semantic correction

The active latch described a successfully created inert agent followed by a
stop before session creation. The pinned package proves that state cannot
exist: `AgentRegistry.create()` delegates to `AgentLoop.createAgent()`, which
first prepares one private Session and one private Agent, awaits setup and its
optional synchronous commit, and only then publishes both registries and emits
`session/created`, `agent/created` and `agent/session-start`.

Accordingly this tranche uses the package-supported transaction boundary:

- exactly one `agents.create()` invocation is admitted;
- exactly one private Agent/Session pair may be prepared inside the factory;
- the setup commit deliberately throws the exact typed publication veto;
- `agents.create()` must reject with that exact veto;
- live-agent count, live-session count, all three lifecycle-event counts and
  every request-bearing count must remain zero after rollback.

The accepted claim is **one real factory construction reaching the
prepublication commit**, not a published agent and not a live session. A later
separately frozen tranche may allow atomic publication of the inseparable
agent/session pair; this tranche cannot honestly call the prepared Session
object absent.

## Frozen identities and sources

- Operation:
  `deepseek-native-harness-provider-free-rebound-future-runner-agent-creation-boundary-rehearsal`
- Native attempt: `rebound-agent-creation-boundary-attempt-001`
- Fixed private identity: `session-emr4-agent-creation-boundary-001`
- Exact publication veto: `EMR4_AGENT_PUBLICATION_STOP`
- Exact preset: `emr4-bounded-worker`
- Exact ordered effective tools: `edit`, `glob`, `read`
- Provider/model/reasoning: `deepseek-official` / `deepseek-v4-flash` / `high`
- Per-request output cap configured but never exercised: `4096`
- Accepted rebound stock-headless HMR closeout source:
  `f7eff76e61f4b139741aadbe61df4c3c6a7f8d68`
- Accepted native execution source:
  `560f471b72bef0b9790120238657dc7afd4d602b`
- Accepted native preset-mount/effective-tool source:
  `c8b0e3a587191b65da212edda36b8b833a2ecc2c`
- Accepted rebound runner parent SHA-256:
  `d025d1929f20589e86cbd66e983bda17435c1a21e937468532c38bb4895efb1a`
- Accepted helper SHA-256:
  `7ff2630b1523c66bb9e0cb5d649879cfa97d0340b791a85255c7dc5ad1fb66fd`
- Accepted effective-tool guard SHA-256:
  `6678ed31bdcd30a5018689b72ad509c182854bf5d63862f59b397acc8de40894`
- Exact preset bytes/SHA-256: `158` /
  `3de182eb702e6f2b397941c73393b87f65acb9b401565f966059d2bd46f649d1`
- Inert target remains `workspace/authored_synthetic_control_probe.py`; it must
  remain absent and unused.

The implementation must resolve every Git source to a full 40-character object
and prove the accepted sources are ancestors of the execution candidate.

## Pinned package semantics

The plan binds the verified package-only seed and these exact rc.7 members:

| Package member | Bytes | SHA-256 | Required observation |
|---|---:|---|---|
| `@deepseek-ai/dsh-agent/lib/index.js` | 31,560 | `e7e40c5ca66d9827a5084c5c0c68983f9685842bb9b6d604803d4cb4642bb263` | `agents.create()` delegates to the registered factory. |
| `@deepseek-ai/dsh-agent/lib/types/index.d.ts` | 20,667 | `577f7ee31d8954c8a09c4af9ce9c1fbdb25220caff40b2120bec257305bd2a35` | Setup is unpublished and optional commit runs immediately before publication. |
| `@deepseek-ai/dsh-agent-loop/lib/index.js` | 47,416 | `bf8ca1e9b05e9b78320a5e2f0b4e25395eba91dd72db6d3cb5626e3dfb529204` | `SessionPreparation.create` and private Agent construction precede setup; commit precedes `publish`; rejection disposes. |
| `@deepseek-ai/dsh-agent-presets/lib/index.js` | 52,188 | `a0b417514e3d285ad5fef74867e8049af333ebdec6e4d7639e388aa0903e0039` | `mount()` resolves and joins the exact standing preset inside setup. |
| `@deepseek-ai/dsh-session/lib/index.js` | 79,221 | `9270186b579bc8a4c6c53c256e4471d3f134e94308462c6a413a722e9c7556fb` | `prepare` is private until `enter`/`announce`; `get` and `list` expose only live sessions. |

All named packages must report version `0.1.0-rc.7`. Any byte or semantic
mismatch rejects before a native process.

## Materialisation and composition

Reuse the accepted process-free package-only seed exactly:

- 588 lockfile packages;
- 32,744 files;
- 219,364,530 bytes;
- zero reparse points;
- tree SHA-256
  `d84e73067c8dbbf4836969eb948012fd364ee454bb07744cfe486995a256084d`.

One fresh disposable root contains installation, home, profile, runner/helper
copies and bounded typed ledgers. The exact 158-byte preset is copied only to
`home/.agent-presets/emr4-bounded-worker/agent.cordis.yml`. The effective roots
must be exactly the installed shipped root with `system` trust followed by this
derived user root with `user` trust. No real Harness home, preset or repository
configuration is read or changed.

## Runner transaction

After exact stock readiness and one HMR mutation, the descendant runner must:

1. await loader readiness and resolve `agents`, `sessions`, `agentPresets` and
   the effective-tool guard;
2. require the two exact roots, exact healthy preset row and initially empty
   live registries;
3. attach bounded counters for `session/created`, `agent/created` and
   `agent/session-start` before the factory call;
4. increment its own `agents.create` invocation counter once and call the real
   factory with the fixed SessionId, absolute disposable cwd and frozen
   provider/model/max-token options;
5. inside setup, require `agentCtx.agent` and its private session to share the
   fixed id, require both live registries still not to contain that id, mount
   and restrict the exact preset through the accepted guard, install the frozen
   DeepSeek model selection, and return one commit object;
6. inside `commit()`, recheck the unpublished identity, preset and exact tool
   projection, set only bounded in-memory flags, then throw
   `EMR4_AGENT_PUBLICATION_STOP`;
7. accept only that exact rejection, then require the fixed id absent from both
   registries, both complete live lists empty, and all lifecycle counters zero;
8. write exactly one typed sidecar with digests and finite booleans/counts, ask
   `appExit(0)` once and retain no raw error or stream.

If `agents.create()` resolves, any publication event occurs, either registry is
non-empty, the factory is absent, the preset/tool guard differs, the veto is not
exact or the sidecar already exists, the run fails closed. It must not dispose a
returned handle because a returned handle is itself a forbidden publication.

## Canonical terminal and independent gauges

The controller may accept only sidecar coordinate
`agent_create_prepublication_veto_passed` and must join it with:

- exact readiness and one HMR mutation;
- one native process and one `agents.create` invocation;
- one private agent preparation and one private session preparation;
- zero live/published agents and sessions;
- zero `session/created`, `agent/created` and `agent/session-start` events;
- zero turns, broker processes/requests, workers, model/provider requests,
  network attempts, target creations/uses, database and Docker invocations;
- exact preset/tool/source bindings;
- process absence, disposable-root absence and target absence.

The sole controller coordinate is
`post_hmr_agent_create_prepublication_stop`. It must state
`factory_construction_supported: true`, `publication_zero: true`,
`broker_zero: true`, `pre_request_supported: true` and
`occupied_launch_authorized: false`.

## Execution and recovery

- Implementation, schemas, deterministic checks and focused tests are committed
  and pushed before execution.
- Exactly one native attempt may be admitted after a fresh pre-execution
  receipt and exact clean-source readback.
- There is no automatic/manual retry, resume, alternate runner, alternate
  transport, npm materialiser, online install or fallback terminal.
- A prelaunch rejection consumes no native attempt but must leave no owned
  process/root. A post-launch failure consumes attempt 001 and ends this tranche
  unless a separately evidenced recovery remains within the same frozen
  semantics.
- Stdout and stderr go to the null device and are never read or retained.
- Credential/proxy variables are scrubbed and the accepted network guard stays
  preloaded; any network attempt rejects acceptance.

## Verification

Acceptance requires deterministic package/source checks, hostile sidecar and
patch tests, exact schema validation, focused controller tests, the complete
inherited native-Harness chain, Ruff, compilation, Git whitespace checks,
post-execution typed readback and the applicable governance suite. Exact test
counts may be reported only from completed machine output.

## Parallelism assessment

- DeepSeek lane: **declined before and during this rehearsal**. The real native
  factory construction is the object under test; dispatching a worker before it
  passes would cross the very boundary being established.
- Gemini lane: **declined unless the typed terminal or package semantics become
  ambiguous**. Exact local source bytes, registries, event counters and schemas
  provide the controlling evidence.
- Native-subagent lane: **declined**. Developer policy prohibits delegation and
  the one-process factory transaction is serial.
- GPT Sol owns implementation, the one possible native execution, evidence,
  acceptance, clockwork closeout and successor derivation.

## Explicit exclusions

This tranche authorises no published agent/session, turn, prompt, broker/model/
provider request, target edit, product source/configuration, API, database,
route, adapter, feature flag, allowlist, action grammar, first-party client,
waiting-area or ordinary-practice change. It authorises no generic-status
`Arrived` change; no product, patient, appointment, clinical, historical or
protected data; and no production runtime, deployment, release, Pages rebuild,
protected-evidence access or protected-ref movement.
