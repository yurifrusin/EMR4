# DeepSeek native Harness provider-free preterminal observability corrected-veto recovery plan

Date: 2026-08-20

Timestamp: 2026-08-20T06:39:41.6215071+10:00 (Australia/Brisbane)

Status: `frozen`

Planning source HEAD:
`e5c2d6dbc4ccc77879070ef29fcbd7dbf218b26e`

Operation:
`deepseek-native-harness-provider-free-preterminal-observability-corrected-veto-recovery`

## Objective and authority

Obtain one fresh corrected Gemini 3.7 Flash/high independent veto of unchanged
candidate `b5f0bc0d823a1c8009f3bb49efcc9a588b9703ab` after Yuri restored or
reauthorised Antigravity inference. The review packet's exact C03 count is 85.
No user-provided authorization code may be retained, copied into the
repository, printed, logged or passed unless the CLI explicitly requests it;
the existing authenticated session must be tested first.

This successor changes no candidate, implementation, test, product or accepted
evidence. It owns only this plan/threat delta, the clockwork transition, a fresh
five-source receipt, verifier preflight/receipts, and predecessor closeout
reconciliation if the veto passes.

## Frozen review contract

1. Review worktree HEAD is exactly
   `b5f0bc0d823a1c8009f3bb49efcc9a588b9703ab` and remains clean.
2. The corrected packet and exact ten-command manifest remain unchanged.
3. C03 contains and passes exactly 85 tests: 12 + 13 + 21 + 13 + 8 + 8 + 10.
4. Gemini must return one structured `pass` or `revision_required` decision and
   all ten exact command results.
5. A transport failure admits no review. One restored-authority attempt is
   permitted; any repeated 403 stops fail-closed without fallback.
6. A clean pass may reconcile and accept the predecessor recovery. A finding
   requires a separately frozen correction; it cannot silently mutate the
   reviewed candidate.

## Parallelism assessment

- **DeepSeek native Harness/model:** `declined`, negative leverage. The exact
  candidate and the failed native attempt are review subjects; no DeepSeek or
  native process is authorised.
- **Gemini:** `selected`, required independent leverage. One Gemini 3.7
  Flash/high read-only veto owns the only executable work package.
- **Native subagents:** `declined`, negative leverage. Developer policy forbids
  proactive delegation and the exact worktree/transport evidence chain is
  serial.

## Acceptance and boundaries

Acceptance requires the exact clean candidate, a passed fresh worktree
preflight, one structured Gemini 3.7 Flash/high `pass`, all ten commands at
exit zero, exact 85-test acknowledgement, unchanged HEAD and zero worktree
residue. Only then may the predecessor closeout be reconciled and its separately
named one-process recovery successor considered.

No native Harness, DeepSeek, broker, agent/session/turn, fallback model,
candidate change, product/API/configuration/client/flag/grammar/waiting-area
change, ordinary-practice enablement, data, network other than the single
Gemini verifier transport, Docker/database, production, deployment, release,
Pages or protected-ref movement is authorised. Protected refs remain exact at
`2e34bdad732fdab32fbf778280b3d3c70d66d602`; preserve every untracked file and
stage only explicit paths.
