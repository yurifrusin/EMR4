# Reception One v6.1 Pre-schema Continuation

Status: authorised; one closed historical failure eligible
Recorded: 2026-07-30

The frozen fifteen-case v6.1 cohort closed in seventeen calls with fourteen
exact outcomes. `b-move-shift` received HTTP 200, but its sole provider text
part was not valid JSON. The broker retained only
`provider_text_not_json`; no candidate reached the proofreader and nothing was
released.

Under Yuri's standing authored-synthetic diagnose-repair-evaluate authority,
this distinct continuation may replay only that same case with the unchanged
v6.1 prompt, request schema, proofreader, model, project, identity, keyless ADC,
Sydney endpoint, data class and isolation boundary. It receives a new
single-use ledger. One proofreader-ticket replacement remains possible only if
a schema-admitted first form is revision-required.

The continuation ceiling is one primary plus at most one correction, USD 1.
No raw provider response, hidden reasoning, API-key information, product data,
database access, write, confirmation, delivery, tool, fallback, deployment or
release is permitted. The closed seventeen-call cohort remains immutable.
