# DeepSeek native Harness rebound agent-creation boundary rehearsal closeout

Date: 2026-08-22

Timestamp: 2026-08-22T02:48:06.6590250+10:00 (Australia/Brisbane)

Status: **closed failed; native attempt consumed; no retry**

## Result

The provider-free rehearsal is not accepted as an agent-creation proof. The
single authorised native attempt is consumed at exact execution source
`39373fbbde75c7614dfe9c64e0fe1bcb5c5af212`.

The process admitted the pinned rc.7 package tree, reproduced the exact stock-
headless readiness sequence and accepted one HMR mutation. It then exited with
code 2 before writing the required typed sidecar. The controller classified the
result `PROCESS_EXIT_REJECTED`, performed no retry or resume, and proved process
absence, disposable-root absence, target absence, unchanged package seed and
zero broker/model/provider/network activity.

Because the sidecar is absent, the exact factory stage is unknown. The evidence
object's zero-valued `factory_boundary` map is a fallback projection and is not
accepted as an observation. This closeout makes no claim that `agents.create`
was or was not invoked, or that setup, private preparation, preset composition,
model selection, commit or publication veto was reached.

## Prelaunch correction

The controller's earlier invocation stopped before native launch because the
preset builder received the wrong typed contract family. That invocation left
zero native processes and did not consume attempt 001. The repaired controller
now selects the effective-tool contract through its owner, verifies preset id,
ordered tools, byte count and digest during deterministic checking before any
disposable root, and passes those already checked bytes into execution.

The repair passed 28 focused tests and the exact 205-test inherited chain, plus
Ruff, compilation, deterministic source/package checks and Git whitespace.

## Rejected generated claims

The generated Markdown report unconditionally states that the factory reached
the unpublished setup commit, and the generated efficacy object unconditionally
claims that this commit was under typed veto. Both contradict the failed result
and absent sidecar. Their exact bytes are preserved but their success claims are
rejected by `rejected-generated-claims.json`; `failure-interpretation.json` is
the authoritative bounded reading.

## Parallelism efficacy

DeepSeek was correctly declined because its factory boundary was under test.
Gemini remains declined: no external reviewer can reconstruct a runtime stage
that the process did not record, and a provider call would not improve this
provider-free evidence. Native subagents remained prohibited and non-leveraged.
GPT Sol owned the serial process, readback and containment.

## Successor

The narrow recovery is
`deepseek-native-harness-provider-free-post-hmr-agent-factory-closed-subcoordinate-diagnostic-rehearsal`.
It is not a retry of attempt 001. It may run one separately identified native
process whose minimal wrapper dynamically imports each package inside a guarded
transaction and emits exactly one finite typed stage/error sidecar on every
in-module path. Its accepted outcome is attributable diagnosis, not agent-
creation success.

No published agent/session, turn, worker, broker/model/provider request, target
use, product/configuration/API/database/route/adapter/flag/allowlist/grammar/
client/waiting-area change, ordinary-practice or generic `Arrived` change, data,
production, deployment, release, Pages, protected evidence or protected-ref
movement is authorised or claimed.
