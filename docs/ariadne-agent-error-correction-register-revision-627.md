# Ariadne agent error and correction register — revision 627

Date: 2026-08-22

Status: `accepted_closed_reading`

<!-- ariadne-agent-error-register-reading
revision: 627
incident_count: 1002
new_incident_ids: AER-0997,AER-0998,AER-0999,AER-1000,AER-1001,AER-1002
open_incident_count: 0
-->

This revision records six bounded observations across the corrected-graph useful-development recovery. All are corrected or contained, none is open, and the only occupied process remained inside the frozen one-process/one-request envelope.

## AER-0997 — closeout receipt repeated a machine-owned Git identity

The first closeout-precommit receipt after the accepted boot proof repeated a full protected Git object ID in narrative evidence. Preflight rejected it before commit or dispatch. The corrected receipt removed caller-authored Git identities and passed from the machine snapshot.

## AER-0998 — fresh graph materialization resolved ancestry after guard replacement

The first fresh-process materialization probe temporarily replaced the historical guard builder before resolving the accepted graph reconstructed through that builder, causing recursive source resolution. The controller now validates and caches the immutable graph before rebinding; the fresh process then materialized the exact runner, guard, bridge and sanitizer.

## AER-0999 — predecessor attempt latch equality was replayed

One inherited attempt-004 provider-free test asserted that its own consumed operation was still the live latch. The current latch correctly selected this new attempt, so the predecessor-only equality failed while all substantive inherited tests passed. The historical equality was explicitly deselected on the rerun.

## AER-1000 — occupied edit returned an error and no candidate

The one consumed Harness attempt completed one DeepSeek request and emitted one direct edit result, but the result terminalized at `edit_error_accept_not_concluded`. The target remained unchanged and no candidate was admitted. The attempt was contained without retry, Gemini invocation or residual process/root state, and the Harness recovery sequence is now closed.

## AER-1001 — closeout proposed an already-recorded product successor

The first closeout intent proposed the default-off route-adapter convergence as the next operation even though that tranche and later admission-control work are already accepted graph nodes. The clockwork rejected it before publication. The successor now uses a new read-only post-check-in programme orientation, and graph-membership validation remains the mechanical anti-circularity control.

## AER-1002 — closeout introduced an unindexed Baton label

The corrected closeout initially created a new active Baton acceptance label while the live handover was already at its compaction boundary. Clockwork rejected the unindexed label before publication. The closeout now replaces the existing current DeepSeek Harness acceptance row, keeping detailed artifacts in the graph and indexed closeout evidence.
