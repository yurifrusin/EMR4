# Provider-free Practice Context Fabric and Bureau Memory Bank contract closeout

Date: 2026-08-06

Status: accepted provider-free authored-synthetic contract

## Result

The first Practice Context Fabric descendant passes as
`raisa_provider_free_practice_context_fabric_bureau_memory_contract_pass` at
exact reviewed source HEAD `cb1b0a712f8ee5340e73d8adde19103af0d9ed97`.

It establishes one closed, unmounted and provider-free contract pipeline:

1. a model-shaped `ContextNeedCandidate` carries no principal, tenant, role,
   consent, retention or command authority;
2. authenticated backend context supplies a separately sealed
   `ContextAuthorityBinding`;
3. deterministic policy can only narrow purpose, Bureau, source, frame, field,
   interval, freshness, byte and result scope;
4. a closed `BureauMemorySelector` derives a bounded, lossy and expiring
   recent-work projection without exposing raw audit;
5. the assembler emits source-labelled immutable frames and exact selector and
   weave traces; and
6. a same-packet proofreader recomputes all bindings and blocks stale,
   superseded, out-of-scope or tampered material before release.

The documentation-only GraphQL extension adds one read field to the existing
`Query` graph. It adds no mutation, subscription, standalone Memory Bank root,
resolver, route, persistence or command surface.

## Evidence repair and exact-head veto

The first schema-constrained Antigravity review correctly returned
`revision_required`: the committed evidence embedded a pre-commit Git HEAD and
used checkout-byte hashes that differed under LF/CRLF conversion. The bounded
repair removed the impossible self-reference, canonicalised text artifacts to
LF before hashing, added the generator and focused test to the bound artifact
set, and required an external exact-head review receipt.

The repaired evidence uses schema
`emr4.practice_context_fabric_bureau_memory_acceptance.v2` and binds eight
artifacts under digest
`sha256:9ec8724ebcca37b99f690ccfae23cbd58d7b794b6c39e2588e12e72c3fb1c6d7`.

A fresh Gemini 3.6 Flash/high Antigravity veto then passed exact HEAD
`cb1b0a712f8ee5340e73d8adde19103af0d9ed97` through the closed
`schema_constrained_json_v1` decision contract. The reviewer ran its listed
seven-module packet with 132 passing tests and left the independent worktree
at the same HEAD with `dirty_after: false`. Sol separately reproduced the
expanded ten-module packet in that worktree with 157 passing tests.

## Accepted claim boundary

This acceptance proves deterministic authored-synthetic component behavior,
closed-schema admission, scope narrowing, canonical Memory Bank selection,
same-packet proofreading, read-only API composition and zero provider, network,
database, subprocess, product-runtime, command, deployment or protected
operations by the engine.

It does not prove or authorise patient, clinical or product-derived data; a
real practice database; raw audit access; persistence or retention; provider or
external retrieval; a product route, resolver or service; commands or writes;
deployment, production, release, Pages, protected evidence or protected-ref
movement. `docs/branding/` remains preserved and excluded.

## Next descendant

Under Yuri's standing planned-gate authority, the next safe descendant is the
provider-free **Current operational weave**. It may compose existing authorised
Diary, waiting-room, practitioner-directory and application-session read
projections into authored-synthetic Context Fabric frames without adding a new
product route or source. All real-data, persistence, provider, command,
deployment, release and protected boundaries remain closed.
