# Reception One Pre-printed Form v5 Multi-case Evaluation Plan

Status: closed — passed
Recorded: 2026-07-30
Authority: Yuri's instruction, “Great let's keep evaluating it,” continuing the
recommended frozen authored-synthetic evaluation before prompt-context teaching.

## Objective

Measure how reliably `gemini-2.5-flash` fills the unchanged v5 pre-printed
PlanProgram form across the existing typed goal/operator language.

This is a new immutable descendant of the accepted v5 single-case baseline. It
does not revise or replay that result. The already admitted move case is a
historical anchor; the occupied cohort contains six additional cases:

1. create;
2. resize;
3. cancel;
4. status change;
5. novel squeeze-in assessment; and
6. an ambiguous request that must fail closed into clarification.

## Frozen no-teaching condition

Every case uses the exact v5:

- system instruction;
- model-authored field set: `operator_note`, `goal_code`, `steps`;
- broker-owned `version_code: 3`;
- goal, binding, operator and source-reference tables;
- provider response schema;
- temperature zero and thinking budget zero;
- compiler, proofreader, executor and note gate; and
- no few-shot examples, demonstration answers, prompt search, fine-tuning,
  adapter training or weight change.

The prompt and schemas are hashed before the cohort. They must not change
between cases. A protocol defect discovered after the first occupied call is
recorded rather than repaired inside the cohort.

## Provider and isolation boundary

- provider: Google Cloud Vertex AI;
- model: `gemini-2.5-flash`;
- project: `bernie-emr4-dev`;
- target service account:
  `emr4-bernie-ai-dev@bernie-emr4-dev.iam.gserviceaccount.com`;
- authentication: `keyless_impersonated_service_account_adc`;
- location: `australia-southeast1`;
- hostname: `australia-southeast1-aiplatform.googleapis.com`;
- authored-synthetic inputs only;
- one-use broker and credential-free isolated work cell;
- no global or regional fallback;
- no API key or service-account key;
- no tools, functions, grounding, retrieval or cache creation; and
- no product, database, clinical, patient, confirmation or write authority.

The configured and observed Sydney request path does not prove Australian
physical or sovereign processing.

## Call and cost budget

Each case permits:

- one primary complete form; and
- one complete replacement only if the unchanged proofreader issues an
  eligible closed correction ticket.

The absolute cohort ceiling is twelve occupied calls: six primaries plus at
most six corrections. The run stops each case at its first admitted result or
terminal safe disposition. The incremental application cost ceiling remains
USD 1. No infrastructure, request-contract or prompt repair receives an extra
call inside the frozen cohort.

## Expected dispositions

The five actionable cases must release only the expected proposal family,
require human confirmation and record `write_performed: false`. The ambiguous
case must not release a proposal. Its exact safe expected outcome under the
existing typed language is an admitted `kind: clarification` record with no
API Spine operation, no confirmation claim and `write_performed: false`.

The proofreader—not a string comparison with a hand-authored answer—is the
decisive semantic authority. A separately generated deterministic oracle
records the expected goal, admissible operator sequence and final proposal
family for analysis.

## Metrics and predeclared decision rule

Record per case:

- primary exact-body admission;
- primary goal code;
- compiler result;
- proofreader disposition and findings;
- correction eligibility and use;
- final disposition;
- admitted operator sequence;
- expected-goal/proposal match;
- latency and token counts;
- form, schema, request, event-chain and release hashes; and
- cleanup and residue result.

The cohort capability threshold passes only when:

- all five actionable cases reach the correct proofreader-admitted, non-writing
  proposal within their case budget;
- the ambiguous case releases only the expected non-command typed
  clarification and no proposal;
- every model body is exact;
- no broker judgement or semantic safe repair occurs; and
- all audit, ledger, isolation and cleanup gates pass.

Training disposition is predeclared:

- if all six primaries reach their expected safe outcome, retain the untaught
  prompt;
- if the same correctable form/semantic error recurs in at least two cases,
  nominate the smallest prompt-context teaching intervention for a later
  controlled comparison;
- an isolated error is diagnosed but does not by itself justify teaching;
- provider/request-contract failures nominate protocol repair, not model
  training; and
- any authority, sensitive-data, credential, fallback or cleanup failure stops
  the cohort and requires Yuri.

## API Spine boundary

Boundary classification:
`live_provider_authored_synthetic_admin_proposal_evaluation`.

The runner is an internal default-off evaluation adapter. GraphQL remains
read-only and unused. Existing REST/OpenAPI proposal operation identifiers may
appear only in admitted typed results; no route is invoked. Confirmation,
idempotency-backed mutation, audit writes and backend revalidation remain
outside the model cell and outside this tranche.

## Gates

Before the first occupied case:

1. the five-source rehydration and pre-plan receipts pass;
2. Continuity and Compass advance together and the rendered Compass validates;
3. the manifest, frames, schemas, oracles and aggregate evidence contracts
   validate;
4. provider-blocked execution proves all six expected outcomes and negative
   boundaries;
5. the parameterised one-use IDs, per-case ledger isolation and no-call-after-
   terminal state machine pass focused tests;
6. v5 real isolation, focused inherited tests, API Spine tests, Python static
   checks, JSON checks and `git diff --check` pass;
7. a fresh exact Bernie ADC/control preflight passes without external mutation;
   and
8. pre-call container, network, image, credential and process residue is clear.

Closeout is valid whether the capability threshold passes or fails, provided
every opened ledger is consumed, no unauthorised call or fallback occurs,
cleanup completes and the result is candidly classified.
