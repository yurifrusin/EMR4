# Ariadne Sydney Vertex Gemini 2.5 Flash — ADC-Restored Continuation Plan

Date: 2026-07-24  
Owner: GPT Sol  
Authority: Yuri's instruction that the exact Bernie impersonated ADC has been
authenticated, read together with the frozen bounded-work-cell authority and
Gemini 2.5 Flash model delta  
Status: active at repeated Tranche 3

## Historical integrity

The earlier
`ariadne_vertex_sydney_gemini_25_adc_preflight_blocked` result remains
immutable. It correctly closed after the exact configured impersonated ADC
failed two permitted non-interactive refresh checks. It consumed no occupied
provider call, opened no durable rehearsal ledger and started no container.

Yuri has now reported an external credential-state change and directed the
Tranche 3 preflight and conditionally gated successors to be tried again. This
plan creates a new Continuity descendant. It does not revise the historical
node, evidence, audit, Sol review or closeout.

## Exact frozen binding

| Control | Binding |
|---|---|
| Provider | Google Vertex AI only |
| Model | `gemini-2.5-flash` only |
| Project | `bernie-emr4-dev` |
| Target service account | `emr4-bernie-ai-dev@bernie-emr4-dev.iam.gserviceaccount.com` |
| Authentication | `keyless_impersonated_service_account_adc` |
| OAuth scope | `https://www.googleapis.com/auth/cloud-platform` |
| Required permission | `aiplatform.endpoints.predict` |
| Location | `australia-southeast1` |
| Base URL | `https://australia-southeast1-aiplatform.googleapis.com` |
| Data | authored-synthetic only |
| Primary occupied calls | at most one |
| Conditional request-contract repair retry | at most one |
| Absolute occupied calls | two |
| Occupied calls already consumed | zero |
| Application cost ceiling | USD 1 |
| Provider/model/project/identity/region fallback | prohibited |
| Product/database/clinical/patient/command authority | none |

The work cell receives no ADC, OAuth credential, service-account secret,
Google Cloud CLI state or unrestricted network access. Only the one-use broker
may use `google.auth.default()` and only for the exact impersonated binding.
Provider API keys are neither read nor used and are omitted from child
environments. No service-account key is created, inspected, copied, mounted or
used.

## Ordered continuation gates

1. Repeat the complete read-only Tranche 3 preflight. Require exact
   impersonated credentials, project, service account, scope, existing billing
   entitlement, enabled Vertex AI API, exact prediction-only custom role and
   binding, exact prediction permission, Vertex Data Access logging, absent or
   disabled request-response logging, disabled provider in-memory caching,
   zero user-managed service-account keys, exact Sydney endpoint and current
   `gemini-2.5-flash` regional catalogue presence.
2. If and only if every Tranche 3 control passes, implement and execute the
   provider-free real-isolation Tranche 4 with an exact disposable build
   context, internal-only cell network, one-use broker, proofreader, consumed
   dry-run ledger, deterministic teardown and independent residue checks.
3. Before an occupied call, require repository-only and provider-blocked gates,
   synchronized Continuity and Compass revisions, exact rendered Compass
   validation, a fresh pre-attempt receipt and one distinct open occupied
   ledger.
4. If every predecessor passes, make one primary authored-synthetic call to the
   exact Sydney Vertex `generateContent` URL. Use no tools, function calling,
   grounding, retrieval, cache creation, product/database input or fallback.
5. Deterministically proofread the exact typed output and atomically release
   only schema-admitted authored-synthetic fields. Mechanical whitespace, enum
   casing and deterministic ordering are the only safe repairs.
6. Consume every opened ledger, perform complete teardown and residue checks,
   create the bounded external audit, independent audit analysis, Sol review,
   closeout and synchronized Continuity/Compass/handover updates whether the
   occupied attempt passes or fails.

## Retry and error boundary

A second occupied call is eligible only if the primary fails solely because
of a deterministic request-contract defect. Before such a call, close and
independently audit the first ledger, diagnose only through the bounded
provider-error contract, add a focused regression, rerun every
repository-only/provider-blocked gate, open a distinct ledger and prove the
entire frozen binding remains unchanged. No other retry is authorised.

The durable error record may include only HTTP status, bounded provider code,
normalized status, allowlisted field paths, a scanned diagnostic no longer
than 2 KiB and a hash of the discarded complete raw error. Raw prompts,
responses, credentials, tokens, free-form provider text, rejected drafts and
reasoning remain excluded.

## Stop conditions

Stop for Yuri without changing external state if the exact ADC remains
unusable; interactive or replacement credential action is needed; API,
billing, IAM, project, service-account or human-account state must change; the
role is missing or broader than exact prediction; Data Access logging is
unavailable; request-response logging or cache disablement cannot be proved;
the exact model is unavailable in Sydney; any provider/model/project/identity/
credential/region/endpoint substitution or fallback is needed; API/static-key
authentication, non-synthetic data, greater cost, credential exposure,
weakened isolation/audit, unexpected charges or broader authority would be
required.

No result may claim Australian physical or sovereign processing beyond
Google's published locational-endpoint contract and the observed exact Sydney
request path.
