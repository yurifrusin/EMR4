# Disposable PostgreSQL behavior row-projection rebind

Date: 2026-08-08

Status: provider-free contract rebind candidate; behavior runtime closed

## Trigger

Behavior attempt 015 stopped before admitting a scenario at `BTR-E01` with
SQLSTATE `22P02`. The exact function coordinate exposed positional drift
between complete typed row projections and their physical PostgreSQL table
composites. The source-wide correction and renderer invariant now pass, and
the corrected inert artifact separately passed a fresh PostgreSQL 16
parse/catalogue rehearsal with all exact catalogue digests unchanged.

## Exact parent rebind

The behavior contract's six parents now bind:

- accepted corrected parse/catalogue evidence source
  `2f0047cd90a8448ec4e738483a7237fbf2860bcb` through its final closeout;
- inert SQL
  `sha256:83359fbc0cf2fb8f7d147b5dc820aa28910129428c9727daa1e1dc0259ce73f5`
  at corrected source `0931f3e658f06e02e7de4c5ea02238184da9e767`;
- render-manifest file SHA-256
  `sha256:66c103adac8c9ba52440077e25d2f3fc58ed6d30005576034bb42115c746dd71`
  at the same corrected source;
- the unchanged structural migration/transaction contract;
- corrected body-contract file SHA-256
  `sha256:4338082445261a8a4aeaaa09d9aa615812d1585fa47b44a05abc716a1df84242`
  at the same corrected source; and
- the unchanged synthetic prerequisite contract as exercised by the accepted
  corrected parse run.

All six text parents retain canonical UTF-8/LF hashing and exact Git-source
binding. The accepted runtime-source ledger is the only deliberate exception:
its current accepted content names the exact earlier runtime evidence source,
as the harness already requires.

## Unchanged scenario and authority boundary

All twenty scenarios remain byte-for-byte unchanged, in the exact existing
order and category counts `6/4/3/4/3`. No principal, fixture, SQLSTATE,
transaction shape, readback, forbidden effect, Docker profile, evidence field,
claim or cleanup rule changes.

The current mutable behavior evidence and immutable attempts 001-015 remain
predecessor evidence only. They cannot prove the rebound contract. Attempt 016
remains closed until the rebound deterministic and hostile packet passes and a
fresh exact-HEAD Gemini 3.6 Flash/high veto returns one pass from an unchanged
clean worktree.

## Closed boundary

There is no patient, clinical, product-derived or protected data; no provider
or external retrieval; no applied migration, application/API/Diary wiring,
operational database or persistence, watcher/listener/feed, command or product
write, deployment, production, release, Pages or protected-ref movement.
`docs/branding/` and all unrelated untracked files remain preserved and
excluded through explicit-path staging only.
