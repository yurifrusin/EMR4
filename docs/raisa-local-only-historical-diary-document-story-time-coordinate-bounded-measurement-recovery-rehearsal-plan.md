# Raisa local-only historical Diary document-story time-coordinate bounded measurement recovery rehearsal — plan

Date: 2026-08-24

Timestamp: 2026-08-24T06:47:31.5187538+10:00 (Australia/Brisbane)

Status: `frozen_fail_closed_plan`

Operation: `raisa-local-only-historical-diary-document-story-time-coordinate-bounded-measurement-recovery-rehearsal`

Planning source HEAD: `7c681d56d2e6084838c96625a4c240e4f4d0e59f`

Reasoning level: Extra High. This tranche returns to a narrowly approved local
historical source after an earlier terminal timeout. It freezes privacy,
one-run, process-control and first-use meaning before any new metadata bind or
content access.

## Objective and inherited evidence

Perform exactly one fresh local measurement of whether complete time tokens in
the Word document main story can be associated with structural table-cell
segments through bounded same-page rendered coordinates.

The accepted authored-synthetic recovery at exact reviewed source
`9cbc39cda3503216b27e81aca4f9199db8561b49` proved:

- typed `word_extractor_timeout` classification;
- exact PID, `WINWORD` class and start-time parent cleanup without a process-
  name fallback;
- preservation of the pre-existing Word process set;
- strict count-only progress; and
- 2,016 coordinate attempts plus 288 story anchors in the
  `30_to_119_seconds` / `16_or_more_per_second` buckets.

This evidence justifies an honest 1,800-second controller ceiling. It does not
predict historical `.doc` completion or authorize a reusable derived artifact.

## Exact local source and new attempt

The only source root is:

`local_data/historical-diary-trove/raw/pilot_01`

The only new ignored attempt root is:

`local_data/historical-diary-trove/measured-probes/2026-08-24-story-coordinate-v2`

The source remains one dense day, one leaf root, non-recursive and read-only.
Selection is the first 80 chronological regular non-reparse files, capped at
8 MiB each and 128 MiB total. Symlink/reparse traversal, a second root or day,
and any reuse or retry of the consumed `story-coordinate-v1` attempt are
forbidden.

The `v2` attempt root, manifest, terminal and aggregate must be absent before
Phase A. Any unexpected prior state blocks rather than being removed or reused.

## Frozen mapper

The accepted mapper is unchanged:

1. PowerShell locally parses only complete time tokens in the document main
   story outside tables and emits integer minute plus private adjusted-page and
   quarter-point vertical coordinates; main-story text never crosses the pipe.
2. Each structural table-cell segment receives its own coordinate when Word
   supplies one. Manual-line subdivisions without an independent position are
   unavailable and never inherit a guessed parent baseline.
3. Candidates must share the adjusted page and be no farther than 16 quarter-
   points (four points).
4. One unique nearest minute maps; identical duplicate anchors may collapse,
   but a same-distance different-minute tie remains unmapped.
5. Row, column, paragraph ordinal, opening hours, filename, timestamp and the
   expected interval mode confer no fallback.
6. Page, coordinate, distance, anchor and mapping values are private ephemeral
   inputs and are destroyed after reduction.

No tolerance, selection, parsing or fallback rule may change after archive
access.

## Deterministic pre-access admission

Before the `v2` attempt root is created, all relevant provider-free controls
must pass, including:

- strict manifest, story-anchor, segment-coordinate, control, progress,
  terminal, aggregate and cleanup schemas;
- zero/boundary/out-of-bound, cross-page, duplicate-anchor, different-time tie
  and unavailable/manual-line mapper cases;
- exact timeout, abnormal-exit, invalid-output and cleanup-failure typing;
- exact process-identity cleanup with no broad name kill;
- count-only monotonic bounded progress with no private value fields;
- no row/column/ordinal/opening-hour/filename/interval fallback;
- zero source text, identity, filename, timestamp, page, coordinate, distance,
  key or mapping in public output; and
- unchanged H5/H15, selection, privacy, stable-linkage, adjacent-change and
  cleanup controls.

Ruff, compileall, PowerShell parsing, source-boundary scans and
`git diff --check` must pass. A deterministic failure prevents Phase A.

## Phase A — one metadata-only bind

Phase A may run exactly once. It creates the new ignored `v2` attempt root and
binds exactly 80 files, byte caps, controller/extractor digests and selection
facts without opening file content. Strict readback must prove zero content
reads and the exact source/attempt boundaries before Phase B.

A failed or ambiguous bind closes the tranche. There is no second bind.

## Phase B — one content run

After Phase A passes, Phase B may run exactly once. It writes the one-way
content terminal before Word launch and opens only the 80 bound files in one
owned invisible read-only Word process. The Python parent gives the extractor
1,800 seconds and owns exact process cleanup on timeout, abnormal exit or
invalid output.

Progress contains aggregate counts and the existing coarse elapsed/rate
buckets only. It contains no document identity, path, exact timestamp, text,
time value, page, coordinate, distance, key or mapping. No second content run
is authorized for pass, revision, timeout or failure.

## Decision and acceptance

`locally_restricted_candidate` requires all of the following in the sole run:

- exactly 80 snapshots opened and parsed;
- at least three explicit story anchors and three distinct mapped minutes;
- at least 25% of structural occupancy segments mapped through the frozen
  unique same-page distance rule;
- a positive deterministic interval mode;
- nonzero stable linkage and adjacent changes;
- zero ambiguous, cross-page or over-distance admission;
- zero source/private value leakage; and
- complete removal of Word, manifest, projection, key, mapping, coordinate,
  control and progress state.

`revision_required` applies when containment and cleanup pass but mapping,
interval, linkage or motion utility is insufficient. `blocked` applies to any
root, binding, digest, schema, raw output, leakage, process-identity, cleanup or
authority failure. A timeout is reported with its exact typed reason and
cleanup disposition and remains terminal without retry.

The strongest result is an ignored local research reading with no downstream
authority. It is not an anonymity claim and does not admit a reusable artifact.

## First-use gate

The historical-derived first-use gate is useful at promotion, not at private
measurement. It remains `closed_pending_candidate_specific_evaluation` for the
whole tranche. Even a passing measurement cannot create or retain a reusable
scenario, fixture, benchmark, replay, corpus, memory/RAG input or product test.

If a useful candidate exists, only a later separately frozen gate may evaluate
whether a particular minimized derivative is safe and useful enough for first
use. That evaluation must not need or retain raw source values.

## Parallelism assessment

- **DeepSeek:** declined with negative leverage. Native Harness occupied use is
  paused, silent Claude fallback is forbidden, and no external worker may
  receive private historical text, coordinates or the one-run terminal.
- **Gemini:** not applicable with neutral leverage. Provider/model transmission
  is forbidden and acceptance is deterministic and local.
- **Native subagents:** declined with negative leverage. Manifest, Word,
  control/progress files, ephemeral keys and the content terminal form one
  serial private state.
- **GPT Sol:** owns planning, implementation, deterministic admission, the one
  bind, the one content run, cleanup, acceptance, Git and closeout.

Reassess at a later named tranche boundary only. There is no separable occupied
work package in this tranche.

## Closed surfaces

No new source root/day, recursive access, reparse traversal, cap increase,
source mutation, content retry or prior-attempt reuse; no raw text, identity,
note, contact value, filename, exact date/timestamp, page, coordinate, distance,
key or mapping in Git/stdout/conversation; no provider, network, model,
telemetry, clipboard or external release; no historical-derived first use; no
product runtime, route, API, client, database or configuration; no ordinary-
practice activation; and no production, deployment, release, Pages, protected
evidence or protected-ref movement. Local/origin `master` and
`handoff/current` remain exactly
`2e34bdad732fdab32fbf778280b3d3c70d66d602`. Preserve `docs/branding/` and
all unrelated untracked files. Stage explicit paths only.
