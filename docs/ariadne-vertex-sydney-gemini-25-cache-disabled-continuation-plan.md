# Ariadne Sydney Vertex Gemini 2.5 Flash — Cache-Disabled Continuation Plan

Date: 2026-07-24
Owner: GPT Sol
Authority: Yuri's complete bounded Sydney Vertex authorization, Gemini 2.5
Flash reorientation, and the sanitized operator verification supplied on
2026-07-24
Status: active at fresh rehydration

## Historical integrity

The earlier ADC failure and later
`ariadne_vertex_sydney_gemini_25_cache_control_blocked` continuation remain
immutable. Yuri supplied a sanitized external operator observation showing
that the exact project-level Vertex cache control now has
`disableCache: true`. This plan creates a new Continuity descendant and does
not revise either historical result.

The terminal image is not retained. Durable evidence records only the
allowlisted control name, exact project, documented management hostname,
field-presence result and Boolean value. It records no access token,
credential, authentication response, API-key information or human account
identifier.

## Exact binding

| Control | Binding |
|---|---|
| Provider | Google Vertex AI only |
| Model | `gemini-2.5-flash` only |
| Project | `bernie-emr4-dev` |
| Target service account | `emr4-bernie-ai-dev@bernie-emr4-dev.iam.gserviceaccount.com` |
| Authentication | `keyless_impersonated_service_account_adc` |
| OAuth scope | `https://www.googleapis.com/auth/cloud-platform` |
| Required prediction permission | `aiplatform.endpoints.predict` |
| Inference location | `australia-southeast1` |
| Inference base URL | `https://australia-southeast1-aiplatform.googleapis.com` |
| Cache-control management URL | `https://us-central1-aiplatform.googleapis.com/v1/projects/bernie-emr4-dev/cacheConfig` |
| Data | authored-synthetic only |
| Primary occupied calls | at most one |
| Conditional deterministic request-contract retry | at most one |
| Absolute occupied calls | two |
| Occupied calls already consumed | zero |
| Application model-call ceiling | USD 1 |
| Provider/model/project/identity/region fallback | prohibited |
| Product/database/clinical/patient/command authority | none |

The documented `us-central1` cache-control resource is a project-level
management endpoint whose setting applies across regions. A GET to that
resource carries no rehearsal prompt or model payload and is not an inference
fallback. Every model payload remains pinned to the exact Sydney inference
hostname.

## Ordered gates

1. Repeat mandatory five-source rehydration and create a new receipt.
2. Repair the repository-local cache check to use Google's documented
   project-level management resource, add a focused regression and repeat the
   complete read-only Tranche 3 preflight.
3. Proceed only if the exact impersonated ADC, project, service account, scope,
   billing/API, prediction-only role/binding/permission, Vertex Data Access
   audit, absent/disabled request-response logging, explicit
   `disableCache: true`, zero user-managed keys, Sydney endpoint and current
   publisher-model catalogue all pass.
4. Run the full provider-free real-isolation lifecycle with one-use dry-run
   ledger, non-root/read-only/bounded cell, broker-only communication,
   negative route tests, deterministic proofreader, teardown and independent
   residue checks.
5. Increment Continuity and Compass together, validate their revision binding
   and exact rendered Compass, create a fresh pre-attempt receipt and open one
   distinct occupied ledger.
6. Make exactly one primary small authored-synthetic call through the exact
   Sydney Vertex URL. The work cell receives no ADC, OAuth, API key, Google
   Cloud CLI state or service-account secret. Only the one-use broker may
   refresh the existing impersonated ADC and call Vertex.
7. Run the deterministic proofreader. Release only schema-admitted,
   authored-synthetic typed fields; allow only whitespace, canonical enum
   casing and deterministic-order repair. No command-shaped, product, human or
   database delivery exists.
8. Consume every opened ledger, perform complete cleanup, write the external
   audit, obtain an independent repository/runtime-boundary review, complete
   Sol review and synchronize Continuity, Compass, the rendered report,
   `AGENTS.md` and the orchestration ledger.

## Retry and error contract

A second occupied call is eligible only when the primary fails solely because
of a deterministic request-contract defect repairable without changing the
provider, model family, project, service account, credential class, region,
endpoint, data classification, isolation, response contract, cost ceiling or
residency assurance. Before it, the first ledger must be consumed and
independently audited, a focused regression added, all repository-only and
provider-blocked gates repeated, and a distinct ledger opened.

Provider errors retain only HTTP status, bounded provider code, normalized
status, allowlisted field paths and the hash of discarded raw error bytes.
Free-form messages, raw prompts, raw responses, credentials, tokens, rejected
drafts and reasoning are excluded.

## Stop boundary

Stop for Yuri on an unusable ADC; required login or credential
reconfiguration; API/billing/IAM/project/service-account/human-account change;
role broader than exact prediction; unavailable Data Access logging;
unverified request-response logging or cache disablement; unavailable Sydney
model; provider/model/project/identity/credential/region substitution; global
inference or fallback; API/static key; non-synthetic data; possible USD 1 or
two-call breach; credential exposure; weakened isolation/audit; unexpected
charge; or broader authority.

No result may claim Australian physical or sovereign processing beyond the
configured Sydney locational endpoint, the observed request path and Google's
published contract.
