# Provider-free check-in relay-free recovery attempt 004 plan

Date: 2026-08-20

Timestamp: 2026-08-20T02:54:58.3293864+10:00 (Australia/Brisbane)

Status: `frozen`

Planning source HEAD:
`1ddfb4819b161e64731f73ecf80b540ac4e5a9fd`

Accepted attempt-003 execution source:
`19e4414fec067fcbb6af12818e432953432878be`

Accepted attempt-003 retained-evidence source:
`d2c6f7e465b1bcf2f8cf458a8fbd5721631db422`

Accepted call-site/pre-registry implementation source:
`95d456a1e3861ae463cf3643f347fa666c75fa48`

Accepted call-site/pre-registry reviewed candidate:
`8bda88069daeb314998341fc961b9aa061d496e5`

Accepted no-database clockwork source:
`958ae762e7c6a065b5926f47eb1a2b63115212c7`

Accepted native-Harness boot-proof clockwork source:
`5ff79d68f6df25d8bebdba78a6d504afb64de2ab`

Accepted relay-free transport source:
`4f0f54c2b0861828f9994444201b8da1bd54be00`

Accepted runtime-role and tenant-isolation source:
`6a2832575e9b4df5c40a13984db7281e79814a94`

Operation:
`raisa-provider-free-check-in-relay-free-recovery-attempt-004`

Target result:
`raisa_provider_free_check_in_relay_free_recovery_attempt_004_pass`

Reasoning level: Extra High freezes the fourth one-run authority, immutable
failure lineage, repaired lifecycle, transaction identity, forced-RLS,
provider-free and exact cleanup boundaries. High is sufficient for the fixed
adapter, deterministic admission, exactly one occupied execution, independent
veto and clockwork closeout while this plan remains unchanged.

## Authority and objective

Yuri's standing uninterrupted-development authority admits this separately
named descendant after the accepted attempt-003 call-site/pre-registry cleanup
repair, no-database interlock and native-Harness boot proof. It authorises
planning and deterministic construction first, then exactly one occupied local
execution only after every preexecution gate and a clockwork one-run checkpoint
pass.

The single execution must prove, in one uniquely named provider-free disposable
PostgreSQL 16 environment, that:

1. an authored-synthetic effect/receipt/audit transaction is explicitly rolled
   back and fresh restricted-role readback observes zero members;
2. a disjoint transaction commits once and loses its complete terminal response
   through the accepted relay-free caller path;
3. the caller releases neither success nor retry;
4. fresh restricted-role authoritative readback observes one exact consistent
   packet, zero duplicates and zero cross-practice visibility; and
5. the ephemeral role, attachments, sidecars, server and network are exactly
   absent at close.

Invocation consumes attempt 004 regardless of pass, failure, interruption or
unexpected terminal form. This plan has no retry path. Any future occupied run
requires a new operation, output namespace and frozen plan.

## Immutable negative and corrective evidence

Attempts 001 through 003 are consumed and immutable. Their exact artifacts may
be read and hash-checked but never edited, replaced, renamed, regenerated or
used as attempt-004 output:

| SHA-256 | Exact source |
|---|---|
| `5c38080aa27615ea1efad166d14a61605596130058498ea03c8b631bbeae3be2` | `orchestration/continuity/raisa-provider-free-disposable-postgresql-default-off-check-in-relay-free-rollback-unknown-commit-recovery-rehearsal/rehearsal-failure-evidence.json` |
| `a8920b0a294b43c8f67d0348bc6087b84921f8f8788ccd3f969913d95861c06a` | `orchestration/continuity/raisa-provider-free-disposable-postgresql-default-off-check-in-relay-free-rollback-unknown-commit-recovery-rehearsal/attempt-001-cleanup-recovery.json` |
| `7efb9853beee9723dbb01fac1f03c4392216bfcc15e9f490f4cb0baae08920ff` | `orchestration/continuity/raisa-provider-free-check-in-relay-free-recovery-attempt-002/rehearsal-failure-evidence.json` |
| `6418ecf2e2356b6c875a70106136cdc65d6e545ead5fceeb2c793db45ebe2e40` | `orchestration/continuity/raisa-provider-free-check-in-relay-free-recovery-attempt-002/attempt-002-execution-envelope.json` |
| `e8bf62e86fd3dbcfbcd7a0d68628e0d736b06617f4ef1a023a9a8928344fe96b` | `orchestration/continuity/raisa-provider-free-check-in-relay-free-recovery-attempt-003/rehearsal-failure-evidence.json` |
| `91e12b3268283fc3be48df583f7a0650a5a30bdaee40b1f74297d8185af91c75` | `orchestration/continuity/raisa-provider-free-check-in-relay-free-recovery-attempt-003/attempt-003-execution-envelope.json` |
| `048cd946166fabb8b2ce3400e31c85ee2fe410e6a3c07d5d26cbc79141250b71` | `orchestration/continuity/raisa-provider-free-check-in-relay-free-recovery-attempt-003/attempt-003-cleanup-recovery.json` |

The accepted corrected harness is executable input at exact SHA-256
`eda68427b87db48064bcfb82762d55c51b600cf2ba5d4724a0faae24d8a3db5b`:
`scripts/raisa_provider_free_disposable_postgresql_default_off_check_in_relay_free_rollback_unknown_commit_recovery_rehearsal.py`.
It preserves the two real `network_name` bindings and ownership-checked cleanup
for every post-create/pre-registry exception. Its Created-state evidence and
repair attestation remain exact:

| SHA-256 | Exact source |
|---|---|
| `9f721e0d0e11f5570c2ebe95f8e62d4f1f0e7b2af27f704e4108e2f1792fb98b` | `orchestration/continuity/raisa-provider-free-docker-created-state-profile-conformance-repair/created-state-representation-evidence.json` |
| `49c5a3673d388fc84b2f046a993a8f4c747f9887252ef4cdd2dfcc59e9a11410` | `orchestration/continuity/raisa-provider-free-docker-created-state-profile-conformance-repair/repair-attestation.json` |

The accepted transaction contract and attestation schema remain exact SHA-256
`bed2a89a3814ba9e9ac006d0fdb0c68d204fec53d8c21b6128190605b6ad9ec2`
and `d2c186b0d30419e0459d93d92af1f84907125becdeb75c7e1890dce597d3e72c`.
The accepted no-database interlock evidence is exact SHA-256
`9770af5d6d8e4282456e2ddd43ce6359c5dbff13b974c7d37a887fab331476d8`:
`orchestration/continuity/ariadne-provider-free-no-database-manifest-runner-admission-repair/provider-free-no-database-admission-evidence.json`.
The accepted provider-free native-Harness HMR evidence is exact SHA-256
`68d4168649d80268fdb81ba3582bed261e7944fc0f19d24aee5933270882afcc`:
`orchestration/continuity/deepseek-native-harness-provider-free-stock-headless-to-custom-runner-hmr-boot-proof/provider-free-native-harness-hmr-boot-evidence.json`.
Those two harness controls are prerequisites and lineage evidence only; attempt
004 invokes no DeepSeek model or provider.

Every Git binding is a full 40-character commit object and must be an ancestor
of the execution candidate. Seven-character abbreviations and caller-completed
objects are inadmissible.

## Narrow implementation boundary

The repaired transaction/Docker harness remains byte-for-byte unchanged. The
only permitted executable addition is one fixed fail-closed attempt adapter:

- add `scripts/raisa_provider_free_check_in_relay_free_recovery_attempt_004.py`
  with only `--check` and `--execute` and no caller-supplied output path;
- add one closed attempt-004 execution-envelope schema and one collision-free
  Continuity namespace;
- bind the base harness's attestation/evidence/failure globals only for the
  single runner call and restore all three unconditionally in `finally`;
- refuse execution if any attempt-004 terminal path already exists and never
  delete, rotate, rename or overwrite a terminal;
- bind the full frozen-plan commit, all accepted full-Git sources, repaired
  harness digest, immutable attempts 001-003, no-database evidence and native
  boot evidence into static admission and the terminal envelope; and
- add focused pure tests for CLI closure, full-object ancestry, byte-exact
  inputs, collision denial, exact routing, unconditional binding restoration,
  closed envelope validation, predecessor immutability and sanitized failure.

The exact attempt-004 terminal filenames are:

- `transaction-attestation.json` plus `rehearsal-evidence.json` on pass;
- `rehearsal-failure-evidence.json` on failure; and
- exactly one `attempt-004-execution-envelope.json` for the occupied execution.

All live terminal files are under
`orchestration/continuity/raisa-provider-free-check-in-relay-free-recovery-attempt-004/`.
Construction creates only schemas and deterministic source/tests; it creates no
terminal file and no Docker or database resource.

## API Spine and product boundary

This is a security/audit/idempotency rehearsal of the accepted REST command
shape, not an API change. The mixed API Spine remains exact:

- GraphQL is read-only and receives no mutation, database or provider authority;
- the closed fixture retains explicit actor and practice scope, command and
  idempotency identity, exact request digest, atomic receipt/effect/audit
  membership, confirmer-grade semantics, default denial and fresh authoritative
  readback;
- events remain post-commit evidence hints, not commands or source truth; and
- no YAML capability or runtime manifest gains executable authority.

No REST/OpenAPI, GraphQL, async contract, route, schema, migration, feature
flag, authored-synthetic allowlist, first-party client or product configuration
may change. Dedicated check-in remains default-off. Generic status does not gain
`Arrived`, action grammar and waiting-area movement remain separate, and the
rehearsal writes no product record or ordinary-practice admission.

## Fixed containment, data and cleanup boundary

The accepted containment remains exact: cached `postgres:16-bookworm`, pull
policy `never`, one internal captured network, no published port, host relay,
Docker-exec byte bridge, external network, bind, volume, multiprocessing process
or queue. Credentials remain random controller-memory values delivered only
through post-inspection attached stdin. Docker configuration and retained
evidence must contain no credential or nonce beyond the exact ownership label.

The three admin-owned non-product relations retain enabled and forced RLS. The
ephemeral login is non-owner and `NOBYPASSRLS`, receives only the exact probe
grants, and is removed before teardown. Exact terminal OCI state plus the prior
observer predicate—not attachment output/status—classifies the incomplete
response. Restricted authoritative readback alone can prove committed exactly
once. All partial, duplicate, mismatched or cross-practice shapes deny.

One controller is the one cleanup owner for every acquisition and cleanup.
Before removal it reinspects
captured identity, name, image, labels, nonce, network and lifecycle. Pass or
failure requires role, attachments, sidecars, server, network and matching
labelled resources absent. Cleanup uncertainty is terminal failure and never a
reason to retry.

## Deterministic and preexecution admission

Before any Docker object, credential, PostgreSQL process, SQL or database
execution, one committed candidate must pass all of the following:

1. this plan, threat delta, fresh five-source receipt and explicit DeepSeek,
   Gemini and native-subagent dispositions validate;
2. the attempt-004 adapter, schema and pure tests exist, and the repaired base
   harness digest plus every immutable artifact hash and full-Git ancestry
   binding match exactly;
3. the base static gate rejects all 366 contract, 96 manifest, 96 OCI-state and
   24 classifier mutations with zero escapes;
4. source inspection proves both real call sites pass `network_name`, the
   pre-registry cleanup interval is owned, and no forbidden host relay,
   listener, port publication, Docker exec, process or queue path exists;
5. focused plan, adapter, repaired-harness, API Spine, no-database interlock,
   latch, Baton and clockwork tests pass through the correct admitted runner;
6. Ruff, compilation, JSON/schema validation and `git diff --check` pass;
7. read-only Docker/version/image inspection proves the exact cached image and
   zero pre-existing attempt-004 labelled resources without creating an object;
8. a fresh preexecution five-source receipt binds the exact clean candidate and
   repeats the parallelism assessment; and
9. a clockwork checkpoint `--check`, followed only on success by a separate
   `--publish`, advances the latch to exactly one execution.

Then exactly one command may run:

`.venv/Scripts/python.exe -m scripts.raisa_provider_free_check_in_relay_free_recovery_attempt_004 --execute`

No automatic or manual retry is permitted. The invocation count is one even if
the command exits nonzero, output is absent, or cleanup requires bounded
ownership-checked recovery.

## Acceptance and independent veto

Pass requires the closed attempt-004 envelope, all 12 accepted relay-free
scenarios, explicit rollback zero effect, committed-exactly-once authoritative
readback, no success from the incomplete response, retry count zero, other-
practice visibility zero, ordinary/product effect zero, schema-valid sanitized
evidence and exact cleanup with zero residue.

Only after occupied success and a deterministic clean candidate may one fresh
Gemini 3.7 Flash/high read-only veto run in an isolated unchanged worktree. A
veto or any occupied failure closes attempt 004 with immutable evidence and no
retry.

## Explicit parallelism assessment

- **DeepSeek:** declined. Its native Harness boot prerequisite now passes, but
  this provider-free database proof has no model role. A first occupied worker
  requires a separate v2 WorkOrder, exact no-database admission and one-run
  latch; Claude Code is no fallback.
- **Gemini:** reserved for one fresh post-success exact-candidate read-only veto;
  no verifier call occurs during planning, implementation or preexecution.
- **Native subagents:** declined under current developer policy and because the
  adapter, one mutable database lifecycle and cleanup lease are inseparable.

No worker receives plan, execution, cleanup, acceptance, Git or protected-ref
authority. Reassess all lanes at preexecution, occupied result, pre-verifier and
closeout.

## Protected and continuation boundaries

No product, patient, appointment, clinical, historical or protected data; live
provider; DeepSeek model call; ordinary-practice enablement; feature-flag or
allowlist change; product/API/OpenAPI/GraphQL/config/client/waiting-area change;
production runtime; deployment; release; Pages; protected evidence access; or
protected-ref movement is authorised. Local/origin `master` and
`handoff/current` remain exact
`2e34bdad732fdab32fbf778280b3d3c70d66d602`.

At closeout the clockwork is the sole writer of canonical governance surfaces.
Sol writes the paired lay/technical Yuri summary, sends the usual non-PHI
Pushover notification, stages only explicit paths, and preserves
`docs/branding/` plus every unrelated untracked file. `git add .` and
`git add -A` are forbidden.

After accepted closeout, proceed to the narrowest dependency-satisfied
successor. Pause only for a truly extraordinary, genuinely non-inferable or
safety-critical fork.
