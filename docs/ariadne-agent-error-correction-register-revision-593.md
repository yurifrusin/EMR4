# Ariadne agent error and correction register — revision 593

<!-- ariadne-agent-error-register-reading
revision: 593
incident_count: 808
new_incident_ids: AER-0805,AER-0806,AER-0807,AER-0808
open_incident_count: 0
-->

This revision note binds four corrected workflow observations to the
prospective clockwork-projected register. The canonical JSON register and
pattern report remain clockwork-owned.

## AER-0805

The first preplanning runtime-state prose repeated the current task and
protected commit IDs even though the Ariadne receipt requires machine-only Git
readings. Preflight returned `revision_required` with two manual objects before
planning or execution. The rejected receipt is preserved; the corrected receipt
contains zero caller-supplied Git IDs and passes.

## AER-0806

The first staged commit command separated `git diff --cached --check` and
`git commit` with semicolons, so two blank EOF warnings did not stop the commit.
Both lines and the bound engine hash were corrected in the immediate descendant
before preexecution or Node launch. Every later compound gate uses explicit
fail-fast exit checks.

## AER-0807

A widened compatibility packet included the already-known immutable
pre-source-repair direct-check selector. It failed only because its historical
contract intentionally binds the old repaired-profile controller digest. The
original controller remains byte-identical; the fresh inert-task engine is
disjoint, and the final applicability packet names the one exclusion.

## AER-0808

The first retained-report assertion assumed two semantic phrases were
contiguous across a deliberate Markdown line break. Postexecution verification
failed closed without changing the terminal or consuming a process. The test
now asserts the two claim-boundary clauses independently; the final 25-test
packet passes.
