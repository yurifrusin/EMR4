# DeepSeek native Harness provider-free effective-tool composition native-boot proof plan

Date: 2026-08-20

Timestamp: 2026-08-20T05:22:33.9249587+10:00 (Australia/Brisbane)

Status: `frozen`

Planning source HEAD:
`2678b43abec8f29e7af432a0a3c64759fada8fda`

Accepted deterministic guard source:
`dc167c20f5b54b783a57fccb7843f434136c8ca8`

Accepted stock-headless/HMR boot source:
`5ff79d68f6df25d8bebdba78a6d504afb64de2ab`

Operation:
`deepseek-native-harness-provider-free-effective-tool-composition-native-boot-proof`

Reasoning level: Extra High freezes the one-process, network-denial, terminal
and cleanup boundary. High is sufficient for its deterministic implementation,
the single admitted execution, exact review and clockwork closeout while this
plan remains unchanged.

## Objective and authority

Yuri's standing uninterrupted-development authority admits exactly one offline,
network-denied provider-free rc.7 native Harness boot. It must reuse the
accepted stock-headless/HMR lifecycle, reach the accepted effective-tool guard,
observe exactly `edit`, `glob` and `read`, emit exactly one sanitized terminal,
exit cleanly and leave no owned process or disposable root.

The proof creates no agent, session, turn, broker request, model request,
provider call or occupied worker. The public rc.7 `createScope()` seam is
key-agnostic, and `AgentPresets.mount()` may attach that disposable scoped
context to a standing preset composition without starting an agent or session.
That is the only admitted route to the guard.

## Exact owned surface

The tranche owns only:

- this plan and its threat-model delta;
- `scripts/deepseek_native_harness_provider_free_effective_tool_composition_native_boot_proof.py`;
- `tests/test_deepseek_native_harness_provider_free_effective_tool_composition_native_boot_proof.py`;
- the closed contract, schemas, one-run evidence and report under
  `orchestration/continuity/deepseek-native-harness-provider-free-effective-tool-composition-native-boot-proof/`;
- normal Ariadne receipt, verifier, closeout, acceptance and Yuri-summary
  artifacts for this operation.

The controller may import the already accepted lifecycle primitives from
`scripts/deepseek_native_harness_provider_free_hmr_boot_proof.py` and the exact
generated helper from
`scripts/deepseek_native_harness_provider_free_effective_tool_composition_guard.py`.
It may not change either predecessor or any EMR4 product source.

## Frozen prelaunch construction

Before native launch the controller must:

1. require exact full Git planning and accepted predecessor object ids;
2. bind the exact accepted predecessor script bytes, guard helper SHA-256
   `6678ed31bdcd30a5018689b72ad509c182854bf5d63862f59b397acc8de40894`
   and the four exact rc.7 package identities from the guard contract;
3. verify the existing local npm cache only, with online fallback, lifecycle
   scripts, audit and funding requests disabled;
4. create one collision-free controller-owned disposable root outside the
   repository;
5. materialize `@deepseek-ai/dsh@0.1.0-rc.7` offline and verify its declared
   `lib/bin.js`, `--profile headless`, `--expose-internals`, HMR registration
   and two watched stock patch paths through the accepted source predicates;
6. author one home preset at
   `.agent-presets/emr4-bounded-worker/agent.cordis.yml` containing exactly:
   one `@deepseek-ai/dsh-tool-fs` row and one
   `@deepseek-ai/dsh-tool-fs-search` row with
   `sampleOverCapGlobResults: false`;
7. write the accepted generated guard as a local ESM module and bind its exact
   digest before use;
8. write an initial profile patch that disables `headless-runner`,
   `code-runtime` and `session-telemetry-otel` and contains only the HMR
   readiness sentinel; and
9. write but do not mount the changed patch, whose only additional row is the
   effective-tool proof runner.

The filesystem preset deliberately registers `read`, `write`, `edit`, `glob`
and `grep` in the standing preset layer. The disposable child scope has no own
tool registrations. The guard must therefore prove all selected names are
inherited/restrictable, apply its one restriction and observe final schemas
exactly `edit`, `glob`, `read`.

## Single native execution

The admitted execution is one package-declared native Harness process with:

- exact Node `--expose-internals` and `--profile headless` invocation;
- scrubbed credential and proxy environment;
- the accepted network guard preloaded through `NODE_OPTIONS`;
- telemetry, stock model runner and code runtime disabled;
- one in-process sentinel as the sole HMR readiness source;
- one atomic profile-patch replacement only after both stock watched patch
  paths are observed;
- no broker process, provider credential, task prompt, product data or session
  request.

The HMR-loaded proof runner must:

1. confirm the exact HMR paths and active `appExit` service;
2. emit `effective_tool_guard_started`;
3. call `createScope(ctx, key)` once with a fresh opaque in-process key;
4. call the accepted guard once on `scope.ctx`, preset
   `emr4-bounded-worker` and sorted tools `edit`, `glob`, `read`;
5. write one exclusive terminal record containing only the closed stage, code,
   safe optional detail, sorted effective tool names and count;
6. emit `effective_tool_guard_terminal`;
7. dispose the exact scope and require disposal completion;
8. emit `scope_disposed`, then `app_exit_requested`; and
9. request exit `0` only for `EFFECTIVE_TOOL_COMPOSITION_PASSED`, otherwise
   request exit `2`.

The complete successful event order is:

1. `sentinel_activated`;
2. `stock_headless_hmr_ready`;
3. `effective_tool_guard_started`;
4. `effective_tool_guard_terminal`;
5. `scope_disposed`;
6. `app_exit_requested`.

The terminal must be exactly:

- stage `pre_provider_tool_composition`;
- code `EFFECTIVE_TOOL_COMPOSITION_PASSED`;
- detail `null`;
- effective tool names `edit`, `glob`, `read` in sorted order;
- effective tool count `3`.

Any other closed guard code is valid failure evidence but not acceptance.
Unknown text, exception messages, stacks, prompts, payloads, paths, environment
values and credentials are never retained.

## One-run and cleanup latch

All construction defects found before process start may be corrected through
deterministic tests. The first native process start consumes
`native-composition-attempt-001`. There is no automatic or manual second native
boot, retry, resume or same-attempt reclassification. A non-passing terminal is
retained exactly and any further execution requires a separately frozen
successor.

The controller owns termination escalation and must wait for exact process
absence, remove only its verified disposable root and prove that root absent.
It retains bounded digests, versions, event names, terminal projection, counts,
duration, exit code and cleanup booleans. Raw stdout/stderr bodies, environment,
npm cache, package source, disposable paths and transient preset/plugin files
are not retained.

## Deterministic admission and acceptance

Before the native process can start, tests must prove:

1. exact predecessor object ids, script digests, package identities and guard
   digest;
2. exact preset, initial/changed patch and generated runner shapes;
3. one `createScope`, one accepted guard call, one exclusive terminal write,
   one scope disposal and one exit request in causal order;
4. rejection of an own-layer tool, missing inherited tool, surplus/missing/
   duplicated schema, reordered selected input, generic terminal, unsafe
   detail, early patch mutation, duplicate process, malformed events,
   ambiguous exit, network attempt and incomplete cleanup;
5. zero agent/session/turn, broker, model, provider, Docker and database paths;
6. focused provider-free tests, neighbouring guard/HMR/profile/broker/no-
   database tests, Ruff, compile, JSON/schema validation and Git whitespace.

Acceptance then requires the single native terminal to pass, exit `0`, record
zero network/agent/session/broker/model/provider counts and complete cleanup.
Only after that exact clean candidate exists may one fresh Gemini 3.7
Flash/high isolated read-only veto run. A P0-P2 finding requires bounded source
repair but can never authorize a second native execution; corrections after a
consumed passing terminal may change only deterministic source/evidence
interpretation that does not invalidate the terminal's exact bound digests.

## Explicit parallelism assessment

- **DeepSeek native Harness/model lane:** `declined` as worker and reviewer,
  negative leverage. rc.7 is the provider-free subject; a model/provider call
  or occupied worker would violate the proof boundary.
- **Gemini:** `reserved`, required independent leverage. It owns one fresh
  exact-candidate read-only veto only after deterministic and one-run evidence
  acceptance.
- **Native subagents:** `declined`, negative leverage. Current developer policy
  prohibits proactive delegation, and launch, readiness, terminal, disposal
  and cleanup are one serial ownership chain.

Sol owns all planning, implementation, native-process execution, cleanup,
acceptance, recovery, clockwork and Git. Reassess all lanes at pre-execution,
native terminal, pre-verifier and closeout.

## Claim, governance and protected boundaries

This tranche can prove only the pinned local rc.7 native composition path and
safe pre-provider terminal. It cannot prove DeepSeek reasoning, coding quality,
provider reliability, occupied-worker completion, future Harness versions,
production isolation or product suitability.

The clockwork remains the sole canonical governance writer. Its closeout intent
must describe this operation's measured terminal in
`current_position.outcome`; it must not restate a future successor target as an
already achieved Current result. Run a separate `--check` and `--publish`.

No attempt 004 retry/resume/reclassification or attempt 005; no agent session,
broker/model/provider call, credential, external network, Docker, PostgreSQL,
SQL or database execution; no product source/configuration/API/OpenAPI/GraphQL/
schema/client/waiting-area change; no ordinary-practice enablement, feature
flag, allowlist, command mount, generic-status `Arrived`, action grammar or
first-party client change; no product, patient, appointment, clinical,
historical or protected data; no production, deployment, release, Pages,
protected evidence or protected-ref movement.

Local/origin `master` and `handoff/current` remain exactly
`2e34bdad732fdab32fbf778280b3d3c70d66d602`. Preserve `docs/branding/` and
every unrelated untracked file. Stage explicit paths only; `git add .` and
`git add -A` are forbidden. At closeout write the paired Yuri summary, send the
non-PHI Pushover notification and continue under standing authority.
