# Ariadne agent error and correction register — revision 616

Date: 2026-08-22

Timestamp: 2026-08-22T14:31:55.5258598+10:00 (Australia/Brisbane)

<!-- ariadne-agent-error-register-reading
revision: 616
incident_count: 956
new_incident_ids: AER-0954,AER-0955,AER-0956
open_incident_count: 0
-->

This revision adds three bounded, corrected observations from the provider-free
tool-result/conclusion coordinate recovery. Each failed before any provider,
product or canonical-governance effect. None is open and none authorises a
retry of the consumed occupied attempt.

## AER-0954 — Git-ref evidence prose repeated a machine-owned object identity

The first post-compaction receipt repeated a Git object in its
`git_refs_and_worktree` prose. The receipt builder rejected it before action.
The corrected receipt removes object identifiers from prose and relies on the
machine snapshot. The controller additionally resolves the accepted runner's
full 40-character source object and tests that binding.

## AER-0955 — real-runtime fixture derived the Cordis peer from the wrong parent

The first real-runtime fixture derived the Cordis peer one directory above the
`@deepseek-ai` package scope and emitted `fixture_output_rejected` before Node
started. The rejected terminal records zero external activity and complete
cleanup. The corrected relation imports the exact accepted peer and the test
now executes all five variants through rc.7's real `ToolRuntime`.

## AER-0956 — contract-evidence shape was validated only during projection

The first closeout intent used path strings where Continuity requires
contract-evidence objects. Initial manifest validation admitted them and the
prospective projection rejected them before publication. This foundation node
now correctly uses no contract evidence, and the shared manifest reader plus a
regression test reject every non-object entry before projection.

## Register reading

The durable improvement is smaller than the incident prose: Git objects are
machine-resolved, the lifecycle fixture uses the actual accepted runtime, and
one late clockwork validation has moved to the first form-reading boundary.
The next occupied attempt remains a separately named, one-request experiment;
no success or general Harness-reliability claim is inferred.
