# Yuri Authority — Ariadne Sydney Vertex Bounded Work-Cell Sequence

Date: 2026-07-24
Authority owner: Yuri
Repository custodian: GPT Sol
Authority status: active, conditional and bounded

## Preservation statement

This document is the durable repository authority source for Yuri's complete
pasted authorization in the Codex task and the additional continuity
instruction that followed it. It preserves the authorization as one semantic
whole; it does not revise, relabel or reopen any historical Continuity node.
Where a later summary differs, this authority record and the originating Yuri
messages control.

## Intended endpoint

Work continuously through seven conditionally chained Ariadne tranches toward
one authored-synthetic occupied cognitive-work-cell rehearsal using an
officially supported Gemini Flash model through the Australian Google Cloud
Vertex locational endpoint, preferably the current fully qualified identifier
or stable alias for Gemini 3.5 Flash in `australia-southeast1` (Sydney).

The exact endpoint, project, identity and authentication class are:

- project: `bernie-emr4-dev`;
- target service account:
  `emr4-bernie-ai-dev@bernie-emr4-dev.iam.gserviceaccount.com`;
- location: `australia-southeast1`;
- regional base URL:
  `https://australia-southeast1-aiplatform.googleapis.com`;
- authentication:
  `keyless_impersonated_service_account_adc`; and
- required prediction permission: `aiplatform.endpoints.predict`.

The rehearsal must use authored-synthetic input only, typed request and output,
deterministic egress verification, bounded safe repair, an external audit,
complete isolation cleanup, no global inference fallback, and no product,
database, clinical or command authority.

The exact official Gemini 3.5 Flash identifier or stable alias may be resolved
without further approval. No model family, provider, project, identity or
region substitution is authorised. If Gemini 3.5 Flash is not officially
supported for the available project through an Australian locational endpoint,
the sequence stops for Yuri rather than substituting another model.

## Mandatory rehydration

Before work, Sol must:

1. read `AGENTS.md` completely;
2. read the latest attempt-004 plan, closeout, audit analysis and Sol review;
3. read the provider-blocked Gemini request-contract diagnostic;
4. read the Terra/Gemini audit policy and provider-contract profiles;
5. read the T3R5 Vertex Australian feasibility and entitlement records;
6. read the T3R7 Sydney approval, control evidence, report and closeout;
7. read `scripts/use_bernie_adc.ps1` and
   `scripts/bernie_t3r7_vertex_sydney_live_pilot.py`;
8. read the Bounded Cognitive Work Cell and Proofreader Gate plan, protocol,
   evidence and closeout;
9. restore the protected-evidence and user-decision boundaries in `AGENTS.md`
   sections 5 and 6;
10. verify `HEAD`, `master`, `handoff/current`, `origin/master`,
    `origin/handoff/current` and the worktree; and
11. create a fresh receipt naming
    `live_handover_current_baton`, `current_authority_allocation`,
    `active_plan_and_acceptance`, `protected_evidence_boundaries`, and
    `git_refs_and_worktree`.

All unrelated user changes must be preserved.

## Continuity instruction

Before Tranche 1, this complete authorization is the authority source for a new
Continuity descendant of the consumed Terra/Gemini rehearsal. The descendant,
plan and pre-plan receipt must bind the Sydney Vertex endpoint, Bernie
impersonated ADC, authored-synthetic classification, isolation and audit
boundaries, USD 1 ceiling, one-primary-plus-one-conditional-repair-retry
ceiling, absolute two-call ceiling and named stop conditions.

Historical nodes are immutable. The Continuity graph and Compass increment
together whenever durable programme state changes. Their revision binding and
the rendered Compass report must validate before an occupied provider call.

## Authentication boundary

Only the existing Bernie-style keyless Application Default Credentials
service-account impersonation path may be used. The authoritative precedents
are `scripts/use_bernie_adc.ps1` and the T3R7 Sydney pilot. No parallel
credential mechanism may be introduced.

The preflight must prove that `google.auth.default()`:

- returns impersonated credentials;
- resolves exactly `bernie-emr4-dev`;
- identifies exactly
  `emr4-bernie-ai-dev@bernie-emr4-dev.iam.gserviceaccount.com`;
- uses the `https://www.googleapis.com/auth/cloud-platform` scope;
- retains the prediction-only custom role;
- retains exactly `aiplatform.endpoints.predict`;
- retains Vertex Data Access audit logging;
- has request-response logging disabled or unconfigured; and
- requires no service-account key.

`GEMINI_API_KEY`, `GOOGLE_API_KEY` and every other provider API key must not be
read or used. Their host presence is not itself a failure, but every broker and
container child environment must omit them. Evidence records only that API-key
authentication was not used and never records presence, values, prefixes,
lengths or hashes.

`GOOGLE_APPLICATION_CREDENTIALS` must not identify a service-account JSON key.
No service-account key may be created, downloaded, mounted, copied, inspected
or persisted.

The occupied work-cell container receives no ADC file, OAuth credential,
service-account secret or Google Cloud CLI configuration. It communicates only
with the purpose-built one-use broker through the typed Ariadne protocol. Only
the broker may use `google.auth.default()` to obtain or refresh an existing
short-lived impersonated token, and only the broker may call the approved
regional Vertex endpoint.

The broker may not call `generativelanguage.googleapis.com`, a global Vertex
inference endpoint, OpenAI, Terra, DeepSeek, another provider or another
region. Non-interactive refresh of an already valid impersonated short-lived
token is permitted.

The sequence may not revoke or replace ADC, perform interactive ADC login,
change the human account, IAM, project, service account, API enablement,
billing, configured Cloud project, or create a service-account key.
`scripts/use_bernie_adc.ps1` must not run in a mode that changes ADC.

If existing impersonated ADC cannot be verified or refreshed
non-interactively, the sequence stops for Yuri. API-key, static-key,
user-credential and provider fallback are prohibited.

## Tranche 1 — residency and provider admission

Create a provider-neutral admission policy that explicitly distinguishes
authored-synthetic data, product-derived data, patient or health information,
Australian regional processing, regional storage, global endpoints, provider
training controls, provider abuse-monitoring retention, independently observed
evidence and provider-contractual residency evidence.

Use current official Google documentation to confirm model and regional
support. Record that a container constrains local capability but does not
determine the remote provider's processing geography.

The policy rejects the Gemini Developer API,
`generativelanguage.googleapis.com`, global Vertex inference endpoints,
automatic cross-region fallback, API-key authentication, OpenAI, Terra,
DeepSeek or another provider, a model without published Sydney support,
project/region/impersonated-service-account mismatch, and any physical or
sovereign locality claim stronger than the provider contract.

## Tranche 2 — provider-blocked implementation

Implement and test the local launcher, one-use broker, policy manifests,
request constructor and audit contracts without a model call. Reuse the
established Ariadne one-use-broker and isolated-work-cell architecture where
suitable.

The occupied cell may not receive ADC configuration, OAuth credentials, the
service-account identity beyond a non-secret policy identifier, provider API
keys, Google Cloud CLI configuration or unrestricted network access.

Only the minimum Google identity traffic needed to refresh the already
configured impersonated ADC and the
`australia-southeast1` Vertex data-plane endpoint may be reachable. No rehearsal
prompt or model payload may be sent to an authentication or control-plane
endpoint. Verify the exact Vertex request contract locally or through metadata
or discovery that transmits no rehearsal prompt.

No Google Cloud project, API, billing, service account, IAM or external cloud
configuration may be created or changed.

## Tranche 3 — existing ADC and entitlement preflight

Inspect only the usability of the existing Bernie ADC, exact project, regional
model entitlement and least-privilege controls. Never print, persist, hash or
log access tokens, refresh tokens, credential files, API keys, unapproved
human identifiers or raw authentication responses.

Require exact checks for project, service account, impersonated-credential
type, Sydney region, enabled Vertex AI API, prediction-only custom role and
binding, exact prediction permission, Vertex Data Access audit logging,
disabled/absent request-response logging, no service-account key, no API-key
path, no global endpoint and no automatic regional fallback.

Missing credentials, project, billing entitlement, API enablement, IAM control
or regional model entitlement require Yuri. External state must not be
repaired.

## Tranche 4 — real-isolation provider-free dry run

Run the full launcher, broker, typed exchange, audit, proofreader and cleanup
sequence provider-free. Where the established architecture supports it,
require non-root execution, a read-only container filesystem, bounded
temporary storage, no host networking, no Docker socket, no database or
product mount, dropped capabilities, bounded CPU/memory/process/runtime,
explicit endpoint allowlisting, no work-cell credential, consumed single-use
ledger, deterministic teardown and independent residue checks.

The dry run must prove that the work cell can communicate only with the broker
and that the broker fails closed for every unapproved provider, identity,
project, model, hostname or region.

## Tranche 5 — occupied Sydney Vertex rehearsal

Only after every prior gate passes, make exactly one primary occupied call with
one small authored-synthetic request. Use only the existing keyless
impersonated Bernie ADC, exact service account and project, an officially
supported Gemini 3.5 Flash identifier or stable alias,
`australia-southeast1`, the exact regional base URL, no global fallback, no API
key and no provider-executed tools.

Use one typed response schema, deterministic proofreader, low output-token
ceiling, no tools/function calling/grounding/retrieval/cache/database/product
data, no command-shaped release, no human or product delivery, no provider or
regional fallback, an application cost ceiling of USD 1, and an absolute
occupied-call ceiling of two.

One repaired retry is conditionally authorised only when the primary failure
is solely a deterministic request-contract defect repairable without changing
model family, provider, project, region, service account, IAM, credential
mechanism, data class, isolation, output contract, cost ceiling or residency
assurance.

Before that retry, close and independently audit the first ledger, diagnose
under the bounded error contract, add a focused regression, rerun every
repository-only and provider-blocked gate, create a distinct ledger, and
reverify the exact identity, model family, project, endpoint and controls. No
other retry is authorised.

## Bounded provider-error contract

An error record may retain only HTTP status, provider error code and normalized
status, allowlisted field-violation paths, a sanitized diagnostic message no
longer than 2 KB after credential/prompt/sensitive scanning, and a hash of the
discarded raw error.

It may not retain raw prompts, raw provider responses, credentials, access
tokens, hidden reasoning, chain-of-thought, unconstrained provider messages or
rejected draft payloads.

## Tranche 6 — proofreader and release gate

The proofreader verifies the exact schema, allowed paths and types,
authored-synthetic grounding, absence of ungrounded additions, policy,
command-authority exclusion, safe-repair eligibility, freshness and
supersession, retry count, model/project/region binding, human-gate or
edge-abortion disposition, and atomic release of admitted fields only.

Safe repair is restricted to authorised mechanical normalization such as
whitespace, canonical enum casing or deterministic ordering. It cannot invent
evidence, reinterpret an unsafe answer or repair clinical meaning.

A failed proofreader must follow the frozen bounded-retry, human-gate or
edge-abortion rule and never release an unverified draft.

## Tranche 7 — external audit and closeout

The external audit records typed “API typewriter keys,” never raw model
reasoning. It includes attempt and ledger identifiers; provider/model; project;
impersonated service account; authentication class; confirmation that API-key
authentication was not used; region and endpoint hostname; request/schema
hashes; event sequence and hash chain; status, latency and supplied token use;
admitted output manifest; proofreader findings and safe repairs; released
authored-synthetic values if admitted; retry/freshness/supersession decisions;
cleanup and residue evidence; Vertex audit-control posture; and exclusions.

It excludes credentials, tokens, API-key information, raw prompts, raw
responses and chain-of-thought.

Produce tranche plans and acceptance contracts, implementation and focused
tests, dry-run and occupied evidence, independent external audit analysis, Sol
review, closeout, rehydration and pre-acceptance receipts, Continuity and
Compass updates, `AGENTS.md` and orchestration-ledger updates, and a candid
statement of what the evidence proves and does not prove.

## Verification and Git boundary

Run the repository-only Ariadne verifier, focused rehearsal tests, relevant API
Spine tests, Compass validation, JSON/schema validation, Python compilation and
static checks, applicable JavaScript syntax checks, `git diff --check`, and
independent container/network/image/credential/process residue checks.

Do not commit, push, open a pull request, move protected refs or dispatch
external workers.

## Automatic continuation and stop conditions

Routine tranche boundaries do not require confirmation. Sol may repair ordinary
repository-local defects and deterministic request-constructor defects within
the frozen contract.

Stop and request Yuri only if:

- existing Bernie impersonated ADC is missing, unusable or requires interactive
  recreation, revocation or replacement;
- an API, billing, IAM, project, human account or service-account state must
  change;
- the prediction-only role is missing or broader than approved;
- Vertex Data Access audit logging is unavailable;
- request-response logging cannot be confirmed disabled or absent;
- Gemini 3.5 Flash is unavailable through `australia-southeast1`;
- another model, provider, project, identity, credential class or region is
  required;
- a global endpoint or regional fallback would be used;
- API-key or static-key authentication is required;
- real, product-derived, patient, protected or historical data is required;
- the USD 1 or two-call ceiling may be exceeded;
- a credential may have been exposed;
- the isolation or external-audit boundary cannot be maintained;
- unexpected external charges are possible; or
- a necessary action exceeds this authority.

Whether an occupied attempt passes or fails, consume every opened single-use
ledger, perform no unauthorised retry or fallback, clean all containers,
networks, images and temporary credentials, preserve immutable evidence,
document unresolved gates, update the handover accurately and report candidly.

No result may claim Australian physical or sovereign processing beyond the
configured `australia-southeast1` locational endpoint, the observed request
path and Google's published contractual documentation.
