# Provider-free unmounted status-confirm route-convergence composition rehearsal closeout

Date: 2026-08-12

Source: `41f978ae9837cba50737cfb5f457ab62ac28dbdb`

Result: `raisa_provider_free_unmounted_status_confirm_route_convergence_composition_rehearsal_pass`

## Accepted result

The status-confirm lineage now has one real but unmounted composition callable.
It joins the accepted pure status-only adapter, server-owned authority/session
ingress, request and session binding, the exact physical transaction factory,
locked-state readmission, one injected status effect and closed outcome
mapping. No router imports it.

Twelve authored-synthetic in-memory scenarios pass for clean execution,
byte-identical replay, conflict, incomplete-scaffold rollback, authority
revocation, missing target, waiting-area discrimination, changed locked
generation, warning mismatch, terminal deferral, receipt corruption and
response-loss recovery. All 65 hostile input and response mutations fail
closed.

## Public-response reconciliation

Exact source inspection found a real pre-composition contradiction. The current
route/API contract returns the complete `AppointmentConfirmStatusProposalOut`
envelope, while the unmounted physical design had called a five-field status
projection the complete public response. Rebuilding the larger envelope on
replay from later appointment state would violate stored-result replay.

The composition resolves that contradiction before receipt v1 is ever mounted:
canonical receipt bytes contain the complete current envelope already stored by
the legacy idempotency service. The five status fields remain a strictly
validated projection over those bytes. Stored JSON, canonical bytes, SHA-256,
locked appointment identity/practice/status/reason/waiting area, warning codes
and audit identity must agree or the transaction releases no success body.

No model, migration, database constraint, route or API Spine schema changed.

## Verification

- 13/13 focused composition tests pass;
- 12/12 frozen scenarios and 65/65 hostile mutations pass;
- 163/163 descendant-compatible status-confirm lineage tests pass;
- the canonical fast profile passes 191/191 tests, Ruff, compilation of 207
  maintained Python sources, Diary JavaScript syntax and Git whitespace; and
- the preplanning, source pre-commit and closeout pre-commit five-source
  Ariadne receipts pass.

An exploratory sweep that deliberately included archival tranche-position and
pre-physical-state tests returned their expected obsolete assertions, plus a
standalone review fixture outside the repository test root. Those historical
tests are not current acceptance gates; the descendant-compatible lineage above
is green.

## Boundary and next work

This proves composition control flow with injected authored-synthetic doubles.
It does not prove that the mounted handler uses the callable, that product
locked-state/effect adapters exist, or that the callable executes the physical
PostgreSQL seam. It adds no runtime or command authority.

The next dependency-satisfied tranche is a provider-free read-only
status-confirm route-mounting readiness re-review against exact source
`41f978ae9837cba50737cfb5f457ab62ac28dbdb`. It should classify which of the
seven prior blockers are now satisfied and name only the remaining narrowest
prerequisites. It may not edit, mount or call the route or database.

No product/patient data, provider, credential/IAM, browser/network, deployment,
production, release, Pages or protected-ref authority opened.
