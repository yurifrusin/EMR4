# Provider-free read-only post-check-in admission-control programme orientation plan

Date: 2026-08-22

Timestamp: 2026-08-22T22:38:25.4278425+10:00 (Australia/Brisbane)

Status: `frozen_for_read_only_execution`

Operation:
`raisa-provider-free-read-only-post-check-in-admission-control-programme-orientation`

Task baseline: `ea05e120d0974542a5a56b2ffde5df0a9494f217`

Target result:
`raisa_provider_free_read_only_post_check_in_admission_control_programme_orientation_pass`

Reasoning level: Extra High freezes the current gap meanings and selects the
next product-facing tranche. High is sufficient for the bounded read-only
evidence, deterministic validation and closeout while this plan remains exact.

## Objective

Reconcile the accepted default-off canonical check-in route, the twelve-part
ordinary-admission readiness verdict, the unmounted admission-control
architecture and kernel, the later environment/role/recovery evidence and the
closed native-Harness sequence. Identify which original gaps are now satisfied,
which remain unproved, and which exact artifact is genuinely absent. Select one
narrowest dependency-satisfied successor without enabling check-in or changing
product source.

This tranche is an orientation, not another recovery attempt. It must use the
accepted Continuity graph as the membership register and reject a successor
whose operation ID is already recorded.

## API Spine boundary classification

- Existing check-in is a typed REST/OpenAPI proposal/confirmation command, not
  a GraphQL mutation.
- The route remains default-off and authored-synthetic-practice-only.
- The admission-control evaluator, record transitions and kill switch remain
  pure and unmounted.
- A rollout/kill-switch/rollback runbook is declarative manifest policy. It
  cannot grant command authority; runtime code must enforce it.
- Async events remain observational acceleration hints, not admission,
  mutation or recovery authority.

The accepted command continues to require a current human actor, server-owned
practice scope, Receptionist confirmation, correlation and idempotency,
freshness/revalidation, append-only audit, a patient-free committed event and
a typed receipt. This orientation changes none of those fields.

## Exact eligible evidence

Read only the following non-protected sources:

1. `AGENTS.md`, the current active-operation latch, Continuity graph and
   Compass;
2. the accepted route-convergence plan/closeout/Sol acceptance;
3. the accepted ordinary-practice readiness plan/closeout/Sol acceptance;
4. the accepted admission-control architecture and unmounted-kernel
   plan/closeout/Sol acceptance;
5. the accepted environment-manifest/secret-posture architecture and disposable
   runtime-role/tenant-isolation closeouts and acceptances;
6. the blocked rollback/unknown-response closeout, accepted relay-free transport
   redesign and final blocked database attempt;
7. the accepted final native-Harness plan, typed terminal, efficacy reading,
   closeout and Sol acceptance;
8. `orchestration/api_spine_adr.md`,
   `orchestration/api_spine_programme.md`,
   `docs/api-spine/openapi/appointment-commands.yaml` and
   `tests/test_api_spine_artifacts.py`;
9. `orchestration_harness/check_in_admission_control.py` and
   `orchestration_harness/check_in_rollout_runbook.py`; and
10. the exact API-Spine manifest directory and focused public tests needed to
    prove whether the runbook target exists.

No protected holdout, historical Diary/PHI, live route, application import,
database, Docker, browser, provider, network or external state is eligible.

## Required reconciliation matrix

The evidence must classify each dimension with one closed value:

- `satisfied_accepted` — accepted design or operational evidence now closes the
  original prerequisite without opening admission;
- `satisfied_contract_only` — the exact fail-closed contract exists, but its
  product-facing declarative artifact or runtime remains absent;
- `operational_evidence_gap` — accepted design exists but the required occupied
  proof did not pass; or
- `closed_later_gate` — deliberately separate activation/client/runtime work.

At minimum record:

1. accepted route and API command identity;
2. ordinary-admission architecture and unmounted evaluator;
3. ordinary rollout, kill-switch and rollback runbook;
4. non-PHI metrics and alerts;
5. environment and secret-reference posture;
6. runtime role and tenant isolation;
7. rollback and unknown-commit recovery;
8. native-Harness worker allocation;
9. ordinary-practice activation; and
10. atomic two-client cutover and waiting-area separation.

## Successor selection rule

Select exactly one successor only when:

1. its operation ID is absent from the accepted Continuity graph;
2. it completes a named original readiness gap rather than repeating an
   accepted route, architecture, kernel, environment or role tranche;
3. its inputs already exist as accepted, closed-form, provider-free evidence;
4. it avoids the exhausted unknown-response execution chain and the closed
   native-Harness worker sequence;
5. it remains smaller and safer than mounting admission-control commands,
   enabling an ordinary record, changing the feature gate or cutting over a
   client; and
6. it adds no product runtime authority.

The provisional candidate is
`raisa-provider-free-default-off-canonical-check-in-rollout-kill-switch-rollback-runbook-convergence-rehearsal`.
It is selected only if deterministic evidence proves that
`orchestration_harness/check_in_rollout_runbook.py` already freezes one exact
closed-form candidate, that the named API-Spine manifest is absent, and that no
accepted graph node already records this operation.

The successor may add only the exact default-off declarative API-Spine manifest
and focused deterministic validation around the existing closed-form contract.
It may not edit `app/**`, mount a command, activate a practice, clear the kill
switch, expand the synthetic allowlist, call a route or use a database.

## Exact owned outputs

GPT Sol may create or update only:

- this plan and its threat-model delta;
- one closed evidence contract/schema and derived evidence/report under the
  tranche Continuity directory;
- one provider-free deterministic orientation reader and focused tests;
- the human-readable orientation report;
- required Ariadne receipts and qualifying closed incident-register evidence;
  and
- closeout, Sol acceptance, paired Yuri summary and transactional clockwork
  inputs/outputs if the tranche passes.

No application, configuration, OpenAPI/GraphQL, manifest, migration, database,
client, product test, Harness runner/broker or protected source is editable in
this orientation.

## Deterministic acceptance

Pass requires:

1. the five-source receipt names all five authority sources and passes with zero
   caller-supplied Git identities;
2. the accepted graph contains each claimed completed node and not the selected
   successor;
3. the check-in OpenAPI proposal and confirmation operation IDs remain exact;
4. the readiness matrix and all later accepted/blocked outcomes reconcile
   without turning architecture into operational proof;
5. the current API-Spine runbook target is proved absent while the exact
   closed-form validator and target path are present;
6. exactly one new successor is selected and its boundary is frozen;
7. no product or protected path changes;
8. focused orientation, API Spine, latch, Baton, Continuity, Compass and graph
   checks, Ruff, compilation and `git diff --check` pass; and
9. local/origin protected refs remain exact while `docs/branding/` and every
   unrelated untracked file remain preserved.

Gemini is reassessed after the matrix is frozen. A fresh independent veto is
required only if deterministic evidence leaves a material successor ambiguity;
otherwise the read-only graph/non-membership and exact-artifact evidence is the
stronger control.

## Parallelism-efficacy assessment

- DeepSeek V4 Flash/high: `declined`, negative leverage. The native Harness is
  unavailable, Claude Code is historical only, and this authoritative-lineage
  reconciliation has no implementation package.
- Gemini 3.7 Flash/high: `reserved`, neutral leverage before the matrix exists.
  Reassess after deterministic successor selection; no candidate is dispatched
  merely for ceremony.
- Native subagents: `declined`, negative leverage. Developer policy prohibits
  proactive delegation and one graph, one latch and one successor require
  serial custody.
- GPT Sol owns the read-only matrix, API-Spine classification, successor
  selection, deterministic admission, acceptance, Git and closeout.

Parallel work packages: none. Reassess after plan freeze, after matrix freeze,
at any conflicting evidence, before verifier admission and at closeout.

## Stop and claim boundary

If the runbook manifest already exists, the candidate operation already appears
in the graph, or the closed-form validator does not bind the exact absent target,
reject the provisional successor and select no implementation until a narrower
read-only factual gap is frozen.

Passing proves only a current repository-static programme map and one
non-circular successor. It does not prove ordinary-practice readiness, live
secret custody, rollback/unknown-commit recovery, operational monitoring,
activation, client cutover, product runtime or production suitability.

No ordinary-practice enablement, feature-flag or allowlist change, product
source/configuration/API/route/client change, generic-status `Arrived` change,
action grammar, waiting-area movement, product/patient/appointment/clinical/
historical/protected data, DeepSeek/native-Harness work, provider/network,
database/Docker, production runtime, deployment, release, Pages or protected-ref
movement is authorised. Preserve `docs/branding/` and every unrelated untracked
file. Use explicit-path staging only, never `git add .` or `git add -A`.
