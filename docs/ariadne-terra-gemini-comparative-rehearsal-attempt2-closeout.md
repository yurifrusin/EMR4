# Ariadne Terra/Gemini Comparative Rehearsal — Attempt 002 Closeout

Date: 2026-07-24
Owner: GPT Sol High
Result:
`ariadne_terra_gemini_comparative_rehearsal_attempt2_revision_required`

## Outcome

The fresh, corrected attempt ran once through both isolated lanes. The earlier
Windows LF/CRLF sealing defect did not recur: the shared task, full output
schema, provider schema, system prompt and task prompt hashes matched exactly.

Each broker then made its one authorised provider request:

- Terra called `POST /v1/responses` with `gpt-5.6-terra` and received HTTP 500.
- After complete Terra teardown, Gemini called
  `POST /v1beta/models/gemini-3.5-flash:generateContent` and received HTTP 400.

Both one-use process ledgers are consumed. No retry, fallback, provider
substitution or third call occurred or remains authorised.

## What the result proves

- Both credential gates, the byte-stable build-context regression, focused
  tests and real-isolation preflight passed before either process was consumed.
- The model-independent work cell generated byte-identical sealed inputs for
  both lanes.
- The provider key remained in the broker and absent from the work cell.
- Each broker issued exactly one request to its frozen provider host, path and
  model.
- Terra cleanup completed before Gemini started.
- Both lane cells, brokers, private networks and lane image tags were removed.
- No prompt, provider response body, draft payload or secret was recorded.

This does not prove Terra or Gemini draft quality, full-schema compliance,
proofreader handling, provider suitability, clinical correctness, product
behaviour or autonomous safety.

## Provider outcomes

### Terra

The sanitised broker record contains:

- request bytes: 18,259;
- request SHA-256:
  `sha256:35394237346af2c8fc8c2446cca9a060ec69ddd241ffe3a25b2db9ec85538ff8`;
- provider status: 500;
- response bytes: 369; and
- response SHA-256:
  `sha256:70da52ae18cc091b1db30c1c9d3a467f7d3289ae227f6404a9aed98c3581b75a`.

The current OpenAI model catalogue identifies `gpt-5.6-terra` as a Responses
API model, and the frozen envelope uses documented Responses request fields.
The retained evidence does not contain the provider error body or request ID,
so it cannot distinguish a transient provider failure from another server-side
cause. HTTP 500 is recorded without a model-quality inference.

### Gemini

The sanitised broker record contains:

- request bytes: 18,287;
- request SHA-256:
  `sha256:c1fd25839ef4cbf9dc1c8453bc48634ff983283a49ce997f71503488185c7d70`;
- provider status: 400;
- response bytes: 127; and
- response SHA-256:
  `sha256:b01105ed229707571186fdc753a734401dcd3dca044538c55285824604725bd3`.

Google's current model and GenerateContent references identify
`gemini-3.5-flash`, structured output, thinking, `generationConfig` and
request-level storage policy as supported. The structured-output guidance also
warns that a large or deeply nested schema may be rejected. Because the error
body was deliberately not retained, this attempt cannot prove whether schema
complexity, a field combination or another request-validation rule caused the
400 response.

## Evidence-design finding

The raw-content exclusion worked, but error sanitisation was too coarse for a
decisive protocol diagnosis. A future separately authorised diagnostic should
retain only an allowlisted provider error type/code/parameter and provider
request identifier, where present, while continuing to discard messages,
headers, prompt content and response bodies.

That is a candidate design amendment, not authority for another provider call.

## Schema and proofreader

Neither provider returned a draft envelope:

- generated draft count: 0 per lane;
- provider-schema status: `not-presented`;
- full-schema status: `not-presented`;
- proofreader: not invoked; and
- downstream and human-gate delivery: false.

The deterministic proofreader therefore neither passed nor failed model
content.

## Isolation and cleanup

Both work cells ran non-root, read-only, capability-free and with
`no-new-privileges`, no published port, no repository mount and only their
lane-private internal network. Provider credentials were read from temporary
broker-only secret mounts and were not printed, hashed or persisted.

Final read-only inspection found:

- comparison containers: 0;
- comparison networks: 0; and
- comparison image tags: 0.

No PostgreSQL, GraphQL, REST/OpenAPI product command, FastAPI, event feed,
mailbox, human-gate action, PII, protected holdout or historical Diary material
was connected.

## Provenance

- runtime evidence:
  `orchestration/continuity/ariadne-terra-gemini-comparison/attempt-002-comparison-evidence.json`;
- runtime evidence SHA-256:
  `sha256:d52511061e61539979e22c4bbc8350c70461c9fa288403af76f5da356bfbce33`;
- executed host runner SHA-256:
  `sha256:63515f5e3688d14f665691428937faf42ad9225e3cc5ae2ae58adec1b15875cc`;
- executed broker SHA-256:
  `sha256:6716a034dc9765fe5f128680d0f0c9c2f3ee3bd89aeeca00f81de948a34a9543`;
- executed work-cell launcher SHA-256:
  `sha256:97eab934798a1236d15eb863ffeb3354a0109d921e31d781c7134f49eece10b8`.

Local cost estimates are unavailable because neither error response supplied
usage metadata. Provider billing records remain authoritative.

## Post-runtime CI annotation

The exact reviewed Bandit gate identified the two literal `/tmp` strings used
as Docker `--tmpfs` destinations. They are container-internal, size-limited,
memory-only filesystems, not host temporary paths. Two scoped `nosec B108`
annotations document that boundary. This comment-only change occurred after
the consumed attempt and changes no command, image, runtime observation,
provider request or evidence value; the executed host-runner hash above
remains the runtime provenance. The same post-runtime whitespace gate removed
only trailing presentation whitespace and the final blank line from named
documents and the Dockerfile; the executed build hashes in preflight evidence
remain the authoritative runtime inputs.

## Verification

- focused comparative population: 20 passed;
- broader serial Ariadne/API Spine/handover population: 261 passed after
  deselecting only the documented immutable DeepSeek runtime-source-hash
  assertion;
- the deselected DeepSeek assertion was reproduced separately and remains the
  pre-existing mismatch between historical evidence and three already-tracked
  source files; this tranche did not alter that evidence;
- Continuity graph revision 25 and Compass revision 13 pass Draft 2020-12 and
  semantic validation;
- the generated Compass Markdown matches the deterministic renderer;
- Ruff, Python compilation, Node syntax, exact reviewed Bandit, JSON parse and
  whitespace gates pass; and
- final read-only Docker residue inspection remains zero.

## Next decision

The comparative direction is paused. The smallest defensible next candidate is
a provider-contract diagnostic design that adds allowlisted, non-content error
metadata and locally validates both request envelopes against current
provider contracts. Any occupied process, provider request or retry requires
fresh Yuri authority.

No new container, image build, credential mount, prompt transmission, model
call, product surface or command authority is granted by this closeout.
