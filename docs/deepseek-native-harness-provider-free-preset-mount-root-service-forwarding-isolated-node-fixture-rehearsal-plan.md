# DeepSeek native Harness root-service-forwarding isolated Node-fixture rehearsal plan

Date: 2026-08-22

Timestamp: 2026-08-22T09:38:51.9994497+10:00 (Australia/Brisbane)

Status: **frozen before implementation or process execution**

Reasoning level: **Extra High** for the first JavaScript execution of the exact
prospective root-service correction.

## Objective

Materialize the exact accepted derived preset-mount bridge and its accepted
safe-subcoordinate sanitizer only inside one disposable authored-synthetic
fixture root. Run exactly one isolated Node process containing three cases:
successful bound mounting, missing root service and missing mount handle.

Admit only the exact typed case vector or fail closed. The native Harness,
runner, guard, installed package seed, DeepSeek worker, model and provider must
not load or start.

## Accepted basis

The accepted predecessor at Continuity 387 / Compass 369 proves a prospective
source correction only. Its immutable v2 evidence binds the derived bridge
SHA-256 as
`3a49b28174eeefd77d7efe0a00498901ac6636b637ed9dfe60aba46980df1d0b`
and the accepted sanitizer SHA-256 as
`12552925a600dc951afc30b9a738746499c7e2f4cefc9962bc05fb06780f158f`.
It also proves that service and mount validation occur inside the bridge's
sanitizing `try`, but it deliberately executes no JavaScript.

The consumed native attempt and every earlier Node attempt remain immutable and
grant no retry.

## Frozen materialization boundary

The controller may read only:

- the accepted process-free correction contract, v2 evidence and controller;
- the accepted source-reconciliation inventory reached through that
  controller;
- this plan, its threat-model delta and the tranche-owned schemas/contract; and
- repository Git-ref and full-object resolution helpers.

It may recompute only the derived bridge and select the exact accepted
sanitizer. It may not materialize the derived runner or guard. The disposable
root may contain exactly:

1. the byte-exact derived bridge under its source-owned module basename;
2. the byte-exact accepted sanitizer under its source-owned module basename;
   and
3. one authored-synthetic fixture module with no package imports, filesystem
   API, network API, subprocess API, environment read, prompt, response or raw
   error release.

No generated JavaScript survives cleanup.

## Exact single-process matrix

The fixture must execute these cases in order inside one Node process:

1. `success`: a synthetic service owns one `mount` method. The method must
   observe its own object as `this`, the exact synthetic context object and
   the exact preset ID, increment exactly one call and return. The bridge result
   is exactly `{ passed: true, terminal: null }`.
2. `missing_service`: `presetService` is `null`. The bridge result is
   exactly `passed: false` with terminal
   `{ stage: "preset_mount", code: "PRESET_MOUNT_UNCLASSIFIED", detail: null }`.
3. `missing_mount`: `presetService` is an object with no `mount` member.
   It produces the same exact content-free terminal.

All cases use one local synthetic `PresetMountError` class only as the
bridge's type input. No case may release an exception, message, stack, cause,
path, stream fragment or environment value.

The fixture writes exactly one canonical JSON line to stdout and zero bytes to
stderr. The outer controller admits exact schema, key order, case order and
values only.

## One-process envelope and cleanup order

Before launch, the controller must:

- resolve one absolute existing Node executable without executing a version
  probe;
- build the same five-key Windows child environment already accepted for the
  sanitizer fixture: `SystemRoot`, `WINDIR`, `ComSpec`, `TEMP` and
  `TMP`; and
- prove `PATH` and `NODE_OPTIONS` absent from the child environment.

After the one process exits, the controller must delete the entire disposable
root and prove its absence. Before parsing either stream, it must persist a
schema-valid content-free process envelope containing only the numeric exit
code, byte counts, SHA-256 stream digests, environment-key projection, process
counts and cleanup reading. Raw stdout, stderr, paths and environment values
must never be persisted.

Only after that envelope exists may the controller require exit zero, zero
stderr and parse the exact stdout object.

## Machine-only Git and push binding

The caller-authored contract contains no Git object identity and rejects any
embedded 40-character hexadecimal identity. At evidence time, the controller
asks Git for the last commit changing the frozen plan and the last commit
changing its own controller path, then passes both through the repository
resolver. It records only those full machine-resolved commits and proves the
plan is an ancestor of the implementation owner.

The post-closeout notification push exposed a recurrence in which the
orchestrator manually expanded a short displayed commit into an invalid
40-character expectation. The guard stopped before push and the corrected
command derived the expected remote parent directly with `git rev-parse
HEAD^`. This tranche will register that recurrence as AER-0920. No plan,
contract, evidence or push precondition may accept a manually expanded object
identity.

## Closed result vocabulary

- `isolated_node_fixture_pass`
- `fixture_preflight_rejected`
- `fixture_process_terminal`
- `fixture_result_rejected`

A pre-process rejection consumes no process and may be corrected within the
frozen plan. Once the single Node process starts, the attempt is consumed
whether it passes or fails. No automatic or manual retry is authorised. A
failure preserves the content-free process envelope and one typed failure
terminal, then stops for a distinct evidence-led successor.

## Acceptance

Acceptance requires:

- schema-valid contract, process envelope and evidence;
- exact accepted bridge and sanitizer byte/hash bindings;
- zero caller-authored Git object IDs and full machine-resolved plan and
  implementation-owner bindings;
- exactly one Node process and the exact three-case vector;
- one successful bound invocation with call count one;
- exact `PRESET_MOUNT_UNCLASSIFIED` terminals for both missing coordinates;
- exit zero, zero stderr and one schema-owned stdout line;
- content-free stream evidence persisted before interpretation;
- disposable-root absence after process exit;
- focused hostile tests for byte drift, extra process, case/key/order drift,
  receiver/context/preset mismatch, raw detail, environment expansion and
  cleanup failure;
- direct CLI, Ruff, `py_compile`, JSON Schema and relevant governance suites
  passing; and
- zero native-Harness, runner, guard, installed-package, worker, model,
  provider, network, database, Docker, target, retry and resume activity.

This proves the exact bridge behavior only in an isolated authored-synthetic
Node fixture.

## Parallelism assessment

- DeepSeek: **declined**. The native Harness and worker are the governed object;
  this tranche authorises one Node fixture process only and no self-review.
- Gemini: **declined provisionally**. The three-case vector, stream envelope and
  cleanup are deterministic. Reassess before acceptance only if the single
  consumed execution leaves a material semantic ambiguity.
- Native subagents: **declined**. Current developer policy prohibits
  delegation, and one process plus cleanup is inherently serial.
- GPT Sol owns planning, implementation, execution, deterministic verification
  and acceptance.

## Recovery and next coordinate

If the fixture passes, the next coordinate must be separately derived from its
evidence. The narrow expected successor is a provider-free package-unloaded
runner/guard/bridge module-graph evaluation, still without native Harness,
worker, model or provider activity. A native attempt is not authorised by this
plan.

## Explicit exclusions

No second Node process, retry, native Harness, runner or guard execution,
installed package import or mutation, DeepSeek worker, model/provider request,
target creation or use, product source/configuration/API/database/route/
adapter/flag/allowlist/grammar/client/waiting-area change, ordinary-practice
enablement, generic-status `Arrived` change, patient/product/clinical/
historical/protected data, production, deployment, release, Pages, protected
evidence or protected-ref movement is authorised.
