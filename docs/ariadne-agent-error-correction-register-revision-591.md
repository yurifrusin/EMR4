# Ariadne agent error and correction register — revision 591

<!-- ariadne-agent-error-register-reading
revision: 591
incident_count: 801
new_incident_ids: AER-0796,AER-0797,AER-0798,AER-0799,AER-0800,AER-0801
open_incident_count: 0
-->

This revision note binds six corrected or contained observations to the
clockwork-projected register. The canonical JSON register and pattern report
remain clockwork-owned.

## AER-0796

The fresh wrapper initially changed the engine's contract globals but inherited
the predecessor path captured in `load_contract`'s definition-time default.
Static tests stopped before Node. The wrapper now installs and tests a fresh
explicit loader binding.

## AER-0797

The first committed wrapper reused the predecessor engine's disposable-root
prefix despite the plan requiring a distinct namespace. A final pre-launch
contract audit stopped before the process lease. The wrapper now installs a
checked fresh-prefix proxy and exposes the prefix in deterministic admission.

## AER-0798

The first preexecution runtime-state draft repeated the structured latch source
Git object ID in prose. The orchestrator preflight rejected one manually
supplied object ID. The prose now delegates identity entirely to the machine
snapshot, and the accepted receipt records zero manual IDs.

## AER-0799

The widened closeout packet repeated the known historical-selection pattern by
including two predecessor digest selectors frozen before the accepted source
repair. Their only failures were the expected old controller digest. The exact
selectors are documented and preserved; all four applicable predecessor and 67
current/surrounding selectors pass.

## AER-0800

The generic human report boundary sentence mentioned stock-headless readiness
without distinguishing pass from failed-closed grammar. The structured terminal
was correct. The template and retained report now state explicitly that the
sentinel loaded but readiness was not reached.

## AER-0801

Two direct in-progress checkpoint commits changed the structurally valid active
latch but did not advance the clockwork generation manifest that owns that
canonical surface. The first closeout dry run therefore rejected canonical
drift before publication. The exact clockwork-generated latch was restored,
live-state validation returned drift zero, and candidate/execution identity
remains bound by the immutable consumed terminal and machine receipts. Future
in-progress detail stays in receipts until a clockwork-owned latch transition.
