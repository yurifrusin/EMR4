# DeepSeek native Harness provider-free complete-composition native-boot recovery closeout

Date: 2026-08-20

Timestamp: 2026-08-20T11:55:38.3228395+10:00 (Australia/Brisbane)

Status: `accepted_with_three_contained_procedure_incidents`

Exact reviewed candidate:
`6ef058b87a2c927efd9d9d2027b59d6ad279fec5`

The sole admitted attempt
`complete-composition-native-boot-recovery-attempt-001` passed. One pinned
offline rc.7 Harness process observed exact HMR readiness before the controller
atomically introduced the official `agent-presets` row and the custom runner.
The loader row and runner both required exactly ordered `hmr`, `agentPresets`,
`tools`. Successful runner activation therefore passed through Cordis's real
three-service dependency gate rather than a controller-authored shortcut.

The disposable home contained the accepted exact 158-byte
`emr4-bounded-worker` preset. The runner mounted it, the accepted guard reduced
the broader inherited filesystem surface, and the immutable terminal exposed
exactly sorted `edit`, `glob`, `read`. Readiness and activation ledgers were in
their exact expected order; exit was `0` after 9316 ms. Network, agent,
session, turn, broker, model, provider, occupied-worker, Docker and database
counts were all zero. The owned process and disposable root were both proved
absent. Evidence SHA-256 is
`9ba784b0726addb5644ac3786def410aed56e5bf9da3e23ec21d8e10f6ba1ea0`.

One fresh Gemini 3.7 Flash/high veto passed the unchanged clean candidate. Its
ten-command manifest passed 86 provider-free tests, Ruff, compilation,
zero-process deterministic checking and Git cleanliness. It independently
classified the disclosed ancillary mock-Node test breach as a contained P3
procedure incident rather than a P0-P2 terminal-claim blocker.

Two procedure incidents remain deliberately visible. First, Sol's widened
neighbouring suites ran three historical mock-Node driver tests once before and
once after the sole boot, contrary to the plan's no-Node-unit-test sentence.
The exact files were the effective-tool native-boot proof, HMR-boot proof and
preterminal-observable recovery-boot tests. None invoked `lib/bin.js`,
`--profile headless`, the Harness or provider code, and none touched the sole
terminal, but future execution-sensitive manifests must statically exclude any
test with a Node/subprocess launch unless explicitly admitted.

Second, Gemini's otherwise passing receipt named three different, non-spawning
test modules when describing that incident. Sol corrected the filenames from
direct source evidence and did not request a second provider review. The
receipt's essential adjudication still rests on the correct distinction between
synthetic mock drivers and the single real Harness entry, but future verifier
packets must bind exact incident filenames and the verifier must echo them
without substitution.

The first clockwork publication then exposed a third bounded closeout issue.
Lease 58 generated the correct next default-off boundary in substance but
omitted the exact legacy compatibility token forbidding ordinary-practice
enablement, feature-flag/allowlist expansion and command mounting. The retained
630-test postpublication session `47449` returned one exact failure. Lease 58
is preserved and rolled back byte-for-byte at lease 59; the corrected intent
adds the exact token and requires prospective next-latch compatibility-subset
checking before publication.

This result proves the previously missing composition seam: native preset
discovery, service-gated activation, scope/guard execution and exact effective
tools now work together in one provider-free pre-provider boot. It does not
prove an occupied DeepSeek worker, the outer broker, model reasoning, coding
quality, worker reliability, production isolation or future Harness versions.

No product, API, client, feature flag, allowlist, grammar, ordinary-practice,
waiting-area, patient or clinical data, production, deployment, release,
Pages or protected-ref boundary changed.

The next tranche returns to the authorised product track:
`raisa-provider-free-default-off-canonical-check-in-route-adapter-convergence-rehearsal`.
Its fresh plan may allocate one explicitly brokered native-Harness DeepSeek
worker package, while the product rehearsal itself remains provider-free and
default-off. It may only make unchanged A5.1 delegate to the accepted check-in
adapter under the existing feature flag, authored-synthetic allowlist, exact
contract and default denial. No ordinary-practice enablement or wider product
authority is opened.

The usual non-PHI continuing Pushover notification succeeded with request
`ec01f12b-6fec-4680-95b5-02391ce84d65`.
