# Ariadne agent error and correction register — revision 655

Date: 2026-08-24

Timestamp: 2026-08-24T03:42:32.7376775+10:00 (Australia/Brisbane)

<!-- ariadne-agent-error-register-reading
revision: 655
incident_count: 1143
new_incident_ids: AER-1143
open_incident_count: 0
-->

## AER-1143

Five contained prepublication lapses are recorded as one bounded workflow
incident rather than five expanded forms:

1. one early collect-only command used ordinary pytest instead of the
   provider-free runner; it acquired the lock and collected 46 tests without
   executing test bodies or changing state;
2. initial size and timestamp assumptions required six metadata-only selector
   corrections before the seventh bind created a manifest, with zero content
   reads beforehand;
3. one timestamp convention label reused the wrong year position and produced
   a false tie that a synthetic test rejected before another archive read;
4. one pre-verifier intent included forbidden implementation/test evidence
   roots and was rejected before receipt creation; and
5. expanding those four observations as four incident forms exceeded the
   clockwork's 100-scalar budget and was rejected before verification or
   publication.

The corrected controls are now concrete: all admitted tests use the
provider-free literal-path runner; private-data binders begin with aggregate
shape/count diagnostics; every convention label has a source-position test;
continuation intents carry governance roots while Git identity comes from the
machine snapshot; and related bounded prepublication lapses use one compact
incident backed by a typed detail artifact. The passing candidate, one content
run, privacy result and protected refs were unchanged throughout.
