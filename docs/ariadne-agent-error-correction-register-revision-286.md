# Ariadne agent error and correction register — revision 286

Date: 2026-08-15

Timestamp: 2026-08-15T13:27:22+10:00 (Australia/Brisbane)

Revision 286 records AER-0325. The register now contains 325 bounded known
incidents, all corrected or contained by an explicit control.

AER-0325 preserves a repeated protected-path filename-metadata scope breach at
the start of the delete-confirm physical representability review. Sol queried
several scoped application, migration and API directory roots; the filtered
output still surfaced two protected authoring path names. No protected file
content was opened, no protected value was persisted, and no application,
database, provider, product, credential or Git ref was changed.

The output is discarded and forbidden as review evidence. The corrected review
must begin only from exact already-known non-protected paths copied from
accepted documents. Every Git metadata or content command must name literal
files; a directory root is prohibited even when the directory looks narrow or
the output is filtered. Any source expansion must follow an exact link inside
an already allowlisted file and requires a plan revision before that file is
opened.

This is the second incident with the exact
`orchestrator.overbroad_protected_path_metadata_enumeration` recurrence
signature after AER-0292. The recurrence demonstrates that remembering a prior
control is insufficient; the new plan must mechanically bind the exact file
allowlist before any source-content review or worker dispatch.
