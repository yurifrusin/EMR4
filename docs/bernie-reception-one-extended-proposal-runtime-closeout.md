# Reception One Extended Proposal Runtime Closeout

Date: 2026-07-30

Result: `reception_one_extended_proposal_runtime_local_pass_live_vertex_unadmitted`

Continuity binding: graph revision 74; Compass map revision 61.

## Outcome

Reception One now has a default-off authored-synthetic proposal runtime for four additional receptionist families:

1. move or reschedule an existing appointment;
2. extend or shorten its duration;
3. prepare a cancellation review; and
4. assess a squeeze-in without moving an existing appointment.

The browser selects an appointment from the trusted Diary surface. Its database UUID stays inside the authenticated practice-scoped backend and becomes an opaque request handle before planning. The deterministic proofreader admits only the closed typed operators and exact grounded bindings. Move, resize and cancel then reuse the existing update/delete proposal services as non-mutating safety adapters; squeeze-in assessment remains explicitly manual and proposal-only.

No confirmation endpoint is called. No appointment, audit command, committed event or database truth is changed.

## Provider-free proof

One disposable authored-synthetic practice exercised four non-intercepted browser requests through the real Diary client, FastAPI route and PostgreSQL database. All four returned a safe proposal:

- move rendered fresh backend-owned candidate times;
- resize rendered the current and proposed duration;
- cancel rendered a cancellation review;
- squeeze-in rendered a manual-review assessment and no reservation.

The evidence records four route calls, zero provider calls and zero writes. Appointment truth and forbidden-write counts and hashes were unchanged. The disposable database, server processes and temporary artifacts were removed. A separate credential-free, non-root, read-only, network-none container rehearsal admitted the move fixture and left no container or image residue.

The relevant API Spine, appointment proposal, runtime, UI, Ariadne, Compass and provider-contract tests pass. Python compilation, focused Ruff checks, JavaScript syntax, JSON parsing and `git diff --check` pass. The fixed repository-only Ariadne verifier also passes.

## Occupied Sydney Vertex denouement

The exact read-only preflight passed:

- project `bernie-emr4-dev`;
- impersonated service account `emr4-bernie-ai-dev@bernie-emr4-dev.iam.gserviceaccount.com`;
- `keyless_impersonated_service_account_adc`;
- `australia-southeast1`;
- `australia-southeast1-aiplatform.googleapis.com`;
- `gemini-2.5-flash`;
- prediction-only role and `aiplatform.endpoints.predict`;
- Vertex Data Access audit logging;
- request/response logging disabled or absent;
- provider in-memory cache disabled;
- no service-account key and no API-key path; and
- no global or automatic regional fallback.

Attempt 001 returned HTTP 200 in 921 ms but the one-use broker rejected the provider representation at `$.goal` before candidate construction. Its ledger was consumed and independently audited. A focused request-contract repair added a literal closed mapping from receptionist intent words to exact goal tokens without changing the provider schema, local wire schema, typed output, proofreader or any security boundary. All repository-only, live-local, provider-blocked, isolation, revision and exact ADC gates then passed again.

The sole repaired retry returned HTTP 200 in 1,114 ms but failed the same strict pre-proofreader boundary at `$.goal` and `$.plan_lines[0]`. It released nothing. Its distinct ledger is consumed. The absolute occupied-call ceiling of two is exhausted, so no further retry or fallback occurred.

The conservative two-request application bound is USD 0.0208896, below the USD 1 ceiling. It is not an invoice and exact billing is not proved.

## What this proves

The evidence proves:

- a functioning default-off authored-synthetic Bureau runtime for four extended proposal families over real local browser, FastAPI and PostgreSQL plumbing;
- exact practice scoping, opaque appointment handling, deterministic typed proofreading and backend-owned proposal adapters;
- zero appointment writes and complete disposable-runtime cleanup;
- an exact keyless Bernie ADC/control preflight; and
- two fail-closed provider responses through the configured and observed Sydney Vertex locational endpoint, with complete one-use ledgers, hash-chained audits and cleanup.

## What this does not prove

It does not prove:

- a successful model-generated extended move plan;
- a live model connected to product or database context;
- safety for real, product-derived, patient, health, clinical or historical data;
- Australian physical or sovereign processing;
- exact billing, general model reliability or production suitability;
- appointment confirmation or mutation;
- voice, Word development-environment wiring, representative receptionist usability;
- production, deployment, release or public-brand authority.

## Unresolved gate and next work

The live-model planner remains closed. A future model-connected extended-family tranche requires fresh authority and a new request/wire-contract design; it must not revise these consumed ledgers or infer the discarded provider values.

Product work may continue provider-free with the deterministic engine and authored-synthetic data. Yuri's proposed online Word development-environment wiring remains a separate architectural and authority decision for a later tranche.
