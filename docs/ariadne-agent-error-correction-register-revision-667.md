# Ariadne agent error and correction register — revision 667

Date: 2026-08-24

Timestamp: 2026-08-24T17:10:39.6960019+10:00 (Australia/Brisbane)

Register revision: `667`

Incident count: `1161`

Open incidents: `0`

New incident: `AER-1161`

<!-- ariadne-agent-error-register-reading
revision: 667
incident_count: 1161
new_incident_ids: AER-1161
open_incident_count: 0
-->

## AER-1161

Six contained verification lapses occurred before acceptance. The planning
commit retained one extra terminal blank line in each new Markdown file even
though cached diff hygiene reported both. The first deterministic reviewer
then expected one line-contiguous source phrase and obsolete transaction-
attestation key names. After those fail-closed checks were corrected from the
exact accepted bytes, one overbroad combined test profile included a known
historical assertion whose generated `candidate_source` follows current HEAD
rather than its retained accepted candidate. The first long clockwork rehearsal
wrapper then printed only stdout and discarded the yielded session identifier,
leaving its owned parent process unable to return the receipt. The first
retained clockwork rehearsal subsequently rejected a missing threat timestamp
and two closeout filenames shortened from its machine-derived artifact slug.
The first closeout-precommit serial intent then overlisted the Yuri human-inbox
summary outside the preset's admitted evidence roots.

The candidate removes both terminal blank lines. The reviewer now normalises
prose whitespace and reads the exact typed `readback_counts`,
`ambiguous_response` and `authoritative_readback` fields. The final bounded
profile contains the current reviewer and its accepted temporal predecessor;
24/24 nodes pass, as do 10/10 focused nodes, Ruff, compileall, no-write replay
and Git diff hygiene. The historical failure changed no source and is retained
as baseline evidence rather than rewritten. The exact orphaned rehearsal tree
was identified by PID, parent and command line and stopped with zero canonical
write. The threat timestamp and exact machine-derived acceptance/Yuri paths
were then supplied before the rehearsal was repeated. The serial preflight
rejected the overlisted path before receipt release; the corrected intent uses
only admitted documentation, Codex-inbox, continuity and latch roots.

Durable prevention: treat a non-empty diff-hygiene result as a stop before
commit; inspect typed source shapes before encoding assertions; and exclude a
known HEAD-sensitive historical idempotence check from descendant profiles
unless its accepted-candidate semantics are the subject under review; and
always retain and poll a yielded exec session; and derive human-evidence paths
from the artifact slug before the expensive rehearsal; and copy the admitted
serial evidence-root pattern from the immediately preceding accepted intent.
No route,
database, runtime, provider, historical data, product or protected ref was
opened.
