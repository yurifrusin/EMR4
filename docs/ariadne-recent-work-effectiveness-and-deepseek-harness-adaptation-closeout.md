# Ariadne recent-work effectiveness and DeepSeek Harness adaptation closeout

Date: 2026-08-17

Timestamp: 2026-08-17T12:40:11.5438451+10:00 (Australia/Brisbane)

Status: `accepted`

Source candidate: `73bea42b37424ca3f53240d52f8e5c10120a5ce7`

Result: `ariadne_recent_work_effectiveness_and_transport_repair_pass`

## Outcome

The review confirms that recent closeout tails were materially long enough to
optimize, but does not support deleting safety checks or calling the whole tail
waste. Codex remains conductor; migration to DeepSeek Harness is rejected for
current subscription, maturity, switching-cost and protected-evidence reasons.

Five repository-only improvements are accepted:

1. machine-derived Git/ref/worktree receipt snapshots;
2. durable shell-free sequential validation receipts plus pytest admission;
3. bounded DeepSeek worker environment hardening;
4. terminal and user-attention latch states cannot dispatch workers; and
5. Antigravity full-veto runs use a bounded 45-minute deadline and atomically
   persist digest-only failure evidence on every nonzero exit.

## Evidence

- The exact historical eight-command manifest passed provider-free in 150.578
  seconds, separating command cost from the former 30-minute review deadline.
- Focused Antigravity tests pass, including raw-output exclusion, elapsed/exit
  evidence and unchanged-worktree assertions.
- The final durable deterministic envelope passed 88 orchestrator/latch tests,
  295 register/baton tests, Ruff, compilation and diff checks.
- Fresh Gemini 3.7 Flash/high receipt
  `orchestration/agent_inbox/antigravity/ariadne-effectiveness-and-deepseek-harness-review-gemini37-fresh-review-receipt.json`
  returned exactly one schema-constrained `pass`; C01-C08 all exited zero and
  the exact `73bea42b...` worktree remained clean and unchanged.
- AER-0380 and AER-0381 close at register revision 334 without erasing either
  historical failure.

## Efficiency judgment

The sampled median candidate-to-closeout tail was 38.5 minutes, and tails were
40.3% of sampled plan-to-closeout time. That is directional evidence, not a
timesheet or waste percentage. The operating target for ordinary non-occupied
tranches remains a median tail below 25 minutes after several more samples,
complexity-adjusted for database and provider work.

## Deliberately closed

No Raisa API, database, migration, runtime, UI or clinical meaning changed. No
product/patient/clinical/historical/protected data, provider product call,
credential/IAM mutation, package import from DeepSeek Harness, deployment,
production, release, Pages or protected-ref movement was opened.

## Next direction

Resume the dependency-satisfied Reception One product sequence with the
narrowest provider-free visible selected-appointment cancellation composition,
reusing the accepted selected-action console and strict delete-confirm HTTP/
PostgreSQL truth envelope. Freeze its exact projection/confirmation boundary
before implementation; raw compatibility DELETE and all real/product data,
deployment and production surfaces remain separate and closed.
