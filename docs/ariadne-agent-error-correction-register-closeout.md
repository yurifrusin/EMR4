# Ariadne agent error and correction register closeout

Date: 2026-08-03

Decision: `accepted`

Terminal result: `ariadne_agent_error_correction_register_pass`

## Accepted result

One closed Draft 2020-12 schema and deterministic validator now govern a
bounded initial register of seven preserved incidents. Six are classified as
verifier `agent_behavior`; the DeepSeek two-lane timeout is separately
classified as `transport`, not faulty reasoning. Each row binds an immutable
attempt identity, direct observation, invariant, detection, repository-relative
evidence, closed candidate state, disposition, correction and recurrence
signature. Split rows from one attempt require complete peer linkage.

The deterministic report groups only by the full origin/category/role/resource/
signature composite. It finds one current recurrence: two independent-review
attempts emitted multiple terminal decision envelopes. That is an operational
signal for the already installed exact-single-decision wrapper gate, not a
model/provider/role quality verdict. Version 1 permits only
`observation_only`; higher causal claims require a future evidence contract.

The verifier execution policy now requires qualifying rejected reviews and
worker transports without closeout to be registered before a corrected attempt
is accepted. Historical failure evidence remains immutable and corrections are
linked rather than replacing it.

## Independent review and repair

The first read-only native review found six issues: over-broad recurrence
grouping, unenforced causal policy, over-strong AER-0003 wording, case-sensitive
branding exclusion, optional split-attempt linkage and an ambiguous candidate
change boolean. Sol repaired all six with composite grouping, observation-only
v1, conservative postflight wording, case-insensitive resolved-path exclusion,
required `attempt_id` peer closure and a three-state candidate enum. The same
independent reviewer then reproduced 20 focused tests and returned one pass on
the unchanged candidate without editing it.

Root's final deterministic gate passes 56 serial Ariadne and handover tests covering the
register, verifier policy, orchestrator receipt, settings fingerprint and
Antigravity wrapper plus acceptance-index/archive integrity. Ruff, JSON/YAML
parsing and `git diff --check` pass. The
pattern report reproduces byte-for-byte, including across LF/CRLF register
checkouts, with canonical register SHA-256
`6153c7b63653081b3507c7dbbd02df1d7dd4645997f4d1812ff319162f300c02`.
The acceptance-index guard now deliberately classifies the already-active
four-pair lane rows and retains a live-baton ceiling below 75 KB / 475 lines,
still less than half the 170,970-byte pre-compaction source.

## Boundaries

This is repository-local process governance. It changes no product runtime,
API Spine, database, provider/model call, identity, patient/clinical/document
or product-derived data, cloud/IAM, deployment, production, release, protected
evidence or protected ref. Evidence paths fail closed outside the repository
and under any case variant of `docs/branding/`; the user-owned branding
directory remains untouched and excluded.

## Next work

Resume the already authorised fifth pair: the provider-free route-intercepted
Diary browser rehearsal and the architecture-only non-executing Davida
proposal-to-confirm REST boundary. Actual administrative apply/write remains a
material Yuri-owned gate.

Reasoning level: GPT Sol High for bounded governance architecture, repairs,
acceptance and task-branch integration. External agents received no acceptance
or integration authority.
