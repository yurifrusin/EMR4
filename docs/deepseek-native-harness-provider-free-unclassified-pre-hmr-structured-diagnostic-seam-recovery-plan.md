# DeepSeek native Harness provider-free unclassified pre-HMR structured diagnostic seam recovery plan

Date: 2026-08-21

Timestamp: 2026-08-21T08:07:16.9546464+10:00 (Australia/Brisbane)

## Operation

`deepseek-native-harness-provider-free-unclassified-pre-hmr-structured-diagnostic-seam-recovery`

Planning source: `ae07978123a3fd4029715f971b76e7307a4839c1`.

## Accepted starting fact

Attempt 003 is immutable failed-closed evidence. It proves one rc.7 native
process exited `1` before the first HMR event, with zero provider, runner, tool
or source-change activity. Its 7,314 stderr bytes matched no admitted signature
group and were destroyed after their count and SHA-256 were retained. This
tranche must not reconstruct, replay, reclassify or overwrite that attempt.

## Narrow objective

Freeze and deterministically prove a future-only structured diagnostic seam at
the smallest source-backed boundary that still sees a native JavaScript error
object: a generated entrypoint wrapper immediately around the pinned
`@deepseek-ai/dsh/lib/bin.js` dynamic import.

The wrapper must:

1. preserve the launcher's argument vector and pinned `--expose-internals`
   Node flag;
2. await-import the exact installed launcher;
3. on rejection, project only a closed diagnostic vocabulary into one
   exclusive sidecar inside the exact disposable attempt root;
4. never retain a raw message, stack, path, environment value, prompt,
   response, credential or arbitrary error property;
5. rethrow the identical rejected value whether diagnostic construction or
   writing succeeds or fails; and
6. permit the controller to validate and embed the safe projection in a v2
   pre-HMR terminal before exact disposable-root cleanup.

The accepted v1 byte-signature terminal remains valid and is the fallback when
no valid structured sidecar exists.

## Closed diagnostic vocabulary

The future sidecar may contain only:

- fixed schema, operation, attempt, full candidate Git object and phase;
- at most six cause-chain nodes;
- an error-kind enum: `error`, `type_error`, `aggregate_error`,
  `config_file_error`, `validation_error`, `cordis_error` or `unknown`;
- a code coordinate drawn from the frozen Node/Cordis allowlist, or the fixed
  values `none` and `unrecognized`;
- a `ConfigFileError` stage of `read`, `parse`, `validate` or `none`;
- one installed-source-backed message coordinate, or `none`;
- an aggregate shape of `none`, `zero`, `one`, `multiple` or `unreadable`;
- chain-cycle and chain-truncation booleans; and
- fixed false raw-retention declarations.

No stack-frame or package-name projection is needed: the wrapper phase and
installed-source message coordinates already partition the useful source
interval without exposing dynamic paths or plugin names.

## Installed-source facts to bind

The contract binds rc.7's launcher, re-export shim, profile boot,
`dsh-app-boot`, plugin loader and Cordis sources by SHA-256. Deterministic
inspection must prove:

- `bin.js` awaits `runProfile` without an outer catch;
- the shim re-exports the actual profile boot;
- `runProfile` installs fail-loud handling and awaits `boot` before HMR
  watchers;
- `boot` has the fixed `host preparation failed` and
  `plugin tree failed to load` stages and preserves a cause chain;
- `ConfigFileError`, `ValidationError`, `CordisError` and `AggregateError`
  structures exist in the pinned sources; and
- the current controller directly launches `bin.js`, locating the future
  wrapper insertion point before the first HMR observation.

## Implementation and proof

The tranche may add the safe diagnostic component and separate v2 terminal
validation, freeze the exact future controller binding envelope, and add
provider-disabled deterministic evidence and hostile fixtures. It must leave
the accepted v1 terminal and consumed attempt controller byte-identical; a
separately authorised future controller can consume the new seam. Tests may
execute Python only. They must monkeypatch or source-inspect every subprocess
surface and prove zero Node, Harness, broker, worker, model and provider
processes.

Hostile fixtures must cover unknown names/codes/stages, cyclic and over-depth
causes, aggregate cardinalities, dynamic secret/path-shaped messages, invalid
JSON, oversized sidecars, symlinks, extra keys, identity mismatch and a failed
diagnostic write. The only permitted result of uncertain input is a closed
coordinate, rejection of the sidecar, or fallback to the accepted v1 terminal.

## Acceptance

Acceptance requires all of the following:

- exact installed-source hashes and source shapes pass;
- the wrapper has one dynamic entrypoint import, one exclusive diagnostic
  writer and one identical-value rethrow;
- the frozen future controller envelope launches the wrapper, reads the
  sidecar before root cleanup and embeds only a validated projection;
- v1 historical terminals still validate byte-for-byte unchanged;
- v2 identity and relationship rules reject every hostile mutation;
- deterministic evidence records zero prohibited processes and zero access to
  raw attempt streams; and
- targeted tests, Ruff and Python compilation pass.

## Parallelism assessment

- DeepSeek: `not_applicable`; the latch forbids Harness, worker and provider
  execution.
- Gemini: `not_applicable`; provider requests are forbidden and deterministic
  source/fixture evidence owns this recovery.
- Native subagents: `declined`; developer policy and the tightly coupled seam,
  vocabulary and controller ordering require one serial implementation.

Reassess only if the installed source has no safe error-object seam, the
candidate changes acceptance meaning, or deterministic acceptance fails.

## Explicit non-authority

This tranche authorises no new occupied attempt, retry, provider call, DeepSeek
performance claim, ordinary-practice enablement, product/configuration/API/
database/route/adapter/flag/allowlist/grammar/client/waiting-area change,
product or clinical data, production runtime, deployment, release, Pages,
protected-evidence access or protected-ref movement.
