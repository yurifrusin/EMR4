# Provider-free read-only post-cancellation programme orientation plan

Date: 2026-08-18

Timestamp: 2026-08-18T03:07:24.5811715+10:00 (Australia/Brisbane)

Status: `frozen_for_read_only_execution`

Task baseline: `5981b6cacdd3d488462803748c0d86f1e9bc2457`

Target result:
`raisa_provider_free_read_only_post_cancellation_programme_orientation_pass`

Reasoning level: Extra High. The tranche changes no product or command runtime,
but it selects the next architecture-strengthening direction after the complete
first-party cancellation chain.

## Objective

Reconcile the current Reception One and ordinary Diary command consumers with
the API Spine appointment command families and static Diary action grammar.
Identify which remaining gap is already dependency-satisfied, then freeze one
narrowest successor before any product, route, schema or database edit.

The orientation must distinguish:

- a command family that is mounted and used by both first-party projections;
- a command family implemented only behind a default-off authored-synthetic
  gate;
- a presentation gesture that currently reuses another command family;
- static action metadata that still says an operation is unimplemented; and
- a genuinely absent command family.

## Exact evidence sources

Read only:

1. `AGENTS.md`, the accepted ordinary Diary cancellation plan, closeout and Sol
   acceptance, and the terminal predecessor latch;
2. `orchestration/api_spine_adr.md`,
   `orchestration/api_spine_programme.md` and the current appointment command
   alignment inventory;
3. `docs/api-spine/openapi/appointment-commands.yaml`, the legacy compatibility
   write deprecation map and the idempotency/audit continuity indices;
4. `docs/diary/diary.js`, `docs/diary/meta-grid.js` and the focused first-party
   command-consumer tests;
5. `app/routers/appointments.py`, `app/config.py`, appointment schemas and the
   status/check-in product services as repository source only;
6. `app/services/diary/action_grammar.py`,
   `app/services/diary/action_route_contract.py` and
   `app/services/diary/planned_action_promotion.py`; and
7. the current implementation plan and authority-kernel/reference-client seam.

Protected holdouts, historical Diary/PHI, live source or database state,
product data, provider output and external channel state are ineligible.

## Required command-family matrix

For each current appointment family, record:

1. proposal and confirm route posture;
2. ordinary Diary consumer posture;
3. Reception One consumer posture;
4. authority, confirmation, idempotency, audit and fresh-read posture supported
   by current accepted evidence;
5. any conflicting static grammar or route-contract declaration; and
6. the smallest safe next action: complete, preserve, read-only review,
   architecture, implementation or explicitly retained horizon.

At minimum compare create/slot selection, update/reschedule, status,
waiting-area movement, delete/cancel, dedicated check-in and patient linking.

## Selection rules

The successor must:

1. address a current evidence-backed architectural inconsistency or missing
   first-party semantic seam;
2. be dependency-satisfied as a provider-free repository-static tranche;
3. precede any user-visible control when two routes can express materially
   overlapping intent;
4. preserve explicit staff confirmation, current authority/source recheck,
   idempotency, audit, strict receipt and fresh readback as distinct duties;
5. avoid opening a default-off feature merely because its route exists; and
6. remain narrower than implementation, route mounting, schema change or
   product-data execution.

## Acceptance

The tranche passes only if it:

1. produces the exact command-family and first-party-consumer matrix;
2. proves all source claims with deterministic repository assertions;
3. identifies whether dedicated check-in, generic `Arrived` status and
   waiting-area movement currently form one coherent or conflicting semantic
   surface;
4. records any static grammar/route-contract drift without correcting it in
   this orientation;
5. selects exactly one narrowest dependency-satisfied successor;
6. changes no product, backend, API/OpenAPI, GraphQL, schema, service,
   migration, database or raw compatibility behavior;
7. passes focused plan/report/API Spine/static checks, the canonical fast
   profile, latch/baton/Compass checks and Git whitespace; and
8. receives a fresh Gemini 3.7 Flash/high exact-candidate read-only veto if the
   final matrix freezes material architecture meaning.

## Parallelism-efficacy allocation

- **Sol:** owns the coupled source reconciliation, matrix, successor selection,
  deterministic admission, acceptance and Git.
- **DeepSeek V4 Flash/high — declined:** there is no stable separable
  implementation or mechanical test package before the matrix is frozen; its
  briefing and review cost would erase leverage.
- **Gemini 3.7 Flash/high — reserved:** one fresh read-only veto is required if
  the final result selects between overlapping check-in/status semantics.
- **Native subagents — declined:** current developer policy prohibits proactive
  native delegation.

Reassess after the matrix is frozen, before verifier admission and at closeout.

## Stop and recovery

Correct stale citations or matrix facts inside this read-only scope. If the
evidence cannot distinguish the overlapping arrival surfaces, retain both as
unresolved and select a narrower read-only factual review. Do not infer that a
default-off authored-synthetic route is generally admitted, and do not update
static grammar merely to make declarations agree.

### Post-freeze deterministic baseline classification

The first broader static packet found that
`tests/test_diary_action_route_endpoint_coverage.py` already fails six checks at
the exact baseline because `action_route_contract.py` records the status path as
`{appointment_id}` while FastAPI mounts `{appointment_id:uuid}`. None of those
source or test files changed in this orientation. Preserve that packet as
negative evidence and carry the path-spelling/literal-shadow diagnosis into the
selected convergence review. Do not edit the contract or endpoint-coverage test
inside this orientation merely to make the broader packet green.

Acceptance therefore uses the new factual matrix assertions, the existing API
Spine artifact and static grammar/promotion checks, canonical fast profile and
continuity gates. It reports, but does not claim a pass for, the exact existing
endpoint-coverage packet.

## Closed surfaces

No product behavior, FastAPI/OpenAPI/GraphQL/schema/service/migration/database
source, route mounting, raw compatibility behavior, feature flag, live route,
database/source/watcher access, external client, product/patient/clinical/
historical/protected data, provider/ADC, credential/IAM/network, executable
model tool, command/write, deployment, production, release, Pages or protected
ref action is authorised. Preserve `docs/branding/` and every unrelated
untracked file; use explicit-path staging only.
