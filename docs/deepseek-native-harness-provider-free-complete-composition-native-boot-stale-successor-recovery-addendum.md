# Complete-composition native-boot stale-successor recovery addendum

Date: 2026-08-20

Timestamp: 2026-08-20T12:29:30.8678914+10:00 (Australia/Brisbane)

Status: `frozen`

Recovery source HEAD:
`642e315772548ca4589f7bf87914f6dd74b235bc`

Affected operation:
`deepseek-native-harness-provider-free-complete-composition-native-boot-recovery`

Rejected successor:
`raisa-provider-free-default-off-canonical-check-in-route-adapter-convergence-rehearsal`

Correct successor:
`raisa-provider-free-check-in-relay-free-recovery-attempt-005`

Reasoning level: Extra High freezes this recovery because it changes the live
operation lineage and rolls back one clockwork generation. High is sufficient
for the exact code correction, deterministic checks, byte-exact rollback,
corrected publication and Git closeout while this addendum remains unchanged.

## Diagnosis

The complete-composition native-boot result itself is accepted and unchanged.
Its lease-60 closeout selected the 18 August default-off route-adapter tranche
as the next operation even though the live Continuity graph already records
that operation as accepted at exact source
`c82c3a741053a9c8da260aa62e1a968af22bb54e`. Later accepted descendants include
the read-only admission review at
`27101faa86b5aa3850e90bc4ded8600e5f8d7dc9` and the unmounted admission kernel
at `4204ec6348abb0f92b1a30314699d4a469fa860a`.

The retained historical guard did not prevent publication. It extracts only
backticked `docs/*-plan.md` names from the Baton and hard-codes the once-current
readiness successor. It is therefore a post-publication historical assertion,
not a general pre-publication operation-identity interlock.

The dependency chain now supports one distinct successor. Attempt 004 is
consumed. Its accepted server-attachment and observability repair states that a
new attempt 005 may be frozen only after the missing Harness terminal boundary
is proved. The complete-composition result now proves required-service
injection, the exact bounded-worker preset, dependency-gated activation,
effective `edit`/`glob`/`read`, a safe terminal and cleanup together. The next
operation may therefore plan attempt 005; it does not execute attempt 005 as
part of this recovery.

## Exact recovery

1. Preserve lease 60, Git source
   `642e315772548ca4589f7bf87914f6dd74b235bc`, the stale latch and the rejected
   first rehydration receipt as immutable evidence.
2. Add a clockwork build-time interlock that rejects any clean-closeout or
   user-decision successor whose exact operation ID already occurs anywhere in
   the validated Continuity graph. Graph identity is authoritative; prose and
   plan-filename extraction are not.
3. Replace the historical live-successor test with a general assertion that
   the live latch operation matches the Baton Next row and is absent from every
   existing Continuity node. Add a clockwork unit test that mutates a valid
   intent to an existing node ID and requires
   `tick_next_operation_already_recorded` before publication.
4. Roll lease 60 back through the clockwork's existing pointer-last rollback.
   The rollback must select its immediately previous full-Git-bound generation,
   increase only the lease sequence and restore all canonical and metadata
   bytes exactly.
5. Correct the accepted Harness closeout, Sol acceptance, Yuri summary and
   closeout intent to name only
   `raisa-provider-free-check-in-relay-free-recovery-attempt-005` as the next
   planning operation. Preserve the accepted Harness claim and all three
   earlier contained incidents.
6. Register this stale-successor conflict, the first receipt's invalid
   `expected_leverage` vocabulary and the hard-coded historical successor guard
   as corrected incidents through the same closeout clockwork intake.
7. Commit the rollback and correction explicitly, run one clockwork `--check`,
   then one separate `--publish`. The corrected generation must pass the full
   safe closeout suite and leave local/origin `master` and `handoff/current`
   unchanged.

## Parallelism assessment

- **DeepSeek native Harness:** declined. The proposed route package is already
  accepted, and successor repair has no model implementation role. No worker or
  provider request is permitted.
- **Gemini 3.7 Flash/high:** reserved only if the deterministic correction
  changes more than successor identity and its fail-closed interlock. The
  existing accepted Harness veto is not rerun.
- **Native subagents:** declined because current developer policy prohibits
  proactive delegation and the rollback/publication lease is serial.

Sol owns diagnosis, rollback, the interlock, deterministic verification,
acceptance, clockwork and Git. Reassess all three lanes before the corrected
publication and before the later attempt-005 worker decision.

## Acceptance

- the current stale live-successor test first fails on the repeated route ID;
- a valid closeout intent mutated to any existing graph node fails before
  projection or publication;
- the corrected successor is absent from existing graph nodes and matches both
  the generated latch and Baton Next row;
- the rollback reports `byte_exact: true` and the corrected publication has one
  new generation, zero drift and a full 40-character source binding;
- all six incidents are retained in the generated register with none open;
- the focused clockwork, successor, latch, Baton, API Spine and register suites
  pass; Ruff, compilation and `git diff --check` pass; and
- no worker, model/provider, Docker, PostgreSQL, SQL, product route or data path
  executes during recovery.

## Closed boundaries

No accepted product result is repeated or rewritten. No attempt 005 execution,
ordinary-practice enablement, feature-flag or allowlist change, command
mounting, generic-status `Arrived` change, action grammar, first-party client,
waiting-area movement, product/patient/appointment/clinical/historical/
protected data, provider call, occupied worker, Docker/database execution,
production runtime, deployment, release, Pages or protected-ref movement is
authorised. Local/origin `master` and `handoff/current` remain exact
`2e34bdad732fdab32fbf778280b3d3c70d66d602`. Preserve `docs/branding/` and all
unrelated untracked files; use explicit-path staging only.
