# Ariadne agent error and correction register — Revision 621

Date: 2026-08-22

Timestamp: 2026-08-22T18:23:35.2663093+10:00 (Australia/Brisbane)

Status: `accepted_closed_reading`

<!-- ariadne-agent-error-register-reading
revision: 621
incident_count: 976
new_incident_ids: AER-0967,AER-0968,AER-0969,AER-0970,AER-0971,AER-0972,AER-0973,AER-0974,AER-0975,AER-0976
open_incident_count: 0
-->

This revision records ten bounded observations from the first controlled
integrated-runner development rehearsal. Nine are low-severity local form or
implementation errors; the substantive observation is the consumed native-
Harness factory-stage failure. None is open and none authorizes a retry.

## AER-0967 — Ariadne prose repeated machine-owned Git identities

The preflight rejected the first receipt before dispatch. Exact Git identities
now come only from its machine snapshot.

## AER-0968 — source count conflated definition and invocation

The focused test caught the shared substring. The validator now asserts the
definition and call-site anchors separately.

## AER-0969 — direct script lacked repository-root import bootstrap

The first direct CLI run failed before writing. Direct and imported execution
now share the explicit repository-root bootstrap.

## AER-0970 — closed nested schema omitted its property maps

Draft 2020-12 validation rejected the intended members. Every nested closed
object now declares its exact admitted properties.

## AER-0971 — PowerShell chunk arithmetic was not scalarized

The first read-only AGENTS chunk command failed. Explicit typed Start/End
objects then completed the required full read.

## AER-0972 — checkpoint probe omitted the nested controller context

The first readback exposed historical base identities and was rejected. The
corrected probe activated both adapter contexts and matched the frozen values.

## AER-0973 — native Harness failed at factory before provider dispatch

One consumed process crossed both HMR coordinates and then failed at
`factory`. Provider, tool and candidate counts stayed zero; cleanup completed.

## AER-0974 — closeout omitted the prospective revision path

The first dry-run rejected before publication. Revision 621 is now bound in
both node artifacts and baton acceptance.

## AER-0975 — revision note omitted ordered incident headings

The second dry-run found the machine comment but only a bullet summary. This
revision now renders the exact projected heading sequence.

## AER-0976 — exhaustive active Baton inventory exceeded its budget

The third dry-run rejected the prospective live handover before publication.
Detailed artifacts remain in the closeout graph while the active Baton keeps a
compact essential lookup set.

The corrections add no product, data, provider, runtime, deployment, release,
Pages or protected-ref authority. The factory failure moves only to a
provider-free diagnostic successor.
