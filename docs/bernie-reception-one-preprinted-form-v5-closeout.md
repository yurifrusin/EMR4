# Reception One Pre-printed Form v5 Closeout

Result: `reception_one_preprinted_form_v5_occupied_pass`
Occupied calls: 1 of 2
Correction turns: 0 of 1
Fallbacks: 0
Writes: 0

## Outcome

Gemini 2.5 Flash successfully filled the pre-printed PlanProgram form on its
first untaught authored-synthetic attempt.

The provider returned only:

- `operator_note`;
- `goal_code`; and
- `steps`.

The one-use broker supplied only `version_code: 3`. It did not normalize,
repair, infer or select any model judgement. The assembled PlanProgram passed
the unchanged exact schema and compiler. The deterministic proofreader admitted
all six operators with no violation, safe repair or wire repair.

The released typed value was a move proposal for the frozen authored-synthetic
case. It named the existing proposal-only API Spine operation, required staff
confirmation, warned that no reservation existed, and recorded
`write_performed: false`. It was not delivered to the product or a human user.

## Provider and control result

- provider: Google Cloud Vertex AI;
- model: `gemini-2.5-flash`;
- project: `bernie-emr4-dev`;
- service account:
  `emr4-bernie-ai-dev@bernie-emr4-dev.iam.gserviceaccount.com`;
- authentication: keyless impersonated service-account ADC;
- location: `australia-southeast1`;
- hostname: `australia-southeast1-aiplatform.googleapis.com`;
- HTTP status: 200;
- latency: 2,574 ms;
- token usage: 2,229 prompt, 535 candidate, 2,764 total;
- API-key authentication: not used;
- provider tools, functions, grounding, retrieval and cache creation: not used;
- fallback: not used.

The fresh preflight confirmed the exact impersonated credential class, project,
target, cloud-platform scope, enabled API and billing entitlement, prediction-
only role/binding and exact permission, Vertex Data Access logging, disabled or
absent request-response logging, disabled provider cache and absence of a
user-managed service-account key.

## Audit and cleanup

The primary single-use ledger is consumed. The turn audit contains ten valid
events through `release_committed`; the parent audit contains four valid events
through `dialogue_closed`. The external audit retains hashes, provider metadata,
typed form codes, bounded operator note, proofreader findings and released
authored-synthetic typed values. It retains no raw prompt, raw provider response,
credential, token, API-key information or hidden reasoning.

No turn-two ledger was created and no provider call followed admission. All
task-owned cell and relay containers, network, images, broker process,
temporary token and temporary context are absent. No daemon-wide prune or cloud
mutation occurred.

The caller's first shell wait returned a timeout before the underlying process
finished. The underlying lifecycle continued to completion and wrote all normal
turn, external, parent and cleanup evidence. No evidence reconstruction, replay
or retry occurred.

## Verification

- 14 focused v5 tests pass after occupied-evidence binding;
- 146 focused and inherited lane/Continuity/Compass tests passed before call;
- the fixed repository-only Ariadne verifier passed 266 tests;
- 60 relevant API Spine route, artifact and drift guards passed;
- both credential-free real-isolation cells passed;
- Continuity, Compass and rendered Compass validation passed;
- Python compilation and Ruff checks passed;
- JSON and JSON Schema checks passed;
- `git diff --check` passed; and
- independent pre-call and post-run runtime/process residue checks passed.

An attempted sweep of every `test_api_spine_*.py` file stopped during collection
on an unrelated pre-existing test importing the no-longer-exported private
`_BERNIE_SESSION_STORE`. V5 changes neither that router nor that test. The
API-Steward-required artifact test and the relevant current route/ownership
guards pass, and the unrelated user work was preserved.

## Training conclusion

No prompt-context training is warranted on the evidence of this case alone:
there is no observed v5 defect to correct. Equally, one successful familiar
authored-synthetic move cannot show that teaching is unnecessary.

The next sound experiment is a frozen authored-synthetic evaluation matrix
covering the existing goal/operator catalogue and ambiguity boundaries. Measure
first-turn exact-body admission, compiler success, proofreader findings,
correction use and release accuracy. If repeatable errors cluster, introduce the
smallest prompt-context intervention and compare it against this unchanged v5
baseline before considering any fine-tuning.

## Candid claim boundary

This evidence proves one bounded untaught form completion, deterministic
proofreader admission and proposal-only release through the configured and
observed Sydney Vertex locational request path. It does not prove:

- general model or form-filling reliability;
- that prompt-context teaching or tuning will never be needed;
- Australian physical or sovereign processing;
- safety for real, product-derived, patient, health, clinical or historical
  data;
- product or database access;
- appointment confirmation or mutation;
- production suitability;
- Word or voice integration; or
- deployment or release readiness.
