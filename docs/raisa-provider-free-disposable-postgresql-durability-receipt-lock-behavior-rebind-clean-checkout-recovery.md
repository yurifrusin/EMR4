# Receipt-lock behavior rebind clean-checkout recovery

Date: 2026-08-08

Status: bounded replacement candidate pending a fresh exact-HEAD independent
veto; behavior runtime remains closed.

The first Gemini 3.6 Flash/high review at exact
`ba25816d65b4f50dfc2d71bb0ed1ead44166c5cd` returned
`revision_required`. It independently accepted every substantive receipt-lock
policy, structural/body/DDL/parse parent, behavior-contract, scenario and
authority-boundary challenge. It rejected the candidate for two verification
defects:

1. the failure-042 diagnosis unconditionally read the protected untracked
   mutable behavior-evidence alias, so two tests failed in a clean checkout;
2. the touched agent-error-register test was absent from the orchestrator's
   hand-selected Ruff format target set.

The replacement makes no database, policy, body, DDL, parse, behavior
contract, scenario, principal, fixture, SQLSTATE, allowed-effect, authority or
runtime change. It only:

- keeps tracked immutable failure-042 evidence mandatory while validating the
  mutable alias when and only when that optional untracked file exists;
- adds clean-absence and hostile wrong-present-alias tests; and
- formats the exact changed Python set.

The rejected review receipt remains immutable. AER-0222 records the recurrent
clean-checkout mutable-fixture defect and AER-0223 records the recurrent
touched-Python format-preflight omission. AER-0221 separately records a
PowerShell parse failure before Git ran during r170 setup.

Before attempt 043, the replacement must be committed, reproduced in a new
clean short-path worktree and receive a fresh Gemini 3.6 Flash/high exact-HEAD
`pass`. No same-head reinterpretation of the rejected review is permitted.

This recovery grants no Docker/PostgreSQL run, applied migration, operational
database, watcher/feed, application/API/Diary wiring, provider other than the
bounded reviewer, patient/clinical/product data, command/write authority,
deployment, production, release, Pages or protected-ref movement.
