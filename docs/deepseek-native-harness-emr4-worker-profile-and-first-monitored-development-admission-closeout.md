# DeepSeek native Harness EMR4 worker profile and first monitored development admission closeout

Date: 2026-08-18

Timestamp: 2026-08-18T17:41:39.5012901+10:00 (Australia/Brisbane)

Status: accepted attributable pre-provider negative worker evidence; no candidate admitted

Reasoning level: high

Accepted evidence source: `af1a79f93024a7186849e550b4d529c8c601c93f`

Result:
`occupied_worker_dispatch_traceable_broker_tool_contract_rejected_zero_provider_zero_candidate`

## Outcome

The versioned three-profile EMR4 authority contract, exact pinned
`@deepseek-ai/dsh@0.1.0-rc.7` package identity and two-container credential
enclosure are preserved. The corrected sparse provider-free boot reached exact
`MISSING_CREDENTIAL` with zero broker, provider or model activity. The worker
had only the sparse worktree and internal broker network; the separate broker
held the provider credential and no worker mount.

Exactly one fresh occupied `emr4-bounded-worker` session then started. The
Harness composed one request declaring seven model-facing tools: `edit`,
`glob`, `grep`, `read`, `read_image`, `str_replace_editor` and `write`. The
frozen broker admitted only `edit`, `glob` and `read`, so it returned
`tool-not-allowlisted` / HTTP 400 before provider I/O. The worker exited 1 in
761 ms with `INVALID_REQUEST`.

Sanitized reduction found four Zstandard frames, 17 logical rows, one request
header, zero provider calls, zero successful model steps, zero usage records,
zero tool calls and zero candidate changes. Neither owned output file was
created. The single occupied attempt is consumed and was not retried.

This is a useful fail-closed result. The broker prevented an unreviewed tool
expansion, and the native Harness made the exact request/tool/terminal cause
traceable. It does not demonstrate successful real-EMR4 worker completion.

## Correction and cleanup

AER-0459 corrects the earlier three-tool package-mapping claim. The package
composition exposed four surplus model schemas: `grep`, `read_image`,
`str_replace_editor` and `write`. The broker allowlist was not broadened to
absorb them. AER-0460 separately preserves and corrects the first overlong
precommit latch checkpoint; AER-0461 and AER-0462 correct the Continuity node's
path and typed evidence inventories; AER-0463 corrects the pre-existing stale
Compass current-node sentinel. Register revision 402 contains 463 incidents,
all corrected or explicitly contained and none open; its complete focused
suite passes.

All exact disposable containers, Docker volumes, networks, raw-session
storage, sparse worktree and worker branch are absent. The disposable profile
root was moved to the Windows Recycle Bin. Raw prompt, reasoning, response,
tool payload, credential and environment content was not copied into EMR4.

## Parallelism closeout

- DeepSeek native-Harness lane: completed with mixed leverage. It supplied a
  precisely attributable negative result and no candidate. The no-retry rule
  was preserved.
- Gemini lane: declined with neutral leverage. There was no candidate to veto;
  terminal, Git, cleanup and register evidence was deterministic.
- Native-subagent lane: declined with neutral leverage. Developer policy
  prohibited proactive delegation and the evidence/cleanup path was serial.

Sol alone admits the result.

## Protected boundaries

No product, patient, clinical, historical Diary or protected data was used.
No application source, route, API, database, status, grammar, client,
waiting-area, ordinary-practice, live product runtime, deployment, release,
Pages or protected ref changed. `docs/branding/` and every unrelated untracked
file remain preserved. Local/origin `master` and `handoff/current` remain exact
`2e34bdad732fdab32fbf778280b3d3c70d66d602`.

## Next tranche

Begin
`deepseek-native-harness-exact-tool-view-recovery-and-second-monitored-development-admission`.
It must first identify and enforce the pinned package's native scoped-tool
mechanism, then prove from a provider-free composed request header that the
model sees exactly `read`, `glob` and `edit`. Only after that deterministic
gate may one fresh second monitored worker session receive the same two-path
provider-free EMR4 validator package. It may not broaden the broker allowlist,
reuse or resume the consumed session, or open product/patient/clinical data,
application behavior, runtime, deployment, release, Pages or protected refs.
