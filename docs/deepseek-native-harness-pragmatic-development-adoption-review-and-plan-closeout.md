# DeepSeek native Harness pragmatic development-adoption review closeout

Date: 2026-08-23

Timestamp: 2026-08-23T09:22:37.2550535+10:00 (Australia/Brisbane)

Status: `accepted_pragmatic_secondary_worker_adoption`

Exact reviewed candidate:
`0c73fdea7d0158086720de08c1ff796365288cd5`

Result:
`pragmatic_secondary_worker_adoption_plan_frozen_first_real_assignment_selected`

## Conclusion

The native DeepSeek Harness is admitted now as a monitored secondary EMR4
implementation worker. It is not promoted to default, and it needs no further
generic boot, provider-free interoperability or synthetic qualification
tranche before one bounded real repository assignment.

The reason is evidentially narrow. The Harness is materially better at telling
the orchestrator what happened: process, session, provider request, tools,
results, usage, terminal, changed paths and cleanup can be attributed. It has
not yet proved that it completes useful EMR4 work more reliably than DeepSeek
through Claude Code.

## Review result

The review covered 68 accepted native-Harness Continuity nodes, 176 top-level
documents including 71 plans and 71 closeouts, 64 Continuity directories, 75
scripts, 96 tests, the pinned profile and broker, and the key occupied and boot
terminals.

The accepted stock-headless-to-custom-runner proof already passed at exact
evidence source `f0f8e59ebd70da5167edf907c9ece26049cdcf1c`. The live Baton's
older sentence requiring that future proof is stale. The clockwork publication
will replace it with the monitored-secondary-worker posture.

The incident corpus contains 30 Claude-labelled DeepSeek rows and 21 direct
native-Harness rows. Different tasks, denominators and observation quality make
them unsuitable as failure rates. Claude Code would also expose failures under
equally deep scrutiny; it is historical transport evidence, not a validated
control group.

## Lean operating shape

Keep the pinned package/profile/runner, exact sparse worktree and full source
OID, `read`/`glob`/`edit` tool view, serial tools, isolated broker, no direct
credential, no automatic retry/fallback/auxiliary model, 900-second session,
sanitized trace, cleanup and Sol-owned diff/tests/acceptance.

Retire generic boot reproof, one-request containment, per-step clockwork ticks,
subcoordinate closeouts and diagnostic sequels. Natural multi-turn
read/edit/error/reread/edit work is allowed inside the one session. Yuri's
prepaid balance remains the monetary boundary.

After a terminal, only a directly attributable pre-packet mechanical envelope
defect may receive one correction and at most one fresh run. Otherwise Sol
recovers the task without another Harness investigation series or silent
fallback.

## First real assignment

The successor is
`raisa-native-harness-first-pragmatic-real-development-run-canonical-check-in-manifest-normalizer`.
It owns only `closed_manifest_normalizer`, with intended paths
`app/services/appointment_check_in_environment_manifest.py` and
`tests/test_appointment_check_in_environment_manifest.py`.

The work is real EMR4 source and tests, but remains unmounted, reference-only,
non-secret and provider-brokered. It cannot read `.env`, process environment,
configuration, credentials, secret stores, databases, routes or product data,
and gains no admission, command or activation authority.

## API Spine boundary

The normalizer follows the accepted manifest pattern: declarative input is
normalized into typed code. A manifest remains neither a command nor a policy,
secret, environment selection or activation grant. GraphQL, REST/OpenAPI,
async events, Access AI and clients remain unchanged.

## Verification and workflow efficacy

The profile, broker and API Spine packet passed 53/53. Structured evidence was
recomputed at 68 native nodes, 30 Claude rows and 21 direct native rows. JSON,
Git whitespace and exact protected-ref readings passed.

Ten cheap local evidence/closeout-command lapses were corrected before publication:
one invalid `foreach` pipeline, two Windows-wildcard searches, two JavaScript
tool-composer syntax errors, one nonexistent graph-directory target, one
DateTime formatter error, one omitted closeout subdirectory and one rejected
three-parent Continuity node, and one rejected descriptive incident-stage set.
AER-1050 through AER-1059 preserve them. None caused a Harness, provider, runtime,
product, database or protected rerun. Their practical lesson is not another
control layer; it is to keep the clockwork interface sparse and take readings
at meaningful boundaries.

## Parallelism closeout

- DeepSeek was declined during its own evidence review and is selected only
  for the separately latched successor assignment.
- Gemini was declined because no candidate code or material API meaning needed
  an independent veto.
- Native subagents were declined under developer policy and the serial evidence
  reconciliation.
- GPT Sol owned review, plan, deterministic verification, acceptance,
  clockwork and Git.

## Closed surfaces

No Harness, worker or provider ran during this tranche. No broker, runner,
preset, application, route, API, GraphQL, OpenAPI, client or configuration
changed. No secret, environment, database, Docker, product, patient,
appointment, clinical, historical or protected data was used. No ordinary-
practice enablement, feature flag, allowlist, command mounting, generic-status
`Arrived`, grammar, waiting-area, production, deployment, release, Pages or
protected-ref action is accepted.

Local/origin `master` and `handoff/current` remain exact
`2e34bdad732fdab32fbf778280b3d3c70d66d602`. `docs/branding/` and all unrelated
untracked files remain preserved.
