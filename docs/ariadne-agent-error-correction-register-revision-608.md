# Ariadne agent error and correction register — revision 608

Date: 2026-08-22

Status: **ten corrected incidents added; none open**

<!-- ariadne-agent-error-register-reading
revision: 608
incident_count: 907
new_incident_ids: AER-0898,AER-0899,AER-0900,AER-0901,AER-0902,AER-0903,AER-0904,AER-0905,AER-0906,AER-0907
open_incident_count: 0
-->

## AER-0898

The first native-rehearsal preplanning state repeated Git object IDs in its
descriptive source evidence. Preflight rejected it before implementation. The
replacement lets the machine snapshot own every object identity.

## AER-0899

Three read-only PowerShell projections used invalid inline control-flow or
pipeline syntax. Each stopped at parsing before changing Git, evidence,
processes or files. The replacement readings assign intermediate values before
one final projection.

## AER-0900

The first controller draft recalled seven conceptual preset-mount codes rather
than importing the exact accepted bridge vocabulary. The deterministic contract
check rejected it before commit or native execution. The accepted draft derives
the list from the source owner.

## AER-0901

The first nested module binding mutated a shared object used by the accepted
runner generator and recursed. A focused deterministic test caught it before
commit or execution. The accepted binding mutates only the post-HMR execution
controller.

## AER-0902

Two first-draft tests treated Markdown line wrapping as semantic. Their only
failures were corrected by normalized-whitespace assertions before native
execution.

## AER-0903

The first owned-process query matched its PowerShell observer because the query
text appeared in that process's own command line. The corrected control filters
to `node.exe` first and then matches the unique retained-root coordinate.

## AER-0904

The sole native process emitted the schema token owned by the accepted
preset-composition runner, while the successor controller expected a newly
named token. The 1,646-byte sidecar therefore failed closed before semantic
admission. The presemantic envelope retained its exact length and digest. The
offline recovery admitted that observed token as history and added a prospective
source-token check.

## AER-0905

After that sidecar rejection, the controller tried to replace its immutable
sidecar-seen envelope with a sidecar-absent envelope. The deliberate mismatch
then interrupted summaries and cleanup. Offline recovery persisted the typed
terminal, proved zero owned processes and removed only the exact retained root.
The prospective controller now preserves an existing envelope byte-for-byte.

## AER-0906

The first recovery-plan commit followed two EOF blank-line warnings because a
semicolon-separated PowerShell sequence did not test `LASTEXITCODE`. A distinct
descendant normalized both documents before implementation and publication;
all later staged-diff gates explicitly stopped before commit on failure.

## AER-0907

The first AER-0903 through AER-0905 drafts used the descriptive stages
`preexecution`, `native_execution` and `planning`, plus the unadmitted causal
label `deterministic`. Successive validation passes rejected them before report
generation or publication. The accepted rows use `dispatch`,
`deterministic_verification`, `implementation` and `observation_only`; future
intake copies schema tokens first.

## Control reading

Eight incidents were caught before the sole native process or closeout. Two material
controller-composition defects consumed that process but failed closed with
enough content-free evidence for an exact zero-process recovery. No retry,
provider call, worker turn, product action or protected-ref movement occurred.
The recovered terminal is finite and traceable but does not prove the new
preset-mount bridge runtime path.
