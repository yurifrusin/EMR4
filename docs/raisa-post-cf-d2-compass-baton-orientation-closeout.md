# Post-CF-D2 Compass and baton orientation closeout

Date: 2026-08-13

Timestamp: 2026-08-13T21:25:00+10:00 (Australia/Brisbane)

Status: accepted

Accepted source: `edba8f57380a48fd98decc332608349f2d9012e6`

Result: `raisa_post_cf_d2_compass_baton_orientation_pass`

## Outcome

The programme can return to visible Reception One work without making that
work wait for an operational durability watcher. The selected next tranche is
the provider-free **Reception One selected-appointment status-action
composition**.

This is the narrowest dependency-satisfied successor because it composes an
already secured task into an already accepted product surface. Reception One
currently shows appointment status as read-only information. The ordinary
Diary already has a complete visible status interaction over the canonical
status proposal/confirm family. The next tranche will bridge one selected
Reception One appointment to that same interaction, not create a second
command implementation.

## Evidence and candidate comparison

- The accepted status HTTP/product-adapter lineage and visible native Diary
  wiring already own safe, warning, terminal, provisional-identity, cancel,
  blocked, stale/failure and committed behavior.
- The current `EMR4DiaryMetaGridBridge` exposes current reads, availability,
  proposal handoff and date movement, but no status action.
- The Reception One appointment-card renderer displays status and supports
  selection but does not offer a status transition.
- The implementation plan already names status actions as supervised Reception
  One proposals and requires all interfaces to converge on the backend-owned
  command path.
- CF-D2 now has sufficient serial database evidence to remain a later optional
  acceleration layer; status-command correctness does not depend on its
  delivery.

Stage 3B participant execution, another event family, the first external
patient channel, operational watcher/restart work and another command family
are not dependency-satisfied within current authority. General visual polish
is safe but less useful than composing this complete routine task.

## Issues found and resolved

The first focused check exposed two mechanical evidence defects: one test
expected a phrase without accounting for Markdown line wrapping, and the new
in-progress latch used a descriptive terminal-reason value rather than the
schema's exact `unfinished_authorized_operation` literal. Both were corrected
without changing the selected direction or any product source. The complete
focused packet then passed.

## Verification

- Four orientation assertions pass over the exact programme and source facts.
- The combined orientation, accepted UI/foundation, latch, current-baton and
  Compass packet passes 91/91 serial tests.
- The canonical fast profile passes Ruff, maintained-source compilation, 193
  API Spine/handover/receipt/maintenance tests, Diary JavaScript syntax and Git
  whitespace.
- Exact source `edba8f57380a48fd98decc332608349f2d9012e6` contains the
  frozen orientation candidate; no product file changed.
- Local/origin `master` and `handoff/current` remain exactly protected
  `2e34bdad732fdab32fbf778280b3d3c70d66d602`.

No external worker, independent verifier, provider or subagent was selected.
The small read-only programme comparison remained Sol-owned.

## Next tranche

Freeze and execute a provider-free Reception One selected-appointment
status-action composition. Limit it to one selected current appointment, the
existing status vocabulary, a small bridge to the existing
`setAppointmentStatus` interaction, modeless in-surface state feedback, fresh
reload/reconciliation and responsive keyboard/interruption evidence.

The tranche must add no backend route, API/OpenAPI/GraphQL contract, database
object, command family, raw fallback, event runtime, provider or product data.
Yuri's attention is not required.

## Claim boundary

This orientation changes no product behavior and proves no runtime. Protected
evidence, historical Diary/PHI, patient/product/clinical data, external patient
identity/clients/channels, source watcher, persistence, restart/unknown commit,
provider/ADC, credentials/IAM/network, new commands/writes, deployment,
production, release, Pages and protected refs remain closed.
