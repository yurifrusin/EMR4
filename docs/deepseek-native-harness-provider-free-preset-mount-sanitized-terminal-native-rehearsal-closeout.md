# DeepSeek native Harness preset-mount sanitized-terminal native rehearsal closeout

Date: 2026-08-22

Timestamp: 2026-08-22T07:43:26.3147932+10:00 (Australia/Brisbane)

Status: **accepted finite negative native result with complete offline recovery**

Reasoning level: **Extra High**

## Result

The sole authorised native rc.7 process ran at exact consumed candidate
`c1ae13df334dfdffefb229c3ae5a502a7251451c`. It reached the private-identity
boundary and emitted one 1,646-byte typed sidecar before any turn, worker,
model/provider request, network, database, Docker or target activity.

The sidecar's closed terminal is
`preset_composition_failure_attributed` at
`EFFECTIVE_TOOL_COMPOSITION_UNCLASSIFIED`. This is a finite, traceable native
failure. It is not `preset_mount_failure_attributed`, so it does **not** prove
that the new preset-mount bridge ran inside the native Harness.

## Controller incident and recovery

The exact generated runner emitted its accepted predecessor schema token while
the successor controller expected a newly named token. The content-free outer
envelope correctly retained the sidecar byte count and SHA-256 before semantic
admission. The controller then tried to rewrite that immutable sidecar-seen
envelope as sidecar-absent; the mismatch interrupted canonical summaries and
cleanup.

At exact reviewed recovery source
`05a721a075a96a5e248818371d7bdfc8e136c792`, the separate offline controller:

- bound the immutable consumed-attempt record, envelope, sidecar length and
  digest;
- admitted only the exact closed typed projection while preserving the observed
  predecessor token as history;
- verified the exact readiness pair, zero broker/network counters, exact
  runner/guard/bridge/sanitizer/sentinel hashes and absent target;
- proved zero Node processes owned the retained root;
- removed only that exact disposable root; and
- retained no raw streams, messages, stacks, causes, paths, environment values
  or credentials.

The recovery launched zero Node, native-Harness, worker, model or provider
processes. The native retry and resume counts remain zero.

## Control gain

This is a meaningful but incomplete gain in orchestrator control. We now have
durable proof that one native Harness process can be held to a single private,
zero-request attempt and can release a finite typed failure instead of an
untraceable timeout. We also have an exact recovery path when the outer
controller rejects a valid bounded sidecar.

We do not yet have proof that the preset-mount bridge survives the native
composition boundary, nor any occupied DeepSeek worker performance evidence.

## Verification

- The offline recovery `--execute` and persisted `--check` passed with zero
  recovery native processes and complete root cleanup.
- The current native/recovery focused suite passed with one platform-specific
  directory-symlink test skipped.
- The combined native/recovery, latch and Current Baton suite passed.
- The complete Ariadne agent-error register suite passed after canonical report
  regeneration.
- Ruff, `py_compile` and Git whitespace checks passed.

AER-0898 through AER-0907 preserve ten corrected incidents. AER-0904 and
AER-0905 are the two material controller defects; none of the incidents opened
provider, product or protected-ref authority.

## Parallelism disposition

- DeepSeek: declined after its sole native process was consumed; self-review or
  retry would have violated the latch.
- Gemini: declined because exact digest, schema, closed-terminal and cleanup
  admission were deterministic and left no product judgment.
- Native subagents: declined because delegation is prohibited and the retained
  root required one serial owner.
- GPT Sol owned execution readback, offline recovery, verification and
  acceptance.

## Next tranche

Proceed with the provider-free
`deepseek-native-harness-provider-free-preset-mount-composition-unclassified-source-reconciliation-rehearsal`.
It may compare the exact emitted runner, guard, bridge, sanitizer and installed
native composition sources to locate the narrowest deterministic explanation
for `EFFECTIVE_TOOL_COMPOSITION_UNCLASSIFIED`. It authorises no Node/native
process, worker/model/provider request, retry or product change.

## Boundaries

No native retry, occupied DeepSeek worker, model/provider request, target edit,
product/data/configuration/API/database/route/adapter/flag/allowlist/grammar/
client/waiting-area change, ordinary-practice enablement, generic-status
`Arrived` change, production, deployment, release, Pages, protected evidence or
protected-ref movement is accepted.
