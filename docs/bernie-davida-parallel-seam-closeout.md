# Bernie/Davida parallel seam closeout

Date: 2026-08-03

Decision: `accepted`

Terminal result: `bernie_davida_parallel_seam_pass`

## Accepted result

The shared seam required before genuine two-lane execution is frozen. Bernie
and Davida are separate bounded probabilistic work cells and future runtime
identities. They may share deterministic kernel code, but their capability
manifests, context allowlists, operation vocabularies, proofreader policies,
retry budgets and output schemas remain separately pinned. Neither receives a
database credential, ORM session, generic API client or model-to-database path.

The native Diary active-practitioner consumer is explicitly a deterministic
product composition, not an agent. Its next lane is the default-off
application-session composition of the accepted read while the existing
bearer-authenticated GraphQL/REST-fallback behavior remains unchanged when the
feature is off. It shares the lower auth/product-read bridge only and does not
reuse the Office one-use terminal reload/logout lifecycle.

Davida is the custodian/interface for Practice Administration, not owner of its
data or hidden memory. Authoritative structured practice state,
provenance-bearing advisory institutional knowledge and bounded expiring
session state remain distinct. Davida can emit intent, explanation,
clarification and proposal drafts but never a confirmation or
`writes_authorized=true` envelope. A future apply command requires a closed
operation enum, pure read projections, optimistic concurrency, idempotency,
append-only audit/outbox, explicit human confirmation and backend
revalidation.

## Parallel control result

No Ariadne Python reconstruction is required. The current deterministic-first
verifier policy, worker pool and exact task packets support two concurrent
DeepSeek V4 Flash/high implementation lanes. Repository pytest loading
`conftest.py`, shared PostgreSQL, shared-state browser runs, the single Gemini
3.6 Flash/high reviewer and root integration remain serial.

Workers receive disjoint paths in exact worktrees from one source head. Shared
auth/API contracts, `AGENTS.md`, Continuity/Compass, harness settings, schemas,
models, migrations, workflows, security policy and final integration artifacts
remain root-owned. Explicit-path staging is mandatory; `git add -A`, `git add .`
and the `agent_worktrees.py` commit-message staging path are forbidden while
the user-owned branding directory remains untracked.

## Verification

- Three independent read-only Sol audits covered Diary composition, Davida
  Practice Administration and Ariadne parallel control.
- The focused seam, verifier-policy and API Spine population passed 49 tests.
- Ruff, Python compilation, JSON/JSON Schema parsing and diff hygiene passed.
- Fresh Gemini 3.6 Flash/high at explicit high effort reviewed exact clean
  candidate `9b4d7dcce2d18182fb0b5f31f010496a1ef983dd`, reproduced all 49 tests,
  reported no finding, returned exactly one `DECISION: pass` and left HEAD and
  its worktree unchanged.
- The five-source preacceptance receipt passed with no reason for revision.

## Boundaries and next work

No product runtime, provider product path, real identity or data, protected
evidence, database migration, command/write, cloud/IAM action, deployment,
production, release or protected-ref movement occurred. `docs/branding/`
remains user-owned, untouched and excluded.

The exact next work is two concurrent bounded lanes from one final seam head:

1. Diary: architecture/static contract for the default-off application-session
   native Diary active-practitioner composition.
2. Davida: architecture-only Practice Administration boundary and closed
   capability/command-risk taxonomy.

Yuri's standing authority permits logical bounded descendants in both lanes.
Material architecture/product forks, new providers/cost/licence, real data or
identity, actual administrative apply authority, deployment/protected actions
and economically preferable manual intervention return to Yuri.

Reasoning level: Extra High for architecture, security and authority
acceptance; High for deterministic artifact construction, tests and Git
mechanics.
