# DeepSeek native Harness EMR4 worker profile and first monitored development admission plan

Date: 2026-08-18

Timestamp: 2026-08-18T15:46:46.3145392+10:00 (Australia/Brisbane)

Status: frozen

Reasoning level: high

Operation:
`deepseek-native-harness-emr4-worker-profile-and-first-monitored-development-admission`

## Objective

Turn the accepted rc.7 occupied traceability result into a versioned EMR4
control surface, then use it for one real low-risk provider-free repository
development package. The tranche must establish a deterministic profile-family
contract, validate the exact effective authority of each profile, run one fresh
native-Harness DeepSeek V4 Flash/high worker session over exact owned EMR4
paths, and admit or reject its candidate through independent Sol readback.

This is the first monitored real-EMR4 trial transport. It is not a broad
synthetic comparison, an unrestricted default-worker promotion or authority
for product/runtime data.

## Accepted predecessor

The tranche builds on accepted evidence source
`25067e7d633eae597929d6969a35b22b735b253e`. That source proves materially
strong occupied session/tool/usage/terminal traceability and a correct partial
synthetic repair. It does not prove complete worker reliability. The trial
must therefore preserve exact attribution and treat incomplete work as an
ordinary rejectable candidate, never as authority to hide, repeat or broaden
the session.

## Versioned EMR4 profile family

Freeze one repository-owned normalized contract with exactly three named
profiles:

1. `emr4-provider-free-preflight`
   - unoccupied diagnostic only;
   - no credential injection and no provider request;
   - plugin/profile composition must reach the expected missing-credential
     boundary;
   - read-only sandbox, approval `never`, zero model-facing tools and no
     auxiliary model route.
2. `emr4-readonly-review`
   - occupied only when a later accepted packet assigns a review;
   - read-only sandbox, approval `never`, repository-local read/glob tools;
   - no edit, shell, web, browser, plugin, workflow, subagent or auxiliary
     model route.
3. `emr4-bounded-worker`
   - occupied implementation/test profile for this tranche;
   - workspace-write sandbox and approval `never`;
   - only repository-local read, glob, string editor and PowerShell tools;
   - one parallel tool call, zero automatic retries, zero fallback, no title,
     compaction, telemetry, web, workflow, skill, job or subagent route.

All profiles pin official `@deepseek-ai/dsh@0.1.0-rc.7`, exact registry
identity, `deepseek-official/deepseek-v4-flash`, high reasoning and explicit
output headroom. They must carry a canonical normalized digest and reject
unknown fields or authority broadening. Profile selection is session-pinned.
Resume is allowed only for the same interrupted tranche; this first monitored
session starts fresh and is not resumed after a terminal candidate.

Yuri's prepaid DeepSeek balance is the monetary budget mechanism. The EMR4
contract therefore does not invent a Harness-side token or turn budget. It
still fixes zero retry/fallback, a bounded wall clock, a single occupied
session, exact data/file/tool authority and attributable usage evidence.

## Sol-owned profile-contract source

Before occupied dispatch, Sol owns and commits:

- `orchestration/continuity/deepseek-native-harness-emr4-worker-profile-and-first-monitored-development-admission/profile-family.yaml`;
- `orchestration/continuity/deepseek-native-harness-emr4-worker-profile-and-first-monitored-development-admission/profile-family.schema.json`;
- deterministic profile validation, package-source mapping and a
  credential-absent preflight;
- the exact sparse worker worktree, DSH home, package cache and worker packet.

The normalized contract is authority evidence. A disposable rc.7 composition
may translate it into package-native configuration only after every package
field is re-bound against pinned local source. Static YAML/schema success alone
does not admit an occupied request.

## First monitored real EMR4 work package

Create a new non-protected worker branch and sparse worktree under
`C:\Users\sarashera\EMR4-worktrees\`. The sparse checkout contains only the
full root `AGENTS.md`, this plan and threat delta, the frozen profile contract,
the exact neighbouring source examples named in the worker packet, and the two
owned output paths.

DeepSeek owns exactly:

- `scripts/ariadne_deepseek_native_harness_profiles.py`;
- `tests/test_ariadne_deepseek_native_harness_profiles.py`.

The script must load and validate the frozen YAML against its schema, enforce
semantic invariants not expressible in JSON Schema, select a named profile,
produce a deterministic sanitized effective-authority document and canonical
SHA-256 digest, and fail closed for unknown profiles, unknown fields, secret-
looking values, nonzero retry/fallback, auxiliary model routes, profile/tool
authority mismatches and cross-tranche resume. It must never execute DSH,
resolve a credential, contact a provider, edit global configuration or persist
raw sessions. The focused tests use temporary files and provider-free
fixtures only.

The worker must run its focused tests and return a concise changed-path/test
summary. It may not commit, push, edit the frozen contract/plan, change any
product source, or touch untracked files. Sol independently validates the
diff, hashes, owned-path boundary, complete focused suite and relevant
neighbouring orchestration tests before adopting or rejecting the candidate.

This package is real EMR4 harness-development work: the resulting validator is
intended to become the admission boundary for later native-Harness workers.
It has no application/product effect.

## Occupied execution envelope

- One fresh DeepSeek V4 Flash/high native-Harness session only.
- Exact pinned rc.7 package and committed `emr4-bounded-worker` contract.
- No local request-count/budget proxy; prepaid account credit supplies the
  monetary ceiling.
- Zero automatic retry, zero fallback, zero auxiliary model calls.
- At most one parallel tool call and a 15-minute process wall clock.
- Direct official DeepSeek API route; no web/browser/MCP/ACP, subagent,
  workflow, skill, job, title, compaction or telemetry route.
- No same-session or fresh-session retry after a terminal result. A local
  pre-provider configuration failure may receive one provider-free corrected
  boot only; it cannot consume or repeat the occupied attempt.

Retain sanitized package/profile/process/session identifiers, model-step and
tool counts, ordered tool names/targets/statuses, usage totals, terminal class,
changed paths, diff/hash/test readback and cleanup. Do not commit raw reasoning,
prompts, responses, tool payload bodies, credentials, environment dumps,
package caches or raw session transcripts.

## Acceptance

The tranche passes only if:

- the three-profile normalized contract and schema validate and their
  canonical digests are stable;
- exact rc.7 package-source composition maps every normalized authority field
  without an unrepresented enabled route;
- the provider-free preflight reaches `MISSING_CREDENTIAL` with zero HTTP;
- exactly one occupied fresh session uses only the bounded profile and owned
  sparse worktree;
- the durable trace and process terminal agree on model steps, tools, usage,
  retries, fallbacks and auxiliary calls;
- only the two owned output paths change;
- the validator and focused tests satisfy every frozen semantic invariant;
- all focused and neighbouring deterministic tests pass;
- raw session/package/worktree material is reduced and cleaned recoverably;
  and
- every Git, untracked-file, data, runtime and protected boundary passes.

An incomplete or incorrect candidate may be accepted only as attributable
negative worker evidence. It cannot become profile source and receives no
automatic repeat. Sol may recover under the existing non-transferable recovery
lease only if the defect is mechanically bounded and the same frozen profile
contract remains intact.

## Parallelism assessment

- DeepSeek native-Harness lane: `planned`, positive leverage. It owns the one
  sparse-worktree implementation/test package and native session trace after
  Sol commits deterministic profile admission.
- Gemini lane: `declined`, neutral leverage at plan freeze. The profile
  contract and first worker candidate have deterministic tests and no product
  behavior. Reassess only if the worker changes a material authority meaning
  or Sol recovery becomes conceptual.
- Native-subagent lane: `declined`, neutral leverage. Current developer policy
  prohibits proactive delegation and no independent package is authorised.

The plan, contract admission, sparse checkout, occupied session, candidate
readback, cleanup, acceptance and Git closeout remain serial.

## Protected boundaries

No product, patient, clinical, historical-diary, real-person or protected-
holdout data. No application API, database, route, client, generic-status
`Arrived`, action grammar, waiting-area movement, ordinary-practice enablement,
live product runtime, deployment, release or Pages change. No global npm/DSH
install, shell-profile, user-profile, credential or IAM mutation. No protected
ref movement. Local/origin `master` and `handoff/current` remain exact
`2e34bdad732fdab32fbf778280b3d3c70d66d602`. Preserve `docs/branding/` and
every unrelated untracked file. Stage explicit paths only.

## Claim boundary

Passing can establish that a pinned, versioned native-Harness profile family
controlled one useful low-risk EMR4 repository worker package with attributable
evidence. It cannot establish general model reliability, production safety,
privacy or sovereignty, or authorise unrestricted default transport, product
data, runtime, deployment or protected integration.
