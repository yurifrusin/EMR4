# Reception One Receptionist-first v6.6 Closeout

Status: complete; occupied cohort did not pass at 23/24 exact outcomes
Recorded: 2026-07-30

## Result

The complete twenty-four-request authored-synthetic regression ran through the
exact bounded Sydney Vertex lane. Twenty-three terminal results matched their
frozen deterministic oracle. One did not, so the tranche result is
`reception_one_receptionist_first_v66_full_cohort_fail_closed`.

All twenty-eight opened ledgers are consumed, every turn and case audit chain
verifies, no call followed the terminal result and task-scoped residue is
clear. The mismatch released no value.

## What improved

- The sole v6.5 mismatch, `b-squeeze-without`, now passes after one correction.
- Its typed squeeze-in form remained exact while the corrected internal
  decision note stayed de-identified.
- Every create, move, resize, cancel, status and squeeze-in case passed.
- The unstamped no-show request still safely clarified; no `dna` alias was
  invented.

## Remaining mismatch

`b-clarify-fit` used the exact clarification goal and typed operator on both
calls. The primary natural response failed both the general review boundary
and clarification vocabulary checks. The terminal response passed the general
boundary but still used none of the frozen clarification markers, so the
proofreader edge-aborted. The discarded response text was not retained and no
stronger wording diagnosis is claimed.

The bounded descendant repair is to teach one explicit
proofreader-compatible clarification sentence pattern, such as beginning with
`Which` or `Could you`, and the exact no-booking boundary. It must not loosen
the proofreader, change the typed form or oracle, add a no-show alias, or alter
the provider lane.

## Calls and controls

- 24 primary calls and 4 terminal second calls; 28 total against a ceiling of
  48;
- all 28 provider responses completed with HTTP 200;
- 125,795 prompt, 13,882 visible candidate, 19,549 thinking and 159,226 total
  tokens reported by Vertex;
- exact `gemini-2.5-flash`, `bernie-emr4-dev`, Bernie impersonated service
  account, `australia-southeast1` and regional hostname throughout;
- no API key, service-account key, global endpoint, fallback, provider tool,
  grounding, retrieval, cache creation, product/database access, write or
  delivery; and
- raw prompts, raw provider responses, credentials, API-key information and
  hidden chain-of-thought were not retained.

## Claim limit

This evidence proves twenty-three exact safe outcomes and one bounded
clarification-channel vocabulary mismatch over a reused authored-synthetic
cohort through the configured and observed Sydney locational path. It does not
prove independent generalisation, production fitness, Australian physical or
sovereign processing, or safety for real, product-derived, patient, health,
clinical, protected or historical data.
