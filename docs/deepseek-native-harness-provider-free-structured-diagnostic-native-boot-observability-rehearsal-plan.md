# DeepSeek native Harness provider-free structured diagnostic native-boot observability rehearsal plan

Date: 2026-08-21

Timestamp: 2026-08-21T09:41:43.2918015+10:00 (Australia/Brisbane)

Status: `frozen`

Operation:
`deepseek-native-harness-provider-free-structured-diagnostic-native-boot-observability-rehearsal`

New immutable attempt:
`structured-diagnostic-native-boot-observability-attempt-001`

Planning source HEAD:
`90e40cc8fdc154ca48e79dbfb24df68e7f05b558`

Accepted canonical wrapper and Node-fixture source:
`3ed856dac63bf0e51312e2b3dee14bdc2c934daf`

Accepted complete provider-free rc.7 composition result:
`6ef058b87a2c927efd9d9d2027b59d6ad279fec5`

Reasoning level: High. The active latch already freezes the provider-free,
single-process, no-worker and no-product boundary. This plan selects the
narrowest source-backed pre-HMR failure that exercises the accepted wrapper
without revising architecture, authority or acceptance meaning.

## Objective and exact claim

Execute exactly one pinned `@deepseek-ai/dsh@0.1.0-rc.7` native entrypoint
through the accepted canonical diagnostic wrapper. The invocation uses one
fixed authored profile name that is absent from the disposable Harness home,
passes no task argument, and therefore rejects during profile resolution
before Cordis boot or the first HMR event.

While the raw local streams and structured sidecar still exist inside the
exact disposable root, the controller must:

1. validate the canonical sidecar against the accepted closed vocabulary;
2. read only bounded stream prefixes for the accepted v1 relationship check;
3. build and exclusively persist one v2 structured pre-HMR terminal outside
   the disposable root;
4. validate its exact readback; and
5. remove the raw streams, wrapper, copied package tree and disposable root.

Acceptance proves only that the structured diagnostic gear composes with the
real pinned native entrypoint and terminalizes one authored pre-HMR rejection
safely. It does not prove a DeepSeek worker, model reasoning, provider
reliability or occupied Harness readiness.

## Exact package and executable boundary

The controller may copy, without a package-manager process, the previously
accepted local rc.7 installation rooted at
`C:/Users/sarashera/EMR4-worktrees/deepseek-check-in-attachment-observability-native-001`.
Before and after copying it must validate the accepted package-lock identities,
all required installed package versions and the source SHA-256 bindings used by
the structured-diagnostic and complete-composition acceptances.

The exact DSH package remains:

- name `@deepseek-ai/dsh`;
- version `0.1.0-rc.7`;
- entrypoint `lib/bin.js`;
- tarball SHA-256
  `2f8f0b763d611ac536f7a9411ee43c0afc067c1b8732c3102c04dbe398bcacc5`;
- launcher SHA-256
  `c0226687bb20f45c603ec6fe50f3de16d1c3510c3a803304ec575ef9bc366c62`;
  and
- Node flag `--expose-internals`.

The only external executable process admitted after deterministic validation
is one resolved local Node process. Copying uses Python filesystem operations
only. No npm, pnpm, registry, network, Docker or database process is admitted.

## Authored pre-HMR failure

The fixed profile is `emr4-diagnostic-observability-missing`. The disposable
home must not contain that profile directory. The launch command is exactly:

```text
node --expose-internals <exact-wrapper> --profile emr4-diagnostic-observability-missing
```

There is no trailing task, prompt, work order, session identifier or tool
request. Pinned installed-source inspection must prove that an absent
non-shipped profile throws an ordinary JavaScript `Error` during profile
resolution before `boot(...)`, HMR watchers, broker or runner construction.

The accepted canonical wrapper must have exactly one dynamic import of the
pinned `lib/bin.js`, one `wx` sidecar writer and one identical-value rethrow.
Its recursive key-order serializer is mandatory for this attempt.

## Expected safe diagnostic and terminal

The exact accepted sidecar has:

- schema `ariadne.native_harness_pre_hmr_structured_diagnostic.v1`;
- phase `entrypoint_import_rejected`;
- the operation, attempt and full candidate Git object identities;
- one cause node at position zero with `error_kind: error` and
  `code_coordinate`, `config_stage`, `message_coordinate` and
  `aggregate_shape` all `none`;
- no cycle or truncation; and
- all raw-retention declarations false.

The Node process must exit exactly `1` before an HMR event. The controller then
builds the accepted v2 terminal with:

- schema `ariadne.native_harness_pre_hmr_startup_terminal.v2`;
- stage `native_process_started_before_first_hmr_event`;
- cause `structured_entrypoint_import_rejected`;
- controller coordinate `native_process_exited_nonzero`;
- HMR event count zero; and
- the exact safe structured diagnostic above.

The terminal may retain only stream byte counts and SHA-256 digests. Raw
stdout, stderr, messages, stacks, paths, environment values and credentials
remain inside the disposable root until that terminal validates, then are
destroyed. If the structured sidecar is absent or invalid, the controller may
derive and exclusively preserve the accepted v1 fallback, but the tranche
result is fail-closed and not accepted.

## One-process latch and cleanup

All deterministic defects must be corrected before process start. The first
`Popen` consumes the attempt. There is no automatic or manual retry, resume,
same-attempt reclassification, second Node process or reuse of its output
namespace.

The controller uses the accepted credential/proxy scrubber and fail-closed
network guard. It waits within one fixed 30-second bound, assigns duration in a
`finally` path, terminates only its exact owned process tree when necessary,
and proves process absence. It writes the safe terminal under a fresh exact
evidence directory outside the disposable root, validates it before root
removal, removes only the verified root, and proves root absence.

Canonical evidence must report process counts exactly
`Python controller / copied-package materializer / Node native Harness / broker / worker session / prompt / tool execution / model request / provider request`
as `1/0/1/0/0/0/0/0/0`, plus zero network, Docker and database activity.

## Deterministic admission and verification

Before execution, tests and the controller's `--check` path must prove:

1. every Git binding is a full 40-character commit and an ancestor of HEAD;
2. accepted wrapper, terminal, materialization, package and installed-source
   bytes match their frozen digests;
3. the source copy is Python-only and preserves the accepted package/source
   projection;
4. the authored profile is absent and the pinned source throws before boot and
   HMR for that exact condition;
5. command argv has one wrapper, `--expose-internals`, one exact profile pair
   and no task or surplus argument;
6. wrapper, diagnostic, raw streams and network ledger remain under the exact
   disposable root while the safe terminal remains outside it;
7. one process launch, no retry loop, terminal validation before cleanup,
   duration-before-termination and termination-before-root-removal ordering;
8. canonical v2 identity, relationship and diagnostic coordinates reject
   extra keys, stale paths, wrong identities, noncanonical sidecars, unsafe
   diagnostic coordinates and raw-value retention;
9. attempts 001, 002 and 003 and all accepted predecessor evidence remain
   byte-identical; and
10. focused and predecessor-compatibility tests, Ruff, Python compilation,
    JSON/schema validation, deterministic re-check and `git diff --check`
    pass.

A fresh preexecution Ariadne receipt must repeat the five authority sources,
the exact attempt identity, zero prior canonical output, exact package/source
identity, empty worker slots and the explicit three-lane assessment. Any
failure forbids process start.

## Parallelism assessment

- DeepSeek Flash: `not_applicable`, neutral leverage. The native Harness is the
  provider-free subject under observation; no worker session, model or provider
  request is admitted.
- Gemini 3.7 Flash/high: `not_applicable`, neutral leverage. Provider calls are
  closed and deterministic source, terminal, count and cleanup postconditions
  own acceptance.
- Native subagents: `declined`, negative leverage. Developer policy prohibits
  proactive delegation and package copy, single launch, terminal readback,
  process termination and cleanup are one serial stateful lease.

GPT Sol owns planning, implementation, deterministic admission, the single
process, monitoring, cleanup, acceptance, recovery, clockwork and Git. Reassess
all lanes at plan freeze, preexecution, terminal capture and closeout.

## Explicit non-authority

No broker, worker session, task prompt, tool execution, DeepSeek model or
provider request; no occupied attempt retry, resume, overwrite or
reclassification; no raw historical stream reconstruction or durable raw
stream; no product source/configuration, API, OpenAPI, GraphQL, database,
schema, route, adapter, feature flag, allowlist, action grammar, first-party
client or waiting-area change; no ordinary-practice enablement or generic
status `Arrived`; no product, patient, appointment, clinical, historical or
protected data; no production runtime, deployment, release, Pages, protected
evidence or protected-ref movement.

Local/origin `master` and `handoff/current` remain exactly
`2e34bdad732fdab32fbf778280b3d3c70d66d602`. Preserve `docs/branding/` and
every unrelated untracked file. Stage explicit paths only; `git add .` and
`git add -A` are forbidden.

At closeout the clockwork is the sole canonical governance writer and must run
a separate `--check` before `--publish`, followed by full postpublication and
idempotent verification. Write the paired Yuri summary, send the usual non-PHI
Pushover notification and continue under standing authority.
