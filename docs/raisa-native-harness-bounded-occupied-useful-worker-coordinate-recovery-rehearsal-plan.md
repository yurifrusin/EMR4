# Raisa native Harness bounded occupied useful-worker coordinate-recovery rehearsal plan

Date: 2026-08-22

Status: frozen

Operation: `raisa-native-harness-bounded-occupied-useful-worker-coordinate-recovery-rehearsal`

Reasoning level: high

## Purpose

Run exactly one separately named DeepSeek V4 Flash/high native-Harness worker
turn against the same closed, unmounted and default-off canonical check-in
rollout/kill-switch/rollback runbook candidate used by the consumed predecessor.
The only capability change is the accepted typed tool-result/conclusion
coordinate: the runner marks conclusion after the exact edit boundary passes but
before dispatch, observes the post-execute decision and authoritative final
`tools/result`, and reduces the lifecycle to a closed coordinate.

This is a recovery successor, not a retry or reclassification of the immutable
predecessor. It tests whether the native Harness can complete one useful typed
box-filling task under stronger orchestration. It does not enable ordinary
practice or establish general Harness reliability.

## Accepted input and frozen work package

The accepted provider-free coordinate diagnostic is bound through its
deterministic evidence and exact `future-runner.mjs`. Its source commit and
runner digest are read from those machine-generated artifacts; no abbreviated
or manually recalled Git identity is accepted.

The disposable workspace contains exactly one tracked baseline file:

`docs/api-spine/manifests/canonical-check-in-rollout-kill-switch-rollback-runbook.json`

The baseline, exact required candidate, closed vocabulary, task text and
candidate validator are unchanged from the predecessor. DeepSeek may make
exactly one direct literal `edit` that replaces only `"runbook": null` with the
frozen typed object. It may not read, glob, create, rename or delete a file, and
the resulting bytes are admitted only when they canonicalize to the exact
required candidate.

The required semantics remain:

- `prepared_not_authorized`, with no active or activated stage;
- ordinary activation, feature-flag change, allowlist change and route mounting
  all false;
- kill switch default `engaged`, transition `clear_to_engaged_only`, and unknown
  state never releasing a command;
- unknown commit denies success and blind retry;
- rollback engages containment before source-truth readback and requires zero
  ordinary release;
- non-PHI audit identifiers only, including a full 40-character Git object ID;
- every product, data, runtime, deployment and protected-ref effect false; and
- claim exactly `runbook_contract_present_default_off`.

## Typed runner and terminal

The occupied runner is a mechanically derived instance of the accepted future
runner. It changes only the synthetic target to the exact disposable target,
uses this operation's terminal schema version, and represents failures that
occur before tool lifecycle observation as `tool_lifecycle: null`.

After lifecycle observation, exactly five closed coordinates exist:

- `edit_success_accept_concluded`;
- `edit_success_accept_late_marker`;
- `edit_error_accept_not_concluded`;
- `edit_success_blocked_not_concluded`; and
- `post_execute_decision_failed_not_concluded`.

Only `edit_success_accept_concluded`, one request, one direct edit, one tool
result, `turn_kind: completed`, an exact changed-path inventory and the exact
candidate can pass. Every other coordinate or pre-lifecycle failure is a typed
failed-closed terminal. Raw exception text, prompt, response, reasoning, stream,
session, environment and credential material is not retained.

## Occupied envelope

- provider/model: DeepSeek official / `deepseek-v4-flash`;
- reasoning effort: `high`;
- maximum output tokens: 4,096;
- maximum native Harness processes, agent sessions and turns: one each;
- maximum provider requests and parallel tool calls: one each;
- exposed tools: exactly `edit`, `glob`, `read`;
- admitted model-requested calls: exactly one direct literal `edit` on the
  absolute disposable target;
- upstream ceiling: 300 seconds;
- native-process ceiling: 420 seconds;
- automatic retry, manual retry, resume, fallback, auxiliary model and Claude
  Code fallback: zero; and
- financial ceiling: the user's existing prepaid DeepSeek balance, with no
  broker currency claim and no top-up authority.

The new attempt identity, work order, lease, disposable root and evidence
namespace are disjoint from the consumed predecessor and are written once. The
lease is consumed immediately before launch. A terminal is never retried or
resumed.

The first recovery preparation identity (`coordinate-recovery-001`) was closed
before checkpoint admission when the clockwork detected direct drift on its
canonical surfaces. It consumed no lease, worker or provider authority, its
exact root was removed, and its evidence remains immutable. The occupied
authority is therefore attached only to the fresh disjoint
`coordinate-recovery-002` identity; this is a preparation recovery, not a retry
of an occupied turn.

## Admission and custody chain

Before any native Harness or provider process:

1. the five-source Ariadne receipt passes and names all three lane dispositions;
2. the contract, schemas, derived runner, exact package sources, task and
   baseline/candidate bytes are hash-bound;
3. focused hostile tests, JSON Schema, Ruff, compilation, provider-free command
   admission, broker syntax, latch/Baton checks, exact protected refs, clean
   tracked Git state and disposable-root/process absence pass;
4. a clean exact candidate commit is aligned to its task-branch origin; and
5. the clockwork checkpoint admits the full 40-character candidate and
   checkpoint identities plus the one-request ceilings.

After the one occupied process, the controller terminates the Harness and
broker, removes the exact disposable root, then interprets only hashes, counts,
closed coordinates and any schema-valid canonical candidate retained outside
that root.

If no candidate is admitted, Gemini is not invoked and the typed terminal is
accepted as failed-closed evidence. If a candidate is admitted, GPT Sol first
performs deterministic readback and exact-diff validation, then dispatches one
fresh Gemini 3.7 Flash/high read-only veto from an isolated exact-candidate
worktree. Only a deterministic pass and independent pass permit adoption.

## Frozen historical test exclusion

The live command manifest excludes
`tests/test_deepseek_native_harness_provider_free_tool_result_conclusion_coordinate_diagnostic_recovery_plan.py::test_active_latch_is_the_exact_in_progress_operation`.
That immutable diagnostic assertion correctly binds its then-active latch and
is superseded only by this dependency-satisfied successor latch. The remaining
diagnostic tests stay in the live gate; its accepted artifacts are not changed
or regenerated.

## Parallelism efficacy assessment

- DeepSeek V4 Flash/high: **planned**, positive leverage, owning one closed
  runbook-member fill through the native Harness.
- Gemini 3.7 Flash/high: **reserved**, positive leverage only after a candidate
  is deterministically admitted; it owns a serial independent veto, not
  fallback implementation.
- Native subagents: **declined**, negative leverage because current developer
  policy prohibits delegation and the occupied custody chain is serial.
- Parallel work packages: none. Plan freeze, admission, DeepSeek, cleanup,
  deterministic candidate gate and possible Gemini veto occur in that order.

Reassess at plan freeze, pre-worker dispatch, typed-terminal/candidate gate,
pre-verifier dispatch and closeout.

## API Spine stewardship result

This work produces at most a declarative non-actuating manifest candidate. It
does not add or change REST/OpenAPI commands, GraphQL/read models, schemas,
migrations, routes, identity, idempotency, audit runtime or source-truth
readback. Any future activation remains separately gated.

## Acceptance

Acceptance requires one of two exact outcomes:

- a `useful_worker_pass` terminal with
  `edit_success_accept_concluded`, exact candidate admission, complete cleanup,
  deterministic validation and an independent Gemini pass; or
- a typed failed-closed terminal with complete cleanup, zero retry/resume/
  fallback/auxiliary-model counts and no candidate adoption.

The closeout must record whether this successor reduced ambiguity and rerun
pressure compared with the predecessor. It must use the governance clockwork,
write paired lay/technical summaries to `orchestration/human_inbox/yuri/`, send
the usual non-PHI Pushover notice, and identify the next dependency-satisfied
tranche without moving protected refs.

## Protected boundaries

No product source/configuration, API/OpenAPI/GraphQL, schema, migration, route
or database change is authorised. No ordinary-practice enablement, feature-flag
or authored-synthetic allowlist change, command mounting, generic-status
`Arrived` change, action grammar, first-party client, waiting-area movement,
product/patient/appointment/clinical/historical/protected data, live product
runtime, production, deployment, release or Pages action is authorised.
Protected evidence and protected refs remain closed. Local/origin `master` and
`handoff/current` remain at the exact protected identity recorded by the active
latch and machine-generated receipts. Preserve `docs/branding/` and every
unrelated untracked file. Stage explicit paths only; never use `git add .` or
`git add -A`.
