# Ariadne agent-error register revision 33

Date: 2026-08-06

Status: verifier authentication fallback contained; review worktree restored

## AER-0039 contained

The fresh Gemini 3.6 Flash/high Antigravity veto reached an expired Google
OAuth boundary and timed out before model admission. The candidate remained at
exact `3edbe828fa1f261e59b8478db79d80e4c291cbbc`, tracked-clean, and no
provider/model call or terminal decision occurred. After the bounded retry, the
repository's explicit authentication/transport fallback transferred the same
read-only packet to a fresh independent GPT Sol context. It did not transfer
acceptance, implementation or integration authority.

## AER-0040 corrected

Sol's five-file review reconciliation collected and passed exactly 122 tests.
Sol then incorrectly ran the timestamp-regenerating acceptance command inside
the read-only review worktree. The mandatory Git postcondition caught exactly
two modified generated evidence paths. Both were restored from the exact HEAD;
their Git blob identities matched the commit, the HEAD never moved, and the
worktree was clean before the independent fallback review was admitted.

Review worktrees may now run only non-regenerative tests and static checks.
Acceptance generators run in the primary candidate worktree before freeze;
review verifies their committed outputs through hashes and tests.

Revision 33 contains 40 bounded incidents: 28 agent-behaviour observations,
three harness failures, two repository defects and seven transport timeouts.
No incident remains open. Counts remain workflow-improvement signals only and
do not establish model, provider, transport or role causation.
