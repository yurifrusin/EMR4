Date: 2026-09-09
Timestamp: 2026-09-09T09:52:00+10:00 (Australia/Brisbane)

# G1B.2 controller maintenance contract

This one generation installs the reviewed cumulative-policy correction and an
independently authenticated maintenance route. It does not accept the journal
implementation or reopen product work. Yuri approved opaque Git-index processing
to verify the complete staged tree, without opening sealed files or exposing
individual sealed paths or hashes, and approved bounded activation and recovery
branch publication in advance, subject to implementation, review and verification.

The governing parent is `f726021a71e08f81529d3eecd807a695f7ba7e3a`, tree
`e2d41bc658e836587c34bbff201462f45beca8c3`. The original controller remains
`9334903cbdc7fe04e1c58759ca4babf0fd6d453b`, tree
`b78dd45caadad2bee651aaec6601a9072a7edeaa`. The only publication destination is
`refs/heads/codex/raisa-ariadne-recovery-g0`, with an exact lease on the governing
parent. Protected local/tracking and live remote master/handoff refs remain
`2e34bdad732fdab32fbf778280b3d3c70d66d602`.

## Executable source and admission

The outer operator must authenticate three independent exact inputs: bootstrap
bytes, the complete code-only capsule and the final operation request. The
launcher compiles the same verified bootstrap bytes under Python `-I -B -S`.
The bootstrap validates a closed capsule inventory, its canonical source commit
and tree, and exact request bytes before importing only the verified in-memory
module closure. Target files never provide executable source or acceptance pins.
Installed Python/stdlib, the OS and the selected stock Git are the trusted base;
this is not a complete platform attestation or process sandbox.

Generation `g1b2-controller-maintenance-20260909-v1` uses source commit
`a7b2c3140eb2d9e735494ce7203dee0a0ecf259c`, source tree
`b2496dec67fca6edfb724e9b3eef07fdfc0bce8e`, capsule SHA-256
`1ed7b8919b8d704d8ea84d5fa4a414a30faf19c9c508e197ded7ad8754c4f252`
and bootstrap SHA-256
`3fd9e4bd3bb278a2c8b45b6efb055bd0bd0e5c9aca811c34ce0af87d1db35838`.
The 29 source files comprise 13 explicitly named harness files and 16 pure-Python
YAML files. Historical loader functions remain present as inert lineage; the
maintenance route does not call their complete-tree/configuration discovery.

The external request fixes all roots, preserved source bytes, review subject and
PASS record, owner-approval digest, exact candidate tree, changed/frozen file
digests, original controller identity, remote identity and protected refs. A
matching digest or syntactically valid PASS is insufficient: the outer operator
must bind the actual independent review to this exact subject. Preparation may
stage only explicitly named verified ordinary files; final admission reverses
that complete declared delta in an opaque scratch index and requires the exact
governing base tree. Selected physical bytes must equal staged bytes. No full
physical-worktree cleanliness or absence of unselected files is claimed.

## Effects and uncertainty

Execution requires detached HEAD at the governing parent. It reserves external
activation evidence before effects, then uses the shared exact-index commit
core, shared exact-SHA push core and exclusive receipt core. The sole-parent
commit CAS-updates detached HEAD only. Push uses the fixed destination, exact
result SHA and frozen lease; hooks, follow-tags, signing, recursive submodule
push, lazy fetch and automatic maintenance are disabled. Input identity, index,
selected bytes, source, preserved files, remote and protected refs are checked
around effects. Publication and activation receipts require exact readback.

There is no automatic retry or mid-operation adoption. Failure preserves any
consumed or unfinished receipt. After uncertain publication, observe the exact
remote ref and preserved receipts before deriving any separately reviewed
recovery; never repeat the request blindly. Ordinary source corrections do not
erase failed-attempt evidence.

## Journal successor boundary

The activation binds one journal base equal to the maintenance commit. Both
development-manifest and committed-scope validation call the same base resolver.
Missing or contradictory binding denies admission. This route is addition-only:
both `orchestration_harness/clockwork_journal.py` and
`tests/test_clockwork_journal.py` must be absent from the governing and maintenance
trees. Their frozen byte digests remain respectively
`98686ae95c8477511e0ebee0170e0232ee39c5270b14d1cdcba74def11791b6c` and
`53f9daef9c2051e2422924f179a23bd953dbc3cceedbfe956bf3dd4744620126`.
The complete maintenance delta and frozen authority become immutable history;
the only later current-scope additions are those two journal files.

The runner's immediate activation digest is self-consistency evidence. A later
journal operation must receive an independently authenticated activation digest
from the external operator. Scope validation supplies no operation authority or
journal-review acceptance. Future journal commit/push, replay acceptance and
closeout require their own exact reviewed operation. Historical public
entrypoints retain their original bindings and are not silently redirected.

## Evidence and limitations

The bounded suite has 307 distinct focused passing cases across index observation,
strict records, candidate reversal, activation, shared commit/receipt/operation
cores, isolated bootstrap, request parsing and the native synthetic runner.
These use newly authored tiny Git repositories and local bare remotes. Tests are
serial, exact-module, plugin/conftest-disabled and use external temporary paths.
The journal scope test uses authored marker bytes to exercise the algorithm; it
does not execute or accept the frozen real journal. Earlier six real journal
tests and 341 replay cases are separate preserved evidence.

The archived full suite remains 430 passed, 33 failed, 1 skipped. Historical
whole-tree fixtures and loaders remain quarantined pending a separately scoped
repair; no equivalence or global-green claim is made. Earlier zero-enumeration
claims were withdrawn. Older fixture drafts were preserved externally and
restored to the governing base, excluding them from this publication.

Luna handled bounded mechanics and tests; root checked and corrected its output.
Terra supplies independent native review; root owns acceptance and execution.
DeepSeek and Gemini provider lanes remain closed. This maintenance creates no
provider, product, real-data, deployment, Pages, G1C or protected-ref authority.

Operational source, review, request and result evidence is retained outside the
target at `C:/Users/sarashera/EMR4-operation-receipts/g1b2-maintenance-20260908/`.
This contract describes admission requirements; it is not a completion receipt.
