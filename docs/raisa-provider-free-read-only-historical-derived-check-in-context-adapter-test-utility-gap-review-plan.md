# Raisa provider-free read-only historical-derived check-in-context adapter-test utility gap review plan

Date: 2026-08-24

Timestamp: 2026-08-24T15:01:18.0384025+10:00 (Australia/Brisbane)

Status: `frozen_narrow_read_only_plan`

Operation: `raisa-provider-free-read-only-historical-derived-check-in-context-adapter-test-utility-gap-review`

## Objective

Read only the committed sanitised occupied result, its accepted efficacy
reading and the accepted repository implementation/test source. Distinguish
traceable provenance influence from independently selected adapter behavior,
identify the smallest honest utility gap, and freeze a minimum authored-
synthetic temporal scenario-axis contract before any further historical-
derived use is considered.

This tranche does not reopen or consume the ignored fixture, its terminal
local control, the historical archive or any other `local_data` material.

## Authority and exact baseline

- The five authoritative rehydration sources are
  `live_handover_current_baton`, `current_authority_allocation`,
  `active_plan_and_acceptance`, `protected_evidence_boundaries` and
  `git_refs_and_worktree`.
- The active-operation latch is continuity evidence and remains `in_progress`
  for this operation.
- The task-branch baseline is exact full Git source
  `7f7822bccc2bb22a682cf54cd5a787d8739ffc9f`.
- Local/origin `master` and `handoff/current` must remain exact
  `2e34bdad732fdab32fbf778280b3d3c70d66d602`.
- The accepted occupied adapter-test candidate is
  `517fda26c5c7f46397acc91976bc97b0be3778ef`; the accepted sanitised closeout
  source is `6a1de186f096636402d50a89feae6a81953cd862`.
- The occupied result SHA-256 is
  `48f12096fc319a896b7ef70e8dddbd99f8ea3151aa90212cfb030d710d5dad71`.
- The current accepted adapter source remains
  `c82c3a741053a9c8da260aa62e1a968af22bb54e`, with exact Git blob
  `6955dec2e31e14c0ae4847acba22f9fb0087715b`.

## Read allowlist

Only these tracked repository paths may supply review facts:

1. the accepted `report.md`, `efficacy-reading.json`, `occupied-result.json`
   and `next-tranche-contract.json` under the immediately preceding tranche;
2. `orchestration_harness/historical_diary_check_in_adapter_test_consumption.py`;
3. its focused repository test;
4. `app/services/appointment_check_in_product_adapter.py`;
5. the accepted unmounted adapter test and default-off route-convergence test;
6. this plan, its threat-model delta, AGENTS.md, the active latch and generated
   review artifacts.

No ignored fixture, local control, filename inventory, archive, mapping, token,
key, raw row or `local_data/**` path may be opened, listed, searched, hashed or
used as input.

## Closed interpretation vocabulary

Each admitted structural measurement must receive exactly one utility label:

- `digest_only_provenance`: it changes the one-way synthetic namespace but
  does not independently select an adapter input, branch or expectation;
- `synthetic_time_parameter_only`: it changes injected synthetic time but does
  not independently select an adapter branch; or
- `independent_behavior_selector`: it directly changes an admitted adapter
  input and expected branch.

The review must separately classify each exercised adapter branch as
`historical_derived_incremental_coverage` or `already_covered_product_contract`.
Digest-derived identities, evidence and idempotency material are provenance
bindings, not behavioral coverage.

## Expected evidence join

The deterministic review must test, rather than merely narrate, the following
source-backed propositions:

- all six admitted structural measurements are present in the sanitised
  result;
- `relative_minute_span` alone directly adjusts the injected synthetic
  `now` value;
- the canonical digest influences synthetic identifiers, evidence and
  idempotency material;
- event count, distinct-minute count, event-kind count, subject-slot count and
  resource-slot count do not independently select the appointment state,
  waiting-area mode, authority, evidence verdict, idempotency outcome,
  transaction outcome or readback outcome;
- the occupied run selects only `Booked`, no supplied or preserved waiting
  area, active same-practice Receptionist, valid freshness/evidence, started
  claim, successful commit and matching readback; and
- that atomic success is already covered by the accepted product-adapter suite,
  which also covers `Confirmed`, assign/preserve waiting areas, replay,
  authority/evidence/freshness rejection, idempotency stops, transaction
  failures and outcome-unknown states.

## Minimum authored-synthetic successor axes

If the expected join passes, the review will freeze a successor contract for
time-ordered authored-synthetic scenarios, not another digest-decorated atomic
success. The minimum axis families are:

1. `source_and_waiting_area_transition`: Booked/Confirmed with none, valid
   assignment and valid preservation, plus a later state or area-topology
   change that must fail closed;
2. `authority_evidence_and_freshness_transition`: unchanged authority and
   evidence versus actor revocation, evidence invalidation or stale proposal
   after an intervening synthetic update; and
3. `idempotency_and_outcome_transition`: first execution, exact replay,
   conflicting/in-progress execution, precommit failure, commit-outcome unknown
   and committed-readback unavailable.

The successor may compose a minimal pairwise set across these families. It may
not claim these axes occurred in the historical trove, infer their frequency,
or derive product truth from structural counts.

## Deliverables

1. One closed JSON review contract and one standard-library deterministic
   reviewer restricted to the exact read allowlist.
2. One generated JSON evidence artifact and paired Markdown report using only
   the closed vocabularies above.
3. One frozen authored-synthetic successor-axis contract with no authority to
   execute it or consume further historical material.
4. Focused tests for exact bindings, influence classification, no-`local_data`
   access, fail-closed mutations and the claim ceiling.
5. Sol acceptance, paired Yuri lay/technical summary, clockwork closeout and a
   non-PHI Pushover continuation notice.

## Parallelism assessment

- DeepSeek: declined, negative leverage. The native lane remains paused and
  this tranche is conductor-owned acceptance-semantic analysis rather than a
  separable mechanical implementation package.
- Gemini: not applicable, neutral leverage. Exact repository bindings and a
  closed deterministic reading decide this read-only review; reassess only if
  contradictory evidence creates a genuine independent-veto surface.
- Native subagents: declined, negative leverage. The review has one tightly
  coupled interpretation owner and no independently writable product package.
- GPT Sol owns the serial review. Reassess on deterministic failure, before any
  worker dispatch or at the next named tranche boundary.

## Acceptance

- Every Git object binding is a machine-resolved lowercase full 40-character
  ID; no abbreviation is admitted.
- The exact occupied result, accepted source blobs and closed structural values
  match.
- Every structural value receives exactly one admitted utility label and every
  exercised adapter branch receives exactly one admitted coverage label.
- No digest-only influence is counted as behavioral coverage.
- The review identifies zero new product rules and no more than the three
  minimum authored-synthetic temporal axis families above.
- Focused deterministic tests, Ruff, compileall and the governance profile
  pass.
- No historical/local fixture, control or archive access; no provider, model,
  network, product data, database, route invocation, client, runtime,
  configuration or ordinary-practice activation.
- No production, deployment, release, Pages, protected evidence or protected-
  ref movement; `docs/branding/` and every unrelated untracked file remain
  preserved; staging is explicit-path only.

## Stop conditions

Return `revision_required` if any exact binding, structural value, closed
classification, source assertion, protected ref or no-access boundary differs.
Return `blocked` if a required conclusion would need fixture, control, archive,
provider, runtime or product access. A mismatch cannot be repaired by
reopening historical data, weakening a check or replacing a closed label with
free-form interpretation.
