# EMR4 model-required Bureau A3/B3 preflight-blocked checkpoint

Date: 2026-08-04

Result: `model_required_bureau_a3_b3_adc_preflight_blocked`

## Completed readiness

The exact A3/B3 authored-synthetic occupied advisory boundary is frozen to
Vertex AI `gemini-2.5-flash`, project `bernie-emr4-dev`, the named Bernie
service account, existing keyless impersonated ADC and
`australia-southeast1`. Provider-free contracts, broker/cell isolation,
selector-only candidates, deterministic proofreading, fixed-clock freshness,
exact observed model identity, disjoint evidence paths, one-use/cost limits,
no-fallback policy and cleanup checks pass.

Fresh Gemini 3.6 Flash/high source review 4 passed unchanged source
`c95a0ee1a235843fe8498a8979e43151f7f4277e` with no findings. It was a
source-only development review and not an A3/B3 candidate-runtime call.

## Exact occupied stop

The first occupied harness launch reserved USD 0.25 for Rayleen primary turn
1, then its read-only cloud-control preflight stopped at
`impersonated_adc_refresh_failed`. It occurred before prompt transmission,
provider contact, model inference, attempt-ledger creation or cell/container
startup. Candidate-runtime provider-call count is zero. No product/database
read, command, write, event, actuator, cloud/IAM mutation, deployment, release,
Pages or protected-ref movement occurred.

The original parent cost ledger is preserved open with exactly one Rayleen
reservation, zero consumed calls and SHA-256 bound by
`occupied-preflight-blocked-evidence.json`. No A3/B3 container, network, image,
broker process, run lock or runtime temporary artifact remains.

## Harness correction

AER-0016 records that the original entry guard could not resume that exact open
reservation. The corrected harness admits only the canonical ledger and closed
blocked receipt when their hash, one-reservation/zero-call state and absent
attempt artifacts all match. It then reuses Rayleen's existing reservation
rather than allocating a second one. Deterministic tests prove two successful
primary turns would finish with two reservations, not three.

The changed recovery source requires a fresh exact-HEAD Gemini 3.6 Flash/high
source veto before it may resume.

## Required user action and next safe step

Yuri must restore the already approved Bernie keyless impersonated ADC through
the external interactive credential workflow. Codex is not authorised to
revoke, replace, recreate or reconfigure it.

After Yuri reports restoration, Sol must repeat five-source rehydration, verify
the exact task/protected refs and recovery receipt, rerun the read-only cloud
preflight against the unchanged same-reservation continuation, and continue
the serial occupied A3/B3 turns only if every exact control passes.

This checkpoint is not an A3/B3 pass and proves no model result, provider
quality, product behavior, real-data safety, Australian physical or sovereign
processing, production suitability or effect authority.
