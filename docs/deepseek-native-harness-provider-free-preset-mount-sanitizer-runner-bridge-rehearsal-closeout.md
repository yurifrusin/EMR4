# DeepSeek native Harness preset-mount sanitizer runner-bridge rehearsal closeout

Date: 2026-08-22

Timestamp: 2026-08-22T06:36:06.5611095+10:00 (Australia/Brisbane)

Status: **accepted deterministic bridge pass**

Reasoning level: **Extra High**

## Result

At exact execution candidate `993a3b383aa79afba857bb53af29177bffacd566`,
one provider-free pure Node fixture process proved the exact runner-bridge
composition. The process emitted the frozen eight-result vector: one success
and one stage/code/null-detail result for each of the seven admitted
preset-mount codes. It exited zero with zero stderr.

The controller retained a content-free process envelope before parsing stdout.
It records 1,089 stdout bytes and zero stderr bytes with SHA-256 digests, but no
stream content or environment values. The one-process latch is consumed and no
retry is authorised.

## Accepted control gain

- The admitted sanitizer is imported and called exactly once inside a pure
  mount wrapper.
- The wrapper owns exactly one `await mount(agentCtx, presetId)` boundary and
  releases only `{stage, code, detail}` with `stage: preset_mount` and
  `detail: null`.
- The accepted effective-tool guard is deterministically derived to pass its
  exact native mount call through that wrapper.
- The accepted runner is deterministically derived to give the preset-mount
  terminal precedence over its retained broader composition fallback.
- Thirteen exact source invariants pass; the derived runner and guard are
  SHA-256 bound.
- DSH imports, native Harness processes, workers, turns, model/provider
  requests, network, database, Docker, target and product activity are zero.

This admits the source bridge and its pure fixture. The derived runner itself
was not executed, and no native-Harness or DeepSeek capability is inferred.

## Verification

Ten focused tests and the exact 90-test selected current/inherited collection
pass after execution. Deterministic `--check`, both JSON schemas, Ruff,
`py_compile` and Git whitespace checks also pass without a second Node process.

Three low-severity workflow incidents were contained: an unregistered
continuation-event shorthand rejected by preflight, another logical-directory
guess corrected by repository inventory, and a phase-blind test assertion
corrected to validate both fresh and accepted artifact states. They are
recorded as AER-0895 through AER-0897.

## Parallelism disposition

- DeepSeek: declined because its runner was the governed object and no worker,
  model or native-Harness process was authorised.
- Gemini: declined because hashes, source coordinates, grammar and fixture
  vector were deterministic and produced no semantic ambiguity.
- Native subagents: declined because delegation is prohibited and the process
  budget was serial.
- GPT Sol owned implementation, the one pure fixture, readback and acceptance.

## Next tranche

Proceed with one separately frozen provider-free preset-mount sanitized-terminal
native rehearsal. It may materialise the exact admitted runner, guard, bridge
and sanitizer into the accepted disposable rc.7 package-only boundary and run
one native process with zero turns and zero model/provider requests. Every
finite result is typed; no retry follows.

## Boundaries

No occupied worker, model/provider request, retry, target edit, product/data/
configuration/API/database/route/adapter/flag/allowlist/grammar/client/waiting-
area change, ordinary-practice enablement, generic-status `Arrived` change,
production, deployment, release, Pages, protected evidence or protected-ref
movement is accepted.
