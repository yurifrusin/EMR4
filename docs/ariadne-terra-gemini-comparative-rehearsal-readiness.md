# Ariadne Terra/Gemini Comparative Rehearsal — Credential-gate Readiness

Date: 2026-07-24
Interim result: `ariadne_terra_gemini_comparative_rehearsal_credential_gate_pending`
Reasoning level: high
This is not tranche closeout or model-attempt consumption.

## Completed

- mandatory five-source Ariadne rehydration and aligned protected-ref check;
- frozen provider-neutral comparative plan, protocol design, and threat delta;
- one identical authored-synthetic six-frame cognition task;
- one common provider schema plus the unchanged exact full output schema;
- direct one-use Terra Responses API broker;
- direct one-use Gemini Developer API `generateContent` broker;
- provider-neutral fixed work-cell launcher with no SDK, CLI, tool, provider
  identity, provider key, repository mount, or product access;
- 14/14 focused tests, lint, JavaScript syntax checks, and provider-free
  real-isolation preflight;
- both lane skeletons inspected as non-root, read-only, capability-dropped,
  resource-bounded, zero-published-port containers;
- exact preflight container/network cleanup.

The selected broader Ariadne regression population passes except for one
pre-existing DeepSeek historical-evidence source-hash assertion. The three
tracked DeepSeek sources and its evidence are outside this tranche and were
not changed.

## Credential gate

At preflight:

- `OPENAI_API_KEY` present: false;
- `GEMINI_API_KEY` present: false;
- credential values recorded: false;
- provider calls performed: zero;
- prompts transmitted: zero.

The authorised live command returned
`both-provider-credentials-required-before-consumption`. Both single-use
ledgers remain `available`; no retry or fallback authority has been consumed.

## Prepared runtime state

Four lane-specific Docker tags are prepared from two credential-free image
identities. There are no Ariadne comparison containers or networks left
running. The tags are inputs for the eventual one-shot run and will be removed
by lane cleanup.

## Unresolved gate and next action

Both API keys must be made available as process or user environment variables
without placing them in the repository or conversation. After both presence
checks pass, rerun the static/preflight receipt, execute Terra once, verify
exact Terra cleanup, execute Gemini once, run the full-schema and deterministic
proofreader gates, persist sanitised comparison evidence, and complete
independent review, closeout, baton update, and protected Git integration.

No model substitution, Vertex fallback, Antigravity transport, retry, provider
diagnostic call, or single-lane consumption is authorised by this readiness
record.
