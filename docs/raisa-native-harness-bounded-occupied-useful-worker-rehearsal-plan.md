# Raisa native Harness bounded occupied useful-worker rehearsal plan

Date: 2026-08-22

Timestamp: 2026-08-22T12:50:14.0608610+10:00 (Australia/Brisbane)

Status: frozen

Operation: `raisa-native-harness-bounded-occupied-useful-worker-rehearsal`

Reasoning level: high

## Purpose

Use the accepted native-Harness guard, bridge, sanitizer, broker and complete
runner lineage for exactly one useful low-risk Raisa development turn. The
worker will fill one declarative, unmounted and default-off API Spine runbook
contract for canonical check-in rollout, kill-switch and rollback. It receives
no acceptance, activation, product-runtime or integration authority.

This closes only the admission-readiness review's missing runbook-contract
dimension. It does not prove operational rollout, enable ordinary practice or
close the separate non-PHI observability, tenant-role, unknown-commit exercise,
environment or secret-posture gaps.

## Frozen useful work package

The disposable workspace contains exactly one tracked baseline file:

`docs/api-spine/manifests/canonical-check-in-rollout-kill-switch-rollback-runbook.json`

The baseline freezes the schema, command identity and unchanged default-off
posture, with `runbook` set to `null`. DeepSeek may make exactly one direct
literal `edit` replacing that one member with a typed object. It may not read,
glob, create, rename or delete any file, and it may not change bytes outside the
single `runbook` member.

The resulting object must use only the frozen closed vocabulary and satisfy all
of these invariants:

- rollout status is `prepared_not_authorized` and no active/activated stage is
  represented;
- ordinary activation, feature-flag change, allowlist change and route mounting
  are all false;
- the kill switch defaults to `engaged`, moves only `clear_to_engaged`, and
  never releases an unknown state;
- unknown commit denies success and blind retry;
- rollback engages the kill switch before source-truth readback and requires
  zero ordinary release;
- audit metadata is non-PHI, names only the frozen identifier classes and
  requires a full 40-character Git object identity;
- every product, runtime, data, deployment and protected-ref effect remains
  false; and
- the claim is only `runbook_contract_present_default_off`.

GPT Sol owns the schema, validator, runner/controller, provider-free tests,
preexecution admission, terminal reduction, candidate readback and adoption or
rejection. A passing candidate is retained outside the disposable root only as
the exact schema-valid JSON file and its digest. Raw prompt, response, reasoning,
stream, session, environment and credential material are destroyed.

## Occupied envelope

- provider/model: DeepSeek official / `deepseek-v4-flash`;
- reasoning effort: `high`;
- maximum output tokens: 4,096;
- maximum native Harness processes, agent sessions and turns: one each;
- maximum provider requests: one;
- maximum parallel tool calls: one;
- allowed model-facing tools: exactly `edit`, `glob`, `read`;
- admitted model-requested calls: exactly one direct literal `edit` on the
  absolute disposable target;
- upstream ceiling: 300 seconds;
- native process deadline: 420 seconds;
- automatic/manual retry, resume, fallback, auxiliary model and Claude Code
  fallback: zero; and
- financial ceiling: the user's existing prepaid DeepSeek balance, with no
  broker currency claim and no top-up authority.

The worker is launched only after a clean exact candidate commit is aligned to
its origin branch, a machine-resolved full-object deterministic-admission
receipt passes, one fresh disjoint work order/lease/root/evidence namespace is
prepared, and the clockwork checkpoint binds the package and request ceilings.

## Derived occupied runner

The occupied runner is derived from the accepted complete runner lineage but
changes exactly the capability needed for useful work:

1. retain the exact accepted preset-root, preset-roster, effective-tool,
   sanitizer, model-selection and empty-registry preconditions;
2. permit the factory commit instead of throwing the provider-free publication
   veto;
3. require the agent/session publication to the process-local registries;
4. send the one frozen task through the loopback broker;
5. admit one direct literal edit and conclude the turn immediately after its
   accepted result;
6. require one request, one edit call/result and one completed turn;
7. emit only a typed terminal with digest/count/closed-coordinate evidence; and
8. exit, terminate broker/Harness, retain only the admitted candidate, and
   remove the exact disposable root.

The accepted prepublication-veto runner remains immutable. This is a fresh
derivative for the occupied attempt, not a reclassification or overwrite.

## Terminal classes

- `useful_worker_pass`: one completed request/edit, schema-valid candidate,
  exact closed semantics, complete cleanup and Sol adoption after readback;
- `useful_worker_candidate_rejected`: the Harness turn completed but the target
  is missing, structurally invalid, outside the vocabulary or otherwise fails
  the deterministic contract;
- `useful_worker_transport_terminal`: the one native Harness turn ends before a
  valid candidate; or
- `useful_worker_preexecution_rejected`: deterministic admission fails before
  the lease is consumed.

A consumed occupied terminal is never retried or resumed. Any successor is a
separately frozen recovery or next product tranche justified by the exact
terminal.

## Acceptance

Before the occupied process:

- the fresh five-source Ariadne receipt passes with DeepSeek planned, Gemini
  declined and native subagents declined;
- the API Spine review records this as a declarative non-actuating manifest,
  not a REST/OpenAPI command mutation or GraphQL surface;
- contract/schema/runner/controller hashes and the exact target baseline are
  bound;
- focused hostile tests, Ruff, Python compilation, JSON Schema, broker checks,
  latch/Baton checks, protected refs, process/root absence and Git hygiene pass;
  and
- the candidate commit and origin task branch are exact and clean apart from
  preserved unrelated untracked paths.

After the occupied process:

- the attempt identity is consumed once, with no retry/resume/fallback;
- the broker records at most one started and one completed provider request;
- raw streams are reduced to byte counts and hashes only;
- the exact disposable root and every owned process are absent;
- any retained candidate is valid JSON, schema-valid, non-PHI and bound by
  digest before Sol reads it;
- Sol independently reviews the candidate and either adopts it at the exact API
  Spine manifest path or records rejection without product mutation; and
- focused and broader deterministic checks, Git whitespace, clockwork closeout,
  paired Yuri summary and non-PHI Pushover notification complete.

## Frozen historical test exclusions

The active command manifest does not run these historical equality assertions:

- `tests/test_api_spine_status_confirm_idempotency_preflight.py::test_current_confirm_routes_supersede_sprint_136_header_exclusions`
  still requires the pre-adapter delete route's literal
  `claim_appointment_command(` token; accepted delete-confirm route convergence
  superseded that source shape without changing this frozen witness.
- `tests/test_raisa_provider_free_read_only_ordinary_practice_check_in_admission_readiness_review_plan.py::test_plan_and_contract_bind_all_28_exact_inputs`
  binds the 18 August review's exact 28-file snapshot, including an earlier
  A5.1 test hash.
- `tests/test_raisa_provider_free_read_only_ordinary_practice_check_in_admission_readiness_review_plan.py::test_current_latch_and_five_source_receipt_are_exact`
  binds that review's then-current latch operation and boundary vocabulary.

Their frozen artifacts must not be regenerated or weakened for this descendant.
The current API Spine artifact suite, current latch/Baton suites, accepted
admission-control kernel tests and this tranche's focused hostile tests remain
in the live gate.

## Parallelism assessment

- DeepSeek V4 Flash/high: **planned**, positive leverage. It owns exactly the
  one-member declarative runbook fill in one native-Harness turn.
- Gemini: **declined**, negative leverage. The active occupied envelope forbids
  an auxiliary model; GPT Sol supplies independent readback and acceptance.
- Native subagents: **declined**, negative leverage. Current developer policy
  prohibits delegation and the prepare/checkpoint/one-turn/cleanup/readback
  sequence is serial.
- GPT Sol owns meaning, controls, deterministic verification, adoption,
  acceptance, Git and closeout.

## API Spine stewardship result

The runbook is a declarative control manifest. It cannot invoke a route,
mutate GraphQL or OpenAPI, confer identity/role authority, store secrets or
execute rollback. Any future command remains practice-scoped REST/OpenAPI with
server-owned identity, confirmation, idempotency, freshness, audit and source-
truth readback. This tranche adds no runtime mount or release gate.

## Protected boundaries

No ordinary-practice enablement, feature-flag change, authored-synthetic
allowlist change, command mounting, generic-status `Arrived` change, action
grammar, first-party client, waiting-area movement, product/patient/appointment/
clinical/historical/protected data, live product runtime, production,
deployment, release, Pages, protected evidence or protected-ref movement is
authorised. Local/origin `master` and `handoff/current` remain exactly
`2e34bdad732fdab32fbf778280b3d3c70d66d602`. Preserve `docs/branding/` and every
unrelated untracked file. Stage explicit paths only; never use `git add .` or
`git add -A`.
