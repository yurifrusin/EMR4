# Reception One Receptionist-first v6.5 Semantic-role Plan

Status: active bounded repair plan
Recorded: 2026-07-30

## Purpose

Repair the six v6.4 no-release terminals without revising any historical node:

1. preserve recognized exact constraints by typed semantic argument role
   anywhere in the normalized plan, rather than demanding the deterministic
   reference's operator sequence; and
2. make the single proofreader correction turn genuinely informative by
   naming the required goal in the existing typed correction feedback, while
   teaching the exact clarification desk form.

The complete original twenty-four-request authored-synthetic cohort will be
rerun, including every v6.0 pass. v6.4 remains immutable and no v6.4 ledger
may be reopened.

## Exact repair

For every independently recognized actionable request, the proofreader derives
the unique set of `(argument, source kind, field)` constraints from the
deterministic semantic adapter. The model-authored normalized plan must contain
each tuple in some compatible typed operator input. This preserves exact date,
time, duration, status and selected-appointment facts while allowing a shorter
or novel coherent operator composition.

If a typed goal or its natural receptionist channel disagrees with the
deterministically recognized goal, the correction ticket uses its existing
bounded `allowed_output_names` field to name that required goal. No prose,
draft payload or provider response enters the ticket.

The prompt will explicitly teach:

- appointment details without an action request require clarification;
- ambiguous “fit in” language must distinguish an ordinary booking from an
  actual squeeze-in request under the frozen policy; and
- a clarification response asks one focused question, explains the ambiguity
  briefly and states that no booking was changed.

The output schema, typed operator catalogue, desk context, 3072-token response
ceiling, temperature zero and 1024-token thinking budget remain unchanged.

## Frozen boundaries

- provider: Google Cloud Vertex AI;
- model: `gemini-2.5-flash`;
- project: `bernie-emr4-dev`;
- identity:
  `emr4-bernie-ai-dev@bernie-emr4-dev.iam.gserviceaccount.com`;
- authentication: existing keyless impersonated service-account ADC;
- location: `australia-southeast1`;
- endpoint: `australia-southeast1-aiplatform.googleapis.com`;
- data: authored-synthetic only;
- 24 primaries and at most one terminal second call per case;
- absolute 48-call and USD 1 ceiling;
- no API key, static key, global endpoint, fallback, provider tool, grounding,
  retrieval, cache creation, product/database access, appointment write,
  product delivery, production, deployment or release; and
- raw prompts, raw provider responses, credentials, API-key information and
  hidden chain-of-thought are not retained.

The no-show-to-`dna` alias remains closed.

## Deterministic gates

Before an occupied call:

1. all twenty-four reference forms pass the semantic-role constraint review;
2. the four exact v6.4 direct resize/cancel terminal forms admit and produce
   their frozen exact proposals;
3. a corrected 3:00 PM form missing either exact time bound still rejects;
4. a goal-mismatch ticket names the required goal and no rejected prose;
5. clarification form/natural-channel fixtures pass for the two v6.4 cases;
6. all historical wrong forms remain rejected;
7. provider-blocked, real-isolation, focused, API Spine, Continuity, Compass,
   JSON, compilation, Ruff and whitespace gates pass;
8. the exact read-only Bernie/Sydney cloud-control preflight passes;
9. pre-run residue is zero; and
10. Continuity and rendered Compass revisions bind the frozen candidate.

Once occupied execution starts, no prompt, schema, proofreader, desk-context or
generation-setting change is permitted.

## Acceptance

Capability acceptance requires all twenty-four terminal outcomes to match
their frozen oracle, all ledgers and audit chains to close, no call after the
terminal result and zero task residue. Any mismatch closes v6.5 candidly and
releases no product capability.

The evidence remains a reused development cohort, not an independent holdout.
The Sydney locational path does not prove Australian physical or sovereign
processing.
