# Preset-mount effective-tool projection package-materialisation recovery

Status: frozen recovery addendum

Date: 2026-08-20

Operation: `raisa-provider-free-check-in-native-harness-preset-mount-effective-tool-projection-rehearsal`

## Immutable failed attempt 002

Attempt `check-in-preset-mount-effective-tool-native-002` passed its exact
checkpoint but failed before checkpoint consumption and before native Harness
launch. The accepted offline npm materialiser exceeded its bounded timeout and
left one package-install child holding the disposable `installation` directory.
The controller then exposed a cleanup exception before terminal publication.

Sol identified and terminated only the exact package PID whose command line was
bound to the disposable root, verified the resolved root remained under
`C:\Users\sarashera\EMR4-worktrees`, and removed that exact temporary root. No
native Harness process, agent, session, turn, broker, model, provider, network,
Docker or database action occurred. No raw log was retained. The exact bounded
record is `native-attempt-002-prelaunch-failure.json`.

Attempt 002 is closed and must not be rerun, resumed or reclassified.

## Narrow correction

Attempt `check-in-preset-mount-effective-tool-native-003` is a new identity,
not a retry. It replaces only the non-semantic package materialisation step:

1. read the already accepted provider-free occupied-rehearsal `node_modules`
   source at the frozen external root named in the contract;
2. bind its root plus `package.json`, `package-lock.json` and installed lock
   digests, and revalidate the six critical rc.7 packages and installed source;
3. copy only that package tree into the disposable installation, using no npm,
   package subprocess, network, model or provider action;
4. revalidate the copied critical packages and installed source before proof
   materialisation;
5. retry Windows root removal only inside a bounded cleanup loop and publish a
   sanitized failed terminal if cleanup still cannot complete.

The materialisation source is non-product, non-PHI Harness package content that
already belongs to the accepted native-Harness evidence lineage. Product,
patient, clinical, ordinary-practice, provider, deployment, Pages and protected
ref boundaries remain unchanged. Attempt 003 requires a fresh deterministic
ledger, exact-candidate Gemini veto, exclusive checkpoint and five-source
preexecution receipt. This addendum itself launches no process.
