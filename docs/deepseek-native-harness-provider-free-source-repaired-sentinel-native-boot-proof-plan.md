# DeepSeek native Harness provider-free source-repaired sentinel native-boot proof plan

Date: 2026-08-21
Timestamp: 2026-08-21T16:12:26.7707451+10:00 (Australia/Brisbane)
Status: `frozen`
Reasoning level: `high`
Planning source: `1283481f8bfdda0a53027aa04eb48539dcd8e874`

## Objective

Run exactly one fresh pinned `@deepseek-ai/dsh@0.1.0-rc.7` Node/native-Harness process with the accepted source-repaired sentinel and unchanged sentinel-only initial headless profile. A pass requires exactly `sentinel_activated` followed by `stock_headless_hmr_ready`; any other post-launch result becomes one sanitized `failed_closed` terminal and cannot be retried.

This proves only sentinel loading and stock-headless HMR readiness. It does not materialize the changed runner, create a broker, worker, task or session, send a prompt, call a model/provider or establish DeepSeek development reliability.

## Frozen lineage and identity

- accepted source-repair candidate: `eb8913aacb19d823e251731f9393cc54fe71524c`;
- accepted source-repair clockwork source: `1283481f8bfdda0a53027aa04eb48539dcd8e874`;
- accepted preactivation diagnosis source: `1609d7fa2c505fbd3f4f28a1a7f1f823356059d3`;
- source-repaired controller SHA-256: `e64b6c7f6b13bae69dd910963620e03e292b5262c5b05029305d6097f3e6191b`;
- source-repair evidence SHA-256: `a5e57b8ed75b388c3fb105e772ee241bb16f66f3ef23df82dd3870f98a1354ac`;
- fresh attempt ID: `source-repaired-sentinel-native-boot-attempt-001`.

The consumed earlier repaired-sentinel attempt and all authored-synthetic attempts remain immutable. The new operation has distinct canonical outputs and a distinct disposable-root prefix.

## Exact construction and launch

Before the sole process start, the controller must validate the contract, full-hash ancestry, exact components, the one-byte repair evidence and zero-activity proof. It copies the accepted rc7 package tree with Python filesystem operations only, materializes the existing bounded preset and unchanged initial headless profile, writes only the source-repaired `installation/proof/sentinel.mjs`, rejects any runner row/file, scrubs credential/proxy names and preloads the accepted network-denial guard.

The sole command is:

`node --expose-internals <pinned rc.7 lib/bin.js> --profile headless`

There are no task arguments. The first `subprocess.Popen` exclusively consumes the attempt. The controller polls only the bounded event ledger, terminates the exact process after readiness or failure, retains only stream byte counts/digests, deletes the raw streams/environment/copied package tree/disposable root and proves process/root absence.

## One-run latch

- Node/native-Harness process limit: one;
- automatic retry, manual retry, resume, fallback and reclassification: false;
- changed-profile writes, runner, broker, worker, prompts, tools, model/provider requests, network attempts, Docker and database invocations: zero;
- raw stdout/stderr, messages, paths, stacks, environment and session state retained: false.

Construction defects found before `Popen` may be corrected and rechecked. After the first `Popen`, no second process is authorised under any terminal.

## Deterministic admission and acceptance

Before execution, the committed candidate must prove exact source/component bindings, rc7 identity, Python-only materialization, one sentinel row, zero runner rows/files, exact five-argument command, one `Popen`, no retry loop, closed event/failure vocabularies, exclusive consumed/terminal outputs, simulated terminalization/cleanup and zero Node/Harness activity during checks.

Accept `pass` only for the exact two-event sequence, one started process, controller termination after readiness, zero network/provider/worker counters and complete cleanup. A sanitized `failed_closed` terminal remains an accepted one-attempt result but is not a passing boot and grants no retry.

## Parallelism assessment

- DeepSeek Flash: declined with negative leverage; any worker/model/provider call breaches the sentinel-only claim.
- Gemini 3.7 Flash/high: declined with negative leverage; provider review is outside the one-process envelope.
- Native subagents: declined with negative leverage; current developer policy prohibits proactive delegation and the consumed process lease has one serial owner.
- GPT Sol owns the frozen contract, deterministic admission, sole process, terminalization, cleanup and acceptance.

## Protected boundaries

No runner, task, broker, worker, model, provider or network execution; no retry/resume/fallback/reclassification/second process; no product/configuration/API/database/route/adapter/flag/allowlist/grammar/client/waiting-area change; no ordinary-practice enablement; no product, patient, appointment, clinical, historical or protected data; no production runtime, deployment, release, Pages or protected-ref movement. Local/origin `master` and `handoff/current` remain fixed at `2e34bdad732fdab32fbf778280b3d3c70d66d602`. Preserve `docs/branding/` and every unrelated untracked path; stage explicit paths only.
