# Bernie LC4V4D4 Composed Integration Closeout

Date: 2026-07-16

LC4V4D4 is complete with `versioned_composed_integration_valid` evidence.
Legacy remains the exact default across the frozen 60-probe composed baseline,
while explicit Option A now travels through the ordinary typed interpretation
and replay harness without rewriting pure utterance semantics.

All 20 approved Option A cases pass over 40 complete observations with zero
variance and exact category counts `5/2/1/2/5/5`. All 13 gates pass. The report
hash is
`sha256:dd1ecc077a59bf05e777eda1f3a5450c0a1b97a4c8a3fd21dc0363d473abd653`.

Flash's `candidate_complete` evidence was rejected for conceptual fail-open
checks. Sol recovered without a correction loop, and a 329-node selected serial
preservation gate passed. Gemini 3.5 Flash/medium independently reproduced the
focused evidence and returned `DECISION: pass` on exact head `bd51caf0`.

The exact six incompatible D1 expectations remain historical and unchanged;
their Option A differences are now explicit, typed, and versioned. The one
deselected LC3 committed-report equality node also remains immutable and was
proved pre-existing on the D4 contract baseline.

D4 remains development-only. Holdouts v1-v4 are sealed; T3.1-T3.4 remain
blocked, and T3.5/providers/product/write surfaces remain deferred. The next
safe ordinary work is a bounded development-wide Option A adoption audit over
all 60 probes. Any later certification holdout or reuse policy requires Yuri's
separate approval.
