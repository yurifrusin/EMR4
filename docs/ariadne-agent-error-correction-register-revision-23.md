# Ariadne agent-error register revision 23

Date: 2026-08-05

Status: AER-0027 corrected through the completed Sol recovery lease; no
incident remains open

## C5 worker rejection and recovered implementation readiness

Revision 23 adds no incident and erases no failure. AER-0027 remains the
rejected DeepSeek V4 Flash/high C5 implementation self-pass whose authored tests
did not expose fourteen material authority, admission, port-handoff, process,
readback, rollback, cleanup, shared-state, schema, correction and accounting
defects.

Sol adopted that commit only as untrusted source under the named recovery lease.
The recovered descendant binds approval, frame, candidate, proofreader,
provider admission, inherited port, executable, target artifact and command
material; enforces canonical stopped-process evidence and full approval
revalidation; shares provider, issuance, idempotency and attempt state; transfers
one parent-held loopback socket to the child; verifies fresh process plus HTTP
readback; rolls back every post-launch failure; and conditions cleanup success
on every owned-resource absence proof. Direct regressions cover every reproduced
finding.

The first fresh Gemini 3.6 Flash/high veto at exact clean HEAD
`792f6e06a3c91a6684af0b0127c65660fd690b67` reconfirmed all fourteen controls
but correctly required revision because the acceptance artifact embedded
worktree-specific absolute paths and ambiguously labelled the plan baseline as
`source_head`. Sol preserved that veto, projected validated runtime paths into
portable repository-relative evidence, separated `plan_source_head` from the
canonical candidate artifact-set binding and added an alternate-worktree-root
regression.

A genuinely fresh project then reviewed exact clean corrected HEAD
`d82de54ba59071d231adbf45a3aae1bbc0642ff4`. The committed acceptance check
passed from the different worktree, the inherited suite passed 269 tests, Ruff
and Bandit passed, every artifact hash reproduced, one terminal `pass` was
returned and the worktree remained unchanged.

Revision 23 therefore marks AER-0027 corrected through
`recovery_lease_applied` without relabelling the failed worker candidate. The
register contains 27 bounded incidents—21 agent-behaviour observations, three
harness failures, one repository defect and two transport timeouts—with none
open. The exact full-composite
`implementer.self_pass_with_material_acceptance_gaps` recurrence remains the
two C4 worker incidents; AER-0027 is a distinct C5 resource-composite singleton.
There are three incidents carrying the higher-level signature text, but the
register deliberately generates recurrence only from the full origin/category/
role/resource/signature composite. Neither count is evidence of provider or
model causation.

This closes provider-free implementation readiness only. It does not claim that
the C5 target, socket, port, temporary directory or candidate-runtime provider
was exercised; those remain behind the distinct pre-execution gate.
