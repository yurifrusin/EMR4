# Raisa AES-C4 bounded occupied authored-synthetic provider proof closeout

Date: 2026-08-11

Result: `raisa_agent_execution_surface_containment_gate_aes_c4_bounded_occupied_provider_proof_pass`

Exact independently reviewed source HEAD:
`e569da0a9081117b799e9437d8b7025230e2162b`

Evidence label:
`occupied_authored_synthetic_brokered_provider_containment_proof`

## Accepted result

AES-C4 passes the narrow occupied descendant of the accepted AES-C0/C1/C2/C3
chain. One newly authored-synthetic request crossed the exact external broker
boundary to Google Vertex AI `gemini-2.5-flash` through project and quota
project `bernie-emr4-dev`, keyless impersonated service-account ADC for
`emr4-bernie-ai-dev@bernie-emr4-dev.iam.gserviceaccount.com`, and the
`australia-southeast1-aiplatform.googleapis.com` Sydney hostname. No retry,
repair call, redirect, fallback, tool, function, grounding, retrieval or code
execution occurred.

The broker rechecked the exact immutable generation, manifest/grant/lease,
current authority, revocation and cumulative budgets before dispatch. It
reserved the sole call and USD 0.25 ceiling before network I/O. The provider
returned HTTP 200 with `STOP` after 1,726 ms. Safe usage was 363 prompt tokens,
40 candidate tokens, 139 thought tokens and 542 total tokens. At the current
published Standard rates, even charging response plus reasoning output is
approximately USD 0.00056, well below the frozen application ceiling.

The deterministic proofreader admitted exactly:

- `decision_code: contained`;
- the exact AES-C4 synthetic nonce;
- `summary_code: broker_boundary_confirmed`; and
- `command_authority: false`.

No raw prompt, response, provider text, reasoning, token, credential or
exception is retained. The one-call ledger is consumed, retries consumed are
zero, every lease/alias/token is revoked, no provider listener/process or task
temporary root remains, and all further generation calls are disabled.

## Preexecution and independent gates

The first provider-free implementation source passed a fresh Gemini 3.6
Flash/high veto. Immediately before occupied execution, current Google pages
showed the model as GA, listed `australia-southeast1` for availability and ML
processing, recorded retirement on 2026-10-20, and gave Standard pricing as
USD 0.30/M input plus USD 2.50/M text response and reasoning output.

The fact-rebound candidate then passed a distinct fresh exact-HEAD Gemini 3.6
Flash/high veto in clean worktree `r186`: 368 serial tests, Ruff, Python
compilation and restricted Git whitespace all passed, and the worktree remained
unchanged. The exact reviewed source subsequently passed the sanitized
read-only cloud/ADC preflight: project, quota project, impersonated identity,
scope, billing, API, service-account state, prediction-only permission, Vertex
Data Access audit, logging/cache posture, zero user-managed keys, current
publisher catalogue, Sydney endpoint and no fallback all matched. The separate
gcloud CLI impersonation check also passed without printing or persisting a
token.

## Exact artifacts

- provider envelope SHA-256:
  `baa22577ca29b09a783765bb5e5893a7d907f98578bf4b1fbc9d6cce24670393`;
- implementation SHA-256:
  `751f7234817ef73fb46f5631a7c08907daa1e17399c55faba2ba6bdfdba06c0e`;
- live preflight SHA-256:
  `5fabb7299e9a99538078367efacb1f69830e5eef7df56ca7b0ff35c168cb8c26`;
- occupied evidence SHA-256:
  `a24f239a50b0d32739424bcd22796f3252526a7d6b3f8a7217bd22ad3cb03c23`;
- consumed occupied ledger SHA-256:
  `6687b68d39af7fcfdd366d8e5f5ace1078e22326fd7abc173d8fd84584663ea3`;
- exact response digest:
  `sha256:9436fd06c7f35f57ce2cf85ae2936b8b07d44837447e1d5947c60721672a7a08`;
  and
- exact four-field release digest:
  `sha256:dd0559ca8e566f34d0cff7f19777ee3549e5b3818ac11b0f1ba83436f64e487d`.

## Issue exposed and resolved

Before the second exact-head veto or any inference, mandatory rehydration found
that the first factual-rebind zero-call pair carried a non-existent full source
string copied from restored summary state. Both files made zero calls but could
not serve as evidence. They are preserved under explicit `invalid-source`
filenames and recorded as AER-0261. Sol captured the actual HEAD with
`git rev-parse`, verified it with `git cat-file`, regenerated a complete
zero-call pair against that same value and required the fresh exact-head veto.
No invalid evidence supplied review, cloud or occupied authority.

## API Spine and authority boundary

This is one internal Access AI/provider invocation plus minimized audit
evidence. It adds no route, provider tool, product entitlement, Context Fabric
read, database/source adapter or command path. GraphQL remained read-only and
was not called. Events remained signals and were not consumed. REST/OpenAPI
mutations remain separately authorized, current-state checked, human/policy
gated, idempotent, audited and deterministically read back.

Passing proves only one exact broker-admitted authored-synthetic request and
one deterministic four-field release under this immutable generation. The
Sydney hostname and catalogue observations prove the configured and observed
locational request path, not Australian physical or sovereign processing. The
result does not prove general model safety, semantic prompt-injection
detection, product/patient-data safety, concurrency, reusable broker/runtime
safety, command safety, deployment or production readiness.

No continuing provider call is authorised. Patient, clinical, product-derived,
practice, licensed and external-corpus data; product read; database/source;
filesystem capability; executable/provider tool; generic network; command or
write; reusable runtime; IAM or credential mutation; deployment; production;
release; Pages; and protected-ref movement remain closed.

## Next fork

AES-C5 is the named next descendant, but it is the first product-runtime
admission. It requires a separately frozen privacy, identity, retention and
product-data authority plus a choice of exactly one real source and one
purpose. Those data and purpose choices are not inferable from AES-C4's
authored-synthetic envelope, and AES-C4 creates no automatic runtime authority.
AES-C4 therefore closes successfully at a genuine Yuri-attention fork; no
AES-C5 product read or provider call has started.
