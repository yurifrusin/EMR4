# Ariadne agent error and correction register — revision 618

Date: 2026-08-22

Timestamp: 2026-08-22T16:25:00.0826136+10:00 (Australia/Brisbane)

<!-- ariadne-agent-error-register-reading
revision: 618
incident_count: 963
new_incident_ids: AER-0961,AER-0962,AER-0963
open_incident_count: 0
-->

This revision adds three bounded, corrected orchestration-metadata observations
from the provider-free real edit coordinate diagnostic. Neither changed the
accepted candidate, invoked a worker/model/provider, prepared a clockwork
generation or moved a canonical surface.

## AER-0961 — precommit Git evidence prose supplied a manual object identity

The first implementation precommit runtime state repeated the protected Git
identity in authored `git_refs_and_worktree` prose. The local receipt builder
rejected it before staging or commit. The rejected receipt is preserved. The
correction removed all authored object identities from Git prose and let the
receipt's typed machine snapshot supply them; the fresh receipt passed.

## AER-0962 — incident closeout omitted its required human revision artifact

The first closeout dry-run supplied incident observations but did not include
the prospective register revision note in Baton acceptance. The clockwork
rejected `tick_incident_revision_path` before generation preparation or any
canonical mutation. This revision supplies the exact prospective reading and a
focused test now validates the full typed intent before its candidate commit.

## AER-0963 — closeout invented an unindexed Current Baton label

The second closeout dry-run supplied a descriptive acceptance label that was
not in the current Baton compaction manifest's allowlisted active labels. The
clockwork rejected `tick_baton_compaction_unindexed` before generation
preparation or canonical mutation. The correction selects the existing
`Current DeepSeek native Harness acceptance` label from the manifest rather
than creating another row.

## Register reading

All three failures are evidence that the clockwork is catching metadata construction
errors, but they also show the remaining efficiency cost of authoring its input
by hand. The durable direction is to generate Git prose and prospective register
revision bindings from typed selectors, leaving the orchestrator to choose the
semantic disposition rather than reproduce form mechanics.
