# Raisa authored-synthetic native-Harness corrected-guard-graph first useful-development recovery rehearsal plan

Date: 2026-08-22

Timestamp: 2026-08-22T21:44:49.4684132+10:00 (Australia/Brisbane)

Status: `frozen`

Operation: `raisa-authored-synthetic-native-harness-corrected-guard-graph-first-useful-development-recovery-rehearsal`

Reasoning level: `high`

## Objective

Give DeepSeek V4 Flash/high exactly one development-like authored-synthetic
repair through the pinned native Harness and the accepted corrected 14,210-byte
runner/guard graph. Measure useful output, exact traceability, correction cost
and cleanup. Release only one deterministically admitted and independently
reviewed candidate or one exact failed-closed terminal.

This is the first occupied successor after the provider-free corrected-runner
boot passed. It is not a retry or resume of any consumed attempt. It creates a
new attempt, work order, lease, disposable root, checkpoint and evidence
namespace. The consumed runbook attempts, the consumed earlier window-worker
attempt and the provider-free boot remain immutable.

## Why this work package

The frozen task is the unresolved authored-synthetic maintenance-window
coalescer repair used by the earlier integrated-runner rehearsal. That attempt
stopped at `factory` before a model or provider request, so DeepSeek never
received this package through that accepted path.

The task is narrow but not a box-filling form. One literal edit must make two
coupled semantic changes in one Python file: repair the nesting algorithm and
add the visible regression row. The exact candidate must then pass four public
behaviors and three controller-held behaviors. This tests whether the Harness
can govern a small code-and-test maintenance job without reopening generic boot
work, multi-file development or product authority.

## Exact authored-synthetic package

The disposable workspace contains exactly one tracked non-product file:

`synthetic_window_coalescer.py`

The baseline handles disjoint, overlapping and touching windows but shortens a
span when a later window is nested inside it. DeepSeek must make exactly one
direct top-level literal `edit` spanning both related changes:

1. replace the incoming end assignment with the maximum of the current and
   incoming ends and update the adjacent comment; and
2. append one visible `nested` regression row for `[(0, 10), (2, 3)]` with
   expected result `[(0, 10)]`.

The baseline, task text and expected bytes are reused byte-for-byte from
`scripts/raisa_authored_synthetic_check_in_native_harness_bounded_worker_monitored_development_rehearsal.py`.
The controller binds their exact SHA-256 digests and rejects any drift.

The candidate passes only when:

- changed paths equal exactly `synthetic_window_coalescer.py`;
- final bytes equal the frozen expected bytes;
- the visible script passes all four rows: disjoint, overlap, touching and
  nested;
- three controller-held cases pass, including multiple nesting/touching and
  negative-boundary combinations; and
- no disposable process, root, raw stream, environment, session or credential
  material remains.

The retained candidate is evidence only. It does not become EMR4 product
source or runtime code.

## Corrected runner binding

The execution runner, guard, bridge and sanitizer are obtained only through
`accepted_sources()` in the accepted corrected-guard-graph materializer. The
accepted inventory remains:

- runner: 14,210 bytes;
- guard: 4,501 bytes;
- bridge: 1,661 bytes; and
- sanitizer: 2,439 bytes.

The execution copy may replace only the runner's single frozen target-path
literal with the new disposable target. No lifecycle, edit-coordinate,
conclusion, provider, model, tool, request, preset-root, guard, hook, error or
terminal logic may change. Static transformation proof must show that restoring
the original target literal reproduces the accepted runner byte-for-byte.

The provider-free boot proof already established two distinct preset roots,
four `presets.roots` reads, five hook installations and
`EFFECTIVE_TOOL_COMPOSITION_PASSED` through the pinned stock-headless/HMR path.
The occupied controller must bind that exact accepted evidence and source
ancestry before launch; it does not recreate or broaden the boot proof.

## Occupied envelope

- provider/model: DeepSeek official / `deepseek-v4-flash`;
- reasoning effort: `high`;
- maximum output tokens: 4,096;
- maximum native Harness processes, process-local agents, sessions and turns:
  one each;
- maximum DeepSeek provider requests: one;
- maximum model-requested tool calls and parallel tool calls: one each;
- effective exposed tools: exactly `edit`, `glob`, `read`;
- admitted requested tool: exactly one direct literal `edit` on the absolute
  disposable target, with non-empty unequal strings and no `replace_all`;
- `read` and `glob` remain composition-visible but are not admitted for this
  one-call task because the full baseline is present in the frozen prompt;
- upstream ceiling: 300 seconds;
- native-process ceiling: 420 seconds;
- automatic retry, manual retry, resume, fallback, auxiliary model inside the
  attempt and Claude Code fallback: zero; and
- financial ceiling: Yuri's prepaid DeepSeek balance with no broker currency
  claim, no top-up authority and no second request.

The broker may start and complete no more than one provider request and must
reject every later request. The attempt identity is consumed immediately before
native launch. Every occupied terminal is final.

## Typed terminal and candidate admission

The terminal must retain only closed fields and digests:

- HMR readiness order and native-process count;
- accepted runner/guard inventory digests and target-rebinding digest;
- runner stage, request count, tool names/results, turn kind, conclusion and
  typed tool-lifecycle/edit-result coordinates;
- broker started/completed/failed/rejected counts;
- changed-path inventory, exact-byte result and 4/4 public plus 3/3 holdback
  counts;
- retry/resume/fallback/auxiliary-model counts; and
- process, broker, root and raw-artifact cleanup readings.

Raw prompt, response, reasoning, tool arguments, tool error text, stdout,
stderr, session, environment and credential values are destroyed. Only byte
counts and hashes of discarded streams may remain.

Exact terminal classes are:

- `useful_worker_pass`: one completed request, one admitted successful edit,
  completed turn, exact expected bytes, 4/4 public and 3/3 holdback passage,
  complete cleanup and later independent review pass;
- `useful_worker_candidate_rejected`: a request or edit completed but the
  changed path, bytes or behaviors failed deterministic admission;
- `useful_worker_transport_terminal`: the one consumed process ended before an
  admissible candidate; or
- `useful_worker_preexecution_rejected`: no occupied authority was consumed
  because a deterministic binding or preparation gate failed.

No plausible diff, completed provider response or passing Harness exit alone is
accepted as useful output.

## Serial custody and review

1. Freeze and commit this plan, threat delta, contract, schemas, controller and
   focused hostile tests.
2. Provider-free checks bind the accepted runner graph, exact target-only
   derivation, baseline/expected bytes, prompt, broker grammar, work-order
   schema, terminal reducer, cleanup and zero occupied counts.
3. Commit and origin-align the exact candidate source. Generate a machine-
   resolved full-object preexecution receipt and one clockwork checkpoint.
4. Prepare one disjoint root and work order without starting Harness, broker,
   model or provider activity.
5. Consume the new attempt, then launch exactly one native Harness process and
   at most one DeepSeek request. Never retry or resume.
6. Terminate owned processes, remove the exact root and interpret only the
   typed terminal plus any exact retained candidate.
7. If no candidate is admitted, Gemini remains uninvoked. If a candidate is
   admitted, GPT Sol first performs exact-byte, changed-path and seven-case
   readback, then one fresh Gemini 3.7 Flash/high read-only veto from an exact
   isolated candidate worktree. Gemini has no implementation or acceptance
   authority and no fallback model is permitted.
8. GPT Sol alone accepts or rejects the result and closes transactionally
   through the governance clockwork.

## Parallelism efficacy assessment

- DeepSeek V4 Flash/high: `planned`, positive leverage. It owns exactly the
  one authored-synthetic repair after all provider-free and checkpoint gates.
- Gemini 3.7 Flash/high: `reserved`, required independence only after an exact
  candidate passes deterministic admission; it owns a serial read-only veto.
- Native subagents: `declined`, negative leverage because developer policy
  prohibits proactive delegation and the identity-bound one-process custody
  chain is serial.
- GPT Sol owns meaning, controls, monitoring, deterministic admission,
  acceptance, Git and closeout.
- Parallel work packages: none. Planning, admission, DeepSeek, cleanup,
  candidate readback and possible Gemini review occur in that order.

Reassess at deterministic preexecution, occupied terminal, pre-verifier and
closeout.

## Workflow efficacy reading

The closeout must separately report:

- useful candidate present or absent;
- provider requests, native processes, wall-clock time and retries;
- tool and typed-coordinate counts;
- prelaunch local correction count;
- postterminal correction count;
- independent-review cost if invoked;
- cleanup; and
- whether accepted useful output justified the orchestration burden.

The first closeout-precommit receipt after the accepted boot is preserved as a
pending register observation: it manually repeated a protected Git object ID,
was rejected before commit or dispatch, and the corrected receipt passed with
machine-snapshot-only Git evidence. It must receive the next clockwork-owned
register allocation; it cannot be hidden or treated as a model failure.

## Stop rule and next decision

A pass makes the native Harness eligible for one separately frozen low-risk
unmounted EMR4 repository work package. It does not establish general or
multi-file reliability.

Any occupied non-pass ends this native-Harness development-recovery sequence.
There is no further automatic provider-free diagnosis, occupied recovery,
retry, resume or Claude fallback. The Harness returns to unavailable for EMR4
worker allocation; Raisa development continues under other already-authorised
resources unless Yuri later chooses a genuinely new Harness direction. This
prevents the recovery programme from turning into another widening boot circle.

A preexecution rejection may be corrected only before lease consumption and
only when the correction is mechanical within this frozen plan. Every such
correction is counted.

## Historical selection

The live test manifest preserves the documented deselection of
`tests/test_deepseek_native_harness_provider_free_edit_coordinate_integrated_runner_stock_headless_boot_rehearsal_plan.py::test_active_latch_is_the_exact_in_progress_operation`.
That immutable node correctly freezes its predecessor latch. Every other
substantive accepted runner, guard, boot, clockwork, latch and Baton test remains
selected.

## Protected boundaries

- one authored-synthetic non-product file only;
- no ordinary-practice enablement, feature-flag or allowlist change, command
  mounting, generic-status `Arrived` change, action grammar, first-party client
  or waiting-area movement;
- no product source, configuration, API, database, route or adapter change;
- no product, patient, appointment, clinical, historical or protected data;
- no live product database, Docker or production runtime;
- no deployment, release, Pages, protected evidence or protected-ref movement;
- local/origin `master` and `handoff/current` remain exactly
  `2e34bdad732fdab32fbf778280b3d3c70d66d602`;
- preserve `docs/branding/` and every unrelated untracked file; and
- stage explicit paths only, never `git add .` or `git add -A`.
