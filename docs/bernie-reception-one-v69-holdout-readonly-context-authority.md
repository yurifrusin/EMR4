# Reception One v6.9 Holdout and Read-only Context Authority

Recorded: 2026-07-30
Decision owner: Yuri
Status: authorised conditional sequence

Yuri authorised uninterrupted work through these four steps unless an
unexpected condition makes intervention necessary:

1. synchronise the completed v6.8 closeout into Continuity, Compass, the
   rendered Compass, AGENTS.md and programme ledgers;
2. freeze the v6.8 prompt, schemas, typed operator language, deterministic
   proofreader and generation settings;
3. author, seal and evaluate a genuinely untouched authored-synthetic
   holdout without tuning any frozen component against its result; and
4. only if the holdout passes, connect the frozen lane to a minimal,
   typed, source-labelled, freshness-bound and read-only authored-synthetic
   Diary context frame.

The exact occupied lane remains Google Vertex AI `gemini-2.5-flash`, project
`bernie-emr4-dev`, the exact Bernie service account through existing keyless
impersonated ADC, `australia-southeast1` and
`australia-southeast1-aiplatform.googleapis.com`, with no API key, static key,
global endpoint, provider or regional fallback, tools, grounding, retrieval
or cache creation. Each new occupied descendant must be revision-bound before
its first call, stay below USD 1 and use at most one primary plus one bounded
proofreader correction per sealed holdout case.

The context integration may use only deliberately authored-synthetic data in
a disposable local development practice. The trusted backend constructs the
frame and owns authentication, practice scope, opaque handles, freshness,
availability and all adapters. The model receives no database session,
credential, raw UUID, broad Diary, free note, clinical field or command
authority. The proofreader may release only a proposal or clarification; no
confirmation or write is authorised.

Still closed: real or product-derived data, patient or health information,
historical Diary material, appointment confirmation or mutation, Word, voice,
representative sessions, production, deployment and release.

Stop for Yuri if continued progress requires a different provider, model,
project, identity, credential mechanism, region, residency posture, external
IAM/API/billing change, real data, write authority, weakened isolation or
audit, cost above USD 1 for a descendant, credential exposure, or a material
user-visible authority decision.
