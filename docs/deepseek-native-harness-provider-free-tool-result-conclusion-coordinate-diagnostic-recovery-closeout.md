# DeepSeek native Harness provider-free tool-result/conclusion coordinate diagnostic recovery closeout

Date: 2026-08-22

Timestamp: 2026-08-22T14:31:55.5258598+10:00 (Australia/Brisbane)

Status: accepted

Result: `native_harness_tool_result_conclusion_coordinates_pass`

Reviewed source: `d9a5ca40328c336857a44e5b28f94b991f61d269`

## Outcome

The provider-free diagnostic resolved the ambiguity left by the consumed
useful-worker attempt. It bound the exact installed
`@deepseek-ai/dsh-tools` rc.7 package and type sources, machine-resolved the
accepted runner's full Git commit, derived one future runner by exact
replacements, and executed five authored-synthetic lifecycle variants through
rc.7's real `ToolRuntime` in one local Node process.

The decisive lifecycle fact is now mechanical: rc.7 snapshots a successful
result's `concludesTurn` marker during result creation. A marker requested only
after the post-execute waterfall returns is too late for an unchanged accepted
result. The future runner therefore requests conclusion after the exact root
edit pre-execute boundary accepts and before dispatch. It observes the input
result, post-execute decision and authoritative `tools/result` separately.

## Closed coordinates

- accepted successful edit with the pre-execute marker:
  `edit_success_accept_concluded`;
- accepted successful edit with the old late marker:
  `edit_success_accept_late_marker`;
- tool-body error: `edit_error_accept_not_concluded`;
- post-execute block: `edit_success_blocked_not_concluded`; and
- post-execute decision failure:
  `post_execute_decision_failed_not_concluded`.

Unknown keys, unknown vocabulary, contradictory states and mismatched
coordinates fail closed. The final `tools/result` observation agreed with the
returned rc.7 result in every fixture execution.

## Verification and efficacy

- all 24 focused implementation, schema, mutation, plan, receipt and latch
  tests passed;
- the broader serial Harness-lineage, Git-resolution, orchestrator, error
  register, clockwork, latch, Baton and API Spine suite passed;
- Ruff, formatting, compilation and provider-free controller preflight passed;
- the exact consumed occupied terminal remains byte-identical at SHA-256
  `2c66b7de93b25579347d0e9199437fdc8c7ebf5d21f6d59dc2f73dda602970a5`;
- native worker, model, provider, broker, network, database, Docker, retry,
  resume and fallback counts are zero; and
- the exact disposable fixture root and every owned process are absent.

This retires the specific ambiguity that previously made a no-change edit
result untraceable. It does not prove that DeepSeek will produce a valid edit,
that the future runner will be useful in an occupied call, or that the Harness
is generally reliable.

Three low-cost fail-closed corrections were retained. The post-compaction receipt
builder rejected a manually repeated Git object in prose before action; the
corrected receipt used only the machine snapshot. The first real-runtime
fixture attempt derived the Cordis peer path one directory too high and emitted
`fixture_output_rejected`; the path relation was corrected, the rejected
terminal was preserved, and the complete real-runtime fixture then passed. The
first closeout check found that shallow manifest validation had admitted path
strings where Continuity requires contract-evidence objects; the form was
corrected and the object-only rule was moved into initial manifest validation
with a regression test, before projection or publication.

## Parallelism outcome

- DeepSeek V4 Flash/high: declined because this diagnostic authorised no
  worker or provider request.
- Gemini 3.7 Flash/high: declined again at the pre-verifier gate because the
  exact-package real-runtime fixture and mutation suite left no material
  semantic ambiguity for an external model to resolve.
- Native subagents: declined under current developer policy and the serial
  source-bind/derive/execute/release chain.
- GPT Sol: owned the plan, source diagnosis, implementation, deterministic
  review, correction and closeout.

## Closed boundaries and successor

No product source, configuration, API/OpenAPI/GraphQL schema, route, migration,
database, ordinary-practice enablement, feature flag, allowlist, generic-status
`Arrived`, action grammar, first-party client, waiting-area movement,
product/patient/appointment/clinical/historical/protected data, production
runtime, deployment, release, Pages or protected ref changed.

The dependency-satisfied successor is
`raisa-native-harness-bounded-occupied-useful-worker-coordinate-recovery-rehearsal`.
It may freeze one separately named one-request, zero-retry DeepSeek V4
Flash/high native-Harness attempt against the same closed default-off runbook
candidate, using the accepted typed runner. It is not a retry or resume of the
consumed identity. Candidate admission, independent review and every product
or protected boundary remain fail closed.
